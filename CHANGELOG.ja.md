フォームごとに何が変わるか――アプリケーションの視点から書かれており、
内部の仕組みの視点ではありません。*何から*作られているかを知りたい方は
[LIZENZEN.md](LIZENZEN.md) を参照してください。ここには、日々の作業にとって
何が変わるかが書かれています。

番号の付け方は一般的な数え方に従います。**最初**の数字は、これまでと同じ
ように動作しなくなったときに変わり、**2番目**は新しい機能が加わったとき、
**3番目**は不具合の修正のときに変わります。

---

## 0.10.52-alpha.20260903 – 2026年9月3日

- 4件を超えるファイルの一括処理が、複数のプレビューウィンドウで
  確認を済ませた後も停止しなくなりました。後続の文書も引き続き
  バックグラウンドで準備されます。プレビューへの回答後は、対応する
  ファイルが確実に最後まで処理され、一括処理の次の枠が空くように
  なりました。
- PDF 内の短い企業ロゴの照合は、認識された画像テキストだけを対象とする
  ようになりました。そのため通常のページテキストでは、複数行にまたがる
  長いモデル検出によって、別の場所にある同じ単語まで追加で黒塗りされる
  ことはなくなりました。

## 0.10.51-alpha.20260903 – 2026年9月3日

- 変更履歴が、自分の言語で表示されるようになりました。
  maskuro.com/neuigkeitenと、プログラム内の「変更」、さらに更新後の
  「新機能」で表示されます。これまでは18の言語版すべてで、翻訳された
  見出しの下にドイツ語の本文が表示されていました。翻訳がまだない場合、
  そのバージョンは消えずにドイツ語のまま表示されます。バージョン一覧は
  どの言語でも同じです。

## 0.10.50-alpha.20260903 – 2026年9月3日

- PDF内で繰り返し登場する企業ロゴが、一貫して浄化されるようになりました。
  文字認識がページごとにロゴの文字を異なって読み取る場合や、丸い意匠を
  まったく読み落とす場合も同様です。プレビューでの明示的な選択解除は
  その後も有効であり続け、後から自動更新されても取り消されることは
  ありません。
- スキャンされた表内の通貨単位のない価格が、表の見出しと値が異なる
  重なり合ったPDF画像に分かれて存在していても、現在は完全に黒塗り
  されます。数量、時間、重量、パーセントはそのまま残ります。離れた
  位置にある数字同士が、誤って1つの金額として結合されることもなく
  なりました。
- 署名検索は現在、裏付けのある薄い青色の筆跡や、細い赤色の署名略号も
  検出します。点線のグラフ、測定曲線、スタンプ、ロゴ、幅の広い赤色の
  編集マークは、この対象を絞った追跡から引き続き除外されます。
- 回転、反転、せん断、または切り取られたPDF画像内の黒塗りが、現在は
  実際の画像多角形を正確に対象とします。作業項目内の技術的な役割語、
  車両・タイヤの専門用語、技術的な「補償」は、同時に誤検出に対して
  より厳密に区別されます。明示的に見出しの付いた連絡先の役割や電話
  番号は引き続き保護されます。
- PDFを保存する前の目視確認で、ウィンドウがもうフリーズしなくなり
  ました。該当箇所の多い大きな文書では、これまで数秒間何の反応もあり
  ませんでしたが、現在は確認中であることを示す案内が表示され、
  ウィンドウは描画を続けます。
- 修正エディタで画像から値を復元する処理は、現在は各原本画像を文字
  認識で一度しか読み取りません。これまでは、同じ画像に対する復元の
  たびに毎回文字認識が再実行されていました。
- 上位段階と署名モデルの再取得に必要なメモリが、大幅に減りました。
  596MBのパッケージはこれまで完全にメモリ上に保持され、検証・展開
  されていました――実行中のプログラムでは1GB超えのピークとなり、
  8GBのパソコンでは処理全体が止まり始める瞬間でした。現在はブロック
  単位でディスクに流し込まれ、そこで検証・展開されます。
- 修正エディタでの検索が、大きなPDFをもうフリーズさせなくなりました。
  検索欄への最初の1文字が、これまですべてのページを一度に読み込んで
  いました――200ページの場合、ウィンドウは2秒間停止し、黒塗りのたびに
  さらに同じことが起きていました。現在はページが少しずつ読み込まれ
  ます。それまではカウンターに「読み込み中…」と表示され、結果は同じ
  です。
- ラスタライズされたPDFページ――文字認識後、またはテキストがきれいに
  削除できなかった場合――は、現在は画質を落とすことなく、大幅に小さく
  保存されます。常にJPEGとして保存する代わりに、各ページは可逆圧縮
  でもエンコードされ、より小さい方のフッションがファイルに組み込まれ
  ます。浄化されたスキャンはこれにより248KBから48KBに、文字認識付き
  練習用文書は913KBから702KBに縮小します。テキストはくっきりと鮮明な
  ままです。
- 再取得されたモデル(上位段階、署名、顔、第二の文字認識)は、10分間
  浄化処理がないと、メモリから解放されるようになりました。これまでは
  プログラム終了まで読み込まれたままでした――一度でも署名検索と上位
  段階を使った人は、常時2ギガバイト超を保持していました。次回の処理
  では1~2秒で再度読み込まれます。ステータスバーがそれを伝えます。
- PowerPoint:スライドレイアウトやスライドマスターの総称名
  (「Leer(空)」「Titelfolie(タイトルスライド)」)は、もう項目として
  置換されません。「Leer」は地名でもあり、あらゆるドイツ語・英語の
  プレゼンテーションで誤って黒塗りされていました。現在浄化されるのは、
  手動で付けられたスライド自体の名前だけです。
- PDF内で、行の平滑化処理が次の行の先頭をもう発見箇所に巻き込まなく
  なりました。日付の後ろにある次のリスト項目の番号が電話番号として
  扱われ、「Kenncode」や「Auftragsnummer」のような見出しが数字の後ろに
  あると郵便番号と地名として扱われ、住所の下にある地名行が地名を重複
  させていました。それぞれ正しい、より短い発見結果がこれによって押し
  のけられていました。132件のコーパスPDFにわたって、24件の追加平滑化
  発見のうち本物の2件が保持されます。実務コーパスでは、誤検出が発見率
  を保ったまま29件から21件に減少します。
- 修正エディタの「PDFフォルダを検索して黒塗り」が、もうウィンドウを
  ブロックしなくなりました。処理はバックグラウンドで動作し、進捗表示と
  中止ボタンは反応し、メニューやタブが、処理途中の半端なファイルの
  最中に操作されてしまうこともなくなりました。
- 発見箇所のあるスキャンされたページは、黒塗りの際に現在は2回ではなく
  1回だけ再書き込みされます。これまではプログラムが発見箇所の矩形と
  根拠の矩形を2回の処理で埋めており、2回目の処理が、直前に新しく保存
  されたスキャン画像をさらにもう一度圧縮していました。これにより、
  大きなスキャンでの時間と画質劣化の両方が節約されます。
- 修正エディタでのページ送り、ズーム、縮小表示の反応が速くなりました。
  レンダリングされた各ページは、これまで表示されるためだけにPNG
  圧縮を経てすぐに展開されていました――高解像度の画面では、ページ
  ごとに約10分の1秒です。現在は画像が直接届き、画素単位で同一です。
- PDF保存前の目視確認(「出力確認」)は、同じ結果のまま約3倍高速に
  なりました。
- メインウィンドウの表示が、さらに約4分の1秒早くなりました。文字
  認識がこのパソコンで利用可能かどうかの確認は、これまでウィンドウの
  構築時に行われており――Macではシステム認識への試験リクエストも
  含まれていました――さらに追加コンポーネントの設定ページが、全48
  言語の状態を問い合わせていました。両方とも現在はバックグラウンドで、
  または言語一覧が実際に開かれた時点で初めて行われます。それまでは
  「文字認識を確認中…」と表示されます。
- 署名検索の後、プログラムが使用するメモリが約300MB減りました。認識
  モデルはこれまでメモリ上に二重に存在していました――一度は真正性の
  検証用、もう一度は計算用です。検証は引き続き行われますが、2つ目の
  コピーなしで行われます。
- PDF内の文字認識が、体感できるほど速くなりました。ページ内の各見出し
  (「Geburtsdatum:」「Steuernummer:」)ごとに、これまでは項目種類ごとに
  独自の照会を認識処理に送っていました――同じ見出しがすでに10ページ
  前にあった場合でも、各ページで新たに行われていました。現在は応答が
  記憶されます。2ページの仕様書ではこれにより324件の照会が発生して
  いましたが、現在は異なるものだけになります。発見結果は同じです。
- 大きな表が、再び分単位ではなく秒単位で浄化されるようになりました。
  匿名化処理――初期設定です――では、すでに判明している値の照合が、
  セルが増えるたびに遅くなっていました。ヒットのたびにキャッシュが
  破棄され再構築されていたためです。5,000セルには約18秒かかっていた
  ものが、現在は0.5秒です。結果は文字単位で同一です。
- メインウィンドウの表示が、さらに大幅に速くなりました。設定の国一覧
  は、ウィンドウの構築時に検出ライブラリ全体を前面に読み込んでいま
  した――Macで約0.7秒、Windowsではそれ以上です――そのために必要なのは
  国名だけであるにもかかわらずです。この一覧は現在、軽量なカタログ
  から取得されます。ライブラリはウィンドウがすでに表示された状態で、
  想定どおりバックグラウンドで読み込まれます。これは、プログラムを
  再起動させる言語や外観の切り替え後にも適用されます。
- 文書実験室は現在、切れた見出し、局所的な値の影、強いスキャンの
  切り取りを、PDF・DOCX・ODTのコンテナ全体で完全に検証します。この
  マトリクスは40の文書系統と17のコンテナ軸から成る680件のファイル
  を含みます。Maskuroは、新規および完全な基本・特徴プロファイルの
  すべてで、測定された誤検出も破損した保持対象も中断もなく、すべての
  目標項目を削除します。

- 複数回使用されたスキャンが、現在はすべての目に見える配置箇所を対象
  に検証・浄化されます。文書実験室は、PDF内の異なるページ・サイズ・
  回転にまたがって同じ画像オブジェクトを共有し、DOCXやODT内で同じ
  画像部分を複数回参照します。「Formularscan klein quer(小型横向き
  フォームスキャン)」のような技術的なODT枠の名前は、もう人物として
  扱われません。似た先頭を持つ自由な氏名や地名は引き続き保護されます。
  最終的なPDFページ処理の一般的なフォーム推測が、すでに独立して読み
  取られた画像領域上で大きな住所の誤検出を生成することもなくなり
  ました。120件の新規コンテナは、基本・特徴プロファイルでそれぞれ
  813件・840件の目標項目すべてに、誤検出、保持対象の破損、中断なしで
  到達します。完全な800ファイルの特徴受け入れ検査は5,600/5,600を
  確認します。

- ドイツ語のOCR実験室は、現在40の文書系統から成る560件のスキャンを
  含みます。新しいバリエーションは見出しやページの端を切り取ったり、
  値の直上に影を落としたりします。Maskuroはこの際、氏名、住所、生年
  月日、医療上の識別コード、一部が破損した見出しを持つ識別番号も保護
  します。同時に、フォームフィールドの残骸、公式な見出し、事務的な
  法律・情報用語は、もう人物や地名として置換されません。完全な基本・
  特徴プロファイルは、測定された誤検出や中断なしで、それぞれ
  3,794/3,794件、3,920/3,920件の目標項目に到達します。

- PDFの自動画像選択は、大判の商品写真、エネルギーラベル、肖像写真の
  列を、単にページ上部の端から始まっているという理由だけではもう削除
  しません。実際に平坦なヘッダー・フッター画像や、用紙の端から始まる
  レターヘッドは引き続き対象になります。従業員名簿では、目に見える
  文書タイトルが画像としてしか存在しない場合でも、構造的に繰り返され
  る項目から氏名が検出されるようになりました。検出は、もう2つの
  具体的な役割語と略号「DW」だけに限定されていません。1~4個の折り
  返された役割語、および「Durchwahl」「Nebenstelle」「Ext.」
  「Extension」が、共通の構造から推測されます。役割語や見出しは、
  言語モデルが重なり解消処理の後に役割を表す形容詞1語しか残さない
  場合でも、そのまま残ります。横方向の役割の格子は、もう誤って氏名の
  列として扱われません。ページOCRが複数のカードを1つの極端に幅広い
  内部大文字表記の単語に結合してしまう場合、近接するローカルな照合が
  実際の単語矩形を分離します。これにより、単独の氏名も幅広の誤バーも
  残らなくなります。繰り返される複数行の企業ロゴは、すでに確認済みの
  同一画素テンプレートに基づいて、使えるOCRテキストのないページでも、
  最大2画素の位置ずれがあっても黒塗りされます。同時に、短いローカルな
  OCR再読み取りが、より大きな見出し領域を架空の氏名として補完すること
  もなくなりました。企業のレターヘッドの前にあるページ番号は、もう
  組織名の一部として扱われません。数字で始まる本物のブランド名は引き
  続き保護されます。複数の測定された製品・専門・フォーム用語は、もう
  人物として提案されません。

- PDFにおける署名検索は、OCR画像浄化の後に実行されるようになり、
  通常のテキストヒットのないページも訪れ、回転したページの発見矩形を
  正しく文書空間に逆変換します。密集した商品写真は、もう署名として
  黒塗りされません。明確に見出しの付いた署名欄では、狭い線分の
  フォールバックがモデルの薄い隙間を埋めます。事前印刷された日付の
  ある空の線は、この処理を引き起こしません。OCR・署名の発見のみから
  成る純粋なスキャンは、このフェーズでは、テキスト経路で初めて読み
  込まれる画像黒塗り機のせいで中断することがなくなりました。

- 多数同時に開かれた文書が、修正エディタで区別できるようになりました。
  タブが単なる省略記号だけに縮小されることはなくなり、右側の一覧
  ボタンがすべての完全なファイル名を縦に並べて表示します。タブは
  ドラッグで並べ替えができ、メインウィンドウと同じ一覧から×印で
  取り除けます。未保存の作業は引き続き最初に確認されます。右クリック
  では、「閉じる」「他のタブを閉じる」「右側のタブを閉じる」も提供
  されます。

- ウイルス対策ソフトや検索インデックスによる一時的なWindowsのロック
  が、完全に読み込まれた言語モデルまたは辞書フォルダを、最終的な
  配置の際に「アクセスが拒否されました」で失敗させることがなくなり
  ました。Maskuroは現在、この最後のフォルダ移動を短時間再試行します。

- ドイツ語の文書実験室は現在、変化するPDFページ回転、独立して回転
  されたPDF画像、DOCXおよびODT内の拡大縮小・切り取りされた表画像に
  対してもコンテナを検証します。目に見えて回転した画像内のフィールド
  値が再び完全に検出され、技術的な列の識別子はもう地名として置換
  されず、共通の姓を持つ氏名が整合性の後処理によって重複した部分
  ヒットに分解されることもなくなりました。320ファイルに倍増した
  マトリクスは、日付・金額・医療検出を有効にした状態で、測定された
  誤検出や中断なしに2,240/2,240件の目標項目に到達します。

- 複数ページの画像PDF、テキストと画像が混在するPDF、DOCXやODTに
  埋め込まれたスキャンが、現在は40のドイツ語文書系統すべてにわたる
  独自の160ファイルの実験室で検証されます。技術的なODT枠の名前や
  見出し付きの機器コードは、もう地名として置換されません。同じ構造
  内の本物の氏名、地名、住所は引き続き保護されます。医療または金額
  検出を有効にすると、直後に続く用量や支払い間隔も完全に削除される
  ようになりました。コンテナ、テキスト基本、テキスト特徴、OCR特徴の
  各処理は、測定された誤検出や中断なしに、それぞれの完全な状態に到達
  します。

- 保存前の安全性検査は、現在は目立つPDF箇所を個別に選択可能な一覧
  として表示します。「エディタで確認」は、選択したページを正確に開き、
  その範囲を強調表示します。同じ箇所の重なり合う部分ヒットは、現在は
  一度だけ表示されます。新しい操作テキストは、翻訳された全17の表示
  言語すべてに完全に用意されています。

- Markdownファイルは、置換時にリンク・強調表示・脚注の構文を保持
  します。Maskuroはこのために、Markdownの記号を除いた文字数の等しい
  フッションを読み取ります。メールアドレス内のアンダースコア、計算式の
  アスタリスク、個人情報を含まない通常のリンクは変更されません。

- 同じPDFページ上の複数の手書き記入が、現在は最大3回の処理で検索
  されます。すでに発見された筆跡は作業用画像内でのみ非表示にされ、
  それによってより弱い署名が押しのけられることはありません。回転した
  ページでも、黒塗り領域は再び目に見える発見箇所に配置されます。以前
  の安全フェーズによる画像の塗りつぶしは、その後の書き戻し時にも
  保持されます。

- 「すべての設定をリセット」が、現在は「画像内のテキスト」も対象と
  します。OCRコンポーネントが利用できない場合、このスイッチは技術的
  にはオフのままですが、出荷時状態から誤って外れているとは表示され
  なくなりました。

- ページ上部にある大きな画像の断片は、単にその位置だけを理由にもう
  ヘッダーとして扱われません。これにより、特に画像ベースの商品説明や
  表の内容が保持されます。新たに検出された、種類が正確に一致するメール
  やフォームの発見結果は、すでに検査済みの画像領域上でも、最終的な
  目視確認からもう除外されなくなりました。

- 空調・電気設備の見積書内の技術的な明細・品目行が、人物・地名・組織
  とより厳密に区別されるようになりました。これにはケーブルの種類、
  AC電源、明細番号、大文字の製品コードなどが含まれます。本物の氏名や
  住所は引き続き保護されます。

- 実際の浄化済みPDFの検査は、`1 699,59`のような価格の構成要素をもう
  電話番号と誤認せず、`08.05.2025`のような完全な日付から見せかけの
  カード情報を切り出すこともなくなりました。敬称の後ろの氏名は、次の
  通り名の中にではなく、改行の位置で終わるようになりました。添付
  ファイル名内の地名は、実際の地名に限定されます。車両の色、技術的な
  状態値、業種名、製品の法人格も同様に保持されます。`|PLLZ`のような
  破損したプレースホルダーの読み取り結果は、2回目のOCR処理で再び個人
  情報として扱われることがなくなりました。

- 横向きに保存されたPDF画像は、最終的な目視確認において、変更されて
  いない画像の向きでの追加チェックを受けるようになりました。この
  チェックは、Maskuroが同じページですでに確実に検出した値のみを追加で
  黒塗りできます。これにより、例えば小さく回転した住所印がすべて
  覆われる一方、画像の見出しや技術図面から新しい単語を個人情報として
  作り出してしまうこともありません。

- OpenDocumentのテキスト文書では、注記(コメント)の作成者イニシャル
  が、現在は作成者名と一緒に消去されます。LibreOfficeはこれをフル
  ネームの隣に独自の略称として格納し、まさにこれをページ余白に表示
  します。これまでは「SO」がそのまま残っている一方で、隣の
  「Sieglinde Ortner」はすでにプレースホルダーになっていました。消去
  されるのは、作成者が実際に置換された場合のみです――部署名の注記は、
  その表記をそのまま保持します。

- イタリア語のビジネスレターでは、文頭の定型表現がもう氏名や地名として
  扱われなくなりました。「Restiamo a disposizione」「Rimaniamo」
  「Attendiamo」「Alleghiamo」「Comunichiamo」「Auguriamo buon
  lavoro」は、これまで見せかけの人物や地名情報として引っかかっていま
  した。同じ位置にある本物の氏名(「Rossi Mario」)は引き続き認識され
  ます。

- 2段組みのスキャンは、文字認識がまずすべての見出しを、その後すべての
  値を出力する場合でも、現在は見出し付きの識別番号や地名情報を保護
  します。対応関係は目に見える画素の行に従い、90度回転したページでも
  機能します。パスポートや契約番号の近接して分かれた部分は、まとめて
  黒塗りされます。見出し付きの生年月日、ICDおよびPZNコードも同様に
  対象になり、後続の専門用語はそのまま残ります。短い氏名やユーザー名は
  正確なフィールドで保護されます。複数のOCR単語に分解されたメール
  アドレスは、近接していて完全なメールの文法を満たす場合にのみ対象と
  なります。フィールドに紐づいた紛らわしい文字の補正と、まだ空の人物
  フィールドのローカルな再読み取りが、専門的なフィールドやすでに判明
  している値を拡張することなく、破損・回転したスキャンをカバーします。
  安全マージンは単語の大きさに従い、特徴プロファイルは直接隣接する
  用量単位や支払い間隔も対象に含みます。わずかに斜めに給紙された
  フォームは、複数の同方向のOCR行から幾何学的に逆投影されます。丸め
  誤差や矛盾する証拠だけでは不十分です。短い文字接頭辞は、ハイフン
  付き識別番号の前でそのまま保持され、完全な見出し付き住所の発見結果
  は、それと同種の通り名部分ヒットのみを置換します。誤読された役割
  フィールドの見出しは、少なくとも3つの既知の見出しが存在するフォーム
  列でのみ削除されます。チャット上の名前は保護されます。わずかな端の
  切り取りと、斜めの光の反射を伴う局所的な露出過多が、画像マトリクス
  に追加されました。複数のフォーム行にまたがる人物・地名・企業の発見
  結果は、複数回使われているフィールド列では、それぞれの値のみに
  限定されます。技術的な明細値は、明細見出しと一致する形式が揃った
  場合にのみ対象になります。本物の氏名は保護されます。光の反射で
  途切れたメールの値も、明示的なメールの見出しの後ろ、近接に限定
  された狭い画像範囲内であれば削除されます。同じ目に見える行にある
  2つのフィールド・値のペアは、現在は独立して評価されます。より低い
  ベースライン上の値は、3つの一致する幾何学的証拠がそろった場合にのみ
  結合されます。これにより、識別番号、生年月日、住所は、密集した
  フォームレイアウトでも完全に保護されたままです。通り名、郵便番号、
  地名は、同じ住所フィールド内かつ適切な住所文法に一致する場合にのみ
  結合されます。作業・補助器具や歯の状態に関する厳密に区切られた専門
  フィールドは、もう地名や一覧の誤検出を生じません。本物の氏名や似た
  名前のフィールドは引き続き保護されます。ドイツ語の文書実験室は現在
  440件のスキャンを含み、基本プロファイルで2,981/2,981、特徴プロ
  ファイルで3,080/3,080に到達します。11種類の画像変化と40の文書系統
  すべてが100パーセントに達しており、測定された誤検出、保持対象の
  破損、中断は引き続きありません。

- セル区切りが失われたPDFテキストレイヤーは、現在は繰り返される
  フィールド・値の構造を基準に、組織・住所・地名の発見結果を限定
  します。企業の値の前にある見出しや、`=>`や`->`のような技術的な矢印は、
  もうヒットに含まれません。柔軟な改行(ソフト改行)向けの追加表示が、
  法人格や地名の発見結果を複数の表の行にまたがって拡張してしまうこと
  もなくなりました。すでに完全な住所は、次の見出しと値の手前で終わり
  ます。TXT・HTML・PDF・DOCXの全1,600文書にわたる最終処理は、誤検出
  ゼロ、保持対象の破損ゼロ、中断ゼロで10,840/10,840件の目標項目を
  削除します。

## 0.10.44-beta.1 – 2026年9月1日

- パッケージのビルドは、Windows x64・ARM64、Apple SiliconおよびIntel搭載のmacOS、Linux x64・ARM64向けにそれぞれ別個の出力を生成する。パッケージ名、アップデートの選択、リリースはアーキテクチャごとに区別され、6つのターゲットのいずれか、またはその依存関係の証跡が欠けている限り、公開はロックされたままとなる。Linux ARM64はQtの都合上、glibc 2.39以上を前提とする。実機での動作確認が完全に済んでいるのは、今のところWindows x64とApple Silicon搭載macOSのみであり、その他のアーキテクチャ向けパッケージは、実運用ではなく検証目的の先行版であることを明示する。

- 複数ファイルを扱う際、プレビューの確認待ちの間も検出処理は継続するようになった。準備済みのプレビューは最大3件まで順に表示され、その間も演算が進むのは同時に1文書のみで、結果ファイルはプレビューでの承認後にのみ生成される。プレビューで選んだ恒久的な例外は、すでに準備済みの後続文書にも適用される。

- 校閲証明書は、ファイルメニューから墨消し済み文書に対していつでも直接検証できるようになった。Maskuroはその際、一致する署名済みファイル、一致するが未署名の証跡、無効な署名、証明書に対応しない文書を区別する。この照合にはライセンスも、元のオペレーティングシステムアカウントも不要である。
  自動検査用には`--zertifikat-pruefen`から同じ照合が利用できる。戻りコードは一致、操作エラー、無効な証跡を区別する。
  照合はさらに、埋め込まれたMaskuro-IDを証明書と比較する。任意に書き換えられた外部のIDは、未署名の証跡であっても、これによって判明する。
  署名が有効な場合、検証結果は管理設定で有効化された編集者を、オペレーティングシステムアカウント、技術的なアカウントID、プラットフォームとともに表示する。未署名または無効な証跡からの未確認情報は出力されない。

- 新しいドイツ語文書ラボは、10分野・4種の構造バリエーションから、完全に合成されたTXT・HTML・PDF・DOCX文書160件を生成する。マニフェストは、消去すべき情報と、保持すべき専門文書内容ないし業務上の識別番号とを明確に区別するようになった。文書ファミリー、変異形、公開の構造由来は追跡可能な形で記録されている。

- ドイツ語文書ラボは、280ファイル、7種の構造形式、1,540件の正解情報、1,036件の保持アンカーへと拡張された。番号付きフォーム、括弧付きのPDF/マスクフィールド、技術的な`=>`対応関係が新たに検証対象となった。拡張後の全件では、TXT・HTML・PDF・DOCXそれぞれで誤検知ゼロ、検出率100パーセントに達している。括弧付きの日付・識別番号フィールド、矢印区切り、明示的にラベル付けされた組織名も構造的に検出されるようになった。

- 2度目のラボ拡張により、文書数は400件、構造形式は10種、正解情報は2,200件、保持アンカーは1,480件に達した。JSON風のキー値、YAMLリスト、大文字のフォームフィールドは、既存分と合わせて誤検知ゼロ・検出率100パーセントを達成した。引用された生年月日や識別番号、被保険者・応募者・申告義務者・代理権者といった明示的にラベル付けされた役割も、これらのエクスポート形式で検出されるようになった。

- ドイツ語文書ラボの独立したOCRモードは、40の文書ファミリーすべてから純粋な画像スキャン200件をさらに生成する。鮮明なもの、コントラストの低いもの、低解像度のもの、JPEGアーティファクトを含むもの、90度回転したページが、正確なピクセル座標枠で再測定される。従来の1,600ファイルによるテキスト基準値との比較可能性は保たれている。マニフェストは、切り替え可能な日付・金額・医療特性を基本プロファイルから分離し、OCR読み取り済みの箇所を、追加の正解箇所として数えることなく把握する。測定結果は変異形と文書ファミリーごとに内訳される。狭いフィールド境界により、例えば地名「Graz」に含まれる`Az`が後続の日付を事件番号として誤って墨消ししてしまうことなどが防がれる。現行の基本マトリクスは誤検知ゼロ・中断ゼロで動作する。

- 請求書/納品書、銀行/融資、賃貸/不動産管理、学校/大学、物流/通関という5つの新たなドイツ語文書ファミリーにより、ラボは600ファイル、3,520件の正解情報、2,360件の保持アンカーへと拡張された。テキストレイヤーがセル区切りを失う場合に備え、明示的なヘッダー`Feld Angabe`を用いる狭いPDF表対応の経路が追加され、新しい`--familien`選択肢により部分測定が高速化された。新規200ファイルは誤検知ゼロ・中断ゼロで1,320/1,320を達成している。

- 保険/損害、労働/賃金、医療/検査、車両/整備、技術/保守により、ドイツ語文書ラボは800ファイル、4,960件の正解情報、3,200件の保持アンカーへと拡張された。狭くラベル付けされた保険証券番号、患者番号、検査官番号、車両識別番号のほか、新たな役割・住所・組織フィールドが検出される。新しい部分マトリクスと完全マトリクスは、TXT・HTML・PDF・DOCXすべてで誤検知ゼロ・中断ゼロの100パーセントを達成している。

- 建設/入札、エネルギー/環境、団体/協会、コミュニケーション/カレンダー、ホテル/イベントにより、ドイツ語文書ラボは1,200ファイル、7,920件の正解情報、4,800件の保持アンカーへと引き上げられた。新しい役割・企業・住所・登記・入札・予約・ユーザーアカウントのフィールドは、すべてのエクスポート形式でも検出される。メーター番号は業務上の識別番号として保持される。部分マトリクスと完全マトリクスはともに誤検知ゼロ・中断ゼロの100パーセントを達成している。

- 飲食/宅配、薬局/処方箋、葬儀/墓地、スポーツ/会員、不動産/仲介により、ドイツ語文書ラボは1,400ファイル、9,360件の正解情報、5,640件の保持アンカーへと拡張された。新しい人物の役割、住所フィールド、捜索依頼番号が検出される。法人形態を伴ってラベル付けされた企業名は、自動改行をまたいでも完全に保護され続ける。年齢区分や専門職名が誤って置換されることもなくなった。部分マトリクスと完全マトリクスはともに誤検知ゼロ・中断ゼロの100パーセントを達成している。

- 歯科治療、自動車教習所、消防/出動、エネルギー共同体、パッケージ旅行により、ドイツ語文書ラボは1,600ファイル、10,840件の正解情報、6,440件の保持アンカーへと拡張された。新しい役割、住所フィールド、治療・教育・出動・エネルギー・旅行契約の識別番号が構造的に検出される。新しい200ファイルの部分マトリクスは1,480/1,480を、完全マトリクスは10,840/10,840を達成した。いずれも誤検知ゼロ・中断ゼロを維持している。

- 文書ラボの完全測定では、厳密な公的な業務形式と構造規則により、不要な置換が68件から0件に、明示的に測定された保持違反が23件から0件に、中断が3件から0件に減少した。同時に検出率は91.1パーセントから100.0パーセントに上昇し、TXT・HTML・PDF・DOCXはそれぞれ100パーセントを達成した。`Feld`のような一般的な表ヘッダーは、実際に確認されている`Feld`/`Angabe`という並びでのみ抑制され、同名の姓は保護されたままとなる。末尾に文字を伴う裁判所の事件番号、イコール記号のフィールド、`Geburtsdatum des Kindes`(子の生年月日)、および同一行内の複数のラベル付き個人名は完全に検出される。Wordの表と前行フィールドは、そのフィールドヘッダーを一時的な検出コンテキストとして利用する。ラベル付けされたPDFの住所は、文構造上の改行をまたいでも完全に保護され続ける。

- ドイツ語の個人属性・職業・医療フィールドは、Windows改行コードでも機能するようになった。`Geschlecht`/`w`のような1文字の性別表記は、前行形式でも保護される。事実を示す`Artikel-PZN`フィールドは、薬品コードとしても人物としても検出をトリガーしない一方、実際のPZN・ICD・ATCの表記は引き続き検出される。

- ドイツ語のフォーム・番号フィールドの精度が向上した。「DW.」はソフト改行の前でも機能するようになり、明示的にラベル付けされた名前は小文字表記でも削除されるようになり、純粋に数字のみの事件番号は正しい識別番号種別に割り当てられるようになった。逆に、偶然Luhnアルゴリズムで有効な請求書・伝票・商品番号は、もはやクレジットカード番号とはみなされない。合成HTML・PDF出力サンプルにより、完成文書内での削除と保持の両方が確認されている。
  識別番号とユーザー名は、そのラベルが直前の表またはフォームの行にある場合にも検出されるようになった。この形式でも、業務上の伝票番号は引き続き見える状態を保つ。

- パスワードは、単独のフィールド見出しが前の行にある場合にも検出されるようになった。末尾の`!`や`#`のような特殊文字は、保護される値の一部として完全に含まれる。逆に、商品・記事のPINはカードPINとしてマスクされなくなった。明示的な`PIN`・`Karten-PIN`フィールドは引き続き保護される。

- 小文字で書かれたフォーム値は、明確なドイツ語の住所フィールドおよび`PLZ/Ort`フィールドの場合、単なる一般的な地名ではなく、住所ないし郵便番号+地名として出力されるようになった。同様に、「beispiel service」のような小文字表記の企業名も、企業フィールドの後ろで完全に保護されるようになり、末尾の単語が次のフィールド見出しと誤認されて切り捨てられることもなくなった。

- ヘルプ、FAQ、プライバシーポリシー、ウェブサイトは、由来証明について一貫して説明するようになった。文書内の中立的なMaskuro-ID、実際のオペレーティングシステムアカウントへの紐付けは örtlich(ローカル)な検証記録内でのみ任意に行われること、Windows/macOS/Linuxでのユーザー切り替え、そしてSHA-256と署名の意味するところである。

- 画像ベースの技術的な入札仕様書は、より慎重に処理されるようになった。「Abbruchhämmern」「Deckungsrücklass」「Positionsnummern」「Einbauplatine」「Terminsituation」といった明確な一般用語や、単語の途中で分断されたOCR由来の表記は、もはや人物名や地名とはみなされない。ある実際の自治体案件では、新たな誤検知を生むことなく、明確な置換件数が140件から90件に減少した。Schneider、Lang、Bauer、Hahnといった姓は引き続き明示的に保護される。

- 実際の案件書類から得られたさらなる誤検知が修正された。「Digital signiert」(デジタル署名済み)にはもはや疑似的な人物名が含まれず、BICはラベルの後ろでコロンなしでも検出され、`15000 Alternativ`は郵便番号+地名とはみなされず、EU引用「(VO (EG) 715/2007」も組織名を生成しなくなった。ある太陽光発電案件では、置換件数が26件から16件に減少した一方、実際の氏名、地名、口座情報は保持された。

- 従業員一覧では、代理を表す略語「Stv.」と、単独で切り離された「FACILITY」という部門見出しが、もはや人物名として置換されなくなった。実際の13ページに及ぶ照合資料では、置換件数が878件から875件に減少した。氏名、内線番号、企業名は引き続き保護された。

- 処理済みのPDF、OpenDocument、Officeファイルは、文書プロパティに中立的な`MASKURO-…`識別子を持つようになった。検証レポートと署名付き検証記録は、同じ識別子と、元ファイル・結果ファイル双方のSHA-256値を保持する。校閲証明書は完成したファイルから識別子を引き継ぐ。ユーザー名は、管理設定が既存のユーザーフィールドを明示的に有効化した場合にのみ追加される。

- メインウィンドウと設定画面は、より落ち着いた構成になった。保存、コピー、詳細、指標、検出プロファイルの削除は、該当する操作が実際に可能になった時点で初めて表示される。技術的なOCR言語コードや長い例文は、常時ワークエリアに表示されるのではなく、必要に応じてヒントテキストに表示されるようになった。検出ページは、説明文が途切れたり横スクロールバーが出たりすることなく、より狭いウィンドウにも適応するようになった。置換リストの平文に対する警告は引き続き表示される。

- 検出範囲は、さらなるドイツ語および国際的な連絡先の事例に対応した。電話番号は、選択可能なすべての国・地域について検証されるようになり、ハンガリー語およびクロアチア語の契約上の役割は職業名と同形の姓も完全に捕捉するようになり、番号付きの部品/資材リストは「Mutter / Flach」による人物誤検知をもはや引き起こさない。明らかに数字を含む事実上の値を持つ人物フィールドは、氏名として取り込まれない。また、機械可読旅券領域(MRZ)は「識別番号」グループからまとめて有効/無効を切り替えられるようになった。

- 法人形態のない企業名は、あいまいな雇用主フィールドの後ろで人物名とより適切に区別されるようになった。「Huber Handel」「Müller Logistik」「Kowalski Handel」といった名前は完全に企業として捕捉される一方、「Arbeitgeber: Bauer Anna」は引き続き人物名として扱われる。自動的な国選択は、フランス語文書に対して引き続きルクセンブルクを含むフランス語圏全体を考慮する。

- 画像内で検出された署名や個人情報を含むテキストは、これまで常に黒い矩形で覆われていた。墨消しに別の色や「Regenbogen(虹色)」のような模様が設定されていた場合でも同様だった。これらの画像領域は、選択された墨消し表現を引き継ぐようになった。不透明な塗りつぶし面は、引き続きピクセルデータに直接書き込まれる。

- 英語の検出は、手作業で翻訳された11件の実文書に基づいて再測定され、的を絞って改善された。在庫状況、技術的な見積り・ウェブショップのフィールド、従業員名簿内の役割は保持されるようになり、「CV」はテンプレート文内で法人形態として読まれなくなり、引用されたフォント名は保持されるようになり、縦書きの履歴書ヘッダー、複数ページの従業員リスト、「Account manager」の後ろ、数字で始まる企業名の氏名は完全に検出されるようになった。オーストリアの商業登記番号は英語ラベルの後ろでも機能するようになり、略称「Customer:」、EAR登録番号、事業者番号もその値を保持する。寸法連鎖、ケーブル種別、EU法令参照、見積り有効期限、履行地、裁判管轄地、登記裁判所、税制略語「NoVA」、タイヤラベル内の技術番号、および「OVE R6-2」「AStV」といった規格参照はもはや誤検知を生じない。ラベル付きの有効なIBANは、登録フィールドや次行の見出しの手前で正しく終わるようになった。産業団地の付記を含む住所は、Windows改行を含むPDFテキストストリームからも完全に検出されるようになった。英語の企業名の導入部と構造化された貯蓄銀行名も完全に区切られるようになった。元文書の国は、郵便番号や国別識別番号の言語版においても保持される。

- 宛先およびメッセージヘッダーでは、言語モデルがカンマ区切りリストの最初の2つの名前を1つの検出結果に結合してしまうことがあった(「Bcc: Huber, Mayer」)。両方の名前は、「Sent:」「Reply:」「Fwd:」の後ろでも、それぞれ個別に検出・置換され、レポートに記録されるようになった。

- パスポートまたは身分証明書の機械可読領域(MRZ)は、「何を検索するか」のグループ制御に含まれていなかった。現在は「識別番号」グループに属し、このグループとまとめて有効/無効を切り替えられる。

- 置換テキストのテンプレートとして「Regenbogen(虹色)」を選ぶと、墨消し箇所も同じ表現になるようになった。これまでは意外にも従来通り黒いままだった。墨消し面はその後も引き続き、別のテンプレートに独立して切り替え可能である。

- 修正エディタのページパネルは、保存済みのウィンドウ配置を復元した後、幅を手動で変更するまで空のままになることがあった。サムネイルは、可視のウィンドウ構築後に再配置されるようになり、すぐにパネル中央に表示される。

- PDF内の置換テキスト周囲のカラー検証マークは、カテゴリ色や信号色によってはほとんど見えなくなっていた。明るい下地の輪郭が、検証枠を色付きプレースホルダーおよびページ背景から確実に区別するようになった。

- 修正エディタで、行間が狭く組まれた文書(見積書や仕様書に典型的)の行を墨消しすると、下の行の上伸部にはみ出すバーが生成され、その行が半分読めなくなっていた。バーは今では隣接行の実際に描画された文字までで止まる。墨消しされた行自体は、下伸部を含めて引き続き完全に覆われる。

- 練習用文書(「ヘルプ → 練習用文書を開く」、ツアーガイドでも表示)は、あらゆる検出種別を実演するようになった。架空の手紙には、認識可能な顔写真、手書きの署名、職業と部署、診断名と薬品名が新たに加わった。企業名、金額、日付はすでに含まれていた。初期設定が意図的に残す部分は、それを解除するスイッチとともにシート自体が説明する。写真の顔は初期設定でピクセル化される。

- 金額をドイツ語で一般的な表記(数字の後ろに記号、例:「1.240,00 €」)で書いた場合、「金額も削除する」スイッチではこれまで一度も検出されなかった――「1.240,00 EUR」や「€ 1.240,00」は以前から検出できていた。現在は3種類すべての表記が検出される。

- 署名検索は、単独の画像ファイルでも機能するようになった。JPGやPNGとしてスキャンを処理すると、手書きの署名が墨消しされる――PDFと同じ検出、同じレポート表示である。Office文書に埋め込まれた画像は、その検出が測定上不安定であるため、引き続き検索対象外である。そのため、このチェック項目は「PDFおよび画像ファイル: 手書き署名を墨消しする」という名称になった。

- 行間が狭い場合、墨消しバーが下の行の上伸部に目に見える形ではみ出し、半分読めなくすることがあった――バーの高さは実際に紙面に描かれているものではなく、フォントメトリクスから導かれていたためである。バーは今では、修正エディタでも自動処理でも、隣接行の実際に描かれたインクまでで止まる。自分自身の行は下伸部を含め常に完全に覆われる。行同士が本当に重なっている場合、バーは何かを露出させるよりも隣接行に留まる方を選ぶ。

- 名前の下に役職が記載された従業員名簿で、女性の管理職名(「Anna Berger」の下に「Montageleiterin(組立主任)」)が氏名の置換に巻き込まれていた――隣の男性形は正しく残っていた。「…leiterin」という女性形(Montage-、Team-、Projekt-、Bau-、Abteilungs-、Betriebs-、Gruppen-、Amtsleiterin)は、男性形の対応語と同じく職務名称として扱われるようになった。Filial-、Personal-、Vertriebsleitungも両方の形式で新たに対象となった。

- 切り替え可能な職業検出は、「Projektleiterin」「Teamleiterin」「Abteilungsleiterin」のような女性の管理職名を検出できず、男性形は検出できていた。現在は両方の形式が同等に扱われる。

- プレビューウィンドウでは、Mac上で複数件表記が語に直接くっついていた(「Anna Musterfrau2」のように、「Anna Musterfrau 2」ではなく)。間隔は元に戻った。

- 比較ルーペにズームスライダーの横に新しいボタンが追加された。一度押すと結果全体の幅いっぱいに重なる――上下半分ずつ、そして原本は文書と同じ拡大率で表示される(その際ルーペのズームは100パーセントに切り替わる)。もう一度押すと左列の小さな表示に戻り、以前のルーペのズームが復元される。隣の丸いアイコンはズームのリセットのみを行う――これまではウィンドウの再ドッキングも行うという誤った説明があった。

- 修正エディタのツールバーでは、選択中のツールが選択状態であることが再びわかるようになった。アクティブなツールのボタンは青い枠付きの塗りつぶし面を持つ――比較ルーペや学習モードなど、ツールバーの他の有効化されたトグルボタンも同様である。このマーキングは、8月29日の独自のボタンデザインとともに失われていた。

- 仕様書の位置番号(「2.3.3.3、2.3.3.4、2.3.3.5」と縦に並んだもの)がIPアドレスとみなされ、結果から削除されていた。末尾が年に似た3階層番号(「2.3.19、2.3.20」)は日付として検出されていた。行頭の昇順番号列は、今では本来の姿――位置リスト――として扱われる。実際の住所(技術的な語彙を伴うネットワーク表、99を超える数字)と実際の日付は引き続き検出される。

- 「Müller」「Fischer」「Bauer」「Koch」「Wagner」「Schneider」「Weber」「Jäger」「Schmied」「Becker」「Schuster」「Schäfer」「Meister」のような姓は、「姓、名」形式のリスト(例:「Teilnehmer: Müller, Peter; Nowak, Anna」)では、同時に一般的な職業名でもあるため平文のまま残っていた。今では確実に検出される。

- PDFの墨消しの際、狭い表セル内でバーがセル全体を持っていってしまうことがあった。仕様書内の「D-LINK」というヒットから、プレビューではヒットのみが示されていたにもかかわらず、隣の製品説明全体が削除されていた。バーは引き続き住所ブロック全体の行やフィールド見出しを覆うが、保護対象の分量を超えて無関係な部分を巻き込むことは、多くともその分量までとなった――ヒットの隣にある説明は今では残る。

- 修正エディタで「表示をリセット」した後、ページパネルが空のままになっていた――ページのサムネイルは、ウィンドウを閉じて再度開いた後にのみ表示されていた。今ではリセット直後にも、以前と同様に中央に表示される。

- 修正エディタに4番目のツールが追加された。**削除**は、枠の下のテキストを代替なしで取り除く――墨消し(バー)でも置換(プレースホルダー)でもなく、隙間が目に見える形で空のまま残る。単語単位で動作し、下に画像がある場合はその地色が白く消去され、「原本を復元」は代替なしの削除も元に戻す。専用のツールバーアイコンと十字線バッジ(十字)、18言語すべてに専用のショートカットキー(ドイツ語ではF、entFernenのF)。

- PDF検索バーでは、「フォルダ …」がオプションの右側に配置されるようになった。墨消しに加えてヒットの置換機能が追加されて以来、通常のウィンドウ幅では5つのボタンが並ばなくなり、最初のボタンが圧縮されテキストが切れていた。

- 「すべての設定をリセット」は、「赤/緑を他の色に置き換える」のチェックもリセットするようになり、初期出荷状態と異なる場合は他の項目と同様に「変更済み」として記録される。

- PDF内の置換テキストは、より均一に見えるようになった。フルサイズのプレースホルダーが、その行に対して明らかに小さくなりすぎる場合(例えば「[BEG16]」が「Das」のような短い単語に押し込まれる場合)、代わりに行サイズの短縮形(「[B16]」)が表示される――極小表示より読みやすく、復元用の番号は両方の表記を保持する。プレースホルダーが極小になるのは、最短の表記でも収まらない場合のみとなった――それでも、情報を何も伝えないバーよりは良い。

- PDF内の多色(グラデーションまたは虹色)の置換テキストは、次の操作までしか無事ではなかった。同じページ上でのさらなる置換や墨消しが、すでに設定済みのプレースホルダーを判読不能な、圧縮された文字の山に押し込んでしまうことがあった――エディタで単語ごとに置換すると、「[BEG17]」の代わりに重なり合った文字しか見えないことがあった。一度設定されたプレースホルダーは、設定された通りの状態を維持するようになった。

- プレビューの恒久的な例外用のスイッチは「二度と削除しない」に名称変更された――登録先のリストと同じ名称である。これまでは「nie wieder(もう二度と)」と表示されていた。隣のヒット行はより整理された。情報アイコン「ⓘ」は大きくなりクリックしやすくなり、チェックボックス、置換マーク、ボタンは共通の高さになった。ヒット周辺の文はその告知された幅を実際に活用するようになった――これまでの幅指定は表示上黙って無視されており、表示は狭い帯として折り返されていた。

- エディタでは、マウスカーソルが今どのツールが有効かを示すようになった。狙いを定める十字線に加えて小さな記号――墨消しにはバー、置換には交換矢印、復元には元に戻す矢印、モザイクにはピクセルグリッド。従来の手のひらアイコンは廃止された――手のひらは他の箇所ではすべて「つかんで動かす」を意味するためである。手のひらには今、ふさわしい役割が与えられた。赤くハイライトされた単語やバーの上でカーソルは指差しの手に変わり、そこではクリック一つで済む。

- 「最大検出(AI)」は、ダウンロード可能なローカル言語モデルをもはや提供しない――このレベルは今後、「独自のAIを接続」で設定した独自のAIのみを使って演算する。すでに独自のサーバーを接続していたユーザーには違いはない。

- プレビューのガイド付きツアーは、ヒット周辺の文を表示する情報アイコン「ⓘ」についても説明するようになった。そしてこの文自体もより読みやすくなった。1段階大きなフォント、より広い行間、狭く密集した折り返しではなく固定幅である。
- 「ファイルを検証」「検出ルールと独自の用語」「テキストを処理」「画像を処理」にも独自のツアーが追加された――これら4つのウィンドウには独自のメニューバーがないため、新しいボタン「ウィンドウのツアー」から呼び出せる。
- 9つのウクライナ語の契約上の役割ラベルの下にある氏名は、ラベルが単独でその行に立っている場合、同形異義の姓では不完全にしか検出されなかった。「Покупець」/「Продавець」(買主/売主)、「Поручитель」/「Боржник」(保証人/主債務者)、「Свідок」(証人)、「Орендодавець」/「Орендар」(賃貸人/賃借人)、「Спадкодавець」/「Спадкоємець」(被相続人/相続人)。氏名は今では完全に検出される。

- Excelワークブックの名前付き範囲のコメント(名前の管理、「コメント」フィールド)に記入された氏名は、そのまま変更されずに保持されていた。今ではワークブックの他の内容と同様に処理される。

- 7つのハンガリー語の契約上の役割ラベルの下にある氏名は、同形異義の姓の場合まったく検出されなかった。「Bérbeadó」/「Bérlő」(賃貸人/賃借人)、「Vevő」/「Eladó」(買主/売主)、「Kezes」/「Főadós」(保証人/主債務者)、「Tanú」(証人)。氏名は今では完全に検出される。

- チェコ語の買主ラベル「Kupující」の下にある氏名は、同形異義の姓の場合まったく検出されなかった。氏名は今では完全に検出される。

- ロシア語の後見人ラベル「Опекун」の下にある氏名は、同形異義の姓の場合まったく検出されなかった。氏名は今では完全に検出される。

- さらに6つのクロアチア語のラベルの下にある氏名が検出されなかった。「Jamac」(保証人)、「Glavni dužnik」/「Dužnik」(主債務者/債務者)、「Ostavitelj」(被相続人)、「Nasljednik」(相続人)、「Vjerovnik」(債権者)。氏名は今では完全に検出される。

- `<object>`の`data`属性ではなく`<embed>`の`src`属性に埋め込まれたサブページを含む保存済みHTMLページは、その中の個人情報をそのまま変更せずに保持していた。今では`<object>`の場合と同様に処理される。

- 5つのデンマーク語の契約上の役割ラベルの下にある氏名は、ラベルにコロンが付いて氏名の前に立っている場合、同形異義の姓では不完全にしか検出されなかった。「Arvelader」/「Arving」(被相続人/相続人)、「Befuldmægtiget」/「Fuldmagtsgiver」(受任者/委任者)、「Værge」(後見人)。氏名は今では完全に検出される。念のため、対応するノルウェー語のラベルも追加された。

- Word・PowerPointファイル内のプレースホルダーは、選択された表現(単色、グラデーション、虹色、またはカテゴリ別)と同じ色を持つようになった――これまでは、PDFの結果がすでに色付きだったとしても、通常のテキスト色のままだった。

- 「テキストとしてコピー」と「Markdownとしてコピー」は、結果の平文テキストを直接クリップボードに配置する――チャット、メール、他のプログラムへの貼り付け用に、ファイルを開かずに済む。

- さらに5つのスロベニア語のラベルの下にある氏名が検出されなかった。「Toženec」(被告)、「Tožnik」(原告)、「Zastavitelj」(質権設定者)、「Zastavni upnik」(質権者)、「Darovalec」(贈与者)。氏名は今では完全に検出される。

- Wordの追跡された表セルの変更(挿入・削除・結合されたセル)の作成者名は、同じ名前がコメント作成者としてはすでに削除されていても、ファイル内に残っていた。今ではこちらも削除される。

- さらに9つのスロベニア語のラベルの下にある氏名が検出されなかった。「Najemodajalec」/「Najemnik」(賃貸人/賃借人)、「Zapustnik」/「Dedič」(被相続人/相続人)、「Upnik」/「Dolžnik」(債権者/債務者)、「Glavni dolžnik」(主債務者)、「Skrbnik」(後見人/世話人)。氏名は今では完全に検出される。

- 5つのスロベニア語のラベルの下にある氏名が検出されなかった。「Izvedenec」(鑑定人)、「Kupec」(買主)、「Prodajalec」(売主)、「Naročnik」(発注者)、「Izvajalec」(受注者)。氏名は今では完全に検出される。

- さらに5つのリトアニア語のラベルの下にある氏名が検出されなかった。「Užsakovas」(発注者)、「Vykdytojas」(受注者)、「Vežėjas」(運送人)、「Siuntėjas」(発送人)、「Arbitras」(仲裁人)。氏名は今では完全に検出される。

- さらに6つのリトアニア語のラベルの下にある氏名が検出されなかった。「Įgaliotinis」(受任者)、「Įgaliotojas」(委任者)、「Naudos gavėjas」(受益者、保険)、「Trečiasis asmuo」(民事訴訟における補助参加人/第三者)、「Ankstesnis nuomininkas」(前賃借人)、「Naujasis nuomininkas」(新賃借人)。氏名は今では完全に検出される。

- ODT文書内のブックマーク(`text:bookmark`)は、その名前を自由に、多くの場合参照先の場所にちなんで(例:「Herr_Mueller_Unterschrift」)付けている――読者には見えないが、ファイル内には文字通り存在する。この名前も処理されるようになった。

- さらに8つのリトアニア語のラベルの下にある氏名が検出されなかった。「Pareiškėjas」(申立人)、「Suinteresuotas asmuo」(非訟手続における相手方)、「Ekspertas」(鑑定人)、「Bankroto administratorius」(破産管財人)、「Valdybos narys」(監査役会メンバー)、「Direktorius」(取締役)、「Palikėjas」(被相続人)、「Įpėdinis」(相続人)。氏名は今では完全に検出される。

- さらに7つのリトアニア語のラベルの下にある氏名が検出されなかった。「Liudytojas」(証人)、「Vertėjas」(通訳/翻訳者)、「Notaras」(公証人)、「Dovanotojas」(贈与者)、「Apdovanotasis」(受贈者)、「Pirkėjas」(買主)、「Pardavėjas」(売主)。氏名は今では完全に検出される。

- さらに6つのリトアニア語のラベルの下にある氏名が検出されなかった。「Globėjas」(後見人/世話人)、「Palikimo administratorius」(遺産管理人)、「Laiduotojas」(保証人)、「Pagrindinis skolininkas」(主債務者)、「Nuomotojas」(賃貸人)、「Nuomininkas」(賃借人)。氏名は今では完全に検出される。

- リトアニア語のラベル「Ieškovas」/「Atsakovas」(原告/被告、訴訟当事者)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Vilkas」=狼)もそうでない場合も検出されなかった。氏名は今では完全に検出される。

- ODT文書内の人物索引項目(索引用のブックマーク)は、その氏名を独自のソートキーにも二重に記録していた――本文中では見えないが、後で生成される索引には文字通り記録される。このキーも処理されるようになった。

- PowerPointプレゼンテーションのスライド名とセクション名(選択範囲やスライド並べ替えで見える)は、スライドテキストではない要素の属性であったため、処理対象外だった。両方とも今では検出される。

- 「Petraitis-Kazlauskas」のようなリトアニア語のハイフン付き複合姓は、前に何らかの地の文があると後半部分を失っていた(文頭にある場合のみ完全な状態を保っていた)。姓は今では常に完全に検出される。

- クロアチア語のラベル「Cesionar」(債権譲渡における譲受人)の下にある氏名は、フィールドラベル自体が誤って人物名として読まれるため誤検知を起こしていた。ロシア語のラベル「Цессионарий」(同じく譲受人)の下にある氏名は逆にまったく検出されなかった。両方のケースが修正された。

- ドイツ語のラベル「Zedent」/「Zessionar」(債権譲渡における譲渡人/譲受人)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Bauer」)まったく検出されないまま残っていた。氏名は今では完全に検出される。

- ポーランド語のラベル「Darczyńca」/「Obdarowany」(贈与契約における贈与者/受贈者)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」=狼)検出されなかった。同様に、ルーマニア語のラベル「Donatar」(受贈者)は、一般的な姓の場合、それ自体が疑似的な氏名の一部として引っかかっていた。両方のケースが修正された。

- ポーランド語のラベル「Wierzyciel」/「Dłużnik」(強制執行における債権者/債務者、または一般的な債権者/債務者)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」=狼)検出されなかった。氏名は今では完全に検出される。

- ポーランド語のラベル「Poręczyciel」/「Dłużnik główny」(保証契約における保証人/主債務者)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」=狼)検出されなかった。氏名は今では完全に検出される。

- ポーランド語のラベル「Ubezpieczony」/「Ubezpieczający」(保険証券における被保険者/保険契約者)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」=狼)部分的または完全に検出されなかった。同様に、クロアチア語の「Osiguranik」/「Osiguravatelj」(同じ役割)の下にある氏名も、名前ごと完全に消えていた(例:「Golub」=鳩)。両方の氏名は今では完全に検出される。

- ポーランド語のラベル「Pełnomocnik」/「Mocodawca」(委任状における受任者/委任者)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」=狼)検出されなかった。同様に、クロアチア語の「Opunomoćenik」/「Opunomoćitelj」(同じ役割)の下にある氏名も、名前ごと完全に消えていた。両方の氏名は今では完全に検出される。

- ポーランド語のラベル「Pozwany」(訴訟当事者としての被告)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」=狼)検出されなかった。氏名は今では完全に検出される。

- クロアチア語のラベル「Najmoprimac」/「Najmodavac」(賃貸借契約における賃借人/賃貸人)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Kovač」=鍛冶屋)検出されなかった。氏名は今では完全に検出される。

- ポーランド語のラベル「Pracodawca」/「Pracownik」(労働契約における契約当事者としての雇用主/従業員)の下にある氏名は、姓が同時に一般的な単語である場合(例:「Krawiec」=仕立屋)部分的に検出されなかった。氏名は今では完全に検出される。

- ハンガリーは国別カタログに個人識別番号と付加価値税IDしか含まれていなかった。商業登記番号(Cégjegyzékszám)は、フィールド語「Cégjegyzékszám」または略語「Cg.」が直前にある場合に検出されるようになった――番号自体にはチェックディジットがない。

- エストニアは国別カタログにIsikukoodしか含まれていなかった。Käibemaksukohustuslase number(すべてのエストニアの請求書にある付加価値税ID)は、チェックディジット付きで検出されるようになった。

- ラトビアは国別カタログにPersonkodesしか含まれていなかった。PVN reģistrācijas numurs(すべてのラトビアの請求書にある法人識別番号)は、チェックディジット付きで検出されるようになった。

- 暗号化された内容を含むメール(S/MIMEまたはPGP/MIMEエンベロープ、`multipart/encrypted`)は、実際の内容が暗号化されていて検証されていないにもかかわらず、これまで何の警告もなく、あたかも完全に検証済みであるかのように出力されていた。このようなメールは今では、未検証の添付ファイルと同様にその旨を示す。

- マルタは国別カタログから欠落していた。マルタの付加価値税ID(VAT number)が検出されるようになった。

- ルクセンブルクは国別カタログから欠落していた。ルクセンブルクの付加価値税ID(n° TVA)が検出されるようになった。

- 文頭に置かれたブルガリア語の「Изчакайте」(「お待ちください!」)が地名として報告されていた――独自の言語モデルを持たないハンガリー語、ポーランド語、チェコ語などの命令形と同じモデルの限界による。この誤検知は今後発生しない。

- ポーランド語(「Zleceniodawca」「Zleceniobiorca」)、ルーマニア語(「Prestator」)、クロアチア語(「Naručitelj」または「Izvođač」)のラベルの下にある氏名は、姓が同時に一般的な単語である場合(例:「Wilk」「Vuk」=狼、「Vulpe」=狐、「Sokol」=鷹)部分的または完全に検出されなかった。氏名は今では完全に検出される。

- ポーランド語(「Nadawca」)、デンマーク語(「Afsender」)、スロベニア語(「Pošiljatelj」)のラベルの下にある氏名は、姓が同時に一般的な単語である場合(例:「Sowa」=フクロウ、「Bager」=パン職人、「Volk」=狼)部分的または完全に検出されなかった。氏名は今では完全に検出される。

- リトアニア語(「Gavėjas」)またはスロベニア語(「Prejemnik」)のラベルの下にある氏名は、姓が同時に一般的な単語である場合(例:「Vilkas」=狼)部分的または完全に検出されなかった。すでに対応済みの「Primatelj」(クロアチア語)や「Modtager」(デンマーク語)と同様、氏名は今では完全に検出される。

- 「To All Staff」や「To All Employees」のような回覧文の見出しが、誤って人物名として検出・削除されていた。この問題は今後発生しない。

- クロアチア語(「Primatelj」)またはデンマーク語(「Modtager」)のラベルの下にある氏名は、姓が同時に一般的な単語である場合(例:「Golub」=鳩、「Bager」=パン職人)部分的に検出されなかった。すでに対応済みの「Odbiorca」(ポーランド語)や「Destinatar」(ルーマニア語)と同様、氏名は今では完全に検出される。

- デンマーク語、ノルウェー語、ギリシャ語文書の署名欄にあるフルネームは、ラベル「Underskrift」または「Υπογραφή」が氏名の上に単独で置かれている場合、部分的にしか検出されなかった――ギリシャ語の場合、姓が氏名ではなく地名として検出されることさえあった。すでに対応済みの「Подпись」(ロシア語)と同様、氏名は今では完全に検出される。

- 横向きに置かれたスマートフォン写真(向き情報のみで正立表示される、通常の縦位置撮影)上のテキストは、これまで生の横倒しピクセルデータのまま読み取られていたため、文字認識で見逃されることがあった。このような写真は今では、読み取りの前に正しい向きに回転される――以前の顔検出と同様である。

- ロシア語、ウクライナ語、リトアニア語文書の署名欄にあるフルネームは、ラベル「Подпись」「Підпис」「Parašas」が氏名の上に単独で置かれている場合、部分的にしか検出されなかった――名または父称が欠落していた。すでに対応済みの「Potpis」(クロアチア語)と同様、氏名は今では完全に検出される。

- 横向きに置かれたスマートフォン写真(向き情報のみで正立表示される、通常の縦位置撮影)上の顔は、これまで生の横倒しピクセルデータのまま検査されていたため、顔検出で見逃されることがあった。このような写真は今では、検索の前に正しい向きに回転される。

- クロアチア語文書の署名欄にあるフルネームは、ラベル「Potpis」が氏名の上に単独で置かれているか、氏名の前にコロン付きで置かれている場合、部分的にしか検出されなかった――名が独立した行にあるか「Potpis: 名 ミドルネーム 姓」の形式かにかかわらず欠落していた。すでに対応済みの「Unterschrift」や「Signature」と同様、氏名は今では完全に検出される。

- 「verh.」(既婚)や「verw.」(死別)といった身分に関する略語の後ろにある婚姻後の姓は、これまで括弧内、カンマの後ろ、スペースなしで結合された表記(「Anna Meier (verh. Weber)」「Klaus Bauer (verw.Fischer)」)のいずれであっても、完全に未検出のまま残っていた――すでに対応済みの「geb.」と同様、今では確実に検出される。

- 支配人署名を表す「ppa.」の後ろにある氏名(例:業務用メールやビジネスレターの署名欄)は、「Bauer」や「Koch」のような職業名と同形の姓の場合、これまで部分的または完全に未検出のまま残っていた――すでに対応済みの「gez.」と同様、今では確実に検出される。

- ポーランドの身分証明書(dowód osobisty)の番号は、シリーズと番号の間にスペースがない場合(「ABS123456」)のみ検出されていた。しかし文書は実際にはこの形式では印刷されない――公式にはその間にスペースが入る(「ABS 123456」)。この表記では番号がこれまで未検出のまま残っていた。

- アニメーションPNG(APNG、例えばGIFの代わりにPNGとして保存された短い画面録画)は、これまで最初の1コマのみで検証・処理されており、その旨も報告されていなかった――以前のアニメーションWebPと同様、Maskuroは今では、それ以降の各コマが未検証のまま結果に残ることを報告する。

- アニメーションWebP画像(例えばスクリーンショットツールや、1ファイルに複数画像を含むチャットアプリケーション由来)は、これまで最初の1コマのみで検証・処理されており、その旨も報告されていなかった――以前の複数ページTIFFと同様、Maskuroは今では、それ以降の各コマが未検証のまま結果に残ることを報告する。

- ハイフン付きのスロベニア語複合名(「Ana-Marija Novak」)は、テキスト中で前に地の文が続くと前半部分を失っていた――ポーランド語で以前見られたのと同じ不具合である。「Ana-」は未処理のまま平文で残り、名前の残りの部分はすでに置換されていた。

- ハイフン付きのポーランド語複合名(「Anna-Maria Kowalska」)は、前に地の文や「z」「od」のような前置詞が続くと前半部分を失っていた――名前の残りの部分は置換されたが、「Anna-」は未処理のまま平文で残った。

- 独自の言語モデルを持たないカザフ語の丁寧な言い回し「Хабарласыңыз」/「Байланысыңыз」(ご連絡ください)、およびセルビア語の動詞形「Помоћи」「Чекамо」「Пишите」が、電話に関する文の中で誤って人物名や地名として検出されていた。

- 独自の言語モデルを持たないアゼルバイジャン語の丁寧な言葉「Xahiş」(お願い/要請)が、電話に関する文の中で誤って人物名として検出されていた。

- 独自の言語モデルを持たないインドネシア語・マレー語の丁寧な依頼・命令表現、「Silakan」「Mohon」(インドネシア語)、「Sila」「Tolong」(マレー語)が、電話に関する文の中で誤って人物名や地名として検出されていた。

- 独自の言語モデルを持たないウズベク語の命令形「Kutamiz」(お待ちしています)が、電話に関する文の中で誤って地名として検出されていた。

- 独自の言語モデルを持たないトルコ語の命令形「Arayınız」(お電話ください)や「Bekliyoruz」(お待ちしております)が、電話に関する文の中で誤って人物名として検出されていた。

- 独自の言語モデルを持たないその他の言語(チェコ語、スロバキア語、ギリシャ語)の命令形「Zavolejte」(電話してください)、「Prosíme」(お願いします)、「Περιμένουμε」(お待ちしております)が、電話に関する文の中で誤って人物名や地名として検出されていた。

- ハンガリー語・ポーランド語の命令形「Hívjon」(電話してください)、「Kérjük」(お願いします)、「Várjuk」(お待ちしております)、「Zadzwoń」(電話してください)、「Czekamy」(お待ちしております)が、電話に関する文の中で誤って人物名や地名として検出されていた。

- 表形式でない番号付き氏名リスト(例:「1. Robert Brown」、その下に「2. Mary Johnson」)では、特定の英語の姓(「Brown」「White」「Green」「Black」「Young」など)を持つ氏名がまったく見逃されていた――言語モデルが次の行の番号を氏名に結合してしまい、検出結果がもはや正確に一致しなくなっていたためである。

- ポーランド語の言語モデルでは、姓の前に置かれた名のイニシャル(例:「J. Kowalski」「A. Nowak」)が検出されず、テキスト中に未処理のまま残っていた――姓のみが置換されていた。他の検証済み言語(ドイツ語、英語、ルーマニア語、クロアチア語、ハンガリー語、ロシア語など)では、同じイニシャルはすでに以前から検出されていた。

- 「dr.」「ing.」「dipl. ing.」のような小文字表記の肩書きの後ろにある氏名は、ハンガリー語、ルーマニア語、クロアチア語ではまったく検出されなかった――肩書きだけでなく、氏名全体が失われていた(例:「dr. Kovács Béla」「ing. Andrei Popescu」「dipl. ing. Marko Horvat」)。
- スロベニア語の会議議事録では、コロンの前にある純粋な役職名(例:「Tajnik:」「Podpredsednik:」「Poročevalec:」「Predsedujoči:」)が、議事録の別の箇所にすでに実在の発言者名が記載されていた場合、誤って人物名として検出されていた。
- ロシア語の会議議事録では、コロンの前にある純粋な役職名(例:「Секретарь:」「Докладчик:」「Докладчица:」)が、議事録の別の箇所にすでに実在の発言者名が記載されていた場合、誤って人物名として検出されていた。
- ルーマニア語の会議議事録では、定冠詞付きでコロンの前にある純粋な役職名(例:「Secretarul:」「Președintele:」「Vicepreședintele:」「Moderatorul:」「Consilierul:」)が誤って人物名として検出されていた――「Președintele」はそれ単独でも、その他は議事録の別の箇所にすでに実在の発言者名が記載されていた場合に追加で検出されていた。
- クロアチア語の会議議事録では、コロンの前にある純粋な役職名(例:「Izvjestiteljica:」「Zapisničar:」/「Zapisnicar:」「Predsjedavajući:」)が、誤って人物名として検出されていた。
- 発送人または受信人のラベルの後ろにあるポーランド語の私書箱住所「Skrytka pocztowa」(例:「Odbiorca: Skrytka pocztowa 45」)が、誤って人物名として検出されていた。
- 住所ラベル「Adresa:」の後ろにあるクロアチア語の私書箱住所「Poštanski pretinac」(例:「Adresa: Poštanski pretinac 45」、番号を表す「br.」が付く場合も含む)が、誤って人物名として検出されていた。
- 特にラベルのないノルウェー語の地の文における地名(例:「Anna Hansen bor i Oslo」)が検出されなかった――そこでは独自の言語モデルが、通常使われる「LOC」ではなく、これまで対応付けられていなかった独自のラベルで地名を扱うことが多いためである。
- ハイフンまたはピリオド区切りの年-月-日のISO順の日付(例:「2024-12-31」)が、一部の言語ではまったく日付として検出されなかった――公文書の日付表記の大半がこの順序であるリトアニア語で最も目立っていた。
- 区切り記号のない、法的に等しく有効な11桁形式のハンガリーの付加価値税ID(közösségi adószám)(例:「12345678-1-23」ではなく「12345678123」)が検出されなかった。
- 3-2-2-3のグループ化で区切られたポーランドの税番号NIP(例:「856-73-46-215」、企業や個人事業主の請求書によく見られる形式)が検出されなかった――自然人向けの3-3-2-2のグループ化のみが検出対象だった。
- スロバキア語のフィールドラベル「Zamestnávateľ:」または「Názov zamestnávateľa:」(雇用主/企業)の下にある企業名が検出されなかった。
- ルーマニア語のフィールドラベル「Angajator:」または「Denumire angajator:」(雇用主/企業)の下にある企業名が検出されなかった。
- ハンガリー語のフィールドラベル「Cég:」または「Munkáltató:」(企業/雇用主)の下にある企業名が検出されなかった。
- ポーランド語のフィールドラベル「Pracodawca:」または「Nazwa firmy:」(雇用主/企業)の下にある企業名が検出されなかった。
- スロベニア語のフィールドラベル「Podjetje:」または「Delodajalec:」(企業/雇用主)の下にある企業名が検出されなかった。
- クロアチア語のフィールドラベル「Tvrtka:」または「Poslodavac:」(企業/雇用主)の下にある企業名が検出されなかった。
- 小文字表記の通貨単位を伴う書き出し金額表記(例:「500 euro」)が検出されなかった――大文字表記(「Euro」)のみが検出対象だった。
- 「Schwager」/「Schwägerin」(義兄弟/義姉妹)の後ろにある姓(例:「Der Schwager Bauer erhält die Erbschaft.」)が検出されなかった。
- 郵便番号+地名と通り名+番地の間に区切り記号のないトルコの住所(例:「34000 İstanbul İstiklal Caddesi No: 45」)では、番地が未処理のまま残っていた。
- 郵便番号+地名と通り名+番地の間に区切り記号のないスロバキアの住所(例:「831 01 Bratislava Hlavná 15」)では、番地が未処理のまま残っていた。
- 特にラベルのないクロアチア語のフォームフィールド内の出生国(例:「Zemlja rođenja: Njemačka」)が検出されなかった。
- 特にラベルのないリトアニア語のフォームフィールド内の出生国(例:「Gimimo valstybė: Vokietija」)が検出されなかった。
- 特にラベルのないポーランド語のフォームフィールド内の出生国または居住国(例:「Kraj: Niemcy」)が検出されなかった。
- 特にラベルのないスロベニア語のフォームフィールド内の国籍または居住地(例:「Državljanstvo: Nemčija」)が検出されなかった。
- 特にラベルのないノルウェー語のフォームフィールド内の居住国(例:「Bosted: Tyskland」)が検出されなかった。
- 新しい設定ページ「通知」(以前は「プログラム」内の一項目だった):3つのタスクバー通知(プレビュー準備完了、処理完了、更新のダウンロード完了)は、専用の場所に移動した。
- 新機能:結果を、純粋なテキストファイル(.txt)または拡張子.mdとしても、追加で保存できるようになった――AIや他のプログラムでの後続処理向け。
- ラベル「Osoba za kontakt」/「Kontakt osoba」(クロアチア語、例:「Osoba za kontakt: Golub Marko」)を持つクロアチア語の連絡先情報では、姓が同時に一般的な名詞である場合(Golub=「鳩」)、氏名が完全に検出されなかった。

- ラベル「Persoana de contact」/「Persoană de contact」(ルーマニア語、例:「Persoana de contact: Lup Ion」)を持つルーマニア語の連絡先情報では、姓が同時に一般的な名詞であり(Lup=「狼」)、名が非常に短く一般的な場合、氏名が完全に検出されなかった。

- ラベル「Osoba kontaktowa」/「Osoba do kontaktu」(ポーランド語、例:「Osoba kontaktowa: Wilk Adam」)を持つポーランド語の連絡先情報では、姓が同時に一般的な名詞である場合(Wilk=「狼」、Zielony=「緑」)、姓が検出されなかった。

- 郵便番号+地名と通り名+番地の間に区切り記号のないルーマニアの住所(例:「010061 București Strada Victoriei 30」)では、番地が未処理のまま残っていた。
- 郵便番号+地名と通り名+番地の間に区切り記号のないセルビアの住所(例:「11000 Beograd Bulevar Kralja Aleksandra 73」)では、番地が未処理のまま残っていた。
- 郵便番号+地名と通り名+番地の間に区切り記号のないギリシャの住所(例:「104 32 Αθήνα Ερμού 15」)では、番地が未処理のまま残っていた。
- 郵便番号+地名と通り名+番地の間に区切り記号のないスロベニアの住所(例:「1000 Ljubljana Slovenska cesta 58」)では、郵便番号が未処理のまま残っていた。
- 郵便番号+地名と通り名+番地の間に区切り記号のないリトアニアの住所(例:「LT-01100 Vilnius Gedimino pr. 9」)では、郵便番号が完全に未処理のまま残っていた。
- 郵便番号+地名と通り名+番地の間に区切り記号のないハンガリーの住所(例:「1052 Budapest Kossuth Lajos utca 12」)では、郵便番号が未処理のまま残っていた。
- 「Erben」(相続人)の後ろにある姓(例:「Die Erben Wagner erhielten die Mitteilung fristgerecht.」)は、相続・遺産の文脈でほぼ常に未検出のまま残っていた。
- 「Geschwister」(兄弟姉妹)の後ろにある姓(例:「Die Geschwister Bauer wohnen in Linz.」)は、これまでほぼ常に未検出のまま残っていた――「Familie」/「Ehepaar」の場合とは異なり、これは職業名と同形の姓(Koch、Bauer、Richter)だけでなく、この位置にある任意の姓に該当した。
- 「Ehepaar」または「Eheleute」(夫婦)の後ろにある姓(例:「Das Ehepaar Koch zieht um.」)は、同時に一般的な名詞または職業名である場合(Koch、Bauer、Richter)検出されなかった。
- 税番号や社会保険番号の典型的なグループ化パターンに一致する通常の注文・伝票・商品番号(例:「030 4471 2298」)が、対応するラベルが一切なくても誤ってそのように墨消しされていた。
- 「年/連番」形式の伝票・案件番号(例:「Rechnung Nr. 4/2024/778899」内)が、電話番号検出により誤って電話番号として墨消しされていた。
- 「Herr」/「Frau」の後ろに複数語の学術的な肩書き連鎖が続く氏名(「Herr Dr. med. Weber」「Herr Prof. Dr. Krause」)は、これまで完全に未保護のまま残っていた――これまでは敬称と氏名の間の単一の肩書き語のみが検出されていた。
- 部・院の略号を伴う古典的な形式の裁判所事件番号(「4 Ca 1523/24」「Az.: 7 O 234/25」)は、これまで完全に未保護のまま残っていた――正式なラベルに加え、一般的な短縮形「Az.」/「Gz.」も検出されていなかった。
- 4桁ごとのグループの途中で改行によって分断されたクレジットカード番号(狭い表の列などで)は、これまで完全に未保護のまま残っていた。
- グループ化の途中で改行によって分断された税務上の識別番号(狭い表の列やフォームフィールドなどで)は、これまで完全に未保護のまま残っていた。
- グループ化の途中で改行によって分断された社会保険番号(狭い表の列などで)は、これまで完全に未保護のまま残っていた――部分的な置換すら行われなかった。
- 「12a-14b」や「3-5」のような範囲を持つ番地は、半分しか置換されなかった――ハイフンの後ろの後半部分は、結果に開いたまま残っていた。
- 17桁の途中で改行、スペース、ハイフンによって分断された車両識別番号(FIN/VIN)(狭い表の列や車検証のフィールドなどで)は、これまで完全に未保護のまま残っていた。
- カンマなしの「Liebe Anna!」「Lieber Hans」のような手紙・メールの呼びかけ(気楽なメールで最も一般的な形式)は、その下に地の文と結びの挨拶を伴う完全な文書内であっても、氏名を完全に未保護のまま残していた。
- 同じ不具合は、カンマなしの気軽なチャット・メールの呼びかけ「Hallo Anna!」「Hi Anna!」「Hey Anna!」「Servus Anna!」にも及んでいた――氏名は同様に完全に未保護のまま残っていた。
- 「MfG」や「Herzlichst」で直接始まる純粋な署名ブロック――例えばクリップボードからそのままコピーされ、先行する文がない場合――は、その下の氏名を完全に未保護のまま残していた。
- 「Angehörige: Kaczmarek, Piotr (Sohn), Kaczmarek, Anna (Ehefrau)」のような複数人物を含むフィールドでは、両方の氏名が括弧内の情報とともに1つの、あまりに長い検出結果に統合されていた――2つ目の氏名は部分的に未保護のまま結果に残ることがあった。
- 「-straße」/「-weg」のような接尾語を持たない通り名――地方でよく見られる、例えば「Am Marktplatz 5」や「Im Grund 12」――は、その後ろに郵便番号+地名の行が続く場合(例えば住民票の「Neue Anschrift: Am Weidengarten 17, 54295 Trier」など)未検出のままだった――「Am Weidengarten」の例では通り名が完全に失われ、郵便番号のみが削除されていた。
- スラッシュを伴う複合フィールドラベル(例えば「Name/Vorname: Bauer Klaus」)の後ろにある氏名は、部分的に検出されなかった――「Bauer」のような多義的な姓は、そのフィールド上の裏付けなしには未検出のままだった。同じ不具合は「PLZ/Ort: 04109 / Leipzig」のような複合フィールドにも及んでいた。スラッシュの代わりに書き出された接続詞を使う複合フィールド(例えば「Vor- und Nachname: Bauer Klaus」や「Nachname bzw. Vorname: …」)についても同様だった。
- 「Datum der Geburt: …」形式の生年月日と、「Todesdatum: …」または「Datum des Todes: …」形式の死亡日は検出されなかった――「Geburtsdatum: …」および「Sterbedatum: …」のみが検出対象だった。
- 「Datum der Heirat: …」または「Datum der Hochzeit: …」形式の結婚日は検出されなかった――「Hochzeitsdatum: …」「Heiratsdatum: …」「Datum der Eheschließung: …」のみが検出対象だった。一方、離婚日、帰化日、パートナーシップ登録日はすでに以前から同じ「Datum der X」形式に対応していた。
- 「Datum der Scheidung: …」形式の離婚日は検出されなかった――「Scheidungsdatum: …」と後置動詞形のみが検出対象だった。一方、帰化日とパートナーシップ登録日はすでに以前から同じ「Datum der X」形式に対応していた。
- パートナーシップ登録日はこれまでまったく検出されなかった――ラベル付き(「Verpartnerungsdatum: …」「Datum der Lebenspartnerschaft: …」)でも地の文(「… wurden am … verpartnert」)でも検出されなかった。今では生年月日、結婚日、離婚日、帰化日と同様に独自の情報種別として置換される。
- 帰化日はこれまでまったく検出されなかった――ラベル付き(「Einbürgerungsdatum: …」)でも地の文(「… wurde am … eingebürgert」)でも検出されなかった。今では生年月日、結婚日、離婚日と同様に独自の情報種別として置換される。
- 離婚日はこれまでまったく検出されなかった――ラベル付き(「Scheidungsdatum: …」)でも地の文(「Die Ehe wurde am … geschieden」)でも検出されなかった。今では生年月日、死亡日、結婚日と同様に独自の情報種別として置換される。
- ラベルのない系図結婚記号「⚭」の後ろにある結婚日は検出されなかった――同じ行にある生年月日・死亡日は、星印と十字印を通じてすでに検出されていた。今では結婚日も検出される。
- ラベルのない死亡広告十字印の後ろにある死亡日(「*03.06.1940 †21.11.2023」)は検出されなかった――その前にある生年月日は系図の星印を通じてすでに検出されていた。今では死亡日も検出される。
- 事実文言とハイフンが前置された、件名/チケット行末尾の姓-名(「Betreff: Reklamation - Bauer, Anna」)は、職業名と同形の姓の場合検出されなかった――今では検出される。
- ラベルの後ろにある応募者番号・申請者番号(「Bewerbernummer: 4471829」「Antragstellernummer: 7654321」)は、まったく検出されていなかった――今では検出される。
- 読み取り可能なプレースホルダー用のスペースがない場合、「置換」はもはや墨消しに切り替わらない――極小のプレースホルダーは、スペースが少しでも残っている限り、空のバーになるのではなく、より小さく記述されるようになった。さらに新機能として、画像上(便箋、スキャン背景)の検出箇所にラベルを付けるか墨消しのみにするかを、他の結果種別とは独立して設定できるようになった。そして、完全に削除される画像上の検出箇所には、画像が残っているかのようにラベルが付けられていた――プレースホルダーは決して墨消しされない地色の上に明るく表示され、今では白くなった紙面の上で見えなくなっていた。
- **保持される**画像上の検出箇所は、置換の際、選択された表現(カテゴリ色、虹色など)に関係なく常に白黒で墨消しされていた――地の文の色付きラベルと便箋上の黒いバーとの間の断絶として見えていた。画像の地色は今では、隣接するプレースホルダーと同じ色に従うようになった。
- 車両識別番号(FIN/VIN)の検出は、I/O/Qを含まない17桁の英数字コードを無条件に車両識別番号としてマークしていた――偶然同じ形式を持つ注文番号、シリアル番号、ライセンスキー番号も対象になっていた。今では、近くに文脈語(「FIN」「VIN」「Fahrgestell」「Chassis」など)がある場合にのみカウントされる。
- チケット・カレンダーシステムでは、氏名検出が「Assigned to」/「Closed by」などの後ろで、同じ行に区切りなく次のフィールド語が続く場合、それを巻き込んでいた(「Assigned to Max Mustermann Priority High」が「Max Mustermann Priority」になっていた)。Gitのコミットヘッダーでも、氏名検出は同様に、改行の代わりにスペース1つで2行が連結されている場合、**次の**トレーラーキーを巻き込んでいた(「Author: julia bergmann Reviewed-by: …」が「julia bergmann Reviewed-by」になっていた)。両方の抑制策が追加された。
- 「p.A.」「zH」/「zHd」「i.A.」/「i.V.」「geb.」の後ろにある氏名は、区切りなしで同じ行に直後の部署語が続く場合、それを同じ検出結果に巻き込んでいた(「p.A. Max Mustermann Buchhaltung」が「Max Mustermann Buchhaltung」に、「i.A.Max Mustermann Vertrieb」が「Max Mustermann Vertrieb」になっていた)。「Assigned to」/Gitトレーラーの場合と同じ抑制策が、ここにも追加された。
- BIC・BLZ・SWIFT行の直上にあるラベル付きIBANは、「BIC」や「BLZ」自体がさらなる番号ブロックのように見えたため、その行のラベルまで自身の検出結果に巻き込んでいた――「IBAN: DE89 … 0130 00」とその下の行から、1つの範囲の広すぎる検出結果が生成され、処理の際、次の行のラベルも一緒に消えていた。IBANとBICが縦に並ぶほぼすべての銀行口座情報が影響を受けていた。
- ヒットパネルは今、見つからないプレースホルダーの**位置**を表示する。これまでは2つのケースで「見つかりません」としか報告されなかったが、置換自体は行われていた。プレースホルダーが可視でない副次テキスト内(リンクの参照先アドレス、注釈、フォームフィールドなど)にある場合、その行は今では独自の情報として「副次テキスト内」と表示し、クリックするとそれが説明される。そして、プレースホルダーがスペース不足のため短縮表記で書かれた場合(「[NAM382]」ではなく「[N382]」)、長い行へのクリックは今では短縮形の位置にジャンプし、名称変更を説明する――両方の行はそのために明示的に連結される。
- 同じ置換値が文書内に複数回出現する場合、パネル行のさらなるクリックは、ページをまたいでも巡回的に次の検出箇所にジャンプする。ステータス行はそれをカウントする(「検出箇所2/4」)。現在対象となっている箇所は、他よりも太い枠で示される。そして、プレースホルダーがヒットリストにのみ存在し、文書中のどこにも存在しない場合(重なり合う置換に取り込まれたため)、ステータス行は今ではその旨を示す――クリックが黙って無反応になることはない。
- 「an」または「für」の後ろにある省略された名は、今では確実に氏名として検出される――「Überweisung an M. Wagner」や「Rechnung für M. Wagner」は、これまでしばしば未処理のまま残っていたが、同じ氏名が別のラベル(「Zahlungsempfänger:」など)を伴う場合はすでに検出されていた。主に口座明細や記帳行が影響を受けていた。
- 「Angeklagter」/「Angeklagte」/「Beschuldigter」/「Beschuldigte」は今や氏名フィールドとして扱われる。刑事訴訟の文書でこれらのラベルの直後に氏名がある場合、これまで検証済みの氏名の約半数でまったく検出されなかった――名も姓も検出されなかった。
- ヒットパネルでクリックされた箇所は、今では黄色マークの代わりに青枠で囲まれる――色付きの検証信号面の上では、検索結果の黄色が識別できなかった。さらに、クリックは今、複数語からなる置換値(架空の氏名、マスクされた番号)も検出する。これまでは、検出箇所が単語単位でしか検索されなかったため、このような行では無効なままだった。
- 養父母、里親、継父母(「Adoptivvater」「Pflegemutter」「Stiefvater」など)は今や氏名フィールドとして検出される――以前は氏名が未処理のまま検出をすり抜けていた。
- 数字の多い表やリストが誤って破棄されなくなった。短い数字(例えば電話番号として誤読された顧客番号の一部)が置換された場合、最終検証は、そのまったく別の番号の中にたまたま含まれているだけの同じ数字列であっても、それを残存情報として報告していた――その結果、結果が一切出力されないことがあった。数字は今では、実際に独立した数字として存在する場合にのみ、残存情報としてカウントされる。
- 身分証明書:「Vater:」/「Mutter:」は今や氏名フィールドとして検出される――以前は親の氏名が未処理のまま検出をすり抜けていた。
- その他の家族の役割(「Pate」「Großvater/-mutter」「Ehepartner」「Lebenspartner」「Onkel」「Tante」)は今や氏名フィールドとして検出される――以前は氏名が未処理のまま検出をすり抜けていた。
- ドイツの銀行コード(BLZ)は今や、公式のグループ表記でも検出される("370 400 44"、"370.400.44"、"370-400-44"、"370/400/44")――以前は8桁連続の数字としてのみ検出されていた。
- ドイツの年金保険番号は今や、5つのブロックの間にピリオド、ハイフン、スラッシュがある場合でも検出される("65-170839-J-08-8"、"65.170839.J.08.8")――以前はスペース区切りのみだった。
- メインウィンドウの表示が高速化された:検出用ライブラリ(Presidioと言語モデル基盤を含む)は、これまでウィンドウ構築時にすでに読み込まれていた――Windowsでは何も見えるようになる前に約4秒かかっていた。今ではバックグラウンドで完全に読み込まれる。「処理を開始」ボタンは、これまで通り、すべての準備が整った時点で初めて有効になる。
- 画像や動画を多く含むOffice文書の書き込みが高速化された:すでに圧縮済みのメディアは、結果パッケージ内に無駄に再圧縮されることなく保存されるようになった――以前は1バイトも節約されず、むしろJPEGが大きくなることさえあった。
- 小さなテキスト単位を多く含む表計算やその他の文書の検証が高速化された:言語検出は、文書のすべてのセルと段落を、個別処理ではなく一括処理するようになった――検出結果は変わらないまま(セル400件で約4.7秒から2.5〜3.5秒に)。
- リスト形式のPDFページ(目次、位置リスト)は、プレースホルダーの挿入がかなり高速化された:ラベルごとの配置検索はこれまでページ全体の単語を対象に実行されていたが、今では行の周辺のみを対象とする――結果は同一のまま(ラベル300件を含むページで約16倍高速化)。
- 画像を多く含む文書では、画像ごとに複数の不要な処理ステップが省かれる:PDFページ上の顔とコードのカウントはページ画像を二重にデコードしなくなり、メタデータの検証はもはやクリーンな画像を最初に復号しなくなり、モザイク化された画像は最も遅いPNG圧縮ではなく通常の圧縮で書き込まれるようになり(サイズは同じで所要時間は3分の1)、透かしを設定していない場合、最後にPDF全体を無駄に書き直す処理が省かれる。
- 文字認識を有効にしたスキャンPDFの処理がかなり高速化された:各ページはこれまで2回フル解像度でレンダリングされていた(読み取り用とラスタライズ用)――画像は今では再利用される。またWindows/Linuxでは、文字認識が大きなスキャンのストライプを、ストライプごとに個別のプログラム起動ではなく、1回の処理で読み取るようになった。
- 大きな文書の処理がかなり高速化された:既検出値との照合は、これまで検出箇所数とともに増大していた(64KBのテキストブロックは、大きなファイルの終盤で約1秒かかっていたが、今では60分の1)。企業の法人形態の検索は、約280種のカタログ形式すべてをテキスト箇所ごとに実行していたが(今では約20倍高速化、検出結果は同一のまま)。
- 「Beste Grüße」/「Beste Wünsche」の直後にある、先行するテキストや句読点のない氏名はまったく検出されなかった――地の文のない純粋な署名ブロックは、氏名を跡形もなく消してしまっていた。
- 文書冒頭にある、職業名と同形の姓を含む住所フィールド(「Bauer Anna」「Koch Stefan」が通りと地名の上の最初の行として)は、これまで部分的に未検出のままか、人物ではなく地名として分類されていた――先行する文がないため、言語モデルには「Bauer」を職業ではなく氏名として認識させる文構造が欠けていた。
- 署名マーク「gez.」の後ろにある、名の前に職業名と同形の姓を持つ氏名(判決や決定の末尾にある「gez. Bauer Anna」)は、これまで不完全にしか検出されなかった――名のみが検出され、姓は跡形もなく消えていた。
- 顧客番号、契約番号、または類似の識別番号の直後にある、独立した行を持たない氏名(「Vertragsnummer 55219 Bauer Anna」「Kundennr. 4711 Bauer Anna」)は、職業名と同形の姓の場合、これまで不完全または未検出だった。
- macOSのメニューバーのアイコンは今やテンプレートとなり、隣接するアイコンと同様にライトモード・ダークモードに適応する――2本の切り抜きバーによって、Maskuroのアイコンとしては引き続き識別可能である。クリップボード監視が動作中の場合は、盾の先端に離れた点が表示される。
- ヒットパネル内のクリックは今、匿名化処理モードでも検出箇所へ移動する:ページ切り替え、画像へのスクロール、黄色マーキング。これまでこの操作は無反応だった――プレースホルダーがまだ番号を持たないと誤認識されていたためである。すべての検出箇所が独自の番号を持つようになったことで、位置が一意になった。実際に番号を持たないプレースホルダーの場合のみ、ステータス行が引き続き、ジャンプ先を特定できない理由を説明する。
- 修正エディタでの最初の保存(Ctrl+Sまたはディスクアイコンのボタン)は今、保存場所を尋ねるようになった――「名前を付けて保存…」のように。原本のフォルダと結果ファイル名が初期値として入力されている。以前は、ファイルは何も尋ねられずに原本の隣に置かれていた。保存場所をすでにステータス行で選択していたユーザーには、再度尋ねられることはない。以降の保存は、これまで通り同じファイルに上書きされる。
- 保存前のセキュリティチェックが目立つ箇所を報告する場合、「検証に戻る」は今、そこへ誘導する:最初の検出箇所が画像内にスクロールされ、赤枠で囲まれ、ステータス行がそれを示す。以前はページ番号と座標のみが提示されていた。メインウィンドウからの場合、この操作でエディタがその箇所を開いて表示される。異なるページ数についてのヒントの場合も、このボタンは今、両方の文書のうち一方にしか存在しない最初のページへと誘導する。
- プレビューを「2列並べて表示」に切り替えると、今では自動的に両方のレーンが収まるウィンドウが得られる――以前は、自分でウィンドウを広げるまで、以前の幅に押し込められていた。拡大は画面端までにとどまり、決して自動的に狭められることはない。手動で調整した幅は維持される。
- 別々の表列に分かれた姓と名(例えば「Nachname | Vorname」の申込確認書やCSVエクスポート)は、未処理のまま残っていた――各セルはそれぞれ単独では、検出にとって氏名の文脈を持たない任意の単語に見えていた。今では検出される。
- EU運転免許証の裏面にある姓と名は、未処理のまま残っていた――そこではドイツ語の単語ではなく、公式のフィールドコード「1.」と「2.」の後ろに記載されており、そのために検出されなかった。免許証番号(フィールドコード「5.」)が隣にある場合、今では検出される。
- 車両登録証(車検証)上の車両所有者の名は、未処理のまま残っていた――「Vorname」のようなドイツ語の単語ではなく、公式のフィールドコード「C.1.2」の後ろにあるため検出されなかった。フィールドコードC.1、C.1.1、C.1.2の下にある姓と名は、今では検出される。
- パスポートまたは身分証明書の機械可読領域(MRZ)の1行目は未処理のまま残っていた――「姓<<名」形式で氏名を保持しており、チェックディジット行用の新しいMRZ検出機能を使ってもすり抜けていた。検出は今、その隣にチェックディジットが有効な2行目のMRZ行が存在する場合にのみカウントされる――氏名行自体には独自のチェックディジットがない。
- パスポートまたは身分証明書の機械可読領域(MRZ)の2行目は完全に未検出のまま残っていた――旅券番号、生年月日、有効期限を平文で含んでいるが、既存の検出機能には一切一致しなかった。専用の検出機能が今、4つの
  ICAOチェックディジットを検証する。
- ラベルとの間にスペースが一切ない車両番号標(ナンバープレート)は未処理のまま残っていた――「KennzeichenM-AB1234」や「KFZ-KennzeichenM-AB1234」はまったく検出されなかった。基盤となる番号標検証は、番号標の前に非文字記号を前提としているためである。フィールド語と番号標の間にスペースがない車両情報に影響していた。
- ラベルとの間にスペースが一切ない電話番号は未処理のまま残っていた――「Handynummer0171/2345678」や「Tel0171/2345678」はまったく検出されなかった。基盤となる電話番号検証は、番号の前にスペースまたは句読点を前提としているためである。フィールド語と番号の間にスペースがない連絡先情報に影響していた。
- 略語「geb.」(旧姓)の後ろにある旧姓はまったく検出されなかった――「Julia Bergmann (geb. Weber)」は「Julia Bergmann」のみを検出し、「geb.」内のピリオドが言語モデルに後続の氏名を完全に読み飛ばさせていた。括弧内またはカンマの後ろに旧姓がある人物情報に影響していた。
- 敬称と肩書きが一緒に前置されている場合、引用符付きニックネームの前にある名は未処理のまま残っていた――「Herr Dr. Klaus "KP" Peters」は「Peters」のみとなり、「Klaus」は読める状態のまま残っていた。肩書きとニックネームを伴う署名や連絡先情報に影響していた。
- ピリオドなしの短縮形「zH」/「zHd」(気付)の後ろにある氏名はまったく検出されなかった――ピリオド付きの「z.Hd.」とは異なり、不足した文構造が氏名を一緒に巻き込んで失わせていた。略語にピリオドのない住所に影響していた。
- 「p.A.」(気付)の後ろにある氏名はまったく検出されなかった――略語内のピリオドが言語モデルに氏名検出を完全にスキップさせていた。集合住所を伴う請求書や応募書類に影響していた。
- ピリオドなしで結合された「i.A.」/「i.V.」(代理として/代理して)の後ろにある氏名はまったく検出されなかった――例えば「i.A.Robert Lang」のようにスペースなしの場合。「p.A.」と同じ文構造の不具合である。代理案件の署名欄やメール署名に影響していた。
- 他に何も情報がない箇条書きの純粋な出席者リスト(「- Max Mustermann」、行末にピリオドがある場合も含む)は、本来「- Farbe: Blau」のような事実の列挙のみを保護すべき同じ抑制策により、すべての氏名を失っていた。このようなリストは今では検出される。
- 処理できなくなっていたファイルが再び処理できるようになった。すでに検出により置換された値が、「[SVNR1]」のような、すでに置換済みの独自のマーク内で再発見されることがあった――最終検証は、その結果、完全に処理済みのファイルを破棄していた。さらに、CSV表内の電話参照も今では一緒に削除されるようになり、検索を特定の種別に限定するユーザーは、文書内のどこでも同じ扱いを受けるようになった――画像の代替テキスト、Excelのヘッダー行、選択リスト、HTML属性においても同様である。
- メールヘッダー「To:」(またはコロンなしの「To」)の後ろにある氏名は検出されなかった――対応する言語モデルが行全体を1つの目立たない検出結果として読み、その中の氏名を完全に飲み込んでいたためである――同じ氏名の前にある「Cc:」「Bcc:」「From:」とは異なる挙動だった。「To」の後ろの氏名は今では確実に検出される。
- 結婚日は独自ルールの中で日付として扱うことができなかった(「移動」は「日付情報にのみ存在します」として拒否されていた)。検出種別のグループ割り当てから欠けており――そのため「何を検索するか」のマークから無効化できなかった――そして死亡日のような短い略号ではなく、プレースホルダーとして全文がそのまま使われていた。6つの略号/ラベルテーブルすべてに反映された。
- プレビューで意図的に選択解除した値が、それでも別の箇所で墨消しされることがあった:例えばメールアドレスを選択解除すると、アドレス自体は残るものの、そのローカル部分(ドメインを除いた部分)は、別途選択された人物から導出されたユーザー名と一致すると置換されていた(「anna.musterfrau@beispiel.de」と「Anna Musterfrau」)。選択解除された文言は今では、どの検出種別に由来するかにかかわらず、文書全体でタブーとなる。
- 家族登記簿または身分事項証明書が、生年月日と出生地を共通のヘッダーの下にまとめて記載している場合(「Geburtsdatum, Geburtsort: 19.11.1982, Steyr」)、生年月日は検出されなかった――「Geburtsdatum」と日付の間にある2番目のフィールド語が、これまで検出を完全にすり抜けさせていた。
- すでに検出された電話番号が、同じ文書の別の箇所で下4桁のみで言及される確認用の短縮形式(「erreichbar unter der Nummer ...5678」「Rückruf unter ...5678」)で、読める状態のまま残っていた――IBANやクレジットカードと同じ形式である。
- すでに検出されたクレジットカード番号が、同じ文書の別の箇所で下4桁のみで言及される確認用の短縮形式(「Ihre Kreditkarte endet auf ...0366」)で、読める状態のまま残っていた――支払い確認メールでよく見られる、IBANと同じ形式である。
- すでに検出されたIBANが、同じ文書の別の箇所で下4桁のみで言及される確認用の短縮形式(「Die IBAN endet auf ...3201」)で、読める状態のまま残っていた――確認メールでよく見られる形式である。
- チャットまたは会議議事録の発言者は、氏名の前に敬称がある場合(「Herr Bauer: …」「Frau Koch: …」)未検出のままだった――そして、この結果、認識された行が少なくなりすぎて文書がそもそも議事録として評価されなくなるため、同じ議事録内の次の発言者行にも影響が及ぶことが多かった。
- フィールド語「geboren」が日付の前ではなく【後ろ】にある場合(「Das Kind wurde am 14.01.2026 geboren」)、生年月日は未検出のままだった――育児休業証明書や母性保護証明書などが、子の生年月日をこのように表記する。これまでのパターンは、フィールド語が常に日付の前にあることを前提としていた。
- ラベルの直前に反応・チェック記号がある場合(「Ansprechpartner 😊:」「Kontaktperson ✓:」)、フォームラベルとして検出されなくなり、その下または後ろにある氏名は部分的にしか検出されなかった(例:「Mayer Roman」で姓のみ)。
- 同じ不具合は、GDPR第9条に基づく特に保護が必要な情報(宗教、健康、労働組合)にも及んでいた:区切りまたは改行の直前にある反応記号(「Konfession 😊: römisch-katholisch」)は、ラベル全体を検出から完全に除外させ、情報が完全に未検出のまま残っていた。
- 地名にハイフン付き複合名を含む住所(例:「79761 Waldshut-Tiengen」「78050 Villingen-Schwenningen」)は、地名自体は検出・墨消しされていたにもかかわらず、郵便番号が完全に失われていた――車検証や送付状で郵便番号が読める状態のまま残っていた。
- 列間隔のない表列(実際のPDFテキスト抽出)は、氏名列の下で、たまたま隣接する2つの大文字表記(例えば1つのデータ行内の2つの地名)を誤って人物として墨消しすることがあった。これは今では、同じ位置で他の検出結果がすでに何かを検出していない場合にのみ発生する。
- 同じ氏名列は、同じ行形式で、言語モデルに未知の2つの一般的な単語(例:「Frontend Backend」「Turbo Modul」)も、他の検出結果がその抑制をトリガーしない場合、誤って人物として墨消ししていた。今では、2つの単語のうち少なくとも1つが言語モデル自身によって固有名詞として読まれることも追加で要求される。
- ドイツの年金保険番号は、その公式の完全なグループ化(例:「65 170839 J 08 8」――社会保険証や給与明細に記載されている通りの形式)では検出されず、原文のまま残っていた――コンパクト表記と、文字までのみグループ化された形式のみが検出されていた。
- 税務上の識別番号は、その公式の表記(2-3-3-3のグループ化、例:「48 836 075 988」――実際のすべての税額決定通知書や連邦中央税務庁の通知書に記載されている通りの形式)ではまったく検出されず、原文のまま残っていた――より珍しい3-3-3-2のグループ化のみが対象だった。
- ノルトライン=ヴェストファーレン州の税番号(例:「221/5147/0815」、第2グループが3桁ではなく4桁)は、税額決定通知書内でまったく検出されず、原文のまま残っていた――他のすべての州はすでに対応済みだった。
- 労働契約では、ラベル「Arbeitgeber:」(雇用主)の後ろにある氏名が、姓が同時に一般的な単語である場合(例:「Bauer Anna」)完全に見逃されていた――「Arbeitgeber」は氏名ラベルとしても企業ラベルとしてもリストに存在し、企業への割り当てが氏名への割り当てを上書きしていた。
- 「Vermieter:」/「Mieter:」(賃貸人/賃借人)ラベルを持つ賃貸契約のヘッダーでは、姓が同時に一般的な単語である場合(例:「Bauer」)見逃されていた――名のみが検出されていた。番号付きの賃借人(「Mieter 1:」「Mieter 2:」)は、この多義性がない氏名の場合でも追加で影響を受けていた。
- 「Zeuge:」/「Kläger:」/「Beklagter:」(証人/原告/被告)ラベル(番号付き「Zeuge 1:」「Zeuge 2:」も含む)を持つ裁判記録では、姓が同時に一般的な単語である場合(例:「Bauer」)同様に見逃されていた――名のみが検出されていた。
- 相続証明書、委任状、督促手続、売買契約では、姓が同時に一般的な単語である場合(例:「Bauer」)、「Erblasser:」「Erbe:」「Vollmachtgeber:」「Bevollmächtigte:r」「Antragsgegner:」「Schuldner:」「Gläubiger:」「Käufer:」「Verkäufer:」「Vermächtnisnehmer:」「Testamentsvollstrecker:」といったラベルの後ろで見逃されていた――一部では名のみが検出され、一部では氏名全体が失われていた。
- 事件表題の区切り記号「./.」(対 vs.)の前にある複数当事者リスト(例:「Sand, Werner und Huber, Anna ./. Wechsler, Martina」)では、最初の当事者が保護されないままだった――「./.」に直接隣接する当事者のみが検出されていた。
- 事件表題の区切り記号「./.」(例:「Sand./.Wechsler」)では、スペースがない場合、記号の後ろの氏名が完全に見逃されていた――前後にスペースがある場合にのみ検出が機能していた。
- 姓「Wahr」は単独で立っている場合(例:「Frau Wahr bearbeitet Ihren Vorgang.」)完全に見逃されていた――この単語は、「Das ist wahr.」のような文からの氏名の誤検出を通常フィルタリングする、一般的なドイツ語単語のリストにたまたま含まれていた。
- 「Los」「Weit」「Rund」「Hoch」のような姓は、単独で立っている場合(例:「Herr Hoch übernahm die Leitung.」)完全に見逃されていた――これら4つの単語は、「Rund einhundert Gäste kamen zur Feier.」のような文からの氏名の誤検出を通常フィルタリングする、一般的なドイツ語単語のリストにたまたま含まれていた。
- 「Ganz」や「Recht」のような姓は、単独で立っている場合(例:「Herr Ganz unterschrieb den Vertrag.」)完全に見逃されていた――両方の単語は、「Ganz genau, das stimmt.」のような文からの氏名の誤検出を通常フィルタリングする、一般的なドイツ語単語のリストにたまたま含まれていた。
- ラベルの後ろにアスタリスクや上付きの脚注番号があるフォームフィールド(例:「Konfession*: römisch-katholisch」や「Religionszugehörigkeit¹: evangelisch」)は検出されず、平文のまま残っていた――この記号のない形式のみが検出対象だった。
- 同じフィールドは、ラベルの後ろに脚注記号が2つ続けてある場合(例:「Konfession**: römisch-katholisch」や「Gewerkschaft¹²: ver.di」)も引き続き平文のまま残っていた。
- 「Softwareversion 4.2.1.19」や「Firmware Build 2.0.4.11」のようなバージョン番号は、もはや誤ってIPアドレスとして墨消しされない。「Rechnungsnummer 10.20.30.40」や「Bestellnummer 7.8.9.10」のような伝票番号・案件番号も同様に対応した。
- 縦に直接並んだ2つのIBAN(例:請求書ヘッダー内の自社と海外の取引先のもの)は、両方とも検出されなくなっていた――2番目のものが気づかれないまま残っていた。
- ラベル付きのIBANは、続く単語が小文字表記の場合、文中の続く単語を時々巻き込んでいた(「Bankverbindung AT61 … wird belastet」が「wird」まで墨消しされていた)――その隣の平文の残りはそのまま手つかずで残っていた。
- リヒテンシュタインの住所が検出されるようになった(「FL-9490 Vaduz」)――これまでのドイツ、オーストリア、スイスと同様である。
- パスポート番号・旅券番号が、そのラベルの後ろで検出・削除されるようになった(例:「Reisepassnummer: C01X00T471」)。
- 在留資格番号・住民登録証明書番号が、そのラベルの後ろで検出・削除されるようになった。
- ラベルの後ろにある識別番号は、コロンの代わりにダッシュで区切られている場合(例:「Kundennummer – K903944」)も検出されるようになった。
- 「IBAN」または「Kontonummer」とラベル付けされた銀行口座情報は、コロンの代わりにダッシュで区切られている場合も検出されるようになった。
- ラベル「Kontaktperson (Vertrieb)」や「Sachbearbeiter/in」の後ろにある氏名は、括弧付き補足やジェンダーニュートラルなスラッシュ語尾を伴う場合も検出されるようになった。
- 同じアスタリスク形式のジェンダー表記(「Sachbearbeiter*in」)も同様に検出されるようになった。
- ラベルの後ろにある氏名は、コロンの代わりにイコール記号で区切られている場合(例:「Ansprechpartner = Mayer Roman」や「Kontaktperson=Mayer Roman」――設定ファイルやCSVヘッダーによく見られる形式)も検出されるようになった。同じ行にこのようなラベルと値のペアが複数、セミコロンで区切られて並んでいる場合、残りの行全体ではなく、最初の値のみが検出されるようになった。
- 単語「Koordinaten」の後ろにあるGPS座標ペアは、今では確実に検出される(例:「Koordinaten: 48.2082, 16.3738」)――この単語は内部カタログで誤った活用形を保持していた。
- 顧客番号、契約番号、事件番号、身分証明書番号など、約100のさらなるフィールド語のラベルの後ろにある識別番号は、そのラベルが登録されている大文字・小文字表記と正確に一致しない場合、検出されなくなっていた――メール内の「kundennummer:」やフォームヘッダー内の「KUNDENNUMMER:」は手つかずのまま残っていた。

### 新機能

- **現実的な代替値は、規定値ではなく、意図して使う例となった。**「プレースホルダー」タブの例外テーブルは空の状態で始まる。新しいボタンが、希望に応じて、氏名・地名・住所・組織・メール・電話・内線・IBANのもっともらしい架空の値を入力する。金額は明示的に番号付きプレースホルダーのままとする。「架空の値を作る」戦略は、個別の種別についてはこれまで通り手動で選択可能である。
- **AIレベルはグラフィックカードを利用できるようになった。** Windowsでは、そのために約17MBの追加パッケージを後から読み込める。それ以降、AIレベルは適したグラフィックカードがあれば、プロセッサ処理よりかなり高速に演算する。持っていないユーザーや何も読み込まないユーザーは、これまで通り変わらず動作する――ただし速度は変わらない。macOSでは高速化はすでに組み込み済みである。
- **タスクバーアイコンからの新しい2種類の通知**:プレビューが置換前の確認待ちになったときと、処理が完了したとき。両方とも初期設定でオンになっており、*設定 → プログラム → 通知*から個別にオフにできる。

### 変更点

- **身分証明書番号と運転免許証番号が、そのラベルが前にある場合(「Personalausweisnummer: …」「Führerscheinnummer: …」)検出されるようになった**――これまでは両方ともあらゆる検出をすり抜けていた。
- **MaskuroはWindowsのコントラストデザインに従うようになった。** *設定 → アクセシビリティ → コントラストデザイン*の下で有効化しているユーザーは、これまでここ以外のすべての場所でそれが適用されていたが、Maskuroはその後独自の色を設定していた。今後はシステムのデザインが維持される――ウィンドウ、リスト、ドロップゾーン、ログ、ステータス色である。プレビューと修正ウィンドウ内の色付き検証信号は、そこでは意図的に省略される――伝える内容はそれ以来いずれにせよ記号と単語として隣に表示されている。
- **検証の必要性は、もはや色だけで示されない。** 赤・オレンジ・緑はほぼ同じ明るさである――赤緑色覚異常のあるユーザーには、プレビューと検出パネルのリストが違いのないものに見えていた。これは男性のおよそ12人に1人に該当する。各行は今、形の異なる記号を追加で持つ:▲要確認、●確認、○十分に裏付けあり、◆評価なし。短いヒントテキストがそれを言葉で伝え、スクリーンリーダーが読み上げる。
- **Altキーで再びメニューを従来通り開けるようになった。** メニューバーにはキーボードショートカットがなく、マウスを使わないユーザーは各メニューを矢印キーで移動する必要があった。今では各項目に下線付きの文字が付く――Alt+Dで「Datei(ファイル)」、そこからBで「Beenden(終了)」――オペレーティングシステムのすべての言語で。
- **操作要素は再び、それが何のためのものかをスクリーンリーダーに伝えるようになった。** 修正ウィンドウ、ルールウィンドウ、ログ、単語リスト、ヘルプ、検索処理、さらに5つのウィンドウで、リスト、検索フィールド、ドロップダウンリスト、スライダーが、これまで何についてのものかを示さず「ツリー」や「コンボボックス」としか読み上げられていなかった。約40箇所に今、名前が付けられた(メインウィンドウは8月から問題なかった。その後追加されたウィンドウは、この対応がなされていなかった)。
- **キーボードで操作するユーザーは、今どこにいるかが常にわかるようになった。** 検証必要性のスライダー、チェックボックス、プレビューの「二度と削除しない」ボタン、その中の種別見出し、修正ウィンドウのページパネル、設定のサイドバーには、システムが通常フォーカス中の操作要素の周りに表示する枠が欠けていた。
- **大きなシステムフォントで何も欠けなくなった。** *アクセシビリティ → 文字サイズ*で175%を超えて設定するユーザーは、これまでフォルダ監視とショートカットキーフィールドでラベルの末尾を失っていた。ヘルプの目次は、通常のフォントサイズですでに長い章タイトルを切り詰めていた――今では折り返し、短いヒントテキストで完全な名称を示す。

- **検出処理がかなり高速化された。** ラベル付き識別番号(「Kundennummer: K903944」)の検出機能は、これまでテキスト区間ごとに1200以上の個別パターンを順番に検証していた――これは、あらゆる段落と表セルにおいて、検出処理時間の最大の項目だった。今では同じ結果を出す単一のパターンとなった:測定コーパスでは一致する検出結果は1件も変わらず、テキスト区間ごとの基本レベルは約3〜4倍高速化された。
- **ウィンドウは起動時にすぐに表示されるようになった。** これまでメインウィンドウは、表示される前に完全な言語処理ツールを読み込んでいた――起動のたびに約4秒の待機時間があった。モデルは今、予定通りバックグラウンドで、ウィンドウがすでに表示されている間に読み込まれる。「処理を開始」ボタンは、これまで通りすべての準備が整った時点で初めて有効になる。コマンドラインの純粋な情報照会(例えば`--version`)も、数秒後ではなく即座に応答するようになった。
- **自動言語検出時、画像はもう1回しか読み込まれなくなった。** これまで、初期設定「言語:自動」では、同じ画像に対して文字認識が2回実行されていた――1回は言語推定用、もう1回は実際の検証用である。画像ファイル、クリップボード画像、テキストウィンドウはこれによりほぼ2倍速く完了する。文字認識が無効な場合、これまで気づかれずに実行されていた読み取り処理自体がなくなった。
- **保存済みウェブページとメールの処理が高速化された。** HTML属性、コメント、埋め込みデータブロック内の値は、これまで個別に検出されていた――何百ものラベルを持つ自治体のページでは、検出処理に何百もの個別の問い合わせが発生していた。今ではまとめて収集され、異なる値ごとに一度だけ検出される。測定コーパスでは検出結果は1件も変わらず、.htmlと.emlは約3分の1高速化された。
- **表やプレゼンテーションの副次的な格納場所も一括で検出されるようになった。** 代替テキスト、数式の文字列、グラフのラベル、コメント、ピボットキャッシュ、文書プロパティは、それぞれの値ごとに個別の検出問い合わせを発生させていた――何千行ものピボットデータを持つワークブックでは、それに応じて何千もの問い合わせが発生していた。今では異なる値に対して一括処理が実行され、末尾の完全な再走査は、地の文以降に実際に新しい値が追加された場合にのみ実行される。測定コーパスでは検出結果は1件も変わらない。
- **フォームの多いPDFの処理が高速化された。** フィールド、注釈、ブックマーク、参照は、同じ値を大量に繰り返す(すべてのチェックボックスの「Off」、すべての注釈の同じ作成者など)――これまでは、それぞれが独自の検出問い合わせを発生させていた。実行ごとに、値は今では一度だけ検出される。置換と整合性の後処理は、これまで通り箇所ごとに実行される。
- **大きな表ファイル(.csv/.tsv)の処理がかなり高速化された。** 4つの表後処理パスは、これまでそれぞれ独自に同じファイルを文字単位でセルに分解していた(40MBで約30秒の余分な処理)――今では分解処理は1回のみ実行される。列ヘッダーの検出(生年月日列や社員番号列)は、セルごとの問い合わせではなく一括問い合わせとなり――同一の検出結果で約20倍高速化された。そして、大規模な人員リストの氏名列の統合処理は、行数に対してもはや二次関数的ではなくなった。
- **指標パネルはもうウィンドウをフリーズさせない。** 指標パネルを開くと、多くの大きなファイルではそのテキストをまず集約しており、その間ウィンドウが数秒間停止していた。計算は今ではバックグラウンドで実行される――パネルはすぐに開き、数値は後から追加される。
- **検索処理のレポートはもうウィンドウをフリーズさせない。** 何千ものファイルを検索した後、影響を受けた各ファイルについて共通フォルダが再計算されていた――大規模な処理では、ウィンドウは2桁の秒数停止していた。レポートは今では即座に表示される。
- **文字認識付きのPDFの検証が高速化された。** 各ページは照合の際、不必要に2回PNG形式に変換されていた――今では既存の画像がそのまま受け渡される。結果は変わらず、検証のみが迅速化された。
- **大きな画像上のグラデーション注釈がカクつかなくなった。** グラデーション付き注釈のハンドルを動かすと、これまでグラデーションが点単位で再計算されていた――大きなスクリーンショットでは目に見える停滞となっていた。結果は同じだが、その停止がなくなった。

### 修正

- **ファイルをリストから削除するための×印は、再び単純な×になった。** 新しいエディタツール「削除」が誤って同じアイコン識別子を使用してしまい、その結果、赤い×印と破線のテキストラインが各ファイル行にも表示されていた。両方の操作は今では別々のアイコン名を持ち、それぞれふさわしい表示を保つ。
- **複数部分からなる情報は、PDF内で目に見える改行をまたいでも検出されるようになった。** Maskuroは、幾何学的に生成されたページテキストを、オフセットが一致する地の文表示として追加で読み取る。これはすべての基本レベル・高レベルの検出機能や独自の検索パターンに適用され、最初に見つかった「Diabetes mellitus Typ 2」のケースだけではない。空行や検出済みの表・セクション境界は引き続き厳格な境界として扱われ、検出箇所は引き続き墨消し対象の単語に正確に一致する。
- **「仮名化」の例が自己矛盾していた。** 文は「同じ人物、同じ番号」を約束していたが、実際には2つの異なる番号が表示されていた――これは「匿名化」で正しい図である。両方の例は今、それぞれの文と一致する。
- **新しく挿入されたプレースホルダーが、「原本を復元」の際に消えるのではなく、重なり合った文字の塊として残ることがあった。** 単色で挿入されたプレースホルダーは、これまで文字ごとに独自の出力コマンドを書いており、そのうち最初の文字のみが独自のテキスト行列を持っていた――同じ箇所を次に編集する際(例えば直後の「復元」)、残りの文字コマンドは順に最初の文字のインデックスを割り当てられ、プレースホルダーは重なり合う2つの位置に分裂していた。単色のプレースホルダーは今、そのテキスト全体に対して単一の出力コマンドを持つ。

- **修正ウィンドウで同じ墨消しまたは削除された値が2つの行に存在し、両方とも取り消し対象としてマークされた場合、2番目の行が誤って「一意でない」としてカウントされていた――値はすでに元に戻されていたにもかかわらず。** 両方の行は今、完了として扱われる。

- **「Reply-To:」の後ろの氏名が検出されるようになった。** 「Reply-To: Huber」のようなメールヘッダーでは、これまで氏名がまったく検出されなかった――言語モデルが「Reply-To:」自体を誤った独自の人物として読み、その後ろの実際の氏名を見逃していた。

- **メールヘッダー語「Reply」と「Fwd」自体は、もはや氏名として墨消しされない。** 「Fwd: Angebot von Huber」のような件名では、これまで氏名に加えてヘッダー語自体も人物として検出・墨消しされていた。

- **「Arbeitgeber: Siemens AG」は今、企業として検出され、人物としては検出されない。** ラベル「Arbeitgeber」(雇用主)の後ろにある企業名がGmbH、AG、KGのような法人形態を伴う場合、組織検出を有効にしていても人物検出のままだった――法人形態のない狭いケース(「Wollmuth und Partner」)のみが、これまで企業として検出されていた。

- **一度検出された住所は、もはや別の箇所に残ったままにならない。** ある箇所で街路住所が検出・置換された場合、
同じ住所が2番目の箇所に残ることがあった――例えばスキャン文書の読みにくいフッターで、自動文字認識がそれを不完全に読み取っていた場合などである。住所は今、氏名や企業名がすでにそうであったように、文書全体を通して一貫して削除される。

- **複数の受信者を持つメールは、処理中に静かに破損していた。** 受信者が2名以上の`.msg`メッセージは、保存の際にその内部構造の一部を失い、処理済みの結果が不完全になっていた。原因は、各受信者ごとに存在する同名の内部要素の取り違えだった。このようなメッセージは今では完全に再構築される。

- **同梱のテスト文書のうち2件は、WordとPowerPointで開けなかった。** 測定コーパスをダウンロードすると、`format_dokument.docx`では「Wordでファイルを開く際にエラーが発生しました」、`format_praesentation.pptx`では「ファイルが破損しています」と表示された。両方のファイルはMaskuroが処理する前からすでに不具合を抱えており、処理後のファイルはその不具合を引き継いでいただけだった。LibreOfficeでは両方とも問題なく開けたため、誰も気づいていなかった。

- **独自のAIをインターネット越しに使う場合、今では暗号化通信で接続される。** 独自のAIに「https://」なしの外部アドレスを入力すると(IT部門のメモによく書かれている形式)、これまでは暗号化されていない接続経由でそこに到達していた――墨消しされていないテキストが平文で送出されていた。このようなアドレスは今、「https://」経由で接続される。自社ネットワーク内のサーバーは変わらず到達可能である。サーバーが別のマシンへのリダイレクトに従う場合、アクセスキーはもはや一緒には送られない。

- **破損した画像も、今では隠れたメタデータを失うようになった。** 埋め込まれた画像が完全に開けなくなっていた場合(例えば切り詰められた写真)、これまではEXIFとGPSデータをそのまま保持していた――撮影場所と撮影者名が結果の中に見えない形で残っていた。このような画像も今では、まったく表示できない場合でも、これらのデータから解放される。

- **処理できなかった埋め込みファイルは、今では黙って一緒に渡されるのではなく、報告されるようになった。** プレゼンテーションやワークブックの中に、階層が深すぎるか開けない埋め込みオブジェクトがあった場合、これまでは何の警告もなく変更されずに結果の中に残っており――ファイルは処理済みとみなされていた。このようなケースは今、埋め込まれた旧形式ファイルと同様に、「【検証できませんでした】」という警告に表示される。

- **ダークモードのリストは、再び一貫して暗く読みやすくなった。** macOSでは、ファイルリストがほぼ黒に近い行と明るいグレーの行を交互に表示しており、修正処理の際、同じ緑・オレンジ・赤の検証値が行によって異なって見えていた。ウィンドウ、リスト、フォント、プレースホルダー、選択状態は今、共通のライト/ダークパレットから取得される。色分けされた検出結果リストも、色の下に縞模様(ゼブラストライプ)を敷かなくなった。

- **「als」を伴う職業表記が誤って氏名として墨消しされていた。**「Als Koch ist er seit vier Jahren bei uns tätig.」のような文では、氏名ではなく職業が失われていた――「als」は「der」や「die」と同様に、役割を表す表現を導入する。同じ位置にある実際の姓(例えば前に敬称がある場合)は影響を受けない。

- **表の見出しが位置番号を金額に巻き込むことがあった**(「金額も削除する」オプションが有効な場合のみ)。ある行が通貨単位で終わり(「… Einzelpreis EUR」)、次の行が数字で始まる場合、改行をまたいで誤って1つの金額として扱われていた。通貨と数字の間の区切り記号は今、同じ行内にとどまる。

- **大文字の短い略語が、文の一部全体を飲み込んだり、正しく検出された氏名の前に付着したりすることがあった。** 「DI」「AG」「KG」のような2文字の大文字語――日常的な略語であり氏名ではない――が行内にある場合、行全体が試験的に小文字化されて検索され、その略語が時々、隣接する単語(動詞も含む)を1つの疑似氏名に巻き込んでいた。今では3文字以上の大文字語のみがこの2次検証をトリガーする。「CEO」や「USB」のようなやや長い略語では、2つ目の不具合が残っていた:すでに正しく検出されていた氏名(「Schneider」)が、前置された略語を接頭辞として結果に巻き込んでいた(「CEO Schneider」)。略語は今、除外される。

- **後ろにスペースのない生年月日は未処理のまま残っていた。**「geb.」の後ろに日付の前のスペースがない場合――狭く組まれたフォームによくある形式(「geb.14.03.1988」)――Maskuroはこのフィールドを検出せず、日付は手つかずのままだった。「Geburtsdat.」や「Geb.-Dat.」といった一般的な短縮形も今では検出される。

- **スラッシュを区切り記号に使ったIBANは未処理のまま残っていた。** 電話番号(「0664/1234567」)と同様に、一部のテンプレートはIBANもスペースやハイフンの代わりにスラッシュ区切りのブロックで表記する(「AT48/3200/0000/1234/5864」)。この表記も今では検出される。

- **ハイフン、ピリオド、またはスラッシュを含むオーストリアの社会保険番号は未処理のまま残っているか、誤ってラベル付けされていた。** 2つの数字ブロックの間には、これまでスペースのみが想定されていた。「1237-010180」「1237.010180」「1237/010180」といった表記は検出されないか(スラッシュの場合は誤った種別として)検出されていた。チェックディジットは、区切り記号にかかわらず、引き続きすべての検出結果を確認する。

- **住所内の「c/o」の後ろにある氏名は、そもそも削除されていなかった。**「c/o Max Mustermann, Hauptstraße 5, 1010 Wien」は通りと地名を墨消ししていたが、その後ろの氏名は完全にそのまま残していた。氏名は今では検出される。「c/o」自体は住所の補足情報として引き続き表示される。

- **ピリオドでグループ化されたクレジットカード番号は未処理のまま残っていた。**「4111.1111.1111.1111」のような表記は検出されなかった――スペースやハイフンで区切られた番号はこれには影響を受けていなかった。チェックサムは引き続きすべての検出結果を確認する。

- **ハイフンでグループ化された税務識別番号、およびハイフンまたはピリオドを含むオーストリアのUIDは未処理のまま残っていた。** 税IDでは、スペース、スラッシュ、ピリオドはすでに対応済みだったが、ハイフンが欠けていた。UID(「ATU12345678」)では、接頭辞の後ろのハイフンとピリオドが欠けていた。税IDのチェックディジットは、引き続きすべての検出結果を確認する。

- **引用符付きのフィールド値は未処理のまま残っていた**、例えば「vorname": "Max"のようなJSON風の行である。フィールドラベル(「Vorname: …」)を通じた検出は、これまでラベルと値のいずれも引用符で囲まれていないことを前提としていた。このような行も今では検出される――YAMLリストの箇条書き記号が前置されたフィールドラベル(「- Vorname: Max」)や、コロンの前にスペースの代わりにタブがある場合も同様である。

- **メールヘッダー語「Sent」自体が氏名として墨消しされていた。**「Sent: Huber」のようなヘッダーでは、これまで「Sent」自体と実際の氏名の両方が検出されていた――「Subject」や「Betreff」のような関連ヘッダー語は、これまで常に影響を受けていなかった。「Sent」も今後は手つかずのまま残る。

- ヘッダー「Errors-To:」または「Resent-From:」の後ろにある氏名は、そのような行が平文でコピーされている場合(例えば転送メッセージや事故報告書)未検出のままだった――「Reply-To:」や「Return-Path:」とは異なり、ここでは氏名が不正確に区切られるのではなく、完全に失われていた。今では検出される。
- 同一のファイルが2回の処理で異なる結果を生むことがあった:2つの検出が、同じ長さ・同じ信頼度でまったく同じ箇所に一致した場合(例:「Sozialversicherungsnummer 1237/010180」がAT_SVNRとしても一般的な識別番号としても検出される場合)、どちらが優先されるかは偶然に左右されていた――値はどちらの場合も削除されていたが、プレースホルダーのラベルだけが変わっていた。この同点は今、常に同じ結果に解決される。
- 主語の直前にある機能名(例:「Behandelnder Arzt: Dr. …」や「Zuständiger Sachbearbeiter ist …」)が、時々それ自体が氏名であるかのように誤って一緒に墨消しされていた。隣にある実際の姓はこれに影響されない。
- 形容詞(例:「Schöne」「Lange」「Junge」)とたまたま同じ形をしている実際の姓が、別の名詞の直前にある場合(例えば「Kontaktperson: Schöne Assistentin」)、前回の修正以降、墨消しされないまま本文に残っていた――データ漏洩である。今では、実際の機能名(例:「Behandelnder」「Zuständiger」)からなる限定的なリストのみが、この形式で非氏名として扱われる。
- 複数行にまたがる氏名検出結果の末尾にある単独の姓で、形容詞(例:「Schwarz」「Kurz」「Alt」「Frisch」「Gut」「Reich」)とたまたま同じ形をしているものは、直後にコロンが続く場合検出されないまま残っていた――処理が、それを「Telefon:」のようなフィールドラベルと取り違えていた。既知の多義的な姓の閉じたリストが、今ではこれを保護する。
- 一般的なドイツ語単語とたまたま一致する単独の姓(「Gross」/「Grosse」「Gut」「Kurz」「Lang」/「Lange」)は、これまで**完全に**失われていた――「Herr Gross unterschrieb den Vertrag.」のような単純な文でも同様だった。原因は、これらの単語を含むspaCy自体のストップワードリストにあった――既知の姓の閉じたリストが、今ではこれらを破棄から保護する。
- 労働契約、消費貸借契約、保証契約、信託契約、倒産手続、および後見/世話、鑑定依頼では、姓が同時に一般的な単語である場合(例:「Bauer」)、「Auftraggeber:」「Auftragnehmer:」「Arbeitnehmer:」「Versicherter:」「Darlehensgeber:」「Darlehensnehmer:」「Bürge:」「Sicherungsgeber:」「Treuhänder:」「Treugeber:」「Insolvenzverwalter:」「Gutachter:」「Sachverständiger:」「Vormund:」「Pfleger:」といったラベルの後ろで見逃されていた――一部では名のみが検出され、一部では氏名全体が失われていた。
- 会社概要(奥付)では、姓が同時に一般的な単語である場合(例:「Bauer」)、「Geschäftsführer:」「Geschäftsführerin:」「Vertretungsberechtigt:」「Inhaber:」「Inhaberin:」といったラベルの後ろで見逃されていた――「Geschäftsführer:」/「Inhaber:」では氏名全体が失われ、「Vertretungsberechtigt:」では名のみが検出されていた。
- ラベルが単独でその行にあり、ジェンダーニュートラルなコロン形式を伴う連絡先ブロック(「Ansprechpartner:in」、その下に氏名)は、**完全に**見逃されていた――コロンがフィールド区切りとして読まれ、「in」が(破棄される)フィールド値として扱われ、次の行にある実際の氏名は二度と処理対象にならなかった。アスタリスク形式(「Ansprechpartner*in」)はこれに影響されなかった。
- 氏名とラベルが同じコロン付きジェンダー形式で**1行**にある場合(「Ansprechpartner:in Anna Berger」)、プレースホルダーは氏名だけを削除するのではなく、単語「in」も置換に巻き込んでいた――氏名自体は引き続き完全に検出されていた。
- 人物用の列見出しの下にある表列内の氏名(例:「Bauer Anna 03.05.1985」の上の「Name Vorname Geburtsdatum」――給与明細のような)は、列の間にスペース1つしかなく、かつどの行も見出し番号で始まっていない場合、完全に見逃されていた――実際のPDFテキスト抽出がこのような行を出力する、まさにその形式である。
- コロンの前に発言者名があるチャットまたは会議議事録(例:「Bauer 🙂: Ich stimme dem Vorschlag zu.」)では、氏名と姓が同時に一般的な単語(「Bauer」「Koch」「Schneider」など)である場合、名前とコロンの間に反応記号があると氏名が完全に未検出のままだった――議事録全体が、1人の発言者も検出されないまま終わることもあった。
- 同じ発言者行の不具合は、コロンの前にある他の中間記号でも発生していた:括弧内のステータス補足(「Bauer (Vorsitz): …」「Bauer (abwesend): …」)、角括弧内の時刻(「Bauer [14:32]: …」)、氏名に直接付いた脚注記号(「Bauer*: …」)。これらの場合も、姓が同時に一般的な単語であると、発言者は完全に未検出のままだった。
- すでに検出済みの人物が、同じメッセージに添付された議事録やログの抜粋(例えばサポートチケット)の中に、「vorname.nachname」形式(小文字、スペースなし、ピリオドで連結)のユーザー名として追加で存在する場合、同じ氏名が本文ではすでに墨消しされていたにもかかわらず、この平文のユーザー名は読める状態のまま残っていた。
- 同じユーザー名の不具合は、ピリオドの代わりにアンダースコアを使う場合(「vorname_nachname」)にも存在していた――議事録やログの抜粋で同様に一般的な形式である。
- そして逆順の場合(「nachname.vorname」または「nachname_vorname」)もユーザー名は読める状態のまま残っていた――一部のシステムでは、ログのユーザー名で姓を名の前に置く。
- 他の情報が隣にない場合、死亡日は未検出のままだった(「Herr Bauer ist am 12.03.1985 verstorben」)――これまでこれに対する専用の検出機能が一切存在せず、汎用の日付検出はこの標準の閾値では作動しない。
- 文が分詞ではなく動詞形を使っている場合(「Frau Meier verstarb am 12.03.1985」「Er starb am 12.03.1985」)も、死亡日は未検出のままだった――これまでは「ist … verstorben」/「ist … gestorben」のみが対応していた。
- 結婚日は、どのような形式であっても未検出のままだった(「Eheschließung am 12.03.2010」「Hochzeitsdatum: 12.03.2010」「Herr und Frau Bauer heirateten am 12.03.2010」)――これまでこれに対する専用の検出機能が一切存在せず、汎用の日付検出はこの標準の閾値では作動しない。

- **修正エディタで、たった今挿入したばかりのプレースホルダーの上にもう一つ枠を重ねると、赤い文字の残骸が残ることがあった**、例えば「[BEG1]」ではなく「[G」のように――何の警告もなく、というのもその残骸はもはや秘匿情報の一部ではなく(それは最初の操作ですでに削除されていた)、プレースホルダー自体の一部でしかなかったからである。原因は色付けの仕組みにあった:新しく挿入されたプレースホルダーは、単色設定の場合でも文字単位でファイルに書き込まれていた――同じ箇所に対する後続の枠は、それによって一貫した文字列を見つけられなくなり、位置を特定できなくなっていた。単色のプレースホルダーは今、自動処理が常にそうしていたように、ストリーム内で1つのまとまりとして存在する。グラデーションや虹色のテキストのみが引き続き個別の文字を必要とする。組み込みの照合は、プレースホルダーの正確な文字列がもはや存在しない場合でも、このような残骸を今では検出する。
- 段階的な見出し番号を持つ番号付き氏名リスト(「1.1 Max Mustermann」「1.2 Huber Franz」…)は、実際のアウトラインや位置リストのみを保護すべき同じ抑制策により、すべての氏名を失っていた――リストの上に列見出しがないため、氏名を保護できる手がかりが何もなかった。
- システムログの英語のログインメッセージ内の氏名(「Accepted password for Max Mustermann from 10.0.0.5 port 51000 ssh2」)は検出されなかった――ドイツ語の言語モデルは、その前に「invalid user」がある場合にのみ検出でき、それ以外は見逃していた。このようなログの抜粋は、しばしば事故報告書にそのまま添付される。IPアドレスの前の「for」の後ろにある氏名は、今では確実に検出される。
- 口座明細や記帳簿のSEPA委任状参照番号内にある支払義務者の氏名(例:「MREF+Mustermann Klaus+SVWZ+Miete August」)は未処理のまま残っていた――スペースも文構造もなく、「+」で区切られた大文字フィールドのみで、そこでよく使われる「姓 名」の順序でも、検出は偶然にも見つけられなかった。今では検出される。
- 住所テーブルの最初の行にある通りと番地(例:「Nachname | Vorname | Straße | PLZ | Ort」)は未処理のまま残っていた――言語モデルはそこで、複数列にまたがる間違った、より長い地名を推測し、それが正しく短い住所の検出結果を押しのけていた。今では検出される。
- 同じ漏洩は、区切り記号として「|」や「;」の代わりにタブを使う場合にも発生していた――そこでは住所が単に失われるのではなく、完全に消えていた。今では検出される。
- 通り名と番地の直後に、スペースなしでカンマ付きの郵便番号が続く場合(例:「Bahnhofstrasse 12,80331 München」、カンマ区切りの表列にあるような形式)、通りが未処理のまま残っていた――カンマが数量の小数点のように見えたため、通りはそもそもパターンにとって住所とみなされなかった。今では検出される。
- 通り名と番地の直後に、カンマなしで地名接頭辞「St.」(聖)が続く場合(例:「Hauptstraße 5 St. Pölten」、郵便番号が前置されていない便箋のような形式)、通りが未処理のまま残っていた――「St.」が個数の単位のように見えたため、通りはそもそもパターンにとって住所とみなされなかった。今では検出される。
- 番地の後ろにあるドア/階段番号の補足(例:「Lerchenfelder Gürtel 43/12」)は、その直後に単位記号とたまたま一致する単一の文字が続く場合(例:「h」=時間)、そのまま読める状態で残っていた――住所は補足なしで番地までしか処理されず、完全に処理されるか、まったく処理されないかのどちらかにならなかった。
- 名の前に職業名と同形の姓がある件名行(「Betreff: Bauer Anna」「Betreff: Bauer, Anna」)は、これまで完全に未検出のままだった――文書中で、前に完全な文が先行する場合でも同様だった。今では検出される。
- ブロック間にスペース、ピリオド、ハイフンを含むドイツの税番号(例:「Steuernummer: 30 815 08153」または「30.815.08153」)は、これまで未検出のままだった――スラッシュ表記のみが検出されていた。今では検出される。
- 医療フィールドラベル(「Patient:」「Hausarzt:」「Behandelnder Arzt:」「Überweisender Arzt:」およびその女性形)の後ろにある氏名は、姓が同時に一般的なドイツ語単語である場合(例:「Patient: Bauer Thomas」)、これまで未検出のままだった。今では検出される。
- 単独行のフィールドラベル「Zahnarzt」(例:「Zahnarzt」、その下に「Huber Franz」)の後ろにある氏名は、これまで未検出のままだった――名も姓もである。「Zahnärztin」と単純な「Arzt」形式はこれに影響されなかった。今では検出される。
- 「Herr」/「Frau」の後ろにあり、「zur Kenntnisnahme」「zur Unterschrift」「zur Weiterleitung」のような官庁用語が続く姓は、これまで検出範囲が広すぎ、その用語まで氏名の検出結果に巻き込んでいた――「Frau Petra Klein zur Vertretung in allen Angelegenheiten」から「Petra Klein zur Vertretung」が置換され、文の残りは文法的に不完全なまま残っていた。「von der Leyen」や「zu Guttenberg」のような実際の貴族称号はこれに影響されない。
- 同じ官庁用語の過剰検出は、メールの「To:」ヘッダー内の氏名、登録コード(C.1/C.1.1/C.1.2)、運転免許証コード、括弧付きフォームフィールド(「[Vorname]: …」)、ピリオドなしの結びの挨拶の背後にも潜んでいた――これらすべての箇所で、「zur」/「von」などが、続く「zur Unterschrift」や「zur Vertretung」のような用語を検出結果に巻き込み、単なる助詞自体が氏名の残骸として結果に残ることさえあった。ここでも実際の貴族称号は完全に保持される。
- ラベルの後ろにある学籍番号は、これまでまったく検出されなかった――「Matrikelnummer 7654321」は、識別番号としても言語モデルによっても検出をすり抜けていた。数字だけでは認識可能な形式を持たないためである。
- 参加者番号についても同様だった――「Teilnehmernummer 4471829」は、識別番号としても言語モデルによっても検出をすり抜けていた。
- 履歴書では、見出し「Persönliche Daten」の下にある氏名が、敬称なしで「姓 名」の形式で直下にある場合、しばしば完全または部分的に検出をすり抜けていた。
- 見出し「Kontaktdaten」についても同様だった――そこでは氏名は部分的にではなく完全に検出をすり抜けていた。
- 「Name, Vorname」を1列に統合した住民登録証明書や申請リスト(住民登録の表記方式、値は例えば1セル内の「Mustermann, Max」)では、生年月日のような別の列が続く場合、氏名が完全に検出をすり抜けていた。
- 身分証明書や住民登録証明書によく見られる形式「Geburtsdatum/-ort: 22.07.1978 / Rostock」の生年月日は検出されなかった――カンマ形式「Geburtsdatum, Geburtsort: …」のみが検出対象だった。
- 「Bürgerservice」と「Bürgerbüro」が、特に箇条書き区切りとしてのダッシュの後ろで(例えば「Wenden Sie sich an das Bürgerservice – Bürgerbüro …」)、誤って地名として墨消しされることがあった。
- 改行によって途中で分断されたラベル付き電話番号(例えば狭い便箋の列やPDFテキスト抽出の列幅による分断:「Telefon: 0176 12\n34567」)は、部分的にしか墨消しされないことがあった――改行の後ろの残りは読める状態のまま残っていた。
- 改行によって途中で分断されたラベル付き識別番号(顧客番号、会員番号、契約番号など、例えば狭い列からの「Kundennummer: K903\n944」)は、部分的にしか墨消しされなかった――改行の後ろの残りは読める状態のまま残っていた。
- カンマの後ろに職業名が続く、学術称号付きの氏名(例えば「Dipl.-Ing. Sabine Roth, Projektleiterin」)は、完全に未保護のまま残っていた――その行は表の列見出しのように見え、誤って事実的な内容として破棄されていた。
- 氏名の前にある「Dr.-Ing.」(一般的なドイツの工学博士号)は、マスクされる人物値に含まれず、読める状態のまま残っていた――「Dipl.-Ing.」と同じハイフンの落とし穴である。
- 氏名の前にある「Dipl.-Kfm.」「Dipl.-Kffr.」「Dipl.-Psych.」(経営学士/経営学修士/心理学修士)は、マスクされる人物値に含まれず、読める状態のまま残っていた――「Dipl.-Ing.」や「Dr.-Ing.」と同じハイフンの落とし穴である。
- コロンの代わりにピリオドを使うCisco表記のMACアドレス(例:「aabb.ccdd.eeff」、スイッチのログやサポートチケットに出力される形式)はまったく検出されず、読める状態のまま残っていた。
- 「Familie」(家族)の後ろにある姓(例:「Die Familie Gruber unterschreibt den Vertrag」)は、文構造によっては検出されず、読める状態のまま残っていた――前に貴族称号がある場合(「Familie von der Leyen」)も同様だった。

- 郵便番号+地名と通り名+番地の間に区切り記号のないクロアチアの住所(例:「10000 Zagreb Ulica Ivana Lučića 5」)では、番地が未処理のまま残っていた。

- ラベル「Kontaktinis asmuo」(例:「Kontaktinis asmuo: Vilkas Jonas」)を持つリトアニア語の連絡先情報では、姓が同時に一般的な名詞である場合(Vilkas=「狼」、Vanagas=「鷹」)、姓が検出されなかった。

- 特にラベルのないデンマーク語のフォームフィールド内の出生国または居住国(例:「Fødeland: Tyskland」または「Bopæl: Tyskland」)が検出されなかった。

- 特にラベルのないルーマニア語のフォームフィールド内の出生国または居住国(例:「Țara: Germania」または「Țara de reședință: Franța」)が検出されなかった。

- リトアニア語のフィールドラベル「Darbdavys:」または「Įmonės pavadinimas:」(雇用主/企業)の下にある企業名が検出されなかった。

- ロシア語のフィールドラベル「Работодатель:」または「Наименование организации:」(雇用主/企業)の下にある企業名が検出されなかった。

- ルーマニア語の月名を用いた書き出し日付(例:「31 decembrie 2024」)が検出されなかった。

- 略語「szül.」の後ろにあるハンガリー語の旧姓(例:「Nagy Éva (szül. Kovács)」)が検出されず、読める状態のまま残っていた。

- 保存済みのHTMLプロフィールページ(またはウェブページが添付されたメール)は、本名がOpen Graphのプロフィールフィールド`profile:first_name`/`profile:last_name`/`profile:username`にのみ記載されている場合、未処理のまま残ることがあった――これらは`og:title`のように説明的にではなく、氏名を分解した形で保持しており、今ではこちらも処理されるようになった。

- 配送不能通知(バウンス/NDR)は、元々配送できなかったメールのヘッダー(送信者、受信者、件名)を、独自の3番目の添付部分に持つことが多かった――この部分は処理後のファイルでも完全に手つかずのまま残っていた。この部分は今では、配送レポートの他の部分と同様に処理される。

- Word内の保護された範囲(編集の制限 → 例外、`w:permStart`)に個別に名前が指定された編集者は、同じ氏名が本文ではすでに処理されていたとしても、平文のまま残っていた。今ではこちらも削除される。

## 0.10.42-alpha.20260827 – 2026年8月27日

### 新機能

- **名前付きの検出プロファイルにより、さまざまな作業ケースをワンタッチで
  切り替えられます。** *設定→検出→何を削除するか*で、現在のカテゴリー・
  種類の選択を保存し、選択欄からすぐに再適用できます。固定プロファイル
  *標準*は、これまでの出荷時設定に相当し、削除できません。プロファイルが
  変更するのは何が削除されるかだけです。言語、出力方式、検出の深さ、
  独自の語句や検索パターンは影響を受けません。

- **結果の種類が、現在は浄化の直前に直接選択できます。** メインウィンドウの
  共通選択欄が、Maskuroが読める形のプレースホルダーを挿入するか、黒塗り
  するか、跡形なく削除するかを、バッチ全体に対して決定します。設定
  ウィンドウにあったPDF用とOffice用の2つの独立した項目は廃止されました。
  これにより、この重要な決定が目に見えるようになり、複数形式が混在した
  バッチで意図せずばらついてしまうことがなくなりました。ガイド付き
  ツアーは、最初の浄化の前にこの新しい選択項目を説明します。

- **テーマと透かしが、希望に応じて完成したPDFを明確に識別できるように
  します。** 12種類の全体的な外観が、代替テキストと黒塗り領域を互いに
  調和させます。新たにPrideと、春・夏・秋・冬が加わりました。
  *機密文書*は、対角線上の「TOP SECRET」をそのまま持ち込みます。それとは
  別に、自由な識別文言や、独自の画像・アイコン・SVGを、色と不透明度と
  ともに選択できます。インポートされた画像はメタデータなしで埋め込まれ、
  元ファイルが移動しても引き続き利用可能です。修正時には、Maskuroは
  以前の透かしを重ねて配置するのではなく、置き換えます。テキストの透かし
  はPDFの最後のレイヤーとして、明るい輪郭付きで描画され、暗い画像や
  密集したテキストの上でも見えるようにします。修正エディタはMaskuroの
  透かしを完全に無視し、その文言を黒塗り候補として提示しなくなりました。

- **独自の出力テーマを保存して共有できます。** 代替テキスト、黒塗り、
  透かしの現在の組み合わせに名前を付け、設定内に保持し、平文を含まない
  JSONとしてエクスポート・インポートできます。モノクロ印刷プレビューは
  コントラストが弱い箇所を警告します。任意の成功時の紙吹雪演出は、完全に
  画面表示だけにとどまります。

- **最終的なエクスポート確認と、説明付きの検査上乗せが、表示処理の
  仕上げを行います。** 最終保存の前に、Maskuroは値が既知のすべての
  PDF箇所を、テキストレイヤーとレンダリングされた画素の両方で再度
  比較します。警告はページと座標のみを示します。エディタの*なぜこれが
  隠されているのか?*には、カテゴリー、検出経路、安全マージンが表示
  されますが、削除された平文は決して表示されず、最終文書にも決して
  現れません。

- **黒塗りバーが、現在はきれいに装飾できるようになりました。** *設定→
  外観*には、色の初期設定、自由な色選択、グラデーション、レインボー、
  ストライプ、水玉、花、星、ハート、肉球、雲、稲妻、コーヒー豆、アヒル、
  太陽、葉、雪の結晶、紙・蛍光ペン・粘着テープ・再現可能なランダム
  パターンが即時プレビュー付きで用意されています。代替テキストには、
  選択に応じて色、グラデーション、レインボー、ピル型、ラベルが付けられ
  ます。カテゴリー別の色は、氏名・住所・連絡先・医療情報を区別します。
  PDFは装飾を完全に反映します。Word、PowerPoint、OpenDocument、HTMLは、
  選択された不透明な基本色を使用します。これにより保護の内容自体は
  変わりません。Maskuroは機密情報をまず削除し、その空いた箇所に色や
  模様を後から描画します。

- **MaskuroがLinux向けに再び提供されます――AppImage、DEB、RPM、
  ポータブルアーカイブとしてです。** DEBとRPMは、プログラム項目、
  ファイルの関連付け、ターミナルコマンド、アイコンをシステムに登録
  します。AppImageはインストールなしで動作します。更新は、既存のDEBまたは
  RPMインストールに対しては同じパッケージ形式のまま行われ、それ以外の
  場合はAppImageが優先されます。

- **目視確認が、通常のPDFテキストを新しいヒットとして二重に提示しなく
  なりました。** 最終的なOCR確認と、表示ページの安全な再構築処理は
  引き続き完全に有効です。ただし、初期設定では、ページテキストと個別
  画像確認がまだ読み取っていない領域のみが新しい発見源として扱われます。
  これにより、製品行が、単に2回目のOCR読み取り結果が異なっただけの
  理由で新しい氏名や企業名として扱われることがなくなります。表示される
  テキスト全体について、引き続き2つの独立した判定を得たい場合は、設定で
  *表示されているPDFページ全体を再度項目確認する*を有効にしてください。

- **PDFを、連続・単ページ・見開きページで表示できます。** 3つのコンパクト
  な表示アイコンが、「幅」と「ページ」のすぐ下に並びます。連続表示は
  用紙の端で次のページへスクロールします。単ページ表示はマウスホイールを
  現在の用紙に留めます。見開き表示は見開きを表示し、クリックした用紙を
  編集可能にし、前後の移動は見開き単位で行われます。ページの縮小表示と
  比較ルーペは、さらに大幅に幅の狭くなった左側の基本カラムで開き、作業
  ページにより多くの余地を残します。

- **AI段階が何を行ったかが、現在は確認できます。** 各処理の後、ファイルごと
  に「詳細」の下に、「AI段階:境界事例を12件検査、3件破棄」のような1行が
  表示されます。何も変更しなかった場合も、その旨が表示されます。これ
  まで、この最もコストのかかる段階は完全に沈黙していました。そもそも
  問い合わせが行われたかどうかさえ、外部からは分かりませんでした。

  さらに詳細が必要な場合は、「設定→AI」で*すべてのAIへの問い合わせを
  記録に残す*を有効にします。すると、記録ファイルには問い合わせごとに
  サイズ、所要時間、発見件数が記録され、さらに接続先の件数上限による
  待機時間も記録されます。その隣の「記録ファイルを表示」ボタンは
  フォルダを開きます――このフォルダはアプリケーションデータ
  ディレクトリ内にあり、Windowsでは隠しフォルダのため、自力で見つける
  人はいません。このファイルにはサイズのみが記録され、お客様の文書の
  テキストが記録されることは決してありません。

- **Maskuroは、接続先のAIサービスがリクエスト数を制限していることを
  検出します。** ホスティングされたサービスの多くは、1分あたりわずか
  数件のリクエストしか許可していません――4件というのも珍しくありません。
  超過分は拒否されるのではなく待機させられ、1回の応答にかかる時間が
  2秒から40秒になります。これまでは、あたかもモデルが遅いように見えて
  いました。現在はMaskuroがサービスの応答からその上限を読み取り、
  受け入れられる以上の同時問い合わせを送らず、「接続確認」にその上限を
  表示し、所要時間の見積もりにも反映させます。

- **ページプレビューが、お使いのWord、Excel、PowerPointを利用するように
  なり、約6倍高速になりました。** これまでは、多くのオフィスパソコンには
  導入されていないLibreOfficeが必要でした。持っていない人は、外部
  インストールを要求するボタンを目にしていました。現在は、Microsoft
  Officeがインストールされていれば自動的に使用されます――設定不要、
  ダウンロード不要、チェックを入れる必要もありません。LibreOfficeは
  引き続き2番目の手段として、そしてOpenDocumentファイルではむしろ最初の
  手段として残ります。一方が失敗すれば、もう一方が試されます。

  違いは特に作業中に実感できます。置換のたびにページが再構成されますが、
  これがOffice経由では約0.5秒で済み、これまでの3秒からの短縮です。
  文書を最初に表示する際は引き続き数秒かかりますが、その後は待ち時間
  なく操作に追従します。

  お使いの開いているWord自体には手を加えません。Maskuroは独自の見えない
  セッションを起動し、ファイルを読み取り専用で開き、マクロを無効化し、
  修正ウィンドウが閉じられるとすべてを終了します。パスワードで保護
  されたファイルは、見えないダイアログで固まる代わりに、拒否されます。

- **初回セットアップが、現在は顔・コード・署名についても尋ね、不足して
  いるものをまとめて一度に取得します。** 高度な検出機能の隣に、最初の
  ページで3つの画像用スイッチが並びます。顔領域を判別不能にする、
  バーコード・QRコードを判別不能にする、PDFページ上の手書き署名を黒塗り
  する、です。PDFという制限が、そのチェックのすぐ下に明示されます。
  Office文書は自動的には署名を検索されません。チェックの下には、
  「次へ」をクリックすると何メガバイトかかるかが表示されます。読み込みは
  その後、複数のダイアログを順に表示するのではなく、**1つ**のウィンドウ
  で**1つ**の全体進捗バーとともに行われます。中止すると処理全体が終了し、
  中途半端な状態が残ることはありません。何も望まない場合は、チェックを
  外せば何も読み込まれません。

- **プレビューは、確認の必要性に応じて絞り込んだり、種類ごとに折り
  たたんだりできます。** 一覧の上に*十分裏付けのある項目を隠す*という
  スライダーがあります。右に動かすほど、緑から赤の方向に向かって多く
  非表示になります。一番右では、プログラムが単独で推測した項目だけが
  残ります。ある種類の見出しをクリックすると、その種類が折りたたまれ
  ます。どちらも読みやすくするための機能であり、選択そのものではあり
  ません――非表示または折りたたまれた項目も、チェックは入ったままで
  置換対象です。現在何件がそれに該当するかは、スライダーの下に表示
  されます。短い一覧ではスライダーは表示されません。2列表示への切り替え
  は現在、*二度と表示しない*のスイッチも保持するようになりました。

- **画像一覧は、各処理の前に自動的に開くことができます。** 画像1枚ずつ
  を自分で判断したい場合は、「画像」の下にある新しいチェック*処理前に
  毎回個別に指定する*を入れます。すると、プレビュー付きの一覧が浄化の
  たびに自動的に表示され、毎回「個別に指定…」を自分でクリックする必要が
  なくなります。この一覧を中止すると、浄化自体も行われません。選択した
  ファイルのいずれにも画像が含まれない場合は、何も表示されません。この
  チェックは初期設定ではオフです。
- **MaskuroはPDFページ上の手書き署名を見つけ、画素から削除します。**
  これまでは、浄化された文書の下に署名がそのまま残っていました――文字
  認識は活字を読み取るため、読み取れないものは置換されません。この検索は
  独立したスイッチで、一度だけ再取得が必要な検出モデルを使用します。

  測定では、100件の署名のうち約84件を発見し、そのうち約4/5を隠します。
  これは補助であり、保証ではありません。処理のたびに報告書には何件
  発見されたかが表示されます――署名がなかった場合もそのように表示され
  ます。というのも、それは実際に署名がなかったことを意味する場合も、
  見落とされたことを意味する場合もあるためです。署名のない72件の実在
  する業務文書のページでは、存在しない署名を作り出すことはありません
  でした。

  **手書きで描かれた**署名は発見されますが、削除はされません。それは
  線から成り、画素から成るものではないため、その上にバーを置いても
  ただの覆いにすぎず、下の線はそのまま残ってしまうためです。こうした
  箇所はカウントされ、名前が挙げられます。修正ウィンドウで自分で黒塗り
  できるようにするためです。

  Word、Excel、PowerPoint、OpenDocumentのファイルは、自動的には署名を
  検索されません。画面表示、初回セットアップ、モデルのダウンロード、
  コマンドライン、マニュアルは、現在この制限を明示的に述べています。

- **ガイド付きツアーが、現在はプレビュー――お客様が判断を下すウィンドウ
  ――も案内します。** 練習用文書では、プレビューを普段はオフにしている
  場合でも自動的に開きます(お客様自身の設定はそのまま維持されます)。
  色が何を意味するか、各行に1つの質問しかない理由――そもそもそこに人物が
  いるかどうか――、そして「二度と表示しない」が何の役に立つかが説明され
  ます。色については、十分裏付けのある行(たいていIBAN――案内文が挙げる
  緑色の例)に最初のスポットライトが当たり、その後、最も裏付けの弱い行に
  当たります。そこでは、説明の途中でも実際にクリックできます。チェックを
  外すと、その値は文書内にそのまま残ります。一覧が長い場合、案内のために
  ウィンドウはより大きく開き、説明が行の上に重ならないようにします。
  ウィンドウが2回目に開いた場合、ツアーはその理由も説明します。完成した
  ページが画像として再度読み取られ、その際に氏名のように見える断片が
  生じるためです。

- **エディタは、初回起動時に大きく開きます。** 原本、結果、ツールバー、
  ヒット一覧が並んで表示されますが、これまでの初期サイズでは余地が
  足りませんでした。ウィンドウを小さくした場合は、次回もそのサイズが
  維持されます――誰にも強制的に上書きされることはありません。

- **プレースホルダーをダブルクリックすると復元されます**――Word、Excel、
  PowerPoint、OpenDocument、テキスト、メール、HTMLでです。複数の
  プレースホルダーにまたがってドラッグし「選択箇所を復元」を選ぶと、
  その中に含まれるすべてが一度に復元されます。角括弧を正確に狙う必要は
  もうありません。匿名化の際に複数の異なる値を表しているプレースホルダー
  はこれの対象外です――それらはカウントされ、名前が挙げられます。推測は
  されません。

- **マニュアルに「置換前のプレビュー」という章ができました。** この
  ウィンドウは初期設定でオンになっており、お客様が判断を下す唯一の場所
  です――マニュアルにはこれまで従属節としてしか書かれていませんでした。
  現在は、チェックが何を意味するか(**すべて**の該当箇所に適用され、
  表示された箇所だけではありません)、なぜ各行に1つの質問しかないのか、
  「二度と表示しない」が恒久的に何をもたらすのか、なぜPDFではウィンドウ
  が2回目に開くことがあるのかが記載されています。18言語すべてに反映
  されており、設定一覧にもこのスイッチが現在記載されています。

### 変更

- **「置換された値」パネルに色のスライダーが付き、学習モードはそこから
  なくなりました。** 値が8件を超えると、一覧の上にプレビューウィンドウと
  同じスライダーが表示されます。*十分裏付けのある項目を隠す*は、実際に
  確認が必要なものだけに表示を絞り込みます。文書自体には影響しません。
  何件の行が何件中表示されているかはその下に示されます――検索欄と
  スライダーは合わせてカウントされます。*学習モード*のチェックはパネル
  から消えました。引き続き*ツール*メニューとツールバーにあります。

- **「置換された値」パネルは、現在は文書と同じ色を表示します。** その中の
  各行は、文書内の箇所やプレビューでの値と同じ背景色になっています。
  赤は「単独で推測された、まずここから確認する価値がある」を意味し、
  緑は「名前付きパターンで検出された」ことを意味します。各種類の中では、
  最も不確実なものが上に表示されます。つまり、一覧を上から下へ処理して
  いけば、最も重要なものを最初に確認したことになります。これまでは
  すべて同じ明るさで表示され、アルファベット順に並んでいました。

- **学習モードは、出荷時にはオフです。** 修正ウィンドウで修正を行うと、
  これまではプログラムが自発的に、それを独自ルールにするかどうか尋ねて
  いました。この質問は作業の真っ最中に現れます。それを望んでいない人に
  とっては、これは中断でしかありません。ルールが欲しい人は、ツールバーの
  *学習モード*ボタンをオンにします――その選択は以後、両方向で恒久的に
  維持されます。

### 修正

- **エクスポートされたルールファイルは、現在は明示的に保護が必要な
  ものとして表示されます。** 独自の語句や例外がそこに平文で含まれる
  可能性があります。さらに、推定値を確認するために使われるハッシュの
  ソルトが含まれる場合もあります。そのため、エクスポートが成功すると
  警告が表示され、このファイルを保護し、正当な受信者にのみ意図的に
  渡すよう求めます。

- **最終安全確認は、現在は自分自身が生成したプレースホルダーを理由に
  浄化済みのオフィスファイルを差し止めなくなりました。** 「SVNR」の
  ような種類の略号は`[SVNR1]`の中にも含まれます。これまでは、それが
  平文の残りかもしれないと見なされ、完成したファイルが破棄されて
  いました。同時に、電話番号やIBANについても、Officeが同じ項目を目に
  見える空白なしで参照や埋め込みファイルの中に格納している箇所まで
  現在は追跡されるようになりました。

- **Word、Excel、PowerPoint、OpenDocumentでは、後から発見されたフィールド
  のコピーが残らなくなりました。** ある値が副次的な格納領域や埋め込み
  オフィスファイルで初めて検出された場合、その後の対象を絞った追跡処理
  が、それ以前に読み取り済みの表示・非表示のコピーも整理します。すでに
  生成された参照用プレースホルダーは、その際に再度置換されることは
  ありません。

- **Wordの選択リストを個別に復元する際、隣接する選択項目がもう無断で
  一緒に戻ってくることはなくなりました。** 完全な原本の段落は、その
  属性にも未処理のプレースホルダーが残っていない場合にのみ取り込まれ
  ます。

- **読み取りにくいスキャンで、関連する項目の欠落が減りました。** 敬称と
  2部構成の氏名を含む代替のOCR読み取り結果は保持されます。通り名の断片、
  番地、郵便番号・地名は、それらが隣接するOCRブロックにまたがって分断
  されていても、住所行全体を一緒に保護します。その隣にある請求・品目
  フィールドやイベント行は、これに巻き込まれません。「geboren」の後ろで
  複数のOCR単語や句読点に分断された有効な日付も、同様に完全に判別不能
  にされます。

- **成功時の紙吹雪演出が、エディタの自動オープン時にも見えるように
  なりました。** 紙片は、ウィンドウ上端から降るのではなく、*浄化*ボタン
  から直接噴き出します。エディタは、最初の850ミリ秒の短い噴出だけを
  待ってから自動的に開きます。紙吹雪が無効の場合、遅延は引き続き
  ありません。

- **表示アイコンにマウスを重ねた際、ページカウンターとズームバーが
  もう行ったり来たりしなくなりました。** ステータスバーの空き領域を、
  Qtがそこにアイコンのヒントが表示されるたびに再配分していました。両方の
  操作グループは現在、ホバー時にも本来の幅と固定された位置を維持し
  ます。

- **接続されたAIサーバーの速度測定が、常に失敗していました**――自社製
  AIが存在して以来、どのサーバーでもです。狭い応答上限で問い合わせを
  行い、その後、それによって切り詰められた応答を読み取ろうとしていま
  した。これは失敗せざるを得ず、「測定されていません」と記録されて
  いました。その影響はあちこちに現れていました。所要時間の見積もりは、
  お客様のサーバーを、オフィスパソコン上の同梱モデルの速度で計算して
  おり、設定画面には常に「速度はまだ測定されていません」と表示されて
  いました。現在は、サーバーが生成した量を基準に測定され、応答の内容を
  基準にすることはありません。

- **「最大検出(AI)――低速」という表示が、事実と異なる場合にも表示
  されていました。** 表示文言と説明は、オフィスパソコン上の同梱モデル
  について述べていました――「このパソコン上の言語モデル」「大きな
  文書では最大1時間」。独自のAIサーバーを接続している人は、そこで
  2つの誤りを目にしていました。計算は自分のパソコン上では行われず、
  応答は時間ではなく秒単位で返ってきます。両方とも現在は測定結果から
  取得されます。測定結果がない場合、アプリケーションはもう何も主張
  せず、まだ測定されていない旨を伝えます。

- **復元機能が、現在はドラッグで選択した範囲にも作用します。** 複数の
  プレースホルダーにまたがってドラッグし、*選択箇所を復元*を押そうと
  した人は、ボタンがグレーアウトしているのを目にしていました。これは、
  選択範囲が**正確に**1つのプレースホルダーである場合にのみ有効になって
  いたためです――段落にまたがってドラッグした場合、それに該当することは
  決してありません。その背後の処理経路自体はすでに存在していましたが、
  誰もそこにたどり着けませんでした。現在は範囲を選択するだけで済み、
  その中のすべてのプレースホルダーが一度に復元されます。

- **比較ルーペが開いている状態で、復元処理がクラッシュしていました。**
  ルーペは、原本と連動させるためにマウスカーソルの下の位置を記憶して
  います。復元後の再読み込みの際、この位置をテキスト表示側が処理でき
  ない形式で返していました――そして、こうしたエラーが表示処理の
  真っ最中に発生するとプログラムが終了してしまうため、復元操作が
  クラッシュに変わっていました。ルーペは初期状態で開いているため、
  これは通常の操作でも発生していました。

- **復元後に、表示が文書の先頭に飛ばなくなりました。** 長い文書では、
  操作のたびに、作業していた箇所が見えなくなっていました。現在は、
  それ以前に上部にあった段落がそのまま上部に留まります。

- **LibreOfficeがない場合、ページプレビューは単に機能しないと表示する
  のではなく、その理由を伝えるようになりました。** *ページプレビュー*
  と*PDFとして黒塗り*の2つのボタンはロックされており、ツールチップには
  LibreOfficeが見つからないとしか表示されていませんでした。そこへの
  導線はアプリケーション内のどこにもありませんでした。現在は、クリック
  すると無料でオープンソースのLibreOfficeへの入手方法を示す案内が
  表示されます。マニュアルとFAQのこの箇所は誤っていました――アプリ
  ケーションが提供していない追加取得用の部品の存在を告知していました。

- **提供前に、完成したファイルを最後にもう一度、完全に検索します――
  現在はWord、Excel、PowerPoint、LibreOffice、メール、HTML、テキスト
  でもです。** これまではPDFだけがこの最終確認を受けていました。それ
  以前のすべての検査は、あらかじめ誰かが指定した箇所を確認するもの
  です。誰も想定していなかった格納場所は、そのため誰も検査しません。
  最後に、Maskuroは書き出されたファイル全体を、置換した対象すべてに
  ついて、単純に検索するようになりました。パッケージのあらゆる部分が
  対象です。何かが残っていた場合、結果は**生成されず**、通知にはその
  値が表示されます。浄化されたとみなされる文書は、文書が存在しない
  よりも悪い結果です。

- **`<script>`や`<style>`内にある氏名が、現在は通知されるようになり
  ました。** どちらも引き続き変更されません――そこにはプログラム
  コードが書かれており、識別子の途中での置換はウェブページを壊れた
  ウェブページにしてしまうためです。ただし、これまではその旨が伝えられて
  おらず、それが問題でした。スタイルルール`content: "Anna Musterfrau"`
  は受信者の画面に**目に見える形で**表示されるにもかかわらず、結果には
  そのまま残っており、プログラムはそのページを浄化済みと通知していま
  した。

- **設定画面で、追加モデルを再び読み込み・削除できるようになりました。**
  「高度な検出」と「最大検出(AI)」の隣にあるボタンは、押すとモデルを
  取得する代わりにエラー報告ウィンドウで終わっていました。もう1つの
  経路――検出設定内のチェックが自動的にモデルを問い合わせる方式――は、
  この問題の影響を受けていませんでした。

- **表計算のシート名や範囲名に含まれる氏名が、現在は通知されるように
  なりました。** シート名は下部のタブに、名前付き範囲の名前は名前
  ボックスと、それを使用するすべての数式に表示されます。どちらも
  引き続き置換はされません――数式はそれらを介して参照しており、参照
  エラーだらけのブックは誰の役にも立たないためです――ただし、現在は
  その旨が表示されます。これまでは、Excelブックのシート名についての
  通知しかありませんでした。「Bezuege_Brunnthaler」という名前付き
  範囲は黙って一緒に出荷されており、LibreOfficeの表計算ではプログラムは
  完全に沈黙していました。「Notizen Ortner」というシートはそのため
  浄化済みとみなされ、受信者が最初に目にするのはその氏名でした。

  通知されるのは、実際に人物につながるものだけです。同じブック内で
  すでに置換された単語、または複数の単語のうち一部を選び出したヒットで
  す。「Zustaendig」や「Bezug_Umsatz」のような単独の単語はもう警告を
  発しません――以前であれば発していたでしょうし、2つに1つのブックで
  出てくる警告は、3回目以降は誰も読まなくなります。

- **「原本を復元」が、現在は本当にすべてを取り戻すようになりました。**
  一部の文書では、その後個々の文字が欠けていました――「Seestraße 14」
  が「Seestraße 4」に、「An:」が「An」に、「nordlicht-planung」が
  「nordlicht planung」になり、行がまったく戻らないこともありました。
  まさにそこでは、その後マウスで何も選択できず、黒塗りもできませんで
  した。テキストは紙面上には存在していましたが、プログラムはもうそれを
  認識していなかったのです。影響を受けたのは、各文字を個別に配置する
  文書内の、幅の狭い文字――数字の1、コロン、ハイフン――でした。練習用
  文書はそのひとつです。

- **そして、同じ文書が浄化時にもう画像に変換されなくなりました。**
  こうした文字が残ることで、事後確認が残存を報告し、そのページが念の
  ためラスタライズされていました。その上のテキストは、その後単なる
  画像になっていました。検索できず、選択もできず、ファイルサイズも
  大きくなっていました。練習用文書は現在、両方のページとも本物の
  テキストのままです。

- **色付きのマークが、復元されたテキストの上にもう残らなくなりました。**
  置換を取り消した人は、復元された単語の上に色付きの矩形がそのまま
  残っているのを目にしていました――そこには原本が戻っているにも
  かかわらず、「ここから何かが削除されました」と主張し続けていたのです。

- **バーが、その下の単語の長さをもう漏らさなくなりました。** 黒塗りの
  際、短い行ではバーが現在**行全体**を覆います――住所ブロック、ヘッダー
  情報、幅の狭い表のセルです。行全体が収まらない場合(3列ある通常の
  表の行など)は、これまでどおりフィールド単位になります。地の文の
  行では、単語単位のままです。そうしないと、文中の氏名1つで文全体が
  黒塗りになってしまうためです。そして、上下に並ぶバーは**同じ長さ**に
  なります。住所ブロックでは各行に1つの値が並びますが、長さの異なる
  3本のバーは、それでも各行の長さを漏らしてしまっていました。バーは、
  用紙に空きがある範囲でのみ伸びます――隣の列の手前でバーは止まります。

- **「行全体」が、現在は本当に行全体を黒塗りするようになりました。**
  これまでは、バーは次の大きな空白――つまりフィールドの終わりで止まって
  いました。地の文ではこれは目立ちませんでした。そこではフィールドが
  行そのものだからです。しかし、ヘッダー情報や表では目立ちました。
  「Name: Anna Musterfrau   Abteilung: Vertrieb」から、氏名の最後の
  文字でちょうど終わるバーが生成され――その結果、氏名の長さがまた紙面上に
  残ってしまっていました。バーは現在、行の最初の単語から最後の単語まで
  伸び、隣の列も巻き込みます。値だけを対象にしたい場合は「単語」を
  選択してください。自動処理は引き続きフィールド単位で黒塗りします。

- **提供前に、完成したファイルを最後にもう一度検索します。** これまでの
  すべての検査は、あらかじめ誰かが指定した箇所――ページテキスト、
  検出矩形、画像領域――を確認するものでした。しかしPDFには、一覧に
  収まりきらないほど多くの格納場所があります。注釈、フォームの値、
  ブックマーク、文書情報、添付ファイル、JavaScriptなどです。そのため
  最後に、Maskuroは書き出されたファイルを、置換したすべての対象に
  ついて単純に検索します――同じ文言がそのまま存在することが許される
  ページテキストを除く、すべての場所が対象です。何かが残っていた場合、
  結果は**生成されず**、通知にはその値が表示されます。浄化されたと
  みなされる文書は、文書が存在しないよりも悪い結果です。

- **確認できなかったものは、もう確認済みとはみなされなくなりました。**
  これまで、3つの経路で事後確認の失敗がきれいな結果のように見えて
  いました。テキストレイヤーが読み取れなかったページは、特にきれいだ
  とみなされていました――そこには何も見つからなかったからです。現在は
  そのようなページはラスタライズされます。残存が見つかったページを
  代わりにラスタライズできなかった場合、これまでは黙って出荷されて
  いました。現在は、その場合は浄化処理自体が中止されます。そして修正
  ウィンドウ内の照合処理は、自身のエラーの後に「何も残っていません」と
  報告していました――ウィンドウ内では、それがすべて削除されたことと
  区別がつきませんでした。現在は、「ページをラスタライズする」ボタン
  付きの警告が表示されます。

- **「初期設定に戻す」が、ほとんどの設定を実際には元に戻していません
  でした。** 22個のチェックのうち9個が、操作後も変更されないままでした
  ――プレビュー、「浄化されたファイルをその後開く」、修正ウィンドウ、
  即時保存、そして両方の更新チェックが含まれます。保存されたファイルは
  空にされていましたが、ウィンドウは古い値を保持し続け、次のクリック
  時にそれを再び書き込んでいました。現在はすべてのチェックが元に戻り、
  「変更済み」の表示もそれとともに消えます。
- **「浄化ごとに検査報告書を自動保存」は、有効に表示されていましたが
  実際は無効でした。** リセット後、チェックは入ったままでしたが、値は
  削除されていました――何の兆候もないまま、報告書が作成されなくなって
  いました。検査記録と独自の画面録画についても同様でした。これらの
  キーボードショートカットも、リセット時に現在は正しく有効・無効が
  設定されます。

- **同じ行内のバーが、現在は見た目が揃うようになりました。** これまでは、
  各該当箇所が独自のバーを持ち込み、その高さは対象となった単語の
  フォントから決まっていました。見出しと値のサイズが異なる行では、
  太い線と細い線が端をずらして並んでおり、2つの該当箇所が空白1つで
  区切られている場合は、その上に明るい隙間が残っていました。同じ行の
  バーは現在、上端と下端が揃います。空白1文字で区切られているだけの
  箇所は、1本のバーになります。2つの該当箇所の間に残すべきもの――氏名の
  後ろのコンマ、見出し、金額など――は、これまでどおり区切って残ります。
  組版されたページでもスキャンでも同様に適用されます。

- **「このプログラムについて」の下のタブが、再び上部から表示される
  ようになりました。** プライバシー、ライセンス条項、ライセンス情報は
  文章の途中で開いていました――それを読む人は、最初の行を見るために
  まず一番上までスクロールする必要がありました。

- **ペンのアイコンが、もう2つ目のエディタウィンドウを開かず、既存の
  ものを前面に呼び出すようになりました。** これまでは、クリックの
  たびに新しいウィンドウが作られていました。このウィンドウはタスク
  バーに独自の項目を持たないため、最小化した人はそこにたどり着けず、
  もう一度クリックしていました。メインウィンドウを復元すると、たまった
  ウィンドウがすべて一度に前面に出てきていました。現在は、追加の文書は
  開いているウィンドウのタブバーに収まり、すでに開かれている文書には
  2つ目のタブは作られません。

- **「高度な検出」は、モデルが不足している間はもう「変更済み」の表示を
  持たなくなりました。** これは出荷時にオンで提供されますが、再取得
  可能なモデルなしではオンにできません――そのため、設定画面には、新規
  セットアップされたどのパソコンでも、誰も触っていないのに変更済みと
  表示されていました。現在は、そのチェックがオフである理由を表示自体が
  伝えます。「モデルはまだ読み込まれていません」です。

- **導入用の帯が、Office文書やテキストファイルでPDF用のキャンバスの
  説明をしていました。** そこには「単語をクリックすると黒塗りされます」
  と表示されていましたが、Wordファイルではクリックしても何も黒塗り
  されず、そこでは選択してからボタンを押す必要があります。現在は、
  それぞれの表示で実際に当てはまる内容が説明されます。
- **ツールバーが、テキスト表示ではラベルで埋め尽くされていました。**
  「選択箇所を置換」「選択箇所を黒塗り」「選択箇所を復元」「ページ
  プレビュー」「PDFとして黒塗り」は、現在はPDFにある兄弟と同じように
  アイコンとして表示されます。名前は簡易ヘルプとメニューに引き続き
  残ります。
- **比較ルーペでのCtrl+マウスホイールが、そのズームスライダーに反映
  されていませんでした。** 文字は大きくなりますが、スライダーとその
  隣のパーセント表示は古い状態のままでした。
- **更新のインストーラーが前面に来ませんでした**――タスクバーで一度
  クリックする必要がありました(Windowsのみ)。
- **行頭の年号が、オーストリアの郵便番号として扱われていました。**
  履歴書では、「2020 Verkaufsstrategien」がプレースホルダーになり、
  行全体が消えていました。1900から2099の間の4桁の数字は、現在は
  2つ目の住所の手がかり――その上の通り名、前にあるフィールド語、国
  コード、または既知の地名――を必要とします。住所ブロックにはこれが
  ありますが、年号の並ぶ列にはありません。
- **月と年の組み合わせが、電話番号として扱われていました。** 「Seit
  08.2010 123-Verkauft GmbH」から「電話番号」が生成されていました――
  月、年、そしてその後ろの会社名の最初の数桁からです。
- **報告書には「文字認識で確認済み」とだけ表示され、それが読み取れ
  ないものについては何も伝えていませんでした。** 画像がそのまま残る
  場合、現在は、手書きのものはそこで検出されないという説明が加わり
  ます――署名や手書きで記入された氏名はそのまま残ります。これまでは、
  この一文はスキャンされたページにのみ表示されていました。埋め込み
  署名を含む通常のPDFには、この点について何も表示されていませんでした。
- **黒塗りされた画像背景の上のプレースホルダーが、そのバーの左端に
  寄っていました。** 画像内で値が見つかった場合――例えばスキャンされた
  署名の隣にタイプされた氏名など――、その画像領域は幅いっぱいに黒塗り
  される必要があります。これまでの短いプレースホルダーは、その隣に
  何もない黒だけを残しており、2回の操作が行われたように見えていました。
  現在は、バーの上に中央揃えで配置されます。

## 0.10.41-alpha.20260826 – 2026年8月26日

### 新機能

- **試用期間終了後、起動ごとに一度だけライセンスを知らせるウィンドウが表示されます。**
  起動直後ではなく、起動5分後に現れます――最初の作業の妨げにならないためで、
  バージョン整理が実行中の間は待機します。ここから購入への導線と、すでに購入
  済みのキーを入力する導線が伸びています。「後で」はボタン内の5秒が経過すると
  閉じます。何かが制限されるわけではありません。無料版はこれまでどおり動作し
  続けます。

- **無料版で実行前に表示される待ち時間は、30秒ではなく10秒になりました。**
  ライセンスを思い出させるためのものであり、作業を止めるためのものではあり
  ません。

- **ライセンスに関する3種類の案内が、すべて同じ見た目になりました。**
  待ち時間、試用期間終盤の案内、試用期間終了後の案内は、同じ帯・同じ構成・
  同じボタンを備えています。残り時間は大きな数字として横に置くのではなく、
  ボタンの中に表示されます。

- **プレビューの検出結果一覧は、再び縦一列になりました。**
  9件以上になると2列表示になっていましたが、目を通す際に2本の列を行き来する
  ことになり、ここで判断すべきは行ごとの内容です。2列表示を好む場合は、
  ウィンドウ左下で再び有効にできます――設定は保存され、切り替えても選択解除
  済みの値はそのまま選択解除された状態を保ちます。

- **KIステージは、自前のKIサーバーを接続する誰にでも開放されました。**
  「設定 → KI」にすべてがまとまっています。接続設定、KIに許可すること、
  KIに行わせること――そしてサーバーが登録され次第、その上にステージの
  スイッチと確認テストが現れます。自分のパソコンで演算する言語モデルは、
  引き続き控えめな扱いのままです。10ページに数分かかり、日常使いには
  向きません。

- **自前のKIを接続できます。**
  同梱の言語モデルの代わりに、別のマシン上のより大きなモデルに応答させる
  ことができます――社内のサーバーでも、強力なグラフィックカードを積んだ
  ワークステーションでも構いません。必要なのはOpenAI互換のインターフェース
  を持つサービス(Ollama、LM Studio、llama.cpp-server、vLLM、LocalAI)です。
  「設定 → 自前のKI」で設定し、実際にモデルへ問い合わせて速度を測定し、
  可能な応答形式を確認する接続テストも備えています。複数のテキスト区間は
  順番にではなく、同時に処理されます。

- **KIに許可すること、KIに行わせることを、今から設定できます。**
  境界事例の判定、自律的な検索、地の文における検索を決める3つのスイッチが
  あります。モデルへの指示文はそのまま表示され、独自の社内用語を追加でき、
  ボタン一つで初期設定に戻せます。

- **その際にテキストが自社ネットワークを出る場合は、実行のたびに警告が
  表示されます。**
  MaskuroはアドレスからKIサーバーが社内にあるかどうかを判別し、既知の
  プロバイダーであれば名指しします。警告はオフにできますが、そのために
  必要なのはこの送信を行う権限があるという明示的な確認のみで、しかも
  まさにこのアドレスに対してのみです。この処理自体は変わりません。転送は
  引き続き、各ファイルのログと確認レポートに記録されます。コマンド
  ラインでは確認は行われず、そこで止まります――そこには
  `--ki-auswaerts-erlauben` が必要です。

- **置換前のプレビューは、新規の設定では既定で有効になり、今後は明示的に
  クリーニングしたクリップボードの内容、およびプログラムに貼り付けられた
  テキストと画像にも適用されます。**
  文書の一括処理では、これまでどおり文書ごとにすべてのページを含む
  プレビューが一度だけ表示されます。短いコピーの静かな即時クリーニングは、
  意図的にウィンドウを開きません。

- **プレビュー内の検出結果は、色付きの行全体をクリックしてオン・オフを
  切り替えられるようになりました。**
  チェックマークは今や大きく、コントラストの高いものになりました。加えて
  状態表示欄が「置換する」あるいは取り消し線付きの「置換する」を示すため、
  暗い信頼度の色の上でも、選択済みと選択解除済みの値を即座に区別できます。

- **目視のセキュリティ再確認を伴うPDFでも、プレビューは文書ごとに一度しか
  開かなくなりました。**
  選択解除された用語は、後段のページ検証でも選択解除されたままになります。
  その検証は引き続き実行されますが、同じ処理を2つ目のダイアログで中断する
  ことはありません。

- **修正エディターでは、置換語がラスタライズされたページ上でも同じ見た目に
  なりました。**
  赤いプレースホルダーがPDFのテキスト層ではなくビットマップ上にある場合
  でも、通常のPDFテキストのプレースホルダーと同じ、信頼度に応じて色分け
  された背景を得られるようになりました。

- **置換前のプレビューが、検出された用語の確認要否をすでに示します。**
  各行は、後にエディターで置換に付くのと同じ赤・オレンジ・緑の色を
  帯びます。カテゴリー内では、信頼度が低く赤の誤検出候補が上に、強い
  緑の裏付けがある値が下に並びます。同点の場合はアルファベット順です。
  同じ値が複数の検出箇所から得られた場合は、念のためそのうち最も疑わしい
  評価が採用されます。評価のない特殊事例は、赤とオレンジの中間の中立的な
  黄色で示されます。

- **結果を修正エディターから直接ファイルとしてコピーできるようになりました。**
  「結果をコピー」は、エディターを閉じてメインリストでファイルを探し直す
  ことなく、現在のクリーニング済みの版をクリップボードに置きます。保存
  されていない手作業がある場合は、事前に自動的に完全な安全な保存経路が
  実行されます。「画像をコピー」は、純粋なビットマップ用の独立した機能
  として引き続き利用できます。

- **置換された語は、エディター上で何を最初に確認すべきかを一目で示す
  ようになりました。**
  純粋な言語モデルの推測は、spaCyが一律で85パーセントと報告していても赤
  です。裏付けのないその他のモデル判定は、最大でもオレンジにとどまり
  ます。強い固有名の裏付けがあれば緑になり得ます。手作業や、評価できる
  根拠のない過去の割り当ては、中立的な黄色のままです。自動生成の黒塗り
  バーもこの色をエディターのプレビューに帯びます――バーがラスタライズ
  されたPDFページの一部である場合も今後は同様です。ただし、その割り当て
  が整合しており、以前の単語枠が実際に不透明な黒であることが確認できる
  場合に限られます。通常の太字は色付けされません。保存されたPDFでは、
  すべてのバーは変わらず不透明な黒のままです。

- **プレビューで選択解除した内容を、恒久的に記憶させられるようになりました。**
  チェックマークを外す操作は「ここで検出が誤っていた」という意思表示
  です。これまでは当該の一文書に対してのみ有効でした。今後は行に
  「二度と表示しない」というスイッチが現れ、押すと当該の値は恒久的に
  「常に保持」リストに入り、以後はどの文書でも問題なしとみなされます。
  リストの下には、「置換する」を押す前に何が恒久化されるかが表示されます。
  逆方向の操作は意図的に用意されていません――一度検出されたものは、
  検出が再び見つけ出します。

- **ボタン一つですべての設定を出荷時の状態に戻せます。**
  設定ウィンドウの左下にあり、事前に確認を求めます。ファイル、ライセンス、
  独自の検出ルール、自動起動は影響を受けません。組織による管理設定が
  ある場合はそちらが引き続き優先されます。出荷時の状態から変更されている
  設定にはすべて「変更済み」の印が付くため、何を変更したかが一目で
  わかります。

### 変更点

- **結果は、保存を押すまで自動的には保存されなくなりました。**
  ウィンドウからの実行は、クリーニング済みの版をまず暫定的な場所に
  書き込みます。原本の隣にある「…_bereinigt」ファイルは、「保存」を
  押した時点で初めて作成されます。それまでは結果の閲覧、修正、コピーが
  できます。完了した各行には保存ボタンがあり、リストの下には「すべて
  保存」があり、エディターではCtrl+Sが使えます。リストを空にする、
  またはプログラムを終了する際には確認が求められます――誰も保存しな
  かったものは、どこにも残りません。「フォルダーで表示」は保存前は
  無効です――暫定的な場所は、人に案内する対象ではありません。対応表
  ファイルは保存時に一緒に移動します。

  設定の「プログラム」にある「結果を原本の隣へ即座に保存する」を有効
  にすると、従来の動作に戻せます。コマンドライン、フォルダー監視、
  クリップボード監視は変わらず即座に保存します――そこには保存操作を
  行う人がいないためです。

- **修正エディターのツールバーが整理されました。**
  学習モードは、比較ルーペや「置換済みの値」と並んで右端に移動しました
  ――これら3つのスイッチは、いずれも動作モードのオン・オフを切り替える
  もので、まとまって配置されます。「すべてのページに適用」は、そこに
  しか作用しないため、3つの黒塗り形式の近くへ移動しました。「結果を
  コピー」「ファイル – 元に戻す」「すべてのページに適用」はラベルなしに
  なりましたが、名前は引き続きツールチップとメニューに表示されます。
  「置換する」と「原本を復元」の間には区切り線が入りました――この2つは
  逆方向の操作であり、並べると同じ道具の2種類のように見えていたため
  です。

- **「結果をコピー」のアイコンが文書を示すものになりました。**
  同じ形の2枚の紙に小さな角の矢印というデザインから、角が折れて文字行の
  ある2枚の紙というデザインに変わりました。「画像をコピー」の側は
  引き続き画像アイコンを使い、ラベルなしでも両者を区別できます。結果
  一覧の「コピー」ボタンも同じ文書アイコンを表示します――保存する
  ファイルは同一だからです。

- **設定が整理され、見出しが付きました。**
  「検出」は4つの節に分かれました。*削除する内容*、*置換方法*、
  *検索の徹底度*、*実行の前後*です。顔検出とバーコード/QRコードは、
  探しやすいよう画像の項目に移動しました。「プログラム」は*結果
  ファイル*、*起動時*、*更新*、*表示*、*当社へのフィードバック*に
  分かれ、結果ファイルの名前の付加部分は、言語と外観の間ではなく
  結果ファイルの項目に置かれています。

- **拡張検出は、出荷時から有効になりました。**
  対応する言語モデルが未読み込みの場合でも有効です。以前は初期設定が
  モデルの有無に左右されており、セットアップしたばかりのパソコンは
  常に弱い方の段階で動作していました。導入ウィンドウの最初のページで
  モデルの読み込みが提案され、その隣に費用が表示されます。モデルが
  ない場合は、動作していない段階を実際に動いているかのように装う
  のではなく、チェックマークが引き続きそのことを示します。

- **2つの用語リストは、その機能に合った名前になりました。**
  「独自の用語」は「常に削除」に、「例外」は「常に保持」になりました。

- **プレビューウィンドウが見やすくなりました。**
  9件以上になると2列表示になり、行はより低くなり、検出件数は右端では
  なく用語のすぐ後ろに表示されます。

- **修正エディターでは、置換が黒塗りより先に並びます。**
  ツールバー、「ツール」メニュー、ページ上の右クリックのいずれでも
  同様です。置換が原則です。プレースホルダーはクリックして元に戻せ
  ますが、バーはそれができません。

- **エディター内の重複するボタンが減りました。**
  「名前を付けて保存…」と「画像をコピー」は、使い慣れたキーボード
  ショートカットとともに「ファイル」メニューにのみ残ります。ツール
  バーには保存と「結果をコピー」が一つずつ残ります――どこに保存
  されるかは、いずれにせよステータス行に表示され、そこでクリック一つ
  で変更できます。

- **クリップボード監視は、初回起動時には提案されなくなりました。**
  この機能はシステムのすべてのコピー操作に介入するため、プログラムを
  初めて見る利用者にはその影響を判断できません。設定には引き続き
  存在し、そこにはこの機能に関する条項が併記されています。

- **明るい外観のまぶしさが軽減されました。**
  これまでウィンドウの背景はシステムのスタイルに由来しており、誰も
  意図的に決めていない唯一の大きな面でした――Windowsではほぼ白でした。
  今後は、どのシステムでも同じ、少し崩した白になります。

- **ツアーとマニュアルが色の意味を説明します。**
  置換済みの語の背後にある赤・オレンジ・緑・黄色が何を意味するかは、
  ツアーの一場面として、またマニュアルの一段落として――すべての言語
  版で――記載されるようになりました。

### 修正

- **マニュアルとFAQには、もう存在しないプレースホルダーが表示されて
  いました。**
  短縮形への切り替え以降、Maskuroは `[NAM1]` と表記しますが、ヘルプには
  引き続き `[NAME1]` と書かれており、「初期設定は `[NAME1]`」という文は
  単純に誤りでした。17の翻訳版では、それぞれ自国語の表記ではなく
  **ドイツ語**の表記がそのまま残っており、スペイン語の読者は、自分の
  プログラムが `[NOMB1]` と書くところに `[NAME1]` を見ることになって
  いました。結果ファイルの拡張部分も同様です。すべての版で
  `_bereinigt` と案内されていましたが、プログラムは `_limpiado`、
  `_nettoyé`、`_除去済み` のいずれかを付与します。番号なしの形式
  (匿名化ではすべて `[NAME]` ではなく `[NAM]` になる)や、ハッシュ化の
  際に値から導かれる識別子も影響を受けていました。

- **プレビューウィンドウが文書ごとに中断するのは一度だけになり、2回目が
  発生するのは本当に新しい内容がある場合のみになりました。**
  PDFは2つの経路から読み取られます。一度はコンテンツストリームから、
  もう一度は最後にレンダリングされた、表示用のページからです。これ
  までは、この2つがそれぞれ個別に確認を求めていました。今後は、最初の
  ウィンドウで決定した内容がそのまま有効になり、そこですでに提示された
  値は再度表示されません。一方、完成したページの目視確認によって、
  それ以前にはどこにも現れなかった値が見つかった場合は、すでに決定
  済みの値を除いて、その値だけがもう一度提示されます。

- **プレビューウィンドウは、どのような基準で判断すべきかを示すように
  なりました。**
  「チェックを外す = その値はそのまま残る」――これはチェックマークが
  *何をするか*を示していても、*いつ*外すべきかは示していませんでした。
  代わりに、次のように表示されます。個人に関する値が存在しない箇所は
  すべてチェックを外してください――そこでは検出が誤っています。加えて、
  各ウィンドウは、その値がどの検出処理に由来するかを示すようになり
  ました。

- **プレースホルダーは、文書全体で同じ見た目になりました。**
  OCR経路で画像ページとして再構成されたページでは、これまで表示用
  プレースホルダーが等幅フォントで組まれており、「[PLZ4]」が同じ
  ページの細い「[NAM1]」の横で幅広くセリフ付きで表示されていました。
  今後は他の箇所と同じサンセリフ体で表示され、レイアウト時に計画
  されていた以上に幅広く組まれることもなくなりました。目に見えない
  検索用の層は独自のフォントを保持します――見た目ではなく、信頼できる
  寸法が必要だからです。

- **エディターのツールバーに重複した区切り線が表示されなくなりました。**
  開いているファイルの種類にとってツールグループ全体が不要になる
  箇所――PDFにおけるページビューやレンダリングなど――では、これまで
  その空白の両側に区切り線が残っていました。

- **元に戻す操作で、まれに白い箇所だけが残ってしまう不具合を修正しました。**
  すでに正確に復元された原本のテキストが、削除されたプレースホルダーの
  幅広くまとめられた枠によって白く塗りつぶされることはなくなりました。
  テキストと画像が混在する復元では、ページ画像がすでにその原本の状態を
  可視の形で保持している場合に限り、テキストが不可視な形で挿入される
  ようになりました。これは枠、検出パネル、PDFの添付ファイルにも
  適用されます。

- **「原本を復元」が、不要にページのラスタライズを提案しなくなりました。**
  厳格な残テキストチェックは、黒塗りと置換では引き続き有効です。復元
  では省略されます――そこでは原本の内容が意図的に戻されるものであり、
  拡張された復元枠内で変更されていない隣接語は、クリーニングの不具合
  ではなく誤検出でした。

- **エディターのツアーは、「置換する」と「原本を復元」を独立した手順
  として説明するようになりました。**
  両方の道具はツールバー内で直接強調表示され、枠をドラッグすると
  プレースホルダーが挿入される、あるいは元のファイルからその箇所の
  原本の内容が復元される、という説明が加わりました。

- **国別のプレースホルダーも、最大4文字に収まるようになりました。**
  この種類はこれまで中央の略語カタログに含まれておらず、そのため
  `[UMSATZSTEUER_ID1]` のように省略されずに表示されることがありました。
  新しい処理では代わりに `[UID1]` と表記されます。自動検出される
  すべてのドイツ語・英語の種類は、その際も一意性を保ちます。他の
  表示言語で自動計算される略語も、名前が重複する場合に4文字を超えて
  伸びることはなくなりました。独自のルールの表示名は、入力された
  とおりの名前のまま変更されません。

- **置換は、黒塗りを行う前に実際に空いている行のスペースを目一杯
  利用するようになりました。**
  従来の、元の単語幅の3倍という硬直した上限は、ほぼ空欄のフォーム
  フィールドでもバーを生じさせていました。目視によるOCRの再確認による
  検出結果も、PDFのテキストが埋まっている場合には読みやすい
  プレースホルダーを得られるようになりました。黒いままなのは、純粋な
  画像・注釈・ベクター内容、選択されている黒塗り方式、そして一意な
  短縮形すら収まらない本当の狭小箇所です。

- **すでに表示されているプレースホルダーが、セキュリティ用ラスタライズ
  の際に赤で二重に上書きされることはなくなりました。**
  ラスタライズ処理は、既存の置換をページ画像からそのまま引き継ぎ、
  不可視の検索用コピーのみを新たに作成します。セキュリティバーが
  まさにこの箇所を塗りつぶす必要がある場合は、その短い元のアンカー
  だけでなく、実際のプレースホルダー枠全体が更新されます。

- **「原本を復元」は、ドラッグした枠の中の安全な対象のみを強調表示する
  ようになりました。**
  その中の置換済みの用語はすべて個別に、正確に光ります。変更されて
  いない地の文はそのまま残ります。実際のベクター形式の黒塗りバーも、
  その黒いPDF面の下に原本のテキストがある場合は個別に強調表示され
  ます。ラスタライズされたページでは、プレビューは見かけ上のバー
  領域の表示をあえて行いません――従来のビットマップ検索は、そこで
  文字、下線、表の罫線を結び付けて、誤った箇所に大きな赤い領域を
  作り出していました。復元処理そのものはこれによる影響を受けません。

- **ラスタライズされたページの復元処理で、テキストが正しく戻る
  ようになりました。**
  これまでは、色付きの矩形が重なった空白の箇所が残っていました。
  復元されたテキストは文書内に存在していましたが、ページ構成のより
  後段で描画されるプレースホルダーの白い背景に塗りつぶされていました。

- **確認用の色が何重にも重なって表示されなくなりました。**
  同じ箇所が対応表のエントリーごとに色付けされていたため、あるページ
  で5件の実際の検出箇所があると、それぞれが5重に上塗りされ、薄いマーク
  が濃いブロックになってしまうことがありました。また、実際には置換
  されていない語の上には表示されなくなりました――原本の値がまだ
  ページ上に残っている場合、そこにマークは付きません。

## 0.10.40-beta.1 – 2026年8月24日

### 修正

- **エディタの黒塗りバーに、現在は安全マージンが設けられています。**
  単語、行、自由枠は、はみ出した字形やアンチエイリアスされた端の画素も
  覆います。レンダリング検査がさらに、目に見える残りも読み取り可能な
  原本テキストも残らないことを保証します。

- **代替テキストは、読みやすく統一された短さを保ちます。** 新しい氏名、
  住所、自由語句は例えば`[NAM1]`、`[ADR2]`、`[BEG3]`のように表示され
  ます。固定の下限は4.5ポイントです。スペースが不足する場合、まず短縮
  され、その後利用可能な送り幅が拡張されます。長いプレースホルダーを
  持つ古い対応関係も、引き続き読み取り・復元できます。

- **ヒットパネルからの複数語の置換が、重複したマークや原本の残りに
  対して安全になりました。** 番号付きプレースホルダーの有無にかかわらず
  回帰テストに合格します。各該当箇所には、正確に1つの共通した対応関係
  だけが保持されます。

- **復元されたクリップボードの内容が、macOSでは即座に再浄化されなく
  なりました。** 書き込み後にシステムの署名が遅れて変化する場合でも、
  Maskuroは自身のコンテンツを確実に認識します。

### 新機能

- **エディタは、ファイルを新しく浄化された初期状態に完全にリセット
  できます。** 「ファイル―リセット」は、確認の後、現在のタブの
  すべての修正――置換一覧やカウンターを含む――を破棄します。このコマンド
  は変更がない場合はロックされ、「元に戻す」でこの操作自体も取り消せ
  ます。

- **ずらされた日付は、現在は複数ファイルにまたがっても確実に時系列を
  保持します。** 共通のずらし幅は、戦略を有効化した時点でルールに恒久的
  に固定されます。さらに、このずらし幅がゼロ日になり、気づかれないまま
  本物の日付が残ることもなくなりました。

- **PDFの手作業処理が、現在はプロフェッショナルな黒塗りの一連の流れ
  を完全にカバーします。** 個別の語句、一覧、正規表現パターンを、開いて
  いるPDF内、またはフォルダ内のすべてのPDFにわたって検索し、安全に
  黒塗りできます。ページ全体やページ範囲も直接選択できます。色、
  ニュートラルな白い面、重ね表示テキスト、フォント、配置、繰り返しには
  プレビューがあり、再利用可能なコードを管理し、インポート・エクス
  ポートできます。PDF浄化は、完全な再構築によってすべての隠れた
  コンテンツを削除するか、選択したデータ種別のみを削除するかを選べ
  ます。最も安全な選択が明確に推奨され、無効な検索パターンは説明され、
  フォルダ処理は結果のコピーのみを書き込みます。

- **任意の利用統計が、現在はインストール数とフッションの切り替えを
  示します。** Maskuroはそのために、ランダムでローカルに保存される
  インストール識別子を生成します。これには機器、利用者、ライセンスに
  関する情報は一切含まれません。サーバーはそのSHA-256値のみを保存し
  ます。この統計は設定で完全にオフにできます。

- **ツアーは、現在は両方のウィンドウを巡るガイド付き演習になりました。**
  架空の練習用文書を自動的に一覧に追加し、浄化までの流れを説明し、
  処理後は自動的にエディタで続きを行います。ツアーを中止すると、この
  続きも終了します。

- **さらに15の法域の会社が認識されます。** バルト三国、ベルギー、
  北欧、チェコ、ポーランド、南東欧、シンガポール、ブラジル、メキシコ
  からの書類を浄化する人は、法人格が未知であるために会社名を失うこと
  がなくなりました。新たに対応したのは、OÜ、MTÜ、SIA、VZW、ASBL、
  P/S、Sh.p.k.、EIRELI、z.s.、o.p.s.、S.K.A.、Pte. Ltd.、S.A. de
  C.V.、S. de R.L.などです。

### 変更

- **エディタのツールバーが、現在は空間をより的確に使うようになりまし
  た。** 明確な標準アイコンや形からすぐ分かるツールは、繰り返しになる
  文字なしでバーに表示されます。曖昧な操作は名前を保持します。
  「表示」の下で「ツールの名前を表示」をオフにすると、両方のツール
  バーを完全にアイコンだけに縮小できます。ツールチップとメニューは
  引き続き完全に名前が表示され、選択は記憶されます。

- **学習モードが、現在はツールバーに恒久的に表示されるようになりました。**
  置換された値のパネルが閉じている場合でも、そこで直接オン・オフを
  切り替えられます。ツールバー、ツールメニュー、これまでパネル内に
  あったチェックは、常に同じ状態を示します。

- **比較ルーペの「リセット」は、現在はそのズームのみをリセットします。**
  このボタンは、ルーペをドッキングしたり移動したり、ウィンドウサイズを
  変更したりすることなく、初期値の125%を復元します。全体の配置に
  ついては引き続き「表示をリセット」が担当します。

- **不具合報告と要望は、現在はヘルプボタンからも送信できます。**
  「不具合を報告…」と「要望を伝える…」が、従来のヘルプメニューと同様
  にそこに表示されます。どちらの経路も、既存の安全な不具合報告、また
  は公開の要望リストを開きます。

- **タスクバーメニューが、より短く分かりやすく整理されました。** グロー
  バルなキーボードショートカットを持つ2つのコマンド――クリップボードの
  浄化とスクリーンショット――は、現在共通の右側のショートカット列と
  ともに直接上下に並びます。「最後の原本内容を復元」はそこからなくなり
  ました。より分かりやすい復元ボタンは、引き続きメインウィンドウで
  利用できます。

- **法的なページは「ヘルプ→法的情報」から直接アクセスできます。** この
  サブメニューは、maskuro.com上のライセンス条項、プライバシーポリシー、
  運営者情報、利用規約につながります。撤回に関する案内は、引き続き
  購入時にウェブサイト上に表示されます。

- **手作業で黒塗りされたPDFは、保存時に完全に再構築されます。** 表示
  されるのはページとその新たに読み取られた検索レイヤーのみです。メタ
  データ、ファイル添付、ブックマーク、コメント、フォームの値、隠れた
  レイヤー、検索インデックス、スクリプト、切り取られたコンテンツ、
  その他のオブジェクト内に隠されたコンテンツは、出力ファイルには引き
  継がれません。文字とベクターグラフィックは、その後画素から成ります
  ――これが、未知のPDFオブジェクトツリーとの間に証明可能な境界を設ける
  ことの代償です。

- **Ctrl+Shift+Bが、現在はすべてのシステムで初期設定としてMaskuroで
  スクリーンショットを撮影します。** Print Screenキーおよびその組み
  合わせは、引き続き独自の割り当てとして設定可能です。タスクバー
  アイコンのメニューでは、グローバルなキーボードショートカットが現在、
  対応するコマンドの右側に表示されます。独自に保存された割り当ては
  そのまま維持されます。

- **エディタは、ページと比較ルーペを左側に配置した状態で起動します。**
  ページパネルが上部に、開かれた原本ルーペがその直下に配置され、置換
  された値は右側にそのまま残ります。意図的に保存された独自の配置は
  引き続き優先されます。

- **練習用文書は、もうメインウィンドウに恒久的には表示されません。**
  これはガイド付き演習の一部であり、それに加えて「ヘルプ」からも引き
  続きアクセスできます。

- **初回起動は、現在は直接実践的な演習に進みます。** 図解入りの
  クイックガイドは、内容が重複する2つ目の導入経路としてはもう提供され
  ません。引き続き「ヘルプ→クイックガイド」からいつでもアクセスでき
  ます。

- **休止中のタスクバーアイコンは、フルカラーのまま表示されます。** 現在
  は、クリップボードモードが有効な場合と同じ、はっきりとしたMaskuroの
  盾が表示されます。監視が実際に有効な場合にのみ、緑の点灯が追加され
  ます。

- **練習用文書は、Maskuro内に留まります。** 導入用のボタンは、架空の
  PDFを生成してファイル一覧に直接追加しますが、もう追加のPDF閲覧
  ソフトを起動しません。

- **修正ウィンドウでの検索は、入力中も滑らかに動作するようになりまし
  た。** ヒット件数表示のための余白は、ウィンドウを開いた時点ですでに
  確保されます。最初のテキストが表示されてもキャンバスは変化せず、
  新たなPDFのラスタライズ処理を引き起こしません。

- **型式表示内のメーカー名が、そのまま表示されるようになりました。**
  「Fabrikat: TRILUX oder gleichwertig(型式:TRILUXまたは同等品)」
  のような記載は必要な商品を説明するものであり、この見出しだけを
  理由にもう会社名として黒塗りされることはありません。仕入先・会社・
  メーカーのフィールドは、これによる影響を受けません。

- **コーパス測定は、過度に広く黒塗りされたヒットを誤検出としてカウント
  します。** Maskuroが期待される氏名を削除する際、文の一部を巻き込んで
  しまった場合、現在は誤検出数が増加します。報告書はこうした過剰処理を
  さらに個別に示します。そのため、以前の誤検出数とは直接比較できません。

### 修正

- **ドイツ語の原本書類にある技術用語や行政用語が、氏名や地名として
  黒塗りされることが少なくなりました。** 車両装備、明細・合計行、
  発注・データ保護に関する用語、法令参照、公開資料のファイル名は、
  その裏付けとなる専門的な文脈がある場合にのみ抑制されます。文字認識
  で失われたウムラウトを含む「Marz 2026」は、月として引き続き保護
  されます。日付との関連がない「Marz」は、引き続き実際の氏名や地名で
  ある可能性があります。

- **「原本を復元」が、必要な幅全体を即座に取得するようになりました。**
  枠が対応関係に登録された値の1単語だけに当たった場合、Maskuroは対応
  関係と原本の行を基に、それを自動的に項目全体まで拡張します――例えば
  「Planungs」から「Nordlicht Planungs GmbH」までです。その後操作可能
  になる枠も、実際に復元された全幅を示します。

- **「原本を復元」が、黒いバーを明確な対象として表示するようになり
  ました。** マウスを重ねたりドラッグしたりすると、認識されたバー全体が
  明るいコントラストの輪郭とともに赤く光るようになりました――これまでの
  ように、ほとんど対応が分からないテキスト矩形が隣に表示されるだけでは
  ありません。これは、バーが画素だけから成るラスタライズされたページ
  にも適用されます。

- **エディタのツアーが、パネルが閉じられていた場合にステーションを
  飛ばさなくなりました。** ツアーのために、Maskuroはページパネル、
  比較ルーペ、置換された値を一時的に自動的に開き整理します。「完了」
  または中止の後は、個人の配置が復元されます。あるツールがある文書
  種類で根本的に利用できない場合、その説明は気づかれないまま消える
  のではなく、テキストの停止点として残ります。

- **「置換」が、PDFの安全フォールバック時にも表示されたままになりま
  した。** Maskuroが、残存した文字や破損したテキストランのためにページ
  を画像として再構築しなければならなかった場合、正しい置換は検索
  レイヤー内にのみ見えない形で存在し、ページ上には黒いバーが表示され
  ていました。実際に設定された代替値は、現在はすべてのラスタライズ・
  OCR再構築を通じて、赤く見え、検索可能な状態で保持されます。

- **浄化されたフッションの上にある案内が、ダーク表示でも読める状態に
  なりました。** フッションの見出し、操作行、導入文は、現在は実際に
  表示されているQtウィンドウから直接、文字色を取得します。

- **黒塗り枠が、ラスタライズされたPDFページで再びテキストの上に正しく
  重なるようになりました。** 見えない単語の矩形は、元のフォントに
  よっては見える文字よりも幅が狭くなっていました。これによりバーに
  隙間ができたり、最後の文字が読める状態で残ったりしていました。矩形は
  現在、見える単語の幅・高さ・方向を保持します。

- **「新機能」が、再び一番上から始まるようになりました。** 変更履歴
  ダイアログは、ウィンドウの構築完了後、Qtの状態によっては新機能の
  途中から始まっていたところを、明示的にテキストカーソルとスクロール
  バーを先頭に設定するようになりました。

- **スキャンの単語認識中の終了処理が、静かに行われるようになりました。**
  ちょうど完了しようとしているOCRのバックグラウンド処理が、すでに
  閉じられた修正ウィンドウに送信しなくなりました。

- **相対的な時間表現が、もう氏名として扱われなくなりました。**
  「heute」「gestern」「morgen」「nächste Woche」のような定型表現を、
  Maskuroは現在、各文書言語の公式なカレンダーデータから認識します。

- **最初のモデル読み込み中の終了処理が、正常にクリーンアップされる
  ようになりました。** Maskuroや修正ウィンドウを開いた直後に閉じた
  場合でも、ネイティブの音声認識処理でまだ動作しているスレッドが
  プロセス終了時に残ることはありません。これにより終了時の散発的な
  クラッシュ報告が防止され、すでに開始している読み込みは正常に完了
  します。

- **遅延した起動ダイアログが、終了後に表示されなくなりました。**
  起動直後にメインウィンドウを閉じた場合、その後見えない状態や遅延した
  状態で、最良の検出、新機能、導入についての質問が表示されることは
  なくなりました。

- **HTMLとメールが、改行コードを保持するようになりました。** Windows
  では、HTMLのシリアライズが浄化と復元の後にLFとCRLFを混在させて
  いました。内容と書式は正しいものの、ファイルはもうバイト単位で
  一致しませんでした。HTMLファイルとMIMEメッセージは、現在は再び
  元の書き方を引き継ぎます。

- **関係を表す語を含む会社名が、完全な形で保持されるようになりまし
  た。** 前置詞の後ろで、Maskuroは「Gesellschaft für Systemtechnik
  mbH」や「Bank für Arbeit und Wirtschaft AG」のような会社名を
  「für」の単語で切り捨てていました。現在は会社名全体が認識されます。
  「Wir sind bei Alpha GmbH versichert」のような本物の文の前置き
  部分はそのまま見える状態を保ちます。

- **中国語の会社名が、法人格の前で完全な形を保持するようになりまし
  た。** 動詞として解釈可能なブランド構成要素が、「有限公司」という
  明確な付加語があるにもかかわらず、名前全体を破棄してしまうことが
  ありました。大文字・小文字の区別がない文字体系では、この公式な
  法人格の目印が、この不確実な品詞の境界より優先されるようになりまし
  た。

- **PDFページが、理由もなく画像になっていました。** 一般的な生成
  ツールがそう構築する、ページ間でフォント一覧を共有する複数ページ
  のPDFでは、最初のページの後、それ以降のすべてのページがそのフォント
  への参照を失っていました。その結果は二重でした。ウムラウトは結果内
  で検索できなくなり(「Auftragsbestätigung」が見つけられませんでした)、
  事後確認はその結果、ページに存在しない文字を見落としと判断し――
  無傷のテキストページを画像にラスタライズしてしまい、検索も コピーも
  できず、サイズも大幅に大きくなっていました。検査対象では、これが
  17ページ中4ページに該当していました。

- **コンマ単独では、もうラスタライズを引き起こしません。** 発見範囲が
  単語で終わる場合、隣接する句読点もぎりぎり含まれることになります。
  しかしコンマやピリオドは見落とされた項目ではなく、ラスタライズには
  ページ全体という代償が伴います。文字や数字は、引き続き追加の黒塗り
  を要する理由のままです。

## 0.10.38-alpha.20260824 – 2026年8月24日

### 新機能

- **法人格のない会社名も、見出しがそれを示している場合は現在検出され
  ます。** 「Lieferant: Kranzbichler Handels GmbH(仕入先:
  Kranzbichler Handels GmbH)」は以前から削除されていました――法人格が
  会社名であることを示すためです。「Lieferant: Dehner Märkte」は
  そのまま残っており、見積書、入札書類、注文書では仕入先はたいてい
  まさにこのような形で書かれています。同じことは「Firma:」
  「Hersteller:」「Fabrikat:」「Arbeitgeber:」やその他8言語の対応語に
  も当てはまり、見出しがそれだけで独立した行にあり、名前がその下に
  ある場合も同様です。

  見出しの後ろに続くものが会社名では*ない*場合は、そのまま残ります。
  「Lieferant: siehe Anlage(仕入先:添付資料参照)」は黒塗りされ
  ません――そうしないと「Lieferant: [ORGA1]」となり、存在しない名前を
  主張してしまうためです。同じくらいの頻度で人物を指す見出し
  (「Kunde:」「Auftraggeber:」)は意図的に対象外です。

- **挿入された画像も、現在は編集できるようになりました。** 「画像を
  浄化」ウィンドウには、「結果をコピー」の隣に*エディタで編集*ボタンが
  あります。画像は浄化された後、追加の黒塗り、注釈、強調表示のために
  開きます――これはスクリーンショットと同じ経路です。

- **見出しの後ろにある番号は、それが取引先を表す場合でも現在検出され
  ます。** これまでは顧客番号、契約番号、社員番号が対象でしたが、現在は
  買掛先番号、売掛先番号、仕入先番号、オーストリアの雇用主番号、
  ANKÖ登録番号、そしてメーカーのWEEE・EAR・EPR番号――ドイツ語でも
  英語でも――も対象です。さらにMaskuroは現在、組版された見積書ヘッダー
  でのコロンの前にスペースが入る書き方(「Kunden-Nr : K903944」)も
  理解します。品目番号、注文番号、発注番号、見積番号、請求書番号は
  引き続き対象外です。これらは案件や商品を表すものであり、人物を表す
  ものではないためです。それでも削除したい場合は、独自の検索パターン
  として登録してください。

- **ファイルにかかった時間が、現在は表示されます。** 完了した行には
  検出言語の隣に所要時間が表示され(「完了・ドイツ語・2.4秒」)、
  まとめには処理全体の時間が、指標のクラッパーには合計が表示されます
  ――検査報告書にも独立した項目として記載されます。複数ファイルの場合、
  この行はどれが時間を要したかを教えてくれます。

- **システムのOCRが対応していない文字体系も、対応する言語データが
  あれば代替として読み取れるようになりました。** これまでは、システム
  標準の文字認識がある文字体系に対応していない場合(Macでの
  デーヴァナーガリーなど)、結果に「画像は【確認されていません】」
  と表示され、画像内の項目はそのまま残っていました。現在は、対応する
  言語データがあれば、同梱の文字認識が代わりに機能します。この方法で
  読み取られた画像は通常の検査よりも不確実であるため、結果には「代替
  方式で読み取りました――ご確認ください」と表示されます。ヒンディー語
  テストのある過去の中間段階での測定では、**発見項目が10件増加し、
  誤検出が4件減少**しました(64%→73%)。現在の最終値はさらに上に
  記載されており、これと混同しないでください。

- **文字認識が、正しい言語を尋ねるようになりました。** ドイツ語と英語
  以外のすべての文書言語では、これまで英語の認識モデルが使われていま
  した。対応する言語データがすでに用意されていた場合でもです。Windows
  では、これがすべての言語に影響していました――ギリシャ語、日本語、
  ヒンディー語も英語モデルで読み取られていました。

- **初回起動時のセットアップウィザード。**(すでにMaskuroを使ったこと
  がある人には表示されません――「初回起動」とは本当の初回起動を意味し、
  この更新後の初回起動という意味ではありません。)6枚の画像の代わりに
  3つの質問です。お使いの文書の言語、画像内のテキストも読み取るか、
  普段どのようにMaskuroを利用したいか。最後には、練習用文書、ツアー、
  図解入りのクイックガイドという3つの選択肢がこれまでどおり用意され
  ています。すべてスキップでき、「ヘルプ→セットアップをやり直す」で
  再度呼び出せます。

- **F1キーで、マニュアルの該当する章が開きます。** メインウィンドウ、
  設定(ページごとに対応する章)、確認ウィンドウ、言語管理で機能します。
  修正ウィンドウではShift+F1です。F1はそこでは以前からキーボード
  ショートカット一覧を表示するためです。これまでヘルプは常に先頭
  ――25章の最初――から開始していました。

- **マニュアルの新しい第一章:「3分で始める」。** 文書1件の処理に必要
  なのは4つの手順だけです――全18言語版で提供されます。

- **ウィンドウを巡るツアー。** 「ヘルプ→ウィンドウを巡るツアー」は、
  操作要素を1つずつスポットライトで照らし、その脇に説明文を表示します
  ――メインウィンドウでは8箇所、修正ウィンドウでは7箇所です。図解入り
  のクイックガイドとは異なり、これは今まさに開いているウィンドウを
  説明します。Escキーでいつでも中止できます。

- **安全に試せる練習用文書。** ドロップ領域の下に「練習用文書を開く」
  (ヘルプメニューにもあります)が加わりました。氏名、住所、電話番号、
  IBAN、社会保険番号を含む架空の用紙が作成され、その用紙にはこれで
  何ができるか、その後何が表示されるかも同時に記載されています。この
  中の言葉は誰か実在の人物のものではありません。Maskuroに最初に送る
  文書は、実物である必要はないのです。

- **「確認するだけ…」が現在「浄化」の隣にあります。** これは、個人
  データがどこにあるか――ファイル、種類、件数――を、何も変更・保存
  することなく表示します。文書をドロップした人は、これで浄化する前に
  まず確認できます。これまでこの機能はファイルメニューの「フォルダを
  確認…」にしかなく、ドロップされたファイルではなくフォルダ全体を
  対象にしていました。

- **何も見つからなかった場合、現在はその理由の可能性が表示されます。**
  例えば、ファイルに画像は含まれているが「画像内のテキストも確認する」
  がオフになっている、あるいは設定されている言語が文書内の言語と
  一致していない、などです。そのいずれにも該当しない場合、Maskuroは
  その旨も伝えます。

- **修正ウィンドウは初回、3つの文で挨拶します:**クリックすると単語が
  黒塗りされる、ドラッグすると範囲が選択される、右側に置換された値が
  表示される、です。「理解しました」でこの案内は恒久的に消え、「ヘルプ
  →案内を再表示」で呼び戻せます。

- **単語のクリックが、現在はスキャンされたページでも可能です。** これ
  までは、PDFがテキストレイヤーを持つ箇所でしか単語をクリックでき
  ませんでした――スキャンではできず、同じ文書内でもページごとに
  切り替わることがありました。現在は、そうしたページは一度だけ文字
  認識で読み取られ、その後は他の箇所と同様に単語をクリックできます。
  ステータスバーには現在何が起きているかが表示されます。

- **ページパネルが、再び一つの面になりました。** これまでは自身の
  カラムの途中で途切れており、タイトルバーが切れて、その隣に別の色の
  帯があり、現在のページはその番号の後ろにある色付きの四角でしか
  分かりませんでした。現在はカラムを埋め尽くし、より幅広くドラッグ
  でき、現在のページはタイル全体として強調表示されます――中には加工
  されていないページプレビューが表示されます。

- **置換された箇所が、薄い黄色に光ります。** ページプレビューでは、これ
  により何が置換されたかが一目で分かります――比較ルーペが原本の上で
  使用するのと同じ色です。マウスを重ねた際の赤い枠は変わりません。

- **修正ウィンドウの「表示をリセット」**(「表示」メニュー)。ページ
  パネルやヒット一覧を移動・分離・閉じた場合、これによりすべてが初回
  起動時の位置に戻ります。

### 変更

- **プレースホルダーが短くなりました。** `[SOZIALVERSICHERUNGSNR_1]`は
  `[SVNR1]`に、`[ORGANISATION_1]`は`[ORGA1]`に、`[EMAIL_1]`は`[MAIL1]`
  になります。理由は見た目の美しさではありません。置換対象の値より
  長いプレースホルダーは行を押し広げ、幅の狭い表の列にはまったく収まら
  なくなります――これまではそこに黒いバーが残り、それはもう誰にも
  「ここに何かがあった」ことを伝えませんでした。一般的な略称がある
  場合はそれが使われます(`[BLZ1]`、`[KFZ1]`、`[IBAN1]`)。以前の実行
  結果も引き続き利用できます。古い表記は引き続き認識され、過去の対応
  関係ファイルもこれまでどおり機能します。

- **プログラムアイコンが、現在はどこでも同じ見た目になりました。** これ
  までMacのメニューバーには単色の盾が表示され、システム自体がそれを
  黒または白に着色していました。Windowsのタスクバーでは緑または灰色
  でした。現在はどちらのバーにも同じ青いMaskuroの盾が表示されます。
  クリップボードが監視されているかどうかを示す表示は、これまでどおり
  明確です。監視中は盾に緑の点が付き、休止中は同じ盾が淡く表示されます。
  最も小さいサイズでも、現在は盾の中に2本の黒塗りバーが表示されます
  ――これまでタスクバーには1本しか表示されていませんでした。

- **顔検出は、同意の上で撮影された訓練画像によるモデルを使用するように
  なりました。** 現在同梱されているのはMediaPipe BlazeFace(Apache-2.0)
  です。以前の検出器は組み込まれたまま切り替え可能ですが、その訓練
  データの出所が最終的に確認できていないため、もう同梱はされません。
  検出自体に変化はありません。324件の肖像写真と143件の顔のない画像に
  おいて、新しいバージョンは同数を検出し、誤検出数も同じで、所要時間は
  3分の1です。

- **OCRは、最も強力なPDF保証のための安全の要です。** 通常のPDF処理は
  これを使用し、完全な最小構成を生成します。OCRを明示的にオフにした
  場合は、より互換性の高いオブジェクト経路が使われます。画面表示、
  完了通知、マニュアルは現在、この経路が未知の隠れたPDFチャネルに
  対して同じ構造の防御を提供しないことを明示的に述べます。

- **販売ゲートは現在、これまで同梱されていたYuNetモデルもロックし
  ます。** その正確な重みのMITライセンスは文書化されたままですが、
  WIDER FACEを経由した公開されている訓練データの経路は、保守的な
  製品リリースの根拠としては不十分です。販売前には、書面での確認、
  または裏付けのある商用データ・重みの経路を持つモデルへの交換が
  必要です。

- **会社名・組織名は、現在は自発的に削除されます。** これまでは、明示的
  に要求しない限りそのまま残っていました。これはビジネスレターに
  とって誤った初期設定でした。見積書を転送する人は、そこに発注者の
  名前を残したくありません。そのため「Kranzbichler Handels GmbH」や
  「Institut für Bauphysik」のようなものは、氏名と同様に扱われます。
  異なる動作が必要な場合はウィンドウでオフにできます。コマンドラインで
  はこのスイッチは現在`--ohne-organisationen`という名前です。古い
  `--mit-organisationen`は引き続き受け付けられますが何も行いません。
  既存のスクリプトやショートカットが壊れないようにするためです。日付
  と金額の情報は、これまでどおり対象外です。

- **黒塗りには、現在2つのチェックの代わりに3つの形式があります。**
  「単語」「行全体」「自由枠」が、1つの選択として並びます――常に
  ちょうど1つが有効です。これまでは「テキスト行」と「行全体」が独立
  した2つのスイッチで、両方とも押された状態になり得ました。自由枠は
  ボタンですらなく、最初のスイッチのオフ状態にすぎませんでした。3つは
  現在、そのツールのそばに目に見える形で並び、別のツールが選択されて
  いる間は灰色になります。

### 改善

- **最初の文書の完了が、約1秒速くなりました。** 浄化の開始前に、
  Maskuroは文書の言語を確認します――そのためにこれまで、実際に必要な
  単語よりはるかに多くを読み込む方法で、全48言語の単語リストを取得
  していました。これは最初の結果が出るまでの待ち時間の約半分を占めて
  いました。検出処理自体に変更はありません。以前と同じ単語を見ますが、
  より速くなっただけです。2件目以降の文書は、もともとこの影響を受け
  ていませんでした。

- **非常に長い段落を含む文書の確認が、速くなりました。** 改行のない
  段落では、Maskuroは発見された箇所ごとにその段落全体を読み直して
  いました。現在は1回で済みます。段落が長いほど差は大きくなります――
  測定では計算時間が約7分の1減少しました。結果自体に変化はありません。

### 修正

- **会社名とともに、文の半分が消えることがよくありました。** 地の文に
  会社名がある場合(「Information über die Gottwald GmbH & Co KG」、
  「…(AGB) der Musterbetriebe GmbH」)、その名前だけでなく、そこから
  文頭までのすべてが黒塗りされていました。テキストはこれにより読め
  なくなり、無差別に黒塗りされたように見えていました。それ自体に
  「für」や「und」を含む会社名(「Bank für Arbeit und Wirtschaft AG」)
  は、これにより変化することなく完全に保たれます。

- **会社名が、本文では削除されたにもかかわらずレターヘッドには残って
  いました。** ある見積書では、会社所在地がレターヘッドの画像内には
  読める状態で残っていました――同じ地名が本文では黒塗りされていたに
  もかかわらずです。検索可能な結果のテキスト内には、見えない形で
  さらに残っていました。一度削除されたものは、現在は画像としてしか
  存在しない箇所でも削除されます。これはロゴやグラフィックとして
  描かれた文字商標にも適用されます。

- **macOSでは、起動のたびに画面収録の許可を尋ねられていました**――
  許可がすでに与えられている場合でもです。起動時のヒント表示が試験的な
  録画を行い、それがまさにシステムダイアログを画面に呼び出していまし
  た。現在は起動時にMaskuro自身が一度だけ尋ね、システムは実際にスク
  リーンショットを撮影する際にのみ尋ねます。

- **技術的な専門用語が、地名や会社名と誤認されていました。**
  「Einspeisepunkt」「Flachdach」「Verteileranlage」「Meldersockel」や
  類似の数十の単語が、見積書や仕様書から消えていました。Maskuroは現在、
  それらの基本語で識別します。「-anlage」「-punkt」「-kanal」で終わる
  ものは物です。BerlinやMelk、Wieselburgのような地名にはそのような
  基本語がなく、影響を受けません――「Der Graben」や「Alter Markt」の
  ような住所も同様です。

- **日本語、韓国語、中国語、タイ語、グジャラート語の文書が、プログラム
  をクラッシュさせることがありました。** これら5言語のいずれかの文書に
  「https://」の付かないインターネットアドレスが含まれていた場合、
  浄化処理が内部エラーで中断していました。ウィンドウが開いていた場合、
  その他の作業内容も失われていました。選択可能な48の文書言語すべてが
  現在は正常に処理を完了します。ある言語の頻度辞書が不足している場合、
  その項目は疑わしい場合は消去されずそのまま残ります。

- **フィールドの見出しは、ドイツ語と英語でしか保護されていませんでした。**
  「Reference」はそのまま残る一方、イタリア語の「Riferimento」や
  ポルトガル語の「Referência」は地名として削除されていました――同じ
  フィールド名、同じ行なのに、結果が異なっていました。英語で作業して
  いない人は、これにより不利な扱いを受けていました。Maskuroは現在、
  対応する11言語すべてで同じフィールド名を認識します。

- **「原本を復元」が、スキャンされたページで取り戻しすぎていました。**
  住所ブロックの黒塗りされた1行の上に枠をかけると、**ブロック全体**が
  再び公開されてしまい、ページは断片化した状態で残っていました。バーの
  残骸が残り、そこから個々の単語の一部が突き出ていました。原因は、
  ラスタライズされたページ上で上下に並ぶバーが接触しており、そのため
  1つの領域として扱われていたことでした。現在は、枠が指し示す行だけが
  正確に復元されます。隣接する行は黒塗りされたままで、対象となった
  行のバーは完全に消えます。

- **明細一覧内の数量表現が、住所と誤認されていました。** 「1.4
  Kabelgraben  100,00  m」のような行では、「Kabelgraben 100」が番地
  付きの通りとして置換されていました。このような行は現在そのまま
  残ります。実際の住所(「Hauptplatz 1, 3250 Wieselburg」など)は
  変わらず認識されます。

- **会社名の前で、文の半分が消えていました。** 「Vertrag zwischen der
  Firma Gottwald GmbH & Co KG und dem Auftraggeber.」から
  「[ORGANISATION_1] und dem Auftraggeber.」になっていました――文頭が
  なくなり、何についての文書かという手がかりも失われていました。現在は
  会社名自体だけが削除されます。総称語が名前の一部を成す場合
  (「Deutsche Bank AG」「Universität Wien」)は、これまでどおりです。

- **議事録で、氏名が同時に職業でもある発言者がそのまま残っていました。**
  発言の前にある「Bauer:」「Koch:」「Weber:」は見落とされていました
  が、隣の「Gruber:」はそうではありませんでした――Maskuroはこれまで、
  行を発言として読み取るために、文書内に少なくとも1つ検出済みの氏名を
  必要としていました。文書に「Ergebnisprotokoll」や「Niederschrift」
  のような見出しがある場合、現在はそれだけで十分です。注意書きの行
  (「Achtung: …」「Hinweis: …」)は影響を受けません。

- **フィールドの見出しが、その値とともに消えていました。**
  「Projekt: Sanierung und Erweiterung Gemeindezentrum」から単一の
  プレースホルダーが生成されていました――「Projekt:」という単語自体も
  消えており、その箇所に何が書かれていたかの手がかりも失われていま
  した。見出しは現在そのまま残ります。見出しが項目自体の一部であり、
  その意味を担っている場合(「Durchwahl 214」など)は、変化ありません。

- **最大検出は、専門用語を除去できていませんでした。**
  「Flachdach」「Einspeisepunkt」「Elektrotechnik」やその他の専門用語
  は、AI段階が有効な場合でも地名や会社名として置換されていました――
  AIには、まさにこうしたヒットが判定のために提示されることが一度も
  ありませんでした。現在はAIもこれらを検証します。入札・契約文書の
  コーパスでは、これにより27件の誤検出すべてが解消され、しかも本物の
  項目が1件も残されることはありません。氏名、会社名、地名は変わらず
  検出されます。

- **施設の種類を表す総称語が、組織名として扱われていました。** ある
  契約文書では、「Hochschulen und Universitäten」「Staatliche und
  private Schulen」「Akademische Lehrkrankenhäuser」
  「Bildungseinrichtung」「Zulieferfirmen」といった、特定の施設では
  なく施設の種類を表す語が消えていました。これらは現在そのまま残り
  ます。前に固有名詞がある場合(「EU-Kommission」など)は引き続き
  置換され、会社名はこのルールの対象にそもそも含まれません。

- **一覧内の氏名は、それがよく知られたものである場合にしか対象になり
  ませんでした。** 参加者一覧や出席者一覧で、列見出し「Name」の下に
  ある「Anna Huber」や「Thomas Müller」は削除されましたが、「Wójcik
  Aleksandra」や「Kücükgöl Sinan」は削除されませんでした――同じ行、
  同じ構造にもかかわらずです。あまり一般的でない氏名を持つ人は、これに
  より保護が弱くなっていました。現在は列見出しが判断基準です。
  「Name」の下にあるものは氏名として扱われます。事務的な列見出しを
  持つ明細一覧は、影響を受けません。

- **「Durchwahl(内線)」の後ろにある電話番号が、途中で切断されていました。**
  「Durchwahl 0732 771190」から「[DURCHWAHL_1] 771190」になっていました
  ――番号の後半が読める状態で残っていました。現在は番号全体が完全に
  削除され、見出しはそのまま残ります。本物の内線番号(「Durchwahl 214」)
  は変わらず見出しごと置換されます。

- **一部のPDFが、まったく浄化できなくなっていました。** 画像内のカラー
  プロファイルやメタデータが確実に削除できなかった場合、処理は結果を
  出さずに中断していました――影響を受けたのは、約款のページ、仕様書、
  入札書類のような通常の業務文書でした。こうしたファイルは現在浄化
  され、残った箇所については警告がその位置を示します。それらには
  機器、生成元、または撮影の識別情報が含まれている可能性があります。
  原本はこれまでどおり変更されません。

- **契約上の役割が、人物として扱われていました。**
  「Bieter」「Verbraucher」「Mieter」「Käufer」「Auftraggebers」や
  約40の役割語が、冠詞なしで単独で書かれている箇所――契約書の見出し、
  表の列、署名欄――で置換されていました。人物データを1つも含まない
  契約文書が、これにより部分的に読めなくなっていました。これらの単語は
  現在そのまま残ります。隣に人物を示す手がかり――敬称、名、
  「Ansprechpartner」のようなフィールド語――がある場合は、引き続き
  置換されます。「Herr Bieter」や「Frau Käufer」は氏名です。職業でも
  ある一般的な姓(Bauer、Richter、Koch)は、このルールの対象にそもそも
  含まれません。

- **略称で書かれた通り名は、番地がピリオドに直接くっついている場合、
  見落とされていました。** 「Schlesischestr.31」は住所として認識され
  ておらず――隣接する郵便番号は住所検出からその手がかりを得るため、
  それも残ったままでした。結果には、通り名と郵便番号から成る住所が
  再構成可能な状態で残っており、しかも同じ文書内の一部のページでしか
  それが起きていませんでした。両方とも現在は一緒に削除されます。数字が
  付いた一般名詞(「Kabelrinne200」)はそのまま残ります。

- **2行にまたがる住所が、1つのプレースホルダーにまとめられていました。**
  住所ブロックで郵便番号が通り名の上にある場合、Maskuroは両方の行を
  1つの該当箇所として結合していました。結果では改行が消え、郵便番号
  だけがその前に読める状態で残っていました。現在は各行が個別に検出
  され置換され、レイアウトは保たれます。同じ原因により、時折、上の行
  にある姓が住所に巻き込まれることもありました。

- **最大PDF処理は、もう原本のオブジェクトを引き継がなくなりました。**
  文字認識が有効な場合、Maskuroは表示されているPDFium画像から各ページ
  を完全に再構築します。新しい最小構成ファイルには、この画像ページと、
  OCRテキストに限定して新規生成された検索レイヤーだけが含まれます
  ――コメント、添付ファイル、アクション、レイヤー、メタデータ、カラー
  プロファイル、秘密鍵を含む元のオブジェクトツリーは含まれません。
  これは注釈の外観、パターン、Type 3フォント、フォームオブジェクト、
  ソフトマスク内のコンテンツにも適用されます。元ファイルは変更され
  ません。

- **入れ子になったPDFグラフィック内の顔やコードが、見落とされていま
  した。** 両方の検出器は現在、レンダリングされたページ画像全体も
  追加で参照します。これにより、注釈、パターン、Type 3グリフ、透過
  マスク内の肖像写真やQR・バーコードも検出器に届くようになりました。
  検出された領域は(有効な場合)最小構成の構築前に判別不能にされます。
  検出自体には引き続き誤りがあり得ます。

- **OCRエンジンが不足している場合、PDFで内部エラーが発生していました。**
  最大処理は現在、不完全または未検査のファイルを出力する代わりに、
  制御された形で、対象ファイルを出さずに中断します。

- **複数の実在する連絡先・業務データが、専門的なテキストが置換される
  一方で見逃されていました。** 改行をまたぐ氏名欄、銀行名・会社名、
  法人格、見出し付きの識別番号、生年月日、電話・URL・IBANの境界検出が
  より厳密になりました。同時に、地の文中の国名、役割語・総称語、
  品目・規格コード、数字の羅列、一般的な略語は、より頻繁にそのまま
  残されるようになりました。

- **混在した回転OCR行が、誤って読み取られていました。** 不確実な縦書き
  単語は現在、ローカルで向きを直してから再読み込みされます。非ラテン
  文字のテキスト内の技術的なラテン文字の値は、独立した英語での照合を
  受けます。孤立した不確実な単一の数字は、2つの近接する数字列が一致
  する場合にのみ修正されます。「sp. z 0.0.」というOCR形式のポーランド
  の法人格表記は、文脈が確定している場合「sp. z o.o.」として読み取ら
  れます。

- **画像測定が、部分的に見える残存値を見落とすことがありました。** 現在
  は重なり合うローカルな切り出し範囲を検証し、黒塗りバー上の白い
  プレースホルダー文字を原本の字形と区別し、生画像の矩形を、回転され
  再レンダリングされた最小PDFにも反映します。固定の合成メインコーパス
  では、これにより目標項目1,392/1,392件が削除され、誤検出0件、処理
  エラー0件となりました。これはコーパスに基づく証跡であり、一般的な
  100%保証ではありません。

- **非商用の言語モデルはもう提供されません。** CC BY-NC-SA 3.0の下に
  ある6種類のイタリア語・ギリシャ語spaCyバリアントは、カタログ、
  ダウンロード、読み込み経路から削除されました。すでに存在するモデル
  フォルダも無視されます。両言語は代わりにMITライセンスの多言語
  モデルを使用します。

- **「Ansprechpartner(担当者)」の下の氏名が、半分しか削除されていま
  せんでした。** 見出しが単独で1行にあり、その下に「Nachname
  Vorname(姓 名)」がある場合、名が同時に一般的な単語でもあると、
  名がそのまま残っていました――「Mayer Roman」が「[NAME_1] Roman」に
  なっていました。このような行は現在、丸ごと処理対象になります。
  同じ位置にある部署名(「Technischer Innendienst」)は引き続き影響を
  受けません。ついでに修正:「Ansprechpartner」はこれまで氏名フィール
  ドとしてまったくカウントされていませんでした。「Kontaktperson」は
  以前からそうだったにもかかわらずです。

- **法人格のない会社名が、業種を表す語が間にある場合はそのまま残って
  いました。** 「Kranzbichler Handels GmbH」は削除されていましたが、
  3段落後にある単独の「Kranzbichler」は削除されず――「Kranzbichler
  GmbH」の場合は削除されていました。現在はどちらも対象になります。
  一般的な単語はこれの対象外です。「Deutsche Bank AG」があっても、
  本文中の「deutsche」が会社名扱いになることはありません。

- **同じ値が、同じ文書内で氏名と地名の両方として扱われていました。**
  「Anna Musterfrau … Musterfrau」は「[NAME_1]」と「[ORT_1]」になって
  いました――2箇所目には名が欠けており、それがないと地名として扱われて
  いました。どちらも削除はされていましたが、まるで2つの別のものである
  かのように読めていました。値は現在、最初に出現した際の分類を保持
  します。

- **日付が削除されなくなっていました。** 数字だけから成る日付
  (「01.03.2026」)は、前回のフッション以降、氏名向けに作られた検査に
  引っかかり、文書内にそのまま残っていました――「置換」の動作モードで
  もです。検査報告書にも記録されていませんでした。影響を受けたのは
  日付表示を明示的にオンにしていた人だけでした。

- **国名や大陸名は、もう黒塗りされなくなりました。** 「The Lieferung
  geht in die Vereinigten Staaten」「Marktschwäche in Asien」
  「die Norm gilt in Rumänien」――こうした表現は人物について何も語って
  おらず、現在はそのまま残ります。国名が住所の一部である場合、または
  「Wohnsitz」や「Geburtsort」のような見出しの後ろにある場合は、引き
  続き削除されます。**都市名は影響を受けません**――「Ich bin gerade
  in Bilbao」は引き続き人物についての情報として黒塗りされます。

- **省略された単語が、ウェブアドレスになっていました。** 本文に
  「bzw. deutsche」や「incl. der」とある場合、一部のPDFではピリオドの
  後ろにスペースが入らずに出力され、「bzw.de」や「incl.de」――国コード
  付きの有効なアドレスになり、削除されていました。このような単語の
  組み合わせは現在そのまま残ります。実際のアドレスは影響を受けません。
  「www.」が前に付いていない場合でも同様です。

- **決算書の数字の列が、電話番号として黒塗りされていました。** 事業
  報告書や価格表では、前年度と当年度が並んで表示されます――
  「64.518  65.133」のようにです。これは電話番号1件として扱われ削除
  されていました。「12200-23200」のような数値範囲や、数字が続く日付も
  同様でした。こうした数字は現在そのまま残ります。逆に、本物の電話
  番号はより確実に認識されるようになりました。「Telefon」「Fax」
  「Mobil」「Durchwahl」やその他の画面表示言語での対応語が現在は
  カウントされます――これまでプログラムはそこで英語の単語しか認識して
  いませんでした。

- **番号付き表内の氏名が、そのまま残っていました。** 参加者一覧や人事
  表の一般的な形式――列見出しの下に「1.1 Auersperg Bernhard Montage
  03.03.2026」――は、まったく浄化されていませんでした。このような行
  は、専門用語がそのまま残るべき見積書の明細一覧のように見えていた
  のです。列見出しに人物を示す語(「Name」「Nachname」「Surname」など)
  がある場合、現在はその下の行は氏名として扱われます。明細一覧は、
  レターヘッドに「Sachbearbeiter:」とある場合でも、これまでどおり
  対象外のままです。

- **1つの氏名から、隣り合う2つのプレースホルダーが生成されることが
  ありました。** 姓が文書内に単独でも存在する場合、後処理は「Anna
  Musterfrau GmbH」のような箇所で、まず姓を、次に名を置換していまし
  た――結果としてまるで2人の別人がいるかのように見えていました。現在は
  最も長い既知の氏名が優先されます。

- **架空の値が、対応関係にまったく記録されていませんでした。** 「値を
  架空にする」を選択した人は、「Anna Musterfrau」が「Greta
  Mayrhofer」になった結果を受け取っていましたが、同じ文書内に匿名化
  された置換が1件でもあると、対応関係にはそれについて何も記載され
  ませんでした。これにより、架空の値を復元することができず、対応
  関係ファイルはその置換について何も語っていませんでした。最も深刻
  だったのは3つ目の点です。結果を読む人は、説得力のある名前を目にし、
  それが架空のものであるという手がかりを一切持てません。現在は、
  すべての置換が対応関係に記録されます。

- **対応関係は、黒塗りされたものも「置換」と呼んでいました。** メール
  は添付ファイルと対応関係を共有しており、添付ファイルは黒塗りされる
  一方、本文にはプレースホルダーが付くことがあります。対応関係には、
  この3箇所すべてに対して同じ「置換」という表記があり、復元処理は
  添付ファイル内に存在しないプレースホルダーを探していました――バーは
  そのまま残っていました。現在は、各該当箇所ごとに実際に何が起きたか
  が記録され、両方の添付ファイルとも復元されます。

- **画像内にしか存在しない値は、復元できませんでした。** ヒットパネル
  には二重に表示されていました――1つは、文書内のどこにも存在しない
  プレースホルダーとして(「プレースホルダーは文書内に見つかりません
  でした」)、もう1つは黒塗りされた箇所としてです。最初の行は単なる
  帳簿上の記載であり、なくなりました。

- **黒塗りされた値は、一度しか復元できませんでした。** 同じ値が複数
  箇所にある場合、1回のクリックですべて復元されますが、残りの行は
  ヒットパネルに残ったままで、そこを次にクリックすると「一意では
  ありません」と表示されていました。現在はそれらも一緒に消えます。

- **学習モードがオフの場合、復元操作が検査記録に記載されていません
  でした。** 修正ウィンドウで復元した値を元に戻した人は、学習用の
  質問がオフになっている場合、この操作を検査記録の中に見つけられ
  ませんでした――証跡が、ルール提案のみを意味するスイッチに紐づいて
  いたのです。検査記録が有効な場合、現在はこれとは独立して理由が尋ね
  られ、行が記録されます。

- **ドラッグ&ドロップで挿入されたファイルは浄化されず、通知すらされて
  いませんでした。** ファイルを添付として送る代わりに文書にドラッグ&
  ドロップすると、WordやPowerPointはそれを文書内に完全に格納します。
  これは結果の中に、その元のファイル名や格納パスとともに変更されずに
  残っていました――そしてこれらは実務上、それ自体に氏名を含んでいる
  ことがよくあります。このようなファイルは現在、文書の他の部分と
  同様に浄化されます。

- **それができない場合、Maskuroはその旨を伝えます。** 埋め込みオブジェ
  クト内に、浄化手段のない旧形式(Word 97、Excel 97)が含まれている
  場合、現在はそのファイル名を含む注意メッセージが表示されます。これ
  まではそれが黙って変更なしにそのまま渡されていました。

- **切断された単語や略語が、氏名として扱われていました。** PDF内で
  単語が行末で分割されている場合、一部のファイルの読み取り時に
  断片が生じます――「Jahresent… gelts」「Gewerbli…」のようにです。
  このような断片、結合された単語(「TürverschlussmitV」)、単独の
  略語(「JY」「FFB」)は、氏名であるかのように黒塗りされていました。
  現在はそのまま残ります。同じ分割の問題を持つ氏名は、敬称が伴う
  限り引き続き黒塗りされます。もともと単語の途中に大文字を含む氏名
  (McKenzie、MacDonald、LeBlanc)は、そもそもこれの影響を受けません。

- **測定値や月が、住所として扱われていました。** 技術書類では、
  「2000 Lux」「1200 Mbit」「1500 Watt」「5308 Platz」「2022 Mrz」が
  黒塗りされていました――4桁の数字と大文字で始まる単語が、郵便番号と
  地名のように見えていたのです。郵便番号は現在、住所の手がかりが
  同時にある場合にのみカウントされます。国コード、フィールド見出し、
  行頭、上の行にある通り名、または言語検出がそこに見出した地名です。
  5件の仕様書では、これにより本物の住所を1件も残すことなく14件の誤った
  黒塗りが解消されます。

- **より精密な検出が、対象を広げすぎていました。** 追加で有効化できる
  「より精密な検出」段階は、ドイツ語の業務文書で専門用語を氏名や地名
  として扱っていました――「Photovoltaikanlage」「Einspeisepunkt」
  「Flachdach」「Personaleingang」――さらに、連続した明細一覧の中の
  会社名も黒塗りしていました。原因は、ある種の保護措置にありました。
  そのヒットは、明細行や一覧行を検出する検査から除外されていたのです。
  この保護は現在、その段階が対象とする複数語から成る氏名にのみ適用
  されます。つまり、一覧行の中の「Anna Huber」は引き続き検出され、
  明細行の中の単一の専門用語は対象外になります。技術的な入札書類では、
  これによりこの段階の誤った黒塗りが半減し、氏名が失われることは
  ありません。

- **図表が、確認されないまま完全な元データを持ち込んでいました。**
  WordやPowerPointに図表を挿入すると、プログラムはその計算に使った
  表を独自のファイルとして文書内に格納します。目に見えるのは図表内の
  わずかな数字だけですが、表には、図表にはまったく現れない行も含めて
  全体の一覧が記載されています。この表はこれまで変更されずにそのまま
  渡されていました。現在はこれも、文書の他の部分と同じプレースホルダー
  で一緒に浄化されます。

- **OpenDocumentファイル内の埋め込みオブジェクトについても同様です**
  (ODT、ODS、ODP)。挿入された図表や表が、変更されないまま残ってい
  ました。

- **Word文書:脚注と注釈が浄化されていませんでした。** そのテキストは
  結果の中に完全にそのまま残っていました――氏名、住所、口座番号も
  含めてです。脚注や注釈を含むすべてのWord文書が影響を受けていました。
  同様に、文書に見えない形で付随する定型句(オートテキスト)も影響を
  受けずそのままでした。

- **Word:選択リスト、コメント、画像の説明文の中の情報。** 選択
  フィールドの項目(展開して初めて表示される)、コメントの作成者、
  図の説明文、参照コマンドの後ろにあるアドレスは、結果の中にそのまま
  残っていました。

- **Excel:ピボットテーブルが元データを二重に保持していました。**
  ピボットテーブルを含むブックは、その中に集計元の行の完全なコピーを
  ――見えない形ですが、ファイル内に――保持しています。このコピーは
  これまで、シート自体のすべてが置換されていても変更されないままでした。
  ピボットテーブルとともに配布されるあらゆる集計が影響を受けていました。

- **Excel:会話形式のコメントとその作成者。** 新しいタイプのコメントの
  テキストと、コメント作成者の一覧――表示名とログインID、企業では
  たいていメールアドレス――が、結果の中にそのまま残っていました。同じ
  一覧はWord文書にも同様に含まれていました。

- **WordとExcelの独自定義された文書プロパティ。** 「Mandant(依頼人)」
  や「Aktenzeichen(事件番号)」のような、法律事務所がテンプレートに
  付与するフィールドは、これまで浄化されていませんでした。どの表示にも
  見えないにもかかわらず、コピーのたびに一緒に運ばれます。

- **表計算(ODS):セルの選択リスト。** 前のフッション以降のExcelと同様
  に、現在はOpenDocumentの表計算でも、セルを展開した際に表示される
  内容が浄化されます。他のセルへの参照は、一覧が引き続き機能する
  ように変更されません。

これらすべての箇所は、これまでどおり対応関係を通じて復元できます。

- **Outlookのメッセージ:破損したファイルが浄化処理を強制終了させて
  いました。** 特定の破損した`.msg`ファイルは、通知の代わりに処理の
  中断を引き起こしていました。現在は、読み取り可能な範囲で読み取ら
  れます。

- **対応関係ファイルは、現在はお客様だけが読める状態になっています。**
  このファイルには元のデータが平文で含まれており、これまでは通常の
  権限で結果の隣に置かれていました――共有の保存場所では、誰でも開ける
  状態でした。浄化済みの結果自体には変化はありません。これは配布される
  ことが前提だからです。

- **再取得された言語モデルは、展開前により厳密に検証されます。** 細工
  されたパッケージ――例えば複数の作業台に配布される社内共有からの
  ものなど――は、展開時に想定されたフォルダの外にファイルを配置できて
  しまう可能性がありました。通常の再取得には変化はありません。

- **スクリーンショットを撮影――そのまま浄化されます。** `Ctrl+Shift+B`、
  「ファイル→スクリーンショットを撮影…」、またはタスクバーの
  アイコンから、画面上に枠をドラッグします。その中にあるものは、
  その後他のファイルと同じ経路をたどります。文字認識が画面上の
  テキストを読み取り、氏名、住所、電話番号、メールアドレスが黒塗り
  され、その後画像がエディタで開き、見落とされたものを枠で追加の
  黒塗りができます。浄化された画像はデスクトップ(または設定した
  出力フォルダ)に保存されます。**加工前**の撮影データはどこにも
  保存されず、終了時に削除されます。文字認識は、普段オフの場合でも
  この処理のためにオンになります。それなしでは画像から何も見つけ
  られないためです。Macでは、初回にシステムが「画面収録」の許可を
  尋ねます。

- **画像に描画できるようになりました:四角形、楕円、矢印、テキスト、
  番号付きの手順マーク。** 6色、3種類の線の太さから、1~5のキーで
  選べます。これはスクリーンショットや手順書向けのものです。別の
  プログラムを開くことなく、重要な箇所を示せます。取り消しやハンドル
  でのドラッグ調整は、他のバーと同様に適用されます――つまり注釈は、
  一度配置した後でも移動やサイズ変更ができます。
  **描画は、明示的に黒塗りではありません。** 描画された四角形は枠で
  あり、バーではありません。その下にあるものは引き続き読める状態で
  あり、ファイルとともに送り出されます。項目を削除するには、これまで
  どおり「黒塗り」と「モザイク」があります。そのため描画ツールは
  ツールバーの独立した行にあり、そのいずれかが選択されている間は、
  案内表示がその旨を伝えます。

- **編集した画像を、クリック一つでクリップボードに送れます。**
  エディタの「画像をコピー」(または`Ctrl+C`)は、表示されているとおり
  の状態で画像を保存します――貼り付けるだけでメッセージやメールに
  組み込めます。これにより、キー操作からチャットに届くまでが4ステップ
  で完結し、フォルダも不要です。

- **さらに蛍光ペン、影、グラデーション。** 「強調表示」は領域を色付け
  しますが覆い隠しません――その下の内容は読める状態のままで、まさに
  この点がバーとの違いです。「影」は乱雑な背景から注釈を浮き上がら
  せ、「グラデーション」はドラッグの方向に色を薄くしていきます――
  どちらも6つの描画ツールすべてに適用されます。

- **誰かに影響が及ぶ前に修正されたもの:** 新しいツール行は、すでに
  Maskuroを使ったことがある人にとってはほぼ空に見えていたはずです
  ――記憶されているウィンドウ配置がそれ以前のものであり、この行の
  ための余地がなかったのです。古い配置は現在破棄され、エディタ
  ウィンドウは一度だけ基本の配置で表示されます。

- **独自のスクリーンショット機能をオフにできます。** Greenshot、
  ShareX、切り取り&スケッチに慣れている人は、「設定→プログラム」で
  「Maskuroでスクリーンショットを撮影」をオフにできます。その場合、
  Maskuroはそもそもキーボードショートカットを登録しません――それは
  お使いのツールのために残ります――そしてこの切り替えは再起動なしで
  即座に反映されます。このようにして撮影された画像は、引き続き浄化
  できます。Ctrl+Vでクリップボードからウィンドウに取り込めます。

## 0.10.37-alpha.20260821 – 2026年8月21日

### 新機能

- **匿名化の際、現在は各該当箇所が独自の番号を持ちます。** これまでは
  すべての人物が`[NAME]`、すべての地名が`[ORT]`と呼ばれていました――
  そのため、どの箇所がどの値に属するかが判別できず、復元するものも
  ありませんでした。現在は、出現ごとに番号が続けてカウントされます。
  同じ氏名が3箇所で`[NAME_1]`、`[NAME_3]`、`[NAME_7]`のように表示され
  ます。文書内では、どの箇所同士が対応しているかは引き続き判別でき
  ませんが、対応関係ファイルがあれば個々の箇所を復元できます。そのため
  対応関係ファイルは匿名化の際にも再び選択可能になりました。結果とは
  別の場所に保管してください。
- **月、曜日、通貨、単位、会社の法人格が、全48の文書言語で氏名や地名と
  みなされなくなりました。** カレンダーや単位の名称はUnicode CLDR
  から取得され(生成されたもので、手書きではありません)、法人格は
  各国の会社法から取得されます――複数語のもの(「sp. z o.o.」、
  「Pty Ltd」)や前置されるもの(「株式会社」)も含みます。月名が同時に
  名でもある場合(Juli、August、May)は、その構造で判断されます。日付
  や年が隣にあれば日付、なければ名前です。これに加えて、敬称・肩書き、
  挨拶文全体、書類の種類、独自の言語モデルを持つ28言語向けの通り名の
  基本語、法令略号(DSGVO、UStG、ABGB、§ 6 Abs 1 Z 27 UStG)、
  フィールド値としての言語名(「Sprache: Deutsch」)も対象です。これら
  の一覧は「ヘルプ→語句リスト…」にあります。
- **インド:住所とPINコードが認識されます** ――「15 गांधी मार्ग」
  「नई दिल्ली 110001」および「15 Gandhi Marg, New Delhi 110001」の
  両方です。インドの国別パッケージはこれまで識別番号しか認識しておらず、
  ヒンディー語の文書では住所がそのまま残っていました。
- **浄化された各オフィスファイルは、提供前にもう一度パッケージとして
  開かれます。** テキスト抽出処理では、Word、Excel、LibreOfficeが
  そのファイルを拒否するかどうか(重複した項目、途切れたXML、部品の
  欠落)には気づきません。そして、浄化によって決して変わってはならない
  もの――PDFのページ数、表計算のシート・行・セル、プレゼンテーション
  のスライド数――が原本と照合してカウントされます。この検証に引っかかる
  と、結果と検査報告書に注意警告が表示されます。原本は変更されません。
- **自動処理も、フィールド全体を黒塗りします。** 黒塗りモードでは、
  短い行(住所ブロック、表のセル、ヘッダー情報)では、バーが発見された
  値だけでなく行全体を覆うようになりました。単語の長さに合わせたバーは、
  その単語がどれだけ長かったかを漏らしてしまうためです。隣の見出しや
  金額はそのまま残り、地の文の行(テキスト幅の半分より長い場合)は
  引き続き単語単位で黒塗りされます。氏名1つで文全体が黒塗りされる
  ことを防ぐためです。
- **復元されたものが、再び原本と同じ見た目になります。** PDFエディタの
  「原本を復元」と「置換を取り消す」は現在、その範囲を元ファイルから
  正確に書き戻します――同じフォント、同じサイズ、同じ色、同じ位置、
  スキャンでは同じ画素です。これまでは、テキストが代替フォントで再
  挿入され、明らかに再構成されたように見えていました。以前の黒塗り
  のバーは、白で上塗りされるのではなく完全に消えます――表内の色付き
  のセル背景は保たれます。これは回転したページ、組み込まれたフォーム
  オブジェクト内のテキスト、そして**入力済みのフォームフィールド**
  にも適用されます。そのためにラスタライズされた作業コピー上でも、
  新たにレンダリングされた原本ページの該当部分が戻ってきます――
  テキストレイヤーがフィールド値を認識していない箇所でもです。PDF内で
  **置換された画像**もこの方法で戻ります――モザイク、ぼかし、または
  完全削除されていたもの、画像全体でもドラッグした一部でもです。元
  ファイルが結果の隣にもう存在しない場合にのみ、これまでの方式が
  使われます。
- **黒塗りおよび跡形なく削除された値は、Word、Excel、PowerPoint、
  OpenDocumentでも復元できます。** これまで、そこでの復元にはテキスト
  内のプレースホルダーが必要でした――バーや空白には戻す手段がありま
  せんでした。現在は、手を加えていない元ファイルが結果の隣にある場合、
  ヒットパネルが「黒塗り」「削除」という行を提示します。Maskuroは結果
  と原本を比較し、バーや空白の位置に値を書き戻します――書式込みで、
  分断された文字列も一つにまとまります。これはテキスト、HTML、メール、
  そしてメールのオフィス添付ファイルにも同様に適用されます。メール本文
  にプレースホルダーがあり、添付ファイルにバーがある場合、両方が一度に
  復元されます。
- **メールやOutlookメッセージのPDF添付ファイルも復元できます**――
  プレースホルダー(番号付き・匿名の両方)、バー、跡形なく削除された
  ものです。キャンバスがない場合、位置は元の添付ファイルから取得され、
  値は原本の読み取り順序に沿って、字形単位で正確に戻ってきます。
- **マスクされた値も復元できます**――PDFとテキスト表示の両方です。
  マスク(「**** **** **** **** 3201」)は決して一意ではなく、2つの
  番号が同じマスクを持つことがあります。そのため復元処理は決して文字
  どおりの経路を取らず、原本にその箇所にどの値があったかを問い合わせ
  ます。これまで、こうした行はヒットパネルでまったく操作できません
  でした。
- **Word、Excel、PowerPoint、OpenDocument内の埋め込み画像を復元でき
  ます。** 画像内で黒塗りされた値は、そのパネル行から復元できます
  ――Maskuroは元の画像を読み取り、まさにその箇所を取得します。ぼかし、
  削除、あるいは顔やコードが処理された画像は、編集メニューの新しい
  項目「埋め込み画像を復元」で、元ファイルから丸ごと取得されます――
  メールやOutlookメッセージのオフィス添付ファイルを通じても機能します。
  それ自体が添付ファイルとして存在し、文字認識によって黒塗りされた
  画像も、同様にそのパネル行から復元できます。
- **架空の値は、テキスト表示でも復元できます。** これまで、パネルは
  そこで「一意ではありません」と表示していました。現在は復元処理が
  原本内でその値を検索し、結果内の同じ箇所にまさにその架空の代替値が
  あることを求めます――架空の氏名は、どこかに実在の氏名として存在する
  可能性があるため、文字どおりすべての箇所で置換されることは決して
  ありません。
- **Word、Excel、PowerPoint、OpenDocumentでの復元は、原本の書式を保持
  します。** ある値が複数のテキストランにまたがっていた場合(「Anna」が
  通常の書式、「Musterfrau」が太字・赤色など)、これまでは復元される際
  すべて最初のランに入ってしまい、太字や色が失われていました。現在は
  文字が原本と同じように再び配置されます。Wordの段落は、その後
  バイト単位で原本と同一になります。同じことがHTMLページ、メールの
  HTML部分、Outlookメッセージ(.msg)のHTML本体にも当てはまります――
  メールについては、これまで浄化処理が黙って削除していたDoctypeも
  保持されるようになりました。
- **テキストファイルは、そのエンコーディングを保持します。** 浄化と
  復元は現在、`.txt`、`.md`、`.csv`を、それらが供給された際の
  エンコーディング――BOM付き/なしのUTF-8、UTF-16、Windows-1252――で
  書き込みます。これまでは、Windows-1252のファイルは常にUTF-8になり、
  UTF-16のファイルは、たとえ置換対象が何もなくても破損した状態で
  戻ってきていました。
- **復元された画像は、その色モードを保持します。** グレースケール
  スキャンはグレースケールとして戻り、3倍のサイズのRGBファイルには
  なりません。パレット画像はパレットのまま、白黒は白黒のまま――画像
  全体が原本と同じ値を保ちます。これは画像ファイルとPDF内の画像の
  両方に適用されます。CMYKと16ビットはRGBのままです。PNG形式の結果は
  どちらも扱えないためです。
- **画像内の枠は、それが触れた編集内容すべてを一度に復元します。**
  モザイク処理された顔は、検出された矩形の周りに余白を持っており、
  顔の上だけに枠をドラッグした人は、モザイクの輪だけが残っていました。
  現在は、枠が原本との差分がある連続した領域全体まで自動的に広がり
  ます――目の周りに枠をかけるだけで十分です。隣接する独立したバーは
  そのまま残ります。写真全体が削除またはぼかされている場合は、これ
  までどおりドラッグした枠がそのまま適用されます。画像ファイルとPDF内
  の画像の両方に適用されます。
- **行全体にわたる黒塗りバー。** エディタの行モードでは、現在バーは
  対象となった単語だけでなく、行の最初の単語から最後の単語まで伸び
  ます――単語の長さに合わせたバーは、その単語がどれだけ長かったかを
  漏らしてしまい、郵便番号の前の6文字から地名を推測できてしまうため
  です。値の隣にある見出し、金額、表の列はそのまま残ります――バーは
  フィールドを覆うのであって、請求書の行全体を覆うわけではありません。
  「テキスト行」の隣にある新しいスイッチ「行全体」は、隣接する単語を
  残したい場合に単語単位に戻せます。この選択は記憶されます。

### 修正

- **HTMLページやメール内の画像が未検査のままでした――浄化後もロゴの
  中の氏名が読める状態で残っていました。** ページに埋め込まれた画像
  (`data:`アドレス)は、その代替テキストしか処理されていませんでした。
  メールのHTML部分にあるロゴ(ファイル名のないインライン画像)は
  添付ファイルフィルターをすり抜け、名前付きの画像添付ファイルでは
  画像ルール(「ぼかす」/「削除」)が機能していませんでした。現在は
  この3つすべてが、画像ファイルと同じ経路をたどります。保持される
  画像内の文字認識、顔、コード、メタデータ、画像ルールです。報告書には
  画像が明記されます――文字認識なしで未検査のまま残る場合の警告も
  含めてです。「埋め込み画像を復元」およびヒットパネルからの復元も、
  これらの画像を認識するようになりました。
- **文字認識がその言語に対応していない場合、画像を含むオフィス
  ファイルはまったく浄化できませんでした。** Macではシステム標準の
  文字認識が使われますが、ヒンディー語、ギリシャ語、クロアチア語、
  リトアニア語には対応しておらず、最近はその旨も伝えるようになり
  ました――しかしWord、Excel、PowerPoint、OpenDocumentでは、これに
  よって浄化処理**全体**が中断し、ファイルが生成されませんでした。
  それでいて、テキスト自体は問題なく浄化できていました。画像だけが
  読み取れなかったのです。現在は、PDFや単体の画像と同様にファイルが
  書き出され、結果には画像が【確認されていません】と表示されます
  ――理由と「言語を管理」への案内付きです。
- **Excelブックでは、選択リスト内の氏名がそのまま残っていました。**
  ドロップダウンフィールド(データの入力規則)の一覧が、現在は他の
  セル内容と同様に浄化されます。セル範囲への参照は変更されません。
  ブックが壊れないようにするためです。
- **プレースホルダーが収まらない箇所には黒いバーが表示されていました
  ――現在はより短い表記が表示されます。** `[GEBURTSDATUM_1]`ではなく
  `[GEBU_1]`が表示され、最も短い表記でも収まらない場合にのみ黒塗り
  されます。バーはもう誰にも「そこに何かがあった」ことを伝えません。
  短いプレースホルダーはそれを伝えます。修正エディタはすでにこれが
  できていましたが、自動浄化はできていませんでした。対応関係ファイルは
  両方の表記を同じ値に紐づけるため、短縮された表記からも復元できます。
- **「置換」への最初のクリックが、修正ウィンドウを一瞬止めていました。**
  プレースホルダーの種類を判定する処理(`[BEGRIFF_3]`ではなく
  `[NAME_3]`にする)が、まさにこの瞬間に読み込まれていました――約2~3秒
  です。これは現在、ウィンドウを開く際にバックグラウンドで準備される
  ようになりました。測定では2289ミリ秒が193ミリ秒になりました。
- **2つの浄化処理が同時に、同じ言語モデルを二重に読み込むことが
  ありました**――例えばフォルダ監視とメインウィンドウが同時に動作した
  場合です。各モデルは数百メガバイトを占有するため、メモリ使用量が
  一時的に2倍になっていました。現在は、2つ目の処理が最初の処理の
  モデル読み込みを待つようになりました。
- **日付行内の地名は、言語モデルが単独では認識できない場合でも現在は
  削除されます。** 郵便番号と地名の組み合わせとして確実に検出された
  もの(「3335 Amstetten」)は、その地名を文書全体にわたって連動させ
  ます――フルネームから姓を連動させるのと同様です。また、氏名の前に
  ある数字付きの略号(「T3 Hofbauer Christian」)は、プレースホルダー
  に巻き込まれることなく読める状態で残ります。
- **実在する発注書の再確認から見つかった3つの漏れを解消しました:**
  担当者「T3 Hofbauer Christian」は「T3」という略号のせいで列見出し
  とみなされ、読める状態のまま残っていました。言語モデルが改行をまた
  いで列見出しに読み込んだ地名は「Pos.」を巻き込み、顧客の名だけを
  そのまま残していました。そして敬称付きの氏名(「Herr Robert
  Köttel」)は姓だけを連動させ、名や「Herr」のすべての出現を連動させて
  いませんでした。略号は現在、純粋な文字だけを対象とし、2語の氏名は
  列見出しとして扱われず、ヒットは列見出しの手前で区切られ、敬称は
  氏名にカウントされません。
- **住所ブロックの真下にある日付行の地名(「Melk, 05.08.2026」)が、
  読める状態で残っていました。** 言語モデルはこれを郵便番号の行にある
  地名と結合して1つのヒットにしており、それが全体として郵便番号
  パターンに引っかかっていました。現在は、はみ出した残りの部分が独立
  したヒットのまま残ります。これは新しい結果の再確認処理
  (`werkzeuge/zweitlesung.py`)によって発見されました。
- **Mac:システム標準の文字認識が対応していない言語(ヒンディー語、
  ギリシャ語、クロアチア語、リトアニア語など)のスキャンが、確認済み
  として扱われていました。** 英語のフォールバックで読み取られ、外国語
  の文字は画像内にそのまま残り、報告書には「何も見つかりませんでした」
  と表示されていました。現在は「画像は【確認されていません】」と理由
  とともに表示され、言語管理は、単にTesseractの言語データが存在する
  というだけの理由で、こうした言語に対する文字認識をもう約束しません。
- **PDF内で、置換された値の後ろの句読点が消えていました。** 「Aufnahme
  am 01.03.2026, Entlassung am 04.03.2026.」から、これまでは
  「Aufnahme am [DATUM_1] Entlassung am [DATUM_2]」になっていました
  ――コンマと文末のピリオドが失われていました。プレースホルダーの場合
  も、日付をずらす方式の場合も同様でした。現在は、検出された値だけが
  削除され、次の空白までの単語全体は削除されません。後ろのコンマ、
  セミコロン、ピリオド、括弧はその位置にそのまま残り、プレースホルダー
  がそれらを飲み込むことはありません。
- **ロシア語とウクライナ語は、語形解析の補助パッケージ(`pymorphy3`)が
  不足している場合、気づかれないまま弱い多言語モデルで処理されて
  いました。** その場合、専用モデルを読み込むことができず、
  「Львів」が人物として扱われていました。氏名の検出には語形解析は
  不要です。現在はこれなしでモデルが読み込まれ、地名は再び地名として
  扱われます。
- **16言語のライセンス情報が古い内容のままでした。** そこには、MPLの
  ソースコードは「要求に応じて」提供されると書かれ、QPDFはMPL-2.0と
  され、表から7つの部品(wordfreq、Qt、ONNX Runtime、tokenizers、
  zxing-cpp、llama.cpp、YuNet)が欠落し、spaCyの段落は英語のままで、
  末尾には英語の代替段落が付いていました。現在は全18言語版がドイツ語
  版と同じ内容になっています。ソースアーカイブはmaskuro.com/quellcode/
  oss/に恒久的に、QPDFはApache-2.0、Qtの経路はLGPL、モデルの出所も
  記載されています。英語版の表にも欠落していた行が追加されました。

- **属格の契約用語(「des Angebotsinhaltes」「des Anbotes」
  「des Terminplanes」)が、もう地名として扱われなくなりました。**
  属格または与格の冠詞に語尾変化を伴った単語1語は総称語です――地名は
  変化しません(「nach Graz」)。文書内の他の場所に冠詞なしで地名が
  ある場合(「Burgenland」)、「des Burgenlandes」も引き続き検出され
  ます。
- **ずらされた日付、マスクされた値、架空の値が、PDFページをラスタライズ
  していました。** 削除後の事後確認は、発見矩形内に角括弧付きの
  プレースホルダーしか許可していませんでした。ずらされた日付
  (「01.07.2026」)やマスクされた値(「****1234」)は見落とされた残存と
  見なされ、ページは念のため画像に変換されていました――「置換」の場合
  は該当しませんでした。現在は、こうしたページはテキストのまま残り、
  パネルや枠からの復元は再び原本を返します。
- **複数語から成る代替値は、PDFではヒットパネルから復元できませんでした。**
  架空の氏名(「Greta Mayrhofer」)やマスクされたIBAN(「**** ****
  **** **** 3201」)は複数の単語から成ります。該当箇所検索は単語ごとに
  比較しており、「プレースホルダーは文書内に見つかりませんでした」と
  報告していました。現在は、同じ行内の連続する単語がまとめて読み取ら
  れます。
- **跡形なく削除された値を復元した後、そのパネル行が残ったままでした。**
  プレースホルダーモードで代替なしに値を削除する「黒塗り」戦略で
  削除された値には、パネルが消失を検出できるプレースホルダーが
  ありません。現在は、値が文書内に再び存在するようになった時点で、
  その行は取り消し線で消されます。

- **「E-Helfer」や「U-Bahn」のような略語を含む複合語が、もう氏名として
  扱われなくなりました。**
- **ハイフネーション(改行時の分割)の残骸(「Leis-」)や非常に長い
  複合語(「Bauarbeitenkoordinationsgesetzes」
  「Baustellenkoordinator」)が、もう氏名や地名として扱われなくなり
  ました。** スキャンされた入札文書では、これにより28語の誤った黒塗り
  が減りました。
- **スキャンされた見積書の明細一覧が、もう氏名の一覧として扱われなく
  なりました。** 一覧向けの追加処理(短い行を対象)は、「Kälterohr」や
  「Außengeräte」を人物にしていました。現在は、「1.1.5」のような明細
  番号が行頭にある場合、この処理は動作を停止します。メールのやり取り
  内の日付行は、これによって明細番号としてカウントされません。
- **スキャンされた見積書の列見出しと明細番号(「Pos.」「Pos. 1.1.3」、
  略号「E/L/S」)が、氏名や地名として扱われていました。** 単独で行に
  ある略号、番号付きの見出し、行ごとの単独文字は、これらに該当しま
  せん。
- **修正ウィンドウで比較ルーペを開いた後、ページが「呼吸する」ように
  動いていました。** 「ページ幅に合わせる」や「全体を表示」では、
  スケールが表示ウィンドウに依存しており、スクロールバーが現れたり
  消えたりするたびにそれが変化していました。その後の操作のたびに、
  ページが少しずつずれていっていました。キャンバスは現在、これを
  安定するまで自動的に追従修正します。ズームボタン、スライダー、
  ショートカットキーも、拡大時にスクロールバーが現れた場合でも
  画像の中心を維持します。
- **横向きに保存されたスキャンが、正しい向きで読み取られるようになり、
  大きなスキャン内の小さな文字が失われなくなりました。** 24ページの
  スキャンされた見積書では、各フッターに6つの銀行IBAN、商業登記番号、
  UIDが読める状態で残っていました。スキャンはPDF内で90度回転して
  おり、文字認識は非常に大きな画像で、画像サイズによっては行全体を
  丸ごと読み飛ばしていました。現在は表示上の回転が考慮され、大きな
  画像は重なり合う帯に分けて読み取られます――フッターは現在黒塗りされ
  ます。
- **基本語の前にハイフンのある人名由来の通り名(「Josef
  Admanseder-Straße 7」「Abt-Karl-Straße 8」「Dr.-Karl-Renner-Straße
  12」)が、住所として認識されるようになりました。** スキャンされた
  見積書のレターヘッドでは、パターンが「Straße」の前にスペースを要求
  していたため、このような住所が読める状態で残っていました。
- **文字認識でOを0、lを1と誤読したIBANが、現在は認識されます。**
  スキャンの小さな文字では、文字認識は数字を文字として読みがちです。
  その番号はIBANの形をしていましたが、チェックサムが一致せず、その
  まま残っていました。チェックサムが失敗した場合、現在は数字としての
  読み方も試されます――それで一致すればIBANとみなされます。誤った
  チェックディジットは引き続き誤りのままです。
- **「folgenden Codes auf der」のような文の断片が、地名として扱われて
  いました。** 小文字で始まる単語で始まる氏名や地名は存在しません――
  貴族の姓の一部(「van Gogh」「de Vries」)を除きます。
- **エディタでは、黒塗りバーの隣に最後の1文字が残っていました**
  (「…6」「…t」「…g」)。バーの高さは、行ではなくドラッグした枠の
  高さになっていました。原因:エディタがページを測定できない場合、
  すべての枠を「単語に当たっていない」とみなし、そのまま適用して
  いました――「単語の半分が残ることは決してない」というルールが
  適用されていなかったのです。エディタが位置を特定できない個別の
  テキスト描画命令でも同じことが起きていました。現在は、常に単語の
  矩形が基準として使われます。枠が実質的に重なっている対象は、丸ごと
  対象になります。
- **単語の最後の文字が、黒塗りバーからはみ出していました。** バーは
  フォントメトリクスの送り幅に基づいて計算されていました。フォントが
  字形をそれより幅広く描く場合、その残りがバーの隣に表示されていま
  した。文字の矩形は、現在は描画された字形自体も含むようになりました。
- **ページを画像に変換する際の通知が、実態以上のことを約束していま
  した。** 「表示は変わりません」は、ラスタライズ後には正しくありません
  ――文字も図形もその時点で画素になり、ファイルは大きくなります。通知は
  現在それを伝えます――さらに、ラスタライズされるもう一つの理由(その
  改変がページを損傷させる可能性があったこと)も伝えます。
- **削除された値の後ろのテキストが、最大1ポイント左にずれていました。**
  行の再構成の際、先頭は字形の端で測定され、続きはペン原点で測定されて
  いました――最初の文字の送り幅の差が誤差として残っていました
  (「C」で0.5pt、「I」で1.0pt)。現在は再構成全体でペン原点を基準に
  計算されます。後続テキストは、10分の1ポイント単位で正確な位置に
  表示されます。
- **スペース付きのオーストリアUID(「ATU 187 35901」)、および
  「FN」のない商業登記番号(その見出しの下、「Firmenbuchnummer:
  30799v」)が認識されます。** どちらも、スキャンされた入札書式に
  手書きで記入されており、文字認識自体は正しく読み取っていたにも
  かかわらず、読める状態で残っていました。
- **横向きのPDFページが、黒塗り後に理由もなく画像に変換されていました。**
  完全性検査は、回転した表示で原本と結果を比較していましたが、その
  黒塗り領域自体は回転させずに計算していました――回転情報を持つ
  ページでは、そのため自身の黒塗りがその領域の隣にずれてしまい、
  破損として扱われていました。このようなページは現在、テキスト
  レイヤーとベクターグラフィックを維持します。
- **正立したページでも、時折不必要に画像へ変換されていました。**
  プレースホルダーの後ろのテキストが1ポイントずれる場合(これは許容
  範囲内です)、画像比較がその許容範囲より厳密になっていたためです。
  現在は0.5ポイント単位で比較され、許容範囲が正確に適用されます。
  2ポイントまでのずれでは反応せず、それを超えるとすべて反応します。
- **埋め込まれたフォームオブジェクト内の項目が、そのまま残っていま
  した。** 一部のテンプレートは、レターヘッドやレターフッターをページ
  に組み込むだけの独自フォームとして格納します。その中のヒットは
  計画され削除済みとしてカウントされていましたが、実際には決して
  書き込まれていませんでした――テキストはそのまま残っており、ページ
  全体をラスタライズした場合にのみ捕捉されていました。現在はフォーム
  自体が書き換えられます。複数ページにまたがるフォームは一度だけ
  処理されます。
- **何も読める状態で残っていないにもかかわらず、PDFページが画像に
  ラスタライズされていました。** 7ページの見積書のうち6ページがこれに
  該当し、73KBから3.3MBに膨れ上がり、文字が画像に置き換わって
  いました。原因は、文書内で連続して並ぶ空白文字が、読み取りライブ
  ラリでは1つとしてしか報告されないことでした。削除された項目の
  後ろのテキストが、その幅の分だけ右にずれ、事後確認が発見矩形内の
  隣接単語を見つけ、ラスタライズに頼っていました。保持された行の残り
  部分は、現在は正確にその位置に表示されます。同じ見積書は、ラスタ
  ライズされたページを1つも生成せずに浄化されます(76KB)。
- **鍵の名前や請求書の見出しが、人物として扱われていました。** ある
  アクセス用ファイルでは、環境変数名(「AWS_ACCESS_KEY_ID」)がその値
  だけでなく置換されていました。英語の請求書では、見出し「Bill to」が
  名として扱われていました。大文字とアンダースコアから成る識別子は
  決して氏名ではなく、行全体がフィールド見出しである行の中の単語も
  同様です――その下にある受取人は引き続き検出されます。
- **修正ウィンドウの検索が、大きなPDFページで詰まっていました。**
  強調表示が変わるだけであるにもかかわらず、検索欄への入力1文字ごと
  にページが再ラスタライズされていました。現在は、ページ・ズーム・
  表示が同じである限り、レンダリングされたページ画像はそのまま保持
  されます――比較ルーペ内の原本表示も同様です。ページ送り、ズーム、
  ファイル状態の変更は引き続き新しく描画されます。
- **見積書の明細番号が、IPアドレスや電話番号として扱われていました。**
  「1.3.3.4 … 5-Port Gigabit Switch」のような品目行では、構造番号が
  ネットワークアドレスになっていました。「Port」が技術的な文脈として
  カウントされていたためです――現在は「Port 80」のような独立した項目
  としてのみカウントされ、単語の一部としてはカウントされません。また
  「1.3.3.6 216879」(明細番号と品目番号の組み合わせ)は、もう電話
  番号として黒塗りされません。こうした一覧内の本物のIPアドレスや電話
  番号は引き続き検出されます。
- **見積書の品目行が、郵便番号と地名として扱われていました。**
  「35252 DIETZEL SALR」(品番とメーカー)や「1000 AWG」(数量と
  導体断面)は、番号付き明細行の中で住所として黒塗りされていました
  ――数字の後ろにある大文字の単語が、大文字表記の地名として扱われて
  いたためです。明細一覧では、現在はこれに該当しません。住所ブロック
  内の「1080 WIEN」や小文字表記の地名は、引き続きどこでも検出され
  ます。
- **追加の氏名検出が、見積書内の役割行や列見出しを黒塗りしていました。**
  「Partiestundensatz Monteur + E-Helfer」は49回人物として扱われ、
  列見出し「Pos. Bezeichnung Menge EH」は19回地名として扱われ――
  19ページの発注書がこれによって読めなくなっていました。明細行内の
  こうしたヒットは現在、氏名には含まれない文字(プラス、スラッシュ、
  数字、略号)自体を含む場合、対象外になります――行が金額で終わる場合
  (「Alternativ Markt … - PV/LS AC-Versorgung 1 290,00」)も同様
  です。一覧や名簿内の氏名――この段階の本来の目的――は影響を受け
  ません。
- **「Der Kunde(顧客)」が、業務条件内のすべての「Kunde」を氏名に
  してしまっていました。** 追加の氏名検出が冠詞をヒットに含めると、
  それが2語から成る氏名として扱われ、同じ単語の残り35箇所すべてを
  保護していました。現在は冠詞が除外され、「der Kunde」は、すでに
  そうなっている「des Kunden」と同様に対象になります。
- **見出し語が値として扱われていました。** 「E-Mail」は7回メール
  アドレスとして、「Telefonnummer」と「Faxnummer」は電話番号として
  黒塗りされていました。@のないアドレスや数字のない電話番号は、
  もう対象になりません。
- **1文字の列略号(「L: 154,50」「S: 0,00」)が氏名として扱われて
  いました**――ある太陽光発電の見積書で25回です。単独の1文字は氏名
  でも地名でもありません。
- **PDFページが、あまりにも頻繁に画像に変換されていました。** 実在
  する見積書で見つかった2つの原因があります。PDFが各字形を独自の
  命令として配置し、その下にテキスト文字を持たない空白の字形が
  ある場合、そこから対応関係が1つずれていました――削除された値の
  最後の1文字が残っていました(「ŠkodaTopCar**d**」)。事後確認は
  正当にそのページをラスタライズしていました。そして、行末で分割
  された単語(「Datenschutz-」)は、読み取りライブラリのハイフン
  マークのせいで、ずれたものとして扱われていました。どちらも修正
  されました。ある車両の見積書はラスタライズされたページが4から0に、
  19ページの発注書は7から0になりました――文字は文字のまま、ファイルは
  小さいままです。
- **さらに2つのラスタライズの原因を修正:** 文書自体が「F1」という
  名前のフォントを持ち込んでいる場合、画像上のプレースホルダーは
  その文書のフォントで表示され、読めない状態になっていました――現在
  は独自の見出し用フォントに空いている名前が割り当てられます。また、
  長いテキスト描画命令の途中に読み取りライブラリが空白を見つけられ
  ない場合、現在はマルチバイト文字でもその位置が(同じコード、同じ
  文字により)証明され、末尾に押しやられることがなくなりました――
  以前は、それによって削除された値の1文字が残り、後続のテキストが
  目に見えて横にずれていました。さらに最後の2つのケースです。数十の
  空白字形から成る命令が対応関係をずらしてしまうケース(その後の氏名
  が残っていました)と、大きな見出しで送り幅を伴うものがその最初の
  文字を見つけられないケース(会社名が残っていました)です。**実在
  する9件の見積書のうち、ラスタライズされるページは現在1件もあり
  ません**――以前は90ページ中30ページでした。
- **ラスタライズの際、画像が黒い塊の下に消えていました。** ページを
  画像に変換する必要がある場合、それは原本からレンダリングされます
  ――そこには画像浄化処理は関与しません。これまでは、そのためページ
  内の*すべて*の画像領域がバーの下に隠されていました。手を加えて
  いないものも含めてです。ある見積書では、住所と2つの認証ロゴが同じ
  レターヘッド画像内に含まれており、バーがロゴも巻き込んでいました。
  現在はすでに浄化済みの画像が使用されます。その中の住所は黒塗りされ、
  それ以外はそのまま見えます。削除された画像は、黒い四角ではなく白紙
  を残します。

- **浄化されたスキャンが、原本の何倍もの大きさになっていました。** 何か
  が黒塗りされたすべての画像が、無圧縮の生画像としてファイルに戻され
  ていました――24ページのスキャンでは、これによりサイズが11.8MBから
  52.9MBに増加していました。画像は現在、元々の種類を保持します。
  写真は写真のまま、ファックススキャンは白黒のまま、色のない画像が
  カラー画像として保存されることもありません。同じファイルは現在
  15.6MBで、見た目上の違いはありません。
- **オフィス機器から取り込まれたスキャンPDFファイルが、縞模様に
  なって戻ってきました。** このようなスキャンは、文字を鮮明な白黒
  レイヤーとして粗いカラー画像の上に重ねます――Canon、Xerox、Kofax
  はこの方式でファイルを構築します。画像内で黒塗りを行う際、この
  レイヤーが誤って書き戻され、結果は読めない状態になっていました。
  6ページの見積書では16画像のうち9画像がこれに該当していました。
  現在は正しく、それ自身の色空間のまま処理され、黒塗りされた箇所は
  実際に消えます。
- **「すべての画像を削除」が、スキャンされたページからテキストを
  奪っていました。** このようなスキャンの文字レイヤーは、技術的には
  画像です――それも一緒に削除・ぼかしの対象になり、白紙だけが残って
  いました。現在はそのまま残ります。ロゴ、スタンプ、署名は引き続き
  除去対象です。
- **破損したPDFページの検査が、わずかなずれを理由にもうラスタライズ
  しなくなりました。** 浄化時に新たに配置し直されたテキスト片は最大
  2ポイントずれることが許容されますが、画像比較はそれでも破損として
  カウントし、ページを画像として再構築していました――その際、表の
  罫線のようなベクターグラフィックが失われ、該当箇所にはプレース
  ホルダーの代わりにバーが表示されていました。現在は、比較処理は
  単語検証と同じ小さなずれを許容します。本物の破損は引き続き検出
  されます。
- **多数の値を連続して復元する処理が、Windowsで「アクセスが拒否され
  ました」で失敗しなくなりました。** オフィスファイル内で多数のパネル
  行を短時間に連続して復元すると、ウイルス対策ソフトによる一時的な
  ファイルロックで失敗することがありました。現在は、交換処理がこの
  ようなロックを少し待つようになりました。
- **Windowsでのコマンド伝達経路が、検査の代わりに検査プロセスを終了
  させていました。** 常駐インスタンスの生存確認が、Windowsでは誤って
  自身のコンソールグループに本物のCtrl+Cを送信していました――現在は
  信号を送らずにシステムに問い合わせます。
- **複数語のフィールド見出しが機能せず、代わりにその断片が機能して
  いました。** 「Date of birth」「Bank account」「Cuenta bancaria」
  「Numero de cliente」は見出し一覧にありましたが、そこで個別の単語
  に分解されていたため、決して一致しませんでした。残った「de」や
  「of」のような単語の断片が、それ以降見出しとして扱われていました
  ――しかし「de」は氏名の一部です(「Anna de Vries」)。両方とも修正
  されました。この語句は現在まとまりとして機能し、断片は取り除かれ
  ました。
- **「ß」を含むドイツ語の挨拶文が、一覧に登録されているにもかかわらず
  人名として扱われていました。** 「Herzliche Grüße」や「Mit
  freundlichen Grüßen」の下には、結果にプレースホルダーが表示されて
  いました。両方の表現は以前から除外リストに登録されていたにもかかわ
  らずです。原因は、比較の際に一致しない表記にありました。5つの
  一覧にまたがる8件の項目が影響を受けていました。現在はすべて機能し
  ます。
- **「John Staff」が置換されないままでした。** 姓であると同時に英語の
  列見出しでもある単語が、見出しフィルターによって一緒に破棄されて
  いました。見出し自体は引き続き変更されず、その下にある氏名は再び
  置換されます。
- **見出し付きフォームフィールドの値が、AI段階でも保護されるように
  なりました。** AI段階のローカル審判は、これまでフィールド見出し
  (値の上にある「Geburtsdatum:」など)によってその意味がすでに証明
  されているヒットも判定対象として受け取っており、それを破棄すること
  ができました。このように構造的に裏付けられた値は、現在は判定対象と
  して提示されません。対応関係ファイルには現在、各置換ごとに検出経路
  (「証拠」)も追加で記載されます。
- **浄化によって保持されたテキストが損傷を受けたPDFページは、現在
  検出され、原本の画像として再構築されます。** 一部の生成元フォント
  では、保持されたテキスト箇所が、削除すべき項目がすべて正しく削除
  されているにもかかわらず、浄化後に黒い塊として表示されたり、単語が
  くっついてしまったりすることがありました。Maskuroは現在、結果を
  原本と単語単位・画素単位で比較します。破損したページは、その
  クリーンな画像に置き換えられます――該当箇所には黒塗りバー、画像
  領域には黒塗り、そして検索可能なテキストを伴います。ページは読める
  状態のまま、削除は確実に行われます。

### 変更

- **翻訳された画面表示では、現在どの専門用語もどこでも同じ表記に
  なりました。** 同じドイツ語の単語に対して、ウィンドウによって2つ
  または3つの訳語が並存していました。検査記録はノルウェー語で
  「Revisjonslogg」だったり「Kontrollogg」だったりし、フリープランは
  「Gratisnivå」だったり「Gratisversjon」だったりしました――他の
  十数言語でも同様でした。設定を探す人は、次のウィンドウでは別の
  名前で見つけることになっていました。表記は、画面表示上でもともと
  最も多く使われている単語に統一されました。

  この作業を通じて、1つの単語が2つの**異なる**ものを表している箇所も
  明らかになりました。フランス語、ギリシャ語、韓国語では「黒塗り」と
  「マスク」に同じ表現が使われていました――よりによって、プログラム
  がその違いを説明する箇所で(「黒塗りは跡形なく削除し、マスクは
  形を保持します」)です。この2つは現在区別されています。スウェーデン
  語についてはこの決定はまだ保留です。そこでは黒塗りが「maskera」――
  マスクと同じ単語です。

- **初回起動時の利用形態に関する質問がなくなりました。** 起動直後に
  「個人利用ですか、業務利用ですか?」というウィンドウが表示され、
  設定にもそれに関する行がありました。両方とも、代替なしで廃止され
  ました。何にも紐づかない情報は、誤ったライセンスを望む人が誤った
  回答をし、正直な人にはそもそも不要なものです。誰にとっても、誰も
  ライセンス種別について考えていないタイミングでのクリック1回分の
  コストでした。どのライセンスが正しいかは、それが決定される場所――
  価格ページ、購入画面、ヘルプ――に記載されています。Maskuroを一括
  展開する組織は、引き続き設定ファイルを通じて利用形態を指定できます。

- **ライセンス種別に関する説明が、対象となるケースを明示するように
  なりました。** 個人向けライセンスは私的利用のみに適用され、業務上
  または営利目的の作業にはすべて――従業員のいない個人事業主でも――
  法人向けライセンスが必要です。これはライセンス条項にはそう記載
  されていましたが、プログラム内にもヘルプにも記載がありませんでした
  ――そこには常に会社ドメインの話しかなく、まさにこのケースは対象外
  でした。個人事業主のパソコンはどのドメインにも属していません。個人
  向けライセンスを読み込む際の案内には、現在この点が明記されており、
  マニュアルのライセンスの章とよくある質問にも、専用の項目が追加され
  ました。制限は引き続き一切かかりません。

- **まだ提供されていない機能が、一箇所にまとめられました。** 設定には
  「開発者」ページが追加され、そこに最大検出(AI)とその照合機能、
  語句リストのカタログ、フォルダ監視があります。この3つはすべて構築
  済みですが、実地では検証されていません――そのため、開発者ライセンス
  がある場合にのみ表示され、しかもすべて同時に切り替わります。ページ、
  メニュー項目、実行時の効果はすべて同じ判定に基づきます。このライ
  センスがない場合、以前有効化されていたAI段階は無効のままです。その
  設定は削除されず、この機能が提供された時点で再び有効になります。

### 改善

- **「何が検出されるか」に、氏名検出由来の3つの追加リストが表示され
  ます。** 後に続く単語が氏名として読まれる敬称、その後に続く語が
  **まだ**氏名ではないと判断される肩書きや役職(「Herr Bürgermeister
  Huber」)、そして事件番号・案件番号・受付番号を検出するための80の
  多言語の見出しです。この3つは以前から機能していましたが、一覧には
  表示されていませんでした。

- **「何が検出されるか」に、これまで欠けていた2つの語句リストが表示
  されます。** 前にある単語を氏名にする敬称と肩書き(「Herr」
  「Frau」「Dr.」)、そしてMaskuroが「ÖNORM B 2110」のような規格参照
  を人物と区別するための規格団体の略号です。どちらも以前から検出に
  影響していましたが、一覧には表示されていませんでした。

- **明細一覧、目次、装備の羅列、規格参照が読める状態のまま残ります。**
  検出処理は現在、行の構造を確認します。構造見出しの行(「1.3.1
  Energieerdkabel 1kV」)、リーダー点を含む目次の行、箇条書き
  (「- kabelloses Laden mit Magnetring」)、数量・価格行の上、列見出し、
  「mittels」の後ろにある推測された氏名は専門用語であり、もう置換
  されません。本物の氏名は――敬称、フィールド見出し、文書内の別の
  場所での裏付けによって――引き続き保護されます。測定コーパスでは
  保護を失った項目は1件もありませんでした。業務コーパスでは、これに
  より誤検出が25件から6件に減少しました。

- **見出し、フォームの見出し、挨拶文が、ドイツ語と英語でより頻繁に
  氏名と区別されるようになりました。** Maskuroが専門用語と人名を
  区別するために使う語句リストが大幅に拡充されました。請求書、
  フォーム、行政文書からの見出し(「Aktenzeichen」「Verwendungszweck」
  「Kostenstelle」「Sort code」「Subtotal」)、応募書類や報告書の
  見出し(「WERDEGANG」「QUALIFIKATIONEN」「SUMMARY」「REFERENCES」)、
  ドイツ語と英語の書類の種類(「Auftragsbestätigung」
  「Niederschrift」「Timesheet」「Agreement」)、手順書の命令文
  (「Sende…」「Select…」)です。英語版はこれまで、際立って登録数が
  少なくなっていました。

- **見出し付きフィールドは、その見出しが複合語である場合でも、現在は
  中身を正しく伝えます。** 「Lieferanschrift」「Rechnungsadresse」
  「Sachbearbeiterin」「Kontoinhaber」「Contact person」「Billing
  address」は、その隣または下にある値を、単純な「Anschrift」や
  「Name」と同じ種類に分類するようになりました――記入済みのチェック
  ボックス付きフォームでは、これが発見と見落としの差になります。

- **修正ウィンドウでは、マウスホイールがページの端でも続けて動作
  します。** ページの終わりでさらにスクロールすると、次のページの上部
  に移動します。先頭からさらに戻ると、前のページの下部に移動します
  ――これにより、ページボタンに触れることなく、文書を最初から最後まで
  スクロールできます。キーボード(Page↑/Page↓)は以前からこれができ
  ました。2回のページ切り替えの間に短い間隔を置くことで、トラック
  パッドの慣性スクロールが文書の半分まで一気に進んでしまうことを
  防ぎます。

- **修正ウィンドウのページの縮小表示が、パネル内で中央に配置される
  ようになりました。** これまでは左端に張り付いており、幅を広げると
  右側の空白だけが増えていました。

- **修正ウィンドウのツールバーが、そのグループを示すようになりました。**
  区切り線には現在余白と色が付き、「検索」と「全ページに反映」は
  ツールとは別の独立したグループとして表示されます。「反映」は、
  それが何かの効果を持ち得る文書種類でのみ表示されます。ツールバーと
  メニューの各項目には現在アイコンが付いています。「テキスト行」と
  比較ルーペには独自のアイコンが割り当てられました(ルーペはこれまで
  「前後比較」とアイコンを共有していました)。ズーム、全体表示、
  ページ幅、回転、ページ送り、ショートカットキーにも同様です。
  「システムのプログラムで開く」も現在、印刷の隣にツールバー内に
  表示されます――完成した結果から使い慣れたプログラムへの経路が、
  メニューをたどることなくクリック一つになりました。

- **クリップボードの浄化では、確認が必要である旨が再び表示されるよう
  になりました。** 設定では、この案内がスイッチの隣に恒久的に表示され
  ます。Maskuroは個人データを見落としたり、項目を誤って処理したり
  する可能性があり、貼り付けたテキストは渡す前に見直す必要があります。
  有効化の際には、これに加えて通知にもその旨が表示され、通知領域に
  アイコンが表示されていない場合でも、出力領域に記録されます。個々の
  コピー操作ごとには意図的に表示されません。1日に50回表示される案内は、
  3回目以降は誰も読まなくなるためです。

## 0.10.36-beta.1 – 2026年8月20日

### 改善

- **技術的な業務文書が、過度に黒塗りされなくなりました。** 実在する11件の
  見積書・発注書から得られた4つの検出ブレーキです。番号構造(「1.3.1.1」)
  はもはやIPアドレスとして扱われません。規格参照(「ÖNORM EN 62446」)や
  識別コードはもはや郵便番号や電話番号として扱われません。冠詞の後ろに
  続く役割語(「der Kunde(顧客)」「des Auftraggebers(発注者の)」)は
  もはや氏名として扱われません――ある実在の見積書の取引条件では、これに
  より46件すべての役割語が、黒塗りされることなく再び読めるようになり
  ました。国コード付きの住所(「A 3390 Melk」「D-94032 Passau」)は現在、
  郵便番号だけが孤立して残ることなく、完全に削除されます。

- **語句リストが、完全に閲覧できるようになりました。** 「ヘルプ→語句
  リスト…」から、ローカルで使用されている検出用および照合用のリストを、
  言語・目的・出典・内容とともに検索できます。これにはWordfreq、医療、
  個人管理、中央管理のリスト、および架空の代替値の在庫も含まれます。
  マニュアルはこのカタログを独立した節で説明しています。

- **完了したファイル行に、使用された検出言語が表示されるようになり
  ました。** 「完了」の後ろに「ドイツ語」や「英語」などと表示され、
  適切でない自動言語選択がすぐに分かるようになります。別のインストール
  済み言語が代わりに使われた場合は、矢印が両方の言語を示します。

- **新しい比較ルーペが、読み進める際に原本の該当箇所を即座に表示します。**
  拡大された原本の抜粋部分は、引き続き編集可能な結果の上でマウス
  カーソルに追従します。テキストの場合は段落単位で追従します。ルーペは
  ウィンドウの端で使うことも、最大化可能な独立したウィンドウとして
  引き出すこともできます。ズームは50~300パーセントの間で直接調整でき、
  オンにしたことと同様に記憶されます。「リセット」は、最大化された、
  あるいは扱いにくい位置にドッキングされたルーペも、左側の操作しやすい
  大きさに戻します。置換された原本の値はルーペ内で黄色く強調表示され、
  読んでいる際に該当する単語がすぐに目に入るようになっています。一度
  有効化すると、以後、対象となる文書では再びプログラムを起動し直しても
  開くようになります。これまでの前後比較切り替えは、表示メニューに
  引き続き残ります。マニュアルはこれを独立した節で説明しています。

- **オープンソースおよびモデルの証跡が、リリースごとに正確になりました。**
  パッケージのビルドは、同梱されたライセンス文書のハッシュを含む機械
  可読なコンポーネント一覧を生成します。MPLのソース、モデルの出所、
  固定されたリビジョン、変更内容、SHA-256は個別に証明されます。
  再取得されたモデルは、モデルフォルダに直接、出所の証跡を受け取ります。
  変動していたTesseractおよびspaCyの取得先一覧は固定されました。販売用
  成果物は、すべてのソースとモデルの同梱物が公開・検査されるまで
  ロックされたままです。

- **ローカルのwordfreqデータ資産が、完全にライセンス文書化されました。**
  パッケージのビルドは、バージョン3.1.1、CJKを含む39件の変更されていない
  小規模リスト、および中国語文字表を、件数・サイズ・マニフェストの
  チェックサムに対して検証します。Apache-2.0のコード表記、完全な
  CC-BY-SA-4.0ライセンス、帰属表示、データ出典、そして除外された大規模
  リスト、Jiebaリスト、非対応リストは、パッケージ内に文書化されています。

- **よく使われる文中の語が、誤って黒塗りされることが少なくなりました。**
  氏名検出が動詞・代名詞・冠詞・前置詞を人名だと判断した場合、ローカルの
  頻度辞書が追加の照合手段として機能します。この辞書が単独で判断を
  下すことはありません。一般名詞、複数部分から成る氏名、およびフィール
  ド・リスト・敬称の後ろにある氏名は引き続き保護されます。中国語、
  日本語、韓国語では、既存の言語モデルの正確なトークン境界のみが使用
  されます。対応する言語モデルがない言語に対して、似ていると思われる
  辞書言語が代用されることはありません。このためにインターネットへ
  文書のテキストが送信されることはありません。

- **技術的な製品・装備に関する用語が、氏名や地名と誤認されにくくなり
  ました。** ローカルの照合処理は現在、頻度、品詞、技術的な語形成、
  分野語彙を組み合わせて判断します。これにより、たとえば
  「Travel-Assistent」「Family-Bonus」「WLTP-Wert」「Easy-Start」、
  および型式・保持具・ブレーキに関する複合語は文書内に残ります。英語の
  構成要素は、ドイツ語の専門的な文章内でもローカルで照合されます。実在
  する固有名詞、敬称、および人物・地名フィールドは引き続き優先されます。
  さらに、「2年間のメーカー保証」はもはや年齢として扱われません。

- **Qt/PySideのライセンス上の権利が、完全に追跡可能になりました。**
  プログラムパッケージには、GPL-3.0の全文、正確なQtのフッション、
  ソースコード提供の申し出、動的ライブラリの交換手順(ローカルでの
  macOS再署名を含む)についてのドイツ語/英語の手引きが追加で含まれます。
  提供済みフッションの正確なソースアーカイブが自社のソースコードページで
  入手可能になるまで、販売用ビルドはブロックされます。

- **ライセンスと更新の状況が、各段階について何が適用されるかを明確に
  示すようになりました。** ライセンスウィンドウと更新設定には、更新が
  含まれているか、それがいつまで続くか、実行中のフッションが恒久的に
  使用可能なままかどうかが表示されます。個人向けライセンスは、基準日
  以降に公開されたフッションをインストールしなくなります。新たに
  ダウンロードされたインストーラーも、自身に組み込まれた公開日を基に、
  入力されたキーがそれをカバーしているかどうかを判断します。カバー
  範囲内の最後の個人向けフッションは、恒久的に使用可能なままです。一方、
  法人向けサブスクリプションが終了すると、利用と更新も終了します。試用
  期間やフリープランが抜け道として開くことはありません。

- **個人向け永久ライセンスは現在、再インストール後も正しいプログラム
  段階を見つけます。** 署名付きのフッションカタログが、すべての安定版
  フッションとそのパッケージを管理します。購入時にカバーされていた
  最後のインストーラーがもはや入手できない場合、代わりに、入手可能な
  中で次に高い安定版フッションが自動的に使用されることが許可されます
  ――ベータ版やナイトリー版が使われることは決してありません。新しすぎる
  インストールの場合、お客様は許可された段階をインストールするか、新しい
  更新期間の購入ページへ移動するかを選べます。ダウングレードが黙って
  行われることはありません。これは管理されたMSIインストールにも当ては
  まります。

- **自動顔黒塗り機能が、現在明確に説明されるようになりました。** プログラム
  ヘルプとプライバシー文書は、この機能を「顔領域の検出と判別不能化」と
  呼び、識別、再識別、顔照合、生体認証テンプレート、人物・顔データベース
  とは区別しています。また、完全にローカルで行われる検出処理は、領域を
  見落としたり誤って検出したりする可能性があるため、結果を目視で確認する
  必要がある旨も明確に示しています。個別に浄化された画像ファイルの場合も、
  結果報告書には現在、検出およびモザイク処理された顔領域が表示されます。
  文字認識が行われなかった場合に、それが誤って「完全に変更されていない
  ファイル」として記載されることはもうありません。

## 0.10.36-alpha.20260820 – 2026年8月20日

### 修正

- **匿名化された項目が、順序に関係なく完全に復元できるようになりました。**
  従来の復元は、値を目に見えるテキストの目印(アンカー)を頼りに探していました。
  密集した表、隣接するプレースホルダー、目に見えないOffice/メールの格納領域では
  この目印が欠けており、ある用語が復元可能になるのは、別の平文が偶然新しい目印を
  作った後だけ、ということもありました。現在は結果と原本が実際のフォーマット
  担体ごとに完全な対応関係で比較され、選んだ値の該当箇所だけが書き込まれます。

- **氏名、メールアドレス、番号、独自の検査語句は、認識が重なった場合でも
  一意に操作できます。** 同じ平文値が2種類に割り当てられている場合、実際に
  該当箇所にあるプレースホルダーと、クリックされたサイドバー行の組み合わせで
  決まります。対応する値/プレースホルダーの組が存在しない場合は、引き続き
  安全にロックされたままです。

- **メールの特殊なケースで、隠れたプレースホルダーが残らなくなりました。**
  これはMIMEエンコードされた件名、テキスト添付ファイル、EMLおよびMSGにおいて
  HTMLマークアップで分断された氏名にも当てはまります。独自の文字コード指定を
  持たないUTF-8のHTMLも、Outlookファイルで編集の都度文字化け(Mojibake)へと
  再エンコードされることがなくなりました。すでにこの形式で書き出された古い
  結果も引き続き復元可能です。

### 改善

- **新しい提供マトリクスが、匿名化された各サイドバー行を個別に、かつ意図的に
  逆順で処理します。** 14種類のテキスト・Office・Web・メール形式に加えて
  PDF、さらに数式、属性、関係情報、コメント、メールヘッダー、添付ファイル、
  内部の副次格納領域まで検査します。macOSでの完全な実行は、現在149/149の
  検査スクリプトが成功しています。

## 0.10.35-alpha.20260820 – 2026年8月20日

### 改善

- **言語別の測定が、本当に同じもの同士を比較するようになりました。** 定期
  測定コーパスには、ドイツ語と英語で同じ7つのテキスト課題と4つの画像課題を
  持つ、同じ14件の文書ケースが含まれています。フル実行では、現存する12の
  コーパス言語すべてに対してこのマトリクスがそのまま繰り返されます。
  フォーム、表、チャットなど、まだ完全には翻訳されていない構造サンプルは
  維持されますが、別扱いとして示され、言語比率にはもう混ぜられません。

- **フル実行では、言語ごとに個別の測定報告書が作成されます。** 言語スイッチ
  なしでは、意図的にドイツ語と英語が検査されます。`--alle-sprachen` は
  完全な12言語コーパスを要求し、言語やケースが1つでも欠けていれば最初の
  文書の前に処理を中止します。同名の結果は言語別のフォルダに分けて置かれ
  ます。総合報告書には、重み付けされた検出率に加えて、言語別比率の
  重み付けなし平均値も示されます。

- **公開されている言語比較が、実際の限界も示すようになりました。** 文字
  認識付きの定期実行では、ドイツ語と英語が誤検出なしで既知の項目218/218を
  除去します。文字認識と上位段階を用いたフルテストでは、17件の誤検出とともに
  1,308件中1,255件の項目が除去されます。11言語が100パーセントに達し、
  ヒンディー語は51パーセントです。以前のフル比率は、不均一な文書数と目標
  項目数に基づいており、新しいマトリクスとは比較できません。

## 0.10.34-alpha.20260819 – 2026年8月19日

### 修正

- **同じ氏名が複数回出現する場合、1件だけ復元してもサイドバーで引き続き
  操作できるようになりました。** これまでは、最初に復元された`[名前]`
  箇所の時点で、氏名の行全体が消えてしまっていました。同じ氏名の他の箇所は
  そのままプレースホルダーとして残り、他の氏名が復元されるまで一時的に
  ブロックされることさえありました。現在は、最後の箇所が復元されて初めて
  行が消えます。すでに復元された平文が、再び自動的に匿名化されることは
  ありません。これは、一部だけ成功した一括復元や、PDFの枠選択ツールにも
  同様に当てはまります。

- **「置換を取り消す」がOfficeのページプレビューからでも機能するように
  なりました。** そこに表示されているページは一時的なPDFプレビューに
  すぎません。現在は正しく背後にあるWord・表計算・プレゼンテーション
  文書が変更され、その後プレビューが更新されます。

- **復元機能は現在、値の隠れた対応箇所も完全に取り戻します。** Word、
  OpenDocument、Excel、PowerPointの各ファイルでは、同じ項目が数式、
  コメント、図表、フィールド値、代替テキスト、リンク先にも追加で
  含まれていることがあります。HTML、EML、MSGでは、さらに属性、JSON、
  メッセージヘッダー、添付ファイルにも含まれています。これまでは形式に
  よって一部がプレースホルダーのまま残っていました。現在は、該当範囲で
  提示されたどの項目も、独立して、任意の順序で復元できます。意図的に
  削除されたメタデータ、変更履歴、転送ヘッダーは、安全上の理由から
  引き続き削除されたままです。

- **画像から復元する際に、黒い縁取り線が残らなくなりました。** 枠の右端と
  下端が、原本からのコピー時にそれぞれ1ピクセルだけ狭く配置されていました。
  座標は現在、黒塗りと一致しています。

### 改善

- **提供検査は現在、対応する22の拡張子すべてに対して完全な往復処理を
  行います。** 内容の豊富なファイルが浄化され、提示されたすべての値が
  復元されたうえで、詳細に検査されます。これに加えて、実際のサイドバー
  操作、ピクセル単位の画像比較、7つのオフィス形式すべての目視可能な
  LibreOfficeレンダリングが行われます。小規模な回帰テストは、独自の
  不具合ケースやセキュリティケースをカバーしている箇所については引き続き
  残されます。重複が実証されたHTML検査と、削除されたモノクロモードの
  テストは廃止されました。

- **このフッションの完全な測定コーパスが、再測定用に用意されています。**
  パッケージには12形式・12言語にわたる294件の合成文書、2,564件の既知
  項目、4つの機械可読な目標一覧、および手引きが含まれます。品質ページ
  でのダウンロードは内容依存のファイル名を使用しており、ブラウザが
  誤って古いフッションをキャッシュから配信することを防ぎます。

## 0.10.33-alpha.20260819 – 2026年8月19日

### 新機能

- **画像ファイルでも、個々の箇所を原本から復元できるようになりました。**
  枠選択ツールの「原本を復元」は、手を加えていない元ファイルの同じ位置
  から画素をコピーして戻します。原本が存在しない場合や画像サイズが
  異なる場合、この操作は引き続き無効のままです。これにより、位置が
  ずれた箇所から内容が挿入されることはありません。

### 改善

- **手動の黒塗りバーが、標準でテキスト行に自動整列するようになりました。**
  複数行にわたるドラッグ操作は、行ごとに均一な高さのバーを作成し、
  行間の余白はそのまま残します。署名、図版などの特殊なケースでは、
  「自由枠」に切り替えることで自分で選んだ高さに戻せます。

- **エディタが、次に行うべき操作を文書の真上に直接説明するようになり
  ました。** ヒントは文書の種類とツールに応じて変わり、単語クリック、
  テキスト選択、枠選択のいずれが期待されているかを示します。さらに、
  ツール、マウスカーソル、ライブプレビューが、指を離す前から何が
  起きるかを示します。

### 削除

- **不具合の多かったモノクロ出力が削除されました。** 一部のPDFでは、
  ラスタライズされたページに対して見えないテキストフィールドが
  ずれた位置に残っていました。見かけ上のファイル縮小は、この安全性
  および表示上のリスクに見合うものではありませんでした。通常のPDF
  浄化と、問題のあるページを対象を絞ってラスタライズする機能は
  引き続き利用できます。

## 0.10.32-alpha.20260819 – 2026年8月19日

### 新機能

- **フォルダ監視が、本当にバックグラウンドで動作するようになりました。**
  受信・送信・ルールは、「設定」内の専用ページにあります。開始と停止は、
  タスクバーまたはメニューバーのMaskuroアイコンから行います。この項目は、
  対応するライセンスで有効化されている場合にのみ表示されます。その後は
  設定ウィンドウを閉じ、メインウィンドウをアイコンに格納しても、監視は
  終了しません。

- **修正エディタに、常設の学習モードスイッチが加わりました。** これは
  ヒット領域と「ツール」メニューにあります。オフにすると、復元時にも
  手動修正の後にも、独自ルールを作成するかどうかの質問が表示されなく
  なります。Maskuroはこの選択を、以後開くすべての文書に対して記憶し
  ます。復元機能自体の動作は変わりません。

### 修正

- **大型の追加モデルが、再びダウンロードできるようになりました。** 公開
  ストレージが、Pythonの一般的な標準識別子を403で拒否していました。
  モデルの取得は現在、他の自社サービスと同じ、明示されたMaskuroの
  ネットワーク経路を使用します。約596MBのファイルとそのチェックサムに
  変更はありません。

- **最大化された比較ルーペが、ドッキングの際に上端の細いバーとして
  張り付いたままにならなくなりました。** ドッキングの前に、自由な
  ウィンドウ状態が正規化されます。保存された最大化状態も、次回開いた
  ときに変更可能なサイズに戻されます。

- **一括復元が、表やその他のテキスト形式で現在、本当に選択したすべての
  値を取り戻します。** `[EMAIL]`のような匿名化されたプレースホルダーでは、
  Maskuroはこれまで値を順番に書き込んでいました。最初の値が置換される
  と、残りのすべての該当箇所の番号が繰り上がりますが、すでに計算済みの
  計画は古い番号のままを指していました。そのため、選択の一部しか戻って
  きませんでした。現在は、同じプレースホルダーの選択されたすべての値が
  まとめて、安定した該当箇所番号とともに書き込まれます。ある箇所が別の
  復元された値によって初めて一意になる場合、Maskuroは同じ処理の中で
  再度それを検証します――これにより、選択の順序はもはや関係なくなり
  ました。

- **「置換を取り消す」が、PDFで選択した値を取りこぼさなくなりました。**
  プレースホルダーが別の単語のすぐ後ろにあったり、原本でカンマが値に
  直接くっついていたりすると、位置検証が隣接する単語や句読点を誤って
  値に含めてしまうことがありました。まとめて復元する際、その結果として
  個々のプレースホルダーやヒット行が残ってしまっていました。この検証は
  現在、実際の単語の先頭を基準にしており、原本と結果の間でページの
  向きが異なる場合も考慮します。

- **復元されたPDFのテキストが、現在は元の大きさを保持します。** これまでは、
  すでに縮小されたプレースホルダーが基準として使われており、さらに原本
  テキストにもプレースホルダー用に想定された上限11ポイントが適用されて
  いました。現在は、元の矩形と元のフォントサイズが元ファイルから引き
  継がれます――枠選択ツールでも、ヒットパネルからの復元でも同様です。

### 改善

- **検査の注意書きが、残存リスクをより明確に伝えるようになりました。**
  Maskuroがデータを見落としたり、項目を誤って処理したりする可能性が
  あることを明示的に伝え、公開や引き渡しの前には必ず完全な確認を行い、
  必要に応じて手動で修正するよう求めます。これはクリップボードからの
  テキストにも当てはまり、17言語すべての翻訳に完全に反映されています。

- **検査記録は現在、行の内部でも利用者名なしで開始します。** 記録機能
  自体は、組織が意図的に有効化するまでオフのままです。有効化された後も、
  追加の組織設定がなければ、行にも中央の月次ファイルの名前にも利用者名は
  現れません。そこでは、その組織のローカルなランダムなプロファイル秘密
  値からのみ導出された、推測不可能な仮名が安全な区別のために使われ
  ます。ライセンスダイアログはこの有効化をもはや推奨せず、「記録なし」を
  前提とし、事前に労使協議会、従業員代表、データ保護についての注意を
  示します。

- **置換は現在、何を置換したかを名前で示します。** 選択された氏名は
  `[NAME_3]`に、地名は`[ORT_1]`に、電話番号は`[TELEFON_2]`になります
  ――これまでのようにすべてが`[BEGRIFF_n]`になることはありません。種類は
  クリック時に判定されます。判定が一意でない場合――一般的な単語である、
  あるいは選択範囲に氏名*かつ*地名が含まれる場合――は、これまでどおり
  一般的な語のままになります。実際とは異なる種類を主張するプレース
  ホルダーは、何も主張しないものより悪いものになるためです。

- **修正ウィンドウ内のツールに、キーボードショートカットが付きました。**
  **S**は黒塗り、**E**は置換、**Z**は原本の復元、**V**はモザイクです。
  テキスト表示では選択範囲に即座に作用し、ページ表示ではツールを選択
  します。**文字はプログラムを操作している言語に従います**――英語では
  B/R/O/P、イタリア語ではO/S/R/Pです。覚え方の助けは自国語でしか役に
  立たないためです。この文字はボタンに表示されます。検索欄に入力して
  いる最中は、そのまま文字が入力され、ショートカットは働きません。

- **プログラムは1日に1回、識別情報を一切含まずに、どのような状態で
  動作しているかを通知します。** これにより、いくつのインストールが
  使用されているか、それが試用期間・フリープラン・ライセンスにどう
  分布しているかを集計します。送信されるのは、状態、オペレーティング
  システム、フッション、チャンネル、国、言語、環境、検出段階です――
  **お客様の文書に関する情報や、パソコンを再識別できるような情報は
  一切含まれません。** お客様からの2件の通知は、私たちから見ると2人の
  別々の人からの通知のように見えます。そこから単一の経路を追跡すること
  はできません。何が正確に送信され、どうすればオフにできるかは、
  プライバシー文書の第5項に記載されています。

- **横向きに給紙されたページが、現在は自動的に正しい向きで表示されます。**
  何の記録もないまま斜めにスキャンされた用紙は、修正機能がテキストの
  流れから検出し、表示の向きを直します。それができない場合――読み取れる
  テキストのない純粋なスキャンの場合――「表示」メニューの新しい2つの
  項目(Ctrl+Shift+LおよびCtrl+Shift+R)で手動で回転できます。回転する
  のは表示のみです。ファイル自体は変更されず、黒塗りは引き続きクリック
  した箇所に正確に適用されます。

- **ローカルでの出力が、現在ライセンス情報を完全かつ目に見える形で
  同梱しています。** ビルド処理は、実際に同梱されているPythonパッケージを
  特定し、そのライセンス文書をバージョン一覧とともに`lizenzen`配下に
  配置し、不足があればビルドを中止します。Qt、Tesseract、顔検出モデルに
  ついても必要な文書が用意されています。Maskuro自体の利用条件は、
  ライセンス契約書として同梱されます。

- **カーソルがどのプレースホルダー内にあるかが、現在確認できます。**
  プレースホルダー内をクリックすると、角括弧と番号を含めて全体が明るく
  表示されます。「選択箇所を復元」ボタンは、以前からクリックだけで
  有効になっていましたが、どのプレースホルダーを対象にしているかは
  見えませんでした。マウスがボタンへ移動しても、その光った表示は
  残ったままです。

- **マウスカーソルが、現在どのツールが選択されているかを示します。** 4つの
  ツールが同じ領域と同じ操作方法を共有しており、これまではどれも同じ
  見た目でした。十字カーソルは黒塗り、閉じた手は置換、開いた手は復元を
  意味します。

- **細工されたOfficeの文書を、現在プログラム自身が拒否します。** Word、
  Excel、OpenDocumentのファイルには、開いた際にお使いのパソコン上の
  別のファイルを本文に取り込んだり、メモリを使い果たさせたりする命令が
  仕込まれている場合があります。これまでもいずれも拒否されてはいました
  が、それはMaskuroではなく、組み込まれたXMLライブラリによるものでした。
  現在は、パッケージにどのバージョンのこのライブラリが入っているかに
  関係なく、プログラム自身が判断します。通常の文書には何の変化もあり
  ません。

### 修正

- **ヒットパネルが、現在は黒塗りされたプレースホルダーを削除します。**
  修正ウィンドウで例えば`[NAME_1]`が黒塗りされた場合、文書内にはもう
  そのような箇所が存在しないにもかかわらず、その値の行はこれまで右側に
  残ったままでした。この行は現在、最後の該当箇所とともに消えます。同じ
  プレースホルダーが別の箇所にまだ存在する場合は、行はそのまま残り
  ます。

- **回転したページで復元する際、隣接する単語がもう消えなくなりました。**
  黒塗りのバーは意図的にテキストより少しはみ出すよう作られています。
  この細いはみ出し部分だけでも、これまでは「im」のような隣接する単語を
  巻き込むことがありました。現在は、明確な重なりのみがカウントされ、
  端での接触はカウントされません。

- **同じ行内での2回目の置換が、後続の語まで巻き込んでいました。**
  「Sachbearbeitung Quaxi Blubbo übernimmt」を続けて2回置換すると、
  「Sachbearbeitung [ORT_1] [ORT_2]」となり――その後ろの単語は何の
  通知もなく完全に消えていました。原因は隣のプレースホルダーにあり
  ました。最初の置換の後、行の残り部分は空白文字から始まりますが、
  そのテキスト位置を探す処理が隣のプレースホルダーの閉じ括弧を誤って
  捉えてしまっていたのです。それ以降、すべてが1文字分ずれていました。
  影響を受けたのは、同じ行で2回置換または黒塗りが行われたすべての行
  でした――隣接する箇所の復元時も含みます。

- **プレースホルダーが長すぎる場合、置換がもう黒塗りにならなくなりました。**
  単語の隣に`[BEGRIFF_2]`のための余地がない場合、これまではその領域が
  黒く塗りつぶされていました――そうなると、そこにかつて何かが存在した
  ことすら見えなくなり、まして復元することもできませんでした。現在は
  より短い表記が書き込まれます。`[BEGR_2]`、`[BE_2]`、最終手段として
  `[B_2]`です。通し番号はどの段階でも維持され、それによって復元処理は
  その箇所を再び見つけられます。最も短い表記すら収まらない場合にのみ、
  バーが使われます。

- **同じ行内ですでに黒塗りが行われていた場合、置換がテキストをそのまま
  残していました。** 修正ウィンドウで原本から氏名を復元し、そのうちの
  名を置換し(そこには余地がなかったためバーになりました)、その後に
  姓を置換した場合、プレースホルダーは挿入されるものの、氏名自体は
  **削除されない**という結果になっていました。これは、事後確認の警告に
  よってのみ発覚しました。原因はその行自体にありました。最初の黒塗りの
  後、残りの部分は空白文字から始まり、そのためテキスト位置の検索が
  手がかりを見つけられませんでした。これは、同じ行内での2回目以降の
  黒塗りすべてに影響していました。

- **モデルなしで高度な検出が有効化されている場合、現在は気づけるように
  なりました。** チェックは入っているのにモデルが存在しない、という
  状態があり得ました――設定はインストールごとに有効ですが、モデルは
  プログラムの隣に置かれているためです。浄化処理は、その段階を使わずに
  何の説明もなく実行されていました。現在は、チェックがモデルの不足を
  伝え、結果には警告が付きます。一度行った選択はそのまま保存され、
  モデルが読み込まれ次第、再び有効になります。

- **匿名化の際、現在は正しい語句が復元されるようになりました。** 複数の
  語句を手動で置換し、その後そのうちの1つを復元すると、常に**最初の**
  ものが返ってきていました――「Schmidt」から「Müller」が出てくるという
  具合です。対応関係はプレースホルダーごとに1つの置換しか記憶しておらず、
  匿名化ではすべてが同じプレースホルダーを持つため、2番目以降の語句は
  失われていました。現在は、各値がそれぞれ独自の行を受け取ります――以前
  短すぎた置換一覧においても同様です。

- **表内でも、現在は復元ができるようになりました。** CSVや人事名簿では、
  プレースホルダーがセミコロンだけで区切られて直接隣り合っています。
  これまでプログラムは、そこでどの値がどの箇所にあったかを特定できず、
  拒否していました――`[NAME]`ではできても、`[GEBURTSDATUM]`と
  `[TELEFON]`ではできませんでした。現在は、行をすべてのプレースホルダー
  の位置で分解します。それでも本当に曖昧さが残る箇所は、引き続き処理を
  断ります。誤って書き戻された値は、情報が得られないことよりも悪い
  結果になるためです。

- **そして、その拒否が現在は見えるようになりました。** これまでは
  ウィンドウ下端に控えめな灰色で表示され、文が長すぎて途中で切れて
  いました――まるで何も起こっていないかのように見えていました。文は
  短縮され、その行は数秒間、警告色で光ります。

- **復元は現在、次の操作の後でも維持されます。** 匿名化の際に複数の
  箇所を復元し、その後別の何かを置換すると、復元したすべての箇所が
  再び置換された状態に戻ってしまい、最初からやり直す必要がありました。
  原因は対応関係にありました。値を保持したままにしており、統一された
  プレースホルダーのための自動照合が、次の書き込み時にそれを呼び
  戻していたのです。現在は、復元したものは復元されたままになります
  ――同じ値の他の箇所には影響しません。

- **テキスト・Word・Excel・メールのファイルで、現在は本当にプレース
  ホルダー内をクリックするだけで済みます。** これに関する通知は前の
  フッションからすでに表示されていましたが、「選択箇所を復元」ボタンは、
  何かが正確に選択されていない限りロックされたままでした――つまり、
  選択自体を自動で設定してくれるはずの経路そのものに、たどり着けなかった
  のです。

### 修正

- **検査記録が、現在はファイル名を漏らさなくなりました。** ファイル名は
  依頼人や係争事項を明らかにしてしまうため、記録はファイルを意図的に
  平文ではなくハッシュ値として扱います。しかし、このハッシュ値は
  推測によって確認できてしまう可能性がありました――パスは乱数では
  ないためです。現在は、このインストール固有のランダムな値が計算に
  組み込まれます。記録内でのカウントや区別は引き続き機能しますが、
  外部からの逆算はできなくなります。

## 0.10.31-alpha.20260819 – 2026年8月19日

### 改善

- **テキストファイルや表ファイルでも、指し示した際にプレースホルダーが
  赤く光るようになりました。** これまで赤いプレビューはPDFのページ上でしか
  表示されませんでした。現在は両方の表示が同じ挙動をします。赤い箇所が
  次の操作の対象であり、クリックひとつで復元できます。

- **単語をクリックするだけでよく、四角い枠はエディタが自動で設定します。**
  修正ウィンドウでは、これまで箇所ごとに枠をドラッグして描く必要が
  ありました。現在はクリックひとつで済みます。枠は単語を囲んで表示され、
  操作可能な状態のまま残るので、さらに広げたり移動したりできます。マウスを
  かざすと単語がすでに赤く光るので、クリックすると何が対象になるかを
  事前に確認できます。単語がない箇所では、これまでどおり枠をドラッグして
  描きます。

- **四角い枠でもう正確に狙う必要はなくなりました。** プレースホルダーや
  黒塗りの上に枠をドラッグする人は、常にその箇所全体を意図しています――
  半分だけということは決してありません。そのため、枠は触れた対象全体に
  向かって自動的に広がります。プレースホルダー全体、バー全体、あるいは
  スキャンされた用紙上では黒塗りされた領域全体です。ドラッグした枠より
  小さくなることは決してありません。

- **黒塗りは現在、単語単位で行われます。** これまでは単語の半分に枠が
  かかると、半分だけが黒塗りされていました――半分だけ黒塗りされた氏名も、
  依然として氏名です。触れた単語は現在、丸ごと対象になります。隣の単語は
  そのまま残ります。

- **テキストや表では、プレースホルダー内をクリックするだけで済みます。**
  「選択箇所を復元」はこれまで、角括弧を含めてプレースホルダーを正確に
  選択することを要求していました。現在はカーソルをその中に置くだけで
  足り、選択範囲はプレースホルダー全体へと見た目にも広がります。

- **ベルギーが対応国として追加されました。** 設定画面で選択でき、選択
  すると、ベルギーの電話番号、国民登録番号(検査数字付き)、BTW/事業者
  番号(検査数字付き)、両公用語での住所、および郵便番号と地名が認識
  されます。これまではベルギーが国のカタログにまったく載っていなかった
  ため、ベルギーの電話番号はそのまま残っていました。

- **更新チャンネルは現在、「どれだけ早く」新しいものが届くかを示し、
  「どれだけ遠く」かは示しません。** 「テスト版」を選んでいた人には、
  新しいプレビュー版や新しい安定版がそもそも提案されず、それに気づく
  ためだけにチャンネルを手動で切り替える必要がありました。現在は、より
  信頼性の高いものもすべて提案されます。テスト版はテスト版・プレビュー版・
  安定版を受け取り、プレビュー版はプレビュー版と安定版を受け取ります。
  逆方向は決してありません――プレビュー版に対しては、たとえより新しくても
  テスト版は提案されません。

- **設定ウィンドウでは、行の間隔がより広く保たれるようになりました。**
  4つのページが、プログラムの他の部分で使われている格子(グリッド)では
  なく独自の間隔を使っていたため、特に「検出」ページではチェックボックス
  が目に見えて詰まって並んでいました。

### 修正

- **入力済みのPDFフォームが、手作業での編集時に空欄に見えることが
  なくなりました。** Maskuroはこのために、一時的な作業コピーだけを静的な
  ページに変換します。入力された値は表示され、実際に黒塗りできるように
  なります。読み取り可能なフォームフィールドが、ファイル内に見えない
  まま残ることはありません。原本は対話的なまま、変更されずに残ります。
  これは現在、動的なXFAフォームにも当てはまります。XFA対応のPDFiumが
  まず値とページ区切りを構築し、その後、静的な画像ページのみからなる
  新しいPDFが作成されます。XFAの構築に失敗した場合は、見かけ上空の
  ファイルとして開かれる代わりに、安全に拒否されます。

- **「中止」が、より精密な検出処理の最中でも機能するようになりました。**
  これまでは、クリックするとボタン自体は押せなくなるものの、処理は最後の
  ブロックまで計算を続けていました――長いファイルではこれが数分間の
  身動きできない状態を意味し、しかもボタンはあたかも効いたかのように
  見えました。現在は、次のブロックの時点で処理が終了します。

- **CSVファイルでは、氏名の前に空白がない場合でも氏名が見つかるように
  なりました。** `P-1000;Brunnthaler, Elisabeth` では、社員番号が
  セミコロンを挟んで氏名に張り付いており、検出処理にとってはこれが
  氏名を含まない単一の単語でした――そのため、人事名簿では行によって
  氏名全体がそのまま残っていました。電話番号、数式、ファイルの列数は
  これによる影響を受けません。

- **名と姓の両方にハイフンが含まれる氏名が、認識されるようになりました。**
  「Marie-Luise Habsburg-Ott」は文中にそのまま残っていた一方、同じ文中の
  「Dragan Mitrović」は検出されていました――よりによって、結合された
  2つの部分の組み合わせを言語モデルが見落としていたのです。
  「Nord-Süd-Verbindung」や「Software-Entwickler」のような結合された
  一般名詞はこれによる影響を受けません。

## 0.10.30-beta.1 – 2026年8月18日

### 改善

- **テキスト表示の文字サイズが、目に見える形で調整できるようになりました。**
  右下のスライダーは、これまでページプレビューでの拡大にしか使えません
  でしたが、修正ウィンドウのテキストファイルやOffice文書では文字サイズ
  (50~300%)を調整します。表示メニューの「拡大」/「縮小」も同様です。
  Ctrl+マウスホイールは以前から使えましたが、それは試した人だけが知って
  いました。現在はスライダー、表示、ホイールが連動します。

- **ダーク表示では現在、暗い作業面の上に白い用紙が表示されます。** これ
  までは逆でした。用紙の周りに明るい面が残り、テキスト自体が暗い背景に
  明るく表示されていました。現在は、両方の表示モードで用紙が紙のように
  白く、文字が黒いままです――ダークモードでも暗くならないPDFのページと
  同様です。周囲の面は暗いままです。

### 修正

- **文の途中での黒塗りの後、文の残りが消えなくなりました。** 修正
  ウィンドウで同じ箇所に3回操作を行った場合――置換、黒塗り、そして
  「原本を復元」――、文の先頭が削除されていました。「Rückfragen richten
  Sie bitte an das Rechnungswesen.」が「bitte an das Rechnungswesen.」に
  なってしまい、警告もありませんでした。行の途中から何かがすでに一度
  削除されていた箇所すべてが影響を受けていました。

- **起動時のエラーが、終了処理まで巻き込まなくなりました。** メイン
  ウィンドウの構築が中断した場合、その後タスクバーアイコンからの終了も
  クラッシュしてしまい、この2つ目のエラーがエラー報告の中で本当の原因を
  覆い隠していました。現在は、半分しか構築されていないウィンドウからでも
  プログラムは正常に終了し、保存済みの設定はそのまま保たれます。

- **「前後比較」が、文書の先頭に飛ばなくなりました。** 修正ウィンドウで
  下にスクロールしてから比較のために原本表示に切り替えると、再び一番
  上に戻ってしまい、その箇所を手動で探し直す必要がありました。現在は、
  どちら方向に切り替えても表示は同じ行に留まります。

- **黒塗りの際、両端揃えの行で最後の文字が残ってしまうことがありました。**
  テキスト描画命令が、読み取りライブラリが報告する文字数より多くの
  字形を描く場合(両端揃えでは空白文字が省略されがちです)、対応関係が
  1つずれてしまい、「Dr. Michael Handler aus Willendorf」が
  「[NAME] r aus f」になってしまいました――浄化された文の途中に2文字が
  残ってしまったのです(実在の議事録で発見)。この対応関係は現在、
  描画命令自体の文言が読み取り可能な場合はそれと照合されます――もう
  推測に頼ることはありません。

- **「Lerchenfelder Gürtel 43/12」は半分しか削除されていませんでした。**
  住所パターンは、Gürtel、Kai、Lände、Zeile、Markt、Grabenのいずれも
  通りの基本語として認識しておらず、また番地にスラッシュを含む部分
  (43/12、Haus/Tür)を持つことを許可していませんでした――そのため
  番号がプレースホルダーの脇にそのまま残っていました。両方とも追加
  されました。ウィーンおよびザルツブルクの住所は現在、完全に対象と
  なります。

- **保存されたウェブページが、浄化後も動作するようになりました。**
  遅延読み込み用のdata属性(`data-lazy-src`、`data-lazy-srcset`)に
  格納されたアドレスが参照として置換されていました――実在する自治体の
  ページでは16箇所――その結果、ページの画像がその後読み込まれなく
  なっていました。ウェブアドレスは現在、`src`や`href`と同様にそこに
  残ります。data属性内の氏名、メールアドレス、電話番号は引き続き置換
  されます。

- **日本語と韓国語の文書が、中国語として処理されていました。** 言語検出
  は3つの文字体系すべてを一括りにしており、日本語のテキスト(空白なし)
  や韓国語のテキスト(助詞が結合)には機能語が見つからず、単純に
  カタログの最初のCJK言語を採用していました。日本語の議事録と韓国語の
  会議録は、こうして中国語モデルで読み取られていました。現在は文字の
  見た目自体で判断します。かなは日本語、ハングルは韓国語を意味します。

- **さらに10言語のフィールドテストで見つかった誤検出:** 「Primar」
  「Gradonačelnik」「Ordfører」「Başkanı」「Δήμαρχος」のような役職は
  もう人名として扱われません。トルコ語の項目見出し(「Adı」
  「Soyadı」)とギリシャ語の会話語(「Ωραία」「Βεβαίως」)は対象外に
  なります。日付付きの決議・条文番号(「323/25-6-2008」「27
  30.09.2024」)はもう電話番号として扱われません。ピリオド付きの文の
  断片(「10.An」「T.U.EE」「…pa」)はもうウェブアドレスとして置換
  されません。

### 新機能

- **検査報告書を、希望に応じて自動作成できます。** 設定(「プログラム」
  ページ)のチェックにより、浄化のたびに検査報告書のPDFが自動的に
  作成されます――名前にタイムスタンプが付き、専用フォルダに、結果とは
  決して同じ場所に置かれません。後から用紙を作成することはできません。
  記録のためにこれが必要な人には、これで常に手元にあります。初期設定
  ではこの保存はオフです。

- **検査記録が、プログラム内で有効化できるようになりました。** 法人向け
  ライセンスを読み込む際、Maskuroは一度、記録を残すかどうかを尋ねます
  ――証跡は最初から動作していてこそ意味があるためです。これに対応する
  スイッチが設定(「プログラム」ページ、法人向けライセンスまたは試用
  期間で表示)にあります。管理者の設定ファイルはこれまでどおり有効で、
  値をこれまでと同様に強制できます。「有効化」という独自の記録行が、
  いつから記録が開始されたかを残します――これにより記録の開始自体が
  証明され、署名されたことになります。記録は初期設定ではオフのままです。

- **指標のクラッパーに、AI段階が何を行ったかが表示されます。** 新しい行に、
  モデルが判定した不確実なヒットの件数、採用件数、破棄件数、そして
  追加で発見した件数が表示されます――これまでは、修正エディタで値を
  ひとつひとつクリックしない限り、その作業は見えないままでした。表示
  されるのは件数のみで、値や理由は表示されません。AIの作業がない場合、
  この行は表示されません。

- **復元機能が、現在はメールやHTMLページでも使えるようになりました。**
  `.eml`、`.msg`、保存されたウェブページでは、これまでプレースホルダーを
  復元できませんでした――アプリケーションは正直にその旨を伝えていました
  が、まさにメールこそが個人情報の最も多い形式です。現在は、そこでも
  復元が同様に機能します。ヒットパネルから、選択範囲から、匿名化された
  プレースホルダーに対しても可能です。メールの見えないHTML部分
  (Outlookが実際に表示している内容)もこれに伴って更新され、表示と
  メッセージが同じ内容を示すようになります。

- **ヒットパネルは、匿名化された値も値ごとに復元できるようになりました。**
  「置換を取り消す」は、匿名化されたファイルではこれまでロックされて
  いました。「[NAME]」がすべての氏名を同時に表しているためです。現在は、
  復元処理が原本を参照し、どの箇所がどの値に属するかを調べます――PDFでは
  該当箇所の座標で、テキスト表示では原本との比較で――そして選択した値の
  該当箇所だけを正確に復元します。残りの値の行はそのまま残ります。

- **匿名化されたプレースホルダーも、個別に復元できるようになりました。**
  匿名化では同じ種類の項目はすべて同じ名前になります――「[NAME]」は
  すべての人物を表し、これまではそのため「個別の復元はできない」と
  されていました。現在は、結果の隣にある原本が参照されます。テキスト
  表示でプレースホルダーを選択し、「選択箇所を復元」を選ぶと、まさに
  その箇所が、まさにその値とともに戻ってきます。原本から値を確実に
  読み取れない場合、アプリケーションは推測する代わりにその旨を伝えます。
  対応関係を記録したファイルが作成されることは、引き続きありません。

- **修正ウィンドウが、浄化の後に自動的に開くようになりました。** どんな
  道具もすべてを見つけることはできません――そのため、結果を確認する
  作業は例外的な追加操作ではなく、通常の流れの一部であるべきです。これを
  望まない場合は、設定の「検出」で無効にできます(「結果をその後、
  修正ウィンドウで表示する」)。

### 改善

- **国の選択が、新たに「自動」を標準とします。** これまでは初期設定で
  画面表示言語の言語圏が適用されていました――つまり、ドイツ語の
  パソコンでは、オランダ語やフランス語の文書もDACH(独墺瑞)向けの
  検出器だけで浄化されており、「Universiteitslaan 1」のような住所が
  そのまま残っていました(実在する公開議事録で発見)。現在は、国の選択が
  文書の言語に従います。設定で固定の選択を行っている場合は、それが
  維持されます。

- **誤った黒塗りが減りました。** 検査コーパスおよび6言語の実在する
  議事録に基づいて測定された一連の誤検出が解消されました。法人格を
  含む会社名(「Musterfirma GmbH」)は、もはや人物や地名ではなく組織と
  して扱われます。挨拶の言葉や単独の敬称(「Saygılarımızla」
  「Buenas tardes」、単独の「Frau」)はもう氏名として扱われません。
  役職(「Bürgermeister」「Sindaco」「Alcalde」)はそのまま残ります。
  法令・決議番号(「39/2015」)や桁区切りの千の位を含む金額
  (「330.000」)はもう電話番号として扱われません。「Envíame」や
  「Estarei」のような文頭の語は、もう氏名として抽出されません。空行を
  またぐヒットは、もう氏名としてカウントされません。請求書の請求書番号は
  証憑情報として保持されます――顧客番号や事件番号は引き続き対象になり
  ます。

- **AIモデルを読み込む前に、それが何のためのものかが表示されるように
  なりました。** 再取得ダイアログには、このモデルの役割――境界線上の
  ヒットを判定する、追加の氏名を見つける、ルールやプロファイルを提案する
  ――が示され、これがチャットアシスタントではないことも率直に示されます。
  FAQでは同じ質問(「AI段階には何ができて、何ができないのか?」)に
  詳しく回答しており、これはすべての言語版で対応しています。

### 修正

- **コマンドラインからの検査報告書PDFが、検索できるようになりました。**
  Windowsでは、ヘッドレスのPDF経路がフォントを一切持たずに起動しており
  ――すべての文字が代替の四角形として描かれ、用紙には読み取り可能な
  テキストがありませんでした。そこで検索したりコピーしたりしようとした
  人は、何も見つけられませんでした。現在は、その場合報告書がシステムの
  フォントを再取得します。テキストは埋め込まれ、読み取り可能です。
  ウィンドウから作成される報告書は、これまでも影響を受けていません。

- **スキャンの複数行にまたがる「原本を復元」が、行の間に黒い縞を残して
  いました。** 画像に変換されたページ上で、枠は行の帯そのものしか
  処理しておらず、以前の黒塗りの残りがその間の隙間に残っていました。
  現在は、ドラッグした枠が行に完全に分割されます。

- **プレースホルダーの上に2つ目の枠を重ねると、赤い残りが残っていました。**
  プレースホルダーは、それが代表する単語よりほぼ常に幅が広いため、その後
  同じ箇所を黒塗りすると、その先頭部分にしか当たらず、「RIFF_1]」の
  ような断片が文の途中に残っていました。その後の復元処理は、単語では
  なくその断片の位置に原本のテキストを挿入していました。切れた
  プレースホルダーは現在、常に全体が対象になります。

- **回転したページで、プレースホルダーの上の黒塗りが無関係な文を削除して
  いました。** 後から描かれたプレースホルダーが、削除時にその前にある
  テキストと混同されていました。プレースホルダー自体はそのまま残り、
  「まだ文書内に存在します」という警告が表示され――そしてページの別の
  箇所では、枠とは無関係な文が代わりもなく消えていました。プレース
  ホルダーは現在、その文言をもとに再検出されます。「置換、黒塗り、
  復元」という一連の操作は、これにより横向きに給紙されたページでも
  正しく機能するようになりました。

- **マニュアルは10言語で、いまだに`python3-tk`を勧めていました。**
  トラブルシューティングの項には、Linuxではtkinterが不足している
  可能性がある、という記述がありました――これはQt表示への移行以前の
  時代の助言で、もう誰の役にも立ちません。現在はすべての言語版で、
  ドイツ語版と同じ内容が記載されています。Qtが描画に必要とするシステム
  ライブラリが不足している、という内容です。

- **マニュアルのライセンスの章が、16言語すべての翻訳で古い内容の
  ままでした。** 10言語では、いまだにWindows Serverではサーバー
  アクセス権付きの法人向けライセンスが必要で、そこには試用期間も
  フリープランもない、という記述がありました――1つの席が機械ではなく
  人を数えるようになって以降、この両方とも誤りです。さらに、埋まった
  席がいつ再び解放されるか、ライセンスが定期的に自己確認を行うこと、
  その際に何が送信されるかについての説明も、どこにもありませんでした。
  インターネットなしでの有効化についても、3つの手順もその後1年間
  接続なしで動作するという説明もない、簡略版のままでした。

- **修正について説明する7つの段落が、10言語で欠落していました。**
  デンマーク語、フィンランド語、フランス語、イタリア語、オランダ語、
  ノルウェー語、ポーランド語、ポルトガル語、スウェーデン語、
  スペイン語でヘルプを読んだ人は、Office文書のページプレビューも、
  「手動で黒塗り」も、プログラムが修正からどう学習するかという節全体
  も――3段階の幅の表を含めて――見つけられませんでした。「何が検出
  されるか」の項でも、同じ10言語で、文書内の見出しを経由する検出方法が
  欠落していました。

- **ライセンスを読み込んだ状態で、プログラムが起動しなくなっていました。**
  ウィンドウの代わりに「プログラムを起動できませんでした」と表示され
  ――しかもどのライセンスでも発生していました。原因は、確認期限が
  近づくと警告するライセンス表示の行にあり、そこに存在しない情報に
  アクセスしていたのです。ライセンスなしの状態(試用期間やフリープラン)
  では発生しなかったため、このエラーはこれまで見つかっていませんでした。

- **フォームでは、フィールド名がそのまま残るようになりました。**
  「Geburtsdatum(生年月日)」や「Anschrift(住所)」がその値ととも
  に消えていました。プレースホルダーは小さく赤い文字で、まさに
  *フィールド名*の位置に表示され、その下の欄は空のままでした。
  フィールド名はデータには属しません――現在はそのまま残り、プレース
  ホルダーは値があった場所に表示されます。

- **外国語の文書タイトルが、もう氏名として扱われなくなりました。**
  イタリア語のフォームの上には「FATTURA」、スペイン語のフォームの
  上には「PERMISO PARENTAL」と書かれていましたが、どちらも置換されて
  いました。書類語の一覧には、ドイツ語の対応語しか登録されていません
  でした。

- **請求書から明細行が消えることがなくなりました。** 「Materialaufschlag
  1  84,00」が住所と誤認され、地名のプレースホルダーに置換されて
  いました――その結果、証憑から1行が欠けていました。金額で終わる行は
  明細行であり、住所ではありません。実際の住所(「Hauptstraße 1
  120,00」)は影響を受けません。

### 変更

- **「フォルダを監視する…」とコマンドラインは、当面利用できません。**
  どちらの経路も構築され動作していますが、いずれも実地では検証されて
  いません。フォルダ監視はWindowsでの実地検証を一度も受けておらず、
  コマンドラインはスクリプトに、これまで実際の利用者の手元で一度も
  動いたことのない20あまりのスイッチを渡してしまいます。監視なしで文書を
  変更するものは、未検証のまま動作させるべきではありません――そのため、
  実地検証が済むまでこれらは撤回されています。メニュー項目はなくなり、
  `--wache`はもう`maskuro --help`に表示されません。

- **読み取り専用のものと、そもそも必要とされているものは、引き続き
  利用できます。** 検索実行(`--suchlauf`)と事後確認(`--nachpruefen`)は
  コマンドラインで引き続き動作します――これらはファイルを変更しません。
  エクスプローラー、コンテキストメニュー、クリップボード、ウィンドウ
  からの起動も同様です。これらに変更はありません。

- **「スキャナーから取得」に、マニュアルの独立した章ができました。**
  これまでは「フォルダを監視する」の末尾にありました。Macでは、そこに
  フォルダを監視させるようにという助言が書かれていましたが、現在は
  読み取ったページをウィンドウにドラッグするようにという内容になって
  います。

### 修正

- **複数行にわたる「原本を復元」が、構造を破壊していました。** プレース
  ホルダー、変更されていない役職名、そして2つ目の置換にまたがる枠は、
  対象領域全体を**1行**として新たに挿入していました――3行が1行になり、
  収まらなくなった部分はバーになっていました。現在は各行が個別に
  復元されます。

- **そして、その際、変更されていないテキストには手を加えません。** 置換
  箇所*と*通常のテキストの両方にまたがって枠をドラッグした場合、戻って
  くるのは置換箇所だけで、残りは変更されません。古いプレースホルダーの
  最後の残骸も、これにより消えます――以前は、その閉じ括弧が文の途中に
  残っていました。

- **置換の際、古いテキストの残骸がもう残らなくなりました。** 太字の
  見出しでは、置換後に「1. R[BEGRIFF_2]ige [BEGRIFF_1] … che」のように
  表示されていました――プレースホルダーはそこにあるものの、原本の
  音節がその脇に残っていたのです。現在は、囲んだ領域全体が整理され、
  その中の単語の矩形だけではなくなりました。

- **匿名のプレースホルダーが、もう誤って復元されなくなりました。**
  匿名化では、すべての氏名が同じ`[NAME]`を持ちます。復元処理は最初に
  見つかった項目を取り、それをすべての該当箇所に書き込んでいました――
  「Georg Aigner」が「Anna Musterfrau」になる、つまり文書内に誤った
  氏名が入ってしまっていました。現在は、どの項目のことか判別できない
  旨が表示されます。文書には手が加えられません。

### 新機能

- **「原本を復元」が、現在はラスタライズされたページでも機能します。**
  ページが画像に変換されていた場合、これまでは拒否されていました。
  復元されたテキストはページ画像の下に隠れてしまうためです。現在は、
  画像内の該当箇所が消去され、その上にテキストが書き込まれます――
  スキャン上のプレースホルダーと同様です。内容は元ファイルから取得
  され、そちらはラスタライズされていません。

- **「選択箇所を復元」が、現在は独立したボタンとして表示されます。**
  これは以前から可能でしたが、たまたまプレースホルダーを選択して
  「選択箇所を置換」を押した場合に限られていました――偶然によってしか
  見つからない機能は、利用者にとって存在しないのと同じです。

### 変更

- **純粋なテキスト、CSV、Outlookのメッセージでは、「選択箇所を黒塗り」
  がもうありません。** これらの形式はバーを扱えません。このボタンは
  そこではプレースホルダーを設定しており、その旨も表示していました――
  しかし、名前と異なる動作をするボタンは、そこにあるべきではありません。

- **道具は現在、その箇所で何もできない場合はその旨を伝えます。**
  プレースホルダーはもう一度置換することはできず、黒塗りの上には
  プレースホルダーは設定されず、すでに原本が表示されている箇所には
  復元するものがありません。これまでは、これらの操作は効果があるように
  見えて、実際には何の効果もありませんでした。

## 0.10.29-alpha.20260817 – 2026年8月17日

### 修正

- **修正ウィンドウでは現在、ドラッグして描いたすべての枠が機能します。**
  同じ箇所を2回操作した場合――まず置換、次に黒塗り、そして原本の復元――、
  2回目と3回目の操作は何も言わずに無効になっていました。前の操作の枠が
  まだ操作可能なままで、それが後の操作を横取りしていたのです。ツールを
  切り替えた場合も同様で、古いツールが黙って効き続けることさえありました。
- **狭すぎる枠を描くと、狭すぎることが伝わるようになりました。** これまでは
  プレビューで単語が赤く光るのに、指を離すと何も起こらず、何の説明も
  ありませんでした。

- **Outlookのメッセージが、ついに修正できるようになりました。** `.msg`は
  修正ウィンドウで「この形式はここでは表示できません」と表示され、手作業で
  修正する手段がまったくない唯一の対応形式でした。現在は、送信者、受信者、
  件名、本文がそれぞれ名前付きで表示に現れ、他のテキスト形式と同じように
  選択して置換できます。

- **「選択箇所を置換」が、メールの中でも選択範囲どおりに動作します。**
  本文中の氏名を選択すると、ヘッダー内の送信者と受信者まで巻き添えで
  失われ、通知は本文にあるものとは別のプレースホルダーを示していました。
  現在は選択した値がすべての箇所で置換されます――送信者欄にある場合は
  そこも含めて――それ以外は何も変更されません。

- **複数行にまたがる枠が、テキストを破壊しなくなりました。** これまでは
  1箇所だけプレースホルダーが1つ生成され、途中で切れた単語の残りが
  そこに張り付いたまま残り、2行目のテキストは代わりもなく消えていました
  ――プレースホルダーもバーもなく、ただの空白だけでした。現在は各行が、
  実際にそこにあった値を持つ、それぞれ独自のプレースホルダーを受け取り
  ます。

- **「原本を復元」が、黒塗りの後でも機能するようになりました。** ウィンドウは
  成功を通知しましたが、テキストは二度と戻ってきませんでした。黒いバーが
  障害物として数えられ、復元されたテキストのための場所がなくなっていた
  のです。現在はバーがそこで退き、復元されたテキストは通常のテキストと
  同じく黒色で表示されます――プレースホルダーのように赤くはなりません。

- **手を加えていない箇所での「原本を復元」は、もう何もしなくなりました。**
  何も変更されていないテキストの上に枠をドラッグすると、そのテキストが
  削除され、縮小されてずれた位置に再挿入され、しかも成功が通知されて
  いました。現在は、復元するものが何もない旨が表示されます。

### 新機能

- **Word、Excel、PowerPoint、OpenDocument、テキストでも黒塗りができる
  ようになりました。** これまでそこにあったのは「選択箇所を置換」だけで、
  バーはPDF表示に限られていました――特に理由もなくです。バーを表示
  できない場所――純粋なテキストとOutlookのメッセージ――では、これまで
  どおり値がプレースホルダーで置き換えられ、通知にもそのとおり表示され
  ます。

- **プレースホルダーを選択すると、それが復元されます。** テキスト表示
  (Word、Excel、PowerPoint、OpenDocument、テキスト)では現在、
  プレースホルダーを選択して「選択箇所を置換」を押すだけで、元の値が
  戻ってきます。これまでウィンドウは、そのためにヒットパネルを参照する
  よう案内していました。

- **議事録の発言者は、その氏名が同時に一般的な単語でもある場合でも
  認識されるようになりました。** 「Gruber: Die Abnahme erfolgt kommende
  Woche.」は置換されましたが、「Bauer: Ich stimme zu.」はそのまま残って
  いました――姓が検出処理にとって普通名詞のように見えたのです。同じ形の
  注意書きの行は影響を受けません。「Achtung: Die Anlage ist
  abzuschalten.」から氏名が作られることはありません。

- **確認自体ができなかった場合でも、「最新のフッションを使用しています」
  と表示されていました。** 更新サーバーが要求を拒否した場合――同じ
  インターネットアドレスからの要求が多すぎたか、サーバー自体が障害を
  起こしていたため――、プログラムは古いフッションのまま動作を続け、
  それが最新であると主張していました。まさにこれが8月17日、あるMacで
  発生しました。0.10.25が留まったまま、0.10.28は何時間も前から準備が
  できていたのです。

  現在はウィンドウが状況を伝え、次に確認する時刻を示し、自分のフッション
  が最新かどうかは**確定していない**ことを明示的に指摘します。

  多くの場合、原因は自分のパソコン側にはありません。多くの回線では、多数の
  顧客が同じインターネットアドレスを共有しており、サーバーはそれをまとめて
  カウントします。そのためMaskuroは、この場合フッション一覧を**別の経路**
  でも検索し、たいていはそれでも新しいフッションを見つけます。それでも
  拒否が続く場合、示された時刻まではサーバーへの問い合わせを控えます
  ――ボタンをもう一度押しても同じです。しつこく押すと、待機時間が延びる
  だけです。

- **数量表現が、地名と誤認されなくなりました。** ある業務委託契約書では、
  「Vier-Tage-Woche(週4日勤務)」が契約対象のまさに真ん中で地名の
  プレースホルダーの陰に消えていました。このような数字とハイフンから
  成る語の組み合わせ(「Drei-Punkte-Plan」、「24-Stunden-Dienst」)は
  現在そのまま残ります。住所はこれから除外されます。「Zwei-Brüder-Weg」
  は引き続き置換されます。

## 0.10.28-alpha.20260817 – 2026年8月17日

### 変更

- **ライセンス台数が現在、実際にカウントされるようになりました。** これまで
  は、いかなる作業台もライセンスサービスに登録されたことがなく、10台分の
  ライセンスが誰にも知られることなく、いくらでも多くのパソコンで動作して
  いました。新機能:プログラムを起動したパソコンが1台分を使用します。
  **7日間起動がない**と、その席は自動的に再び解放されるため、故障した
  機器や退職した従業員が席をずっと塞いだままになることはありません。

  わずかな超過は**表示されるだけで、ロックはされません**。購入台数を10%
  超えるまでは全員がそのまま作業を続けられます――まだ登録が残っている
  古いパソコンの隣に新しいノートパソコンが加わっても、サポート窓口に
  電話する話にはなりません。それを超えて加わった人はフリープランに戻され、
  その旨が通知されます。先にいたパソコンはそれに気づきません。

- **購入済みのライセンスは、定期的に自己確認を行います。** それが**30日間**
  失敗し続けた場合、成功するまでの間はフリープランが再び適用されます。
  何かが停止することはなく、1週間前からウィンドウに案内が表示されます。
  パソコンが再びインターネットに接続すると、自動的に解消されます。試用期間
  とフリープランは引き続き一切通知しません――一度も購入しない人が電話を
  かけることはありません。

- **「インターネットなしで有効化」が、ついに機能するようになりました。**
  有効化情報はこれまで検証され保存されてはいたものの、その後誰にも読まれる
  ことがなく、権限には何の変化ももたらしていませんでした。現在はこれが、
  ネットワークに接続していないパソコンのための解決策です。**1年間**有効で、
  その後は新しい要求コードを使って新たに取得します。そのためにインター
  ネットに接続できる機器が年1回必要になりますが、パソコン自体は恒久的に
  オフラインのままで構いません。

- **有効化は現在、お客様アカウントからも行えます**――ウェブサイトの
  「マイライセンス」からです。そこには、どのパソコンがライセンスに紐づいて
  いるか、その席がいつ再び解放されるかも表示されます。これまではどこにも
  表示されていませんでした。ログインなしのページは、ショップアクセスを
  持たない方向けに引き続き利用できます。その場合はライセンスキーだけでは
  足りず、注文時のメールアドレスも追加で必要です。

- **要求コードをどこに入力すればよいかも、現在はウィンドウに表示されます。**
  紙の案内には「インターネット接続のある機器で入力してください」とだけ
  書かれ、住所は示されていませんでした。有効化用のページはすでに存在して
  いましたが、どこからもリンクされていなかったのです。現在は
  **maskuro.com/lizenz-freischalten** がダイアログ、マニュアル、FAQに、
  そしてウェブサイトのライセンスキー欄の下に表示されます。

- **「インターネットなしで有効化…」ボタンは、ライセンスが現在無効な
  場合でも表示されたままになります。** 以前はライセンスとともに消えて
  いました――まさに必要なときに、です。

- **「すべての席が埋まっています」は、現在正しい内容を伝えます。** この
  案内は「プログラムは変更なく動作を続けます」という文で終わっていました
  が、席が割り当てられなかった場合、それはもう正しくありません。現在は、
  当面フリープランが適用される旨が表示されます。

### 新機能

- **クリップボードの浄化を有効にする際に、確認が必要である旨が表示される
  ようになりました。** この通知は、ファイルの結果に表示されるのと同じ文を
  伝えます。Maskuroは、あらゆる場合に個人データをすべて検出できるとは
  限りません。

  ここでは、他の場面よりもこの点が重みを持ちます。ファイルの場合は、渡す
  前に結果を確認できます。クリップボードではそれができません――コピーして
  貼り付ければ、浄化済みのテキストはすでにメール画面に入っています。その
  ため通知は、**貼り付けた**テキストを見直すよう明示的に伝えます。

  この通知は有効化のときに表示され、コピー操作のたびには表示されません。
  1日に50回表示されるようでは、3回目以降は誰も読まなくなります。

- **一覧の下に「すべてコピー」――そして「すべて削除」は離れた位置に
  移動しました。** 新しいボタンは、完成したすべての結果を一度にクリップ
  ボードへ置きます。メールへの添付や、他のプログラムへの貼り付けの
  ためです。これまではメニューからしかできず、しかもそこでも**選択された**
  行にしか適用されませんでした――すべてを意図する人は、まずCtrl+Aを
  押す必要がありました。

  同時にボタン列も並べ替えられました。左側には何かを追加する操作、右側には
  間隔を挟んで何かを取り除く操作が並びます。「すべて削除」はこれまで
  「追加…」のすぐ隣にあり、操作を誤ると一覧全体が失われていました。同じ
  規則は8月13日以降、完成した各行にもすでに適用されています。

- **インターネットのない作業台も、現在は言語モデルを社内から取得できます。**
  浄化処理はもともと接続なしで動作していましたが、言語モデルの再取得は
  そうではなく、1つのモデルは数百メガバイトの重さがあります。

  管理者は、接続のあるパソコンで一度ファイル一式をまとめ、共有フォルダ、
  ロールアウト、またはUSBメモリに置きます。取得元は中央で登録されます
  (`vorgaben.json`の`modellquelle`項目、または環境変数
  `MASKURO_MODELLQUELLE`)。以降、すべての再取得――言語モデル、日本語
  辞書、上位段階――はまずそこを利用し、ファイルが見つからない場合のみ
  ネットワークにアクセスします。

  検査用のチェックサムはこれまでどおり有効です。社内のファイル共有は、
  ネット上のリリースよりも簡単に用意できることが多いため、これが不正な
  モデルを紛れ込ませる、より手軽な経路になってはなりません。

  このような在庫がどのように作られるか、そしてインターネットなしで
  ライセンスと有効化がどう動くかは、`OFFLINE.md`に記載されています。

- **「原本を復元」――枠で囲むと、削除しすぎた分が戻ってきます。** 修正
  ウィンドウに新しいツールが加わりました。該当箇所に枠をドラッグすると、
  テキストは原本にあったとおりの姿で再び現れます。

  これは、ヒットパネルが残していた隙間を埋めるものです。ヒットパネルでは、
  そのプレースホルダーが一意である場合にしか置換を取り消せませんでした
  ――つまり匿名化の場合(この種のあらゆる項目に「[名前]」が使われる場合)
  や、そもそもプレースホルダーが残らない黒塗り箇所ではできませんでした。
  まさにそこに誤操作が集中します。「利用者」「在庫番号」「署名」は、
  氏名と誤認されがちです。

  枠はプレースホルダーを必要としません。**位置**は矩形から、**内容**は
  元ファイル――前後比較の切り替えが表示するのと同じファイル――から得られ
  ます。匿名化か仮名化かは、これによってもう関係なくなります。

  復元されたテキストは赤ではなく黒で表示されます。それは再び平文であり、
  プレースホルダーではないからです。ヒット一覧から項目が消えるのは、
  そのプレースホルダーが文書内の**どこにも**なくなったときだけです――
  同じ値が複数箇所で置換されていた場合、残りの箇所についてはそのまま
  残ります。

  画像に変換されたページでは、このツールは処理を断り、その理由を説明
  します。復元されたテキストはページ画像の下に隠れてしまい、見えなく
  なってしまうためです。

### 修正

- **「詳細」と「指標」を折りたたむと、画像の残りが画面に残ることがあり
  ました。** 折りたたむと、内容の一部がウィンドウの下端の下に押し込まれ、
  何か他のものがその上に描画されるまで、背景の上にそのまま残っていました。

  両方の領域には最小の高さが設定されており、開いた状態で扱いやすい大きさに
  なるようにしています。しかし折りたたみの動きは最大高さだけを下げており
  ――ある領域はその最小高さより小さくは縮みません。そのため内容は200
  ポイントの高さのまま残る一方、ウィンドウはすでに24まで縮んでおり、その
  差が下端の下に隠れていました。現在は、動きの間だけ最小高さが一時的に
  緩められ、その後元に戻ります。

- **開閉を繰り返すたびに、ウィンドウがだんだん小さくなっていきました。**
  開くとき、ウィンドウは画面高さの最大92%まで大きくなります。空きが
  少ない場合は、必要な分より少なく大きくなります。閉じるときは、それでも
  常に全量が差し引かれていました。現在は、開くのにかかった分だけが正確に
  返却されます。

- **黒塗りされた項目の一部が、見える状態で残ることがありました。** ある
  履歴書では、「*30.12.1991」のうち「*30.1」という文字――つまり生年月日の
  日と月の始まりが結果の中で読める状態のまま残っていました。プログラムは
  この残りに実際に気づいており、そのためにページを画像に変換していました
  が、まさにそれが事態を悪化させていました。残りは検索可能ではなくなった
  ものの、依然として読める状態のままであり、しかももう修正できなくなって
  いたのです。

  原因は2つの検査の間にありました。より厳密な方の検査は、削除された
  項目の領域内にそこにあってはならない何かが残っていないかを調べ、その
  結果を文字数の集合として報告します。置換に伴って読み取り順序がずれる
  ためです。変換前にこうした箇所を上塗りする回避処理は、この文字数の
  集合をページ上のテキストとして検索していました――そして決して見つかり
  ませんでした。そのため何も上塗りされなかったのです。該当箇所はずっと
  分かっていたので、現在は新たに検索する代わりに、そのまま引き継がれ
  ます。

  この検査だけによって残りが発見されるすべてのページが影響を受けて
  いました――ファイル形式や言語には関係ありません。

- **横向きに給紙されたスキャンでは、文字認識が何も見つけませんでした。**
  用紙を給紙口に横向きに入れると、文字が90度回転した状態のファイルに
  なります。これまでMaskuroは、その中の項目を**1件も**読み取れませんでした
  ――そしてその後のファイルは目立たない見た目になっていました。何も
  見つからなかったので何も通知されず、住所はそのまま画像の中に読める
  状態で残っていました。現在は文字認識自体がページの向きを直します。
  検査画像では再びすべての項目が対象になります。

  2つの限界を率直に述べておきます。**逆さま**(180度)の用紙は引き続き
  読み取れません。また非常に画質の悪いスキャンでは、向きを直しても
  役に立ちません――そこでは向きを判定すること自体ができないほど読み取れる
  情報が少ないのです。この処理により、画像ごとにおよそ5分の1長く時間が
  かかります。

### 変更

- **「自動でインストールする」は、現在その内容どおりの動作をします。**
  設定のチェックは、実際に果たす内容以上のことを約束していました。新しい
  フッションを自動的にダウンロードしてインストールを開始しますが、その
  インストールは**目に見える形**で進み、確認を求めます。Windowsでは
  ユーザーアカウント制御の確認も伴います。「自動」という言葉を読んだ人は、
  一晩のうちにパソコンが自動的に更新されると考え、朝になるとインストール
  ウィザードの前に立たされていました。このチェックは現在「更新を自動で
  ダウンロードし、インストールを開始する」という名称になり、その下に意味を
  説明する一文が加わりました。動作自体に変わりはありません――Maskuroが
  気づかれずに入れ替わることがないのは意図的なものであり、これからも
  変わりません。

## 0.10.27-alpha.20260817 – 2026年8月17日

### 新機能

- **新機能:法律事務所ソフトとの連携用`--ersetzen`。** 結果が元ファイルの
  隣に生成されるのではなく、その場所に取って代わります。これにより、
  法律事務所ソフトの「チェックアウト/チェックイン」(電子ファイルの
  「開いて編集」)が、専用のインターフェースなしで機能します。ソフト
  ウェアがファイルを渡し、同じ場所で浄化されたものを受け取ります。

  **このスイッチは第一の原則を覆すものです。** そのためコマンドラインに
  のみ存在し――ウィンドウにはありません――しかも管理者がそれを許可した
  場合(設定ファイルの`ersetzen`項目)にのみ使えます。許可がない場合、
  呼び出しは中断され理由が表示されます。黙って2つ目のファイルを作成
  するのは、より悪い誤りです。それでは変更されていないファイルが再度
  チェックインされてしまうからです。

  最初に書き込まれるのは隣接ファイルです。それが完成して初めて、元の
  場所に取って代わります。中断やエラーが発生した場合、元ファイルは
  **バイト単位で変更されず**、断片も残りません。検査記録には、この
  置換が独自の項目として記録されます――検査担当者は、浄化前のフッション
  がもうここにはないことを知っておく必要があります。

- **マニュアルが、初回起動時のWindows警告について説明するように
  なりました。** 新しい最初の節「初回起動時にWindowsが警告する場合―
  どうすればよいか」には、2枚の画像と3つの手順があります。「詳細情報」
  は小さなリンクであってボタンではありません――まさにそこで多くの人が
  つまずきます――その後「実行」です。

  「発行元不明」と表示されるのは、その警告の内容そのものです。パッケージ
  は現在、証明書なしで配布されています。私たちは、それを隠すよりも
  説明する方が正しいと考えています。

- **復路が、テキストと対応関係が一致しない場合に気づくようになりました。**
  別の案件の回答を貼り付けた人は、これまで正しいテキストの中に見知らぬ
  氏名を受け取っていました――エラーも通知もなく、ただ間違っているだけ
  でした。Maskuroは現在、直前の処理が実際に生成したプレースホルダーを
  記憶し、それに属さないものはすべて通知します。直前の処理由来のもの
  がまったくない場合、何も挿入されず、ウィンドウがその理由を伝えます
  ――これまでのように期限切れと推測することはありません。

  **限界は依然として存在し、それはマニュアルにも記載されています。**
  プレースホルダーは処理ごとに通し番号が振られるため、最初の氏名は
  どの文書でも`[NAME_1]`となります。見知らぬテキストがこのような
  プレースホルダーしか含んでいない場合、取り違えは検出できません。

- **PDFが白黒で出力できるようになりました。** 動作モードのチェックが、
  各ページを白黒画像に変換します――その下には見えないテキストレイヤーが
  あり、引き続き読み取り可能・検索可能です。厳格なサイズ制限のある
  beA等での送信向けです。測定コーパス全体で平均**68%の縮小**
  (コマンドライン:`--monochrom`)。

  **どれだけ効果があるかは文書次第です**――それもチェックの隣に記載
  されています。スキャンや画像を多く含む文書は大きく縮小しますが、
  埋め込みフォントのないシンプルなテキスト文書は、かえって大きくなる
  こともあります。まとめて有効にする前に、1つのファイルで試してみて
  ください。

  代償:各ページが再計算されます――1000ページでは数分かかります。また
  図版は白と黒の間のすべての階調を失います。テキストにとってはどうでも
  よいことですが、写真にとってはそうではありません。

- **修正ウィンドウのヒット一覧が、現在は件数を表示します。** 一覧の上に
  「5件のヒット」と表示され、絞り込むとすぐに「5件中1件」となります。
  これは「絞り込みました」と「5件あって、私はすべて見ました」の違いです
  ――氏名が本当にすべての箇所で置換されたかを確認する際の作業です。

- **検査記録が、現在は検索・絞り込みできるようになりました。**
  「ファイル→検査記録」の下の表示は、これまで表だけで、それ以外は何も
  ありませんでした――1か月に3000回の処理があると、多くのことが起きた
  ことは分かっても、何が起きたかは分かりませんでした。

  新たに**検索欄**、**3つのフィルター**(処理方式、結果、種類)、
  **ページ送り**が追加され、さらに以前はなかった3つの列――**処理方式**
  (黒塗りまたは置換)、**確信度**、**所要時間**が加わりました。一覧の
  上には、現在何件表示されていて、フィルターが何件を非表示にしている
  かが表示されます。

  「CSVとして保存…」は、現在**表示されているとおりのもの**を出力します
  ――絞り込みを行った人は、絞り込まれた結果を受け取り、通知にはその
  件数が表示されます。

  確信度や所要時間欄のハイフンは、その行について何も測定されなかった
  ことを意味します――例えばこの機能より古い記録である場合などです。
  これらの値は事後的に計算されることは**ありません**。利用者による
  フィルターは引き続きありませんが、個々の行は検索で見つけられます。

### 削除

- **「このプログラムについて」ウィンドウの透明性に関する案内が、再び
  なくなりました。** これは0.10.22-beta.1以降そこにあり、このアプリ
  ケーションが人工知能の支援を受けて開発された旨を伝えていました。
  これはどこにも要求されておらず、よりによってプライバシー保護用の
  アプリケーションで、それを動作方式についての言明――つまり文書がネット
  上のサービスに送られているかのように――と読む人もいました。浄化は
  引き続き自分のパソコン上でのみ行われます。その説明は、それがある
  べき場所――「プライバシー」タブにあります。

### 修正

- **プログラムが、自身のアイコンをより粗悪なものに置き換えてしまって
  いました。** プログラムからコンテキストメニューを登録した人は、
  その後インストール直後とは異なる盾がタスクバーに表示されていました
  ――似てはいるものの、バーが中央ではなく左寄せで、目に見えて粗い
  仕上がりでした。原因は代替処理にありました。プログラムがアイコン
  テンプレートを見つけられない場合、自分でアイコンを描画します。これは
  アイコンが**まったく**存在しない場合を想定したものでしたが、実際には
  同梱のものがすでに存在する場合にも描画し、それを上書きしていました。
  セットアップからインストールされたフッションにはテンプレートが
  存在しないため、そこでは全員が影響を受けていました。現在、既存の
  アイコンは変更されません。

  **すでに影響を受けたインストールでは、正しいアイコンは自動的には
  戻りません**――その場合は一度再インストールしてください。

- **「Objektkennung: OB-4711-22」がログイン名として扱われていました。**
  ユーザー名の検出器は、その見出しをその前の語境界を確認せずに検証して
  おり――そのため、そのいずれかで終わる**あらゆる**単語を捕捉していま
  した。Objektkennung、Fahrzeugkennung、Gerätekennungなどです。その
  後ろの値は、ログイン名とは何の関係もないにもかかわらず削除されて
  いました。

  本当に意図された複合語――「Benutzerkennung」「Anmeldekennung」――は
  一覧に個別に登録されており、引き続き検出されます。

- **英語、ギリシャ語、日本語、韓国語では、16個のプレースホルダーが
  結果にドイツ語のまま表示されていました。** これら4言語のいずれかに
  画面表示を設定していた人は、新しいデータ種別についてドイツ語の
  見出しが文書に書き込まれていました――パスワードは
  `[CREDENTIALS_1]`ではなく`[ZUGANGSDATEN_1]`に、診断コードは
  `[DIAGNOSIS_CODE_1]`ではなく`[DIAGNOSESCHLUESSEL_1]`になっていまし
  た。影響を受けたのは、健康、診断、投薬、診断・医薬品コード、宗教、
  労働組合、政治的意見、刑事、認証情報、ユーザー名、カード情報、座標、
  職業、金額、特徴です。

  残り44言語ではこの不具合は一度もありませんでした。これらは見出しを
  言語ファイルから取得しており、そこにはこの種別が最初から存在して
  いたためです。まさにこの4言語だけが、別の理由で独自の表を持って
  います――その文字がPDFの文字セットで生き残らないため、そこにはラテン
  文字の見出しが使われています――そして、その表には単に新しい種別が
  欠けていたのです。

  これはカタログページの翻訳作業中に発覚しました。ウェブサイトは英語の
  読者に、プログラムが書き込まない見出しを約束していたのです。検査
  項目は現在、この4つの表を、発生し得るすべての見出しの一覧と照合し
  ます。

- **ルールウィンドウが、内容に対して小さすぎて開かなくなりました。**
  「独自の検索パターン」タブでは、アシスタントの説明行(「検索対象:
  …」)が「テスト文」欄の半分に隠れていました――よりによって、正規
  表現の知識なしに自分のルールが正しく検索しているかを確認する文です。
  このウィンドウには、タブの少なかった時代の固定の最小サイズがあり、
  そのため、収まる分だけの大きさにドラッグできてしまっていました。
  現在は内容に応じたサイズになり、すべてが読める範囲でのみ小さく開き
  ます。

- **表計算の数式内の氏名が、もう残らなくなりました。** セルにはテキスト
  の格納場所が複数あり、これまではそのうち1つしか処理されていません
  でした。数式内に氏名があった場合(`="Frau "&"Sieglinde Ortner"`)、
  または数式の最後に計算された結果だった場合、隣のセルでは同じ人物が
  置換されているにもかかわらず、ブック内には変更されないまま残って
  いました。そのセルをクリックすると、数式バーでそれが読めました。

  現在は両方とも置換されます。処理対象になるのは引用符の中にあるもの
  だけです。セル参照、関数名、シート名は影響を受けません。
  `=SUMME(K2:K6)`は引き続き計算されます。同じ氏名はどこでも同じ
  プレースホルダーになるため、`=SUMMEWENN(A:A;"Huber";B:B)`も引き
  続きその行を見つけます。

- **図表が、もう氏名を表示しなくなりました。** 図表は軸見出しの独自の
  コピーを保持しています――元のセルがとっくに空になっていても、それは
  描画され続けます。そのため、上の表はきれいであっても、バーの下には
  5人分の氏名が残ったままでした。表計算**と**プレゼンテーションの
  両方に適用されます。

- **固定テキストを持つ名前付き範囲が処理されるようになりました。**
  名前付き範囲は、セル参照の代わりに固定テキストを含むことができます。
  そこに氏名がある場合、これまでは残ったままでした。範囲の**名前**
  自体は引き続き残ります――数式がそれを参照しており、名前を変更すると
  参照エラーになるためです。シート名と同様、これは通知されるだけで
  置換はされません。

- **一度検出された生年月日は、文書全体から消えます。** 日付単独では
  何も語りません――フィールド語があって初めて生年月日になります。
  だからこそ請求書の日付はそのままです。しかし同じ項目が同じ文書内に、
  そのフィールド語なしで2回目に登場した場合(画像タイトル内、記入済み
  のフォームフィールド内など)、数行上で「geboren am …」が確実に
  検出されているにもかかわらず、そのまま残っていました。連動する
  のは、**この**文書内ですでに生年月日として検出されたものだけです。
  引き続き推測は行われません。

- **ウェブページ内の構造化データが、その生年月日を漏らしています。**
  検索エンジン向けのJSON-LDブロックでは、日付は`birthDate`という
  キーの下にあります――このキーは、通常は列見出しが伝えるのと同じ
  情報を伝えています。現在はこれも一緒に読み取られます。「Birthday」
  と「Birthdate」も、フォーム内のフィールド名として扱われるように
  なりました。

- **生年月日と社員番号は、表内でも検出されるようになりました。** セル
  には値そのものだけがあります――`14.03.1988`のようにです。それが何を
  意味するかは列見出しだけが語り、その見出しは何行も上にあります。
  Excelではこれはすでに読み取られていましたが、LibreOfficeの表計算や
  CSVファイルではそうではなく、そこでは生年月日がそのまま残っていま
  した。

  両方とも現在は見出しも読み取ります――**ただし、それ自体がフィールド
  名である場合に限ります**。「Geburtsdatum(生年月日)」の下では日付は
  対象になりますが、「Rechnungsdatum(請求日)」や「Lieferdatum(納品
  日)」の下ではそうなりません。これは意図的に慎重な解釈です。「Name」
  のような見出しが、人物がまったく登場しない任意の注記の上にあれば、
  そこにもプレースホルダーが置かれてしまっていたでしょう。

### 修正

- **浄化されたCSVは、表のままであり続けます。** 検出処理はCSV行を文と
  して読み取るため、その発見箇所がセミコロンをまたいでしまうことも
  ありました。プレースホルダーが区切り文字を飲み込んでしまい、その
  結果その行は列が1つ減り、ファイルは表として開けなくなっていました。
  発見箇所は現在、セルの境界で終わります。マスキング用の引用符は
  そのまま残ります。影響を受けたセルは、その後もう一度単独で読み
  取られます――そうしないと、長すぎたヒットに隠れた隣のセルが浄化
  されないまま残ってしまいます。

- **プレゼンテーション内のコメント。** スライドの余白にある注記――
  しばしば「開催前にMs. …に電話してください」と書かれているまさに
  その箇所――が、それを書いた人の氏名とともに変更されないままでした。
  Excelではすでに両方とも処理されていましたが、PowerPointはコメント
  テキストと作成者を別の方法で格納しており、それが見落とされていま
  した。両方の形式に該当します。古い形式と、PowerPointが2019年以降
  書き込む形式です――後者では作成者に紐づく業務用メールアドレスも
  対象です。PowerPointが吹き出しに表示する頭文字も一緒に削除され
  ます。

- **LibreOfficeファイル:数式、ユーザーフィールド、注記の作成者。**
  Excelではすでに処理されていた内容が、ODSの表計算ではそのまま残って
  いました――そこでは数式が独立した要素ではなくセルのプロパティとして
  格納されており、その中の氏名は生き残っていました。次に開いたとき、
  LibreOfficeがそれを再計算してしまっていました。

  さらに3箇所です。**ユーザーフィールド**の値は、OpenDocumentでは
  宣言部分の上部に一度存在し、本文ではそれが呼び出されているだけです
  ――これまでは呼び出し側だけが置換されており、開くと古い値が戻って
  いました。**注記の作成者**と、追跡された変更の作成者もそのまま
  残っていました。そして**表計算**では、変更履歴の追跡がまったく
  処理されていませんでした――テキスト文書とは異なり――そのため、削除
  されたセルの内容が編集者名とともに保持されていました。セル参照や
  合計の数式はこれによる影響を受けません。

- **保存されたウェブページが、その属性を漏らしています。** ページは
  含まれているものすべてを表示するわけではありません。記入済みの
  フォームフィールドは入力内容を`value`に持ち、JavaScriptのUIは
  データを`data-…`に格納し、検索エンジン向けのブロック(JSON-LD)は
  それを完全かつ整形された形で繰り返します。氏名、生年月日、住所、
  電話番号などです。目に見えるテキストは浄化されていても、これらは
  すべてそのまま残っていました。

  現在はこうした箇所も処理されます。加えて`aria-…`(スクリーン
  リーダーに読み上げられる内容)、`placeholder`、`summary`、そして
  リンクの推奨ファイル名も対象です。JSON-LDブロックはデータとして
  読み取られ、有効なまま保たれます――そのキーと語彙はそのまま残り、
  値だけがなくなります。通常のJavaScriptは引き続き変更されません。

- **画像は、EXIFがなくても付随データを失うようになりました。** 写真は
  撮影者名、撮影時刻、撮影場所のGPS座標を、脇に書き込まれた形で持ち
  歩いています――不動産の広告では、本文に何も書かれていなくても、
  これが住所を漏らします。これはEXIFに含まれる限り削除されていました。
  しかし、その情報が**XMPとしてのみ**格納されている場合(Lightroomや
  Photoshopの保存方式です)や、PNG内のテキストブロック(`Author`、
  `Comment`)として格納されている場合、画像はまったく変更されないまま
  でした。現在はどちらも検出・削除されます――文書内に含まれ、その中に
  保持される画像でも同様です。向き情報は引き続き維持され、付随データの
  ない画像は不必要に再保存されません。

- **表計算、プレゼンテーション、Word文書のリンク先。** リンクがどこに
  つながるかは、テキストではなくファイルの独自の格納領域に記載されて
  います。「メールを書く」の後ろにあるメールアドレスは、そのため
  浄化を無傷で乗り切っていました――同じアドレスが本文では置換されて
  いたにもかかわらずです。`mailto:`と`tel:`は現在、保存されたウェブ
  ページと同様に処理されます。

### 新機能

- **診療情報提供書が、もう破損した状態で戻ってくることはありません。**
  これまで氏名検出は医薬品名を人名と誤認していました――「Metoprolol-
  succinat」から`[NAME]`が、「Ramipril」から`[ORT]`が生成されて
  いました。投薬計画はその結果使い物にならなくなる一方、診断名は
  そのまま残っていました――つまりまったく逆でした。測定では**有効成分
  の63%**、**臨床専門用語の53%**がこれに該当し、ドイツ語だけでは
  ありませんでした。7言語にわたって74%、イタリア語では検査対象すべて
  でした。

  Maskuroは現在、医療用語を認識し、それをそのままにします。残る割合は
  43%ではなく6%(ドイツ語)、74%ではなく1%(全言語)です。敬称が
  前にある場合(「Sehr geehrte Frau …」)、氏名は氏名のままです――
  たとえたまたま医薬品名のように聞こえても同様です。

- **疾患名と薬剤名を、望めば削除できます。** 設定に新しいチェック:
  「疾患名と薬剤名も削除する」(コマンドライン:`--mit-diagnosen`)。
  人事ファイル、解雇通知、鑑定書など、診断内容が誰にも関係ない場合の
  ためです。

  **初期設定はオフです。**これは意図的です。診療情報提供書は、診断名と
  有効成分から*成り立っている*文書です。それを匿名化する人――研究の
  ため、研修のため、AIツールのため――は、たいていまさにその内容は
  保持したいと考えており、誰についてのものかだけを取り除きたいのです。
  そこでは診断名がペイロードであり、識別情報ではありません。

  検出処理は一般的な名称を見つけますが、目視確認の代わりにはなりま
  せん。疾患一覧は決して完全にはなりません。医師が「C2-Abusus」と
  書く場合、分類上は「アルコールによる障害」となるためです。

- **診断・医薬品コードが検出されます。** ICD-10(`I48.2`)、ATC
  (`A10BA02`)、および薬品中央番号は、書き出された診断名と同様の
  健康データです――退院時サマリーや会計書類では、むしろこちらの方が
  よく見られる形式です。これらは初期設定でオンです。他の特別カテゴリー
  (GDPR第9条)と同様です。

  診断コードは、裏付けがある場合にのみ検出されます。前に「ICD」が
  ある、または診断行の後ろに括弧書きである、という条件です。この
  条件がなければ、プログラムはファンクションキー**F10**を依存症の
  診断だと判断してしまいます――分類上、F10はまさにそれを意味する
  ためです。

- **完成したファイルを、現在はコピーできます。** 完了した各行には
  「表示」「修正」「フォルダで表示」の隣に4つ目のボタンが加わりました。
  **コピー**です。浄化されたファイルをクリップボードに置きます――そこ
  からCtrl+V(Mac:⌘V)で、フォルダを経由せずにメール、チャット
  ウィンドウ、AIツールに貼り付けられます。

  コピーされるのは**ファイル**であり、そのテキストではありません。
  ページレイアウト、画像、黒塗りバーはそのまま保持されます。一覧の
  コンテキストメニューからは、複数選択された結果も一度にクリップ
  ボードへ送れます。「ファイル」メニューには、キーボード操作を好む方
  向けに、同じ機能が**「結果をコピー」**として用意されています。

- **国の選択が、現在は文書に追従できます。** 身分証明書番号、社会保険
  番号、税番号は国によって異なりますが、どの国を検査するかはこれまで
  セッション全体を通じて固定されており――表示言語から導かれていまし
  た。ドイツ語で作業しながらフランス語の文書を浄化する人は、その中を
  ドイツ語の税IDで検索し、フランスの社会保険番号は検索していません
  でした。

  ルールウィンドウには、現在**「文書の言語に応じて自動」**という項目が
  あります。固定選択は引き続きその隣に存在します――これは意図的です。
  言語検出は完璧ではなく、誤検出すれば誤った国選択が適用されてしまい
  ます。1つの国の書類しか扱わない人は、固定一覧の方が安全です。

  これに影響されないのは**ドイツの**パターン(税ID、車両番号、内線
  番号)です。これらは国選択ではなく言語に紐づいており、短いドイツ語
  テキストが英語と判定された場合でも引き続き検出されます。

- **パスワード、鍵、ログイン名が検出されるようになりました。** エラー
  メッセージ、ログ、設定ファイルの抜粋をAIウィンドウに貼り付ける人は、
  ほぼ必ずアクセスキーを含んでいます――これまではそのまま残っていま
  した。

  両方が検出されます。それ自体で判別可能な一般的な鍵の形式
  (`sk-…`、`ghp_…`、`AKIA…`、`AIza…`、`xoxb-…`、JSON Web Token、
  秘密鍵のヘッダー)と、見出しの付いた形式――「Passwort:」
  「API-Key =」「Token:」「Benutzername:」です。置換されるのは値だけ
  で、見出しは決して置換されません。「Passwort: [ZUGANGSDATEN_1]」は
  読める状態のまま残り、結果を確認する人には、そこにパスワードが
  あったことが分かります。

  ログイン名とパスワードは2つの独立した種類です。パスワードだけを
  削除したい人は、片方をオフにしてもう片方を残せます。

- **画像内のバーコード・QRコードが判別不能になります。** スキャン
  された通知書には、ほぼ常にコードが貼られており、そこには事件番号
  が入っています――隣のテキストで削除されるのと同じ番号です。これ
  まで、機械可読の形式はそのまま残っていました。番号の上のバーは、
  2センチ離れた場所で機器が1秒でそれを読み取ってしまえば意味が
  ありません。

  検出されるのはQRコード、Data Matrix、Aztec、Code 128、EAN、その他
  一般的な形式です。判別不能にするとはモザイク処理を意味し、顔より
  粗くします。コードの誤り訂正機能は、わずかに残った部分から驚くほど
  多くを復元できるため、中途半端なぼかしは削除とは言えません。

  このオプションは「顔を判別不能にする」の隣にあり、同様に**初期設定
  でオン**です。オプションがオフの場合でも、報告書には何枚の画像に
  コードが含まれているかが表示されます――顔はページをめくれば見え
  ますが、コードは付随物だと思われがちだからです。

- **カード確認番号、PIN、有効期限が検出されるようになりました。**
  クレジットカード番号はすでにプログラムが検出していました。しかし、
  隣の3つの情報があって初めて使用可能になり、どの明細書にもそれらは
  並んで記載されています。3つとも、その見出しの後ろにある場合のみ
  対象です――「123」単独では番地、ページ番号、個数かもしれません。

- **本文中の座標が検出されるようになりました。** 画像からは、Maskuro
  はすでに撮影場所を削除していました。しかし同じ情報が鑑定書や出動
  報告書内にテキストとして記載されていると、そのまま残っていました。
  検出されるのは10進度表記と度分秒表記です。10進度表記では、近くに
  「Standort」「Fundort」「Koordinaten」のような語がある必要があります
  ――そうでなければ、小数点以下2桁の測定値はすべて位置情報になって
  しまいます。

- **金額も、現在は一緒に削除できます。** 新しいチェック「金額も削除
  する」――上の日付表示と同様、初期設定は**オフ**です。契約書では
  金額こそが内容であり、すべてを黒塗りしても誰も守られません。一方、
  給与明細、和解案、口座明細では、金額はまさに、隣の氏名以上にその
  人物について語る情報です――それは、その書類を目の前にしている人
  にしか分かりません。

  金額は**通貨表示を伴う場合のみ**検出されます。「4.250,00」単独では
  個数であり、「4.250,00 EUR」になって初めて金額です。通貨記号、
  略号、正式名称は、前でも後ろでもカウントされ、「990,– CHF」という
  表記も含まれます。

- **GDPR第9条の特別カテゴリーが認識されます。** 宗教、労働組合所属、
  政治的信条、健康情報――そして第10条による刑事関連情報です。これは
  規則が原則として処理を**禁止**しているデータであり、そのため唯一
  初期設定で**オン**の新しいグループです。保持したい場合は、その旨を
  自分で決定します。

  検出されるのは、実務で使われる形式です。人事書式のフォーム項目
  ――「Religionsbekenntnis: röm.-kath.」「Gewerkschaft: ÖGB」
  「Grad der Behinderung: 50」「Vorstrafen: keine」――で、コロンが
  隣にある場合も、記入済みの用紙が提供するような見出しが上にある場合
  も両方対応します。

  **地の文はAI段階の担当です。**「彼は長年、労働組合で活動している」も
  同じ情報ですが、これを確実に見つけられる検索パターンはありません。
  AI段階はこのフッションから、これらのカテゴリーも明示的に検索し
  ます。地の文が必要な人は、これを有効にしてください。

- **人物属性と職業――氏名がなくても誰のことか分かる情報です。** 性別、
  婚姻状況、身長、目や髪の色は、このフッションから削除されます。
  職業、役職、部署は、望めば独自のチェック(「職業と部署も削除する」)
  または`--mit-berufen`で削除できます。

  **一方がオン、もう一方がオフの理由:**「調達部門の部長」は、ある企業
  において、たとえ隣の氏名が黒塗りされていても、まさに1人の人物を
  指し示します――鑑定書や解雇通知ではこれを削除すべきです。しかし
  従業員一覧は職業名から*成り立って*います。これを初期設定で削除
  すれば、空の用紙を返すことになってしまいます。どちらのケースか
  分かるのは、その書類を目の前にしている人だけです。上記の属性は
  ほぼフォームフィールドにしか現れず、まれで、内容そのものになる
  ことは決してありません――つまりコストはかかりません。

- **見知らぬファイルの事後確認。**「ファイル→ファイルを確認…」は、
  すでに黒塗りされた文書を再読み込みし、まだ残っているものを――
  **どこに**あるかも含めて――報告します。ページと行、種類と長さです。
  誰かが他人の作業を確認する場合のためです。事務所からの案件書類、
  官庁からの回答、送信前の自分の発送物などです。

  **値自体は報告書には記載されません。** その箇所を開けばどのみち
  見えるので、報告書自体は個人データの集積とならずに保存・転送でき
  ます。

  **そして報告書は、確認できなかったことを常に伝えます。** 画像は
  読み取られません。テキストレイヤーのないスキャンでは、「発見箇所
  なし」は*未検査*を意味し、*問題なし*を意味しません。コマンドライン
  では、戻り値がこれを区別します。`--nachpruefen`は検査済みかつ問題
  なしで0を、発見箇所ありで4を、検査不能で5を返します。これにより、
  送信物を自動的に許可する代わりに、自動的に差し止めることができ
  ます。

- **検査報告書:浄化ごとに1枚。**「ファイル→検査報告書を保存…」――
  またはコマンドラインの`--pruefbericht <フォルダ>`――は、処理内容、
  発見された種類とその件数、2つの指標、検査記録を含む1ページのPDF
  (CSVまたはテキストも選択可)を書き出します。ファイル保管や監督機関
  向けです。検査記録は確実な証拠ですが、JSON Linesファイルを提出する
  人はいません。

  **新たに2つの数値**が加わり、これまではどこにも表示されていません
  でした。*平均確信度*――検出処理が発見したものについてどれだけ確信
  していたか――と*マスキング率*――テキストに対する置換文字の割合です。
  どちらもその限界とともに表示されます。確信度は見落としについては
  **何も**語らず、その隣には常に、それが何件のヒットについての
  ものかが表示されます。マスキング率は画像をカウントに含まないため、
  画像を多く含む文書では高すぎる値になります。

  **発見された値は用紙には記載されません**――記録や検索実行と同じ
  制限です。下部には2つの、それぞれ異なる内容の行があります。
  チェックサムは、この用紙が変更されていないことを示します。記録行
  ――記録が有効な場合のみ――は、その処理を証明する**署名済みの**行
  を指し示します。出所を証明できるのはこの行だけです。

- **「どれだけ確実だったか?」――結果に付随する指標。** 結果の下にある
  「指標」ボタンは、これまでどこにも表示されていなかったものを開き
  ます。発見箇所、単語数と文字数、種類別の分布(棒グラフ)、そして
  平均確信度とマスキング率です。検査報告書と同じ数値ですが、印刷
  なしで即座に確認できます。

  **同じ領域にその留保も表示されます。** 確信度の隣には、それが何件の
  ヒットについてのものかが表示され、その下には、それが見落としに
  ついて**何も**語らないという文が表示されます。この文のないパーセ
  ント表示は、まるで検出率のように読めてしまい、そう理解した人は、
  その数値がない方がまだましです。

  計算は展開時にのみ行われます。マスキング率の分母は、ファイルごとに
  1回の読み取りが必要であり、その数値をまったく見ない人にそのコスト
  を払わせるべきではないためです。

- **独自の検索パターンを、書かずに作れます。**「独自の検索パターン」
  タブは現在、3つの手順で案内します:*何を検索しますか?→あなたの
  組織でこの項目はどのように書かれますか?→名前を付けて保存。*
  例えば`KD-004711`のような例を入力すると、プログラムがそこから
  ルールを導き出し、それが何を検索するかを言葉で表示します。プレビュー
  はキー入力のたびにヒット数を確認します。

  **正規表現は一切登場しません。** できることが問題だったことは一度も
  ありません――独自の検索パターンは以前から存在していましたが、
  `\bKD-\d{6}\b`のような表現を要求しており、法律事務所や人事部で
  それを書ける人はいません。書き*たい*人は、上級者モードを開けます。

  **テンプレートカタログは新しく整理されました。**名前・説明・サンプル
  値を持つ13枚のカードで、カテゴリータグ――財務、行政、連絡先、人事、
  医療――で絞り込めます。

  そして、導出されたパターンが対象を広げすぎている場合、プログラムは
  自らそれを伝えます。数字だけから成る例はすべての年号やすべての
  金額に一致してしまい、その表現を読めない人は、それに気づけない
  可能性があります。

- **56個のチェックの代わりに7つのタグ。** 新しいタブ「何が検出されるか」
  は、検出可能なすべての種類を7つのグループにまとめます――人物、
  連絡先と地名、識別番号、財務、技術、特別カテゴリー、企業と独自です。
  1つのタグでそのグループを切り替え、「すべてオン」「すべてオフ」で
  一覧全体を切り替えます。その下には引き続き、種類ごとの個別チェック
  があります。

  **初期設定はすべてオンで、それは変わりません。**ここでオフにした
  ものは、そもそも検索対象になりません――ルールウィンドウが許す最も
  大きな操作であり、あらゆる文書に影響します。そのため、一覧の下には
  常に、何種類がオフになっているかが表示され、保存されるのはオフに
  されたものだけです。新しい種類は、おとといのルールファイルでも
  自動的にオンのままとなり、黙って抜け落ちることはありません。

- **枠をすべてのページに反映。** 修正ウィンドウでは、**すべてのページに
  反映**ボタンが、最後にドラッグした枠を取り、以降のすべてのページの
  同じ箇所を黒塗りします――レターヘッド、フッター、事件番号欄のため
  です。80ページのスキャンされたファイルでは、これにより20分が2分に
  なります。

  **「同じ箇所」とは、用紙上の同じ*相対*位置を意味します。** 給紙口
  からのファイルの束には、定期的に横向きの用紙、A3の用紙、回転した
  用紙が混ざります。絶対座標で反映される矩形は、そこではレターヘッド
  の隣に着地してしまい――バーが見えるだけで、処理が完了したと思い
  込んでしまいます。

  **黒塗りが行われ、置換は行われません。**元の枠がプレースホルダー
  だった場合でも同様です。同じ矩形の下には、40ページ目と1ページ目
  では異なる内容があり、同じ番号のプレースホルダーは、実際には存在
  しない同一性を主張してしまうことになるためです。

- **黒塗りバーへの注記。** 情報公開制度では、それぞれの黒塗りの隣に、
  なぜ黒塗りされたのかが記載されます。設定の新しい項目**バーへの注記**
  ――または`--balkenvermerk`――は、各バーに短いテキストを書き込み
  ます。「§ 203 StGB」「DSGVO」「機密」などです。官公庁が発行する
  文書にとって、これは違いを生みます。受け取った側は、決して手に
  入らない記録を持たなくても、その理由を見ることができます。

  **初期設定では空です。**この注記は発行される文書内で目に見えて
  おり、それ自体が1つの情報だからです――受取人に、何の名目で何かが
  差し止められているかを伝えます。これは**黒塗り**の場合にのみ機能
  します。プレースホルダーが表示されている箇所にはバーがありません。
  読める大きさに収まらないほど小さいバーでは省略されます――読めない
  注記は不具合のように見えてしまいます。

- **インターネット接続なしでの有効化――現在は完全に機能します。**
  ライセンスウィンドウには以前から「インターネットなしで有効化」が
  ありました。上部に持ち出し用の要求コード、下部に戻ってくる有効化
  情報のための欄です。ただし、これまでは**誰もそれを発行できません
  でした**――そのための道具がなく、コードは行き場を失っていました。
  これが修正されました。

  隔離されたパソコンを持つ官公庁や法律事務所にとって、これは特殊な
  ケースではなく通常のケースです――そして、まさに「あなたの文書は
  パソコンを一切離れません」という約束が最も重い意味を持つ対象層
  です。このコードは文書について何も明かしません。ライセンス識別子と
  そのパソコンのハッシュ値のみを含み、それ以外は含まれません。

- **スキャナーから取得。**「ファイル→スキャナーから取得…」は、原稿の
  束を直接読み込み、そのページを一覧に追加します――郵便室にとって、
  これは2つの作業手順を1つにするかどうかの違いです。給紙は最後の
  ページまで空にされます。機器、解像度、色は、お使いのスキャナーの
  システムダイアログ――すでにご存じのはずです――で選択します。

  **自動では浄化されません。** まず何が入ってきたかを確認し、その後
  他のファイルと同様に「浄化」を押します。スキャンがすぐに処理されて
  しまうと、斜めに給紙された束を確認する機会が失われてしまいます。

  **これはWindowsのみで提供されます。** メニュー項目もMacではその旨を
  伝えます。そこではスキャナーのソフトウェアがフォルダに書き込み、
  「フォルダを監視する…」がそこに届いたものすべてを浄化します。

### その他

- **発見されるすべての項目の一覧が、現在同梱されており**、ソースコード
  から生成されます(`hilfe/GEFUNDENE-ANGABEN.md`):35か国177種類、
  うち23種類は検査数字の計算付きです。数え方も記載されています――
  私たちは`[NAME]`を1件として数えますが、他では名・ミドルネーム・
  姓を3件として数える場合もあります。

- **黒塗りが、Word、PowerPoint、OpenDocument、HTMLでも利用できるように
  なりました。** プレースホルダーと黒塗りの選択は、これまでPDFファイル
  のみに存在していました。現在は他の形式でも可能です。発見箇所は削除
  され、その場所に黒いバーが表示されます――文書自体の中にであり、上に
  重ねた画像としてではありません。ファイルを転送する人は、黒塗り
  済みの文書を渡すことになり、その下にテキストとして黒塗り対象が
  残っている文書を渡すことはありません。

  **これは個別に決定されます。**2つの選択欄で――「PDFの場合」と
  「Word、PowerPoint、OpenDocument、HTMLの場合」です。これを別々に
  したいという需要があります――黒塗りされたPDFは官公庁へ送られ、
  同じ内容のWordファイルは社内を回覧され続け、読める状態を保つ必要
  があるためです。コマンドラインでは対応する`--pdf-modus`と
  `--office-modus`です。以前のフッションで保存された「黒塗り」設定は
  引き続きPDFに適用されます。

  表、純粋なテキスト、CSV、メールではバーは使えません――そこには
  バーを乗せる面がないためです。引き続きプレースホルダーが挿入され、
  結果には**それが現在明記されます**。以前のように黙って行われる
  ことはありません。

- **新機能:「削除」――発見箇所は単純に空になります。** プレースホルダー
  と黒塗りに次ぐ3つ目の動作モードであり、**すべて**の形式で使える
  唯一のものです。何かを省略するのに面は必要ありません。PDFでは何も
  描画されず、WordとHTMLではその箇所が空のまま残り、表でも同様です。

  これは3つの中で最も目立たない方法です。結果を読む人は、そこに何か
  があったことすら分かりません――値の長さでさえもう漏れません。誰かが
  確認すべき書類では、たいていプレースホルダーの方がより良い選択の
  ままです。

  画像に対してはこの3つの選択のどれも適用されません。画素は
  プレースホルダーに置き換えることも省略することもできません。文字
  認識がそこで見つけたものは、これまでどおり常に上塗りされます。

- **修正ウィンドウが、もう存在しない置換をもう主張しなくなりました。**
  右側には、各値についてプレースホルダーが表示されていました――1つも
  存在しない黒塗り済みファイルでも同様です。そのような行をクリック
  しても何も選択されず、「取り消す」は空振りに終わっていました。現在は
  「黒塗り済み」または「削除済み」と表示され、そうした行はそもそも
  取り消しできません。テキストはなくなっており、復元するものがない
  ためです。これは黒塗り済みのPDFファイル、Word、OpenDocument、そして
  画像内で発見されたすべてのものに当てはまっていました。

- **テキスト表示が、現在はバーをバーとして表示します。** 黒塗りされた
  Wordファイルは、修正時には**空**のように見えていました――黒塗り
  された箇所には、まるでプログラムがテキストを飲み込んでしまったか
  のような空白がありました。原因は結果ではなく表示にありました――
  文書自体には、常に正しくバーが存在していました。現在は表示にも
  それが表示されます。結果と同じ黒色で、Word、PowerPoint、
  OpenDocument、HTMLでです。

- **Outlookのメッセージ(`.msg`)が浄化されるようになりました。**
  `.eml`はすでに以前から対応していました――しかしドイツの企業では
  Outlookがメールそのものであり、そこでは保存されたメッセージは
  `.msg`と呼ばれます。これにより、最も個人情報の密度が高い形式が、
  最も一般的な保存形式でもカバーされます。件名、送信者、受信者行、
  本文、HTML版、受信者一覧、添付ファイル――添付ファイルは既存の経路と
  本文と同じプレースホルダーで処理されます。

  **`.msg`は同じテキストを複数回持ちます。**これが落とし穴です。
  プレーンテキストとして、HTMLとして、**そして**RTFとしてです。
  プレーンテキストだけを浄化した人は、何もしていないのと同じです
  ――Outlookは優先的にRTFを表示します。そのためRTF版は完全に削除
  されます。同様に、Receivedチェーンを含むインターネットヘッダーと、
  あらゆるテキスト浄化を生き延びる、氏名やアドレスを含むバイナリ
  検索キーもです。結果は引き続きOutlookで開くことができ、書式装飾
  なしでテキストを表示します。報告書にはその旨が明示的に記載され
  ます。

- **正規表現を書く代わりに、自分の言葉でルールを説明します。** ルール
  ウィンドウは多くのことができますが、そのために正規表現パターンを
  要求していました――ほとんどの人がそこで諦めるポイントです。現在は
  1文で十分です。「12 C 345/26のような形式の事件番号は残してくだ
  さい。」AI段階がそこから語句と検索パターンを提案します。

  **採用されるのはチェックを入れたものだけであり、初期設定では何も
  チェックされていません。** 各提案には、それが何を意味するかを説明
  する1文と、渡すことのできるサンプルテキスト内でのヒット件数が表示
  されます。保護を**取り除く**ものは、そのように明記されます。
  「この語句を常に削除する」と「この語句を決して削除しない」は、
  一覧では見分けがつかなくなってしまうためです。すべてに一致して
  しまうような提案は、そもそも表示されません。

- **検査記録が、現在はすべての作業台を合算してカウントします。** ある
  組織が`protokoll_pfad`経由で記録を共有フォルダに配置している場合、
  各作業台がそれぞれ独自の月次ファイルを書き込みます――これまでは、
  30台の作業台を持つデータ保護責任者が、30個のファイルを個別に確認
  する必要がありました。一覧の上には現在、その月の合計を示す行が
  あり、**改ざんされた履歴を名前とともに通知します**。後からの変更は、
  誰かが確認して初めて気づかれるものであり、30個のファイルを手作業で
  確認する人はいません。

  **個人ごとの集計はありません**――この表示でもです。「誰がどれだけ
  浄化したか」という順位付けは、行動管理・成果管理として使える可能性
  があり、共同決定権の観点で重要なのは意図ではなくその点です。カウント
  されるのは、組織全体での処理回数、ファイル数、ヒット数です。

- **「文書からプロファイルを提案」:44種類を確認する代わりに、ルールに
  一度だけ尋ねます。** ルールウィンドウに新しいボタンが加わりました。
  これはAI段階に1つの文書を見せ、それが何であるか――診療情報提供書、
  応募書類、契約書、請求書、通知書など――を判断し、それに合った戦略を
  提案します。例えば診療情報提供書では、日付が置換ではなくずらされ
  ます。診療記録では時系列こそが内容だからです。

  **プロファイルはプログラム内にあり、モデルは選ぶだけです。**黒塗り
  ルールは、言語モデルが良いアイデアだと思うものに左右されることは
  ありません。各項目は個別に、根拠とともに提案されます。確認なしに
  何かが採用されることはなく、自分で設定したものはそのまま維持され
  ます。AI段階なしでは、安全な初期設定――すべてにプレースホルダー
  ――が使われます。

- **新しい戦略「架空にする」:プレースホルダーの代わりに、もっともらしい
  誤った値を使います。**「Frau Bergerさんが Herrn Dopplerさんへ
  Fuldaで手紙を書いた」――「[NAME_1]が[NAME_2]へ[ORT_1]で手紙を
  書いた」の代わりに、です。研修資料、デモ用ファイル、テストデータ
  セット、その後AIに提示されるすべてのもの向けです。敬称、文の構造、
  読みやすさが保たれます。

  同じ値は、同じ案件のすべてのファイルにわたって、そして同じルール
  ファイルを持つすべてのパソコンで、同じ架空の値になります――**どこ
  にも対応関係が保存されることなく**です(ハッシュ化と同じ仕組み)。
  メールアドレスは予約されたサンプルドメイン上に、電話番号はそのため
  に確保された範囲に配置され、架空のIBANは正しく計算された検査数字
  を持ちます。氏名、地名、住所、企業名、メール、電話、IBANで利用
  可能です。他の種類ではこのルールは、効果なく残るのではなく拒否
  されます。

  **報告書は、架空の値であることを明示的に伝えます。**このように
  浄化された文書は本物のように読めますが本物ではありません――証拠
  としては使えず、原本として渡してはいけません。

- **照合機能:「誰が識別可能なままか?」** AI段階の下にある新しい
  チェックは、**完成した結果**を再度言語モデルに提示し、浄化にも
  かかわらず誰が識別可能かを尋ねます。これは、どんな検出でも見つけ
  られないケースを対象としています――そこにはそもそも氏名が存在しない
  からです。「その地区で唯一の助産師」「3月に火災の後に退職した同僚」
  などです。パターンは何も一致せず、それでもその場にいれば誰もが
  誰のことか分かってしまいます。

  **これによって何かが削除されるわけではありません。** 該当箇所は
  理由の1文とともに報告書に記載され、判断は手作業で行われます――
  自らの判断で、疑わしいと感じた文を文書から取り除くようなプログラム
  は、浄化を書き換えに変えてしまい、何が欠けているのか誰にも分からなく
  なってしまいます。1ファイルにつき最大5箇所まで。モデルが文言として
  裏付けられないものは対象外です。コマンドラインでは`--restrisiko`を
  `--ki`と併用します。

- **AIからの復路:「回答を逆翻訳」。** これまでは半分のループしか構築
  されていませんでした――テキストをコピーし、浄化したものを貼り付け、
  AIに提示する、というものです。回答は`[NAME_1]`のまま戻ってきて、
  それを必要とする人は、自分が手作業で取り除いたものを手作業で
  戻していました。現在は復路が「プログラム」メニューにあります:
  回答をコピーし、項目をクリックすると、本物の氏名が再び表示され
  ます。

  そのための対応関係は**メモリ内のみ**に存在し、常に直近で浄化された
  箇所にのみ有効で、1時間後に失効します。クリップボード監視をオフに
  すると、すぐに消去されます。復元できるのは置換されたものだけです
  ――黒塗り、マスク、ハッシュ化されたものは復元不可能であり、プログラム
  は、そのためいくつの箇所をそのまま残さざるを得なかったかを伝え
  ます。管理されたインストールは、設定`rueckweg`によりこの復路を
  完全にオフにできます。

- **フォルダを監視:置かれたものは、すぐ後に浄化された状態で出力先に
  現れます。** 郵便室、私書箱チーム、スキャンフォルダのために――一度
  設定すれば、その後誰もクリックする必要がありません。「ファイル→
  フォルダを監視する…」から、コマンドラインでは`--wache <フォルダ>`
  でアクセスできます。

  元ファイルはあった場所にそのまま残ります。希望に応じて、変更され
  ないままサブフォルダ「完了」に移動されます。何かが上書きされること
  はありません。ファイルは、その書き込みが完了して初めて処理対象に
  なります――そうしないと、まだネットワーク経由でコピー中のファイル
  が途中まで読み込まれ、浄化済みとして通知されてしまいます。うまく
  いかないものはそのまま残り、無限に繰り返されることなくその旨が
  伝えられます。そして監視機能は、処理済みのものをファイル名なしで
  記憶します――受信フォルダにあるものは、しばしばその名前だけで
  内容が分かってしまうためです。

  **自分のユーザープロファイル外――例えばネットワークドライブ――の
  フォルダを監視するには、自動化ライセンスが必要です。**複数人がアク
  セスできるフォルダはサービスであり、1台の作業台ではありません。
  自分のプロファイル内、および試用期間中はこの制限は適用されません。

### 修正

- **設定画面が、右側で切れていました。** ウィンドウは固定サイズで開いて
  おり、それは開発時のフォントサイズにしか対応していませんでした
  ――Macでは「今すぐ確認」「変更…」とその案内が、半分画面外にありまし
  た。現在は各ページに必要な幅で開きます――どの言語、どのフォント
  サイズでも、画面サイズによってのみ制限されます。

- **「今すぐ確認」が、現在は目に見える形で応答します。** 結果はメイン
  ウィンドウのステータスバーに表示されていました――つまり、その操作を
  行った設定ウィンドウの後ろにです。確認した人には何も見えませんで
  した。現在は回答が設定画面の上に通知として表示され、新しいフッション
  がある場合はすぐにインストールへと導きます。プログラム起動時は
  引き続きこれまでどおりステータスバーのままです。求められていない
  限りウィンドウは開きません。

- **コピーされたファイルが、Macではクリップボードに届いていません
  でした。** 浄化済みファイルの再配置は成功を通知していたにもかかわ
  らず、使えるものが何も配置されていませんでした――貼り付けても何も
  起きませんでした。ファイルをクリップボードに書き込むあらゆる処理が
  影響を受けていました。

- **そしてクリップボードからは、Macでは最初のファイルしか読み込まれて
  いませんでした。** Finderで3つのファイルをコピーし「クリップボード
  を今すぐ浄化」を選んだ人は、そのうち2件を未浄化のまま受け取って
  いました――そのことを伝えるものは何もありませんでした。現在はすべて
  届きます。

- **「ファイルを確認」が、現在はドラッグ&ドロップされたファイルも
  受け付けます**――メインウィンドウと同様です。ドロップされたものは
  追加され、これまでの選択を破棄することはありません。同じものを
  2回ドロップしても何も変わらず、プログラムが読み取れないものは
  黙って無視されるのではなく通知されます。

- **そしてウィンドウは、お客様を待っていることを伝えます。** これは
  空の枠と灰色の「確認」ボタンとともに開いていました――これは、
  選択が不足しているようには見えず、何もないように見えていました。
  現在は「まだファイルが選択されていません――ここにドラッグするか、
  下の『ファイルを選択…』から選んでください」と表示されます。

- **長い処理が、現在は動作中であることを伝えます。**「より精密な
  検出のための追加モデルを読み込んでいます――少々お待ちください…」
  は、検出処理が計算している間、そのまま表示され続けていました――
  47,500語のファイルでは18分間です。読み込み自体は9秒で終わって
  いたにもかかわらずです。これを見た人は、プログラムが固まったと
  思ってしまいます。現在はその後に「より精密な検出を実行中――長い
  テキストでは数分かかることがあります」と表示され、ステータスバー
  がカウントします:「より精密な検出(7/312)」。これはモデルの処理
  ループから、250語ごと――約6秒ごとに通知されます。テキストブロック
  単位ではありません。1つのテキストブロックが1万2千語を含み、数分を
  要することもあるためです。

- **中断された処理が、現在は中断されたことを伝えます。**「中止」を
  押した人は、その後「0/1件のファイルを浄化しました。」と表示されて
  いました――カウント自体は正しいのに、伝えている内容は誤りでした。
  どのファイルが影響を受けたかという通知は、まさにその瞬間にカウント
  通知に上書きされていました。そしてファイル一覧には「実行中…」が
  そのまま表示され続けていました――もう何も実行されていないにも
  かかわらずです。現在はそこに「中止されました」と表示されます。

- **プライバシーに関する文が、途中で切れていました。**「…クラウドなし、
  アップロードなし。詳細はプライバシ」――プログラムが起動時の
  ウィンドウ幅では、単語の途中で終わっていました。現在は全幅を使い
  ます。

- **ライセンスサービスが何かを伝えようとしていたのに、誰も聞いて
  いませんでした。** すべてのライセンス席が埋まっている、ライセンスが
  期限切れである、キーが不明である、または提供元のライセンス管理が
  停止している場合、サービスはまさにそのための理由を送信します
  ――最初から、それを**一度だけ**説明する仕組みが想定されていました。
  しかしそれが表示されたことは一度もありませんでした。現在は、まず
  プログラムが変わらず動作を続けることを伝え、その後何が問題かを伝える
  通知が表示されます。理由ごとに一度だけです。それを閉じた人は、
  毎日の確認で再び目にすることはありません――理由が変わった場合を
  除きます。

- **ショップで購入した複数席ライセンスが「1席」と表示されていました。**
  ショップは事前に準備されたキーを配布し、購入された席数はショップ
  側で保持しています。しかし表示されていたのはキー自体に含まれる
  数値であり、それはどの在庫キーでも1席となっています。8席購入した
  人には「1席」と表示され――2台目の登録済みパソコン以降は、表示が
  赤字で「管理者にお問い合わせください」と表示されていました。現在は
  サービスが最後に報告した数値が使われます。応答がない場合はキーの
  値が使われ、購入した席数を下回ることは決してありません。追加購入や
  更新についても同様です。これらは提供元側で席数を変更するのであり、
  お客様のキーを変更するものではありません。

- **購入後、「Maskuro個人向けライセンスにライセンスされています」と
  表示されていました。** これは氏名ではなく、キーが準備される際の
  プレースホルダーです――お客様の氏名は、キーが購入前にすでに署名
  されているため、そこには入れられません。見知らぬ氏名をお客様自身の
  ものであるかのように表示する代わりに、現在は単に「個人向けライセンス」
  と席数が表示されます。お客様宛に発行されたライセンスでは、これまで
  どおりお客様の氏名がそのまま表示されます。

- **ヘルプメニューに「ヘルプ_FAQ」と表示されていました。** アンパサンド
  記号が、Qtによってキーボードショートカット文字の印として解釈され、
  アンダースコアに変わっていました。現在は「ヘルプ&FAQ」と表示され
  ます。

- **プログラムがアイコンに格納された際、設定ウィンドウが取り残されて
  いました**――メインウィンドウが閉じられた後もです。現在は一緒に
  格納されます。(このフッションのみの問題です。独自ウィンドウは
  新機能でした。)

- **拒否されたライセンス要求が、現在は原因を伝えます。** ライセンス
  サービスが理由を付けずに要求を拒否した場合、ライセンスウィンドウ
  には赤字で「不明な応答」とだけ表示されていました――お客様にも
  サポートにも対処しようのない文で、原因をご自身のキーに求めさせて
  しまうものでした。現在は実際に何が起きたかが表示されます。サービス
  が理由を示さずに拒否したこと、そしてどこに問い合わせればよいかです。
  提供元のライセンス管理が一時的に停止している場合はその旨も伝えられ
  ます――お客様のキーはそれによる影響を受けていないという説明も
  添えて。

- **Macでは、設定済みの言語が突然「不足」として扱われていました。**
  起動時にプログラムが「言語モデルがインストールされていません」と
  通知し、初回セットアップを提案していました――言語はとっくに読み
  込まれていたにもかかわらずです。「文書の言語」を確認すると、そこには
  すべて揃っていました。プログラムは起動方法によって2つの異なる場所を
  探していました。プログラムフォルダから起動した場合は見つかります
  が、同じビルドが単なるフォルダとして起動された場合は、自分の隣を
  探しており、そこには何もありませんでした。現在は、Macではプログラム
  のパッケージ方法にかかわらず、常にユーザープロファイル内の同じ場所
  が使われます。何かを再読み込みする必要はありません。

- **「新機能」が、一覧の半分しか表示していませんでした。** 更新後の
  ウィンドウは文の途中で表示が止まり、残りの項目は空の箇条書き記号
  として表示されていました。原因は山括弧に囲まれたプレースホルダー
  でした――例えば`<datei>.docx`のようなものです。表示処理がこれを
  マークアップとして扱い、そこから先のすべてを破棄していました。
  よりによってセキュリティに関する新機能が影響を受けていました。
  ヘルプはこうしたプレースホルダーを以前から正しく表示しています。
  このウィンドウも現在は同様です。

- **2本指でつまむ動作が、現在は修正ウィンドウでズームします。**トラック
  パッドでは、これが*まさに*ズームのジェスチャーです――エディタでは
  これまで何も起きず、ある箇所を詳しく見たい人は、スライダーや
  Ctrl+マウスホイールに頼るしかありませんでした。ページはこの
  ジェスチャーに直接追従し、指を離すと再び鮮明に描画されます。

- **ズームは、実際に見ている箇所を対象に行われます。**つまむ操作は
  指の間の点を中心に拡大し、Ctrl+マウスホイールはカーソルの下の点を
  中心に拡大します。ボタン、キーボードショートカット、ズームスライダー
  は中心を保持します――それらには指し示す対象箇所がないためです。
  以前はすべてでスクロール値だけが固定されていました。全体表示された
  ページから始めると、それが上端を固定してしまい、拡大するとその下の
  すべてが画面外に流れ出ていました。

- **「前後比較」は、ページプレビューでは無効なボタンでした。** ページ
  プレビューがオンの間、それは押すことができ――毎回、原本を開けない
  という通知が表示されていました。そこには比較するものすらありません
  でした。ページプレビューは浄化されたフッションの画像であり、原本の
  対応物は存在しないためです。このボタンは現在ロックされ、マウスを
  重ねると理由(テキスト表示への案内)が表示されます。その説明文は
  さらに、比較は「テキスト表示かページ表示かに関係なく」行われると
  明示的に約束していました――これは一度も正しくありませんでした。

- **ページプレビューが、LibreOfficeをクラッシュさせていました。** 2つの
  ページプレビューが同時に生成された場合――例えば「PDFとして黒塗り」
  がプレビューの計算中に実行された場合――システムはLibreOfficeの
  クラッシュを報告していました。最終的にはページが表示されていた
  にもかかわらずです。両方の処理が同じLibreOfficeの作業領域にアクセス
  しており、それに耐えられなかったのです。現在は常に1つの処理だけが
  それを取得し、残りは独自の領域に退避します。そのため数秒余分に
  時間がかかりますが、エラー通知はもう表示されず、どの処理も結果
  なしに終わることはありません。実行中の処理の隣で2つ目のレンダリング
  要求が発生した場合、それはそもそも受け付けられなくなりました。

- **「原本を表示」が、プログラムを終了させることがありました。** 原本
  が開けない場合――移動、リネーム、パスワード保護、または切断された
  ドライブ上にある場合――修正ウィンドウは警告なしに強制終了し、開いて
  いた作業コピーが失われていました。現在は案内が表示され、スイッチは
  元に戻り、浄化済みのフッションはそのまま残ります。原本がそもそも
  対応しない場合――例えばWordファイルから生成されたPDFページプレビュー
  など――スイッチは最初からロックされ、マウスを重ねるとその理由が
  表示されます。押すたびに同じ案内が表示されるのではなく、です。

- **不具合報告が、一度も届いていませんでした。** 不具合を報告しようと
  した人は「相手側が報告を拒否しました」と表示され――誰もそれを見た
  ことがありませんでした。原因は2つあり、どちらも経路上にありました。
  プログラムはサーバーに対して自身の身元を示しておらず、そのため
  大量アクセス防止の仕組みに拒否されていました。またアドレスが2つ目の
  名前を指しており、プログラムはそれに追従していませんでした。両方
  とも修正され、報告は再び送信されるようになりました。**同じことが
  ライセンス有効化にも当てはまっていました。**登録、解除、問い合わせ
  も同様にサービスに届いていませんでした――そこでは目立たなかった
  だけです。応答のない要求は、意図的にライセンスに何の変更ももたら
  さないためです。それでも拒否が原因不明のままの場合は、現在はその
  技術的な番号が表示されます。すべての原因が同じように見えることは
  なくなりました。

- **「原本を表示」のクリックが、プログラムを終了させることがありました。**
  原本が開けない場合――移動、リネーム、切断されたネットワーク
  ドライブ上、パスワード保護、または破損――修正ウィンドウは開いていた
  作業コピーとともに消えていました。現在はこの切り替えボタンが浄化
  済みのフッションのままとなり、何が起きたかを示すボックスが表示され
  ます。技術的な理由は詳細に記載されており、報告したい場合に使え
  ます。同じことが表示できない結果にも当てはまります。ウィンドウは
  消える代わりに開いてその旨を伝えます。

- **クラッシュに関する質問が頻繁すぎて――しかも、その手がかりを削除して
  いました。** これは、何もクラッシュしていない場合にも表示されて
  いました。この記録は、どこかで予期しない不具合が発生するたびに
  作成され、プログラムがそれを乗り越えてその後普通に終了した場合でも
  作成されます――それが片付けられることは一度もありませんでした。
  そして「いいえ」と答えた人は、その事象の唯一の詳細情報を破棄して
  いました――記録は質問を*表示した*時点ですでに消去されていました。
  両方とも修正されました。正常な終了は記録を片付け、質問は本当の
  中断が発生した場合にのみ表示され、確認済みとしてマークされるのは
  お客様の回答の後だけです。詳細情報はいずれにせよ自分のパソコン上の
  エラー記録に残っています――何も送信したくない人も、これによって
  何も失いません。送信されるのは、これまでどおり、事前に完全に確認
  し、自ら承認したものだけです。

- **「浄化」が、静かにロックされたままになることがありました。** 言語
  モデルの読み込みが止まった場合、ボタンは説明なしに無効化されて
  いました。クリックしても何も起こらず、ステータスバーには10分後でも
  変わらず「言語モデルを読み込み中…」と表示されていました。原因は、
  バックグラウンド処理での障害が、ファイルマネージャーから起動した
  際には誰にも見えない場所に送られていたことでした――作業可能に
  見えるのにどのクリックにも反応しないウィンドウが残っていました。
  現在は、こうした障害はエラー記録に記録され、言語モデルの読み込みは
  静かに諦める代わりに常にその失敗を通知します。それでも静かなままの
  場合、アプリケーションは45秒後に、何かがおかしいことを、詳細内の
  助言とともに伝えます。ロックされたボタンは、マウスを重ねるとその
  理由を伝えます。最初の長い再取得はここでは沈黙とはみなされません
  ――進捗が通知されている限り、静かなままです。これらすべてはクラッシュ
  としてはカウントされません。アプリケーションは動作を続けており、
  そのため次回起動時にもそれについて尋ねられることはありません。

- **Macでは、プログラムが更新を見つけられなくなっていました――そして
  最新版だと伝えていました。** Mac版はルート証明書の一覧を同梱して
  いませんでした。それをビルドされたパソコンにしか存在しない場所で
  探していたのです。そのため、どのサーバーとも相手を確認できず、
  すべての接続が中断していました。更新なし、ライセンス有効化なし、
  言語モデルの再取得なし、不具合報告なしです。古いフッションは、
  これを黙って「最新のフッションを使用しています」という回答に
  していました。証明書は現在プログラム自身に同梱されています。そこに
  見つからない場合はシステムのものを使用し、Macでは必要に応じて
  キーチェーンのものも使用します。それでもまったくない場合は、最新
  フッションだと主張する代わりにその旨を伝えます。この確認自体が
  オフになることは決してありません。

  この1回の更新だけは、Macの利用者が手動でインストールする必要が
  あります。サーバーに到達できないフッションは、自分自身を更新する
  こともできません。

### 変更

- **メインウィンドウが整理されました。** 下部には6つの同じ大きさの
  ボタンが並んでいました――「About…」「マニュアル」「ヘルプ&FAQ」が
  その下にありましたが、これらと同じ3つの経路は、その上のヘルプ
  メニューにすでに存在していました。現在はこれらが1つの「ヘルプ」
  ボタンにまとめられ、それを開くとすべてが表示されます。何も失われ
  ません。下部には、実際に作業を始める2つの経路だけが残ります。
  「浄化」と「手動で黒塗り…」です。

- **プログラムが現在何をしているかが、現在は固定の場所に表示されます。**
  通知(「言語モデルを読み込み中…」「(3/7) brief.pdf」「7件中5件の
  ファイルを浄化しました。」)は、これまで2つのボタン列の間に灰色の
  テキストとして表示されていました。現在は独自の領域を持ち、色付きの
  点が前に付きます。何も実行されていない間は灰色、作業中は青、正常な
  処理の後は緑、注意事項が発生した場合は黄色です。この点は、隣に
  書かれていること以上の情報を伝えるものではなく、それをより速く
  伝えるだけです。

- **設定が独自のウィンドウになりました。** これまではメインウィンドウ
  内にあり――4つのタブを持つ箱で、「その他の設定」で展開するもの
  でした――その内容に対しては小さすぎました。常にスクロールバーが
  表示され、匿名化と仮名化の選択が画面の一部にしか収まっていません
  でした。ボタンは現在「設定…」という名前になり、サイドバー付きの
  ウィンドウを開きます。4つのページそれぞれが完全に収まります。
  メインウィンドウは開いた際にもう飛び出さず、隣にファイル一覧を
  同時に見ることができます。変わったのは設定がどこにあるかだけです
  ――何があり、それが何をするかは変わっていません。

- **「詳細」が、飛び跳ねる代わりに展開するようになりました。** ウィンド
  ウはこれまで一瞬で成長し、その後何が変わったのかを探す必要があり
  ました。現在はその場所に向かって動きます。

- **フォントサイズと余白が、ウィンドウ全体で同じ基準に従うように
  なりました。** 見出しは2箇所で大きさが異なり、同じ階層の行が異なる
  間隔で並んでいました。これは目に見える形での落ち着きとして表れます
  ――個別の変更としてではなく。

- **匿名化が現在初期設定になりました。** これまでは仮名化が初期設定
  でした。同じ人物には同じ番号が振られ(`[NAME_1]`、`[NAME_2]`)、
  対応関係が読み取れる状態でした――しかし法的には、これは依然として
  **個人データ**のままでした。何も設定しない人は、現在はデータを
  GDPRの対象から外す方式を使うことになります。同じ種類のすべての
  ヒットが同じ名前になります(`[NAME]`)。番号付けは引き続き選択肢
  として、同じウィンドウ内にそのまま存在します。既存の設定はそのまま
  維持されます。コマンドラインでは`--pseudonymisieren`
  (`--mit-nummerierung`も可)で元に戻せます。

- **匿名化されたプレースホルダーは、もう個別には復元できません。**
  匿名化を行うと、各人物が同じプレースホルダーを持つことになります
  ――そのため、特定の氏名に対応する個別の箇所というものが存在しなく
  なります。修正ウィンドウはそれでも「置換を取り消す」を提示して
  いました。クリックすると、*1つ*の値が*すべて*の箇所に挿入されて
  しまっていたでしょう。こうした行は現在、黒塗りされた項目と同様に
  淡色表示され、クリックするとその理由が表示され、手作業で追加された
  発見箇所には、文書内のどこにも存在しない番号がもう付与されなく
  なりました。

  同じ理由から、匿名化された処理の後は「回答を逆翻訳」もなくなり
  ました――以前は、あらゆる人物の箇所に見知らぬ氏名を挿入してしまって
  いたでしょう。このループが必要な人は「仮名化」を選んでください。
  アプリケーションは現在、期限切れの対応関係を指し示す代わりに、その
  旨を明確に伝えます。

  コマンドラインでは、`--zuordnung`は匿名化の際に、逆翻訳にならない
  ファイルを書き出す代わりに、現在は中断するようになりました――
  ウィンドウでは、そのチェックはとっくにロックされていました。
  `--pseudonymisieren`を併用するか、`--zuordnung`を省略してください
  ――通知にその旨が表示されます。この場合、結果自体もそもそも生成
  されません。スクリプトが半端な作業結果を抱えないようにするためです。

- **更新チャンネルが、新たに「安定版」に設定されます。** 独自の選択が
  ない場合、これまではチャンネルが、実行中のフッションがどのビルド
  由来かに従っていました――一度でもテスト版を試した人は、それ以降
  常にテスト版が提案されていました。チャンネルの切り替えは1つの決定
  であり、それはそのまま維持されるべきです。そのため初期設定は
  「安定版」です。設定済みのチャンネルは変更されません。

### 改善

- **「Beschwerdevorgang(苦情処理)」が、もう地名として扱われなくなり
  ました。** 見出し「Aktennotiz – Beschwerdevorgang 12 C 345/26」
  では、プログラムがこの案件番号も一緒に黒塗りしていました――言語
  モデルはそれを、文脈に関係なく地名だと判断していたのです。対象と
  なったのは単語1つではなく、複合語の**基本語**です。「vorgang」と
  「notiz」は、これによりGeschäftsvorgang(業務処理)、
  Buchungsvorgang(記帳処理)、Zahlungsvorgang(支払い処理)、
  Telefonnotiz(電話メモ)もカバーします。検査した30の行政用語の
  うち、以前は3件が誤検出を引き起こしていましたが、現在は0件です。
  隣にあるものはこれまでどおりすべて検出されます
  (「Beschwerdevorgang: Bernd Meisinger」では氏名は消えますが、
  見出しは消えません)。

- **匿名化が、追跡と記録のために再び記録を取るようになりました。**
  匿名化の動作モードでは、プログラムは発見された値を記憶していません
  でした。これにより2つのことが機能していませんでした。文書全体の
  整合性の後処理(後から単独で登場する姓がそのまま残っていました)と、
  検査記録内の置換一覧です。匿名化がまれな選択であった間は、これは
  ほとんど気づかれませんでしたが、それが初期設定になると通常のケース
  になっていたはずです。文書内では何も変わりません――プレースホルダー
  は番号なしのままです。

- **「Kein personenbezogenes Datum(個人データに該当する日付なし)」
  が、現在は「keine personenbezogene Angabe(個人に関する情報なし)」
  という表記になりました。** 復元ダイアログと顔検出の警告には、法律
  用語の*Datum*――「Daten(データ)」の単数形――が使われていました。
  これはカレンダー上の日付として読まれてしまいました。特に、
  アプリケーションの他の箇所では「日付情報も削除する」という表現が
  使われているためです。現在はどこでも「Angabe(情報)」という表記
  になっています。同じウィンドウ内にある上の4つの理由と同様です。

- **出所を示す一文が、「このプログラムについて」ウィンドウのみに移動
  しました。**「Made with ♥ in Austria」は、メインウィンドウ下部の
  ボタン列の真ん中にあり、そこではまるで別のボタンのように見えて
  いました。これは引き続き「このプログラムについて」ウィンドウ――
  それを探す場所にあります。

- **ドロップ領域に、現在は目に見える縁があります。** 破線の枠はこれ
  まで非常に薄く、ウィンドウからほとんど区別が付きませんでした――
  これは、この領域が単なる面である限りは問題ありませんでした。しかし
  これがTabキーで移動できるボタンになって以降、この線だけがそれを
  操作要素として示す唯一の手がかりです。そのため、規格が要求する
  値まで引き上げられました。

## 0.10.22-beta.1 – 2026年8月15日

### 新機能

- **クリップボード監視をオフにすると、本当にオフになります。** 監視
  機能は、原本を復元できるように直近の内容をメモリ上に保持していま
  した――これまでは監視をオフにしていた場合も同様でした。現在は、
  オフにすると履歴が破棄されます。これによりオフ後の復元はできなく
  なりますが、まさにそれが意図です。オフとはオフを意味します。
- **エラー記録に、もうファイルパスが含まれません。** これはお使いの
  パソコン上にのみ存在し、自動的に送信されることは一度もありません
  でした――しかしパスを平文で記録しており、ファイル名は内容以上に
  多くを語ることがあります。「…/Scheidung_Mueller_Vergleich.docx」
  は、書き込み時に現在は`<datei>.docx`になります。拡張子は残ります
  ――不具合調査で重要な情報だからです。クラッシュ後の記録についても
  同様です。
- **置換一覧が、現在は自身の中で警告するようになりました。** これは、
  お客様の原本データが平文で記載される唯一のファイルであり、結果の
  隣に置かれます――フォルダを転送する人は、これも一緒に渡してしまい
  ます。現在は、この警告がファイル**内**の最初の行に記載され、出力
  領域にはファイル名だけでなく完全なパスが表示され、コマンドライン
  では、そもそもこのファイルの存在自体が言及されるようになりました。
  これまでは、それが作成されたことすら分かりませんでした。
- **匿名化か仮名化かが、現在は名前付きの選択になりました。** これ
  までこの箇所には「同じ氏名を同じ表記にする――そうすればAIも誰が
  誰か分かる」というチェックがありました。これは利点を説明する一方、
  結果を伏せていました。通し番号付きのプレースホルダー
  (`[NAME_1]`、`[NAME_2]`)は**仮名化**であり、仮名化されたデータは
  引き続き個人データです――これで匿名化したと思っていた人は誤解して
  いました。現在は両方の方式が並んで表示され、それぞれの代償が明示
  されます。初期設定は引き続き仮名化です。その後読まれたりAIで処理
  されたりする文書は、その対応関係を必要とするためです。匿名化の
  際、置換一覧はロックされます。それがあると結果を再び追跡可能に
  してしまうためです。マニュアルとFAQは、この違いを全18言語で説明
  します。コマンドラインでは、このスイッチは現在`--anonymisieren`
  とも呼ばれます。
- **ドロップ領域の上の文が、現在は本当に正しい内容を伝えます。** これ
  までは「100%ローカル処理――クラウドもアカウントもなく、GDPR
  対応」と約束していました。お客様の文書についてはそのとおりですが、
  プログラムについては、その言い切り方は正確ではありません。更新を
  確認し、希望に応じて不具合を報告し、モデルを再取得し、購入した
  作業台を登録します。現在は、より限定的で確実な文言が表示されます。
  お客様の文書はパソコンを離れません。
- **結果には現在、常に「確認が必要である」旨が表示されます。** これ
  までは、正常な処理の後、Maskuroが緑色で「12件の項目を削除しました」
  とだけ表示し、それ以外は何も表示していませんでした――これは、
  すべてを発見したという保証のように読めてしまいます。案内は、
  具体的に何かが確認できなかった場合(画像、未知の添付ファイルなど)
  にのみ表示されていました。現在は、すべての結果の下に、あらゆる
  場合に個人データがすべて検出されるとは限らないこと、確認は利用者の
  責任であること、不足分は手作業で補う必要があることが、見落とし
  ようのない形で表示されます――ウィンドウ内、出力領域、コマンド
  ラインでです。閉じられる通知ウィンドウではありません。この文は
  常に表示されたままです。クイックガイドも現在は同じ文言でこれを
  伝えます。
- **更新後の起動時に、何が変わったかが表示されるようになりました。**
  これまで更新は静かに完了し、単なる再起動と見分けが付きませんでした。
  現在は一度だけ「新機能」が表示されます――フッションを飛ばした人には、
  その間の内容もまとめて表示されます。ごく最初の起動時には表示され
  ません――そこでは引き続きクイックガイドが案内します。
- **中国語と日本語でも、氏名が検出されるようになりました。** これまでは
  **まったく**検出されませんでした――少ないのではなく、ゼロです。
  両方の言語モデルには単語分割機能が欠けており、それなしでは空白の
  ない文が単一の単語として扱われてしまいます。プログラムは黙って
  多言語の代替モデルに切り替えていました。両言語とも、現在は他の
  言語と同様に人物や地名を認識します。日本語辞書は言語と一緒に読み
  込まれ、プログラム本体には含まれません――それ単体で約200MBあり、
  さもなければ誰もが常にそれを抱えることになってしまいます。
- **ルーマニアが選択可能な国になりました。** これまで完全に欠けて
  いました。これにより、ルーマニアの住所(「Strada Victoriei 30」)、
  郵便番号と地名(「010061 București」)、個人識別番号(Cod Numeric
  Personal)が検出されます――後者は検査数字が一致する場合のみです。
  そうしないと、請求書上のあらゆる13桁の数字が対象になってしまい
  ます。これまでは、ルーマニア語文書では郵便番号が、黒塗りされた
  地名の隣に読める状態で残っていました。
- **エディタの「ページをラスタライズ」。** PDFからテキストを削除
  できない場合――他社製作成ツールのファイルでよく発生します――現在は
  希望に応じてページがその画像に置き換えられます。テキストはこれに
  より不可逆的になくなり、ページは読める・検索可能な状態を保ちます。
  この事態を通知する警告には、この操作を行うボタンがすぐに用意されて
  おり、「ツール→ページをラスタライズ」からも自発的にアクセスでき
  ます。取り消しでページを元に戻せます。
- **画面表示が、現在はクロアチア語、ギリシャ語、リトアニア語、
  スロベニア語、日本語、韓国語でも利用できます。** これで18言語に
  なりました。マニュアル、FAQ、法的文書はこの6言語すべてに完全に
  対応しています。浄化された文書内の見出しは画面表示に従います――
  `[NAME_1]`が`[IME_1]`、`[ΟΝΟΜΑ_1]`、`[VARDAS_1]`、または
  `[氏名_1]`になります。**ギリシャ語、日本語、韓国語では、見出しは
  ラテン文字で表示されます**――`[ONOMA_1]`、`[SHIMEI_1]`、
  `[IREUM_1]`のようにです。画面表示自体は独自の文字のままですが、
  文書に書き込まれる部分だけがラテン文字になります。理由はPDFの
  文字セットです。そこでは以前、ギリシャ語や日本語の見出しが
  `[??_1]`として出力されており、氏名と地名の区別がつかなくなって
  いました。
- **9か国が新たに追加され、既存の7か国が完全対応になりました。**
  新たに検出されるのは、**クロアチア、スロベニア、ギリシャ、
  リトアニア、北マケドニア、ロシア、ウクライナ、中国、日本**の身分
  証明書番号、税番号、社会保険番号、住所です。既存の国では、より
  重要な欠落が解消されました。**オランダ**と**ポルトガル**には、
  これまで個人番号がまったくありませんでした――オランダのBSNと
  ポルトガルのNIFは、これらの国のほぼすべての書類に記載されている
  にもかかわらず、検出されていませんでした。ポーランドには税番号
  NIPが、デンマーク・ノルウェー・フィンランドにはそれぞれの住所が、
  カナダには郵便番号が追加されました。これで**35か国**になりました。

### 削除

- **Linux向けのパッケージは、当面提供されません。** ソースコード自体は
  そこで動作しますが、このマニュアルが約束する3つの機能――自動起動、
  グローバルなキーボードショートカット、そして環境によってはバー上の
  アイコン――がLinuxでは欠けています。説明より機能の少ないパッケージを
  提供するのは誤った方針です。WindowsとmacOSは影響を受けません。

### 改善

- **事件番号が、現在はすべての言語で検出されます。** 「Aktenzeichen
  12/2026-AB」は削除されていましたが、「File reference 12/2026-AB」や
  「Sygnatura 12/2026-AB」はそのまま残っていました。Maskuroがこの
  ような番号を認識するためのフィールド語が、ドイツ語にしか存在して
  いなかったのです。現在は12言語での対応語を認識します――これまで
  どおり置換されるのは番号だけで、その前の見出しはそのまま残り、結果
  からそこで何が削除されたかが分かるようになっています。
- **Maskuroは、待機状態で約0.5ギガバイト少ないメモリを使用するように
  なりました。** 起動時には、より精密な検出の追加モデルもこれまで
  読み込まれており、最初の浄化がそれを待たなくて済むようにしていま
  した。再測定の結果、これは648MBのメモリを消費して1.9秒を節約する
  というもので――しかも、ウィンドウを開いてすぐに閉じるだけの場合にも
  そのコストがかかっていました。現在はこのモデルは、必要になった
  最初のタイミングで読み込まれます。ステータスバーがその旨を伝え
  ます。言語モデルは引き続き起動時に読み込まれます――クリップボード
  監視がすぐにそれを必要とするためです。
- **ドロップ領域が、現在はマウスなしでも操作できます。** 「ここに
  ファイルをドラッグ」は、クリックに反応する単なる面でした――キー
  ボードではそこにたどり着けず、スクリーンリーダーはそれを中にテキスト
  のある枠として読み上げ、本来の役割としては読み上げていませんでした。
  現在はボタンになりました。Tabキーで移動でき、スペースキーと
  Enterキーでファイル選択が開き、フォーカスが当たったことは縁で
  確認できます。「ファイル→ファイルを選択」メニューからは以前から
  可能でしたが、それを知っている必要がありました。
- **浄化されたファイルの名前が、現在は読み上げられるようになりました。**
  ファイル一覧では、原本の下に2行目の小さな行として表示されます――
  しかしこれはこれまで描画されているだけで、スクリーンリーダーは原本
  だけを読み上げていました。よりによってこの行は、フォルダに未変更の
  原本があるために「処理が無効だった」という誤解を防ぐために存在
  します。この行は現在、読み上げると「rechnung.pdf、結果:
  rechnung_bereinigt.pdf」となります。
- **見出しのない操作要素が、現在は自分が何のためのものかを伝えます。**
  ファイル一覧のアイコンボタン、修正ウィンドウの描画ボタン、すべての
  選択欄・入力欄は、スクリーンリーダーにとって無名の存在でした――
  「ボタン」「コンボボックス」とだけ読み上げられ、何のボタンかは
  伝わりませんでした。ある行のボタンは、現在そのファイル名も一緒に
  伝えます。20件の一覧では、これがないと同じ文が20回読み上げられて
  しまいます。
- **キーボード操作をする人が、現在は自分がどこにいるかを再び確認
  できます。** 「浄化」ボタンとファイル一覧のアイコンボタンは色付きで
  設計されており、そのためシステムが通常フォーカスの当たった操作要素
  の周りに描く枠が機能しなくなっていました――Tabキーで移動すると、
  視線が行き場を失っていました。両方とも現在は、フォーカスが当たると
  独自の枠を持ちます。ボタンのサイズはこれによって変わりません。
- **7つの文字色が、両方の外観モードで薄すぎました。** 通常の規格
  (WCAG 2.1)に基づいて再測定すると、薄い案内行、ドロップ領域の
  補足テキスト、マニュアルの箇条書き、さらにダーク表示では青と赤が、
  4.5:1という基準を下回っていました――良い照明と良い視力があれば
  読めますが、それ以外では読めません。すべて引き上げられました
  ――段階的な違いは維持され、テキストは引き続き補足的な文章として
  読めます。さらに3つ――警告と成功を伝える色――は、基準ぎりぎりで
  あったため一緒に引き上げられました。これらが読めない人は、何かが
  うまくいかなかったという情報自体が読めなくなってしまいます。目に
  見えて変わったのはダーク表示の「浄化」ボタンだけです。現在は
  Windows 11のアクセントボタンと同様、白ではなく暗い色の文字が表示
  されます。
- **ファイル一覧の各行に、それぞれ独自の×印が付きました。** これ
  までは、まず行を選択してから「削除」をクリックする必要がありました
  ――ちょっとしたことに2つの手順が必要でした。×印は行の右側にあり、
  1回で済みます。下にあった「削除」ボタンはこれにより不要になり
  ました。複数の行を一度に削除したい場合は、それらを選択してコン
  テキストメニューの項目を使います――何件かも表示されます。「すべて
  削除」は引き続き残ります。一覧から取り除かれるのは常に行だけで、
  ディスク上のファイルが削除されることは決してありません。
- **AI検査の前に、現在はこのパソコンがそれに向いているかどうかが
  表示されます。** これまでウィンドウには、モデルのサイズしか表示
  されていませんでした。非力なパソコンでこれを有効にした人は、最初の
  文書で初めて、それが非常に時間がかかることに気づいていました――
  5.4GBのダウンロードの後にです。現在ウィンドウは**事前に**メモリと
  空き容量、そしてそれが何を意味するかを伝えます。**事後**には速度が
  測定され、実際に扱う規模で示されます。「10ページの文書は、この
  パソコンでは約12分かかります。」遅すぎる場合、プログラムは思い
  とどまるよう勧め、この段階を再度オフにする選択肢を提示します
  ――何も禁止はしません。
- **速度測定が、現在はすべてのパソコンで実行されます。** これまでは、
  グラフィックアクセラレーションが追加で設定されている場合にのみ
  行われていました――これはWindowsにしか存在しません。それ以外の
  すべてのパソコンでは、プログラムは別のパソコンを基準に所要時間を
  推定していました――よりによって遅い場合に、その推定が外れていま
  した。
- **トルコの住所が、スキャンでも検出されるようになりました。**
  スキャンされたレターヘッドでは「34710 İstanbul」が読める状態の
  まま残っていた一方、隣の本文にある同じ情報は消えていました。文字
  認識はトルコ語の「İ」を点なしで読み取り、パターンは大文字を期待して
  いました。「Bağdat Caddesi」も同様でした。
- **独自の通り名を持たないスペインの住所が検出されるようになりました。**
  「Gran Vía 5」はそのまま残っていました。パターンが、通りの種類の
  後ろに固有名詞を期待していたためです――「Calle Mayor」にはそれが
  ありますが、「Gran Vía」ではその種類自体がすでに名前です。現在は
  「La Rambla」や「Castellana」も同様に対応します。
- **「このプログラムについて」ウィンドウに、透明性に関する案内が
  追加されました。**このアプリケーションが人工知能の支援を受けて
  開発された旨についてです。これはプログラムの成り立ちに関するもの
  であり、その動作方式についてではありません。浄化は引き続き自分の
  パソコン上でのみ行われます。
- **「言語を管理」が、現在は使える言語を先に表示するようになりました。**
  48言語のうち半分には独自の言語モデルがなく、そこでは多言語の代替
  モデルが氏名を弱く検出するか、文字体系によってはまったく検出でき
  ません。一覧に並ぶと、すべてが同等に見えてしまいました。初期設定
  では、現在は独自モデルを持つ言語のみが表示されます――「表示」から
  いつでも残りを表示でき、それぞれ何ができて何ができないかの説明が
  付きます。何かがなくなるわけではなく、制限のある言語をすでに設定
  している人は、そのまま維持されます。
- **不足している言語についての質問が、現在は解決策を示します。** まだ
  何も設定されていない言語が検出された場合、これまでプログラムは
  「読み込む」または「そのまま続ける」しか提示していませんでした。
  しかし検出結果は外れることがあります――短いフォームや、地の文の
  少ない一覧では、わずかな単語で判定が決まります。そのためウィンドウ
  には現在、「自動検出」を使う代わりに中止して正しい言語を手動で
  選択できる旨が表示されます。これにより、実は不要な言語のために
  数百メガバイトをダウンロードしてしまうことを、疑わしい場合に防げ
  ます。
- **プレースホルダーの見出しが、現在は画面表示言語に従います。**
  「[NAME_1]」「[ADRESSE_2]」などは、これまでどの言語が設定されて
  いても、文書がどの言語で書かれていても、常にドイツ語のままでした。
  現在は画面表示言語に従います――英語であれば「[NAME_1]」
  「[ADDRESS_2]」です。文書の言語には従いません。それは「自動検出」
  では推測であり、時に誤ります。画面表示言語が誤ることはありません。
- **修正時の確認が減りました。** 結果がどこに保存されるかは、現在は
  バー下部に常時表示されます(「→ vertrag_bereinigt.pdf」、ツール
  チップにフォルダ)――クリックすると、すぐに保存することなく別の
  場所を選べます。最初の保存時の確認はこれによりなくなりました。
  「すでに編集済み――最初からやり直しますか?」という質問は、その
  セッションについて記憶させることができ、単なる情報を伝えるだけ
  だった2つの通知ウィンドウは、現在はステータスバーに表示されます。
  取り消せない損害を防ぐための質問――閉じる際の未保存の作業や、削除
  されなかったテキストについての警告――は引き続き残っています。
- **結果は現在、スキャン自体が読み取れなかった箇所を伝えます。**
  スキャンされた文書では、機器の文字認識がすべてを正しく読み取る
  わけではありません――「Solarstraße 9」から「Solaret^aß« B」のように
  なることがあります。このように誤読されたものは、もう検査で発見
  できません。どんな検索パターンにも文字の羅列にしか見えないため
  です。プログラムはこれをどうすることもできませんが、現在はこうした
  箇所をページ番号付きで通知します――たいていそこにはスタンプ、
  レターヘッド、手書きの追記が隠れています。これは案内であり警告
  ではありません。組版された文書では表示されません。
- **ファイル一覧が、現在は結果の名前を表示するようになりました。**
  ファイル名の下には、処理後に浄化済みファイルの名前が表示されます
  (「→ vertrag_bereinigt.pdf」)。これまでは記録の「詳細」の下にしか
  表示されておらず、フォルダを確認した人は、未変更の原本しか見つけ
  られませんでした。元ファイルの名前はそのまま残ります――そうしないと
  結果がどのファイルから生成されたか分からなくなってしまいます。
- **完了した行のボタンが、より大きく分かりやすくなりました。** 表示、
  修正、「フォルダで表示」は、面のない平坦なアイコンで、一覧の中に
  埋もれていました――処理後に実際にクリックするのはこれらだけである
  にもかかわらずです。

### 修正

- **外国語の画面表示では、黒塗り・マスク・ハッシュ化の独自ルールが
  黙って無視されていました。** 氏名を置換ではなく黒塗りするよう設定
  していた人は、それでも置換された結果を受け取っていました――プログ
  ラムがドイツ語または英語で操作されていない場合です。設定はそこに
  あるのに機能しておらず、結果からもその違いは見えませんでした。
  影響を受けていたのは12の画面表示言語のうち9つでした。
- **「見出しの言語」設定が、ドイツ語と英語以外では機能していません
  でした。**「ドイツ語」と「英語」は選択できましたが、文書には引き
  続き画面表示の言語が使われていました。現在は3つの選択肢すべてが
  機能します。初期設定「画面表示に合わせる」は、これまでどおり同じ
  動作を提供します。
- **短いテキストの抜粋――例えばコピーされたメールの引用――では、氏名が
  そのまま残っていました。** クリップボード経由で抜粋を浄化した人は、
  多くの場合メールアドレスだけが黒塗りされ、その下の氏名は黒塗り
  されませんでした。決定的だったのは単純な行数でした。6行以上に
  なると、プログラムはその抜粋を名簿として認識し、氏名を検出しました
  ――それ未満では検出しませんでした。コピーされたメール引用は5行
  です。件名など、追加の1行があるだけで結果が変わっていました。現在
  は4行で十分であり、測定では、3分の1ではなく検査対象の氏名すべてが
  消えるようになりました。これは長い文書や地の文には影響しません。
- **AI検査のグラフィックアクセラレーションが、設定した直後にまた
  無効化されていました。** 設定後、プログラムはこのパソコンでグラ
  フィック処理がプロセッサより本当に速いかを測定します――しかし
  この測定は、それを伝えることなく常に失敗しており、「両方とも
  同じ速さ」という結果がプロセッサを選んでいました。65MBを読み込んだ
  人は、その後以前より遅くなっていました。測定は現在正常に動作し、
  失敗した場合はもう何も変更しません。
- **所要時間の見積もりが、どのパソコンでも他人の速度で計算されて
  いました。** これは同じ測定結果に基づいており、それが実行されて
  いない間は、開発機の値が使われていました。「約2分」は、遅い
  パソコンでは実際には30分を意味することもありました。
- **AI段階は、新しく大幅に改善された言語モデルで動作するようになり
  ました**(Qwen3-4Bの代わりにQwen3.5-9B)。もうドイツ語と英語に
  限定されず、12言語で動作します。検査コーパスでの測定:発見された
  項目数はこの段階なしの場合と同数でしたが、不要な黒塗りは半分以下
  になりました(75件→31件)。モデルはより大きく(2.4GBではなく
  5.4GB)、計算時間は約2倍必要です。有効化する際に一度だけ読み込まれ、
  その際に古いモデルは削除されます。
- **フランス語、イタリア語、スペイン語、ポルトガル語、ポーランド語、
  トルコ語、スウェーデン語の住所が、現在は完全に削除されます。**
  これまではそこでは通り名と地名しか消えず、番地と郵便番号は読める
  状態で残っていました(「[ORT_1] 28, 28013 [ORT_2]」)。これらの
  言語には独自の住所パターンがなく、現在追加されました。
- **ギリシャ語と韓国語では、まったく氏名が検出されませんでした。**
  ギリシャ語の場合は代替モデルが原因でした――現在読み込める独自の
  モデルにより、氏名と地名が正しく検出されます。韓国語の場合は
  プログラム側に原因がありました。氏名は大文字で始まるという前提が
  あり、ハングルには大文字という概念がありません。特に影響を受けて
  いたのは、表のセル、フォームフィールド、リスト項目といった短い
  単位でした。
- **読み込めない言語モデルが、浄化処理全体を中断させていました。**
  現在はエラー通知の代わりに多言語モデルが代わりに動作し、結果は
  より弱い検出で処理されたことを示します。現在該当するのは中国語と
  日本語で、これらのモデルにはまだプログラムに同梱されていない単語
  分割機能が必要です。
- **独自モデルを持つ言語が、他の何らかの言語が読み込まれるだけで
  「インストール済み」として扱われていました。** 例えばトルコ語を
  設定すると、それとともに多言語の代替モデルが読み込まれ――中国語、
  日本語、韓国語、ギリシャ語は、その独自モデルが不足しているにも
  かかわらず、一覧でチェック済みかつ「0MB」と表示されていました。
  これにより、これらの言語は決して再取得されることがなく、恒久的に
  より弱い代替モデルで動作していました。現在は一覧が、読み込み
  サイズも含めて実際の状態を表示します。
- **失敗した検出段階が、沈黙していました。**「高度な検出」または
  「最大検出(AI)」が有効になっていて、そのモデルが実行できなかった
  場合、プログラムはその段階なしで処理を続けていました――何の説明も
  ありません。結果は他のものと同じように見え、スイッチは引き続き
  「オン」のままでした。つまり、基本段階の結果を、得られる最良の
  ものだと思い込んでしまっていたのです。現在は結果がその旨を伝え、
  両方――何が検査されなかったか、そしてどうすればモデルを再読み込み
  できるか――を示します。このケースは珍しくありません。一部のパソコン
  では、グラフィックアクセラレーションがない場合、AI段階の読み込みが
  失敗します。
- **追加モデルの読み込みエラーが、浄化処理全体を中断させていました。**
  「高度な検出」では、モデルの評価だけが保護されており、その読み込み
  自体は保護されていませんでした――そしてまさにそこで、ファイルが
  破損していたりパソコンに合わなかったりすると問題が起きます。現在は
  エラー通知の代わりに、案内付きの基本段階の結果が生成されます。
- **言語が削除できなくなり――そのため再取得もできなくなっていました。**
  「言語を管理」でチェックを外して変更を反映すると、「ドイツ語を
  削除しました」と表示されるにもかかわらず、チェックがすぐに再び
  入ってしまっていました。原因は、プログラムフォルダからの取り込み
  処理にありました。全ユーザー向けインストールでは、言語モデルは
  読み取り専用でプログラムフォルダに置かれ、プログラムは不足分を
  そこから取得します。数百メガバイトを新たに読み込む代わりにです。
  この取り込みはアクセスのたびに実行されており――まさに削除した
  ばかりの言語を同じタイミングでコピーし戻していました。現在は
  一度だけ行われます。再取得された言語モデルはこれによって保持され
  ます。さらに、プログラムは削除後に確認するようになりました。削除
  できなかったものは、現在「削除済み」ではなく失敗として通知され
  ます。
- **全ユーザー向けインストールでは、再取得したものを配置できません
  でした。** プログラムを全ユーザー向けにインストールすると、それは
  「Program Files」に置かれ、そこには管理者権限なしでは何も書き込め
  ません。言語モデルについてはとっくに代替の保存先が用意されていま
  したが、他のものについてはそうではありませんでした。
  - **ページプレビュー用コンポーネント**は、290MBのダウンロード後に
    プログラムフォルダに展開しようとして、理由も告げずに失敗して
    いました。現在は言語モデルと同じ場所に置かれます。本来そこに
    置かれるべきだったとおりです。
  - **グラフィックアクセラレーション**は代替先に逃げられません。
    プログラム自体の中でライブラリを入れ替えるためです。まず読み
    込んでから何も言わずに失敗する代わりに、現在はプログラムが
    事前に、ここではそれができないこと、それが何を意味するかを
    伝えます――最大検出は引き続き動作しますが、プロセッサ経由に
    なります。
  - 同梱の**文字認識の言語**が削除できませんでした。プログラム
    フォルダから即座に復元されてしまっていたのです。言語モデルと
    同じ原因、同じ修正です。
  - 言語を削除する際に、**別のTesseractインストールの言語データ**が
    削除されてしまうことがありました。現在は自分自身のフォルダ
    だけが対象になります。
  - 代替の保存先はこれまでWindowsでのみ有効でした。`/opt`への
    Linuxアーカイブには、同じ問題があるのに同じ回避策がありません
    でした。
- **修正時に、1つの単語しか枠に入れていないのに行全体が消えることが
  ありました。** すでに浄化済みのファイルでプレースホルダーを黒塗り
  した人は、それがあった行を丸ごと失っていました――「Sehr geehrte
  Frau Doktor [NAME_1]」からは何も残らず、通知には「文書から単語を
  1つ削除しました」と表示されていました。影響を受けたのは、すでに
  一度プログラムを通過したすべてのファイル――つまり、まさに修正機能
  が必要とされる場合でした。残りのテキストは現在、変更されない位置
  にそのまま残ります。
- **氏名一覧の上の「EMPLOYEES」が、それ自体黒塗りされていました。**
  0.10.19の「MITARBEITER」と同じケースの英語版で、そこでは残された
  ままでした。大文字表記では言語モデルに区別の手がかりが欠け、その
  見出しはすべて本物の氏名が並ぶ上に位置しています。その下の氏名は
  引き続き検出されます。「staff」は登録されませんでした。これは
  実在する姓であり、この項目を登録すると「John Staff」がすべて巻き
  込まれてしまうためです――かつて「Arbeiter」で行ったのと同じ判断
  です。
- **法人格が2回置換されていました。** スキャンされたレターヘッドで、
  言語モデルは「GmbH」、住所、郵便番号を**1つ**の地名として読み
  取っていました。住所と郵便番号がその後それぞれの部分を切り出し、
  残った法人格が独自のヒットになっていました。結果には
  「[ORT_1] [ORT_2]」と表示され、本来は「[ORT_1] GmbH」を意図した
  ものでした。会社名は引き続き置換されます――単独の付加語だけが
  現在そのまま残り、結果はレターヘッドらしく読め、穴埋め問題のよう
  には見えなくなりました。
- **切り詰められたヒットが、再検証されていませんでした。** 上記の
  ケースの原因であり、それはさらに広がっています。推測されたヒット
  に対するフィルターは、検出器が**報告する**内容に対して動作して
  おり――重なり解消後に残るものに対してではありませんでした。長い
  ヒットがより強力な検出器によって切り詰められると、その断片は判定
  された内容とは異なるテキストになりますが、誰もそれを再度確認して
  いませんでした。現在は確認されます。
- **「最新のフッションを使用しています」――まったく確認できていない
  にもかかわらず表示されていました。** 更新チャンネルとして「プレ
  ビュー(ベータ)」または「安定版――推奨」を設定していた人は、この
  回答を受け取っていました。しかしこれらのチャンネルには、これまで
  何も公開されていませんでした。現在はプログラムがまさにその旨を
  伝え、設定で別のチャンネルを選ぶよう提案します。
- **読み込み中にウィンドウを閉じると、スレッドがクラッシュしていま
  した。** Maskuroを起動してすぐにウィンドウを閉じた人は、言語モデル
  がまだ読み込まれている最中であっても、記録にエラー報告が残ってい
  ました。読み込み処理が、すでに存在しないウィンドウに応答しようと
  していたのです。目に見える影響はありませんでしたが、記録には
  クラッシュとして記載されていました――実際には誰かがプログラムより
  速かっただけです。
- **結果は現在、確認され、単に読み返されるだけではなくなりました。**
  これまでは、テキスト内に値がもう存在しなければページはきれいだと
  みなされていました。スキャンではそれは証拠になりません――そこでは
  目に見えるテキストは画像だからです。そのため最後に、結果内の
  該当領域が本当に黒塗りされているかが確認されます。そこに明るい
  紙面が残っている場合、報告書は「置換済み」と伝える代わりに、その
  旨を明示的に伝えます。
- **置換された項目が、画像内に残ったままでした。** 値が画像上にあった
  場合――スキャンされたレターヘッド、スタンプ、スキャンされたページ
  全体など――文書のテキストからは削除されていましたが、依然として
  **見える**状態でした。人が読むのはそこでは画素です。それでも報告書
  は「置換済み」と伝えていました。現在は、どの戦略が設定されていて
  も、画像内の該当領域が黒塗りされ、プレースホルダーがその背景の上に
  明るく表示されます――見た目は美しくありませんが正直であり、対応
  関係は保持されます。画像形式が編集できない場合、結果は現在、
  きれいに見える代わりにその旨を明示的に伝えます。
- **スキャン上でプレースホルダーが完全に欠落していました。** スキャン
  されたページのテキストレイヤーは見えない形で描画されており、その
  中に挿入されたプレースホルダーもそれを引き継いでいました。設定は
  されているが見えない、という状態です。その発見箇所には、その後
  何も表示されていませんでした。
- **そもそも実行できなかった文字認識が、成功として扱われていました。**
  言語データが不足していたり、認識エンジンが中断したりした場合でも、
  報告書には「画像は文字認識で確認されました(発見箇所0件)」と表示
  されていました――つまり一度も行われなかった検査です。スキャンでは
  これが唯一の検査です。ページ画像内に読める住所がある契約書が、
  これによって完了とみなされていました。現在は報告書が、何も確認
  されなかったこと、そしてその理由を伝えます。
- **言語データが、誤ったフォルダで検索されていました。** 自分の言語
  フォルダに、文書とは異なる言語が入っていた場合、認識エンジンには
  まさにこのフォルダが渡され、失敗していました――適切な言語がその
  すぐ隣にあったにもかかわらずです。現在検索されるのは**言語**で
  あり、フォルダではありません。
- **削除されなかったテキストに関する警告が、存在しない機能を勧めて
  いました。** これは「PDFとして黒塗り」を指していましたが、これは
  *Office*ファイルのPDF表示を生成する機能であり、PDF自体には
  そもそも用意されていませんでした。この警告に従おうとした人は、
  見つけられませんでした。現在はその処理を実行するボタンが表示され
  ます。
- **エディタで、バーとプレースホルダーが選択箇所の隣にずれて表示され
  ていました。** 行がハイフンで終わり、単語が次の行に続くすべての
  PDFが影響を受けていました――スキャンでは特に目立ちます。契約文書は
  一貫してハイフネーションで組まれているためです。2つに分かれた行の
  半分が*1つの*単語として、版面全体にまたがって扱われ、その近くの
  すべての枠がこの広がりを引き継いでいました。検出処理自体はこれに
  よって変わりません――測定コーパスは以前と同じ結果を返します。
- **エディタが、テキストがすでに削除されているにもかかわらず「まだ
  文書内に存在します」と警告していました。** 同じ単語がページ内に
  複数回出現する場合(契約書ではよくあることです)、自己検証は各操作
  の後に失敗を報告していました。現在は出現回数をカウントするように
  なり、単にその単語がどこかにまだ存在するかを確認するだけではなく
  なりました。本当の失敗の場合は、これまでどおり警告されます。
- **結果ファイルが、どの言語でも「_bereinigt」という名前になって
  いました。** 本来の意図は、この名前の付加部分が画面表示言語に
  従うことでした――英語では実際にそうなっていました
  (「_cleaned」)が、残り16言語ではそうなっていませんでした。
  フィンランド語でプログラムを使っていた人は、
  「asiakirja_bereinigt.pdf」を受け取っていました。現在はファイル名
  は「asiakirja_puhdistettu.pdf」となり、日本語では
  「書類_除去済み.pdf」というようになります――それぞれ、同じ画面
  表示が完了通知で使う単語です。独自の付加部分を設定している人は、
  それがそのまま維持されます。
- **「言語を管理」が、常にドイツ語で表示されていました。** 48の文書
  言語の一覧には、どの画面表示言語が設定されていてもドイツ語名が
  表示されていました。フィンランドの利用者には「Chinesisch」と
  表示されていました。現在は自分の言語での名称と、その後ろに自称
  (現地語名)が表示されます――「Kiina(中文)」のようにです。この
  自称表示は意図的なものです。自分の言語の自称でその言語を認識する
  人は、フィンランド語の単語に見覚えがなくても見つけられます。

## 0.10.19 – 2026年8月12日

### 改善

- **コンテキストメニューの項目が、現在ご利用の言語で表示されるように
  なりました。** これまでは、どのシステムでもドイツ語の表記が使われて
  いました――英語版のWindowsでもです。現在は設定されている表示言語に
  従い、言語を切り替えると再インストールなしですぐに項目名が変更され
  ます。(Windows。macOSとLinuxでは、メニュー名が同時にファイル名でも
  あるため、これは後日対応します。)
- **エディタは、最後にどの表示で作業していたかを記憶するようになり
  ました。** ページプレビューを使っている人は、次の文書でも自動的に
  それが有効になり、毎回オンにし直す必要がありません。一度も使った
  ことがない人には何の影響もありません。必要な部品がすでに読み込まれて
  いる場合にのみ復元され、そのために何かが新たに読み込まれることは
  ありません。

### 修正

- **氏名一覧の上にある「MITARBEITER(従業員)」という語が、それ自体
  黒塗りされていました。** 従業員名簿や組織図では、この見出しが
  見せかけの氏名として消えていました――実際にすべて本物の氏名が並ぶ
  一覧の上に位置しており、大文字表記では言語モデルにとって区別の
  手がかりが失われるためです。その下にある氏名は引き続き検出されます。
- **数量表現が、住所と誤認されていました。** 請求書、納品書、在庫一覧
  では、「3390 Protokoll」「1030 Betrag」「3390 Lager」のような項目が、
  見せかけの郵便番号と地名として消えていました――4桁の数字は、どんな
  数量でもオーストリアの郵便番号のように見えてしまいます。数字の後ろに
  続く語が、アプリケーションが一般名詞・部署名・業務内容・項目見出しと
  して認識している語である場合、現在はそのまま残ります。本物の地名は
  影響を受けません。それが同時にそのような語でもある場合も同様です
  (「4692 Ort」)。ただし、数字の後ろにごく普通の語が続く場合(「3390
  Regal」)は依然として解決されていません――これには郵便番号一覧が
  必要です。
- **ヘルプが、存在しないメニュー項目について説明していました。**
  マニュアル、画像、インストール完了時の通知は「AI用に文書を浄化」に
  ついて述べていましたが、コンテキストメニューの項目名は実際には
  「個人データを削除」です。ヘルプに従った人は、探しても見つかりません
  でした。この3箇所すべてで、メニュー項目名が実際の名称どおりに
  表示されるようになりました。
- **「システムと同時に起動」をオフにできませんでした。** インストール時に
  「Windowsと同時に起動」にチェックを入れた人は、設定画面では空の
  チェックが表示されており――さらに深刻なことに、アプリケーション内で
  オン・オフを切り替えても効果がなく、プログラムは引き続きWindowsと
  ともに起動していました。原因は、Windowsが起動プログラムを確認する
  場所が2箇所あり、アプリケーションはそのうち1箇所しか把握していな
  かったことです。現在は両方がカウントされ、スイッチは実際の状態を
  表示し、両方向に機能します。さらに、タスクマネージャーで項目をオフに
  した場合、それが現在アプリケーション内にも反映されます。そこで再度
  オンにすると、そのオフ設定は解除されます。
- **氏名一覧の上の見出しが黒塗りされていました。** 「TEILNEHMERLISTE
  WERKSTATTGESPRÄCH(参加者一覧・作業会議)」や「MITARBEITERÜBERSICHT
  INNENDIENST(従業員一覧・内勤)」のような、人物の一覧の上にある見出しが、
  見せかけの氏名として消えていました。大文字表記では言語モデルにとって
  最良の識別手がかりが失われ、ドイツ語ではすべての名詞が大文字で
  始まるため、「Teilnehmerliste Werkstattgespräch」が「Anna Huber」の
  ように見えてしまうのです。`-liste`、`-dienst`、`-gespräch`、
  `-sitzung`、`-besprechung`で終わる合成語は現在そのまま残ります。
  基本となる単語のみの場合は引き続き氏名として扱われます。*Liste*と
  *Dienst*は実在する姓ですが、*Teilnehmerliste*はそうではありません。
- **縦書きの項目には、読めないプレースホルダーが付いていました。**
  ページ余白の書類番号、綴じ代の脇にある担当者略号、縦書きの表見出し
  ――こうした項目は検出され削除されてはいたものの、プレースホルダーは
  テキストに対して横向きに、1~2ポイントに押しつぶされた状態で、時には
  用紙の端を越えて表示されていました。現在はテキストに沿って表示され
  ます――縦向きに、読める大きさで、その項目が実際にあった向きと同じ
  方向にです。同じことは、後から回転されたページ(一部の出力プログラムが
  生成する、横書きテキストにページ回転情報が記録されている場合)にも
  当てはまります。そこでも、プレースホルダーは現在、ページを見るときの
  向きに合わせて表示されます。「Sehr geehrte Frau Doktor Anneliese
  Berger」では「Anneliese」だけが氏名として抽出され、「Berger」は文書に
  残っていました。これは、ミドルネームを持つあらゆる氏名(「Frau Anna
  Maria Berger」)にも同様に当てはまりました。原因は、敬称の後ろにくる
  氏名に対する規則にありました。この規則は2つの語の位置しか想定して
  おらず、肩書きやミドルネームが最初の位置を使ってしまっていたのです。
  「Dr.」の場合は問題になりませんでした――ピリオドが規則を打ち切り、
  言語モデルが氏名全体を捉えていたからです。現在は肩書きは位置を
  消費せずに読み飛ばされ、氏名は3つの部分から成ることが許されます。
  氏名の**後ろ**にくる役職は引き続き対象外です。「Frau Anna Huber
  Geschäftsführerin」では、役職ではなく氏名が置換されます。
