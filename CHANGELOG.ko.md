# 변경 사항

버전마다 무엇이 달라지는지 – 애플리케이션의 겉모습 기준으로 설명하며, 내부
구조 기준이 아닙니다. *무엇으로* 만들어졌는지 알고 싶다면
[LIZENZEN.md](LIZENZEN.md)를 참고하십시오. 여기에는 이 프로그램을 다루는
작업에서 무엇이 달라지는지가 적혀 있습니다.

버전 번호는 일반적인 방식을 따릅니다. **첫 번째** 숫자는 기존 방식대로
작동하지 않게 될 때, **두 번째** 숫자는 새로운 기능이 추가될 때, **세 번째**
숫자는 오류가 수정될 때 바뀝니다.

---

## 0.10.52-alpha.20260903 – 2026년 9월 3일

- 파일이 네 개를 넘는 일괄 작업은 여러 미리보기 창에서 검토를 마친
  뒤에도 더 이상 멈추지 않습니다. 다음 문서들도 계속 백그라운드에서
  준비됩니다. 각 창에서 답한 뒤에는 해당 파일이 이제 안정적으로 끝까지
  처리되고 일괄 작업의 다음 자리가 비워집니다.
- PDF의 짧은 회사 표기 대조는 이제 인식된 이미지 텍스트에만 적용됩니다.
  따라서 일반 페이지 텍스트에서는 길고 여러 줄에 걸친 모델 탐지 때문에
  다른 위치의 동일한 단어까지 추가로 가려지지 않습니다.

## 0.10.51-alpha.20260903 – 2026년 9월 3일

- 변경 목록은 이제 사용자의 언어로 표시됩니다 – maskuro.com/neuigkeiten와
  프로그램의 「변경 사항」, 업데이트 후의 「새로운 점」에서 볼 수 있습니다.
  지금까지는 18개 언어판 모두에서 번역된 제목 아래에 독일어 본문이
  표시되었습니다. 아직 번역이 없으면 해당 버전은 사라지는 대신 독일어로
  남습니다. 버전 목록은 어디서나 동일합니다.

## 0.10.50-alpha.20260903 – 2026년 9월 3일

- 반복되는 회사 로고는 PDF에서 이제 일관되게 정리됩니다 – 텍스트
  인식이 페이지마다 로고 문구를 다르게 읽거나 둥근 심벌을 아예
  놓친 경우에도 마찬가지입니다. 미리보기에서 명시적으로 선택
  해제한 것은 계속 유효하며, 이후 어떤 재검토로도 되돌려지지
  않습니다.
- 스캔된 표의 통화 없는 가격은 표 머리글과 값이 서로 다른, 겹치는
  PDF 이미지에 있는 경우에도 완전히 마스킹됩니다. 수량, 시간,
  무게, 퍼센트는 그대로 남습니다 – 멀리 떨어진 숫자가 잘못 하나의
  금액으로 합쳐지는 일도 더 이상 없습니다.
- 서명 검색은 이제 근거가 확인된 옅은 파란색 필기체와 좁은 빨간색
  서명 약자도 인식합니다. 점선 다이어그램, 측정 곡선, 도장, 로고,
  넓은 빨간색 편집 표시는 이 좁은 재검토에서 계속 제외됩니다.
- 회전, 반전, 기울임, 잘림이 있는 PDF 이미지의 마스킹이 이제
  실제 이미지 다각형에 정확히 적용됩니다. 작업 항목의 기술적
  직무, 차량과 타이어 관련 사물 정보, 그리고 기술적인
  "보상(Kompensation)"도 동시에 오탐에 대해 더 엄격하게 구분됩니다
  – 명시적으로 라벨이 붙은 연락처 직무와 전화번호는 계속
  보호됩니다.
- PDF 저장 전 시각 검증이 더 이상 창을 멈추게 하지 않습니다 –
  검출 위치가 많은 대용량 문서에서는 지금까지 여러 초 동안 아무
  반응이 없었습니다. 이제는 검사 중이라는 안내가 표시되고 창은
  계속 그려집니다.
- 후속 수정 편집기에서 이미지 안의 값을 되돌릴 때 이제 각 원본
  이미지는 텍스트 인식을 한 번만 거칩니다 – 지금까지는 같은
  이미지에 대해 추가로 되돌릴 때마다 다시 실행되었습니다.
- 고급 단계와 서명 모델의 다운로드는 이제 메모리를 거의 필요로
  하지 않습니다 – 596MB짜리 패키지는 지금까지 전체가 메모리에
  담긴 채 검증되고 그 안에서 압축이 풀렸습니다 – 실행 중인
  프로그램에서 최대 1기가바이트 이상, 8GB 메모리의 컴퓨터에서는
  모든 것이 멈추기 시작하는 순간이었습니다. 이제는 블록 단위로
  디스크에 흘려보내며, 그곳에서 검증되고 압축이 풀립니다.
- 후속 수정 편집기의 검색은 더 이상 대용량 PDF를 멈추게 하지
  않습니다 – 검색 필드의 첫 글자는 지금까지 모든 페이지를 한
  번에 읽어들였습니다 – 200페이지에서는 창이 2초간 멈췄고, 마스킹
  이후마다 또 한 번 멈췄습니다. 이제 페이지는 조금씩 읽히며,
  그동안 카운터에는 "읽는 중…"이 표시됩니다 – 결과는 동일합니다.
- 래스터화된 PDF 페이지 – 텍스트 인식 후이거나 텍스트를 깔끔하게
  제거할 수 없었던 경우 – 는 이제 이미지 손실 없이 훨씬 작게
  저장됩니다: 항상 JPEG로 저장하는 대신 각 페이지는 무손실
  인코딩도 시도하며, 더 작은 쪽이 파일에 들어갑니다. 정리된
  스캔은 이렇게 248KB에서 48KB로, 텍스트 인식을 거친 실습
  문서는 913KB에서 702KB로 줄어듭니다 – 텍스트는 여전히 선명하게
  유지됩니다.
- 나중에 로드되는 모델(고급 단계, 서명, 얼굴, 두 번째 텍스트
  인식)은 정리 작업 없이 10분이 지나면 메모리에서 다시 해제됩니다.
  지금까지는 프로그램이 종료될 때까지 로드된 상태로 남았습니다 –
  한 번이라도 서명 검색과 고급 단계를 사용한 사람은 계속 2기가바이트
  이상을 점유하고 있었습니다. 다음 실행에서는 1~2초 안에 다시
  로드됩니다 – 상태 표시줄이 이를 알립니다.
- PowerPoint: 슬라이드 레이아웃과 슬라이드 마스터의 일반 명칭("빈
  화면", "제목 슬라이드")은 더 이상 정보로 간주되어 대체되지
  않습니다. "빈 화면(Leer)"은 지명이기도 해서 모든 독일어와 영어
  프레젠테이션에서 잘못 마스킹되었습니다 – 이제는 슬라이드 자체에
  수동으로 붙인 이름만 정리됩니다.
- PDF에서 줄 정리 기능이 더 이상 다음 줄의 머리 부분을 검출
  결과에 끌어들이지 않습니다: 날짜 뒤에 오는 다음 목록 항목의
  번호가 전화번호로 간주되었고, 숫자 뒤의 "Kenncode"나
  "Auftragsnummer" 같은 필드 머리글이 지명이 포함된 우편번호로
  간주되었으며, 주소 아래의 지명 줄이 지명을 중복시켰습니다. 그
  때문에 실제로 올바른, 더 짧은 검출이 밀려났습니다. 132개의
  코퍼스 PDF에서 추가 정리 검출 24건 중 진짜인 2건만 남습니다 –
  실무 코퍼스에서는 오탐이 29건에서 21건으로 줄었고 검출률은
  동일합니다.
- 후속 수정 편집기의 "PDF 폴더 검색 및 마스킹"은 더 이상 창을
  차단하지 않습니다: 실행은 백그라운드에서 이루어지고, 진행 표시와
  취소 버튼이 반응하며, 절반만 완료된 파일 도중에 메뉴나 탭을
  조작할 수 없게 되었습니다.
- 검출 위치가 있는 스캔된 페이지는 마스킹 시 이제 두 번이 아니라
  한 번만 다시 작성됩니다 – 지금까지는 프로그램이 검출 위치의
  상자와 근거 상자를 두 단계로 채웠고, 두 번째 단계는 방금 새로
  저장된 스캔 이미지를 다시 한번 압축했습니다. 이는 대형 스캔에서
  시간을 절약하고 이미지 품질 손실을 막습니다.
- 후속 수정 편집기의 페이지 넘기기, 확대/축소, 축소판은 더 빠르게
  반응합니다: 렌더링된 각 페이지는 지금까지 표시되기 위해서만
  PNG로 압축되었다가 다시 풀렸습니다 – 고해상도 화면에서는
  페이지당 약 0.1초였습니다. 이제 이미지는 픽셀 단위로 동일하게
  직접 전달됩니다.
- PDF 저장 전 시각 검증("출력 검사")은 같은 결과로 약 3배 더
  빨라졌습니다.
- 메인 창은 다시 약 0.25초 더 빨리 표시됩니다: 이 컴퓨터에서
  텍스트 인식이 준비되었는지 확인하는 검사는 창을 구성하는 동안
  실행되었습니다 – Mac에서는 시스템 인식에 대한 시험 요청도
  포함되었습니다 – 그리고 추가 구성 요소 설정 페이지는 이를 위해
  48개 언어의 상태를 모두 조회했습니다. 이제 두 작업 모두
  백그라운드에서, 또는 언어 목록이 실제로 열릴 때만 실행됩니다 –
  그때까지는 "텍스트 인식 확인 중…"이 표시됩니다.
- 서명 검색 후 프로그램은 메모리를 약 300MB 덜 사용합니다: 인식
  모델은 지금까지 메모리에 두 번 로드되었습니다 – 한 번은 진위
  확인용, 한 번은 계산용. 검증은 계속되지만, 두 번째 사본 없이
  이루어집니다.
- PDF의 텍스트 인식이 눈에 띄게 빨라졌습니다: 페이지의 각 필드
  머리글("Geburtsdatum:", "Steuernummer:")마다 지금까지는 정보
  종류별로 인식을 통한 별도 검사가 실행되었습니다 – 같은 머리글이
  10페이지 전에 이미 나왔더라도 매 페이지 새로. 이제는 응답이
  기억됩니다 – 2페이지짜리 견적서는 지금까지 324번 질의했지만,
  이제는 서로 다른 것만 질의합니다. 검출 결과는 동일합니다.
- 대형 표는 다시 몇 분이 아니라 몇 초 만에 정리됩니다: 익명화
  모드(기본값)에서는 이미 알려진 값과의 대조가 셀이 늘어날수록
  느려졌습니다 – 검출마다 임시 캐시가 폐기되고 다시 만들어졌기
  때문입니다. 셀 5,000개에는 약 18초가 걸렸지만, 이제는 0.5초입니다
  – 결과는 문자 단위로 동일합니다.
- 메인 창이 다시 눈에 띄게 더 빨리 나타납니다: 설정의 국가 목록은
  창을 구성할 때 전체 인식 라이브러리를 앞으로 끌어왔습니다 – Mac에서
  약 0.7초, Windows에서는 그보다 더 걸립니다 – 그러나 실제로
  필요한 것은 국가 이름뿐입니다. 이제 목록은 가벼운 카탈로그에서
  가져오며, 라이브러리는 창이 이미 표시된 상태에서 백그라운드에서
  예정대로 로드됩니다. 이는 프로그램을 재시작시키는 언어나 화면
  모양 전환 이후에도 마찬가지로 적용됩니다.
- 문서 검증소는 이제 잘린 필드 머리글, 로컬 값 그림자, 강한 스캔
  잘림도 PDF, DOCX, ODT 컨테이너 전체에서 완전히 검증합니다.
  매트릭스는 40개 문서군과 17개 컨테이너 축에서 나온 680개
  파일을 포함합니다. Maskuro는 새로 추가된 프로필과 완전한 기본
  및 특징 프로필 모두에서, 측정된 오탐, 손상된 보존 대상, 중단
  없이 모든 목표 정보를 제거합니다.

- 여러 번 사용된 스캔은 이제 눈에 보이는 배치마다 검증되고
  정리됩니다: 문서 검증소는 PDF에서 같은 이미지 객체를 여러
  페이지, 크기, 회전 상태에 걸쳐 공유하고, DOCX와 ODT에서는 같은
  이미지 부분을 여러 번 참조합니다. "Formularscan klein quer" 같은
  기술적인 ODT 프레임 이름은 더 이상 사람으로 간주되지 않습니다 –
  비슷하게 시작하는 자유 이름과 지명은 계속 보호됩니다. 최종 PDF
  페이지 검증의 일반적인 양식 추정은 이미 독립적으로 읽힌 이미지
  영역에서 더 이상 큰 주소 오탐을 만들지 않습니다. 새로 추가된
  120개 컨테이너는 기본 및 특징 프로필에서 813건 및 840건의
  목표 정보를 오탐, 보존 침해, 중단 없이 모두 달성합니다 – 완전한
  800개 파일 특징 검수는 5,600/5,600을 확인합니다.

- 독일어 OCR 검증소는 이제 40개 문서군에서 나온 560개 스캔을
  포함합니다. 새로운 변형은 필드 머리글과 페이지 여백을 잘라내거나
  값 바로 위에 그림자를 놓습니다. Maskuro는 이때 이름, 주소,
  생년월일, 의료 코드, 그리고 라벨이 부분적으로 손상된 식별
  번호도 보호합니다. 동시에 양식 필드 잔여물, 공식 제목, 사실적인
  법률·정보 용어는 더 이상 사람이나 지명으로 대체되지 않습니다.
  완전한 기본 및 특징 프로필은 오탐이나 중단 없이 3,794/3,794건
  및 3,920/3,920건의 목표 정보를 달성합니다.

- 자동 PDF 이미지 선택은 대형 제품 사진, 에너지 라벨, 인물 사진
  열을 단지 페이지 상단에서 시작한다는 이유만으로 더 이상 제거하지
  않습니다. 실제 얇은 머리글/바닥글 이미지와 종이 가장자리에서
  시작하는 편지지 머리글은 계속 처리됩니다. 직원 명부에서는
  눈에 보이는 문서 제목이 이미지로만 존재하는 경우에도, 구조적으로
  반복되는 항목에서 이름이 인식됩니다. 인식 기능은 더 이상 두
  가지 구체적인 직무 단어와 "DW"라는 약자에만 맞춰져 있지 않습니다
  – 1~4개의 줄바꿈된 직무명과 "Durchwahl", "Nebenstelle", "Ext.",
  "Extension"이 공통된 구조에서 도출됩니다. 언어 모델이 겹침
  해소 후 직무를 나타내는 형용사 하나만 남기더라도, 직무명과
  구획 머리글은 그대로 남습니다. 가로 방향의 직무 격자는 더
  이상 잘못 이름 열로 간주되지 않습니다. 페이지 OCR이 여러 카드를
  매우 넓은 대문자 단어 하나로 붙여버린 경우, 좁은 지역적 재검토가
  실제 단어 상자를 분리합니다 – 그 결과 단일 이름도, 넓은 오류
  막대도 남지 않습니다. 여러 줄에 걸친 반복 회사 로고는 이미
  확인된 동일한 픽셀 견본을 기준으로, 사용할 만한 OCR 텍스트가
  없는 페이지에서도, 최대 2픽셀의 위치 편차까지 마스킹됩니다 –
  동시에 짧은 지역적 OCR 재판독은 더 이상 더 큰 머리글 영역을
  꾸며낸 이름으로 보완할 수 없습니다. 회사 편지지 머리글 앞의
  페이지 번호는 더 이상 조직명에 포함되지 않으며, 숫자로 시작하는
  실제 브랜드명은 계속 보호됩니다. 측정된 여러 제품·전문·양식
  단어는 더 이상 사람으로 제안되지 않습니다.

- 서명 검색은 PDF에서 이제 OCR 이미지 정리 이후에 실행되며, 일반
  텍스트 검출이 없는 페이지도 방문하고, 회전된 페이지의 검출
  상자를 문서 좌표로 올바르게 역산합니다. 빽빽한 제품 사진은 더
  이상 서명으로 마스킹되지 않습니다. 명확하게 라벨이 붙은 서명
  필드 위에서는 좁은 선 대체 로직이 얇은 모델 공백을 메웁니다 –
  미리 인쇄된 날짜가 있는 빈 줄은 이를 유발하지 않습니다. OCR
  검출과 서명 검출만 있는 순수 스캔은 이 단계에서 더 이상 텍스트
  분기에서만 로드되는 이미지 마스킹 도구 때문에 중단되지 않습니다.

- 동시에 열린 많은 문서가 후속 수정 편집기에서 계속 구분 가능하게
  유지됩니다: 탭은 더 이상 생략 부호 하나로 줄어들지 않으며,
  오른쪽의 목록 버튼이 모든 전체 파일 이름을 세로로 보여줍니다.
  탭은 드래그로 순서를 바꿀 수 있고, 메인 창과 같은 목록에서
  X 표시로 제거할 수 있습니다 – 저장되지 않은 작업은 이때 계속
  먼저 확인됩니다. 오른쪽 클릭 메뉴에는 "닫기", "다른 탭 닫기",
  "오른쪽 탭 닫기"도 제공됩니다.

- 바이러스 검사기나 검색 색인으로 인한 일시적인 Windows 잠금은
  더 이상 완전히 로드된 언어 모델 또는 사전 폴더를 최종 적용
  단계에서 "액세스 거부"로 실패하게 만들지 않습니다. Maskuro는
  이제 이 마지막 폴더 전환을 짧은 시간 동안 다시 시도합니다.

- 독일어 문서 검증소는 이제 PDF 페이지 회전 변화, 독립적으로
  회전된 PDF 이미지, 그리고 DOCX와 ODT의 크기 조절 및 잘려나간
  표 이미지도 검증합니다. 눈에 보이게 회전된 이미지의 필드 값이
  다시 완전히 인식되고, 기술적인 열 식별자는 더 이상 지명으로
  대체되지 않으며, 같은 성을 공유하는 이름은 일관성 재검토에
  의해 더 이상 중복된 부분 검출로 쪼개지지 않습니다. 320개 파일로
  두 배가 된 매트릭스는 날짜·금액·의료 인식을 함께 켰을 때
  2,240/2,240건의 목표 정보를 오탐이나 중단 없이 달성합니다.

- 여러 페이지로 된 이미지 PDF, 텍스트·이미지가 혼합된 PDF, 그리고
  DOCX나 ODT에 삽입된 스캔은 이제 40개 독일어 문서군 전체에 걸친
  자체 160개 파일 검증소에서 검증됩니다. 기술적인 ODT 프레임
  이름과 라벨이 붙은 기기 코드는 더 이상 지명으로 대체되지 않습니다
  – 같은 구조 안의 실제 이름, 지명, 주소는 계속 보호됩니다. 의료
  또는 금액 인식을 켠 경우, 바로 뒤따르는 복용량이나 지불 주기도
  완전히 제거됩니다. 컨테이너, 텍스트 기본, 텍스트 특징, OCR
  특징 실행은 함께 각각의 완전한 목표치를 오탐이나 중단 없이
  달성합니다.

- 저장 전 보안 검사는 이제 눈에 띄는 PDF 위치를 개별적으로 선택할
  수 있는 목록으로 보여줍니다. "편집기에서 확인"은 정확히 선택된
  페이지를 열고 해당 영역을 표시합니다 – 같은 위치의 겹치는 부분
  검출은 한 번만 표시됩니다. 새로운 조작 문구는 번역된 17개
  화면 언어 모두에서 완전히 제공됩니다.

- Markdown 파일은 교체 시 참조, 강조, 각주 구문을 그대로
  유지합니다. Maskuro는 이를 위해 Markdown 표시가 없는, 문자
  수가 동일한 버전을 읽습니다 – 이메일 주소의 밑줄, 계산용
  별표, 개인정보가 없는 일반 참조는 변경되지 않습니다.

- 같은 PDF 페이지의 여러 손글씨 항목이 이제 최대 3회에 걸쳐
  검색됩니다. 이미 찾은 필체는 작업 이미지에서만 숨겨져, 더
  약한 서명을 밀어내지 않습니다 – 회전된 페이지에서는 마스킹
  영역이 다시 눈에 보이는 검출 위치에 정확히 놓입니다. 이전
  보안 단계의 이미지 채움은 이후 다시 기록될 때도 유지됩니다.

- "모든 설정 초기화"는 이제 "이미지 속 텍스트"도 포함합니다. OCR
  구성 요소가 없으면 이 스위치는 기술적으로 꺼진 상태로 유지되며,
  출고 상태와 다르다고 잘못 표시되지 않습니다.

- 페이지 상단의 큰 이미지 조각은 더 이상 단지 그 위치만으로
  머리글로 간주되지 않습니다. 이로써 특히 이미지 기반의 상품
  설명과 표 내용이 그대로 유지됩니다. 새로 인식된, 종류가 정확한
  이메일 및 양식 검출도 이제 이미 검사된 이미지 영역에서 최종
  시각 검증 시 걸러지지 않습니다.

- 냉난방 및 전기 견적서의 기술적인 항목·품목 줄이 사람, 지명,
  조직으로부터 더 엄격하게 구분됩니다. 여기에는 케이블 종류,
  AC 전원, 항목 번호, 대문자 제품 코드 등이 포함됩니다 – 실제
  이름과 주소는 계속 보호됩니다.

- 실제 정리된 PDF 검증에서는 `1 699,59` 같은 가격 구성 요소를
  더 이상 전화번호로 혼동하지 않으며, `08.05.2025` 같은 완전한
  날짜에서 가상의 카드 정보를 잘라내지도 않습니다. 호칭 뒤의
  이름은 다음 도로명이 아니라 줄바꿈에서 끝납니다 – 첨부파일
  이름 안의 지명은 실제 지명으로 한정됩니다. 차량 색상, 기술적
  상태 값, 업종 명칭, 제품의 법인 형태도 그대로 유지됩니다.
  `|PLLZ` 같은 손상된 자리표시자 판독은 두 번째 OCR 처리에서
  다시 개인정보로 처리되지 않습니다.

- 옆으로 저장된 PDF 이미지는 최종 시각 검증에서 변경되지 않은
  이미지 위치에 대해 추가로 한 번 더 검토됩니다. 이는 Maskuro가
  같은 페이지에서 이미 확실하게 인식한 값만 추가로 마스킹할 수
  있습니다. 이렇게 하면 작게 회전된 주소 도장이 완전히 가려지되,
  이미지 제목이나 기술 도면에서 새로운 단어를 개인정보로 지어내지
  않습니다.

- OpenDocument 텍스트에서는 이제 메모(주석) 작성자의 이니셜도
  작성자와 함께 지워집니다. LibreOffice는 전체 이름 옆에 이를
  별도의 약어로 저장하고 페이지 여백에 정확히 그 약어를 표시합니다
  – 지금까지는 "Sieglinde Ortner"가 이미 오래전에 자리표시자가
  되었는데도 "SO"가 그대로 남아 있었습니다. 지우는 것은 작성자가
  실제로 교체된 경우뿐입니다 – 부서의 메모는 그 표시를 그대로
  유지합니다.

- 이탈리아어 업무 편지에서는 문장 시작의 표준 문구가 더 이상
  이름이나 지명으로 간주되지 않습니다: "Restiamo a disposizione",
  "Rimaniamo", "Attendiamo", "Alleghiamo", "Comunichiamo",
  "Auguriamo buon lavoro"는 지금까지 가상의 사람이나 지명으로
  걸려 있었습니다. 같은 위치의 실제 이름("Rossi Mario")은 계속
  인식됩니다.

- 두 단으로 된 스캔은 텍스트 인식이 먼저 모든 필드 머리글을,
  그 다음에 모든 값을 제공하는 경우에도 이제 라벨이 붙은 식별
  번호와 지명을 보호합니다. 대응 관계는 눈에 보이는 픽셀 줄을
  따르며, 90도 회전된 페이지에서도 작동합니다. 여권이나 계약
  식별 번호의 좁게 나뉜 부분은 함께 마스킹됩니다 – 라벨이 붙은
  생년월일, ICD 및 PZN 코드도 마찬가지로 처리되며, 뒤따르는
  사물 단어는 그대로 남습니다. 짧은 이름과 사용자 이름은 정확한
  필드에서 보호됩니다 – 여러 OCR 단어로 나뉜 이메일 주소는 좁은
  인접성과 완전한 이메일 문법이 있을 때만 처리됩니다. 필드에
  결합된 혼동 가능 문자 보정과, 아직 비어 있는 인물 필드의 지역적
  재판독은 손상되거나 회전된 스캔을 처리하되, 사물 필드나 이미
  대응된 값을 확장하지는 않습니다. 안전 여백은 단어 크기를 따르며,
  특징 프로필은 바로 인접한 복용 단위와 지불 주기도 함께
  처리합니다. 약간 비뚤어지게 넣은 양식은 방향이 같은 여러 OCR
  줄에서 기하학적으로 역투영됩니다 – 반올림 노이즈나 모순되는
  근거만으로는 충분하지 않습니다. 짧은 문자 접두사는 하이픈이
  포함된 식별 번호 앞에서 그대로 유지되며, 완전한 라벨이 붙은
  주소 검출은 같은 종류의 도로명 부분 검출만 대체합니다. 잘못
  읽힌 직무 필드 머리글은 최소 세 개의 알려진 머리글로 채워진
  양식 열에서만 처리됩니다 – 채팅 이름은 계속 보호됩니다. 좁은
  가장자리 잘림과 대각선 빛 반사로 인한 국부 과다 노출이 이미지
  매트릭스에 추가되었습니다. 여러 양식 줄에 걸친 사람, 지명,
  회사 검출은 여러 번 대응된 필드 열에서 각각의 값으로 제한됩니다.
  기술적인 항목 값은 항목 머리글과 일치하는 형식이 있을 때만
  처리됩니다 – 실제 이름은 계속 보호됩니다. 빛 반사로 잘린
  이메일 값도, 명시적인 이메일 필드 머리글 뒤에서 좁고 인접
  영역으로 제한된 이미지 여백을 통해 제거됩니다. 같은 눈에 보이는
  줄의 두 필드-값 쌍은 이제 독립적으로 처리됩니다 – 더 아래의
  기준선에 있는 값은 세 가지 일치하는 기하학적 근거가 있을 때만
  결합됩니다. 이로써 식별 번호, 생년월일, 주소는 빽빽한 양식
  레이아웃에서도 완전히 보호됩니다. 도로명, 우편번호, 지명은
  오직 같은 주소 필드 안에서, 알맞은 우편 문법과 함께 있을 때만
  결합됩니다. 작업·보조 도구 및 치아 상태를 위한 좁게 한정된
  사물 필드는 더 이상 지명이나 목록 오탐을 일으키지 않습니다 –
  실제 이름과 비슷하게 이름 붙은 필드는 계속 보호됩니다. 독일어
  문서 검증소는 이제 440개 스캔을 포함하며, 기본 프로필에서
  2,981/2,981건을, 특징 프로필에서 3,080/3,080건을 달성합니다.
  11개 이미지 변형과 40개 문서군 전체가 100퍼센트를 달성하며,
  여전히 측정된 오탐, 보존 침해, 중단이 없습니다.

- 셀 구분이 손실된 PDF 텍스트 레이어는 이제 반복되는 필드-값
  구조를 기준으로 조직명, 주소, 지명 검출 범위를 제한합니다.
  회사 값 앞의 필드 머리글과 `=>`나 `->` 같은 기술적 화살표는
  더 이상 검출에 포함되지 않습니다. 소프트 줄바꿈용 추가 보기는
  법인 형태와 지명 검출을 여러 표 줄에 걸쳐 확장할 수 없습니다 –
  이미 완전한 주소는 다음 필드 머리글과 값 앞에서 끝납니다.
  1,600개의 TXT, HTML, PDF, DOCX 문서 전체에 대한 최종 실행은
  오탐 0건, 보존 침해 0건, 중단 0건으로 10,840/10,840건의 목표
  정보를 제거합니다.

## 0.10.44-beta.1 – 2026년 9월 1일

- 패키지 빌드는 Windows x64 및 ARM64, Apple Silicon 및 Intel 기반 macOS, Linux
  x64 및 ARM64용으로 각각 별도의 출력물을 만듭니다. 패키지 이름, 업데이트
  선택, 릴리스는 아키텍처를 구분하며, 여섯 대상 중 하나 또는 그 의존성
  증빙이 빠지면 배포는 잠긴 상태로 유지됩니다. Linux ARM64는 Qt 때문에
  최소 glibc 2.39가 필요합니다. 실제 하드웨어에서 완전히 검증된 것은
  현재 Windows x64와 Apple Silicon macOS뿐이며, 나머지 아키텍처 패키지는
  실제 운영이 아닌 테스트용 사전 버전임을 명확히 표시합니다.

- 여러 파일을 처리할 때 이제 미리보기가 검토를 기다리는 동안에도 인식
  작업이 계속됩니다. 준비된 미리보기는 최대 세 개까지 순서대로
  표시되며, 동시에 계산되는 문서는 여전히 하나뿐이고, 결과 파일은
  승인 후에야 만들어집니다. 미리보기에서 선택한 영구 예외는 이미
  준비된 다음 문서에도 적용됩니다.

- 편집 인증서는 이제 언제든지 파일 메뉴에서 직접 검게 칠해진 문서와
  대조하여 검사할 수 있습니다. Maskuro는 그 과정에서 일치하는 서명된
  파일, 일치하지만 서명되지 않은 증빙, 유효하지 않은 서명, 인증서와
  맞지 않는 문서를 구분합니다. 대조 검사에는 라이선스나 원래의 운영체제
  계정이 필요하지 않습니다.
  자동 검사 지점을 위해 동일한 비교가 `--zertifikat-pruefen`으로도
  제공됩니다. 반환 코드는 일치, 사용 오류, 유효하지 않은 증빙을
  구분합니다.
  대조 검사는 추가로 내장된 Maskuro ID를 인증서와 비교합니다. 임의로
  기입된 다른 ID는 서명되지 않은 증빙에서도 이 때문에 드러납니다.
  서명이 유효한 경우 검사 결과에는 관리 측에서 활성화한 담당자가
  운영체제 계정, 기술적 계정 ID, 플랫폼과 함께 표시됩니다. 서명되지
  않았거나 유효하지 않은 증빙에서 얻은 미확인 정보는 출력되지 않습니다.

- 새로운 독일어 문서 실험실은 열 개 분야와 네 가지 구조 변형에서 완전히
  가상의 TXT, HTML, PDF, DOCX 문서 160개를 만들어 냅니다. 매니페스트는
  이제 사라져야 하는 정보와 남아 있어야 하는 전문 용어 및 사실 관계
  식별자를 명확히 구분하며, 문서 계열, 변형, 공개 구조 출처가 추적
  가능하게 기록됩니다.

- 독일어 문서 실험실은 파일 280개, 구조 형태 7종, 목표 정보 1,540개,
  보존 앵커 1,036개로 확장되었습니다. 번호가 매겨진 서식, 괄호로 묶인
  PDF/입력란 필드, 기술적인 `=>` 매핑을 새로 검사합니다. 확장된 전체
  구성은 TXT, HTML, PDF, DOCX 각각에서 오탐 0건, 100퍼센트를
  달성합니다. 괄호로 묶인 날짜 및 식별번호 필드, 화살표 구분자,
  명시적으로 표기된 단체는 이제 구조적으로 인식됩니다.

- 두 번째 실험실 확장으로 문서 수가 400개, 구조 형태 10종, 목표 정보
  2,200개, 보존 앵커 1,480개로 늘었습니다. JSON 형태의 키-값, YAML
  목록, 대문자 서식 필드는 기존 구성과 함께 오탐 0건, 100퍼센트를
  달성합니다. 인용된 생년월일과 식별번호, 그리고 피보험자, 지원자,
  신고 의무자, 대리권자로 명시적으로 표기된 역할도 이제 이러한 내보내기
  형식에서 인식됩니다.

- 독일어 문서 실험실의 별도 OCR 모드는 40개 계열 전체에서 순수 이미지
  스캔 200개를 추가로 만들어 냅니다. 깨끗한 페이지, 대비가 낮은
  페이지, 저해상도 페이지, JPEG 아티팩트가 있는 페이지, 90도 회전된
  페이지는 정확한 픽셀 상자로 다시 측정되며, 비교 가능한 1,600개
  파일의 텍스트 기본 구성은 변경되지 않습니다. 매니페스트는 켜고 끌 수
  있는 날짜·금액·의료 특성을 기본 프로필과 구분하고, 확인된 OCR
  판독을 추가 목표 지점으로 세지 않습니다. 측정은 변형과 문서 계열별로
  세분화됩니다. 좁은 필드 경계 덕분에 예를 들어 지명 `Graz` 안의
  `Az`가 뒤따르는 날짜를 사건 번호로 검게 칠하는 일 등이 방지되며,
  현재 기본 매트릭스는 오탐 0건, 중단 0건으로 실행됩니다.

- 청구서/납품서, 은행/대출, 임대/주택관리, 학교/대학, 물류/통관을 위한
  독일어 문서 계열 다섯 개가 추가되어 실험실은 파일 600개, 목표 정보
  3,520개, 보존 앵커 2,360개로 확장되었습니다. 좁은 PDF 표 처리
  경로는 텍스트 레이어가 셀 구분자를 잃을 때 명시적인 머리글
  `Feld Angabe`를 사용합니다. 새로운 `--familien` 선택 옵션은 부분
  측정 속도를 높입니다. 새로 추가된 200개 파일은 오탐 0건, 중단 0건으로
  1,320/1,320을 달성합니다.

- 보험/손해, 근로/급여, 의료/검사, 차량/정비, 기술/유지보수 계열이
  독일어 문서 실험실을 파일 800개, 목표 정보 4,960개, 보존 앵커
  3,200개로 확장합니다. 좁게 표기된 보험증권, 환자, 검사자, 차량
  식별번호와 새로운 역할·주소·조직 필드가 인식됩니다. 새 부분 매트릭스와
  전체 매트릭스는 TXT, HTML, PDF, DOCX에서 오탐 0건, 중단 0건으로
  100퍼센트를 달성합니다.

- 건설/입찰, 에너지/환경, 협회/단체, 커뮤니케이션/일정, 호텔/행사
  계열이 독일어 문서 실험실을 파일 1,200개, 목표 정보 7,920개, 보존
  앵커 4,800개로 끌어올립니다. 새로운 역할·회사·주소·등기부·낙찰·예약·
  계정 필드가 모든 내보내기 형식에서도 인식됩니다. 계량기 번호는
  사실 식별자로 그대로 보존됩니다. 부분 매트릭스와 전체 매트릭스는
  오탐 0건, 중단 0건으로 100퍼센트를 달성합니다.

- 요식업/배달, 약국/처방전, 장례/묘지, 스포츠/회원, 부동산/중개
  계열이 독일어 문서 실험실을 파일 1,400개, 목표 정보 9,360개, 보존
  앵커 5,640개로 확장합니다. 새로운 인물 역할, 주소 필드, 검색 의뢰
  번호가 인식됩니다. 법인 형태가 표기된 회사명은 자동 줄바꿈이
  일어나도 완전히 보호되며, 연령대와 전문 분야 명칭은 더 이상
  잘못 대체되지 않습니다. 부분 매트릭스와 전체 매트릭스는 오탐 0건,
  중단 0건으로 100퍼센트를 달성합니다.

- 치과 진료, 운전학원, 소방/출동, 에너지공동체, 패키지여행 계열이
  독일어 문서 실험실을 파일 1,600개, 목표 정보 10,840개, 보존 앵커
  6,440개로 확장합니다. 새로운 역할, 주소 필드, 그리고 진료·교육·
  출동·에너지·여행계약 식별번호가 구조적으로 인식됩니다. 새로운
  200파일 부분 매트릭스는 1,480/1,480을 달성하고, 전체 매트릭스는
  10,840/10,840을 달성합니다. 둘 다 오탐 0건, 중단 0건을 유지합니다.

- 문서 실험실 전체 측정에서 좁은 공식 사실 형식과 구조 규칙 덕분에
  불필요한 대체가 68건에서 0건으로, 명시적으로 측정된 보존 위반이
  23건에서 0건으로, 중단이 3건에서 0건으로 줄었습니다. 동시에 탐지율은
  91.1퍼센트에서 100.0퍼센트로 올라갔으며, TXT, HTML, PDF, DOCX 각각
  100퍼센트를 달성합니다. `Feld`와 같은 일반적인 표 머리글은 확인된
  `Feld`/`Angabe` 순서에서만 제동이 걸리며, 같은 이름의 성은 계속
  보호됩니다. 종결 문자가 붙은 법원 사건 번호, 등호(`=`) 필드,
  `Geburtsdatum des Kindes`(자녀의 생년월일), 같은 줄에 표기된 여러
  이름은 완전히 인식됩니다. Word 표와 앞줄 필드는 필드 머리글을 임시
  인식 맥락으로 사용하며, 표기된 PDF 주소는 문장 구조상 줄바꿈이
  있어도 완전히 보호됩니다.

- 독일어 인물 특성, 직업, 의료 필드는 이제 Windows 줄바꿈에서도 제대로
  작동합니다. `Geschlecht`/`w`(성별/여)처럼 한 글자로 된 성별 표기는
  앞줄 형식에서 보호됩니다. 반면 사실에 해당하는 `Artikel-PZN`
  필드는 의약품 식별번호나 인물 인식을 유발하지 않으며, 실제 PZN,
  ICD, ATC 정보는 계속 인식됩니다.

- 독일어 서식 및 번호 필드가 더 정확해졌습니다. `DW.`(내선)는 이제
  부드러운 줄바꿈 앞에서도 작동하고, 명시적으로 표기된 이름은 소문자로
  적혀 있어도 제거되며, 순수 숫자로 된 사건 번호는 올바른 식별번호
  종류에 배정됩니다. 반대로 우연히 Luhn 검증을 통과하는 청구서·전표·
  물품 번호는 더 이상 신용카드로 취급되지 않습니다. 가상의 HTML 및 PDF
  출력 샘플은 완성된 문서에서 제거와 보존을 함께 확인합니다.
  식별번호와 사용자 이름은 표기가 바로 앞의 표 또는 서식 줄에 있을
  때도 인식됩니다. 사실에 해당하는 전표 번호는 이 형태에서도 계속
  보입니다.

- 비밀번호는 이제 앞줄에 단독으로 있는 필드 머리글 뒤에서도 인식됩니다.
  `!`나 `#` 같은 끝맺는 특수문자는 이때 보호 대상 값에 완전히
  포함됩니다. 반대로 제품/물품 PIN은 더 이상 카드 PIN으로 가려지지
  않으며, 명시적인 `PIN` 및 `Karten-PIN` 필드는 계속 보호됩니다.

- 소문자로 적힌 서식 값은 명확한 독일어 주소 및 `PLZ/Ort`(우편번호/
  지역) 필드에서 이제 단순 지명이 아니라 주소 또는 우편번호와 지역으로
  출력됩니다. 마찬가지로 「beispiel service」처럼 소문자로 적힌 회사
  값은 회사 필드 뒤에서 완전히 보호되며, 마지막 단어를 다음 필드
  머리글로 오인해 잘라내는 일도 없습니다.
- 도움말, FAQ, 개인정보 보호 텍스트, 웹사이트는 이제 출처 증빙을 함께
  설명합니다. 문서 안의 중립적인 Maskuro ID, 실제 운영체제 계정과의
  선택적 연결(로컬 검사 기록에만), Windows/macOS/Linux 간 사용자 전환,
  그리고 SHA-256과 서명이 갖는 의미까지 다룹니다.

- 이미지 기반 기술 시방서는 더 신중하게 정리됩니다. 「Abbruchhämmern」,
  「Deckungsrücklass」, 「Positionsnummern」, 「Einbauplatine」,
  「Terminsituation」과 같은 명확한 사실 단어, 그리고 단어 중간에서
  분리된 OCR 형태는 더 이상 인물이나 지명으로 취급되지 않습니다.
  실제 관청 견적서 하나가 이로 인해 새 오탐 없이 140건에서 90건의
  명확한 대체로 줄었습니다. Schneider, Lang, Bauer, Hahn 같은 이름은
  계속 명시적으로 보호됩니다.

- 실제 견적서에서 나온 오탐이 추가로 해결되었습니다. 「Digital
  signiert」는 더 이상 인물처럼 취급되지 않고, BIC는 콜론 없이 표기
  뒤에 와도 인식되며, `15000 Alternativ`는 우편번호+지역으로 취급되지
  않고, EU 인용문 「(VO (EG) 715/2007」는 조직을 만들어 내지 않습니다.
  태양광 발전 견적서 하나는 이로 인해 26건에서 16건의 대체로 줄었으며,
  실제 이름, 지명, 계좌 정보는 그대로 유지되었습니다.

- 직원 명단에서 대리인 약어 「Stv.」와 단독으로 분리된 「FACILITY」
  부서 표제는 더 이상 인물 이름으로 대체되지 않습니다. 실제 13쪽짜리
  대조 문서는 878건에서 875건의 대체로 줄었으며, 이름, 내선번호,
  회사명은 계속 보호되었습니다.

- 정리된 PDF, OpenDocument, Office 파일은 문서 속성에 중립적인
  `MASKURO-…` 식별자를 받습니다. 검사 보고서와 서명된 검사 기록에는
  같은 식별자와 원본·결과의 SHA-256 값이 함께 기록됩니다. 편집
  인증서는 완성된 파일에서 식별자를 그대로 가져옵니다. 사용자 이름은
  관리 측이 기존 사용자 필드를 명시적으로 켰을 때만 추가로 붙습니다.

- 메인 창과 설정은 더 정돈되어 있습니다. 저장, 복사, 세부 정보, 지표,
  인식 프로필 삭제는 해당 동작이 실제로 가능할 때만 나타납니다.
  기술적인 OCR 언어 약어와 긴 예시는 필요할 때만 안내문에 표시되며,
  작업 영역에 항상 떠 있지 않습니다. 인식 페이지는 더 좁은 창에도
  더 잘 맞춰지며, 잘린 설명이나 가로 스크롤바 없이 표시됩니다.
  대체 목록의 평문 경고는 이때도 계속 보입니다.

- 인식 범위가 독일어 및 국제 연락처 사례를 더 포함합니다. 전화번호는
  이제 선택 가능한 모든 국가 지역에 대해 검사되고, 헝가리어와
  크로아티아어 계약 역할 표기는 직업명과 같은 성도 완전히 포착하며,
  번호가 매겨진 부품/자재 목록은 더 이상 「Mutter / Flach」때문에
  인물 오탐을 일으키지 않습니다. 명백히 숫자로 된 값을 가진 인물
  필드는 이름으로 취급되지 않으며, 기계 판독 여권 영역(MRZ)은 이제
  그룹 「식별번호」로 함께 켜고 끌 수 있습니다.

- 법인 형태가 없는 회사는 모호한 고용주 필드 뒤에서 인물과 더 잘
  구분됩니다. 「Huber Handel」, 「Müller Logistik」, 「Kowalski Handel」
  같은 이름은 완전히 회사로 인식되는 반면, 「Arbeitgeber: Bauer Anna」는
  계속 인물 이름으로 남습니다. 자동 국가 선택은 프랑스어 문서에서
  룩셈부르크를 포함한 프랑스어권 전체를 계속 고려합니다.

- 지금까지 이미지 안에서 인식된 서명과 개인정보 관련 텍스트는 항상
  검은색 사각형으로 가려졌습니다 - 검게 칠하기에 다른 색이나
  「무지개」 같은 패턴이 설정되어 있어도 마찬가지였습니다. 이러한
  이미지 영역도 이제 선택한 검게 칠하기 표현을 함께 따릅니다. 덮는
  면은 계속 이미지 픽셀에 직접 기록됩니다.

- 영어 인식은 수작업으로 번역한 실제 문서 11건을 기준으로 다시
  측정되고 개선되었습니다. 재고 상태, 기술 견적 및 웹샵 필드, 직원
  명부의 역할은 그대로 보이며, 「CV」는 서식 문장에서 더 이상 법인
  형태로 읽히지 않고, 인용된 글꼴은 보존되며, 세로형 이력서 머리글,
  여러 쪽에 걸친 직원 명단, 「Account manager」 뒤, 숫자로 시작하는
  회사 이름의 이름이 완전히 인식됩니다. 오스트리아 상업등기번호는
  이제 영어 표기 뒤에서도 작동하며, 약식 「Customer:」, EAR 등록
  번호, 고용주 번호도 값을 갖습니다. 치수열, 케이블 종류, EU 법률
  참조, 견적 유효기간, 이행 장소, 재판 관할지, 등기법원, 세금 약어
  「NoVA」, 타이어 라벨의 기술 번호, 「OVE R6-2」와 「AStV」 같은 규격
  참조는 더 이상 오탐을 일으키지 않습니다. 표기된 유효한 IBAN은
  등록 필드나 다음 줄 표제 앞에서 깔끔하게 끝나며, 사업구역 부기가
  붙은 주소는 Windows 줄바꿈이 포함된 PDF 텍스트 흐름에서도 완전히
  인식됩니다. 영어식 회사 소개 문구와 구조화된 저축은행 이름은
  완전히 구분됩니다. 원본 문서의 국가는 우편번호와 국가별 식별번호의
  언어 버전에서 계속 유지됩니다.

- 수신자 및 메시지 머리글에서 언어 모델이 쉼표로 나열된 첫 두 이름을
  하나의 결과로 합쳐 버리는 경우가 있었습니다(「Bcc: Huber, Mayer」).
  이제 두 이름 모두 개별적으로 인식, 대체, 보고서에 기록됩니다 -
  「Sent:」, 「Reply:」, 「Fwd:」 뒤에서도 마찬가지입니다.

- 여권이나 신분증의 기계 판독 영역(MRZ)이 「검색 대상」의 그룹
  제어에서 빠져 있었습니다. 이제 「식별번호」 그룹에 속하며, 이
  그룹과 함께 켜고 끌 수 있습니다.

- 대체 텍스트 서식으로 「무지개」를 선택하면 이제 검게 칠해진 부분도
  같은 모습을 갖습니다. 지금까지는 뜻밖에도 고전적인 검정색 그대로
  남아 있었습니다. 검게 칠하는 면은 이후에도 계속 다른 서식으로
  독립적으로 바꿀 수 있습니다.

- 후처리 편집기의 페이지 패널은 저장된 창 배치를 복원한 뒤 너비를
  직접 조정하기 전까지 비어 있을 수 있었습니다. 미리보기 이미지는
  이제 눈에 보이는 창 구성 뒤에 다시 배치되며, 곧바로 패널 가운데에
  나타납니다.

- PDF 안의 대체 텍스트 주위의 색상 검사 표시가 분류·신호등 색에 따라
  거의 보이지 않을 때가 있었습니다. 밝은 아래쪽 윤곽선이 이제 검사
  틀을 색상이 있는 자리표시자 및 페이지 배경과 확실히 구분해 줍니다.

- 줄 간격이 좁게 설정된 문서(견적서와 시방서에 흔함)에서 후처리
  편집기로 한 줄을 검게 칠하면, 막대가 아래 줄의 어센더까지 파고들어
  그 줄이 절반만 읽히게 되는 문제가 있었습니다. 이제 막대는 이웃
  줄에 실제로 그려진 글자에서 멈추며, 검게 칠해진 줄 자체는 디센더를
  포함해 완전히 가려진 채로 유지됩니다.

- 「연습 문서」(도움말 → 연습 문서 열기, 둘러보기에도 포함)는 이제
  모든 인식 종류를 보여줍니다. 가상의 편지에 얼굴이 인식되는 사진,
  손으로 쓴 서명, 직업과 부서, 진단명과 약품이 추가되었습니다 - 이미
  있던 회사명, 금액, 날짜 옆에 말입니다. 기본 설정이 일부러 남겨
  두는 것은 문서 자체가 설명하며, 그것을 제거하는 스위치도 함께
  나옵니다. 사진 속 얼굴은 출고 시 기본으로 모자이크 처리됩니다.

- 「금액도 함께 제거」 스위치는 숫자 뒤에 기호가 오는 일반적인 독일식
  표기(「1.240,00 €」)를 지금까지 전혀 찾아내지 못했습니다 - 「1.240,00
  EUR」와 「€ 1.240,00」는 이미 늘 인식되었습니다. 이제 세 가지 표기
  모두 인식됩니다.

- 서명 검색은 이제 단독 이미지 파일에서도 작동합니다. 스캔본을 JPG나
  PNG로 정리하면 그 안의 손글씨 서명도 검게 칠해집니다 - PDF와
  동일한 인식, 동일한 보고서 표기입니다. Office 파일에 삽입된
  이미지는 그곳에서 인식이 측정상 신뢰할 수 없게 작동하기 때문에
  여전히 검사되지 않으며, 이 때문에 해당 체크박스 이름은 이제
  「PDF 및 이미지 파일: 손글씨 서명 검게 칠하기」입니다.

- 줄 간격이 좁을 때 검게 칠하는 막대가 아래 줄의 어센더까지 눈에
  띄게 파고들어 절반만 읽히게 만드는 문제가 있었습니다 - 막대 높이가
  종이에 실제로 있는 것이 아니라 글꼴 메트릭에서 나왔기 때문입니다.
  막대는 이제 후처리 편집기와 자동 정리 모두에서 이웃 줄에 실제로
  그려진 잉크에서 멈춥니다. 자신의 줄은 디센더를 포함해 항상 완전히
  가려지며, 줄이 실제로 겹치는 경우에는 뭔가를 드러내기보다 막대가
  이웃 줄 위에 그대로 남는 쪽을 택합니다.

- 이름 아래에 역할이 오는 직원 명부에서 여성형 관리직 명칭(「Anna
  Berger」 아래 「Montageleiterin」)이 이름 대체에 함께 딸려 들어갔습니다
  옆에 있던 남성형은 정확히 그대로 남았습니다. 여성형 「…leiterin」
  형태(조립·팀·프로젝트·건설·부서·운영·그룹·관청 여성 팀장)는 이제
  남성형 상대와 마찬가지로 직함으로 취급됩니다. 지점·인사·영업
  책임자는 두 형태 모두 새로 추가되었습니다.

- 켜고 끌 수 있는 직업 인식은 「Projektleiterin」, 「Teamleiterin」,
  「Abteilungsleiterin」같은 여성형 관리직을 찾지 못했지만, 남성형은
  찾았습니다. 이제 두 형태 모두 동등하게 인식됩니다.

- Mac 미리보기 창에서 복수 표시가 용어에 바로 붙어 있었습니다
  (「Anna Musterfrau2」 대신 「Anna Musterfrau 2」이어야 함). 간격이
  다시 정상으로 돌아왔습니다.

- 비교 돋보기에는 확대 조절기 옆에 새 버튼이 생겼습니다. 한 번 누르면
  결과 위로 전체 너비로 놓입니다 - 각각 절반 높이로, 원본은 문서와
  같은 배율로 표시됩니다(그러기 위해 돋보기 확대가 100퍼센트로
  바뀝니다). 다시 누르면 왼쪽 열에 작게 다시 붙고, 이전 돋보기
  확대가 복원됩니다. 옆의 원형 버튼은 이제 확대만 초기화합니다 -
  지금까지는 창을 다시 붙인다고 잘못 안내하고 있었습니다.

- 후처리 편집기의 도구 모음에서 선택된 도구가 선택된 상태로 다시
  보입니다. 활성 도구의 버튼은 파란 테두리가 있는 채워진 면을
  갖습니다 - 도구 모음의 다른 켜고 끄는 버튼(비교 돋보기나 학습
  모드 등)도 마찬가지입니다. 이 표시는 8월 29일의 자체 버튼 디자인
  변경으로 사라졌었습니다.

- 시방서의 항목 번호(「2.3.3.3, 2.3.3.4, 2.3.3.5」가 세로로 나열됨)가
  IP 주소로 취급되어 결과에서 제거되었고, 마지막 항이 연도처럼 보이는
  3단계 번호(「2.3.19, 2.3.20」)는 달력 날짜로 취급되었습니다. 줄
  맨 앞의 오름차순 번호열은 이제 그 본연의 모습 - 즉 항목 목록 - 으로
  취급됩니다. 실제 주소(기술 용어가 많은 네트워크 표, 99를 넘는
  숫자)와 실제 날짜는 계속 인식됩니다.

- 「Müller」, 「Fischer」, 「Bauer」, 「Koch」, 「Wagner」, 「Schneider」,
  「Weber」, 「Jäger」, 「Schmied」, 「Becker」, 「Schuster」, 「Schäfer」,
  「Meister」 같은 성은 「성, 이름」 형식 목록(예: 「참가자: Müller,
  Peter; Nowak, Anna」)에서 평문 그대로 남아 있었습니다. 동시에
  흔한 직업명이기도 했기 때문입니다. 이제 확실히 인식됩니다.

- PDF를 검게 칠할 때 좁은 표 셀 안에서 막대가 셀 전체를 가져가 버릴
  수 있었습니다. 시방서에서 「D-LINK」라는 결과 하나 때문에 미리보기가
  결과만 언급했음에도 옆의 제품 설명 전체가 제거되었습니다. 막대는
  계속 주소 블록 줄과 필드 표제 전체를 덮지만, 보호해야 할 만큼만
  최소한으로 관계없는 부분을 함께 삼킵니다 - 이제 결과 옆의 설명은
  그대로 남습니다.

- 후처리 편집기에서 「보기 초기화」를 실행한 뒤 페이지 패널이 비어
  있었습니다 - 페이지 미리보기 이미지는 창을 닫았다 다시 연 뒤에야
  다시 보였습니다. 이제 초기화 직후에도 이전처럼 가운데에 나타납니다.

- 후처리 편집기에 네 번째 도구가 생겼습니다: **제거**는 틀 아래의
  텍스트를 대체 없이 그대로 없앱니다 - 막대(검게 칠하기)도 없고
  자리표시자(대체)도 없습니다. 빈 공간은 그대로 비어서 보입니다.
  단어 단위로 정확히 작동하며, 아래에 이미지가 있으면 해당 부분의
  배경이 흰색으로 비워지고, 「원본 되돌리기」는 대체 없는 제거도
  되돌립니다. 전용 도구 모음 아이콘과 조준선 배지(십자가), 18개 모든
  언어에서 전용 단축키(독일어 F, entFernen의 F)를 갖습니다.

- PDF 검색창에서 「폴더 …」가 이제 검색 옵션 오른쪽에 있습니다.
  검게 칠하기 외에 결과 대체 기능이 생긴 뒤로 일반적인 창 너비에서
  다섯 개 버튼이 나란히 들어가지 않게 되어, 첫 번째 버튼이 눌리고
  텍스트가 잘렸습니다.

- 「모든 설정 초기화」는 이제 「빨강/초록을 다른 색으로 대체」 체크박스도
  초기화하며, 출고 상태와 다르면 다른 항목과 마찬가지로 「변경됨」으로
  표시합니다.

- PDF의 대체 텍스트가 더 일관되게 보입니다. 전체 자리표시자가 줄보다
  눈에 띄게 작아져야 할 곳(예: 「Das」처럼 짧은 단어에 「[BEG16]」이
  욱여넣어짐)에는 대신 줄 크기의 축약형(「[B16]」)이 표시됩니다 - 아주
  작게보다 잘 읽히며, 되돌리기용 번호는 두 표기 모두를 담습니다.
  자리표시자가 극도로 작아지는 경우는 이제 가장 짧은 형태조차 자리를
  찾지 못할 때뿐입니다 - 그래도 아무 정보도 없는 막대보다는 낫습니다.

- PDF 안의 여러 색으로 설정된 대체 텍스트(그러데이션 또는 무지개)는
  다음 작업 전까지만 온전했습니다. 같은 페이지에서 이후의 대체나
  검게 칠하기가 이미 설정된 자리표시자를 읽을 수 없는, 눌린 글자
  더미로 뭉개 버릴 수 있었습니다 - 편집기에서 단어 단위로 대체하면
  「[BEG17]」 대신 겹쳐 인쇄된 문자만 보였습니다. 한 번 설정된
  자리표시자는 이제 설정된 그대로 유지됩니다.

- 미리보기의 영구 예외 스위치 이름이 이제 「다시는 제거하지 않음」으로
  바뀌었습니다 - 그것이 기록되는 목록의 이름과 같습니다. 지금까지는
  「다시는」이라고만 되어 있었습니다. 옆의 결과 줄도 더 정돈되었습니다.
  정보 아이콘 「ⓘ」가 더 크고 누르기 쉬워졌고, 체크박스, 대체 표시,
  버튼의 높이가 통일되었습니다. 결과 주변 문장은 이제 예고된 너비를
  실제로 활용합니다 - 지금까지 너비 지정이 표시에서 조용히
  무시되어, 문단이 좁은 줄로 계속 줄바꿈되었습니다.

- 편집기에서 마우스 포인터가 이제 어떤 도구가 활성인지 알려줍니다:
  조준용 조준선 옆에 작은 표시가 붙습니다 - 검게 칠하기는 막대,
  대체는 교환 화살표, 되돌리기는 되돌림 곡선, 모자이크는 픽셀 격자.
  기존의 손 모양 표시는 사라졌습니다 - 손은 보통 어디서나 「잡고
  끌기」를 뜻하기 때문입니다. 이제 손 모양에는 맞는 역할이 생겼습니다:
  빨갛게 강조된 단어나 막대 위에서는 포인터가 가리키는 손으로
  바뀝니다 - 그곳에서는 클릭 한 번이면 충분합니다.

- 「최대 인식(AI)」은 더 이상 다운로드 가능한 로컬 언어 모델을
  제공하지 않습니다 - 이 단계는 이제 「자체 AI 연결」에서 설정한
  자체 AI를 통해서만 계산됩니다. 이미 자체 서버를 연결했던 사용자는
  차이를 느끼지 못합니다.

- 미리보기의 안내 투어는 이제 결과 주변 문장을 보여 주는 정보
  아이콘 「ⓘ」도 설명합니다. 그리고 이 문장 자체도 더 읽기 쉬워졌습니다:
  한 단계 큰 글씨, 더 넓은 줄 간격, 좁고 빽빽한 줄바꿈 대신 고정된
  너비입니다.
- 「파일 검토」, 「인식 규칙 및 사용자 정의 용어」, 「텍스트 정리」,
  「이미지 정리」도 이제 새 버튼 「창 둘러보기」를 통해 자체 둘러보기를
  갖습니다 - 이 네 창에는 자체 메뉴 표시줄이 없기 때문입니다.
- 아홉 개의 우크라이나어 계약 역할 표기 아래에 있는 이름은 표기가
  단독으로 줄에 있을 때, 동철 이형 성이 있으면 불완전하게만
  인식되었습니다: 「Покупець」/「Продавець」(매수인/매도인),
  「Поручитель」/「Боржник」(보증인/주채무자), 「Свідок」(증인),
  「Орендодавець」/「Орендар」(임대인/임차인),
  「Спадкодавець」/「Спадкоємець」(피상속인/상속인). 이제 이름이
  완전히 인식됩니다.

- Excel 통합 문서에서 이름 정의 영역의 주석(이름 관리자, 「주석」
  필드)에 입력된 이름은 그대로 남아 있었습니다. 이제 통합 문서의
  나머지 내용과 마찬가지로 정리됩니다.

- 일곱 개의 헝가리어 계약 역할 표기 아래에 있는 이름은 동철 이형
  성이 있으면 전혀 발견되지 않았습니다: 「Bérbeadó」/「Bérlő」
  (임대인/임차인), 「Vevő」/「Eladó」(매수인/매도인),
  「Kezes」/「Főadós」(보증인/주채무자), 「Tanú」(증인). 이제 이름이
  완전히 인식됩니다.

- 체코어 매수인 표기 「Kupující」 아래에 있는 이름은 동철 이형 성이
  있으면 전혀 발견되지 않았습니다. 이제 이름이 완전히 인식됩니다.

- 러시아어 후견인 표기 「Опекун」 아래에 있는 이름은 동철 이형 성이
  있으면 전혀 발견되지 않았습니다. 이제 이름이 완전히 인식됩니다.

- 여섯 개의 다른 크로아티아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Jamac」(보증인), 「Glavni dužnik」/「Dužnik」
  (주채무자/채무자), 「Ostavitelj」(피상속인), 「Nasljednik」
  (상속인), 「Vjerovnik」(채권자). 이제 이름이 완전히 인식됩니다.

- `<embed>`의 `src` 속성(`<object>`의 `data`가 아님)에 삽입된 하위
  페이지가 있는 저장된 HTML 페이지는 그 안의 개인정보를 그대로
  유지하고 있었습니다. 이제 `<object>`와 마찬가지로 정리됩니다.

- 다섯 개의 덴마크어 계약 역할 표기 아래에 있는 이름은 표기 뒤에
  콜론이 붙어 이름 앞에 올 때, 동철 이형 성이 있으면 불완전하게만
  인식되었습니다: 「Arvelader」/「Arving」(피상속인/상속인),
  「Befuldmægtiget」/「Fuldmagtsgiver」(수임인/위임인),
  「Værge」(후견인). 이제 이름이 완전히 인식됩니다. 안전을 위해 해당
  노르웨이어 표기도 함께 추가되었습니다.

- Word와 PowerPoint 파일의 자리표시자는 이제 선택한 서식과 같은
  색을 갖습니다(단색, 그러데이션, 무지개, 또는 분류별) - 지금까지는
  PDF 결과가 이미 색을 입고 있어도 그곳에서는 일반 텍스트 색 그대로
  남아 있었습니다.

- 「텍스트로 복사」와 「Markdown으로 복사」는 결과의 평문을 곧바로
  클립보드에 넣습니다 - 채팅, 메일, 다른 프로그램에 붙여넣기 위해
  파일을 먼저 열 필요가 없습니다.

- 다섯 개의 다른 슬로베니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Toženec」(피고), 「Tožnik」(원고), 「Zastavitelj」
  (질권설정자), 「Zastavni upnik」(질권자), 「Darovalec」(증여자).
  이제 이름이 완전히 인식됩니다.

- Word에서 추적된 표 셀 변경(삽입, 삭제, 병합된 셀)의 작성자 이름은
  같은 이름이 주석 작성자로는 이미 오래전에 제거되었어도 파일에
  남아 있었습니다. 이제 함께 제거됩니다.

- 아홉 개의 다른 슬로베니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Najemodajalec」/「Najemnik」(임대인/임차인),
  「Zapustnik」/「Dedič」(피상속인/상속인),
  「Upnik」/「Dolžnik」(채권자/채무자), 「Glavni dolžnik」(주채무자),
  「Skrbnik」(후견인/보호자). 이제 이름이 완전히 인식됩니다.

- 다섯 개의 슬로베니아어 표기 아래에 있는 이름이 발견되지 않았습니다:
  「Izvedenec」(감정인), 「Kupec」(매수인), 「Prodajalec」(매도인),
  「Naročnik」(발주자), 「Izvajalec」(수급인). 이제 이름이 완전히
  인식됩니다.

- 다섯 개의 다른 리투아니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Užsakovas」(발주자), 「Vykdytojas」(수급인),
  「Vežėjas」(운송인), 「Siuntėjas」(발송인), 「Arbitras」(중재인).
  이제 이름이 완전히 인식됩니다.

- 여섯 개의 다른 리투아니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Įgaliotinis」(수임인), 「Įgaliotojas」(위임인),
  「Naudos gavėjas」(보험 수익자), 「Trečiasis asmuo」(민사소송의
  보조참가인/제3자), 「Ankstesnis nuomininkas」(전 임차인),
  「Naujasis nuomininkas」(신 임차인). 이제 이름이 완전히 인식됩니다.

- ODT 문서의 책갈피(`text:bookmark`)는 이름이 자유롭게 붙여지며,
  종종 가리키는 위치의 이름을 그대로 씁니다(예: 「Herr_Mueller_
  Unterschrift」) - 읽는 사람에게는 보이지 않지만 파일 안에는 그대로
  적혀 있습니다. 이제 이 이름도 함께 정리됩니다.

- 여덟 개의 다른 리투아니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Pareiškėjas」(신청인), 「Suinteresuotas asmuo」(비송
  사건의 상대방), 「Ekspertas」(감정인), 「Bankroto
  administratorius」(파산관재인), 「Valdybos narys」(이사회 구성원),
  「Direktorius」(대표이사), 「Palikėjas」(피상속인),
  「Įpėdinis」(상속인). 이제 이름이 완전히 인식됩니다.

- 일곱 개의 다른 리투아니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Liudytojas」(증인), 「Vertėjas」(통역/번역인),
  「Notaras」(공증인), 「Dovanotojas」(증여자),
  「Apdovanotasis」(수증자), 「Pirkėjas」(매수인),
  「Pardavėjas」(매도인). 이제 이름이 완전히 인식됩니다.

- 여섯 개의 다른 리투아니아어 표기 아래에 있는 이름이 발견되지
  않았습니다: 「Globėjas」(후견인/보호자), 「Palikimo
  administratorius」(상속재산관리인), 「Laiduotojas」(보증인),
  「Pagrindinis skolininkas」(주채무자), 「Nuomotojas」(임대인),
  「Nuomininkas」(임차인). 이제 이름이 완전히 인식됩니다.

- 리투아니아어 표기 「Ieškovas」/「Atsakovas」(소송 당사자로서
  원고/피고) 아래에 있는 이름은 성이 흔한 단어인지(예: 「Vilkas」 =
  늑대) 여부와 관계없이 발견되지 않았습니다. 이제 이름이 완전히
  인식됩니다.

- ODT 문서의 색인 항목(찾아보기용 책갈피)은 이름을 자신의 정렬 키에
  다시 한번 담고 있었습니다 - 본문에는 보이지 않지만 나중에 만들어지는
  색인에는 그대로 나타납니다. 이제 이 키도 함께 정리됩니다.

- PowerPoint 프레젠테이션의 슬라이드 이름과 구역 이름(선택 창이나
  슬라이드 정렬에서 보임)은 둘 다 슬라이드 텍스트가 아닌 요소의
  속성으로 붙어 있어 정리되지 않았습니다. 이제 둘 다 인식됩니다.

- 「Petraitis-Kazlauskas」 같은 리투아니아어 하이픈 복성은 앞에
  본문 텍스트가 있으면 뒷부분을 잃어버렸습니다(텍스트 맨 앞에서만
  완전히 남았습니다). 이제 이런 경우에도 성이 완전히 인식됩니다.

- 표기 「Cesionar」(크로아티아어, 채권양도의 양수인) 아래의 이름은
  필드 표기 자체가 잘못 인물로 읽혀 오탐을 일으켰습니다. 반면 러시아어
  표기 「Цессионарий」(마찬가지로 양수인) 아래의 이름은 전혀 발견되지
  않았습니다. 두 경우 모두 이제 해결되었습니다.

- 표기 「Zedent」/「Zessionar」(독일어, 채권양도) 아래의 이름은 성이
  흔한 단어일 때(예: 「Bauer」) 전혀 발견되지 않았습니다. 이제 이름이
  완전히 인식됩니다.

- 표기 「Darczyńca」/「Obdarowany」(폴란드어, 증여계약의 증여자/
  수증자) 아래의 이름은 성이 흔한 단어일 때(예: 「Wilk」 = 늑대)
  발견되지 않았습니다. 마찬가지로 루마니아어 표기 「Donatar」
  (수증자)는 흔한 성이 있으면 그 자체가 이름의 일부처럼 취급되어
  걸려 있었습니다. 두 경우 모두 이제 해결되었습니다.

- 표기 「Wierzyciel」/「Dłużnik」(폴란드어, 강제집행의 채권자/
  채무자 또는 일반 채권자/채무자) 아래의 이름은 성이 흔한 단어일
  때(예: 「Wilk」 = 늑대) 발견되지 않았습니다. 이제 이름이 완전히
  인식됩니다.

- 표기 「Poręczyciel」/「Dłużnik główny」(폴란드어, 보증계약의
  보증인/주채무자) 아래의 이름은 성이 흔한 단어일 때(예: 「Wilk」 =
  늑대) 발견되지 않았습니다. 이제 이름이 완전히 인식됩니다.

- 표기 「Ubezpieczony」/「Ubezpieczający」(폴란드어, 보험증권의
  피보험자/보험계약자) 아래의 이름은 성이 흔한 단어일 때(예:
  「Wilk」 = 늑대) 부분적으로 또는 전혀 발견되지 않았습니다.
  마찬가지로 「Osiguranik」/「Osiguravatelj」(크로아티아어, 같은
  역할) 아래의 이름도 이름 전체가 사라졌습니다(예: 「Golub」 = 비둘기).
  두 이름 모두 이제 완전히 인식됩니다.

- 표기 「Pełnomocnik」/「Mocodawca」(폴란드어, 위임장의 수임인/
  위임인) 아래의 이름은 성이 흔한 단어일 때(예: 「Wilk」 = 늑대)
  발견되지 않았습니다. 마찬가지로 「Opunomoćenik」/「Opunomoćitelj」
  (크로아티아어, 같은 역할) 아래의 이름도 이름 전체가 완전히
  사라졌습니다. 두 이름 모두 이제 완전히 인식됩니다.

- 표기 「Pozwany」(폴란드어, 소송 당사자로서 피고) 아래의 이름은
  성이 흔한 단어일 때(예: 「Wilk」 = 늑대) 발견되지 않았습니다. 이제
  이름이 완전히 인식됩니다.

- 표기 「Najmoprimac」/「Najmodavac」(크로아티아어, 임대계약의
  임차인/임대인) 아래의 이름은 성이 흔한 단어일 때(예: 「Kovač」 =
  대장장이) 발견되지 않았습니다. 이제 이름이 완전히 인식됩니다.

- 표기 「Pracodawca」/「Pracownik」(폴란드어, 근로계약 당사자로서
  고용주/근로자) 아래의 이름은 성이 흔한 단어일 때(예: 「Krawiec」 =
  재단사) 부분적으로 발견되지 않았습니다. 이제 이름이 완전히
  인식됩니다.

- 헝가리는 국가별 목록에 개인 식별번호와 부가가치세 ID만 있었습니다:
  상업등기번호(Cégjegyzékszám)는 이제 필드 단어 「Cégjegyzékszám」
  또는 약어 「Cg.」가 바로 앞에 있으면 인식됩니다 - 번호 자체에는
  검증 숫자가 없습니다.

- 에스토니아는 국가별 목록에 Isikukood만 있었습니다:
  Käibemaksukohustuslase number(모든 에스토니아 청구서에 있는
  부가가치세 ID)는 이제 검증 숫자와 함께 인식됩니다.

- 라트비아는 국가별 목록에 Personas kods만 있었습니다: PVN
  reģistrācijas numurs(모든 라트비아 청구서에 있는 법인 식별번호)는
  이제 검증 숫자와 함께 인식됩니다.

- 암호화된 내용이 있는 메일(S/MIME 또는 PGP/MIME 봉투,
  `multipart/encrypted`)은 실제 내용이 암호화되어 검사되지 않았음에도
  아무 경고 없이 완전히 검사된 것처럼 출력되었습니다. 이런 메일은
  이제 검사되지 않은 첨부파일처럼 이를 알립니다.

- 몰타가 국가별 목록에 빠져 있었습니다: 몰타의 부가가치세 ID(VAT
  number)가 이제 인식됩니다.

- 룩셈부르크가 국가별 목록에 빠져 있었습니다: 룩셈부르크의
  부가가치세 ID(n° TVA)가 이제 인식됩니다.

- 문장 맨 앞에 온 불가리아어 「Изчакайте」(기다려 주세요)가 지명으로
  보고되었습니다 - 자체 언어 모델이 없는 헝가리어, 폴란드어,
  체코어 등의 명령형에서 이미 있었던 것과 같은 모델 한계입니다.
  이 오탐은 이제 나타나지 않습니다.

- 표기 「Zleceniodawca」, 「Zleceniobiorca」(폴란드어), 「Prestator」
  (루마니아어), 「Naručitelj」 또는 「Izvođač」(크로아티아어) 아래의
  이름은 성이 흔한 단어일 때(예: 「Wilk」, 「Vuk」 = 늑대, 「Vulpe」 =
  여우, 「Sokol」 = 매) 부분적으로 또는 전혀 발견되지 않았습니다.
  이제 이름이 완전히 인식됩니다.

- 표기 「Nadawca」(폴란드어), 「Afsender」(덴마크어) 또는
  「Pošiljatelj」(슬로베니아어) 아래의 이름은 성이 흔한 단어일 때(예:
  「Sowa」 = 올빼미, 「Bager」 = 굴착기, 「Volk」 = 늑대) 부분적으로
  또는 전혀 발견되지 않았습니다. 이제 이름이 완전히 인식됩니다.

- 표기 「Gavėjas」(리투아니아어) 또는 「Prejemnik」(슬로베니아어)
  아래의 이름은 성이 흔한 단어일 때(예: 「Vilkas」 = 늑대) 부분적으로
  또는 전혀 발견되지 않았습니다. 이미 「Primatelj」(크로아티아어)와
  「Modtager」(덴마크어)에서처럼 이제 이름이 완전히 인식됩니다.

- 「To All Staff」나 「To All Employees」같은 회람 머리글이 잘못 인물
  이름으로 인식되어 제거되었습니다. 이제 더 이상 일어나지 않습니다.

- 표기 「Primatelj」(크로아티아어) 또는 「Modtager」(덴마크어) 아래의
  이름은 성이 흔한 단어일 때(예: 「Golub」 = 비둘기, 「Bager」 =
  굴착기) 부분적으로 발견되지 않았습니다. 이미 「Odbiorca」(폴란드어)와
  「Destinatar」(루마니아어)에서처럼 이제 이름이 완전히 인식됩니다.

- 덴마크어, 노르웨이어, 그리스어 문서의 서명줄에 있는 전체 이름은
  표기 「Underskrift」나 「Υπογραφή」가 이름 위에 단독으로 있을 때
  부분적으로 발견되지 않았습니다 - 그리스어의 경우 성이 이름이 아니라
  지명으로 인식되기까지 했습니다. 이미 러시아어 「Подпись」에서처럼
  이제 이름이 완전히 인식됩니다.

- 옆으로 눕혀 놓인 휴대전화 사진(회전 표시로만 똑바로 표시되는
  일반적인 세로 촬영)의 텍스트는 지금까지 원본의 눕혀진 픽셀을
  그대로 읽었기 때문에 텍스트 인식이 놓칠 수 있었습니다. 이런 사진은
  이제 읽기 전에 올바른 방향으로 회전됩니다 - 이미 얼굴 인식에서
  그랬던 것처럼 말입니다.

- 러시아어, 우크라이나어, 리투아니아어 문서의 서명줄에 있는 전체
  이름은 표기 「Подпись」, 「Підпис」, 「Parašas」가 이름 위에
  단독으로 있을 때 부분적으로 발견되지 않았습니다 - 이름 또는
  부칭이 빠졌습니다. 이미 「Potpis」(크로아티아어)에서처럼 이제
  이름이 완전히 인식됩니다.

- 옆으로 눕혀 놓인 휴대전화 사진(회전 표시로만 똑바로 표시되는
  일반적인 세로 촬영) 속 얼굴은 지금까지 원본의 눕혀진 픽셀을 그대로
  검사했기 때문에 얼굴 인식이 놓칠 수 있었습니다. 이런 사진은 이제
  검색 전에 올바른 방향으로 회전됩니다.

- 크로아티아어 문서의 서명줄에 있는 전체 이름은 표기 「Potpis」가
  이름 위에 단독으로 있거나 앞에 콜론이 붙어 있을 때 부분적으로
  발견되지 않았습니다 - 「Potpis: 이름 중간이름 성」처럼 한 줄에
  있든 별도의 줄에 있든 이름이 빠졌습니다. 이미 「Unterschrift」와
  「Signature」에서처럼 이제 이름이 완전히 인식됩니다.

- 신분 표기 약어 「verh.」(기혼)와 「verw.」(사별) 뒤의 결혼 후 성은
  괄호 안이든, 쉼표 뒤든, 공백 없이 붙어 있든(「Anna Meier (verh.
  Weber)」, 「Klaus Bauer (verw.Fischer)」) 지금까지 전혀 발견되지
  않았습니다 - 이미 「geb.」에서처럼 이제 확실히 인식됩니다.

- 대리 서명 「ppa.」 뒤의 이름(예: 업무용 이메일이나 서신의 서명줄)은
  「Bauer」나 「Koch」처럼 직업명과 같은 성일 때 지금까지 부분적으로
  또는 전혀 발견되지 않았습니다 - 이미 「gez.」에서처럼 이제 확실히
  인식됩니다.

- 폴란드 신분증(dowód osobisty) 번호는 시리즈와 번호 사이에 공백이
  없을 때만 인식되었습니다(「ABS123456」). 그러나 문서는 정확히 이렇게
  인쇄하지 않습니다 - 공식적으로는 그 사이에 공백이 있으며
  (「ABS 123456」), 이 표기에서는 지금까지 번호가 발견되지
  않았습니다.

- 애니메이션 PNG(APNG, 예: GIF 대신 PNG로 저장된 짧은 화면 녹화)는
  지금까지 첫 번째 이미지만 검사되고 정리되었으며, 이 사실이 보고되지
  않았습니다 - 이미 애니메이션 WebP에서처럼 이제 Maskuro는 나머지
  모든 이미지가 검사되지 않은 채 결과에 남는다는 것을 알립니다.

- 애니메이션 WebP 이미지(예: 스크린샷 도구나 여러 이미지를 한 파일에
  담는 채팅 애플리케이션에서 나온 것)는 지금까지 첫 번째 이미지만
  검사되고 정리되었으며, 이 사실이 보고되지 않았습니다 - 이미 여러
  쪽짜리 TIFF에서처럼 이제 Maskuro는 나머지 모든 이미지가 검사되지
  않은 채 결과에 남는다는 것을 알립니다.

- 하이픈이 있는 슬로베니아어 복수 이름(「Ana-Marija Novak」)은 본문
  텍스트가 앞에 있으면 앞쪽 절반을 잃었습니다 - 폴란드어에서 이미
  있었던 것과 같은 오류입니다. 「Ana-」는 대체되지 않은 채 평문으로
  남았고, 나머지 이름은 이미 대체되었습니다.

- 하이픈이 있는 폴란드어 복수 이름(「Anna-Maria Kowalska」)은 본문
  텍스트나 「z」/「od」같은 전치사가 앞에 있으면 앞쪽 절반을
  잃었습니다 - 나머지 이름은 대체되었지만 「Anna-」는 평문으로
  남았습니다.

- 자체 언어 인식 모델이 없는 카자흐어 인사말 「Хабарласыңыз」/
  「Байланысыңыз」(연락 주세요)와 세르비아어 동사형 「Помоћи」,
  「Чекамо」, 「Пишите」가 전화 문장에서 잘못 인물 이름이나 지명으로
  인식되었습니다.

- 자체 언어 인식 모델이 없는 아제르바이잔어 인사말 「Xahiş」
  (부탁/요청)가 전화 문장에서 잘못 인물 이름으로 인식되었습니다.

- 자체 언어 인식 모델이 없는 인도네시아어와 말레이어 인사·요청
  단어인 「Silakan」, 「Mohon」(인도네시아어), 「Sila」, 「Tolong」
  (말레이어)가 전화 문장에서 잘못 인물 이름이나 지명으로
  인식되었습니다.

- 자체 언어 인식 모델이 없는 우즈베크어 요청형 「Kutamiz」
  (기다리겠습니다)가 전화 문장에서 잘못 지명으로 인식되었습니다.

- 자체 언어 인식 모델이 없는 튀르키예어 요청형 「Arayınız」(전화
  주세요)와 「Bekliyoruz」(기다리고 있습니다)가 전화 문장에서 잘못
  인물 이름으로 인식되었습니다.

- 자체 언어 인식 모델이 없는 다른 언어(체코어, 슬로바키아어,
  그리스어)의 요청형인 「Zavolejte」(전화해 주세요), 「Prosíme」
  (부탁드립니다), 「Περιμένουμε」(기다리고 있습니다)가 전화 문장에서
  잘못 인물 이름이나 지명으로 인식되었습니다.

- 헝가리어와 폴란드어 요청형인 「Hívjon」(전화해 주세요), 「Kérjük」
  (부탁드립니다), 「Várjuk」(기다리고 있습니다), 「Zadzwoń」(전화해
  주세요), 「Czekamy」(기다리고 있습니다)가 전화 문장에서 잘못 인물
  이름이나 지명으로 인식되었습니다.

- 표 형태가 아닌 번호가 매겨진 인명 목록(예: 「1. Robert Brown」,
  그 아래 「2. Mary Johnson」)에서 「Brown」, 「White」, 「Green」,
  「Black」, 「Young」 등 특정 영어 성을 가진 이름이 완전히
  간과되었습니다 - 언어 모델이 다음 줄의 번호를 이름에 붙여 버려서
  결과가 더 이상 정확히 일치하지 않게 되었습니다.

- 폴란드어 언어 모델에서는 성 앞의 이름 이니셜(예: 「J. Kowalski」,
  「A. Nowak」)이 인식되지 않고 정리되지 않은 채 텍스트에 남아
  있었습니다 - 성만 대체되었습니다. 검사한 다른 언어(독일어, 영어,
  루마니아어, 크로아티아어, 헝가리어, 러시아어 등)는 이미 같은
  이니셜을 함께 인식하고 있었습니다.

- 「dr.」, 「ing.」, 「dipl. ing.」처럼 소문자로 시작하는 직함 뒤의
  인물 이름은 헝가리어, 루마니아어, 크로아티아어에서 전혀 인식되지
  않았습니다 - 직함뿐 아니라 이름 전체가 사라졌습니다(예: 「dr.
  Kovács Béla」, 「ing. Andrei Popescu」, 「dipl. ing. Marko Horvat」).
- 슬로베니아어 회의록에서 콜론 앞의 순수 역할 명칭(예: 「Tajnik:」,
  「Podpredsednik:」, 「Poročevalec:」, 「Predsedujoči:」)이 회의록의
  다른 곳에 실제 발언자 이름이 이미 있으면 잘못 인물 이름으로
  인식되었습니다.
- 러시아어 회의록에서 콜론 앞의 순수 역할 명칭(예: 「Секретарь:」,
  「Докладчик:」, 「Докладчица:」)이 회의록의 다른 곳에 실제 발언자
  이름이 이미 있으면 잘못 인물 이름으로 인식되었습니다.
- 루마니아어 회의록에서 정관사가 붙은 콜론 앞의 순수 역할
  명칭(예: 「Secretarul:」, 「Președintele:」, 「Vicepreședintele:」,
  「Moderatorul:」, 「Consilierul:」)이 잘못 인물 이름으로
  인식되었습니다 - 「Președintele」는 그 자체만으로도, 나머지는
  회의록의 다른 곳에 실제 발언자 이름이 이미 있으면 추가로
  발생했습니다.
- 크로아티아어 회의록에서 콜론 앞의 순수 역할 명칭(예:
  「Izvjestiteljica:」, 「Zapisničar:」/「Zapisnicar:」,
  「Predsjedavajući:」)이 잘못 인물 이름으로 인식되었습니다.
- 발신자 또는 수신자 표기 뒤의 폴란드어 사서함 주소 「Skrytka
  pocztowa」(예: 「Odbiorca: Skrytka pocztowa 45」)가 잘못 인물
  이름으로 인식되었습니다.
- 주소 표기 「Adresa:」 뒤의 크로아티아어 사서함 주소 「Poštanski
  pretinac」(예: 「Adresa: Poštanski pretinac 45」, 번호에 「br.」가
  붙기도 함)가 잘못 인물 이름으로 인식되었습니다.
- 노르웨이어 본문 텍스트에서 별도 표기가 없는 지명(예: 「Anna
  Hansen bor i Oslo」)이 인식되지 않았습니다 - 자체 언어 모델은
  대부분 지명에 일반적인 「LOC」가 아닌, 지금까지 매핑되지 않은
  자체 표지를 붙입니다.
- 하이픈이나 점으로 구분된 연-월-일 ISO 순서 날짜(예:
  「2024-12-31」)가 일부 언어에서 전혀 날짜로 인식되지 않았습니다 -
  공식 문서가 대부분 이 순서로 날짜를 표기하는 리투아니아어에서
  가장 눈에 띄었습니다.
- 구분자 없는 11자리 형식(예: 「12345678123」, 「12345678-1-23」
  대신)의 헝가리어 부가가치세 ID(közösségi adószám)도 공식적으로
  유효한데 인식되지 않았습니다.
- 3-2-2-3 그룹 구분이 적용된 폴란드어 세금번호 NIP(예:
  「856-73-46-215」, 기업 및 개인사업자 청구서에서 흔함)가 인식되지
  않았습니다 - 자연인용 3-3-2-2 그룹 구분만 적중했습니다.
- 슬로바키아어 필드 표기 「Zamestnávateľ:」 또는 「Názov
  zamestnávateľa:」(고용주/회사) 아래의 회사명이 인식되지
  않았습니다.
- 루마니아어 필드 표기 「Angajator:」 또는 「Denumire angajator:」
  (고용주/회사) 아래의 회사명이 인식되지 않았습니다.
- 헝가리어 필드 표기 「Cég:」 또는 「Munkáltató:」(회사/고용주)
  아래의 회사명이 인식되지 않았습니다.
- 폴란드어 필드 표기 「Pracodawca:」 또는 「Nazwa firmy:」(고용주/
  회사) 아래의 회사명이 인식되지 않았습니다.
- 슬로베니아어 필드 표기 「Podjetje:」 또는 「Delodajalec:」(회사/
  고용주) 아래의 회사명이 인식되지 않았습니다.
- 크로아티아어 필드 표기 「Tvrtka:」 또는 「Poslodavac:」(회사/
  고용주) 아래의 회사명이 인식되지 않았습니다.
- 소문자로 쓰인 통화 단위가 붙은 서술형 금액(예: 「500 euro」)이
  인식되지 않았습니다 - 대문자 표기(「Euro」)만 적중했습니다.
- 「Schwager」/「Schwägerin」(매형/처남) 뒤의 성(예: 「Der Schwager
  Bauer erhält die Erbschaft.」)이 인식되지 않았습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  튀르키예어 주소(예: 「34000 İstanbul İstiklal Caddesi No: 45」)는
  건물번호가 정리되지 않은 채 남아 있었습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  슬로바키아어 주소(예: 「831 01 Bratislava Hlavná 15」)는
  건물번호가 정리되지 않은 채 남아 있었습니다.
- 별도 표기가 없는 크로아티아어 서식 필드의 출생국(예: 「Zemlja
  rođenja: Njemačka」)이 인식되지 않았습니다.
- 별도 표기가 없는 리투아니아어 서식 필드의 출생국(예: 「Gimimo
  valstybė: Vokietija」)이 인식되지 않았습니다.
- 별도 표기가 없는 폴란드어 서식 필드의 출생국 또는 거주국(예:
  「Kraj: Niemcy」)이 인식되지 않았습니다.
- 별도 표기가 없는 슬로베니아어 서식 필드의 국적 또는 거주지(예:
  「Državljanstvo: Nemčija」)가 인식되지 않았습니다.
- 별도 표기가 없는 노르웨이어 서식 필드의 거주국(예: 「Bosted:
  Tyskland」)이 인식되지 않았습니다.
- 새 설정 페이지 「알림」(이전에는 「프로그램」의 한 항목이었음):
  세 가지 작업 표시줄 알림(미리보기 준비됨, 처리 완료됨, 업데이트
  다운로드됨)이 이제 자체 위치에 있습니다.
- 신규: 결과를 순수 텍스트 파일(.txt)이나 .md 확장자로 추가로 저장할
  수 있습니다 - AI나 다른 프로그램에서 이어서 작업하기 위해서입니다.
- 크로아티아어 연락처 표기 「Osoba za kontakt」/「Kontakt osoba」
  (예: 「Osoba za kontakt: Golub Marko」)에서 성이 흔한 명사(Golub =
  「비둘기」)일 때 이름이 완전히 인식되지 않았습니다.

- 루마니아어 연락처 표기 「Persoana de contact」/「Persoană de
  contact」(예: 「Persoana de contact: Lup Ion」)에서 성이 흔한
  명사(Lup = 「늑대」)이고 이름이 매우 짧고 일반적일 때 이름이
  완전히 인식되지 않았습니다.

- 폴란드어 연락처 표기 「Osoba kontaktowa」/「Osoba do kontaktu」
  (예: 「Osoba kontaktowa: Wilk Adam」)에서 성이 흔한 명사(Wilk =
  「늑대」, Zielony = 「녹색」)일 때 성이 인식되지 않았습니다.

- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  루마니아어 주소(예: 「010061 București Strada Victoriei 30」)는
  건물번호가 정리되지 않은 채 남아 있었습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  세르비아어 주소(예: 「11000 Beograd Bulevar Kralja Aleksandra
  73」)는 건물번호가 정리되지 않은 채 남아 있었습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  그리스어 주소(예: 「104 32 Αθήνα Ερμού 15」)는 건물번호가 정리되지
  않은 채 남아 있었습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  슬로베니아어 주소(예: 「1000 Ljubljana Slovenska cesta 58」)는
  우편번호가 정리되지 않은 채 남아 있었습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  리투아니아어 주소(예: 「LT-01100 Vilnius Gedimino pr. 9」)는
  우편번호가 완전히 정리되지 않은 채 남아 있었습니다.
- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  헝가리어 주소(예: 「1052 Budapest Kossuth Lajos utca 12」)는
  우편번호가 정리되지 않은 채 남아 있었습니다.
- 「Erben」(상속인들, 예: 「Die Erben Wagner erhielten die Mitteilung
  fristgerecht.」) 뒤의 성은 상속 관련 맥락에서 거의 항상
  인식되지 않았습니다.
- 「Geschwister」(형제자매, 예: 「Die Geschwister Bauer wohnen in
  Linz.」) 뒤의 성은 지금까지 거의 항상 인식되지 않았습니다 -
  「Familie」/「Ehepaar」와 달리 직업명과 같은 성(Koch, Bauer,
  Richter)뿐 아니라 이 위치의 어떤 성에도 해당되었습니다.
- 「Ehepaar」나 「Eheleute」(부부, 예: 「Das Ehepaar Koch zieht
  um.」) 뒤의 성은 흔한 명사나 직업명(Koch, Bauer, Richter)일 때
  인식되지 않았습니다.
- 세금번호나 사회보험번호의 전형적인 그룹 형식(예: 「030 4471
  2298」)을 가진 일반적인 주문·계약·물품 번호가 아무 관련 표기
  없이도 잘못 검게 칠해졌습니다.
- 「연도/일련번호」 형식의 전표·처리 번호(예: 「Rechnung Nr.
  4/2024/778899」)가 전화번호 인식에 의해 잘못 전화번호로 검게
  칠해졌습니다.
- 여러 단어로 된 학위 호칭 사슬이 앞에 붙은 「Herr」/「Frau」 뒤의
  이름(「Herr Dr. med. Weber」, 「Herr Prof. Dr. Krause」)은 지금까지
  전혀 보호되지 않은 채 남아 있었습니다 - 지금까지는 호칭과 이름
  사이의 단일 직함 단어만 인식되었습니다.
- 재판부/부 약어가 붙은 전형적인 형식의 법원 사건 번호(「4 Ca
  1523/24」, 「Az.: 7 O 234/25」)는 지금까지 전혀 보호되지 않은 채
  남아 있었습니다 - 정식 표기 옆의 일반적인 축약형 「Az.」/「Gz.」도
  인식되지 않았습니다.
- 네 자리 그룹 구분 중간에서 줄바꿈으로 분리된 신용카드 번호(예:
  좁은 표 열 안)는 지금까지 전혀 보호되지 않은 채 남아 있었습니다.
- 그룹 구분 중간에서 줄바꿈으로 분리된 세금 식별번호(예: 좁은 표
  열이나 서식 필드 안)는 지금까지 전혀 보호되지 않은 채 남아
  있었습니다.
- 그룹 구분 중간에서 줄바꿈으로 분리된 사회보험번호(예: 좁은 표
  열 안)는 지금까지 부분적으로조차 대체되지 않고 전혀 보호되지
  않은 채 남아 있었습니다.
- 「12a-14b」나 「3-5」처럼 범위가 있는 건물번호는 절반만
  대체되었습니다 - 하이픈 뒤의 두 번째 부분이 결과에 그대로 열려
  있었습니다.
- 17자리 중간에서 줄바꿈, 공백, 하이픈으로 분리된 차대번호(FIN/
  VIN, 예: 좁은 표 열이나 자동차등록증 필드 안)는 지금까지 전혀
  보호되지 않은 채 남아 있었습니다.
- 「Liebe Anna!」나 「Lieber Hans」처럼 이름 뒤에 쉼표가 없는 편지·
  메일 인사말 - 편한 메일에서 가장 흔한 형태 - 은 이름을 전혀
  보호하지 못한 채 남겨 두었습니다. 아래에 본문과 인사말이 있는
  전체 문서에서도 마찬가지였습니다.
- 쉼표가 없는 편한 채팅·메일 인사말인 「Hallo Anna!」, 「Hi Anna!」,
  「Hey Anna!」, 「Servus Anna!」도 같은 허점을 가지고 있었습니다 -
  이름이 전혀 보호되지 않은 채 남았습니다.
- 「MfG」나 「Herzlichst」로 곧바로 시작하는 순수 서명 블록 - 예를
  들어 앞선 문장 없이 클립보드에서 복사된 것 - 은 그 아래 이름을
  전혀 보호하지 못한 채 두었습니다.
- 「Angehörige: Kaczmarek, Piotr (Sohn), Kaczmarek, Anna (Ehefrau)」
  처럼 여러 사람이 있는 필드는 괄호 정보까지 포함해 두 이름을
  하나의 지나치게 긴 결과로 합쳐 버렸습니다 - 두 번째 이름은 결과에
  부분적으로 보호되지 않은 채 남았습니다.
- 「-straße」/「-weg」같은 접미사가 없는 도로명 - 시골 지역에서
  흔한, 예를 들어 「Am Marktplatz 5」나 「Im Grund 12」 - 은 뒤에
  우편번호-지역 줄이 이어지면 인식되지 않았습니다. 예를 들어
  주민등록증명서의 「Neue Anschrift: Am Weidengarten 17, 54295
  Trier」는 도로명이 완전히 사라지고 우편번호만 제거되었습니다.
- 슬래시가 있는 복합 필드 표기 뒤의 이름(예: 「Name/Vorname: Bauer
  Klaus」)은 부분적으로 인식되지 않았습니다 - 「Bauer」처럼 애매한
  성은 필드 증빙 없이는 발견되지 않았습니다. 같은 허점이 「PLZ/Ort:
  04109 / Leipzig」같은 결합 필드에도 있었습니다. 슬래시 대신 서술형
  접속사가 있는 결합 필드(예: 「Vor- und Nachname: Bauer Klaus」나
  「Nachname bzw. Vorname: …」)도 마찬가지였습니다.
- 「Datum der Geburt: …」형식의 생년월일과 「Todesdatum: …」또는
  「Datum des Todes: …」형식의 사망일은 인식되지 않았습니다 -
  「Geburtsdatum: …」또는 「Sterbedatum: …」만 적중했습니다.
- 「Datum der Heirat: …」나 「Datum der Hochzeit: …」형식의 결혼일은
  인식되지 않았습니다 - 「Hochzeitsdatum: …」, 「Heiratsdatum: …」,
  「Datum der Eheschließung: …」만 적중했습니다. 이혼일, 귀화일,
  동반자관계 등록일은 이미 「Datum der X」형식을 알고 있었습니다.
- 「Datum der Scheidung: …」형식의 이혼일은 인식되지 않았습니다 -
  「Scheidungsdatum: …」과 뒤에 오는 동사형만 적중했습니다. 귀화일과
  동반자관계 등록일은 처음부터 「Datum der X」형식을 알고 있었습니다.
- 동반자관계 등록일은 지금까지 전혀 인식되지 않았습니다 - 표기
  (「Verpartnerungsdatum: …」, 「Datum der Lebenspartnerschaft: …」)로도,
  본문(「… wurden am … verpartnert」)으로도 마찬가지였습니다. 이제
  출생일·결혼일·이혼일·귀화일처럼 고유한 정보 종류로 대체됩니다.
- 귀화일은 지금까지 전혀 인식되지 않았습니다 - 표기
  (「Einbürgerungsdatum: …」)로도, 본문(「… wurde am … eingebürgert」)
  으로도 마찬가지였습니다. 이제 출생일·결혼일·이혼일처럼 고유한
  정보 종류로 대체됩니다.
- 이혼일은 지금까지 전혀 인식되지 않았습니다 - 표기
  (「Scheidungsdatum: …」)로도, 본문(「Die Ehe wurde am … geschieden」)
  으로도 마찬가지였습니다. 이제 출생일·사망일·결혼일처럼 고유한
  정보 종류로 대체됩니다.
- 계보학 결혼 기호 「⚭」 뒤의 표기 없는 결혼일은 인식되지
  않았습니다. 같은 줄의 출생일과 사망일은 별표와 십자가를 통해 이미
  인식되고 있었습니다 - 이제 결혼일도 인식됩니다.
- 부고 십자가 뒤의 표기 없는 사망일(「*03.06.1940 †21.11.2023」)은
  인식되지 않았습니다. 그 앞의 출생일은 계보학 별표를 통해 이미
  인식되고 있었습니다 - 이제 사망일도 인식됩니다.
- 앞에 서술 텍스트와 구분선이 붙은 제목/티켓 줄 끝의 성-이름
  순서(「Betreff: Reklamation - Bauer, Anna」)는 직업명과 같은 성일
  때 인식되지 않았습니다 - 이제 인식됩니다.
- 표기 뒤의 지원자·신청자 번호(「Bewerbernummer: 4471829」,
  「Antragstellernummer: 7654321」)는
  인식에서 완전히 빠졌습니다 - 이제 인식됩니다.
- 대체는 이제 읽을 수 있는 자리표시자를 넣을 자리가 없을 때 더 이상
  검게 칠하지 않습니다 - 자리가 조금이라도 남아 있는 한, 너무 작은
  자리표시자는 이제 더 작게 쓰일 뿐 빈 막대가 되지 않습니다. 추가로
  새로운 점: 이미지(레터헤드, 스캔 배경) 위의 결과가 표기될지 검게만
  칠해질지를 이제 나머지 결과 방식과 별개로 설정할 수 있습니다. 그리고
  완전히 제거되는 이미지 위의 결과는 마치 이미지가 그대로 남는 것처럼
  표기되었습니다 - 자리표시자가 결코 검게 칠해지지 않은 배경 위에
  밝게 놓여 있어, 이제 하얗게 된 종이 위에서 보이지 않게 사라졌습니다.
- **보존되는** 이미지 위의 결과는 대체 시 선택한 표현(분류색, 무지개
  등)과 관계없이 항상 흑백으로 검게 칠해졌습니다 - 본문의 색색
  라벨과 레터헤드의 검은 막대 사이의 단절로 눈에 띄었습니다. 이제
  이미지 배경은 옆의 자리표시자와 같은 색을 따릅니다.
- 차량 식별번호(FIN/VIN) 인식은 I/O/Q가 없는 17자리 영숫자 코드를
  무조건 차대번호로 표시했습니다 - 우연히 같은 형태인 주문·시리얼·
  라이선스 키 번호도 마찬가지였습니다. 이제 근처에 문맥 단어
  (「FIN」, 「VIN」, 「Fahrgestell」, 「Chassis」 등)가 있을 때만
  인정합니다.
- 티켓/일정 시스템에서 이름 인식은 「Assigned to」/「Closed by」 등
  뒤에 다음 필드 단어가 같은 줄에 구분자 없이 바로 이어지면 함께
  끌고 갔습니다(「Assigned to Max Mustermann Priority High」가
  「Max Mustermann Priority」가 되었습니다). Git 커밋 머리글에서도
  이름 인식은 두 줄이 줄바꿈 대신 공백 하나로 이어지면 **다음**
  트레일러 키를 함께 끌고 갔습니다(「Author: julia bergmann
  Reviewed-by: …」가 「julia bergmann Reviewed-by」가 되었습니다).
  두 제동 장치가 모두 추가되었습니다.
- 「p.A.」, 「zH」/「zHd」, 「i.A.」/「i.V.」, 「geb.」 뒤의 이름은
  같은 줄에 구분자 없이 바로 다음 부서 단어가 오면 그것까지 하나의
  결과로 끌고 갔습니다(「p.A. Max Mustermann Buchhaltung」이
  「Max Mustermann Buchhaltung」이 되고, 「i.A.Max Mustermann
  Vertrieb」이 「Max Mustermann Vertrieb」이 되었습니다). 「Assigned
  to」/Git 트레일러와 같은 제동 장치가 이제 여기에도 추가되었습니다.
- BIC, BLZ, SWIFT 줄 바로 위의 표기된 IBAN은 「BIC」와 「BLZ」
  자체가 또 다른 숫자 블록처럼 보였기 때문에 그 표기까지 자신의
  결과에 끌고 들어갔습니다 - 「IBAN: DE89 … 0130 00」과 그 아래 줄이
  하나의 지나치게 긴 결과가 되었고, 다음 줄의 표기는 정리하면서
  함께 사라졌습니다. IBAN과 BIC가 아래위로 놓인 거의 모든 계좌
  정보가 영향을 받았습니다.
- 이제 결과 패널은 페이지에서 찾을 수 없는 자리표시자가 **어디에**
  있는지 알려 줍니다. 지금까지 두 경우는 대체가 이루어졌음에도
  「찾을 수 없음」이라고만 표시되었습니다. 자리표시자가 보이지 않는
  부수 텍스트(링크의 참조 주소, 메모, 서식 필드 등)에 있으면 이제
  줄이 그것을 「부수 텍스트에 있음」이라는 별도 정보로 표시하며,
  클릭하면 설명해 줍니다. 그리고 자리가 부족해 자리표시자가 축약
  형태로 쓰였다면(「[NAM382]」 대신 「[N382]」) 이제 클릭하면 긴 줄이
  축약형 위치로 이동하며 이름 변경을 알려 줍니다. 두 줄을 서로
  명시적으로 연결해 대응시킵니다.
- 같은 대체 값이 문서에 여러 번 나오면, 패널 줄을 다시 클릭할
  때마다 다음 발견 위치로 순환하며 이동합니다 - 페이지 경계를
  넘어서도 마찬가지입니다. 상태 줄이 함께 세어 줍니다(「발견 위치
  2/4」), 그리고 지금 향하고 있는 위치는 나머지보다 더 진한 테두리로
  표시됩니다. 자리표시자가 결과 목록에만 있고 문서 어디에도 없다면
  (겹치는 대체 안에 흡수되었기 때문에) 상태 줄이 이제 이를 알려
  주며, 클릭이 아무 반응 없이 조용히 끝나지 않습니다.
- 「an」이나 「für」 뒤의 축약된 이름은 이제 확실히 이름으로
  인식됩니다 - 「Überweisung an M. Wagner」와 「Rechnung für M.
  Wagner」는 지금까지 종종 정리되지 않은 채 남았는데, 같은 이름
  앞에 다른 표기(예: 「Zahlungsempfänger:」)가 있으면 이미 발견되고
  있었습니다. 계좌명세서와 회계 줄이 특히 영향을 받았습니다.
- 「Angeklagter」/「Angeklagte」/「Beschuldigter」/「Beschuldigte」가
  이제 이름 필드로 취급됩니다: 형사소송 문서에서 이런 표기 바로
  뒤에 이름이 있으면 지금까지 검사된 이름의 절반 정도는 전혀
  인식되지 않았습니다 - 이름도 성도 마찬가지였습니다.
- 결과 패널에서 클릭한 위치는 이제 노란색 강조 대신 파란색 테두리로
  표시됩니다 - 색이 있는 신호등 검사 면 위에서는 검색 결과의
  노란색이 잘 보이지 않았습니다. 또한 클릭은 이제 여러 단어로 된
  대체 값(가상의 이름, 마스킹된 번호)도 찾아냅니다 - 지금까지는
  결과 위치가 단어별로만 검색되어 그런 줄에서는 클릭이 효과가
  없었습니다.
- 입양·위탁·의붓 부모(「Adoptivvater」, 「Pflegemutter」,
  「Stiefvater」등)가 이제 이름 필드로 인식됩니다 - 이전에는 이름이
  정리되지 않은 채 빠졌습니다
- 숫자가 많은 표와 목록이 더 이상 잘못 버려지지 않습니다: 짧은
  숫자(예: 전화번호로 잘못 읽힌 고객번호 일부)가 대체되면, 최종
  검사는 같은 숫자열이 다른 완전히 다른 번호 안에 우연히 포함되어
  있어도 여전히 남은 정보로 보고했고, 그러면 결과가 아예 나오지
  않았습니다. 이제 숫자는 독립된 숫자로 존재할 때만 잔여물로
  집계됩니다.
- 신분등록 문서: 「Vater:」/「Mutter:」가 이제 이름 필드로 인식됩니다
  이전에는 부모 이름이 정리되지 않은 채 빠졌습니다
- 그 밖의 가족 역할(「Pate」, 「Großvater/-mutter」, 「Ehepartner」,
  「Lebenspartner」, 「Onkel」, 「Tante」)이 이제 이름 필드로
  인식됩니다 - 이전에는 이름이 정리되지 않은 채 빠졌습니다
- 독일 은행코드(BLZ)는 이제 공식 그룹 구분("370 400 44",
  "370.400.44", "370-400-44", "370/400/44")으로도 인식됩니다 -
  더 이상 여덟 자리 연속 숫자만이 아닙니다.
- 독일 연금보험번호는 이제 다섯 블록 사이에 점, 하이픈, 슬래시가
  있어도 인식됩니다("65-170839-J-08-8", "65.170839.J.08.8") - 더
  이상 공백만이 아닙니다.
- 메인 창이 더 빨리 나타납니다: 인식 라이브러리(언어 모델 하부
  구조를 포함한 Presidio)는 지금까지 창이 구성될 때 이미 로드되어,
  Windows에서는 무언가 보이기까지 약 4초가 걸렸습니다. 이제 완전히
  백그라운드에서 로드됩니다. 「정리」버튼은 지금까지처럼 모든 준비가
  끝나야 활성화됩니다.
- 이미지나 동영상이 많은 Office 문서가 더 빠르게 저장됩니다: 이미
  압축된 미디어는 결과 패키지에서 다시 압축하지 않고 그대로
  저장됩니다 - 지금까지는 이렇게 해도 바이트 하나 절약되지 않았고,
  오히려 JPEG가 더 커지는 경우도 있었습니다.
- 작은 텍스트 단위가 많은 스프레드시트와 문서가 더 빠르게
  검사됩니다: 언어 인식이 이제 문서의 모든 셀과 문단을 개별적으로
  처리하지 않고 한 번에 처리합니다 - 확인 결과 동일한 결과를
  유지하면서(셀 400개: 약 4.7초에서 2.5~3.5초로).
- 목록 형태의 PDF 페이지(색인, 항목 목록)는 자리표시자를 넣을 때
  현저히 빨라졌습니다: 표기별 자리 찾기가 지금까지는 페이지의 모든
  단어를 훑었지만, 이제는 줄 주변만 훑습니다 - 확인 결과 동일한
  결과를 유지하면서(표기 300개가 있는 페이지에서 약 16배 빠름).
- 이미지가 많은 문서는 이미지당 여러 불필요한 작업 단계를
  절약합니다: PDF 페이지의 얼굴과 코드 계수는 페이지 이미지를 더
  이상 두 번 디코딩하지 않고, 메타데이터 검사는 이제 깨끗한 이미지를
  아예 복호화하지 않으며, 모자이크 처리된 이미지는 가장 느린 PNG
  압축 대신 일반 압축으로 저장되고(같은 크기, 3분의 1의 시간),
  워터마크가 설정되지 않았으면 마지막의 무의미한 PDF 전체 재작성이
  사라집니다.
- 텍스트 인식이 켜진 스캔 PDF는 현저히 빨라졌습니다: 지금까지 각
  페이지는 두 번(한 번은 읽기용, 한 번은 래스터화용) 전체 해상도로
  렌더링되었는데, 이제 이미지를 재사용합니다. 그리고 Windows/
  Linux에서는 텍스트 인식이 큰 스캔의 각 스트립마다 새 프로그램을
  실행하는 대신 한 번에 읽습니다.
- 큰 문서가 훨씬 빠르게 정리됩니다: 이미 발견된 값의 대조는
  지금까지 발견 개수에 비례해 늘어났는데(64KB짜리 텍스트 블록이
  큰 파일 끝에서는 이것만으로 약 1초가 걸렸는데, 이제 60분의 1),
  회사 법인 형태 검색은 약 280개의 목록 형태 전체를 모든 텍스트
  위치마다 훑었습니다(이제 확인 결과 동일한 결과를 유지하면서 약
  20배 빠름).
- 「Beste Grüße」/「Beste Wünsche」바로 뒤에 앞선 텍스트나 문장부호가
  없는 이름은 전혀 인식되지 않았습니다 - 앞에 본문 없이 순수 서명
  블록만 있으면 이름이 흔적도 없이 사라졌습니다.
- 문서 시작 부분의 주소 필드에서 직업명과 같은 성(「Bauer Anna」,
  「Koch Stefan」이 도로명과 지역 위 첫 줄로 옴)은 지금까지 일부
  인식되지 않거나 인물 대신 지명으로 분류되었습니다 - 앞선 문장이
  없으면 언어 모델에게 「Bauer」를 직업이 아닌 이름으로 인식하게
  해 주는 문장 구조가 없었습니다.
- 서명 표시 「gez.」 뒤에서 직업명과 같은 성이 이름 앞에 오는
  경우(「gez. Bauer Anna」가 통지서나 판결문 끝에 있음)는 지금까지
  불완전하게 인식되었습니다 - 이름만 발견되고 성은 흔적도 없이
  사라졌습니다.
- 고객번호, 계약번호 등 식별번호 바로 뒤에(별도 줄 없이) 오는
  이름(「Vertragsnummer 55219 Bauer Anna」, 「Kundennr. 4711 Bauer
  Anna」)은 직업명과 같은 성일 때 지금까지 불완전하게 또는 전혀
  인식되지 않았습니다.
- macOS 메뉴 표시줄의 아이콘은 이제 이웃 아이콘처럼 밝은/어두운
  모드에 맞춰 바뀌는 템플릿입니다 - 두 개의 뚫린 막대로 여전히
  Maskuro로 식별할 수 있습니다. 클립보드 감시기가 실행 중이면
  방패 끝의 별도 점으로 표시됩니다.
- 결과 패널의 클릭은 이제 익명화 모드에서도 해당 위치로 이동합니다:
  페이지 전환, 이미지로 스크롤, 노란색 강조. 지금까지는 자리표시자에
  아직 번호가 없다고 여겨 클릭이 반응하지 않았습니다 - 이제 모든
  결과 위치가 고유 번호를 가지므로 위치가 명확합니다. 실제로 번호가
  없는 자리표시자에서만 상태 줄이 왜 이동 목표를 정할 수 없는지
  계속 설명합니다.
- 후처리 편집기에서 처음 저장할 때(Ctrl+S나 디스켓 버튼)는 이제
  「다른 이름으로 저장…」처럼 위치를 묻습니다 - 원본 폴더와 결과
  이름이 미리 채워집니다. 지금까지는 파일이 아무 말 없이 원본 옆에
  저장되었습니다. 상태 줄을 통해 이미 저장 위치를 선택한 경우에는
  다시 묻지 않습니다. 이후 저장은 지금까지처럼 같은 파일을 계속
  덮어씁니다.
- 저장 전 보안 검사가 눈에 띄는 위치를 알리면 이제 「검사로
  돌아가기」가 그곳으로 안내합니다: 첫 번째 발견 위치가 화면으로
  스크롤되어 빨간 테두리로 표시되고, 상태 줄이 위치를 알립니다.
  지금까지는 페이지 번호와 좌표만 남겨졌습니다. 메인 창에서는 이를
  위해 편집기가 해당 위치에서 열립니다. 페이지 수 차이에 대한
  안내에서도 이제 버튼이 두 문서 중 한쪽에만 있는 첫 페이지로
  안내합니다.
- 미리보기를 「두 열 나란히」로 전환하면 이제 두 창이 모두 들어갈
  만큼 창이 저절로 넓어집니다 - 지금까지는 직접 늘릴 때까지 예전
  너비에 억지로 끼어 있었습니다. 넓어지는 것은 화면 끝까지가
  최대이며 다시 좁아지지 않습니다. 직접 조정한 너비는 그대로
  유지됩니다.
- 별도 표 열에 나뉜 성과 이름(예: 신청 확인서나 CSV 내보내기의
  「Nachname | Vorname」)은 정리되지 않은 채 열려 있었습니다 - 각
  셀이 인식에는 그저 이름 관계가 없는 임의의 단어처럼 보였습니다.
  이제 인식됩니다.
- EU 카드형 운전면허증 뒷면의 성과 이름은 정리되지 않은 채 열려
  있었습니다 - 독일어 단어 대신 공식 필드 코드 「1.」과 「2.」 뒤에
  있어 인식되지 않았습니다. 옆에 운전면허번호(필드 코드 「5.」)가
  있으면 이제 인식됩니다.
- 자동차등록증의 차량 소유자 이름은 정리되지 않은 채 열려 있었습니다
  「Vorname」같은 독일어 단어 대신 공식 필드 코드 「C.1.2」 뒤에
  있어 인식되지 않았습니다. 필드 코드 C.1, C.1.1, C.1.2 아래의 성과
  이름이 이제 인식됩니다.
- 여권이나 신분증의 기계 판독 영역(MRZ) 첫 번째 줄은 정리되지 않은
  채 열려 있었습니다 - 「성<<이름」형식으로 이름을 담고 있는데,
  검증 숫자 줄을 위한 새 MRZ 인식기로도 완전히 놓쳤습니다. 이제
  바로 옆에 검증 숫자가 유효한 두 번째 MRZ 줄이 있어야만 결과로
  집계됩니다 - 이름 줄 자체에는 검증 숫자가 없습니다.
- 여권이나 신분증의 기계 판독 영역(MRZ) 두 번째 줄은 완전히
  인식되지 않았습니다 - 여권번호, 생년월일, 만료일을 평문으로
  담고 있지만 기존 인식기 어느 것에도 걸리지 않았습니다. 이제
  전용 인식기가 네 개의 ICAO 검증 숫자를 확인합니다.
- 표기와 공백 없이 붙은 자동차 번호판은 정리되지 않은 채 열려
  있었습니다 - 「KennzeichenM-AB1234」나 「KFZ-KennzeichenM-AB1234」는
  근본 번호판 검사가 번호판 앞에 비단어 문자를 요구했기 때문에
  전혀 인식되지 않았습니다. 필드 단어와 번호판 사이에 공백이 없는
  차량 정보에 영향을 미쳤습니다.
- 표기와 공백 없이 붙은 전화번호는 정리되지 않은 채 열려 있었습니다
  「Handynummer0171/2345678」나 「Tel0171/2345678」은 근본 전화번호
  검사가 번호 앞에 공백이나 문장부호를 요구했기 때문에 전혀
  인식되지 않았습니다. 필드 단어와 번호 사이에 공백이 없는 연락처
  정보에 영향을 미쳤습니다.
- 「geb.」 약어 뒤의 결혼 전 성은 전혀 인식되지 않았습니다 -
  「Julia Bergmann (geb. Weber)」는 「Julia Bergmann」만 찾았고,
  「geb.」의 점이 언어 모델에게 다음 이름을 완전히 건너뛰게
  했습니다. 괄호 안이나 쉼표 뒤에 결혼 전 성이 있는 인물 정보에
  영향을 미쳤습니다.
- 호칭과 직함이 함께 앞에 있을 때 따옴표로 묶인 별명 앞의
  이름(「Herr Dr. Klaus "KP" Peters」)은 정리되지 않은 채 열려
  있었습니다 - 「Peters」만 나오고 「Klaus」는 그대로 읽혔습니다.
  직함과 별명이 있는 서명과 연락처 정보에 영향을 미쳤습니다.
- 점 없는 축약형 「zH」/「zHd」(참조) 뒤의 이름은 전혀 인식되지
  않았습니다 - 점이 있는 「z.Hd.」와 달리, 문장 구조가 없어 이름이
  함께 빠졌습니다. 약어에 점이 없는 주소에 영향을 미쳤습니다.
- 「p.A.」(전교) 뒤의 이름은 전혀 인식되지 않았습니다 - 약어의 점이
  언어 모델의 이름 인식을 완전히 건너뛰게 했습니다. 공동 주소가
  있는 청구서와 지원서에 영향을 미쳤습니다.
- 점 없이 붙은 「i.A.」/「i.V.」(대리로/대행으로) 뒤의 이름은 전혀
  인식되지 않았습니다 - 예를 들어 「i.A.Robert Lang」처럼 공백이
  없는 경우, 「p.A.」와 같은 문장 구조 오류였습니다. 대리 사례의
  서명줄과 이메일 서명에 영향을 미쳤습니다.
- 다른 정보 없이 목록 기호만 있는 순수 참석자 명단(「- Max
  Mustermann」, 줄 끝에 마침표가 붙기도 함)은 원래 「- Farbe:
  Blau」같은 사실 나열만 보호해야 할 제동 장치에 모든 이름을
  빼앗겼습니다. 이런 목록은 이제 인식됩니다.
- 더 이상 정리할 수 없게 된 파일이 다시 정리 가능해졌습니다.
  인식으로 이미 대체된 값이 이미 대체된 자체 표시(예:
  「[SVNR1]」) 안에서 다시 발견될 수 있었습니다 - 최종 검사가
  흠잡을 데 없이 정리된 파일을 그 때문에 거부했습니다. 추가로 CSV
  표의 전화 참조도 이제 함께 제거되며, 검색을 특정 종류로 제한한
  경우 이제 문서 전체에서 동일하게 적용됩니다 - 이미지의 대체
  텍스트, Excel 머리글, 선택 목록, HTML 속성에서도 마찬가지입니다.
- 이메일 머리글 「To:」(또는 콜론 없는 「To」) 뒤의 이름은 인식되지
  않았습니다 - 외국어 언어 모델이 줄 전체를 하나의 눈에 띄지 않는
  결과로 읽어 그 안의 이름을 완전히 삼켜 버렸기 때문입니다 - 같은
  이름 앞의 「Cc:」, 「Bcc:」, 「From:」과는 달랐습니다. 「To」 뒤의
  이름은 이제 확실히 발견됩니다.
- 결혼일은 사용자 정의 규칙에서 날짜로 다룰 수 없었고(「이동」이
  「날짜 정보에만 있음」이라는 이유로 거부됨), 결과 종류의 그룹
  배정에도 빠져 있어 「검색 대상」표시로 끌 수 없었으며, 사망일처럼
  짧은 약어 대신 전체 단어를 자리표시자로 받았습니다. 여섯 개의
  약어/표기 표 전체에 걸쳐 반영되었습니다.
- 미리보기에서 일부러 선택 해제한 값이 다른 위치에서는 그래도 검게
  칠해질 수 있었습니다: 예를 들어 이메일 주소를 선택 해제하면
  주소 자체는 남았지만, 도메인 없는 로컬 부분이 다른 선택된
  인물에서 유도된 사용자 이름과 겹치면 대체되었습니다
  (「anna.musterfrau@beispiel.de」가 「Anna Musterfrau」 옆에 있는
  경우). 선택 해제된 문구는 이제 어떤 결과 종류에서 나왔든 문서
  전체에서 금기로 남습니다.
- 가족부나 신분등록 발췌본이 출생일을 출생지와 공통 표제 아래
  담고 있으면(「Geburtsdatum, Geburtsort: 19.11.1982, Steyr」)
  출생일이 인식되지 않았습니다 - 「Geburtsdatum」과 날짜 사이의
  두 번째 필드 단어가 지금까지 인식을 완전히 무너뜨렸습니다.
- 이미 인식된 전화번호는 같은 문서의 다른 곳에서 마지막 네 자리로만
  언급되면(「erreichbar unter der Nummer ...5678」, 「Rückruf unter
  ...5678」) 축약된 확인 형태로 그대로 읽혔습니다 - IBAN과
  신용카드에서와 같은 형태였습니다.
- 이미 인식된 신용카드 번호는 같은 문서의 다른 곳에서 마지막 네
  자리로만 언급되면(「Ihre Kreditkarte endet auf ...0366」) 축약된
  확인 형태로 그대로 읽혔습니다 - 결제 확인서에서 흔한, IBAN과
  같은 형태였습니다.
- 이미 인식된 IBAN은 같은 문서의 다른 곳에서 마지막 네 자리로만
  언급되면(「Die IBAN endet auf ...3201」) 축약된 확인 형태로 그대로
  읽혔습니다 - 확인 이메일에서 흔한 형태였습니다.
- 채팅이나 회의록의 발언자는 이름 앞에 호칭이 있으면(「Herr Bauer:
  …」, 「Frau Koch: …」) 인식되지 않았습니다 - 그리고 같은 회의록의
  다음 발언자 줄에도 종종 영향을 미쳤습니다. 문서 전체를 회의록으로
  평가할 만큼 인식된 줄이 충분히 남지 않았기 때문입니다.
- 필드 단어 「geboren」이 날짜 【뒤에】 오면(「Das Kind wurde am
  14.01.2026 geboren」) 출생일이 인식되지 않았습니다 - 육아휴직이나
  모성보호 증명서가 자녀의 출생일을 이렇게 표현합니다. 지금까지의
  패턴은 항상 필드 단어가 날짜 앞에 온다고 가정했습니다.
- 반응 표시나 체크 표시가 바로 앞에 붙은 서식 표기(「Ansprechpartner
  😊:」, 「Kontaktperson ✓:」)는 더 이상 표기로 인식되지 않았고,
  그 아래나 뒤의 이름도 그로 인해 부분적으로만 발견되었습니다(예:
  「Mayer Roman」에서 성만 발견).
- 같은 허점이 GDPR 제9조상 특별히 보호해야 할 정보(종교, 건강,
  노동조합)에도 영향을 미쳤습니다: 구분자나 줄바꿈 바로 앞의 반응
  표시(「Konfession 😊: römisch-katholisch」)는 표기 자체를 완전히
  무너뜨렸고, 정보는 전혀 인식되지 않은 채 남았습니다.
- 지명에 하이픈이 있는 복합 지명의 주소(예: 「79761
  Waldshut-Tiengen」, 「78050 Villingen-Schwenningen」)는 지명 자체는
  인식되어 검게 칠해졌음에도 우편번호가 완전히 사라졌습니다 -
  자동차등록증이나 서신에서 우편번호가 그대로 읽혔습니다.
- 열 간격이 없는 표 열(실제 PDF 텍스트 추출)은 이름 열 아래에서
  우연히 나란히 있는 두 개의 대문자 단어도 잘못 인물로 검게 칠할
  수 있었습니다 - 예를 들어 데이터 줄에 있는 두 지명. 이제 같은
  위치에서 다른 결과가 이미 무언가를 인식하지 않을 때만 그렇게
  됩니다.
- 같은 이름 열은 같은 줄 형태에서 언어 모델이 모르는 두 사실 단어
  (예: 「Frontend Backend」, 「Turbo Modul」)도 잘못 인물로 검게
  칠했습니다 - 같은 위치에서 다른 결과가 제동을 걸지 않았기
  때문입니다. 이제 두 단어 중 적어도 하나가 언어 모델 자체에 의해
  고유명사로 읽힐 것을 추가로 요구합니다.
- 독일 연금보험번호는 공식적인 완전 그룹 구분(예: 「65 170839 J 08
  8」 - 사회보험증과 급여명세서에 표기되는 그대로)으로는 인식되지
  않아 원본 그대로 남았습니다 - 압축 표기와 글자까지만 그룹 구분된
  형태만 인식되었습니다.
- 세금 식별번호는 공식 표기(2-3-3-3 그룹 구분, 예: 「48 836 075
  988」 - 실제 세금 통지서와 연방중앙세무청 통지서에 표기되는
  그대로)로는 전혀 인식되지 않아 원본 그대로 남았습니다 - 더 드문
  3-3-3-2 그룹 구분만 포함되어 있었습니다.
- 노르트라인베스트팔렌주 세금번호(예: 「221/5147/0815」, 두 번째
  그룹이 세 자리 대신 네 자리)는 세금 통지서에서 전혀 인식되지
  않아 원본 그대로 남았습니다 - 다른 모든 주는 이미 포함되어
  있었습니다.
- 근로계약에서 표기 「Arbeitgeber:」 뒤의 이름은 성이 흔한
  단어일 때(예: 「Bauer Anna」) 완전히 간과되었습니다 -
  「Arbeitgeber」는 이름 표기와 회사 표기 목록에 동시에 있어서,
  회사 배정이 이름 배정을 덮어썼습니다.
- 「Vermieter:」/「Mieter:」표기가 있는 임대차계약 머리글에서 흔한
  단어이기도 한 성(예: 「Bauer」)이 간과되었습니다 - 이름만
  인식되었습니다. 번호가 매겨진 임차인(「Mieter 1:」, 「Mieter
  2:」)은 이런 모호함이 없는 이름에서도 추가로 영향을 받았습니다.
- 표기 「Zeuge:」/「Kläger:」/「Beklagter:」(번호도 포함, 「Zeuge
  1:」, 「Zeuge 2:」)가 있는 법원 조서에서 흔한 단어이기도 한
  성(예: 「Bauer」)도 마찬가지로 간과되었습니다 - 이름만
  인식되었습니다.
- 상속증명서, 위임장, 독촉절차, 매매계약에서 흔한 단어이기도 한
  성(예: 「Bauer」)이 「Erblasser:」, 「Erbe:」, 「Vollmachtgeber:」,
  「Bevollmächtigte:r」, 「Antragsgegner:」, 「Schuldner:」,
  「Gläubiger:」, 「Käufer:」, 「Verkäufer:」,
  「Vermächtnisnehmer:」, 「Testamentsvollstrecker:」같은 표기 뒤에서
  간과되었습니다 - 이름만 인식되거나 이름 전체가 빠지기도 했습니다.
- 소송 표시 구분자 「./.」앞의 다자간 목록(예: 「Sand, Werner und
  Huber, Anna ./. Wechsler, Martina」)에서는 첫 번째 당사자가 마스킹
  처리되지 않았습니다 - 「./.」에 바로 인접한 당사자만 인식되었습니다.
- 소송 표시 구분자 「./.」(예: 「Sand./.Wechsler」)에서 공백이 없으면
  그 뒤의 이름이 완전히 간과되었습니다 - 앞뒤에 공백이 있을 때만
  인식되었습니다.
- 성 「Wahr」는 단독으로 있으면 완전히 간과되었습니다(예: 「Frau
  Wahr bearbeitet Ihren Vorgang.」) - 이 단어는 우연히 「Das ist
  wahr.」같은 문장에서 이름으로 오인되는 것을 막는 일반 독일어
  단어 목록에도 있습니다.
- 「Los」, 「Weit」, 「Rund」, 「Hoch」같은 성은 단독으로 있으면
  완전히 간과되었습니다(예: 「Herr Hoch übernahm die Leitung.」) -
  네 단어 모두 우연히 「Rund einhundert Gäste kamen zur Feier.」같은
  문장에서 이름 오인을 막는 일반 독일어 단어 목록에도 있습니다.
- 「Ganz」나 「Recht」같은 성은 단독으로 있으면 완전히 간과되었습니다
  (예: 「Herr Ganz unterschrieb den Vertrag.」) - 두 단어 모두
  우연히 「Ganz genau, das stimmt.」같은 문장에서 이름 오인을 막는
  일반 독일어 단어 목록에도 있습니다.
- 표기 뒤에 별표나 위 첨자 각주 번호가 붙은 서식 필드(예:
  「Konfession*: römisch-katholisch」나 「Religionszugehörigkeit¹:
  evangelisch」)는 인식되지 않아 평문 그대로 남았습니다 - 이 기호가
  없는 형태만 적중했습니다.
- 표기 뒤에 각주 표시가 두 개 붙어 있어도(예: 「Konfession**:
  römisch-katholisch」나 「Gewerkschaft¹²: ver.di」) 같은 필드가
  계속 평문 그대로 남았습니다.
- 「Softwareversion 4.2.1.19」나 「Firmware Build 2.0.4.11」같은
  버전 번호가 더 이상 잘못 IP 주소로 검게 칠해지지 않습니다.
  「Rechnungsnummer 10.20.30.40」이나 「Bestellnummer 7.8.9.10」같은
  전표·처리 번호도 마찬가지입니다.
- 바로 아래위로 있는 두 개의 IBAN(예: 청구서 머리글에 자신의
  IBAN과 해외 거래처의 IBAN)이 둘 다 인식되지 않는 경우가
  있었습니다 - 두 번째 IBAN이 눈에 띄지 않게 남았습니다.
- 표기가 붙은 IBAN이 뒤따르는 단어가 소문자로 시작하면 문장의
  다음 단어까지 함께 끌고 가는 경우가 있었습니다(「Bankverbindung
  AT61 … wird belastet」가 「wird」까지 검게 칠해짐) - 옆의 평문
  나머지 부분은 그대로 남았습니다.
- 리히텐슈타인 주소(「FL-9490 Vaduz」)가 이제 독일, 오스트리아,
  스위스 주소와 마찬가지로 인식됩니다.
- 여권 번호가 이제 표기 뒤에서 인식되어 제거됩니다(예:
  「Reisepassnummer: C01X00T471」).
- 체류허가증 번호와 주민등록증명서 번호가 이제 표기 뒤에서 인식되어
  제거됩니다.
- 콜론 대신 줄표로 구분된 식별번호도 이제 표기 뒤에서 인식됩니다
  (예: 「Kundennummer – K903944」).
- 「IBAN」이나 「Kontonummer」로 표기된 계좌 정보도 이제 콜론 대신
  줄표로 구분되어도 인식됩니다.
- 「Kontaktperson (Vertrieb)」나 「Sachbearbeiter/in」같은 표기 뒤의
  이름도 이제 괄호 부기나 성중립 슬래시 어미와 함께 인식됩니다.
- 같은 별표 성중립 형태(「Sachbearbeiter*in」)도 이제 인식됩니다.
- 표기 뒤의 이름은 콜론 대신 등호(=)로 구분되어도 이제 인식됩니다
  (예: 「Ansprechpartner = Mayer Roman」나
  「Kontaktperson=Mayer Roman」) - 설정 파일이나 CSV 머리글에서
  흔한 형태입니다. 이런 표기-값 쌍이 세미콜론으로 구분되어 한 줄에
  여러 개 있으면 첫 번째 값만 인식되고 나머지 줄 전체는 인식되지
  않습니다.
- 「Koordinaten」단어 뒤의 GPS 좌표 쌍이 이제 확실히 인식됩니다
  (예: 「Koordinaten: 48.2082, 16.3738」) - 내부 목록의 단어
  변화형이 잘못되어 있었습니다.
- 표기 뒤의 식별번호(고객번호, 계약번호, 사건번호, 신분증번호 및
  약 백여 개의 다른 필드 단어)는 표기가 저장된 대소문자와 정확히
  일치하지 않으면 인식되지 않았습니다 - 이메일의 「kundennummer:」나
  서식 머리글의 「KUNDENNUMMER:」는 그대로 남았습니다.

### 신규

- **현실적인 대체 값은 이제 기본값이 아니라 의도적으로 사용하는
  예시입니다.** 「자리표시자」탭의 예외 표는 이제 비어서 시작합니다.
  새 버튼을 누르면 이름, 지역, 주소, 조직, 이메일, 전화, 내선,
  IBAN에 그럴듯한 가짜 값을 원할 때 입력할 수 있습니다. 금액은 이
  버튼으로 의도적으로 번호 매겨진 자리표시자로 남겨 두며, 「가상
  값 생성」전략은 개별 종류에 대해 여전히 수동으로 선택할 수
  있습니다.
- **AI 단계에서 그래픽카드를 사용할 수 있습니다.** Windows에서는
  약 17MB 크기의 추가 패키지를 내려받을 수 있으며, 이후 AI 단계는
  적합한 그래픽카드에서 프로세서보다 훨씬 빠르게 계산합니다.
  그래픽카드가 없거나 아무것도 내려받지 않아도 계속 작동합니다 -
  다만 더 느릴 뿐입니다. macOS에서는 가속 기능이 이미 기본으로
  내장되어 있습니다.
- **작업 표시줄 아이콘을 통한 새로운 알림 두 가지**: 미리보기가
  대체 전 검토를 위해 준비되었을 때, 그리고 처리가 끝났을 때입니다.
  둘 다 기본으로 켜져 있으며 *설정 → 프로그램 → 알림*에서 개별적으로
  끌 수 있습니다.

### 변경됨

- **신분증 번호와 운전면허번호가 이제 표기가 앞에 있으면
  인식됩니다**(「Personalausweisnummer: …」, 「Führerscheinnummer:
  …」) - 지금까지는 둘 다 어떤 인식도 통과하지 못했습니다.
- **Maskuro는 이제 Windows의 고대비 디자인을 따릅니다.** *설정 →
  접근성 → 고대비 디자인*을 켠 경우 지금까지는 여기를 제외한 모든
  곳에 적용되었습니다 - Maskuro는 이후 자체 색상을 사용했습니다.
  이제 창, 목록, 놓기 영역, 기록, 상태 색이 시스템 디자인을
  따릅니다. 미리보기와 후처리 창의 색상 신호등 표시는 이 경우
  의도적으로 생략됩니다 - 그것이 알려 주는 정보는 이미 기호와
  단어로도 옆에 표시되기 때문입니다.
- **검토 필요 여부가 더 이상 색상만으로 표시되지 않습니다.** 빨강,
  주황, 초록은 밝기가 거의 같습니다 - 적록색약이 있는 사람은
  미리보기와 결과 패널에서 차이가 없는 목록을 보았고, 이는 남성
  약 열두 명 중 한 명에 해당합니다. 이제 각 줄에는 모양이 다른
  기호도 추가로 붙습니다: ▲ 먼저 검토, ● 검토, ○ 잘 증빙됨, ◆
  평가 없음. 짧은 안내문이 말로도 이를 알려 주며, 화면 낭독기가
  읽어 줍니다.
- **Alt 키로 메뉴를 다시 예전처럼 열 수 있습니다.** 메뉴 줄에는
  키보드 단축키가 없었습니다 - 마우스를 쓰지 않는 사람은 모든
  메뉴를 화살표로 훑어야 했습니다. 이제 모든 항목에 밑줄 친 글자가
  붙습니다 - 「Datei(파일)」는 Alt+D, 거기서 「Beenden(종료)」은 B -
  오버레이의 모든 언어에서 마찬가지입니다.
- **조작 요소가 다시 화면 낭독기에 자신의 용도를 알려 줍니다.**
  후처리 창, 규칙 창, 기록, 단어 목록, 도움말, 검색, 그 밖의 다섯
  개 창에서 목록, 검색란, 드롭다운, 슬라이더는 지금까지 「트리」나
  「콤보박스」로만 안내되었고 무엇에 관한 것인지는 알려 주지
  않았습니다. 약 40곳이 이제 이름을 갖습니다. (메인 창은 8월부터
  이미 문제가 없었습니다. 그 뒤에 추가된 창들은 이 개선을 함께
  받지 못했었습니다.)
- **키보드로 조작하면 자신이 어디에 있는지 어디서나 보입니다.**
  미리보기의 검토 필요 슬라이더, 체크박스, 「다시는」버튼, 그 안의
  분류 표제, 후처리 창의 페이지 패널, 설정의 사이드바에는 시스템이
  보통 이동된 조작 요소 주위에 그리는 테두리가 빠져 있었습니다.
- **더 큰 시스템 글꼴에서도 더 이상 잘리지 않습니다.** *접근성 →
  텍스트 크기*를 175퍼센트 넘게 설정하면 지금까지는 폴더 감시와
  단축키 필드의 표기 끝부분을 잃었습니다. 도움말의 장 목록은
  일반 글꼴에서도 이미 긴 장 이름을 잘랐습니다 - 이제 줄바꿈되며
  짧은 안내문에 전체 이름이 표시됩니다.

- **인식 속도가 눈에 띄게 빨라졌습니다.** 표기된 식별번호
  (「Kundennummer: K903944」) 인식기는 지금까지 텍스트 조각마다
  1200개가 넘는 개별 패턴을 순서대로 검사했습니다 - 모든 문단과
  표 셀마다 인식 시간의 가장 큰 부분을 차지했습니다. 이제 같은
  결과를 내는 단일 패턴 하나입니다: 측정 자료에서 결과는 단 하나도
  바뀌지 않으며, 텍스트 조각당 기본 단계는 약 3~4배 빨라집니다.
- **창이 시작하자마자 바로 나타납니다.** 지금까지는 메인 창이
  보이기도 전에 전체 언어 도구를 로드했습니다 - 시작할 때마다 약
  4초의 공백 시간이 있었습니다. 이제 창이 이미 떠 있는 동안
  모델이 원래 의도대로 백그라운드에서 로드됩니다. 「정리」버튼은
  지금까지처럼 모든 준비가 끝나야 활성화됩니다. 순수 정보 조회용
  명령줄 호출(예: `--version`)도 이제 몇 초가 아니라 바로
  응답합니다.
- **자동 언어 인식에서는 이미지를 이제 한 번만 읽습니다.** 지금까지
  「언어: 자동」기본 설정에서는 같은 이미지를 텍스트 인식이 두
  번(한 번은 언어 추측, 한 번은 실제 검사) 훑었습니다. 이미지
  파일, 클립보드 이미지, 텍스트 창은 이제 약 두 배 빠르게
  끝납니다. 텍스트 인식이 꺼져 있으면 지금까지 눈에 띄지 않게
  그래도 실행되던 읽기가 완전히 사라집니다.
- **저장된 웹페이지와 이메일이 더 빠르게 정리됩니다.** HTML 속성,
  주석, 삽입된 데이터 블록의 값은 지금까지 개별적으로 인식되었습니다
  수백 개의 표기가 있는 지자체 페이지는 인식에 수백 번의 개별
  질문을 던졌습니다. 이제 값들을 모아 서로 다른 값마다 한 번만
  인식합니다. 측정 자료에서 결과는 바뀌지 않으며, .html과 .eml은
  약 3분의 1 더 빠릅니다.
- **표와 프레젠테이션의 부속 저장소도 일괄 인식됩니다.** 대체
  텍스트, 수식 문자열, 차트 표기, 주석, 피벗 캐시, 문서 속성은
  값마다 각각의 인식 질문을 던졌습니다 - 피벗 행이 수천 개인
  통합 문서는 그만큼의 질문이었습니다. 이제 서로 다른 값에 대해
  모아서 한 번 실행되며, 마지막의 추가 전체 통과는 본문 텍스트
  이후 실제로 새 값이 추가되었을 때만 실행됩니다. 측정 자료에서
  결과는 바뀌지 않습니다.
- **서식이 많은 PDF가 더 빠르게 정리됩니다.** 필드, 메모, 책갈피,
  참조는 같은 값을 대량으로 반복합니다(모든 체크박스의 「Off」,
  모든 주석의 같은 작성자) - 지금까지는 각각 자신의 인식 질문을
  던졌습니다. 실행당 값은 이제 한 번만 인식됩니다. 대체와 일관성
  후처리는 지금까지처럼 위치별로 실행됩니다.
- **큰 표 파일(.csv/.tsv)이 눈에 띄게 빠르게 정리됩니다.** 네 개의
  표 후처리 단계는 지금까지 같은 파일을 각자 문자 단위로 셀로
  분해했습니다(40MB에서 약 30초의 추가 작업) - 이제 분해는 한
  번만 실행됩니다. 열 머리글 인식(생년월일 및 사번 열)은 셀마다
  하나씩이 아니라 모아서 하나의 질문을 던집니다 - 동일한 결과에서
  약 20배 빠릅니다. 그리고 큰 인사 명단의 이름 열 합산도 더 이상
  줄 수에 제곱으로 비례하지 않습니다.
- **지표 접이식 패널이 더 이상 창을 멈추지 않습니다.** 지표
  펼치기는 크고 많은 파일에서 먼저 텍스트를 모두 모은 뒤에야
  표시되어, 그동안 창이 몇 초씩 멈춰 있었습니다. 계산은 이제
  백그라운드에서 실행됩니다 - 패널은 즉시 열리고 숫자는 나중에
  채워집니다.
- **검색 보고서가 더 이상 창을 멈추지 않습니다.** 수천 개의 파일을
  검색한 뒤, 영향을 받은 파일마다 공통 폴더를 새로 계산했습니다 -
  큰 실행에서는 창이 몇십 초씩 멈춰 있었습니다. 이제 보고서가
  즉시 나타납니다.
- **텍스트 인식이 켜진 PDF가 더 빠르게 검사됩니다.** 대조 검사
  중 각 페이지가 불필요하게 두 번 PNG 형식으로 변환되었습니다 -
  이제 이미 있는 이미지를 그대로 전달합니다. 결과는 변하지 않으며,
  검사만 더 빨라집니다.
- **큰 이미지의 진행 기록 주석이 더 이상 끊기지 않습니다.**
  그러데이션이 있는 주석의 손잡이를 조정할 때 그러데이션이
  지금까지 점 단위로 다시 계산되어, 큰 스크린샷에서는 눈에 띄게
  버벅거렸습니다. 결과는 같지만 멈춤이 없습니다.

### 수정됨

- **파일을 목록에서 제거하는 십자가가 다시 단순한 X로
  돌아왔습니다.** 새로운 편집기 도구 「제거」가 실수로 같은 아이콘
  식별자를 사용해, 빨간 십자가와 점선 텍스트 줄이 각 파일 줄에도
  함께 표시되었습니다. 두 동작은 이제 각자 별도의 아이콘 이름을
  가지며 각각에 맞는 표시를 유지합니다.
- **여러 부분으로 나뉜 정보는 PDF에서 눈에 보이는 줄바꿈을 넘어서도
  인식됩니다.** Maskuro는 기하학적으로 생성된 페이지 텍스트를
  오프셋이 같은 본문 뷰로도 추가로 읽습니다. 이는 「Diabetes
  mellitus Typ 2」라는 처음 눈에 띈 사례뿐 아니라 모든 기본·고급
  단계 인식기와 사용자 정의 패턴에 해당합니다. 빈 줄과 인식된 표
  또는 구역 경계는 여전히 확실한 경계로 남으며, 결과 위치는
  여전히 검게 칠할 단어에 정확히 맞습니다.
- **「가명 처리」의 예시가 스스로 모순되었습니다.** 문장은 「같은
  사람, 같은 번호」를 약속했지만 두 개의 다른 번호를 보여 주었습니다
  「익명 처리」에서 맞는 그림이었습니다. 이제 두 예시 모두 자신의
  문장과 일치합니다.
- **막 삽입된 자리표시자가 「원본 되돌리기」시 사라지지 않고 겹쳐진
  글자 뭉치로 남을 수 있었습니다.** 단색으로 넣은 자리표시자는
  지금까지 글자마다 별도의 출력 명령을 썼는데, 그중 첫 번째만
  자체 텍스트 매트릭스를 가졌습니다 - 같은 위치를 다시 편집할 때
  (예: 곧바로 「되돌리기」) 나머지 문자 명령들이 차례로 첫 번째의
  문자 인덱스를 넘겨받아, 자리표시자가 겹쳐진 두 위치로 쪼개졌습니다.
  단색 자리표시자는 이제 전체 텍스트에 대해 하나의 출력 명령만
  받습니다.

- **후처리 창에서 같은 검게 칠해지거나 제거된 값이 두 줄 아래에
  있고 둘 다 되돌리기로 표시되면, 두 번째 줄이 값이 이미 되돌려진
  뒤에도 잘못 「고유하지 않음」으로 집계되었습니다.** 이제 두 줄
  모두 완료로 처리됩니다.

- **「Reply-To:」 뒤의 이름이 이제 발견됩니다.** 「Reply-To: Huber」
  같은 이메일 머리글에서 지금까지 이름이 전혀 인식되지 않았습니다
  언어 모델이 「Reply-To:」자체를 별도의 잘못된 인물로 읽고 그
  뒤의 실제 이름을 간과했습니다.

- **이메일 머리글 단어 「Reply」와 「Fwd」자체가 더 이상 이름으로
  검게 칠해지지 않습니다.** 「Fwd: Angebot von Huber」같은 제목
  줄에서 지금까지는 이름 외에 머리글 단어 자체도 인물로 인식되어
  검게 칠해졌습니다.

- **「Arbeitgeber: Siemens AG」가 이제 인물이 아니라 회사로
  인식됩니다.** 표기 「Arbeitgeber」 뒤의 회사 값이 GmbH, AG, KG
  같은 법인 형태를 지니고 있으면, 조직 인식이 켜져 있어도 여전히
  인물로 인식되었습니다 - 법인 형태가 없는 더 좁은 경우(「Wollmuth
  und Partner」)만 지금까지 회사로 인식되었습니다.

- **한 번 인식된 주소가 다른 위치에는 더 이상 남지 않습니다.**
  도로 주소가 한 곳에서 인식되어 대체되어도, 같은 주소가 두 번째
  위치에는 남을 수 있었습니다 - 예를 들어 스캔한 문서의 읽기 어려운
  바닥글에서 자동 텍스트 인식이 이를 훼손된 형태로 읽었을 때
  그랬습니다. 이제 주소도 이름과 회사처럼 이미 오래전부터 그랬듯
  문서 전체에서 일관되게 제거됩니다.

- **수신자가 여러 명인 이메일은 정리 과정에서 조용히 손상되었습니다.**
  수신자가 둘 이상인 `.msg`메시지는 저장할 때 내부 구조 일부를
  잃어, 정리된 결과가 불완전했습니다. 원인은 모든 수신자마다
  나타나는 같은 이름의 내부 구성 요소를 혼동한 것이었습니다. 이런
  메시지는 이제 완전히 다시 구성됩니다.

- **함께 제공되는 테스트 문서 두 개가 Word와 PowerPoint에서 열리지
  않았습니다.** 측정 자료를 내려받으면 `format_dokument.docx`에서
  「Word에서 파일을 여는 중 오류가 발생했습니다」, `format_
  praesentation.pptx`에서 「파일이 손상되었습니다」가 나왔습니다.
  두 파일 모두 Maskuro가 손대기 전부터 이미 결함이 있었습니다 -
  정리된 버전은 그 오류를 그대로 이어받았을 뿐입니다. LibreOffice는
  둘 다 문제없이 열었기 때문에 아무도 알아차리지 못했습니다.

- **인터넷상의 자체 AI는 이제 암호화되어 연결됩니다.** 자체 AI에
  「https://」없이 외부 주소를 입력하면(IT 부서의 메모에 흔히 그렇게
  적혀 있음) 지금까지는 암호화되지 않은 연결로 접속했습니다 -
  검게 칠해지지 않은 텍스트가 평문으로 나갔습니다. 이런 주소는
  이제 「https://」로 접속됩니다. 자체 네트워크 안의 서버는 변함없이
  접속 가능합니다. 서버가 다른 컴퓨터로 리디렉션되면 접근 키는
  더 이상 함께 넘어가지 않습니다.

- **손상된 이미지도 이제 숨겨진 메타데이터를 잃습니다.** 삽입된
  이미지를 완전히 열 수 없으면(예: 잘린 사진) 지금까지는 EXIF와
  GPS 데이터를 그대로 유지했습니다 - 촬영 장소와 사진작가 이름이
  결과에 보이지 않게 남았습니다. 이런 이미지도 이제 전혀 표시할 수
  없을 때도 이 정보에서 벗어납니다.

- **정리할 수 없었던 삽입 파일은 이제 조용히 그대로 넘기지 않고
  알려 줍니다.** 프레젠테이션이나 통합 문서에 너무 깊이 중첩되었거나
  열 수 없는 삽입 개체가 있으면 지금까지는 알림 없이 결과에 그대로
  남았습니다 - 파일은 정리된 것으로 취급되었습니다. 이런 경우는
  이제 삽입된 옛 형식과 마찬가지로 「검사할 수 없었습니다」경고에
  표시됩니다.

- **어두운 목록이 다시 일관되게 어둡고 읽기 쉬워졌습니다.** macOS에서
  파일 목록이 거의 검정과 밝은 회색 줄 사이를 오갔습니다 - 후처리
  창에서 같은 초록, 주황, 빨강 검사 값이 줄마다 다르게 보였습니다.
  창, 목록, 글꼴, 자리표시자, 선택은 이제 하나의 공통 명암 팔레트를
  사용합니다. 색상으로 구분된 결과 목록도 더 이상 색 아래에 얼룩말
  줄무늬를 깔지 않습니다.

- **「als」가 들어간 직업 정보가 잘못 이름으로 검게 칠해졌습니다.**
  「Als Koch ist er seit vier Jahren bei uns tätig.」같은 문장은
  이름이 아니라 직업을 잃었습니다 - 「als」는 「der」나 「die」와
  마찬가지로 역할 정보를 이끕니다. 같은 위치의 실제 성(예: 앞에
  호칭이 있는 경우)은 영향을 받지 않습니다.

- **표 머리글이 항목 번호를 금액에 끌어들일 수 있었습니다**(「금액도
  함께 제거」옵션이 켜져 있을 때만). 한 줄이 통화로
  끝나고(「… Einzelpreis EUR」) 다음 줄이 숫자로 시작하면, 줄바꿈을
  넘어 잘못 금액이 만들어졌습니다. 통화와 숫자 사이의 구분자는
  이제 같은 줄에 남습니다.

- **대문자로 된 짧은 약어가 문장 일부를 통째로 삼키거나, 올바르게
  인식된 이름 앞에 붙을 수 있었습니다.** 「DI」, 「AG」, 「KG」같은
  두 글자 대문자 단어 - 일상적인 약어일 뿐 이름이 아님 - 가 줄에
  있으면, 줄 전체가 시험 삼아 소문자로 바뀌어 검색되었고, 약어가
  이웃 단어(동사 포함)를 하나의 가짜 이름으로 끌어들이는 경우가
  있었습니다. 이제 세 글자 이상의 대문자 단어만 이 두 번째 검사를
  유발합니다. 「CEO」나 「USB」처럼 조금 더 긴 약어에는 두 번째
  오류가 남아 있었습니다: 이미 올바르게 발견된 이름(「Schneider」)
  앞에 약어가 접두사처럼 결과에 함께 붙었습니다(「CEO Schneider」).
  약어는 이제 결과 밖에 남습니다.

- **뒤에 공백이 없는 생년월일은 그대로 남았습니다.** 「geb.」 뒤에
  날짜 앞 공백이 없으면(좁게 짜인 서식에서 흔한 「geb.14.03.1988」)
  Maskuro가 이 필드를 인식하지 못해 날짜를 그대로 두었습니다.
  「Geburtsdat.」나 「Geb.-Dat.」같은 흔한 축약형도 이제 함께
  인식됩니다.

- **슬래시를 구분자로 쓴 IBAN은 그대로 남았습니다.** 전화번호
  (「0664/1234567」)처럼 일부 서식은 IBAN도 공백이나 하이픈 대신
  슬래시로 블록을 구분해 씁니다(「AT48/3200/0000/1234/5864」). 이
  표기도 이제 인식됩니다.

- **하이픈, 점, 슬래시가 있는 오스트리아 사회보험번호는 그대로
  남거나 잘못 표기되었습니다.** 두 숫자 블록 사이에는 지금까지
  공백만 예상되었습니다 - 「1237-010180」, 「1237.010180」,
  「1237/010180」같은 표기는 인식되지 않거나(슬래시의 경우) 잘못된
  종류로 인식되었습니다. 검증 숫자는 구분자와 관계없이 계속 모든
  결과를 확인합니다.

- **주소의 「c/o」 뒤 이름은 아예 제거되지 않았습니다.** 「c/o Max
  Mustermann, Hauptstraße 5, 1010 Wien」은 도로명과 지역은
  검게 칠했지만 그 뒤의 이름은 완전히 그대로 두었습니다. 이제
  이름이 인식됩니다. 「c/o」자체는 주소 안내로 계속 보입니다.

- **점으로 그룹 구분된 신용카드 번호는 그대로 남았습니다.**
  「4111.1111.1111.1111」같은 표기는 인식되지 않았습니다 - 공백이나
  하이픈으로 구분된 번호는 영향을 받지 않았습니다. 검증 합계는
  계속 모든 결과를 확인합니다.

- **하이픈으로 그룹 구분된 세금 식별번호는 그대로 남았고, 하이픈이나
  점이 있는 오스트리아 UID도 마찬가지였습니다.** 세금 ID에는
  공백, 슬래시, 점이 이미 지원되었지만 하이픈이 빠졌습니다 - UID
  (「ATU12345678」)에는 접두사 뒤의 하이픈과 점이 빠졌습니다. 세금
  ID의 검증 숫자는 계속 모든 결과를 확인합니다.

- **따옴표로 묶인 필드 값은 그대로 남았습니다.** 예를 들어
  「vorname": "Max"」같은 JSON 형태의 줄에서 그랬습니다. 필드
  표기(「Vorname: …」)를 통한 인식은 지금까지 표기나 값 자체가
  따옴표로 묶이지 않는다는 것을 전제로 했습니다. 이런 줄도 이제
  인식됩니다 - YAML 목록 기호가 앞에 붙은 필드 표기(「- Vorname:
  Max」)나 콜론 앞에 공백 대신 탭이 있는 경우도 마찬가지입니다.

- **이메일 머리글 단어 「Sent」자체가 이름처럼 검게 칠해졌습니다.**
  「Sent: Huber」같은 머리글에서 지금까지 「Sent」와 실제 이름 둘
  다에 해당되었습니다 - 「Subject」나 「Betreff」같은 관련 머리글
  단어는 지금까지 영향을 받지 않았습니다. 「Sent」도 이제 그대로
  남습니다.

- 머리글 「Errors-To:」나 「Resent-From:」 뒤의 이름은 이런 줄이
  평문으로 복사되어 있으면(예: 전달된 메시지나 사건 보고서)
  발견되지 않았습니다 - 「Reply-To:」나 「Return-Path:」와 달리
  이름이 부정확하게 구분되는 정도가 아니라 완전히 빠졌습니다. 이제
  발견됩니다.
- 같은 파일을 두 번 정리하면 때때로 결과가 달랐습니다: 두 인식이
  같은 위치를 같은 길이, 같은 신뢰도로 정확히 맞히면(예:
  「Sozialversicherungsnummer 1237/010180」이 AT_SVNR로도 일반
  식별번호로도 인식되는 경우) 어느 쪽이 이기는지는 우연에
  좌우되었습니다 - 값은 두 경우 모두 제거되었지만 자리표시자 표기만
  바뀌었습니다. 이제 동점은 항상 같은 방식으로 해결됩니다.
- 명사 바로 앞의 직함(예: 「Behandelnder Arzt: Dr. …」나
  「Zuständiger Sachbearbeiter ist …」)이 마치 그 자체가 이름인
  것처럼 함께 검게 칠해지는 경우가 있었습니다. 옆의 실제 성은
  영향을 받지 않습니다.
- 형용사처럼 보이는 실제 성(예: 「Schöne」, 「Lange」, 「Junge」)이
  바로 다음에 명사가 오면(예: 「Kontaktperson: Schöne
  Assistentin」) 지난번 수정 이후로 검게 칠해지지 않은 채 텍스트에
  남아 있었습니다 - 정보 유출이었습니다. 이제 이런 형태에서
  실제 직함(예: 「Behandelnder」, 「Zuständiger」)의 좁게 한정된
  목록만 이름이 아닌 것으로 취급됩니다.
- 여러 줄에 걸친 이름 결과 끝의 단독 성이 형용사처럼 보이면
  (예: 「Schwarz」, 「Kurz」, 「Alt」, 「Frisch」, 「Gut」, 「Reich」)
  바로 뒤에 콜론이 오면 인식되지 않은 채 남았습니다 - 정리 과정이
  이를 「Telefon:」같은 필드 표기와 혼동했습니다. 알려진 모호한
  성의 닫힌 목록이 이제 이를 보호합니다.
- 흔한 독일어 단어이기도 한 단독 성(「Gross」/「Grosse」, 「Gut」,
  「Kurz」, 「Lang」/「Lange」)은 「Herr Gross unterschrieb den
  Vertrag.」같은 단순한 문장에서도 지금까지 **완전히** 사라졌습니다.
  원인은 이 단어들을 담고 있는 spaCy 자체의 불용어 목록에
  있었습니다 - 알려진 성의 닫힌 목록이 이제 버려지지 않도록
  보호합니다.
- 근로, 대출, 보증, 신탁, 파산 계약 및 후견/보호, 감정 의뢰에서
  흔한 단어이기도 한 성(예: 「Bauer」)이 「Auftraggeber:」,
  「Auftragnehmer:」, 「Arbeitnehmer:」, 「Versicherter:」,
  「Darlehensgeber:」, 「Darlehensnehmer:」, 「Bürge:」,
  「Sicherungsgeber:」, 「Treuhänder:」, 「Treugeber:」,
  「Insolvenzverwalter:」, 「Gutachter:」, 「Sachverständiger:」,
  「Vormund:」, 「Pfleger:」같은 표기 뒤에서 간과되었습니다 - 이름만
  인식되거나 이름 전체가 빠지기도 했습니다.
- 회사 소개(Impressum)에서 흔한 단어이기도 한 성(예: 「Bauer」)이
  「Geschäftsführer:」, 「Geschäftsführerin:」,
  「Vertretungsberechtigt:」, 「Inhaber:」, 「Inhaberin:」같은 표기
  뒤에서 간과되었습니다 - 「Geschäftsführer:」/「Inhaber:」에서는
  이름 전체가 빠졌고, 「Vertretungsberechtigt:」에서는 이름만
  인식되었습니다.
- 표기가 자기 줄에 단독으로 있고 성중립 콜론 형태를 가진
  연락처 블록(「Ansprechpartner:in」, 그 아래 이름)은 **완전히**
  간과되었습니다 - 콜론이 필드 구분자로 읽히고 「in」이 (버려진)
  필드 값으로 읽혀, 다음 줄의 실제 이름이 더 이상 처리되지
  않았습니다. 별표 형태(「Ansprechpartner*in」)는 영향을 받지
  않았습니다.
- 이름과 표기가 같은 콜론 성중립 형태로 **한** 줄에 있으면
  (「Ansprechpartner:in Anna Berger」), 자리표시자가 이름만
  제거하는 대신 「in」이라는 단어를 대체 안에 함께 끌고
  갔습니다 - 이름 자체는 계속 완전히 포착되었습니다.
- 인물 열 표제(예: 급여명세서의 「Name Vorname Geburtsdatum」위의
  「Bauer Anna 03.05.1985」) 아래 표 열의 이름은 열 사이에 공백이
  하나뿐이고 줄이 항목 번호로 시작하지 않으면 완전히
  간과되었습니다 - 실제 PDF 텍스트 추출이 이런 줄을 내놓는 바로
  그 형태였습니다.
- 콜론 앞에 발언자 이름이 오는 채팅이나 회의록(예: 「Bauer 🙂: Ich
  stimme dem Vorschlag zu.」)에서, 이름과 콜론 사이에 반응 표시가
  있고 성이 동시에 흔한 단어(「Bauer」, 「Koch」, 「Schneider」
  등)이면 이름이 완전히 인식되지 않았습니다 - 회의록 전체가 인식된
  발언자 하나 없이 끝날 수도 있었습니다.
- 같은 발언자 줄 허점은 콜론 앞의 다른 중간 기호에서도
  나타났습니다: 괄호 안의 상태 부기(「Bauer (Vorsitz): …」, 「Bauer
  (abwesend): …」), 대괄호 안의 시각(「Bauer [14:32]: …」), 이름에
  바로 붙은 각주 기호(「Bauer*: …」). 여기서도 성이 동시에 흔한
  단어이면 발언자가 완전히 인식되지 않았습니다.
- 같은 메시지에 첨부된 회의록이나 로그 발췌본(예: 지원 티켓)에서
  이미 인식된 인물이 「이름.성」형태(소문자, 공백 없이, 점으로
  연결됨)의 사용자 이름으로도 추가로 나타나면, 이 평문 이름은
  같은 이름이 서신에서는 이미 검게 칠해졌음에도 그대로 읽혔습니다.
- 같은 사용자 이름 허점은 점 대신 밑줄이 있는 경우(「이름_성」)에도
  있었습니다 - 회의록과 로그 발췌본에서 마찬가지로 흔한 형식입니다.
- 그리고 순서가 반대인 경우(「성.이름」또는 「성_이름」)에도
  사용자 이름은 그대로 읽혔습니다 - 일부 시스템은 로그 사용자
  이름에서 성을 이름보다 앞에 놓습니다.
- 옆에 다른 정보가 없으면(「Herr Bauer ist am 12.03.1985
  verstorben」) 사망일이 인식되지 않았습니다 - 지금까지 이에 대한
  전용 인식이 전혀 없었고, 일반 날짜는 이 기본 기준에서는
  걸리지 않습니다.
- 문장이 분사 대신 동사형을 쓰면(「Frau Meier verstarb am
  12.03.1985」, 「Er starb am 12.03.1985」) 사망일도 인식되지
  않았습니다 - 지금까지 「ist … verstorben」/「ist … gestorben」만
  적중했습니다.
- 결혼일은 어떤 형태로 있든(「Eheschließung am 12.03.2010」,
  「Hochzeitsdatum: 12.03.2010」, 「Herr und Frau Bauer heirateten
  am 12.03.2010」) 인식되지 않았습니다 - 지금까지 전용 인식이
  전혀 없었고, 일반 날짜는 이 기본 기준에서는 걸리지 않습니다.

- **후처리 편집기에서 방금 삽입한 자리표시자 위에 두 번째 테두리를
  그리면 빨간 문자 잔여물이 남을 수 있었습니다**, 예를 들어
  「[BEG1]」대신 「[G」처럼 - 아무 경고도 없이 말입니다. 이 잔여물은
  더 이상 기밀 정보에 속하지 않고(그것은 이미 첫 단계에서
  제거되었음) 자체 자리표시자에만 속했기 때문입니다. 원인은 색
  처리 방식에 있었습니다: 새로 삽입된 자리표시자는 단색 지정에서도
  문자 단위로 파일에 기록되었습니다 - 같은 위치를 나중에 다시
  편집하면(예: 바로 다음의 「되돌리기」) 그 위의 두 번째 테두리는
  더 이상 자신이 위치를 잡을 수 있는 연속된 문구를 찾지 못했습니다.
  이제 단색 자리표시자는 자동 정리가 항상 그래 왔던 것처럼 스트림
  안에서 한 덩어리로 존재합니다 - 실제 그러데이션이나 무지개
  텍스트만 여전히 개별 문자가 필요합니다. 내장된 대조 검사는 이제
  자리표시자의 정확한 문자열이 더 이상 나타나지 않을 때도 이런
  잔여물을 추가로 인식합니다.
- 단계별 항목 번호가 있는 번호 매겨진 인명 목록(「1.1 Max
  Mustermann」, 「1.2 Huber Franz」 …)은 실제 목차와 항목 목록만
  보호해야 할 같은 제동 장치에 모든 이름을 빼앗겼습니다 - 목록
  위에 열 표제가 없으면 이름을 구해 줄 증인이 없었습니다.
- 시스템 로그의 영어 로그인 줄(「Accepted password for Max
  Mustermann from 10.0.0.5 port 51000 ssh2」)에 있는 이름은
  인식되지 않았습니다 - 독일어 언어 모델은 앞에 「invalid user」가
  있을 때만 찾았고, 그렇지 않으면 그대로 남았습니다. 이런 로그
  발췌본은 사건 보고서에 그대로 첨부되는 경우가 많습니다. IP 주소
  앞의 「for」뒤 이름이 이제 확실히 인식됩니다.
- 계좌명세서나 회계 저널의 SEPA 위임 참조번호에 있는 지급인 이름
  (예: 「MREF+Mustermann Klaus+SVWZ+Miete August」)은 그대로
  남았습니다 - 공백도, 문장 구조도 없이 「+」로만 구분된 대문자
  필드였고, 그곳에서 흔한 「성 이름」순서에서도 인식이 우연히조차
  발견하지 못했습니다. 이제 인식됩니다.
- 주소 표 첫 줄의 도로명과 건물번호(예: 「Nachname | Vorname |
  Straße | PLZ | Ort」)는 그대로 남았습니다 - 언어 모델이 여러 열에
  걸쳐 잘못되었지만 더 긴 지명을 추측했고, 이것이 올바르지만 더
  짧은 주소 결과를 밀어냈습니다. 이제 인식됩니다.
- 같은 허점은 열 구분자로 「|」나 「;」대신 탭을 쓴 경우에도
  발생했습니다 - 그곳에서는 주소가 일부만 사라지는 게 아니라
  완전히 사라졌습니다. 이제 인식됩니다.
- 건물번호가 있는 도로명은 그 뒤에 공백 없이 쉼표가 붙은 우편번호가
  바로 이어지면(예: 쉼표로 구분된 표 열의 「Bahnhofstrasse
  12,80331 München」) 그대로 남았습니다 - 쉼표가 수량의 소수점처럼
  보여서 패턴이 도로명을 아예 주소로 취급하지 않았습니다. 이제
  인식됩니다.
- 건물번호가 있는 도로명은 그 뒤에 쉼표 없이 지명 접두사 「St.」
  (성)가 바로 이어지면(예: 앞에 우편번호가 없는 레터헤드의
  「Hauptstraße 5 St. Pölten」) 그대로 남았습니다 - 「St.」가 개수
  단위처럼 보여서 패턴이 도로명을 아예 주소로 취급하지 않았습니다.
  이제 인식됩니다.
- 건물번호 뒤의 출입구/계단 부기(예: 「Lerchenfelder Gürtel 43/12」)
  는 그 뒤에 우연히 단위와 같은 한 글자(예: 시간을 뜻하는 「h」)가
  바로 이어지면 그대로 보이게 남았습니다 - 주소는 부기 없이
  건물번호까지만 정리되어, 완전히 걸리거나 아예 걸리지 않는 대신
  절반만 걸렸습니다.
- 직업명과 같은 성이 이름 앞에 오는 제목 줄(「Betreff: Bauer
  Anna」, 「Betreff: Bauer, Anna」)은 지금까지 완전히 인식되지
  않았습니다 - 문서 중간에 온전한 문장이 앞서 있어도 마찬가지였습니다.
  이제 인식됩니다.
- 블록 사이에 공백, 점, 하이픈이 있는 독일 세금번호(예:
  「Steuernummer: 30 815 08153」또는 「30.815.08153」)는 지금까지
  인식되지 않았습니다 - 슬래시 표기만 발견되었습니다. 이제
  인식됩니다.
- 의료 관련 필드 표기(「Patient:」, 「Hausarzt:」, 「Behandelnder
  Arzt:」, 「Überweisender Arzt:」와 그 여성형) 뒤의 이름은 성이
  흔한 독일어 단어일 때(예: 「Patient: Bauer Thomas」) 지금까지
  인식되지 않았습니다. 이제 인식됩니다.
- 자기 줄에 단독으로 있는 필드 표기 「Zahnarzt」 뒤의 이름(예:
  「Zahnarzt」, 그 아래 「Huber Franz」)은 지금까지 인식되지
  않았습니다 - 이름도 성도 마찬가지였습니다. 「Zahnärztin」과 단순한
  「Arzt」형태는 영향을 받지 않았습니다. 이제 인식됩니다.
- 「Herr」/「Frau」 뒤의 성에 「zur Kenntnisnahme」, 「zur
  Unterschrift」, 「zur Weiterleitung」같은 관용구가 이어지면
  지금까지 범위를 너무 넓게 잡아 그 관용구까지 이름 결과에 끌고
  들어갔습니다 - 「Frau Petra Klein zur Vertretung in allen
  Angelegenheiten」에서 「Petra Klein zur Vertretung」이 대체되었고,
  문장의 나머지는 문법적으로 훼손된 채 남았습니다. 「von der
  Leyen」이나 「zu Guttenberg」같은 실제 귀족 칭호는 영향을 받지
  않습니다.
- 같은 관용구 과잉 편집 문제는 이메일 「To:」머리글의 이름, 등록
  코드(C.1/C.1.1/C.1.2), 운전면허 코드, 괄호 서식 필드
  (「[Vorname]: …」), 점 없는 인사말 뒤의 이름에도 있었습니다 - 이
  모든 곳에서 「zur」/「von」등이 「zur Unterschrift」나 「zur
  Vertretung」같은 뒤따르는 관용구를 결과에 끌고 들어갔고, 때로는
  단순한 전치사 단어 자체가 이름 잔여물로 결과에 남기도 했습니다.
  여기서도 실제 귀족 칭호는 완전히 보존됩니다.
- 표기 뒤의 학번은 지금까지 전혀 인식되지 않았습니다 -
  「Matrikelnummer 7654321」은 식별번호로도 언어 모델로도 인식에서
  완전히 빠졌습니다. 숫자만으로는 인식할 수 있는 형태가 없기
  때문입니다.
- 참가자 번호도 마찬가지였습니다 - 「Teilnehmernummer 4471829」는
  식별번호로도 언어 모델로도 완전히 빠졌습니다.
- 이력서에서 「Persönliche Daten」구역 표제 아래의 이름은 호칭 없이
  「성 이름」형태로 바로 아래 있으면 완전히 또는 부분적으로 자주
  인식되지 않았습니다.
- 구역 표제 「Kontaktdaten」에서도 마찬가지였습니다 - 그곳에서는
  이름이 부분적으로가 아니라 완전히 인식되지 않았습니다.
- 「Name, Vorname」열이 합쳐진 주민등록증명서나 신청 목록(주민등록
  방식 표기, 값이 예를 들어 「Mustermann, Max」로 한 셀에 있음)에서
  생년월일 같은 다른 열이 이어지면 이름이 인식에서 완전히
  빠졌습니다.
- 신분증과 주민등록증명서에서 흔한 형태인 「Geburtsdatum/-ort:
  22.07.1978 / Rostock」식의 생년월일은 인식되지 않았습니다 -
  쉼표 형태 「Geburtsdatum, Geburtsort: …」만 적중했습니다.
- 「Bürgerservice」와 「Bürgerbüro」는 특히 나열 구분자로 줄표가
  올 때(예: 「Wenden Sie sich an das Bürgerservice – Bürgerbüro
  …」) 가끔 잘못 지명으로 검게 칠해졌습니다.
- 줄바꿈이 중간을 가른 표기된 전화번호(예: 좁은 레터헤드 열이나
  열 너비에 맞춘 PDF 텍스트 추출에서: 「Telefon: 0176 12\n34567」)
  는 일부만, 즉 절반만 검게 칠해졌습니다 - 줄바꿈 뒤의 나머지는
  읽을 수 있게 남았습니다.
- 줄바꿈이 중간을 가른 표기된 식별번호(고객, 회원, 계약번호 등,
  예를 들어 좁은 열의 「Kundennummer: K903\n944」)도 절반만 검게
  칠해졌습니다 - 줄바꿈 뒤의 나머지는 읽을 수 있게 남았습니다.
- 쉼표 뒤에 직업명이 오는 학위 호칭이 붙은 이름(예: 「Dipl.-Ing.
  Sabine Roth, Projektleiterin」)은 완전히 보호되지 않은 채
  남았습니다 - 이 줄이 표 형태의 열 표제처럼 보여 잘못 사실
  내용으로 버려졌습니다.
- 이름 앞의 직함 「Dr.-Ing.」(흔한 독일 공학 학위)는 마스킹된 인물
  값에 포함되지 않고 읽을 수 있게 남았습니다 - 「Dipl.-Ing.」와
  같은 하이픈 함정이었습니다.
- 이름 앞의 직함 「Dipl.-Kfm.」, 「Dipl.-Kffr.」, 「Dipl.-Psych.」
  (경영학/경영학(여)/심리학 디플롬)도 마스킹된 인물 값에 포함되지
  않고 읽을 수 있게 남았습니다 - 「Dipl.-Ing.」, 「Dr.-Ing.」와
  같은 하이픈 함정이었습니다.
- 콜론 대신 점을 쓴 Cisco 표기법의 MAC 주소(예: 「aabb.ccdd.eeff」,
  스위치 로그와 지원 티켓이 출력하는 형태)는 전혀 인식되지 않고
  읽을 수 있게 남았습니다.
- 「Familie」 뒤의 성(예: 「Die Familie Gruber unterschreibt den
  Vertrag」)은 문장 구조에 따라 인식되지 않아 읽을 수 있게 남는
  경우가 있었습니다 - 앞에 귀족 칭호가 있어도 마찬가지였습니다
  (「Familie von der Leyen」).

- 우편번호+지역과 도로명+건물번호 사이에 구분 문장부호가 없는
  크로아티아어 주소(예: 「10000 Zagreb Ulica Ivana Lučića 5」)는
  건물번호가 정리되지 않은 채 남아 있었습니다.

- 리투아니아어 연락처 표기 「Kontaktinis asmuo」(예: 「Kontaktinis
  asmuo: Vilkas Jonas」)에서 성이 흔한 명사(Vilkas = 「늑대」,
  Vanagas = 「매」)일 때 성이 인식되지 않았습니다.

- 별도 표기가 없는 덴마크어 서식 필드의 출생국 또는 거주국(예:
  「Fødeland: Tyskland」또는 「Bopæl: Tyskland」)이 인식되지
  않았습니다.

- 별도 표기가 없는 루마니아어 서식 필드의 출생국 또는 거주국(예:
  「Țara: Germania」또는 「Țara de reședință: Franța」)이 인식되지
  않았습니다.

- 리투아니아어 필드 표기 「Darbdavys:」또는 「Įmonės
  pavadinimas:」(고용주/회사) 아래의 회사명이 인식되지 않았습니다.

- 러시아어 필드 표기 「Работодатель:」또는 「Наименование
  организации:」(고용주/회사) 아래의 회사명이 인식되지
  않았습니다.

- 루마니아어로 월 이름이 서술형으로 적힌 날짜(예: 「31 decembrie
  2024」)가 인식되지 않았습니다.

- 약어 「szül.」뒤의 헝가리어 결혼 전 성(예: 「Nagy Éva (szül.
  Kovács)」)이 인식되지 않고 읽을 수 있게 남았습니다.

- 저장된 HTML 프로필 페이지(또는 웹페이지가 첨부된 이메일)는
  실명이 Open Graph 프로필 필드 `profile:first_name`/
  `profile:last_name`/`profile:username`에만 있으면 정리되지 않은
  채 남을 수 있었습니다 - 이 필드들은 `og:title`처럼 서술형이
  아니라 이름을 분해해 담으며, 이제 함께 정리됩니다.

- 배달 불능 통지(반송/NDR)는 원래 배달되지 않은 메일의 머리글
  (발신인, 수신인, 제목)을 별도의 세 번째 첨부 부분에 담고
  있는 경우가 많았습니다 - 이 부분은 정리된 버전에서 완전히
  그대로 남아 있었습니다. 이제 나머지 배달 보고서와 마찬가지로
  정리됩니다.

- Word에서 보호된 영역의 개별 지정 편집자(편집 제한 → 예외,
  `w:permStart`)는 본문에서 같은 이름이 이미 오래전에 정리되었어도
  평문 그대로 남아 있었습니다. 이제 함께 제거됩니다.

## 0.10.42-alpha.20260827 – 2026년 8월 27일

### 신규

- **이름이 붙은 인식 프로필로 다양한 작업 상황을 한 번에 불러올 수
  있습니다.** *설정 → 인식 → 무엇을 제거할지* 아래에서 현재의 범주 및
  종류 선택을 저장하고 선택 필드로 즉시 다시 적용할 수 있습니다. 고정
  프로필 *표준*은 기존 출고 상태에 해당하며 삭제할 수 없습니다. 프로필은
  오직 무엇을 제거할지만 바꿉니다 – 언어, 출력 방식, 인식 깊이, 사용자
  지정 용어와 검색 패턴은 영향을 받지 않습니다.

- **결과물의 종류를 이제 정리 직전에 바로 선택합니다.** 메인 창의 공통
  선택 필드가 전체 묶음에 대해 Maskuro가 읽을 수 있는 자리표시자를
  넣을지, 마스킹할지, 아니면 대체 없이 제거할지를 결정합니다. 설정
  창에 있던 PDF용과 오피스용 두 개의 분리된 필드는 사라졌습니다 – 이로써
  중요한 결정이 눈에 보이게 되었고, 형식이 섞인 묶음에서 의도치 않게
  서로 달라지는 일이 없어졌습니다. 안내형 둘러보기는 첫 정리 전에
  새 선택 항목을 설명합니다.

- **테마와 워터마크가 원하는 경우 완성된 PDF를 명확하게 표시합니다.**
  12가지 전체 스타일이 대체 텍스트와 마스킹 영역을 서로 맞춥니다 – 새로
  추가된 것은 프라이드와 봄, 여름, 가을, 겨울입니다. *비밀문서*는
  대각선의 `TOP SECRET` 표시를 바로 제공합니다. 이와 별개로 자유
  표시 텍스트나 직접 만든 이미지, 아이콘, SVG를 색상과 불투명도와
  함께 선택할 수 있습니다. 가져온 그래픽은 메타데이터 없이 삽입되며,
  원본 파일이 이동해도 계속 사용할 수 있습니다. 후속 수정 시 Maskuro는
  기존 워터마크를 여러 번 겹쳐 쌓는 대신 교체합니다. 텍스트 워터마크는
  마지막 PDF 레이어로 밝은 윤곽선과 함께 그려져, 어두운 이미지와
  빽빽한 텍스트 위에서도 눈에 띕니다. 후속 수정 편집기는 Maskuro의
  워터마크를 완전히 무시하며 그 텍스트를 더 이상 마스킹 후보로
  제시하지 않습니다.

- **직접 만든 출력 테마를 저장하고 공유할 수 있습니다.** 대체 텍스트,
  마스킹, 워터마크의 현재 조합에 이름을 붙여 설정에 남기고, 평문이
  없는 JSON으로 내보내거나 가져올 수 있습니다. 흑백 인쇄 미리보기는
  대비가 약한 경우 경고하며, 선택적인 성공 색종이 효과는 화면에만
  나타납니다.

- **마지막 내보내기 검사와 이를 설명하는 검증 요건이 표시 단계를
  마무리합니다.** 최종 저장 전 Maskuro는 값이 정확히 알려진 모든 PDF
  위치를 텍스트 레이어와 렌더링된 픽셀에서 다시 한번 비교합니다 –
  경고는 오직 페이지와 좌표만 언급합니다. 편집기에서 *왜 가려졌나요?*는
  범주, 인식 경로, 안전 여백을 보여주지만, 제거된 평문은 절대
  보여주지 않으며 최종 문서에도 결코 나타나지 않습니다.

- **마스킹 막대가 이제 예쁠 수 있습니다.** *설정 → 화면 표시* 아래에
  색상 기본값, 자유 색상 선택기, 그라데이션, 무지개, 줄무늬, 점무늬,
  꽃, 별, 하트, 발자국, 구름, 번개, 커피콩, 오리, 태양, 나뭇잎, 눈송이,
  종이, 형광펜, 접착테이프, 그리고 재현 가능한 무작위 패턴이 즉시
  미리보기와 함께 준비되어 있습니다. 대체 텍스트는 원하는 대로 색상,
  그라데이션, 무지개, 알약 모양, 라벨 중 하나를 가질 수 있습니다.
  범주 색상은 이름, 주소, 연락처, 의료 정보를 구분합니다. PDF는 전체
  디자인을 그대로 적용하며, Word, PowerPoint, OpenDocument, HTML은
  선택한 불투명 기본 색상을 사용합니다. 보호 방식 자체는 바뀌지
  않습니다 – Maskuro는 기밀 내용을 먼저 제거한 뒤에야 빈 자리에 색이나
  패턴을 그립니다.

- **Maskuro가 Linux용으로 다시 제공됩니다 – AppImage, DEB, RPM, 그리고
  포터블 아카이브로.** DEB와 RPM은 프로그램 항목, 파일 연결, 터미널
  명령, 아이콘을 시스템에 등록합니다 – AppImage는 설치 없이 실행됩니다.
  업데이트는 기존 DEB나 RPM 설치의 경우 동일한 패키지 형식을 유지하고,
  그 외에는 AppImage를 우선합니다.

- **시각 검증이 일반 PDF 텍스트를 더 이상 새로운 검출로 두 번 제시하지
  않습니다.** 최종 OCR 확인과 눈에 보이는 페이지의 안전한 재구성은
  완전히 그대로 활성 상태로 유지됩니다 – 다만 기본값에서는 페이지
  텍스트와 개별 이미지 검사가 아직 읽지 않은 영역만 새로운 검출
  출처로 간주됩니다. 이렇게 하면 제품 목록 줄이 단지 두 번째 OCR
  판독이 다르다는 이유만으로 새로운 이름이나 회사명이 되지 않습니다.
  전체 눈에 보이는 텍스트에 대해 두 개의 독립적인 판정을 계속 원하는
  사람은 설정에서 *눈에 보이는 PDF 페이지 전체를 다시 한번 검사*를
  켤 수 있습니다.

- **PDF를 연속, 페이지 단위, 또는 두 쪽 보기로 볼 수 있습니다.** 세
  개의 작은 보기 아이콘이 "너비"와 "페이지" 바로 옆 아래쪽에
  있습니다. 연속 보기는 페이지 가장자리에서 다음 페이지로 스크롤되고,
  단일 페이지는 마우스 휠이 현재 페이지에 머무르며, 두 쪽 보기는
  펼침면을 보여주고 클릭한 페이지를 편집 가능하게 만들며 앞/뒤 이동을
  펼침면 단위로 합니다. 페이지 축소판과 비교 돋보기도 훨씬 좁아진
  왼쪽 기본 열에서 열려, 작업 페이지에 더 많은 공간을 남깁니다.

- **AI 단계가 무엇을 했는지 이제 볼 수 있습니다.** 실행 후 파일마다
  "세부 정보" 아래에 "AI 단계: 경계 사례 12건 검토, 3건 기각"과 같은
  줄이 나타납니다 – 바꿀 것이 없었던 경우에도 그렇게 표시됩니다.
  지금까지는 가장 비용이 큰 이 단계가 완전히 조용했습니다 – 실제로
  질의가 이루어졌는지조차 밖에서는 알 수 없었습니다.

  더 자세히 필요한 사람은 "설정 → AI"에서 *모든 AI 질의를 로그에
  기록*을 켤 수 있습니다. 그러면 로그 파일에 각 질의마다 크기, 소요
  시간, 결과 수가 기록되고, 상대측의 요청량 제한으로 인한 대기 시간도
  함께 기록됩니다. 옆에 있는 "로그 파일 보기" 버튼은 그 폴더를 엽니다
  – 이는 Windows에서는 숨겨져 있고 스스로는 아무도 찾지 못하는
  애플리케이션 데이터 폴더 안에 있습니다. 파일에는 오직 크기만
  기록되며, 문서의 텍스트는 절대 기록되지 않습니다.

- **Maskuro는 사용 중인 AI 서비스가 요청 수를 제한하는지 인식합니다.**
  호스팅 서비스는 분당 몇 건의 요청만 허용하는 경우가 많습니다 –
  4건도 드물지 않습니다. 초과된 요청은 거부되지 않고 대기해야 하며,
  응답당 2초가 40초로 늘어납니다. 지금까지는 이것이 모델이 느린
  것처럼 보였습니다. 이제 Maskuro는 서비스의 응답에서 제한값을 읽어,
  받아들여지는 것보다 더 많은 질의를 동시에 보내지 않으며, "연결
  확인" 아래에 그 제한을 표시하고 소요 시간 예측에 반영합니다.

- **페이지 보기가 이제 사용 중인 Word, Excel, PowerPoint를 사용하며,
  이로 인해 약 6배 더 빠릅니다.** 지금까지는 대부분의 사무용 컴퓨터에는
  없는 LibreOffice가 필요했습니다 – 없는 사람은 외부 설치를 요구하는
  버튼을 보게 되었습니다. 이제는 Microsoft Office가 설치되어 있으면
  별도의 설정, 다운로드, 체크 없이 자동으로 사용됩니다. LibreOffice는
  두 번째 경로로 남으며, OpenDocument 파일에서는 오히려 첫 번째
  경로입니다 – 한쪽이 실패하면 다른 쪽이 시도됩니다.

  차이는 특히 작업할 때 느껴집니다. 교체할 때마다 페이지가 다시
  구성되는데, 이는 Office를 통하면 3초가 아니라 약 0.5초가 걸립니다.
  문서의 첫 화면은 여전히 몇 초가 걸리지만, 그 이후는 대기 시간 없이
  작업을 그대로 따라갑니다.

  이때 열려 있는 자신의 Word는 건드리지 않습니다 – Maskuro는 별도의
  보이지 않는 세션을 시작하고, 파일을 읽기 전용으로만 열며, 매크로를
  끄고, 후속 수정 창이 닫히면 모두 다시 종료합니다. 암호로 보호된
  파일은 보이지 않는 대화상자에 멈추는 대신 거부됩니다.

- **초기 설정이 이제 얼굴, 코드, 서명에 대해서도 묻고, 빠진 것을
  한 번에 모두 내려받습니다.** 고급 인식 옆의 첫 페이지에는 세 개의
  이미지 스위치가 있습니다: 얼굴 영역 알아볼 수 없게 처리, 바코드와
  QR 코드 알아볼 수 없게 처리, PDF 페이지의 손글씨 서명 마스킹. PDF
  제한은 체크 표시 옆에 눈에 보이게 표시됩니다 – 오피스 파일은
  자동으로 서명을 검색하지 않습니다. 체크 아래에는 "다음"을 클릭하면
  몇 메가바이트가 필요한지 표시됩니다. 이후 내려받기는 여러 대화상자를
  순차적으로 거치는 대신, **하나의** 창에서 전체를 아우르는 **하나의**
  진행 표시줄로 이루어집니다. 중단하면 전체 과정이 종료되며 아무것도
  절반만 남지 않습니다. 아무것도 원하지 않는 사람은 체크를 없애면
  됩니다 – 그러면 아무것도 내려받지 않습니다.

- **미리보기를 검토 필요도에 따라 줄이고 종류별로 접을 수
  있습니다.** 목록 위에는 *충분히 근거 있는 항목 숨기기* 슬라이더가
  있습니다 – 오른쪽으로 갈수록 초록에서 빨강 방향으로 더 많이
  숨겨지며, 맨 오른쪽에서는 프로그램이 단독으로 추측한 것만
  남습니다. 종류의 제목을 클릭하면 접을 수 있습니다. 둘 다 읽기
  보조 기능일 뿐 선택이 아닙니다 – 숨겨지거나 접힌 것도 체크된
  상태로 남아 교체됩니다. 현재 몇 개의 값이 그런 상태인지는 슬라이더
  아래에 표시됩니다. 짧은 목록에서는 슬라이더가 나타나지 않습니다.
  두 열로 전환하면 이제 *다시는 안 함* 스위치도 함께 유지됩니다.

- **이미지 목록이 실행 전마다 스스로 열릴 수 있습니다.** 이미지마다
  개별적으로 결정하고 싶은 사람은 "이미지" 아래에서 새로운 체크 *실행
  전마다 개별 지정*을 설정합니다. 그러면 정리할 때마다 미리보기가
  있는 목록이 매번 "개별 지정…"을 직접 클릭하지 않아도 저절로
  나타납니다 – 중단하면 정리도 이루어지지 않습니다. 선택한 파일
  중 이미지가 하나도 없으면 아무것도 나타나지 않습니다. 기본값은
  체크 해제입니다.
- **Maskuro는 PDF 페이지에서 손글씨 서명을 찾아 픽셀에서
  제거합니다.** 지금까지는 정리된 문서 아래에 서명이 그대로
  남아 있었습니다 – 텍스트 인식은 인쇄체를 읽으며, 읽지 못하는
  것은 대체되지 않습니다. 이 검색은 별도의 스위치이며, 한 번
  내려받아야 하는 인식 모델이 필요합니다.

  측정 결과 서명 100개 중 약 84개를 찾아내고, 그중 약 5분의 4를
  가립니다. 이는 도움일 뿐 확약은 아닙니다 – 실행할 때마다 보고서에
  몇 개를 찾았는지 표시됩니다 – 서명이 없었던 경우에도 마찬가지로
  표시됩니다. 서명이 없다는 뜻일 수도 있고, 놓쳤다는 뜻일 수도
  있기 때문입니다. 서명이 없는 실제 업무 페이지 72건에서는 없는
  서명을 지어내지 않았습니다.

  **손으로 그린** 서명은 찾아내지만 제거되지는 않습니다 – 이는
  픽셀이 아니라 선으로 이루어져 있어서, 그 위에 막대를 놓아도
  덮개에 불과하고 그 아래 선은 그대로 남을 것입니다. 이런 위치는
  집계되고 언급되어, 후속 수정 창에서 직접 마스킹할 수 있습니다.

  Word, Excel, PowerPoint, OpenDocument 파일은 서명을 자동으로
  검색하지 않습니다. 화면, 초기 설정, 모델 다운로드, 명령줄, 설명서는
  이제 이 한계를 명시적으로 언급합니다.

- **둘러보기가 이제 미리보기도 함께 안내합니다 – 여러분이 결정을
  내리는 창입니다.** 실습 문서에서는 미리보기를 다른 곳에서 꺼두었더라도
  자동으로 열립니다(여러분의 설정 자체는 그대로 유지됩니다). 색이
  무엇을 의미하는지, 왜 각 줄에 단 하나의 질문만 있는지 – 여기에
  정말 사람이 있는가? – 그리고 "다시는 안 함"이 무엇에 쓰이는지를
  설명합니다. 색상 설명에서는 잘 근거가 있는 줄(대개 IBAN – 문장이
  언급하는 초록 예시)에 먼저 초점을 맞춘 다음, 가장 근거가 약한
  줄로 넘어가며, 그곳에서는 설명 도중 직접 클릭할 수 있습니다 –
  체크를 없애면 그 값은 문서에 그대로 남습니다. 목록이 길면 안내를
  위해 창이 더 크게 열려, 설명이 줄 위에 겹치지 않습니다. 창이 두
  번째로 열리면 둘러보기는 그 이유도 알려줍니다 – 완성된 페이지가
  이미지로 한 번 더 읽히며, 이때 이름처럼 보이는 조각들이 생기기
  때문입니다.

- **편집기는 처음 열릴 때 크게 열립니다.** 원본, 결과, 도구 모음,
  검출 목록이 나란히 배치되며, 기존 기본 크기에서는 공간이
  부족했습니다. 창을 작게 조절한 사람은 다음번에도 그 크기를 그대로
  다시 얻습니다 – 아무도 강제로 바뀌지 않습니다.

- **자리표시자를 더블클릭하면 되돌려집니다** – Word, Excel,
  PowerPoint, OpenDocument, 텍스트, 이메일, HTML에서. 그리고 여러
  자리표시자에 걸쳐 드래그한 뒤 "선택 영역 되돌리기"를 선택하면
  그 안의 모든 자리표시자를 한 번에 되돌릴 수 있습니다. 이제 대괄호를
  정확히 클릭하지 않아도 됩니다. 익명화 시 여러 서로 다른 값을
  나타내는 자리표시자는 이에서 제외됩니다 – 이들은 집계되고
  언급될 뿐 추측되지 않습니다.

- **설명서에 "교체 전 미리보기" 장이 생겼습니다.** 이 창은 기본으로
  켜져 있으며 여러분이 결정을 내리는 유일한 곳입니다 – 지금까지
  설명서에는 부수적으로만 언급되어 있었습니다. 이제 체크가 무엇을
  의미하는지(**모든** 발견 위치에 적용되며, 나열된 것만이 아닙니다),
  왜 줄마다 하나의 질문에만 답하면 되는지, "다시는 안 함"이 영구적으로
  무엇을 하는지, 그리고 왜 PDF에서는 창이 두 번째로 열릴 수 있는지가
  적혀 있습니다. 18개 언어 전부에, 그리고 설정 목록에도 이 스위치가
  이제 함께 나열됩니다.

### 변경됨

- **"교체된 값" 패널에 색상 슬라이더가 생겼고, 학습 모드는 더 이상
  거기 없습니다.** 값이 8개를 넘으면 목록 위에 미리보기 창과 같은
  슬라이더가 있습니다: *충분히 근거 있는 항목 숨기기*는 정말로
  검토해야 할 것만 남도록 화면을 줄입니다. 문서 자체는 바뀌지
  않으며, 전체 중 몇 줄이 보이는지는 그 아래에 표시됩니다 –
  검색 필드와 슬라이더는 함께 집계됩니다. *학습 모드* 체크는 패널에서
  사라졌습니다 – 계속 "도구" 메뉴와 도구 모음에 남아 있습니다.

- **"교체된 값" 패널이 이제 문서와 같은 색을 보여줍니다.** 그 안의
  각 줄은 문서 안의 위치, 그리고 미리보기의 값과 동일하게 배경색이
  칠해집니다: 빨강은 "단독 추측 – 여기부터 다시 살펴볼 가치가
  있음"을, 초록은 "명명된 패턴으로 인식됨"을 뜻합니다. 각 종류 안에서는
  가장 불확실한 것이 위에 옵니다 – 즉 목록을 위에서 아래로 작업하면
  가장 중요한 것을 먼저 보게 됩니다. 지금까지는 모두 같은 밝기로
  알파벳순으로 표시되었습니다.

- **학습 모드는 출고 시 꺼져 있습니다.** 후속 수정 창에서 수정한
  후, 프로그램은 지금까지 그것을 규칙으로 만들지 스스로 물었습니다.
  이 질문은 작업 도중에 나타나며, 요청하지 않은 사람에게는 방해로
  느껴집니다. 규칙을 원하는 사람은 도구 모음의 *학습 모드* 버튼을
  켜면 됩니다 – 그 선택은 이후 양방향으로 계속 적용됩니다.

### 수정됨

- **내보낸 규칙 파일은 이제 명시적으로 보호가 필요한 것으로
  표시됩니다.** 사용자 지정 용어와 예외가 평문으로 들어 있을 수
  있으며, 파일에는 추정 값을 확인하는 데 쓰이는 해시 솔트도 포함될
  수 있습니다. 성공적인 내보내기는 이제 경고를 표시하고, 파일을
  보호하고 오직 권한 있는 수신자에게만 의도적으로 전달하도록
  요청합니다.

- **마지막 안전 검사가 더 이상 정리된 오피스 파일을 자신의 자리표시자
  때문에 보류하지 않습니다.** "SVNR" 같은 종류 약어는 `[SVNR1]`에도
  들어 있습니다 – 지금까지는 이를 잔여 평문으로 간주해 완성된 파일을
  폐기했습니다. 동시에 전화번호와 IBAN도 이제 오피스가 눈에 보이는
  공백 없이 참조나 삽입된 파일 안에 같은 값을 저장하는 곳까지
  추적됩니다.

- **Word, Excel, PowerPoint, OpenDocument는 뒤늦게 발견된 필드
  사본을 더 이상 남기지 않습니다.** 값이 보조 저장소나 삽입된
  오피스 파일에서 처음 인식되면, 좁은 후속 처리가 이전에 읽었던
  눈에 보이는 사본과 숨겨진 사본도 함께 정리합니다. 이미 생성된
  참조 자리표시자는 이때 다시 교체되지 않습니다.

- **Word 선택 목록을 개별적으로 되돌릴 때 인접한 선택 항목이 더
  이상 묻지도 않고 함께 되돌아오지 않습니다.** 전체 원본 문단은
  그 속성에도 열린 자리표시자가 남아 있지 않을 때 비로소
  적용됩니다.

- **읽기 어려운 스캔에서 서로 관련된 정보가 덜 유실됩니다.** 호칭과
  두 부분으로 된 이름을 포함한 대체 OCR 판독이 유지됩니다 – 도로명
  조각, 번지수, 우편번호-지명이 함께 전체 주소 줄을 보호합니다,
  인접한 OCR 블록으로 나뉘어 있어도 마찬가지입니다. 그 옆의 청구서·
  물품 필드와 행사 관련 줄은 이때 함께 처리되지 않습니다. "출생"
  뒤에서 여러 OCR 단어와 문장 부호로 나뉜 유효한 날짜도 완전히
  알아볼 수 없게 처리됩니다.

- **성공 색종이 효과가 편집기가 자동으로 열릴 때 이제 보입니다.**
  조각들이 창 위쪽 가장자리에서 떨어지는 대신 *정리* 버튼에서
  직접 분사됩니다. 편집기는 850밀리초의 첫 분사만 기다린 뒤 자동으로
  열립니다 – 색종이 효과가 꺼져 있으면 지연이 없습니다.

- **보기 아이콘 위로 마우스를 올릴 때 페이지 카운터와 확대 표시줄이
  더 이상 흔들리지 않습니다.** Qt는 아이콘의 안내 문구가 나타날
  때마다 상태 표시줄의 여유 공간을 다시 배분했습니다. 두 조작
  그룹은 이제 마우스를 올려도 원래의 너비와 고정된 위치를
  유지합니다.

- **연결된 AI 서버의 속도 측정이 항상 실패했습니다** – 자체 AI 기능이
  생긴 이후 모든 서버에서 그랬습니다. 좁은 응답 한도로 질의한 뒤
  그로 인해 잘린 응답을 읽으려 했습니다 – 이는 실패할 수밖에
  없었고, "측정되지 않음"으로 저장되었습니다. 그 영향은 곳곳에서
  나타났습니다 – 소요 시간 예측은 서버를 사무용 컴퓨터에 내장된
  모델의 속도로 계산했고, 설정에는 계속 속도가 아직 측정되지
  않았다고 표시되었습니다. 이제는 서버가 응답으로 실제로 생성한
  분량을 기준으로 측정하며, 응답 내용을 기준으로 하지 않습니다.

- **"최대 인식 (AI) – 느림"은 사실이 아닐 때도 표시되었습니다.**
  라벨과 안내 문구는 사무용 컴퓨터에서 동봉된 모델을 설명했습니다
  – "이 컴퓨터에서 실행되는 언어 모델", "대용량 문서에서는 최대
  1시간". 자체 AI 서버를 연결한 사람은 두 가지 잘못된 내용을
  읽었습니다: 연산은 자신의 컴퓨터에서 이루어지지 않으며, 응답은
  시간이 아니라 초 단위로 옵니다. 이제 두 가지 모두 실제 측정에서
  나옵니다. 측정값이 없으면 애플리케이션은 더 이상 아무것도 주장하지
  않고, 아직 측정되지 않았다고 알립니다.

- **되돌리기가 이제 드래그한 선택 영역에도 작동합니다.** 여러 자리
  표시자에 걸쳐 드래그하고 *선택 영역 되돌리기*를 누르려던 사람은
  버튼이 회색인 것을 보았습니다 – 이 버튼은 선택 영역이 **정확히**
  자리표시자 하나일 때만 활성화되었는데, 문단 전체에 걸쳐 드래그한
  경우는 그런 경우가 절대 없었습니다. 그 뒤의 경로는 이미 있었지만
  아무도 도달하지 못했습니다. 이제는 영역을 선택하기만 하면 됩니다
  – 그 안의 모든 자리표시자가 한 번에 되돌아옵니다.

- **비교 돋보기가 열려 있으면 되돌리기가 충돌을 일으켰습니다.**
  돋보기는 원본에서 함께 움직이기 위해 마우스 커서 아래 위치를
  기억합니다. 되돌리기 후 다시 로드할 때, 텍스트 보기가 처리할 수
  없는 형태로 이 위치를 반환했습니다 – 그리고 인터페이스 한가운데의
  이런 오류는 프로그램을 종료시키기 때문에, 되돌리기가 충돌이 되어
  버렸습니다. 돋보기는 기본 상태에서 열려 있으므로 일반적인
  경로에서도 발생했습니다.

- **되돌리기 후 화면이 더 이상 문서 처음으로 이동하지 않습니다.**
  긴 문서에서는 손을 댈 때마다 방금 작업하던 위치가 사라졌습니다.
  이제는 원래 위에 있던 문단이 계속 위에 남아 있습니다.

- **LibreOffice가 없으면 페이지 보기는 없다고만 하지 않고 어디서
  구할 수 있는지 알려줍니다.** *페이지 보기*와 *PDF로 마스킹*
  두 버튼은 잠겨 있었고 툴팁에는 LibreOffice를 찾지 못했다는 말만
  있었습니다 – 애플리케이션 어디에도 거기로 가는 경로가 없었습니다.
  이제 클릭하면 무료 오픈소스 LibreOffice로 가는 경로를 안내하는
  창이 열립니다. 설명서와 FAQ는 이 부분에서 잘못되어 있었습니다 –
  애플리케이션이 제공하지 않는 다운로드 구성 요소를 예고하고
  있었습니다.

- **출고 전에 완성된 파일을 마지막으로 완전히 검색합니다 – 이제
  Word, Excel, PowerPoint, LibreOffice, 이메일, HTML, 텍스트에서도.**
  지금까지는 PDF만 이 마지막 검사를 받았습니다. 이전의 모든 검사는
  누군가가 미리 지정해 둔 위치만 확인합니다 – 아무도 생각하지 못한
  저장소는 아무도 검사하지 않습니다. 마지막에는 이제 교체한 모든
  내용을 패키지의 모든 부분에서 무차별적으로 검색합니다. 무언가
  남아 있으면 결과물이 **생성되지 않으며**, 메시지가 그 값을
  알려줍니다. 정리되었다고 여겨지는 파일은 아예 없는 것보다
  나쁩니다.

- **`<script>`와 `<style>` 안에 있는 이름이 이제 보고됩니다.** 둘 다
  계속 그대로 유지됩니다 – 거기에는 프로그램 코드가 있고, 식별자
  한가운데의 교체는 웹페이지를 망가진 웹페이지로 만들기 때문입니다.
  하지만 지금까지는 그것이 언급되지 않았고, 이것이 문제였습니다 –
  스타일 규칙 `content: "Anna Musterfrau"`는 화면에 **눈에 보이게**
  나타나는데, 결과물에도 그대로 남아 있으면서 프로그램은 페이지가
  정리되었다고 보고했습니다.

- **설정에서 추가 모델을 다시 내려받고 제거할 수 있습니다.** "고급
  인식"과 "최대 인식 (AI)" 옆의 버튼은 눌러도 모델을 가져오는 대신
  오류 보고 창으로 이어졌습니다. 두 번째 경로 – 인식에서 스스로
  모델을 요청하는 체크 표시 – 는 이 문제의 영향을 받은 적이
  없었습니다.

- **표의 시트 이름과 범위 이름에 들어 있는 이름이 이제
  보고됩니다.** 시트 이름은 아래 탭에, 범위 이름은 이름 필드와
  이를 사용하는 모든 수식에 나타납니다. 둘 다 여전히 교체되지
  않습니다 – 수식이 이를 통해 참조하며, 참조 오류가 있는 통합
  문서는 아무에게도 도움이 되지 않기 때문입니다 – 하지만 이제는
  적어도 표시됩니다. 지금까지는 이 알림이 Excel 통합 문서의 시트
  이름에만 나타났습니다 – "Bezuege_Brunnthaler"라는 이름의 범위는
  조용히 함께 빠져나갔고, LibreOffice 표에서는 프로그램이 완전히
  침묵했습니다. "Notizen Ortner"라는 시트는 그래서 정리된 것으로
  간주되었고, 수신자의 첫 시선이 그 이름에 닿았습니다.

  여기서 보고되는 것은 실제로 사람으로 이어지는 경우뿐입니다: 같은
  통합 문서에서 이미 교체된 단어, 또는 여러 단어 중 하나를 골라낸
  검출입니다. "Zustaendig"나 "Bezug_Umsatz" 같은 단독 단어는 더
  이상 경고를 일으키지 않습니다 – 예전에는 그랬을 것이고, 두 번에
  한 번꼴로 나타나는 경고는 세 번째부터는 아무도 읽지 않습니다.

- **"원본 되돌리기"가 이제 정말로 모든 것을 되돌립니다.** 일부
  문서에서는 이후 개별 문자가 빠져 있었습니다 – "Seestraße 14"가
  "Seestraße 4"가 되고, "An:"이 "An"이 되고, "nordlicht-planung"이
  "nordlicht planung"이 되었습니다 – 그리고 일부 줄은 아예 돌아오지
  않았습니다. 바로 그 자리에서는 이후 마우스로 아무것도 선택할 수
  없었고 아무것도 마스킹할 수 없었습니다 – 텍스트는 종이 위에는
  있었지만, 프로그램은 더 이상 그것을 알지 못했습니다. 영향을 받은
  것은 좁은 문자였습니다 – 숫자 1, 콜론, 하이픈 – 모든 문자를
  개별적으로 배치하는 문서에서였으며, 실습 문서도 그중 하나였습니다.

- **그리고 같은 문서들이 정리할 때 더 이상 이미지로 바뀌지
  않습니다.** 그런 문자가 남아 있었기 때문에 검증이 잔여물을
  보고했고, 페이지는 조심스럽게 래스터화되었습니다. 그 위의 텍스트는
  그 이후로 이미지에 불과했습니다 – 더 이상 검색할 수 없고, 표시할
  수 없으며, 파일 용량은 더 커졌습니다. 실습 문서는 이제 양쪽
  페이지 모두 실제 텍스트로 유지됩니다.

- **색이 있는 표시가 더 이상 되돌려진 텍스트 위에 남아 있지
  않습니다.** 교체를 되돌린 사람은 복원된 단어 위에 여전히 색이
  있는 사각형이 있는 것을 보았습니다 – 그것은 원본이 다시 그
  자리에 있음에도 "여기서 무언가가 제거되었다"고 주장하고 있었습니다.

- **막대가 더 이상 그 아래 단어가 얼마나 길었는지 알려주지
  않습니다.** 마스킹 시 짧은 줄에서는 막대가 이제 **줄 전체**를
  덮습니다 – 주소 블록, 머리글 정보, 좁은 표 셀. 줄 전체가 맞지
  않으면(세 개의 열이 있는 일반적인 표 줄) 필드 단위로 유지됩니다
  – 본문 줄에서는 단어 단위로 유지됩니다. 그렇지 않으면 문장 한가운데
  이름 하나가 문장 전체를 검게 만들어 버립니다. 그리고 서로 아래
  위에 있는 막대는 **같은 길이**가 됩니다 – 주소 블록에서는 각
  줄에 값이 하나씩 있고, 길이가 서로 다른 세 개의 막대는 줄의
  길이를 계속 알려주고 있었습니다. 다만 종이가 비어 있는 만큼만
  늘어나며 – 인접한 열 앞에서는 막대가 멈춥니다.

- **"줄 전체"는 이제 정말로 줄 전체를 마스킹합니다.** 지금까지는
  막대가 다음 큰 간격에서 끝났습니다 – 즉 필드의 끝에서. 본문에서는
  눈에 띄지 않았습니다, 거기서는 필드가 곧 줄이기 때문입니다 –
  머리글 정보와 표에서는 그렇지 않았습니다: "Name: Anna Musterfrau
  Abteilung: Vertrieb"에서는 막대가 정확히 이름의 마지막 글자에서
  끝났고, 그 결과 이름의 길이가 다시 종이 위에 남아 있었습니다.
  이제 막대는 줄의 첫 단어부터 마지막 단어까지 이어지며 인접한
  열도 함께 가져갑니다. 값만 맞추고 싶은 사람은 "단어"를 선택하면
  됩니다 – 자동 모드는 변함없이 필드 단위로 마스킹합니다.

- **출고 전에 완성된 파일을 마지막으로 한 번 더 검색합니다.** 지금
  까지의 모든 검사는 누군가가 미리 지정해 둔 위치만 확인합니다 –
  페이지 텍스트, 발견 영역, 이미지 영역. 그러나 PDF에는 나열로는
  다 담을 수 없을 만큼 많은 저장소가 있습니다 – 주석, 양식 값,
  책갈피, 문서 정보, 첨부 파일, JavaScript. 그래서 마지막에는
  Maskuro가 작성된 파일에서 교체한 모든 것을 무차별적으로 검색합니다
  – 같은 문구가 허용되는 페이지 텍스트를 제외한 모든 곳에서입니다.
  그곳에 무언가 남아 있으면 결과물이 **생성되지 않으며**, 메시지가
  그 값을 알려줍니다. 정리되었다고 여겨지는 문서는 아예 없는 것보다
  나쁩니다.

- **검사할 수 없었던 것은 더 이상 검사된 것으로 간주되지
  않습니다.** 지금까지는 세 가지 경로에서 검증 실패가 깨끗한
  결과처럼 보였습니다. 텍스트 레이어를 읽을 수 없는 페이지는
  특별히 깨끗한 것으로 간주되었습니다 – 거기에는 찾을 것이 없었기
  때문입니다 – 이제는 래스터화됩니다. 남아 있는 검출 위치가 있는
  페이지를 대체로 래스터화할 수 없었던 경우, 조용히 그대로
  출력되었습니다 – 이제는 정리 작업이 중단됩니다. 그리고 후속
  수정 창의 대조 검사는 자체 오류가 발생한 뒤 "남은 것 없음"이라고
  보고했습니다 – 창 안에서는 이것이 모든 것이 제거된 상태와
  구분되지 않았습니다 – 이제는 "페이지 래스터화" 버튼과 함께
  경고가 나타납니다.

- **"기본값으로 초기화"는 대부분의 설정을 실제로 초기화하지
  않았습니다.** 22개의 체크 중 9개가 조작 후에도 변경되지 않은
  채였습니다 – 미리보기, "정리된 파일을 이후 열기", 후속 수정
  창, 즉시 저장, 두 개의 업데이트 체크가 포함되어 있었습니다.
  저장된 파일은 비워졌지만, 창은 이전 값을 그대로 유지하고 다음
  클릭 때 다시 써넣었습니다. 이제는 모든 체크가 되돌아오며, "변경됨"
  표시도 함께 사라집니다.
- **"정리마다 검증 보고서 자동 저장"이 표시는 되었지만 실제로는
  꺼져 있었습니다.** 초기화 후에도 체크는 그대로 유지되었지만
  값은 삭제되어 있었습니다 – 보고서가 더 이상 생성되지 않았는데도
  아무 표시가 없었습니다. 검증 로그와 자체 화면 녹화도 마찬가지였습니다
  – 이들의 단축키도 초기화 시 이제 함께 올바르게 등록되거나
  해제됩니다.

- **한 줄의 막대들이 이제 같은 모양입니다.** 지금까지는 각 검출
  위치가 자체 막대를 가져왔고, 그 높이는 해당 단어의 글꼴에서
  결정되었습니다. 서로 다른 크기의 레이블과 값이 있는 줄에서는
  두꺼운 선과 얇은 선이 어긋난 가장자리와 함께 나란히 놓였고,
  두 검출 위치 사이가 공백 하나로만 떨어져 있으면 그 위에 밝은
  틈이 남았습니다. 같은 줄의 막대는 이제 같은 위아래 경계를
  가지며, 공백 하나로만 떨어진 부분은 하나의 막대가 됩니다. 두
  검출 위치 사이에 남아야 할 것 – 이름 뒤의 쉼표, 레이블, 금액 –
  은 계속 이들을 갈라놓습니다. 조판된 페이지와 스캔 모두에
  적용됩니다.

- **"이 프로그램 정보" 아래의 탭이 다시 맨 위에서 시작됩니다.**
  개인정보 보호, 라이선스 조건, 라이선스 안내는 텍스트 중간에서
  열렸습니다 – 읽으려면 먼저 맨 위로 스크롤해서 첫 줄을 봐야
  했습니다.

- **펜 아이콘은 더 이상 두 번째 편집기 창을 열지 않고 기존 창을
  앞으로 가져옵니다.** 지금까지는 클릭할 때마다 새 창이 생겼습니다.
  이 창은 작업 표시줄에 별도 항목이 없습니다 – 최소화한 사람은
  다시 접근할 수 없어 다시 클릭했고, 메인 창을 복원하면 쌓여
  있던 창들이 한꺼번에 앞으로 나왔습니다. 이제 추가 문서는 열려
  있는 창의 탭 표시줄에 들어가며, 이미 거기 있는 문서는 두 번째
  탭을 얻지 않습니다.

- **"고급 인식"은 모델이 없는 동안 더 이상 "변경됨" 표시를 달지
  않습니다.** 출고 시 켜져 있지만, 내려받아야 하는 모델 없이는
  실제로 작동할 수 없습니다 – 설정에서는 그래서 아무도 손대지 않았어도
  새로 설정된 모든 컴퓨터에서 이 줄이 변경된 것으로 표시되었습니다.
  이제 체크가 꺼져 있는 이유는 오직 라벨만으로 알 수 있습니다:
  "모델이 아직 로드되지 않음".

- **소개 안내 띠가 오피스와 텍스트 파일에서 PDF 화면을
  설명했습니다.** 거기에는 "단어를 클릭하면 마스킹됩니다"라고
  되어 있었지만 – Word 파일에서는 클릭해도 아무것도 마스킹되지
  않습니다, 거기서는 선택 후 버튼을 눌러야 합니다. 이제는 각
  화면에서 실제로 적용되는 내용을 알려줍니다.
- **텍스트 보기에서는 도구 모음이 레이블로 꽉 차 있었습니다.**
  "선택 교체", "선택 마스킹", "선택 되돌리기", "페이지 보기",
  "PDF로 마스킹"이 이제 PDF에서의 대응 항목과 마찬가지로 아이콘으로
  표시됩니다. 이름은 계속 간단 설명과 메뉴에 남아 있습니다.
- **비교 돋보기에서 Ctrl+마우스 휠은 확대 슬라이더를 함께 움직이지
  않았습니다.** 글자는 커졌지만, 슬라이더와 그 옆의 퍼센트 표시는
  계속 이전 상태를 나타내고 있었습니다.
- **업데이트 설치 프로그램이 앞으로 나오지 않았습니다** – 작업
  표시줄에서 먼저 클릭해야 했습니다(Windows에만 해당).
- **줄 시작의 연도 숫자가 오스트리아 우편번호로 간주되었습니다.**
  이력서에서 "2020 Verkaufsstrategien"이 자리표시자가 되어 – 줄
  전체가 사라졌습니다. 1900에서 2099 사이의 네 자리 숫자에는
  이제 두 번째 주소 신호가 필요합니다 – 위쪽의 도로명, 앞에 있는
  필드 단어, 국가 코드, 또는 알려진 지명입니다. 주소 블록에는
  이것이 있지만, 연도 열에는 없습니다.
- **월-연도 조합이 전화번호로 간주되었습니다.** "Seit 08.2010
  123-Verkauft GmbH"에서 "전화번호"가 생겼습니다 – 월, 연도, 그리고
  그 뒤 회사명의 첫 자릿수들이었습니다.
- **보고서는 "텍스트 인식으로 검사됨"이라고만 하고, 인식하지
  못하는 부분은 언급하지 않았습니다.** 이미지가 그대로 남는 경우,
  이제 손글씨는 그 안에서 찾을 수 없다는 안내가 함께 표시됩니다
  – 서명이나 손으로 적은 이름은 그대로 남습니다. 지금까지는 이
  문구가 스캔된 페이지에서만 나타났습니다 – 삽입된 서명이 있는
  일반 PDF에는 아무런 언급이 없었습니다.
- **마스킹된 이미지 배경 위의 자리표시자가 막대의 왼쪽 가장자리에
  붙어 있었습니다.** 이미지에서 값이 발견되면 – 예를 들어 스캔된
  서명 옆의 타자로 친 이름 – 이미지 영역은 전체 너비로 마스킹되어야
  합니다. 짧은 자리표시자는 옆에 헐벗은 검은색을 남겨, 마치 두
  번의 처리가 있었던 것처럼 보였습니다. 이제는 막대 위 가운데에
  위치합니다.

## 0.10.41-alpha.20260826 – 2026년 8월 26일

### 신규

- **체험 기간이 끝나면 시작할 때마다 한 번씩 창이 라이선스를 상기시킵니다.**
  시작 후 5분 뒤에 나타납니다 – 첫 작업을 방해하지 않도록 즉시는 아닙니다
  – 그리고 정리 작업이 진행 중이면 기다립니다. 여기서 구매로 이어지는
  경로와 이미 구매한 키를 입력하는 경로가 각각 있습니다. "나중에"는
  버튼의 5초가 지나면 창을 닫습니다. 아무것도 잠기지 않습니다 – 무료
  등급은 지금까지처럼 계속 작동합니다.

- **무료 등급에서 실행 전 대기 시간이 이제 30초가 아니라 10초입니다.**
  이는 라이선스를 상기시키기 위한 것이지, 작업을 멈추기 위한 것이
  아닙니다.

- **라이선스에 관한 세 가지 안내가 이제 모두 같은 모습입니다.** 대기
  시간, 체험 마지막 며칠 동안의 알림, 체험 기간 종료 후 안내는 같은
  띠, 같은 구성, 같은 버튼을 갖습니다 – 남은 시간은 옆에 큰 숫자로
  표시되는 대신 버튼 안에 표시됩니다.

- **미리보기의 검출 목록이 다시 세로로 나열됩니다.** 9개 값부터는 두
  단으로 표시되었습니다 – 훑어볼 때 시선이 두 갈래 사이를 오가게
  되는데, 여기서는 줄 단위로 결정합니다. 두 단을 선호하는 사람은
  창 왼쪽 아래에서 다시 켤 수 있습니다 – 선택은 저장되며, 전환할
  때 이미 선택 해제된 값은 계속 선택 해제된 상태로 남습니다.

- **자체 AI 서버를 연결하는 모든 사람에게 AI 단계가 열려 있습니다.**
  "설정 → AI"에 연결, AI가 할 수 있는 일, AI가 맡게 될 작업이 모두
  들어 있습니다 – 그리고 서버가 등록되면 그 위에 이 단계를 위한
  스위치와 검증 기능이 함께 나타납니다. 자신의 작업 PC에서 함께
  연산하는 언어 모델은 계속 보류됩니다 – 10페이지에 몇 분이 걸려서
  일상적인 사용에는 맞지 않기 때문입니다.

- **자체 AI를 연결할 수 있습니다.** 동봉된 언어 모델 대신, 다른
  컴퓨터의 더 큰 모델이 응답하도록 할 수 있습니다 – 사내 서버나
  강력한 그래픽카드를 갖춘 워크스테이션 등입니다. OpenAI 호환
  인터페이스를 제공하는 서비스(Ollama, LM Studio, llama.cpp-server,
  vLLM, LocalAI)가 필요하며, "설정 → 자체 AI"에서 설정합니다 – 실제로
  모델에 질의하고, 속도를 측정하고, 가능한 응답 형식을 확인하는
  연결 검사도 함께 제공됩니다. 이때 여러 텍스트 구간이 순차적이
  아니라 동시에 처리됩니다.

- **AI가 할 수 있는 일과 맡게 될 작업을 이제 설정할 수 있습니다.**
  세 개의 스위치가 경계 사례 검토, 독자적 검색, 본문 내 검색 여부를
  결정합니다. 모델에 내리는 지시문은 그대로 표시되고, 사내 용어를
  추가할 수 있으며, 버튼 하나로 기본값으로 되돌릴 수 있습니다.

- **이때 텍스트가 자체 네트워크를 벗어나면 실행 전마다 경고합니다.**
  Maskuro는 주소를 통해 AI 서버가 사내에 있는지 인식하며, 알려진
  제공업체는 이름으로 언급합니다. 경고는 끌 수 있지만, 이 전송을
  수행할 권한이 있다는 명시적 확인이 있어야 하며, 정확히 그 주소에
  대해서만 적용됩니다. 그 과정 자체는 바뀌지 않습니다 – 전송은
  계속해서 각 파일의 로그와 검증 보고서에 기록됩니다. 명령줄에서는
  묻지 않고 중단됩니다 – 거기서는 `--ki-auswaerts-erlauben`이
  필요합니다.

- **교체 전 미리보기는 새 설정에서 기본적으로 켜져 있으며, 이제 명시적으로
  정리된 클립보드 내용과 프로그램에 붙여넣은 텍스트·이미지에도
  적용됩니다.** 문서 묶음의 경우 여전히 문서마다 모든 페이지가 담긴
  미리보기가 정확히 한 번씩 나타납니다 – 짧은 복사본의 조용한 즉시
  정리는 의도적으로 창을 열지 않습니다.

- **미리보기에서 검출 항목을 색이 있는 줄 전체를 이용해 켜고 끌 수
  있습니다.** 체크 표시는 이제 크고 대비가 뚜렷합니다. 추가로 상태
  필드가 "교체" 또는 취소선이 그어진 "교체"를 표시하여, 어두운
  신뢰도 색상 위에서도 선택된 값과 선택 해제된 값을 즉시 구분할 수
  있습니다.

- **눈에 보이는 보안 대조 검사를 포함한 PDF도 이제 문서당 미리보기가
  한 번만 열립니다.** 선택 해제된 용어는 나중의 페이지 검증을
  위해서도 선택 해제된 상태로 유지됩니다 – 그 검증은 동일한 실행을
  두 번째 대화상자로 중단하지 않고 계속 진행됩니다.

- **대체 단어는 후속 수정 편집기에서 래스터화된 페이지에서도 동일하게
  보입니다.** 빨간 자리표시자가 PDF 텍스트 레이어가 아니라 픽셀
  안에 있는 경우에도, 이제는 일반 PDF 텍스트 자리표시자와 동일하게
  신뢰도에 따라 색이 지정된 배경을 갖습니다.

- **교체 전 미리보기부터 이미 발견된 용어의 검토 필요성을 보여
  줍니다.** 각 줄은 나중에 편집기에서 대체 값에 쓰이는 것과 같은
  빨강-주황-초록 색을 갖습니다. 같은 범주 안에서는 신뢰도가 낮은
  것과 빨간 오탐 후보가 위쪽에, 강력한 초록 근거가 아래쪽에 표시되며,
  동점인 경우 알파벳 순서를 따릅니다. 같은 값이 여러 발견 위치에서
  나오면, 안전을 위해 그중 가장 의심스러운 평가가 적용됩니다.
  평가되지 않은 특수 사례는 빨강과 주황 사이의 중립적인 노란색으로
  표시됩니다.

- **결과물을 이제 후속 수정 편집기에서 바로 파일로 복사할 수
  있습니다.** "결과 복사"는 현재 정리된 버전을 편집기를 닫거나 메인
  목록에서 파일을 다시 찾지 않고도 클립보드에 넣습니다. 아직 저장되지
  않은 수동 편집이 있으면 그 전에 완전한 안전 저장 경로가 자동으로
  실행됩니다 – "이미지 복사"는 순수 픽셀만을 위한 별도 기능으로
  남습니다.

- **교체된 단어는 편집기에서 무엇을 먼저 검토해야 하는지 한눈에
  보여 줍니다.** spaCy가 일괄적으로 85퍼센트를 보고하더라도 순수한
  언어 모델의 추측은 빨간색입니다. 근거가 뒷받침되지 않은 다른
  모델 판단은 최대 주황색까지만 표시됩니다 – 강력한 명명된 근거는
  초록색이 될 수 있습니다. 수동 작업과 평가할 수 없는 기존 대응
  관계는 중립적인 노란색으로 남습니다. 자동 마스킹 막대도 편집기
  미리보기에서 이 색상을 갖습니다 – 이제는 그 막대가 래스터화된
  PDF 페이지의 일부인 경우에도 마찬가지입니다. 이를 위해서는
  대응 관계가 일치해야 하고, 이전 단어 상자가 확실히 완전히 검은색
  이어야 합니다 – 일반적인 굵은 글씨는 색이 지정되지 않습니다.
  저장된 PDF에서는 모든 막대가 변함없이 완전히 검은색으로
  유지됩니다.

- **미리보기에서 선택 해제한 항목을 영구적으로 기억시킬 수
  있습니다.** 체크를 없애는 것은 곧 "여기서 인식이 틀렸다"는
  의미입니다. 지금까지는 이것이 해당 문서 하나에만 적용되었습니다.
  이제 줄에 "다시는 안 함" 스위치가 나타납니다 – 누르면 그 값이
  "절대 제거 안 함" 목록에 영구적으로 들어가고, 앞으로 모든
  문서에서 문제없는 값으로 간주됩니다. 목록 아래에는 "교체"를
  누르기 전에 무엇이 영구적으로 바뀌는지가 표시됩니다. 반대 방향은
  의도적으로 없습니다 – 한 번 발견된 것은 인식이 다시 찾아냅니다.

- **버튼 하나로 모든 설정을 출고 상태로 되돌립니다.** 설정 창 왼쪽
  아래에 있으며, 실행 전에 확인을 묻습니다. 파일, 라이선스, 사용자
  지정 인식 규칙, 자동 시작은 영향을 받지 않습니다 – 관리자가
  지정한 값은 계속 적용됩니다. 출고 상태와 다른 모든 설정에는 추가로
  "변경됨" 표시가 붙어, 무엇을 바꿨는지 한눈에 알 수 있습니다.

### 변경됨

- **결과는 더 이상 저절로 저장되지 않고, 저장할 때 비로소 저장됩니다.**
  창에서의 실행은 정리된 버전을 먼저 임시 위치에 기록합니다 – 원본
  옆의 "…_bereinigt" 파일은 "저장"을 눌러야 비로소 생성됩니다.
  그때까지는 결과물을 보고, 후속 수정하고, 복사할 수 있습니다. 완료된
  각 줄에는 저장 버튼이 있고, 목록 아래에는 "모두 저장"이 있으며,
  편집기에서는 Ctrl+S가 적용됩니다. 목록을 비우거나 프로그램을
  종료하면 확인을 묻습니다 – 아무도 저장하지 않은 것은 어디에도
  남지 않습니다. "폴더에서 보기"는 저장 전에는 비활성화됩니다 – 임시
  위치는 누군가를 안내할 만한 목적지가 아닙니다. 대응 관계 파일도
  저장할 때 함께 저장됩니다.

  설정의 "프로그램" 아래에서 "결과를 즉시 원본 옆에 저장"을 선택하면
  기존 동작으로 되돌아갑니다. 명령줄, 폴더 감시, 클립보드 감시는
  변함없이 즉시 저장합니다 – 그곳에는 저장할 사람이 아무도 없습니다.

- **후속 수정 편집기의 도구 모음이 정리되었습니다.** 학습 모드는
  이제 비교 돋보기, "교체된 값"과 함께 오른쪽 끝에 있습니다 – 작동
  방식을 켜고 끄는 세 스위치가 이렇게 한데 모였습니다. "모든
  페이지에 적용"은 세 가지 마스킹 형태 쪽으로 옮겨졌습니다 – 거기서만
  작동하기 때문입니다. "결과 복사", "파일 – 초기화", "모든 페이지에
  적용"은 이름 표시 없이 아이콘만 사용합니다 – 이름은 계속 툴팁과
  메뉴에 남아 있습니다. "교체"와 "원본 되돌리기" 사이에는 구분선이
  있습니다 – 이 둘은 서로 반대 방향이며, 나란히 있으면 같은 도구의
  두 가지 변형처럼 보였습니다.

- **"결과 복사" 아이콘이 이제 문서를 나타냅니다.** 접힌 모서리와
  텍스트 줄이 있는 종이 두 장을, 작은 모서리 화살표가 있는 동일한
  종이 두 장 대신 사용합니다. "이미지 복사"는 반대로 이미지 기호를
  사용해, 이름 표시 없이도 둘을 구분할 수 있습니다. 결과 목록의
  "복사" 버튼도 같은 문서 아이콘을 표시합니다 – 같은 파일을
  저장하기 때문입니다.

- **설정이 정리되고 제목이 붙었습니다.** "인식"에는 이제 *무엇을
  제거하는지*, *어떻게 대체하는지*, *얼마나 철저히 검색하는지*,
  *실행 전후*라는 네 개의 절이 있습니다. 얼굴 인식과 바코드/QR 코드는
  찾기 쉽도록 이미지 항목 쪽에 있습니다. "프로그램"은 *결과 파일*,
  *시작 시*, *업데이트*, *화면 표시*, *피드백*으로 나뉘어 있으며,
  결과 파일 이름 접미사는 언어와 화면 설정 사이가 아니라 결과 파일
  항목에 있습니다.

- **고급 인식이 기본적으로 켜져 있습니다** – 해당 언어 모델이 아직
  로드되지 않은 경우에도 마찬가지입니다. 이전에는 기본값이 모델
  보유 여부에 달려 있었고, 새로 설정된 컴퓨터는 계속 더 약한 단계로
  작동했습니다. 설정 마법사는 첫 페이지에서 모델을 내려받도록
  제안하고 그 옆에 용량을 표시합니다. 모델이 없으면 체크 표시는
  실제로 작동하지 않는 단계를 흉내내는 대신 그 사실을 계속 알려
  줍니다.

- **두 용어 목록의 이름이 하는 일 그대로 바뀌었습니다:** "사용자
  정의 용어" 대신 "항상 제거", "예외" 대신 "절대 제거 안 함".

- **미리보기 창이 더 보기 쉬워졌습니다.** 9개 값부터는 두 열로
  표시되고, 줄은 더 납작하며, 발견 위치 수는 오른쪽 끝이 아니라
  용어 바로 뒤에 표시됩니다.

- **후속 수정 편집기에서는 이제 교체가 마스킹보다 앞에 있습니다** –
  도구 모음, "도구" 메뉴, 페이지 오른쪽 클릭 메뉴 모두에서. 교체가
  기본 방식입니다 – 자리표시자는 클릭해서 되돌릴 수 있지만, 막대는
  그렇지 않습니다.

- **편집기에서 중복된 버튼이 줄었습니다.** "다른 이름으로 저장…"과
  "이미지 복사"는 이제 파일 메뉴에만 있으며, 익숙한 단축키를
  유지합니다. 도구 모음에는 각각 하나씩만 남습니다 – 저장과 "결과
  복사". 어디에 저장되는지는 어차피 상태 표시줄에 나타나며, 거기서
  클릭 한 번으로 바꿀 수 있습니다.

- **클립보드 감시는 첫 실행 시 더 이상 제안되지 않습니다.** 이
  기능은 시스템의 모든 복사 작업에 개입합니다 – 프로그램을 처음
  보는 사람은 그 영향을 가늠할 수 없습니다. 설정에는 여전히 관련
  설명과 함께 남아 있습니다.

- **밝은 화면 모양이 눈을 덜 부시게 합니다.** 창 배경은 지금까지
  각 시스템 스타일에서 가져왔으며, 그 결과 아무도 직접 결정하지
  않은 유일한 큰 면적이었습니다 – Windows에서는 거의 흰색이었습니다.
  이제는 모든 시스템에서 동일한, 살짝 깨진 흰색입니다.

- **둘러보기와 설명서가 색상의 의미를 설명합니다.** 교체된 단어
  뒤의 빨강, 주황, 초록, 노랑이 무엇을 의미하는지가 이제 둘러보기의
  독립된 단계와 설명서의 한 문단으로 – 모든 언어판에서 – 제공됩니다.

### 수정됨

- **설명서와 FAQ에 더 이상 존재하지 않는 자리표시자가 표시되어
  있었습니다.** 짧은 형식으로 전환된 이후 Maskuro는 `[NAM1]`을
  씁니다 – 도움말에는 여전히 `[NAME1]`이 남아 있었고, "기본값은
  `[NAME1]`입니다"라는 문장은 그래서 단순히 틀린 내용이었습니다.
  번역된 17개 언어판에도 각 언어 고유의 표기 대신 **독일어** 표기가
  남아 있었습니다 – 스페인어 사용자는 프로그램이 `[NOMB1]`을 쓰는데도
  `[NAME1]`을 보고 있었습니다. 결과 파일 접미사도 마찬가지였습니다 –
  모든 언어판이 `_bereinigt`를 약속했지만, 프로그램은 `_limpiado`,
  `_nettoyé`, 또는 `_除去済み`를 생성했습니다. 번호가 없는 형식
  (익명화 시 모든 것이 `[NAME]`가 아니라 `[NAM]`으로 표시됨)과 해시
  처리 시 값에서 유도되는 식별자도 영향을 받았습니다.

- **미리보기 창은 이제 문서당 한 번만 중단되며, 두 번째 중단은
  정말로 새로운 내용이 추가될 때만 발생합니다.** PDF는 두 경로에서
  읽힙니다 – 한 번은 콘텐츠 스트림에서, 마지막으로는 렌더링된 눈에
  보이는 페이지에서. 지금까지는 둘 다 각자 물었습니다. 이제는
  첫 번째 창에서 결정한 내용이 그대로 유지되며, 거기에 이미 있던
  값은 다시 나타나지 않습니다. 반대로 완성된 페이지의 시각 검증에서
  이전에 어디에도 없던 것이 발견되면, 이미 결정된 값 없이 그것만
  다시 표시됩니다.

- **미리보기 창은 이제 무엇을 기준으로 결정해야 하는지 알려
  줍니다.** "체크 제거 = 값이 그대로 남음"이라는, 체크가 *무엇을
  하는지*는 알려주지만 *언제* 없애야 하는지는 알려주지 않던 문구
  대신, 이제는 다음과 같이 표시됩니다: 개인정보 관련 값이 아닌
  모든 곳에서 체크를 없애십시오 – 그곳은 인식이 틀린 것입니다.
  추가로 각 창은 자신의 값이 어느 검증 실행에서 나왔는지 알려
  줍니다.

- **자리표시자가 문서 전체에서 동일하게 보입니다.** OCR 경로에서
  이미지 페이지로 다시 만들어지는 페이지에서는 지금까지 눈에 보이는
  자리표시자가 타자기체로 표시되었습니다 – "[PLZ4]"는 같은 페이지의
  좁은 "[NAM1]" 옆에 세리프체로 넓게 표시되었습니다. 이제는 다른
  곳과 동일한 산세리프체를 사용하며, 맞춤에서 계획된 것보다 더
  넓게 표시되지도 않습니다. 보이지 않는 검색 레이어는 자체 글꼴을
  유지합니다 – 이 레이어에는 모양이 아니라 신뢰할 수 있는 크기가
  필요합니다.

- **편집기 도구 모음에 더 이상 중복된 구분선이 없습니다.** 열린
  파일 형식에 전체 도구 그룹이 필요 없는 경우 – 예를 들어 PDF에서
  페이지 보기와 렌더링 – 지금까지는 그 빈 자리 양쪽에 구분선이
  둘 다 남아 있었습니다.

- **되돌리기 시 이제 가끔 흰 자리만 남는 일이 없습니다.** 이미
  정확히 복원된 원본 텍스트가 제거된 자리표시자의 넓고 뭉뚱그려진
  상자에 의해 흰색으로 덮이는 일이 더 이상 없습니다. 텍스트와
  이미지가 섞인 되돌리기의 경우, 텍스트는 페이지 이미지가 정확히
  이 원본 상태를 이미 눈에 보이게 담고 있을 때만 보이지 않게
  삽입됩니다. 이는 프레임, 검출 패널, PDF 첨부파일에 모두
  적용됩니다.

- **"원본 되돌리기"는 더 이상 불필요하게 페이지를 래스터화하자고
  제안하지 않습니다.** 엄격한 잔여 텍스트 검사는 마스킹과 교체
  단계에서는 계속 활성 상태입니다. 되돌리기에서는 생략됩니다 – 거기서는
  의도적으로 원본 내용이 다시 추가되며, 확장된 되돌리기 프레임 안의
  변경되지 않은 인접 단어는 정리 오류가 아니라 오탐이었습니다.

- **편집기 둘러보기는 이제 "교체"와 "원본 되돌리기"를 각각의 독립된
  단계로 설명합니다.** 두 도구 모두 도구 모음에서 직접 강조되며,
  드래그한 프레임이 자리표시자를 삽입하는지, 아니면 원본 파일에서
  해당 위치의 원래 내용을 되돌려오는지를 설명합니다.

- **국가별 자리표시자도 이제 최대 4글자로 유지됩니다.** 이런
  종류들은 지금까지 중앙 약어 목록에 없어서 `[UMSATZSTEUER_ID1]`처럼
  풀어서 표시될 수 있었습니다. 새 실행부터는 대신 `[UID1]`을 씁니다
  – 자동으로 인식되는 모든 독일어·영어 종류는 그대로 명확하게
  유지됩니다. 이름이 같을 경우, 다른 화면 언어에서 직접 계산된
  약어도 더 이상 4글자를 넘지 않습니다. 사용자 지정 규칙 이름은
  입력한 그대로 변경 없이 유지됩니다.

- **교체는 마스킹하기 전에 실제로 남는 줄 공간을 이제 모두
  활용합니다.** 기존 원래 단어 너비의 3배라는 고정된 한계는 상당
  부분 비어 있는 양식 필드에서도 막대를 만들어냈습니다. 눈에 보이는
  OCR 대조 검사의 검출 결과도 PDF 텍스트가 있는 경우 이제 읽을 수
  있는 자리표시자를 받습니다 – 순수 이미지, 주석, 벡터 내용, 선택한
  마스킹 방식, 그리고 명확한 축약형조차 들어갈 수 없는 실제 좁은
  공간만 검은색으로 남습니다.

- **이미 보이는 자리표시자가 보안 래스터화 시 다시 빨간색으로
  덧씌워지는 일이 없습니다.** 이제 래스터화는 기존 대체값을 페이지
  이미지에서 그대로 가져오고, 보이지 않는 검색 사본만 추가로
  만듭니다. 보안 막대가 정확히 이 위치를 덮어야 하는 경우, 짧은
  원래 앵커만이 아니라 실제 자리표시자 상자 전체가 새로 그려집니다.

- **"원본 되돌리기"는 이제 드래그한 프레임 안에서 안전한 대상만
  표시합니다.** 그 안의 교체된 모든 용어가 개별적으로 정확하게
  강조됩니다 – 변경되지 않은 본문 텍스트는 그대로 유지됩니다. 실제
  벡터 마스킹 막대도 그 아래의 검은 PDF 영역 안에 원본 텍스트가
  있는 경우 개별적으로 표시됩니다. 래스터화된 페이지에서는 미리보기가
  의도적으로 가상의 막대 영역을 표시하지 않습니다 – 이전의 픽셀
  검색은 글자, 밑줄, 표 선을 잘못된 위치의 큰 빨간 영역으로 묶어
  버렸습니다. 복원 기능 자체는 이에 영향을 받지 않습니다.

- **래스터화된 페이지에서 복원 시 텍스트가 다시 돌아옵니다.** 마지막
  까지 그 자리에는 빈 공간과 그 위의 색이 있는 사각형만 남아
  있었습니다. 되돌려진 텍스트는 문서 안에 있었지만, 페이지 구성에서
  더 뒤에 그려지는 자리표시자의 흰 배경에 덮여 있었습니다.

- **검증 색상이 더 이상 여러 겹으로 겹치지 않습니다.** 같은 위치가
  대응 관계의 항목마다 색이 칠해졌습니다 – 한 페이지에 실제 발견
  위치가 다섯 개면, 각각 다섯 번 덧칠되어 옅은 표시가 짙은 덩어리가
  되었습니다. 그리고 이제는 전혀 교체되지 않은 단어에는 표시가
  나타나지 않습니다 – 원본 값이 여전히 페이지에 있으면, 그곳에는
  더 이상 표시도 없습니다.

## 0.10.40-beta.1 – 2026년 8월 24일

### 수정됨

- **편집기의 마스킹 막대에 이제 안전 여백이 있습니다.** 단어, 줄, 자유
  프레임은 튀어나온 글자 획과 매끄럽게 처리된 가장자리 픽셀까지 덮으며,
  렌더링 검사가 눈에 보이는 잔여물이나 읽어낼 수 있는 원본 텍스트가
  남지 않도록 추가로 보장합니다.

- **대체 텍스트는 읽기 쉽고 일관되게 짧게 유지됩니다.** 새로운 이름,
  주소, 자유 용어는 예를 들어 `[NAM1]`, `[ADR2]`, `[BEG3]`처럼 표시됩니다.
  고정된 최소 크기는 4.5포인트이며, 공간이 부족하면 먼저 줄이고 사용
  가능한 여백을 넓힙니다. 긴 자리표시자를 쓰던 기존 대응 관계도 계속
  읽고 되돌릴 수 있습니다.

- **검출 패널에서 이루어진 여러 단어 교체는 이제 중복 표시와 원본
  잔여물로부터 보호됩니다.** 번호가 매겨진 자리표시자가 있든 없든
  회귀 테스트를 통과하며, 발견 위치마다 정확히 하나의 공통 대응
  관계만 유지됩니다.

- **macOS에서 되돌려진 클립보드 내용이 즉시 다시 정리되지 않습니다.**
  기록 후 시스템 서명이 지연되어 바뀌는 경우에도 Maskuro는 자신의
  내용을 안정적으로 인식합니다.

### 신규

- **편집기가 파일을 방금 정리된 원래 상태로 완전히 되돌릴 수
  있습니다.** "파일 – 초기화"는 확인 후 현재 탭의 모든 후속 수정
  사항을 되돌립니다 – 교체 목록과 개수도 포함됩니다. 변경 사항이
  없으면 이 명령은 비활성화되어 있으며, 실행 후에는 "실행 취소"로
  다시 되돌릴 수 있습니다.

- **이동된 날짜 표기는 이제 여러 파일에 걸쳐서도 시간 순서를 확실히
  유지합니다.** 공통 오프셋은 전략을 켜는 시점에 규칙에 영구적으로
  고정됩니다. 또한 오프셋은 더 이상 0일이 될 수 없어, 실제 날짜가
  모르는 사이에 그대로 남는 일이 없습니다.

- **PDF 수동 작업이 이제 완전한 전문가용 마스킹 절차를 지원합니다.**
  단일 용어, 목록, 정규 패턴을 열려 있는 PDF나 폴더 안의 모든 PDF에서
  검색해 안전하게 마스킹할 수 있습니다. 페이지 전체와 페이지 범위도
  직접 선택할 수 있습니다. 색상, 중성 흰색 영역, 덮어쓰기 텍스트,
  글꼴, 정렬, 반복에는 미리보기가 있으며, 재사용 가능한 코드를
  관리하고 가져오기·내보내기 할 수 있습니다. PDF 정리는 완전한 재구성을
  통해 모든 숨겨진 내용을 제거하거나, 선택한 데이터 종류만 제거하는
  방식 중에서 선택할 수 있습니다. 가장 안전한 선택이 명확히 추천되며,
  잘못된 검색 패턴은 설명되고, 폴더 단위 실행은 결과 사본만 기록합니다.

- **자발적 사용 통계가 이제 설치 수와 버전 전환을 보여 줍니다.**
  Maskuro는 이를 위해 무작위로 생성되어 로컬에 저장되는 설치 식별자를
  만듭니다. 여기에는 기기, 사용자, 라이선스 정보가 포함되지 않으며,
  서버는 그 SHA-256 값만 저장합니다. 이 통계는 설정에서 완전히 끌 수
  있습니다.

- **둘러보기가 이제 두 창을 모두 거치는 안내형 실습이 되었습니다.**
  가상의 실습 문서를 스스로 목록에 추가하고, 정리까지의 과정을
  설명하며, 실행 후에는 편집기에서 자동으로 이어집니다. 둘러보기를
  중단하면 이 이어지는 과정도 함께 종료됩니다.

- **15개 추가 법역의 회사가 인식됩니다.** 발트 3국, 벨기에, 스칸디나비아,
  체코, 폴란드, 남동유럽, 싱가포르, 브라질, 멕시코의 서류를 정리할 때
  법인 형태를 몰라서 회사 이름을 잃는 일이 이제 없습니다 – 새로 추가된
  것으로는 OÜ, MTÜ, SIA, VZW, ASBL, P/S, Sh.p.k., EIRELI, z.s., o.p.s.,
  S.K.A., Pte. Ltd., S.A. de C.V., S. de R.L. 등이 있습니다.

### 변경됨

- **편집기 도구 모음이 이제 공간을 더 목적에 맞게 사용합니다.** 명확한
  기본 아이콘과 바로 알아볼 수 있는 도구 모양이 반복되는 텍스트 없이
  모음에 표시됩니다. 모호한 동작은 이름을 그대로 유지합니다. "보기"
  아래에서 "도구 이름 표시"를 끄면 두 도구 모음을 완전히 아이콘만으로
  줄일 수 있습니다. 툴팁과 메뉴는 여전히 완전한 이름을 유지하며, 선택은
  기억됩니다.

- **학습 모드가 이제 도구 모음에 항상 표시됩니다.** 교체된 값 패널이
  닫혀 있어도 그 자리에서 바로 켜고 끌 수 있습니다. 도구 모음, 도구
  메뉴, 패널의 기존 체크박스는 항상 같은 상태를 보여 줍니다.

- **비교 돋보기의 "초기화"는 이제 확대 비율만 초기화합니다.** 이
  버튼은 돋보기를 도킹하거나 이동하거나 창 크기를 바꾸지 않고 기본값인
  125퍼센트만 복원합니다. 전체 레이아웃은 계속 "보기 초기화"가 담당합니다.

- **오류와 요청을 이제 도움말 버튼을 통해서도 신고할 수 있습니다.**
  "오류 신고…"와 "기능 제안…"이 기존 도움말 메뉴와 마찬가지로 여기에도
  있습니다. 두 경로 모두 이미 있던 안전한 오류 신고 창이나 공개
  기능 제안 목록을 엽니다.

- **작업 표시줄 메뉴가 더 짧고 명확하게 정리되었습니다.** 전역
  단축키가 있는 두 명령 – 클립보드 정리와 스크린샷 – 이 이제 바로
  위아래로 배치되고 오른쪽에 공통 단축키 열이 있습니다. "마지막
  원본 내용 복원"은 여기서 사라졌습니다 – 더 이해하기 쉬운 복원
  버튼은 메인 창에 계속 남아 있습니다.

- **법률 관련 페이지는 "도움말 → 법적 고지"에서 바로 접근할 수
  있습니다.** 이 하위 메뉴는 maskuro.com의 라이선스 조건, 개인정보
  보호정책, 회사 정보, 이용약관으로 연결됩니다. 청약 철회 안내는
  구매 시 웹사이트에 남아 있습니다.

- **수동으로 마스킹된 PDF는 저장 시 완전히 다시 만들어집니다.**
  페이지와 새로 읽힌 검색 레이어만 그대로 남습니다 – 메타데이터,
  첨부 파일, 책갈피, 주석, 양식 값, 숨겨진 레이어, 검색 색인,
  스크립트, 잘려나간 내용, 그 밖의 객체에 숨겨진 내용은 결과 파일에
  포함되지 않습니다. 이후 글꼴과 벡터 그래픽은 픽셀로 이루어지게
  됩니다 – 이것이 낯선 PDF 객체 트리와의 경계를 증명 가능하게
  긋기 위한 대가입니다.

- **Ctrl+Shift+B는 이제 모든 시스템에서 기본적으로 Maskuro로
  스크린샷을 찍습니다.** Print Screen 키와 그 조합은 별도의 지정으로
  계속 사용할 수 있습니다. 작업 표시줄 아이콘 메뉴에서는 이제 전역
  단축키가 해당 명령 오른쪽에 표시됩니다. 직접 저장한 지정은 그대로
  유지됩니다.

- **편집기는 페이지와 비교 돋보기를 왼쪽에 두고 시작합니다.** 페이지
  패널이 위쪽에, 열려 있는 원본 돋보기가 바로 그 아래에 있으며,
  교체된 값은 오른쪽에 남습니다. 의도적으로 저장한 사용자 지정 배치가
  계속 우선됩니다.

- **실습 문서는 더 이상 메인 창에 계속 남아 있지 않습니다.** 이제
  안내형 실습의 일부이며, 추가로 "도움말"에서도 접근할 수 있습니다.

- **첫 실행은 곧바로 실습으로 이어집니다.** 그림이 포함된 간단
  안내서는 더 이상 내용이 중복되는 두 번째 진입 경로로 제공되지
  않습니다 – 언제든지 "도움말 → 간단 안내서"에서 접근할 수 있습니다.

- **대기 중인 작업 표시줄 아이콘은 완전한 색상을 유지합니다.** 이제
  활성화된 클립보드 모드와 같은 선명한 Maskuro 방패를 보여 줍니다 –
  감시가 실제로 켜져 있을 때만 초록색 점이 추가됩니다.

- **실습 문서는 Maskuro 안에 남아 있습니다.** 시작 버튼은 가상의
  PDF를 만들어 파일 목록에 바로 추가하지만, 더 이상 별도의 PDF
  뷰어를 실행하지 않습니다.

- **후속 수정 창의 검색은 입력 중에도 매끄럽게 유지됩니다.** 검출
  개수 표시 공간은 창을 열 때 이미 확보되며, 처음 표시되는 텍스트는
  더 이상 화면을 바꾸거나 새로운 PDF 래스터화를 유발하지 않습니다.

- **제조사명은 사양 표기 안에서도 그대로 남습니다.** "제조사: TRILUX
  또는 동급품"과 같은 항목은 필요한 물품을 설명하는 것이며, 이제는
  이 표기만으로 회사로 간주되어 마스킹되지 않습니다. 공급업체, 회사,
  제조사 필드는 이에 영향을 받지 않습니다.

- **코퍼스 측정은 과도하게 마스킹된 결과를 오탐으로 집계합니다.**
  Maskuro가 예상한 이름을 제거하면서 동시에 문장의 일부를 함께
  가져가면, 이제 오탐 수가 올라갑니다. 보고서는 이런 침범을 별도로도
  표시합니다 – 따라서 이전의 오탐 수치는 직접 비교할 수 없습니다.

### 수정됨

- **독일어 원본 문서의 기술적·행정적 용어가 이름이나 지명으로 마스킹되는
  일이 줄었습니다.** 차량 사양, 항목 및 합계 줄, 조달 및 개인정보 보호
  용어, 법률 참조, 공개 자료의 파일 이름은 실제 문맥에 맞는 경우에만
  억제됩니다. 텍스트 인식에서 움라우트가 손실된 "Marz 2026"은 여전히
  월(月)로 보호됩니다 – 날짜와 관계없는 "Marz"는 계속해서 실제 이름이나
  지명일 수 있습니다.

- **"원본 되돌리기"는 즉시 필요한 전체 너비를 차지합니다.** 프레임이
  대응된 값의 단어 하나만 걸리는 경우, Maskuro는 대응 관계와 원본 줄을
  바탕으로 프레임을 전체 항목까지 자동으로 확장합니다 – 예를 들어
  "Planungs"에서 "Nordlicht Planungs GmbH"로. 이후 잡을 수 있는 프레임도
  실제로 되돌려진 전체 너비를 보여 줍니다.

- **"원본 되돌리기"는 이제 검은 막대를 명확한 대상으로 표시합니다.**
  마우스를 올리거나 드래그할 때 인식된 막대 전체가 밝은 대비 테두리와
  함께 빨간색으로 강조되며, 옆에 거의 알아볼 수 없는 텍스트 상자만
  표시되는 대신입니다. 이는 막대가 픽셀로만 이루어진 래스터화된
  페이지에도 적용됩니다.

- **편집기 둘러보기는 패널이 닫혀 있던 경우에도 단계를 건너뛰지
  않습니다.** 안내를 위해 Maskuro는 페이지 패널, 비교 돋보기, 교체된
  값을 일시적으로 스스로 열고 정리합니다. "완료" 또는 중단 후에는
  개인 배치가 다시 돌아옵니다. 특정 문서 종류에서 도구를 근본적으로
  사용할 수 없는 경우, 그 설명은 눈에 띄지 않게 사라지는 대신 텍스트
  안내로 유지됩니다.

- **"교체"는 PDF 안전 대체 경로에서도 계속 표시됩니다.** Maskuro가
  남은 문자나 손상된 텍스트 흐름 때문에 페이지를 이미지로 다시
  구성해야 했던 경우, 실제 교체 값은 검색 레이어에만 보이지 않게
  남아 있었고 페이지에는 검은 막대만 있었습니다. 이제 실제로 적용된
  대체 값은 래스터화와 OCR 재구성 전 과정에서 빨간색으로 보이며
  검색 가능한 상태로 유지됩니다.

- **정리된 결과물 위쪽의 안내 문구는 어두운 모양에서도 읽을 수
  있습니다.** 버전 제목, 조작 줄, 소개문은 이제 실제로 표시되는 Qt
  창에서 글자 색을 즉시 가져옵니다.

- **마스킹 프레임이 래스터화된 PDF 페이지에서 다시 텍스트 위에 정확히
  놓입니다.** 보이지 않는 단어 상자는 원본 글꼴에 따라 보이는 글자보다
  좁은 경우가 있었습니다. 그 결과 막대에 틈이 생기거나 마지막 글자가
  읽히는 채로 남았습니다. 이제 상자는 보이는 단어의 너비, 높이, 진행
  방향을 그대로 유지합니다.

- **"새로운 기능"이 다시 맨 위에서 시작됩니다.** 변경 사항 대화상자는
  창 구성이 끝난 뒤 텍스트 커서와 스크롤바를 명시적으로 맨 처음으로
  이동시킵니다 – Qt 버전에 따라 새 기능 중간에서 시작되는 대신입니다.

- **스캔 문자 인식 도중 닫아도 조용히 처리됩니다.** 막 완료되는 OCR
  백그라운드 작업이 이미 닫힌 후속 수정 창으로 더 이상 신호를 보내지
  않습니다.

- **상대적 시간 표현이 더 이상 이름으로 간주되지 않습니다.** "오늘",
  "어제", "내일", "다음 주" 같은 고정 표현을 Maskuro는 이제 해당 문서
  언어의 공식 달력 데이터에서 인식합니다.

- **최초 모델 로딩 중 종료해도 깔끔하게 정리됩니다.** Maskuro나 후속
  수정 창을 연 직후 바로 닫아도, 프로세스 종료 시 네이티브 언어 인식
  작업을 계속 수행 중인 스레드가 남지 않습니다. 이는 종료 시 가끔
  발생하던 충돌 보고서를 막아 주며, 이미 진행 중인 로딩은 정상적으로
  완료됩니다.

- **지연된 시작 대화상자가 종료 후에는 더 이상 나타나지 않습니다.**
  메인 창을 시작 직후 닫아도, 최고 인식 단계, 새로운 기능, 소개에 대한
  질문이 나중에 보이지 않게 또는 뒤늦게 표시되지 않습니다.

- **HTML과 이메일은 줄바꿈 방식을 유지합니다.** Windows에서는 정리와
  되돌리기 이후 HTML 직렬화가 LF와 CRLF를 섞어 사용했습니다. 내용과
  서식은 올바랐지만 파일은 더 이상 바이트 단위로 동일하지 않았습니다.
  HTML 파일과 MIME 메시지는 이제 다시 원본의 줄바꿈 방식을 그대로
  따릅니다.

- **관계사가 포함된 회사 이름은 완전하게 유지됩니다.** 전치사 뒤에서
  Maskuro는 "Gesellschaft für Systemtechnik mbH"나 "Bank für Arbeit und
  Wirtschaft AG" 같은 이름을 "für"라는 단어에서 잘라버렸습니다. 이제
  회사 이름 전체가 인식됩니다 – "Wir sind bei Alpha GmbH versichert"
  같은 실제 문장 서두는 그대로 보입니다.

- **중국어 회사 이름은 법인 형태 앞에서도 완전하게 유지됩니다.** 동사로
  해석될 수 있는 브랜드 구성 요소가 명확한 접미사 "有限公司"가 있음에도
  전체 이름을 무너뜨릴 수 있었습니다. 대소문자 구분이 없는 문자
  체계에서는 이제 공식 법인 형태 표지가 이 불확실한 품사 경계보다
  우선됩니다.

- **PDF 페이지가 불필요하게 이미지로 바뀌었습니다.** 여러 페이지가
  글꼴 목록을 공유하는 다중 페이지 PDF에서 – 흔한 생성 프로그램이
  이렇게 만듭니다 – 첫 페이지 이후의 모든 페이지가 글꼴 참조를
  잃었습니다. 그 결과는 두 가지였습니다: 결과물에서 움라우트를 더
  이상 검색할 수 없었고("Auftragsbestätigung"을 찾을 수 없었습니다),
  이후 검증 과정은 실제로는 페이지에 없던 글자를 놓친 것으로
  판단했습니다 – 그래서 멀쩡한 텍스트 페이지를 이미지로 래스터화해,
  더 이상 검색도 복사도 할 수 없고 용량도 훨씬 커지게 만들었습니다.
  검증 대상 중 17개 페이지 중 4개가 이에 해당했습니다.
- **쉼표 하나만으로는 더 이상 래스터화가 일어나지 않습니다.** 검출
  영역이 단어에서 끝나면, 바로 옆의 문장 부호는 아슬아슬하게 포함되어야
  합니다. 하지만 쉼표나 마침표는 놓친 항목이 아니며, 래스터화는 페이지
  전체를 희생시킵니다. 문자와 숫자는 여전히 다시 정밀 조정해야 할
  이유로 남습니다.

## 0.10.38-alpha.20260824 – 2026년 8월 24일

### 신규

- **법적 형태가 없는 회사 이름도 이제 라벨이 이를 지칭할 경우 인식됩니다.** 「Lieferant:
  Kranzbichler Handels GmbH」는 예전부터 항상 제거되었습니다 - 법적 형태가 회사임을
  드러내기 때문입니다. 「Lieferant: Dehner Märkte」는 그대로 남았지만, 견적서, 입찰서,
  발주서에서는 대개 공급자가 정확히 이런 식으로 표기됩니다. 이는 「Firma:」, 「Hersteller:」,
  「Fabrikat:」, 「Arbeitgeber:」와 그 밖에 여덟 개 언어에서의 대응 표현에도 마찬가지로
  적용되며, 라벨이 자기 줄에만 단독으로 있고 이름이 그 아래 줄에 있는 경우에도
  적용됩니다.

  라벨 뒤에 있는 것이 회사가 *아닌* 경우는 그대로 남습니다: 「Lieferant: siehe Anlage」는
  검게 칠해지지 않습니다 - 그렇지 않으면 그곳에 「Lieferant: [ORGA1]」이 남을 것이고,
  이는 존재한 적 없는 이름을 주장하는 셈이 됩니다. 그만큼이나 자주 사람을 가리키는
  라벨(「Kunde:」, 「Auftraggeber:」)은 의도적으로 여기에 포함되지 않습니다.

- **삽입된 이미지도 이제 편집할 수 있습니다.** 「이미지 정리」 창에는 「결과 복사」 옆에
  *편집기에서 편집* 버튼이 생겼습니다: 이미지를 정리한 뒤 추가로 검게 칠하기, 주석 달기,
  강조하기를 위해 열립니다 - 스크린샷과 같은 경로입니다.

- **라벨 뒤의 번호도 이제 거래 상대방을 지칭하는 경우 발견됩니다.** 지금까지는 고객번호,
  계약번호, 인사번호가 대상이었습니다. 이제는 채권자번호, 채무자번호, 공급자번호,
  오스트리아 고용주번호(Dienstgebernummer), ANKÖ 등록번호, 그리고 제조사의 WEEE,
  EAR, EPR 번호도 독일어와 영어 모두에서 대상이 됩니다. 또한 Maskuro는 이제 콜론
  앞에 공백이 있는 조판된 견적서 머리글 표기법도 이해합니다(「Kunden-Nr : K903944」).
  물품번호, 발주번호, 주문번호, 견적번호, 청구서번호는 계속 그대로 남습니다: 이들은
  사람이 아니라 절차나 물품을 지칭하기 때문입니다. 그래도 제거하고 싶은 사람은
  이를 자신의 검색 패턴으로 등록하면 됩니다.

- **파일 처리에 걸린 시간이 이제 표시됩니다.** 완료 줄에는 인식된 언어 옆에 소요 시간이
  표시되고(「완료 · 독일어 · 2.4초」), 요약에는 전체 작업의 소요 시간이, 통계 패널에는
  합계가 표시됩니다 - 검사 보고서에는 별도의 항목으로 나타납니다. 파일이 여러 개인
  경우 어느 파일이 시간을 잡아먹었는지 그 줄이 알려줍니다.

- **시스템 OCR이 지원하지 않는 문자는 이제 설치된 언어 데이터가 있으면 대체 방식으로
  읽을 수 있습니다.** 지금까지는 다음과 같았습니다: 시스템 자체의 텍스트 인식이
  어떤 문자를 다루지 못하면(Mac에서는 예를 들어 데바나가리) 결과에는 「이미지가
  【검사되지 않았습니다】」라고 표시되었고, 이미지 속 정보는 그대로 남았습니다. 이제는
  알맞은 언어 데이터가 있으면 함께 제공되는 텍스트 인식이 대신 나섭니다. 이렇게 읽힌
  이미지는 정식으로 검사된 것보다 불확실하므로, 결과에는 이렇게 표시됩니다: 「대체
  방식으로 읽음 - 확인해 주세요」. 힌디어 테스트의 과거 중간 단계 기준으로 측정한
  결과: **정보 열 개를 더 찾아냈고 오탐 네 개가 줄었습니다**(64% → 73%). 현재의
  최종 값은 위쪽에 있으며 이와 혼동하면 안 됩니다.

- **텍스트 인식이 이제 올바른 언어를 묻습니다.** 독일어와 영어를 제외한 모든 문서
  언어에서는 지금까지 알맞은 언어 데이터가 있어도 영어 인식 모델이 사용되었습니다.
  Windows에서는 모든 언어가 이에 해당했습니다 - 그리스어, 일본어, 힌디어가 그곳에서는
  영어 모델로 읽혔습니다.

- **정말 처음 시작할 때 나타나는 설정 마법사.** (Maskuro를 이미 사용해 본 사람에게는
  나타나지 않습니다 - 「첫 시작」은 정말 첫 시작을 뜻하지, 이번 업데이트 뒤의 첫 시작을
  뜻하지 않습니다.) 여섯 장의 화면 대신 세 가지 질문: 문서의 언어, 이미지 속 텍스트도
  함께 읽을지 여부, 그리고 일상에서 Maskuro에 어떻게 접근하고 싶은지. 끝에는 세 가지
  경로가 계속 남습니다 - 연습 문서, 둘러보기, 또는 그림이 있는 간단 안내서. 모두
  건너뛸 수 있으며, 「도움말 → 설정 다시 진행」으로 다시 불러올 수 있습니다.

- **F1을 누르면 해당 장이 펼쳐진 채로 매뉴얼이 열립니다.** 메인 창, 설정(페이지에 따라
  다름), 검토 창, 언어 관리에서 그렇습니다. 나중에 손보기 창에서는 Umschalt+F1을
  누릅니다. F1이 그곳에서는 예전부터 단축키를 보여주기 때문입니다. 지금까지 도움말은
  25개 장 중 항상 맨 위에서 시작했습니다.

- **새로운 매뉴얼 첫 장: 「3분 만에 시작하기」.** 문서 하나를 처리하는 데 필요한 것은
  네 단계뿐입니다 - 18개 언어판 전부에 있습니다.

- **창을 안내하는 둘러보기.** 「도움말 → 창 둘러보기」는 조작 요소를 하나씩 스포트라이트로
  비추고 그 옆에 문장 하나를 씁니다 - 메인 창에서는 8단계, 나중에 손보기 창에서는
  7단계입니다. 그림이 있는 간단 안내서와 달리, 지금 앉아 있는 바로 그 창을 설명합니다.
  언제든 Esc로 중단할 수 있습니다.

- **위험 없이 시험해 볼 수 있는 연습 문서.** 작업 영역 아래에 이제 「연습 문서 열기」가
  있습니다(도움말 메뉴에도 있습니다). 이는 지어낸 문서 한 장을 만듭니다 - 이름, 주소,
  전화번호, IBAN, 사회보험번호 - 그리고 그 문서 자체에 무엇을 할 수 있는지, 그 뒤
  무엇을 보게 될지가 함께 적혀 있습니다. 그 안의 어떤 것도 실제 사람의 것이 아닙니다.
  Maskuro로 보내는 첫 문서가 실제 문서일 필요는 없습니다.

- **「그냥 확인만 …」이 이제 「정리」 옆에 있습니다.** 이는 아무것도 바꾸거나 쓰지 않은
  채로 개인정보가 어디에 있는지 - 파일, 종류, 개수 - 를 보여줍니다. 문서를 올려둔
  사람은 정리하기 전에 먼저 확인할 수 있습니다. 지금까지 이 경로는 파일 메뉴의
  「폴더 훑어보기 …」 아래에만 있었고, 올려둔 파일이 아니라 폴더 전체를 대상으로
  했습니다.

- **아무것도 찾지 못한 경우 이제 그 이유가 될 만한 것이 함께 표시됩니다.** 예를 들어:
  파일 안에 이미지가 있지만 「이미지 속 텍스트도 검사」가 꺼져 있는 경우. 또는: 설정된
  언어가 문서의 언어와 맞지 않는 경우. 그리고 그중 어느 것도 해당하지 않으면 Maskuro는
  그 사실도 알립니다.

- **나중에 손보기 창은 처음에는 세 문장으로 인사합니다:** 클릭하면 단어가 검게 칠해지고,
  드래그하면 영역이 검게 칠해지며, 오른쪽에는 대체된 값이 표시됩니다. 「확인했습니다」를
  누르면 이 안내는 영구히 사라지고, 「도움말 → 소개 다시 보기」로 다시 불러올 수
  있습니다.

- **스캔된 페이지에서도 이제 단어를 클릭할 수 있습니다.** 지금까지는 PDF가 텍스트
  레이어를 가지고 있는 곳에서만 단어를 클릭할 수 있었습니다 - 스캔에서는 되지 않았고,
  같은 문서 안에서도 페이지마다 다를 수 있었습니다. 이런 페이지는 이제 한 번만
  텍스트 인식으로 읽힙니다. 그 뒤로는 다른 곳과 마찬가지로 단어를 클릭할 수
  있습니다. 상태 표시줄이 지금 무슨 일이 일어나고 있는지 알려줍니다.

- **페이지 패널이 다시 하나의 온전한 면이 되었습니다.** 열의 중간에서 끊겨 있었습니다:
  제목 표시줄이 잘려 있고, 옆에는 다른 색의 띠가 있고, 현재 페이지는 번호 뒤의 색이
  있는 상자로만 알아볼 수 있었습니다. 이제는 자신의 열을 가득 채우고, 더 넓게 늘릴
  수 있으며, 현재 페이지는 하나의 온전한 타일로 강조되고, 그 안에는 있는 그대로의
  페이지 미리보기가 들어 있습니다.

- **대체된 자리는 옅은 노란색으로 빛납니다.** 페이지 보기에서 무엇이 대체되었는지
  한눈에 볼 수 있게 되었습니다 - 원본 위의 비교 돋보기가 사용하는 것과 같은 색입니다.
  마우스를 가져갔을 때의 빨간 테두리는 변함없습니다.

- **나중에 손보기 창의 「보기 초기화」**(「보기」 메뉴). 페이지 패널이나 트레퍼 목록을
  옮기거나 떼어내거나 닫은 사람은 이를 통해 모든 것을 처음 시작했을 때의 자리로
  되돌릴 수 있습니다.

### 변경됨

- **자리표시자가 더 짧아졌습니다.** `[SOZIALVERSICHERUNGSNR_1]`은
  `[SVNR1]`이 되고, `[ORGANISATION_1]`은 `[ORGA1]`이, `[EMAIL_1]`은
  `[MAIL1]`이 됩니다. 이유는 미관이 아닙니다: 대체하는 값보다 긴 자리표시자는
  줄을 벌려놓고 좁은 표 칸에서는 아예 들어갈 자리가 없습니다 - 지금까지는
  그 자리에 검은 막대가 남았고, 그것으로는 그 자리에 무언가 있었다는 사실조차
  더 이상 아무에게도 전해지지 않았습니다. 흔히 쓰이는 약어가 있는 곳에는 그것이
  쓰입니다(`[BLZ1]`, `[KFZ1]`, `[IBAN1]`). 이전 실행 결과는 계속 사용할 수
  있습니다: 예전 표기법도 계속 인식되며, 어제 만든 대응 파일도 변함없이
  작동합니다.

- **프로그램 아이콘이 이제 어디서나 똑같이 표시됩니다.** Mac 메뉴 막대에는
  지금까지 시스템 자체가 검게 또는 희게 칠하는 단색 방패가, Windows 작업
  표시줄에는 녹색 또는 회색 방패가 나타났습니다. 이제는 모든 막대에 같은
  파란색 Maskuro 방패가 표시됩니다. 클립보드가 감시되고 있는지 알아보는
  방법은 똑같이 명확하게 유지됩니다: 감시가 실행 중이면 방패에 초록색 점이
  붙고, 쉬고 있으면 같은 방패가 흐릿하게 표시됩니다. 가장 작은 크기에서도
  이제 두 개의 검은 막대 모두 방패 안에 표시됩니다 - 지금까지 작업 표시줄은
  그곳에 하나만 보여주었습니다.

- **얼굴은 이제 훈련 이미지가 동의를 받아 만들어진 모델로 인식됩니다.** 이제
  MediaPipe BlazeFace(Apache-2.0)가 함께 제공됩니다. 이전 감지기는 내장된
  채 전환 가능한 상태로 남지만, 그 훈련 출처가 최종적으로 명확하지 않기 때문에
  더 이상 함께 제공되지 않습니다. 인식 자체는 달라지지 않습니다: 얼굴 324장과
  얼굴 없는 이미지 143장을 대상으로 새 버전은 같은 수준의 정확도와 같은 수준의
  적은 오탐으로 3분의 1의 시간만 필요로 합니다.

- **OCR은 가장 강력한 PDF 보장의 안전핀입니다.** 일반 PDF 실행은 OCR을 사용해
  완전한 최소 구조를 만듭니다. OCR을 명시적으로 끄는 사람은 더 호환성이 높은
  객체 경로를 얻습니다. 화면, 완료 메시지, 매뉴얼은 이제 이 경로가 알려지지
  않은 숨은 PDF 채널에 대해 같은 수준의 방어 구조를 제공하지 않는다는 점을
  명시적으로 알립니다.

- **판매 게이트는 이제 지금까지 동봉되었던 YuNet 모델도 막습니다.** 정확한
  가중치의 MIT 라이선스는 문서로 남아 있지만, WIDER FACE를 거친 공개적으로
  드러난 훈련 데이터 체인은 보수적인 제품 출시 기준으로는 충분하지 않습니다.
  판매 전에는 서면 확인이나, 입증 가능한 상업적 데이터·가중치 체인을 갖춘
  모델로의 교체가 필요합니다.

- **회사명과 단체명이 이제 자동으로 제거됩니다.** 지금까지는 명시적으로
  요청하지 않는 한 그대로 남았습니다. 이는 비즈니스 서신에는 잘못된 기본값이었습니다:
  견적서를 넘기는 사람은 그 안에서 발주처를 읽고 싶어하지 않습니다. 「Kranzbichler
  Handels GmbH」, 「Institut für Bauphysik」 등은 그래서 이름처럼 다루어집니다.
  다르게 필요한 사람은 창에서 이를 끌 수 있습니다. 명령줄에서 이 스위치는
  이제 `--ohne-organisationen`이라고 불립니다. 예전 `--mit-organisationen`은
  기존 스크립트와 바로가기가 깨지지 않도록 계속 받아들여지지만 더 이상 아무 일도
  하지 않습니다. 날짜와 금액 정보는 변함없이 제외됩니다.

- **검게 칠하기가 이제 두 개의 체크박스 대신 세 가지 형태를 가집니다.** 「단어」,
  「전체 줄」, 「자유 틀」이 나란히 하나의 선택지로 서 있습니다 - 항상 정확히
  하나만 적용됩니다. 지금까지 「텍스트 줄」과 「전체 줄」은 둘 다 눌려 있을 수
  있는 독립된 두 개의 스위치였고, 자유 틀은 아예 버튼이 아니라 첫 번째 것의
  꺼진 상태였습니다. 이 세 가지는 이제 자신의 도구 옆에 눈에 보이게 서 있으며,
  다른 도구가 선택되어 있는 동안에는 회색으로 표시됩니다.

### 개선됨

- **첫 문서가 약 1초 더 빨리 완료됩니다.** 정리가 시작되기 전에 Maskuro는
  문서의 언어를 확인합니다 - 지금까지는 이를 위해 48개 언어 전체의 단어
  목록을, 단어보다 훨씬 많은 것을 불러오는 경로로 가져왔습니다. 이는 첫
  결과까지의 대기 시간 중 약 절반을 차지했습니다. 인식 자체는 변함없습니다:
  이전과 같은 단어를 보되, 더 빠를 뿐입니다. 그 이후의 모든 문서는 애초에
  영향을 받지 않았습니다.

- **매우 긴 문단을 가진 문서는 더 빠르게 검사됩니다.** 줄바꿈이 없는 문단의
  경우 Maskuro는 발견된 자리마다 문단 전체를 다시 읽었습니다. 이제는 한
  번이면 충분합니다. 문단이 길수록 그 차이는 더 커집니다 - 측정 결과 약
  7분의 1의 계산 시간이 줄었습니다. 결과에는 아무 변화가 없습니다.

### 수정됨

- **회사와 함께 문장 절반이 사라지는 경우가 잦았습니다.** 본문 안에 회사 이름이
  있는 경우 - 「Information über die Gottwald GmbH & Co KG」, 「… (AGB) der
  Musterbetriebe GmbH」 - 이름만 검게 칠해지는 것이 아니라 문장 시작 부분까지
  모든 것이 검게 칠해졌습니다. 그로 인해 텍스트를 읽을 수 없게 되었고, 마치
  아무렇게나 검게 칠한 것처럼 보였습니다. 자기 안에 「für」나 「und」를 포함하는
  회사 이름(「Bank für Arbeit und Wirtschaft AG」)은 변함없이 온전하게 남습니다.

- **텍스트에서는 제거되었는데도 편지머리에는 회사 이름이 그대로 남았습니다.**
  어느 견적서에서는 편지머리 이미지 안에 회사 소재지가 여전히 읽을 수 있게
  남아 있었습니다 - Maskuro가 본문에서 검게 칠한 것과 같은 곳입니다. 검색
  가능한 결과 텍스트 안에는 심지어 보이지 않는 채로 계속 남아 있었습니다.
  한 번 제거된 것은 이제 이미지로만 존재하는 곳에서도 함께 제거됩니다. 이는
  그래픽으로 그려진 로고와 워드마크에도 적용됩니다.

- **macOS는 승인이 이미 오래전에 이루어졌는데도 시작할 때마다 화면 녹화
  권한을 물었습니다.** 시작 시의 안내가 녹화를 시험해 보았고, 바로 그것이
  시스템 대화상자를 화면에 불러왔습니다. 이제는 시작 시 Maskuro 자신만
  묻고, 그것도 한 번뿐입니다. 시스템은 실제로 스크린샷을 찍을 때 비로소
  묻습니다.

- **기술적인 전문 용어가 지명과 회사로 간주되었습니다.** 「Einspeisepunkt」,
  「Flachdach」, 「Verteileranlage」, 「Meldersockel」와 비슷한 수십 개의
  단어가 견적서와 시방서에서 사라졌습니다. Maskuro는 이제 이들의 기본 어간으로
  이들을 인식합니다: 「-anlage」, 「-punkt」, 「-kanal」로 끝나는 것은 사물입니다.
  Berlin, Melk, Wieselburg 같은 지명은 그런 기본 어간이 없어 손대지 않은
  채로 남습니다 - 「Der Graben」이나 「Alter Markt」 같은 주소도 마찬가지입니다.

- **일본어, 한국어, 중국어, 태국어, 구자라트어 문서가 프로그램을 멈추게 할 수
  있었습니다.** 이 다섯 언어 중 하나로 된 문서에 「https://」가 앞에 붙지 않은
  인터넷 주소가 있으면 정리 작업이 내부 오류로 중단되었습니다 - 창이 열려
  있던 경우 나머지 작업 내용도 함께 사라졌습니다. 선택 가능한 마흔여덟 개
  문서 언어 모두 이제 문제없이 처리됩니다. 어떤 언어의 빈도 사전이 없으면
  해당 정보는 사라지는 대신 의심스러운 경우 그대로 남습니다.

- **필드 라벨은 독일어와 영어에서만 보호 기능을 했습니다.** 「Reference」는
  그대로 남았지만, 이탈리아어 「Riferimento」와 포르투갈어 「Referência」는
  지명 정보로 간주되어 제거되었습니다 - 같은 필드 이름, 같은 줄인데도 결과가
  달랐습니다. 영어로 작업하지 않는 사람은 이로 인해 불리했습니다. Maskuro는
  이제 관리되는 11개 언어 모두에서 같은 필드 이름을 인식합니다.

- **「원본 되돌리기」가 스캔한 페이지에서 너무 많이 되돌렸습니다.** 주소 블록의
  검게 칠한 한 줄 위의 사각형이 **블록 전체**를 다시 공개했습니다 - 그리고
  페이지는 찢긴 채로 남았습니다: 막대의 잔해가 남아 있었고, 그 사이로 낱말의
  끝부분이 튀어나왔습니다. 원인은 래스터화된 페이지에서 위아래로 붙어 있는
  막대들이 서로 맞닿아 하나의 면으로 간주되었기 때문입니다. 이제는 사각형이
  가리키는 바로 그 줄만 되돌려집니다. 이웃 줄은 계속 검게 칠해진 채로 남고,
  대상이 된 줄의 막대는 완전히 사라집니다.

- **위치 목록의 수량 정보가 주소로 간주되었습니다.** 「1.4  Kabelgraben
  100,00  m」 같은 줄에서 「Kabelgraben 100」이 번지수가 있는 거리로
  대체되었습니다. 이런 줄은 이제 그대로 남습니다. 「Hauptplatz 1, 3250
  Wieselburg」 같은 진짜 주소는 변함없이 인식됩니다.

- **회사 이름 앞에서 문장 절반이 사라졌습니다.** 「Vertrag zwischen der
  Firma Gottwald GmbH & Co KG und dem Auftraggeber.」에서
  「[ORGANISATION_1] und dem Auftraggeber.」가 되었습니다 - 문장 시작
  부분이 사라졌고, 무엇에 관한 것인지에 대한 단서도 함께 사라졌습니다.
  이제는 회사 이름 자체만 사라집니다. 총칭이 이름의 일부인 경우(「Deutsche
  Bank AG」, 「Universität Wien」)는 지금까지와 똑같이 유지됩니다.

- **회의록에서 이름이 동시에 직업명이기도 한 발언자가 그대로 남았습니다.**
  「Bauer:」, 「Koch:」, 「Weber:」는 발언 앞에서 간과되었지만, 옆에 있는
  「Gruber:」는 그렇지 않았습니다 - Maskuro는 지금까지 문서 안에 인식된
  이름이 최소 하나 있어야 그 줄들을 발언으로 읽을 수 있었습니다. 이제는
  문서에 「Ergebnisprotokoll」이나 「Niederschrift」 같은 제목이 있으면
  그것으로 충분합니다. 「Achtung: …」, 「Hinweis: …」 같은 메모 줄은
  손대지 않은 채로 남습니다.

- **필드 라벨이 값과 함께 사라졌습니다.** 「Projekt: Sanierung und
  Erweiterung Gemeindezentrum」이 하나의 자리표시자가 되었습니다 -
  「Projekt:」라는 단어까지 사라졌고, 그 자리에 무엇이 있었는지에 대한
  단서도 함께 사라졌습니다. 라벨은 이제 그대로 남습니다. 라벨이 정보의
  일부이며 그 의미를 담고 있는 경우(「Durchwahl 214」)는 달라지지 않습니다.

- **최대 인식 단계가 전문 용어를 정리하지 못했습니다.** 「Flachdach」,
  「Einspeisepunkt」, 「Elektrotechnik」와 비슷한 전문 용어가 AI 단계를
  켜도 지명이나 회사로 대체되었습니다 - AI는 정확히 이런 결과를 판단
  대상으로 받아본 적이 없었습니다. 이제는 이것도 함께 검사합니다: 입찰과
  계약 텍스트로 이루어진 코퍼스에서 27건의 오탐이 모두 사라지고, 실제
  정보는 하나도 남지 않습니다. 이름, 회사, 지명은 변함없이 인식됩니다.

- **시설 유형을 나타내는 총칭이 단체로 간주되었습니다.** 어느 계약 텍스트에서
  「Hochschulen und Universitäten」, 「Staatliche und private Schulen」,
  「Akademische Lehrkrankenhäuser」, 「Bildungseinrichtung」, 「Zulieferfirmen」이
  사라졌습니다 - 특정 기관이 아니라 기관의 종류를 가리키는 단어들입니다. 이제
  이들은 그대로 남습니다. 앞에 고유명사가 있으면(「EU-Kommission」) 계속
  대체되며, 회사 이름은 애초에 이 규칙의 대상이 아닙니다.

- **명단의 이름은 흔한 경우에만 사라졌습니다.** 「Name」이라는 열 제목 아래
  있는 참가자 또는 출석 명단에서 「Anna Huber」와 「Thomas Müller」는
  제거되었지만, 「Wójcik Aleksandra」나 「Kücükgöl Sinan」은 그렇지
  않았습니다 - 같은 줄, 같은 구성인데도 말입니다. 더 드문 이름을 가진 사람은
  이 때문에 보호를 덜 받았습니다. 이제는 열 제목이 결정합니다: 「Name」
  아래에 있는 것은 이름입니다. 사무적인 열 제목을 가진 위치 목록은 손대지
  않은 채로 남습니다.

- **「Durchwahl」 뒤의 전화번호가 중간에서 잘렸습니다.** 「Durchwahl 0732
  771190」이 「[DURCHWAHL_1] 771190」이 되었습니다 - 번호의 뒷부분이
  읽을 수 있게 남았습니다. 이제는 번호 전체가 사라지고, 라벨은 그대로
  남습니다. 실제 내선번호(「Durchwahl 214」)는 라벨을 포함해 변함없이
  대체됩니다.

- **일부 PDF는 아예 정리할 수 없었습니다.** 이미지 안의 색상 프로필이나
  메타데이터를 확실하게 제거할 수 없는 경우 작업이 결과 없이 중단되었습니다 -
  약관 페이지, 시방서, 입찰 서류 같은 일반적인 업무 문서가 이에 해당했습니다.
  이런 파일은 이제 정리되며, 경고문이 남은 부분을 알려줍니다: 기기, 제작자,
  또는 촬영 식별자가 있을 수 있습니다. 원본은 언제나처럼 변경되지 않습니다.

- **계약상의 역할이 사람으로 간주되었습니다.** 「Bieter」, 「Verbraucher」,
  「Mieter」, 「Käufer」, 「Auftraggebers」와 그 밖에 약 마흔 개의 역할
  단어가 관사 없이 쓰인 경우 대체되었습니다 - 계약서 제목, 표의 열,
  서명란에서 말입니다. 사람 정보가 하나도 없는 계약 텍스트가 이 때문에
  군데군데 읽을 수 없게 되었습니다. 이 단어들은 이제 그대로 남습니다.
  옆에 사람을 가리키는 단서 - 호칭, 이름, 「Ansprechpartner」 같은 필드
  단어 - 가 있으면 계속 대체됩니다: 「Herr Bieter」와 「Frau Käufer」는
  이름입니다. 직업명이기도 한 흔한 성씨(Bauer, Richter, Koch)는 이
  규칙의 대상이 애초에 아닙니다.

- **번지수가 마침표에 바로 붙어 있으면 줄여 쓴 거리명이 간과되었습니다.**
  「Schlesischestr.31」은 주소로 인정되지 않았습니다 - 그리고 옆의
  우편번호는 주소 발견 결과에서 지지를 얻기 때문에 그것도 그대로
  남았습니다. 결과물에서는 거리와 우편번호로 이루어진 주소를 다시
  조합할 수 있었으며, 이는 같은 문서의 일부 페이지에서만 그랬습니다.
  이제 둘 다 함께 사라집니다. 숫자가 붙은 사물 명칭(「Kabelrinne200」)은
  손대지 않은 채로 남습니다.

- **두 줄에 걸친 주소가 하나의 자리표시자로 합쳐졌습니다.** 주소 블록에서
  우편번호가 거리 위에 있는 경우, Maskuro는 두 줄을 하나의 발견 지점으로
  묶었습니다: 결과물에서 줄바꿈이 사라지고, 우편번호는 그 앞에 읽을 수
  있게 남았습니다. 이제 각 줄은 각자 따로 발견되고 대체되며, 글자 배치가
  유지됩니다. 같은 원인이 때때로 그 위 줄의 성까지 주소 안으로 끌고
  들어갔습니다.

- **최대 PDF 경로는 더 이상 원본 객체를 넘겨받지 않습니다.** 텍스트 인식이
  켜진 상태에서 Maskuro는 눈에 보이는 PDFium 이미지로부터 모든 페이지를
  완전히 새로 만듭니다. 새 최소 파일에는 이 이미지 페이지와, OCR 텍스트에
  한정되어 새로 만들어진 검색 레이어만 들어갑니다 - 주석, 첨부파일, 액션,
  레이어, 메타데이터, 색상 프로필, 개인 키가 담긴 낯선 객체 트리는
  들어가지 않습니다. 이는 주석 외관, 패턴, Type-3 글꼴, 폼 객체, 소프트
  마스크 안의 내용에도 적용됩니다. 원본 파일은 손대지 않은 채로 남습니다.

- **중첩된 PDF 그래픽 안의 얼굴과 코드가 간과되었습니다.** 두 감지기 모두
  이제 완전히 렌더링된 페이지 이미지도 함께 봅니다. 그 덕분에 주석, 패턴,
  Type-3 글리프, 투명 마스크 안의 초상과 QR/바코드도 감지기에 도달합니다.
  인식된 영역은 - 켜져 있는 경우 - 최소 구조 재구성 전에 알아볼 수 없게
  처리됩니다. 감지 자체는 여전히 실수할 수 있습니다.

- **OCR 엔진이 없으면 PDF에서 내부 오류로 끝났습니다.** 최대 실행은 이제
  불완전하거나 검사되지 않은 파일을 내놓는 대신, 통제된 방식으로 대상
  파일 없이 중단됩니다.

- **여러 실제 연락처 및 업무 값이 사무적 텍스트를 대체하는 동안
  누락되었습니다.** 줄바꿈에 걸친 이름 필드, 은행명과 회사명, 법적 형태,
  라벨이 붙은 식별번호, 생년월일, 그리고 전화번호·URL·IBAN의 경계가 더
  엄격하게 검사됩니다. 동시에 사무적 텍스트 안의 국가명, 역할·총칭 단어,
  물품·규격 코드, 숫자 열, 흔한 약어는 더 자주 손대지 않은 채로 남습니다.

- **혼합되고 회전된 OCR 줄이 잘못 읽혔습니다.** 불확실한 세로 단어는 이제
  현지에서 바로 세워 다시 읽습니다. 비라틴 텍스트 안의 기술적인 라틴 문자
  값은 독립적인 영어 검증을 거칩니다. 홀로 떨어진 불확실한 개별 숫자는
  가까운 두 개의 숫자 열이 일치할 때만 수정됩니다. 「sp. z 0.0.」 형태의
  OCR로 읽힌 폴란드 법적 형태는 닫힌 문맥에서 「sp. z o.o.」로 읽힙니다.

- **이미지 측정이 부분적으로 보이는 잔여 값을 놓칠 수 있었습니다.** 이제
  겹치는 국소 구간을 검사하고, 검은 막대 위의 흰색 자리표시자 글자와
  원본 글리프를 구분하며, 회전되고 다시 렌더링된 최소 PDF에도 원본
  이미지 상자를 옮겨 적용합니다. 고정된 합성 메인 코퍼스는 이로써
  1,392/1,392개의 목표 정보를 제거하며, 오탐 0건, 처리 오류 0건을
  기록합니다. 이는 코퍼스 기준의 증명이지, 일반적인 100% 보장이
  아닙니다.

- **비상업용 언어 모델은 더 이상 제공되지 않습니다.** CC BY-NC-SA 3.0
  라이선스의 이탈리아어와 그리스어 spaCy 변형 여섯 개가 카탈로그,
  다운로드, 로드 경로에서 제거되었습니다. 이미 있는 모델 폴더도 무시됩니다.
  두 언어 모두 대신 MIT 라이선스의 다국어 모델을 사용합니다.

- **「Ansprechpartner」 아래의 이름이 절반만 제거되었습니다.** 라벨이 한
  줄에 단독으로 있고 그 아래 「성 이름」이 있는 경우, 이름이 동시에 일반
  단어이기도 하면 그대로 남았습니다 - 「Mayer Roman」에서 「[NAME_1]
  Roman」이 되었습니다. 이런 줄은 이제 통째로 처리됩니다. 같은 자리의
  부서명(「Technischer Innendienst」)은 계속 손대지 않은 채로 남습니다.
  덧붙여 수정된 것: 「Kontaktperson」은 예전부터 이름 필드로 취급되었지만
  「Ansprechpartner」는 전혀 그렇지 않았습니다.

- **법적 형태가 없는 회사 이름이 그 사이에 업종 단어가 있으면 그대로
  남았습니다.** 「Kranzbichler Handels GmbH」는 제거되었지만, 세 문단
  뒤의 벌거벗은 「Kranzbichler」는 그렇지 않았습니다 - 반면 「Kranzbichler
  GmbH」는 제거되었습니다. 이제는 둘 다 적용됩니다. 일반 단어는 여기서
  제외됩니다: 「Deutsche Bank AG」는 텍스트 안의 「deutsche」를 회사로
  만들지 않습니다.

- **같은 값이 같은 문서 안에서 한 번은 이름, 한 번은 지명으로 불렸습니다.**
  「Anna Musterfrau … Musterfrau」는 「[NAME_1]」과 「[ORT_1]」이 되었습니다 –
  두 번째 자리에는 이름이 빠져 있고, 이것이 없으면 지명이 되었습니다.
  둘 다 제거되었지만 마치 서로 다른 두 가지처럼 읽혔습니다. 이제 값은 첫
  등장 시의 분류를 유지합니다.

- **날짜 정보가 더 이상 제거되지 않았습니다.** 숫자로만 이루어진 날짜
  (「01.03.2026」)는 지난 버전 이후 이름을 위해 만들어진 검사에 걸려
  문서에 그대로 남았습니다 - 「이동」 작동 방식에서도, 검사 보고서에
  줄 하나 없이도 그랬습니다. 날짜 정보를 명시적으로 켠 사람만 영향을
  받았습니다.

- **국가와 대륙은 더 이상 검게 칠해지지 않습니다.** 「배송은 미국으로
  갑니다」, 「아시아의 시장 약세」, 「이 규격은 루마니아에 적용됩니다」 -
  이런 정보는 사람에 대해 아무것도 말하지 않으며 이제 그대로 남습니다.
  반대로 국가명이 주소의 일부이거나 「Wohnsitz」나 「Geburtsort」 같은
  라벨 뒤에 있으면 계속 제거됩니다. **도시는 영향을 받지 않습니다** -
  「저는 지금 빌바오에 있습니다」는 여전히 사람에 대한 정보이며 계속
  검게 칠해집니다.

- **줄임말이 웹 주소가 되었습니다.** 텍스트에 「bzw. deutsche」나
  「incl. der」가 있는 경우, 일부 PDF는 마침표를 공백 없이 내놓습니다 -
  이로써 「bzw.de」와 「incl.de」가 만들어졌고, 이는 국가 도메인이 붙은
  유효한 주소로 간주되어 제거되었습니다. 이런 단어 쌍은 이제 그대로
  남습니다. 진짜 주소는 「www.」 없이도 영향을 받지 않습니다.

- **재무제표의 숫자 열이 전화번호로 검게 칠해졌습니다.** 사업 보고서와
  가격표에서는 전년도와 당해연도가 나란히 있습니다 - 「64.518  65.133」.
  이는 하나의 전화번호로 간주되어 제거되었고, 「12200-23200」 같은 숫자
  구간과 뒤에 숫자가 붙은 날짜도 마찬가지였습니다. 이런 숫자는 이제
  그대로 남습니다. 반대로 실제 전화번호는 더 안전하게 인식됩니다:
  「Telefon」, 「Fax」, 「Mobil」, 「Durchwahl」와 다른 화면 언어에서의
  대응 표현이 이제 함께 인식됩니다 - 지금까지 프로그램은 그곳에서 영어
  단어만 인식했습니다.

- **번호가 매겨진 표의 이름이 그대로 남았습니다.** 일반적인 형태의 참가자
  명단이나 인사 표 - 열 제목 아래 「1.1 Auersperg Bernhard Montage
  03.03.2026」 - 는 전혀 정리되지 않았습니다: 이런 줄은 사무적 용어가
  그대로 남아야 하는 견적서의 위치 목록처럼 보였습니다. 열 제목에 사람을
  가리키는 라벨(「Name」, 「Nachname」, 「Surname」 …)이 있으면 그 아래
  줄들은 이제 이름으로 간주됩니다. 위치 목록은 편지머리에
  「Sachbearbeiter:」가 있어도 변함없이 보호됩니다.

- **하나의 이름에서 때때로 자리표시자 두 개가 나란히 생겼습니다.** 성이
  문서 안에 단독으로도 있는 경우, 후처리 과정은 「Anna Musterfrau
  GmbH」 같은 자리에서 먼저 성을, 그다음 이름을 대체했습니다 - 결과물에서는
  마치 서로 다른 두 사람처럼 보였습니다. 이제는 알려진 이름 중 가장 긴
  것이 우선합니다.

- **꾸며낸 값이 어떤 대응 파일에도 없었습니다.** 「값 꾸며내기」를 선택한
  사람은 「Anna Musterfrau」가 「Greta Mayrhofer」가 된 결과물을
  받았습니다 - 같은 문서에 익명 대체가 하나라도 있으면 대응 파일에는
  이에 대해 아무것도 없었습니다. 그러면 꾸며낸 값을 되돌릴 수 없었고,
  대응 파일은 이 대체를 숨겼습니다. 가장 위험한 것은 세 번째였습니다:
  결과물을 읽는 사람은 그럴듯한 이름을 보고, 그것이 꾸며낸 것이라는
  단서가 전혀 없습니다. 이제는 모든 대체가 대응 파일에 기록됩니다.

- **대응 파일은 검게 칠한 것을 「대체됨」이라고 표시했습니다.** 이메일은
  첨부파일과 대응 파일을 공유하며, 첨부파일은 검게 칠해질 수 있는 반면
  메일 본문은 자리표시자를 가질 수 있습니다. 대응 파일에는 세 자리
  모두에 같은 말 - 「대체됨」 - 이 적혀 있었고, 되돌리기는 첨부파일
  안에서 거기 없는 자리표시자를 찾았습니다: 막대는 그대로 남았습니다.
  이제는 각 발견 지점마다 실제로 무슨 일이 일어났는지가 기록되며, 두
  첨부파일 모두 돌아옵니다.

- **이미지 안에만 있던 값은 되돌릴 수 없었습니다.** 트레퍼 패널에는 이것이
  두 번 나타났습니다 - 한 번은 문서 어디에도 없는 자리표시자로(「이
  자리표시자는 문서에서 발견되지 않았습니다」), 한 번은 검게 칠한
  자리로 말입니다. 첫 번째 줄은 순전히 장부상의 것이었고, 이제 사라졌습니다.

- **검게 칠한 값은 한 번만 되돌릴 수 있었습니다.** 같은 값이 여러 자리에
  있으면 클릭 한 번으로 모두 되돌려지지만, 나머지 줄은 트레퍼 패널에
  그대로 남아 있었고, 그다음 클릭은 「명확하지 않음」이라고 알렸습니다.
  이제는 그것들도 함께 사라집니다.

- **학습 모드가 꺼져 있으면 검사 기록에 되돌리기가 빠졌습니다.** 나중에
  손보기 창에서 되돌린 값을 복원한 사람은 학습 질문이 꺼져 있으면 검사
  기록에서 그 작업을 다시 찾을 수 없었습니다 - 증거가 규칙 제안만을
  의미하는 스위치에 매여 있었습니다. 검사 기록이 켜져 있으면 이제
  그것과 무관하게 이유를 묻고 줄이 기록됩니다.

- **끌어다 넣은 파일은 정리되지 않았고 - 알려지지도 않았습니다.** 파일을
  첨부파일로 보내는 대신 문서 안으로 끌어다 넣으면 Word나 PowerPoint는
  이를 문서 안에 완전한 형태로 저장합니다. 그것은 이후에도 원래 파일
  이름과 저장 경로까지 그대로 결과물에 남아 있었습니다 - 그리고 이들은
  실제로 그 자체로 이름을 담고 있는 경우가 많습니다. 이런 파일은 이제
  나머지 문서와 마찬가지로 정리됩니다.

- **그리고 그것이 안 되는 경우 Maskuro가 이를 알립니다.** 정리 방법이
  없는 오래된 형식(Word 97, Excel 97)이 삽입된 객체 안에 있으면 이제
  파일 이름이 담긴 주의 메시지가 나타납니다. 지금까지는 조용히 변경
  없이 그대로 전달되었습니다.

- **찢어진 단어와 약어가 이름으로 간주되었습니다.** PDF에서 단어가 줄
  끝에서 나뉘어 있으면 일부 파일을 읽어낼 때 조각이 나옵니다 -
  「Jahresent… gelts」, 「Gewerbli…」. 이런 조각, 붙어버린 단어
  (「TürverschlussmitV」), 그리고 벌거벗은 약어(「JY」, 「FFB」)는 마치
  이름인 것처럼 검게 칠해졌습니다. 이제는 그대로 남습니다. 같은
  분리 손상을 입은 이름은 호칭이 함께 있는 한 계속 검게 칠해지며 -
  본래 단어 안에 대문자가 있는 이름(McKenzie, MacDonald, LeBlanc)은
  애초에 영향을 받지 않습니다.

- **치수 정보와 월(月)이 주소로 간주되었습니다.** 기술 문서에서 「2000
  Lux」, 「1200 Mbit」, 「1500 Watt」, 「5308 Platz」, 「2022 Mrz」가
  검게 칠해졌습니다 - 숫자 네 자리와 대문자로 시작하는 단어가 지명이
  붙은 우편번호처럼 보였습니다. 우편번호는 이제 주소 신호가 함께
  있을 때만 인정됩니다: 국가 코드, 필드 라벨, 줄의 시작, 바로 위
  줄의 거리, 또는 언어 인식도 그곳에서 인식하는 지명 말입니다. 다섯
  개의 시방서에서 이로써 14건의 잘못된 검게 칠하기가 사라지며, 실제
  주소는 하나도 남지 않습니다.

- **정밀 인식이 너무 많이 대체했습니다.** 켤 수 있는 「정밀 인식」 단계는
  독일어 업무 문서에서 「Photovoltaikanlage」, 「Einspeisepunkt」,
  「Flachdach」, 「Personaleingang」 같은 전문 용어를 이름이나 지명으로
  간주했고, 진행 중인 위치 목록의 회사명을 검게 칠했습니다. 원인은
  하나의 예외 처리였습니다: 이 단계의 결과는 위치 줄이나 목록 줄을
  인식하는 검사에서 제외되었습니다. 이 예외 처리는 이제 이 단계가
  존재하는 여러 단어로 된 이름에만 적용됩니다 - 목록 줄의 「Anna Huber」는
  계속 인식되고, 위치 줄의 단일 사무 용어는 사라집니다. 어느 기술
  입찰서에서 이는 이 단계의 잘못된 검게 칠하기를 절반으로 줄이면서도
  이름은 하나도 잃지 않습니다.

- **다이어그램이 검사되지 않은 완전한 원본 데이터를 함께 가지고
  있었습니다.** Word나 PowerPoint에 그림을 삽입하면 프로그램은 그 계산의
  바탕이 된 표를 별도 파일로 문서 안에 넣습니다. 그중 그림에 보이는
  것은 숫자 몇 개뿐이지만, 표 안에는 그림에 전혀 나오지 않는 줄까지
  포함해 목록 전체가 들어 있습니다. 이 표는 지금까지 변경 없이 그대로
  전달되었습니다. 이제는 나머지 문서와 같은 자리표시자로 함께
  정리됩니다.

- **OpenDocument 파일(ODT, ODS, ODP)의 삽입된 객체도 마찬가지입니다:**
  삽입된 그림이나 삽입된 표가 손대지 않은 채로 남아 있었습니다.

- **Word 문서: 각주와 미주가 정리되지 않았습니다.** 그 텍스트는 - 이름,
  주소, 계좌번호를 포함해 - 완전히 결과물에 남았습니다. 각주나 미주가
  있는 모든 Word 문서가 영향을 받았습니다. 마찬가지로 문서와 함께
  보이지 않게 이동하는 자동 텍스트 구성 요소도 손대지 않았습니다.

- **Word: 선택 목록, 댓글, 이미지 설명의 정보.** 선택 필드의 항목들
  (펼쳐야만 보임), 댓글 작성자, 그림 설명, 참조 명령 뒤의 주소가
  계속 결과물에 남아 있었습니다.

- **Excel: 피벗 테이블이 원본 데이터를 두 번째로 담고 있었습니다.**
  피벗 테이블이 있는 통합 문서는 그 안에 분석된 행의 완전한 복사본을
  보존합니다 - 보이지 않지만 파일 안에 있습니다. 시트 자체에서는
  모든 것이 대체되었더라도 이 복사본은 지금까지 변경 없이 그대로
  남았습니다. 피벗 테이블과 함께 전달된 모든 분석 결과가 영향을
  받았습니다.

- **Excel: 대화형 댓글과 그 작성자.** 최신 방식의 댓글 텍스트와 댓글
  작성자 명단 - 표시 이름과 로그인 식별자, 기업에서는 대개 이메일
  주소 - 가 계속 결과물에 남아 있었습니다. Word 문서의 같은 명단도
  마찬가지였습니다.

- **Word와 Excel의 사용자 정의 문서 속성.** 법률사무소가 서식에 넣는
  「Mandant」나 「Aktenzeichen」 같은 필드는 지금까지 정리되지
  않았습니다. 어떤 화면에서도 보이지 않지만 복사할 때마다 함께
  따라다닙니다.

- **표(ODS): 셀의 선택 목록.** 지난 버전 이후 Excel에서와 마찬가지로,
  이제 OpenDocument 표에서도 셀을 펼쳤을 때 나타나는 것이 정리됩니다.
  다른 셀에 대한 참조는 목록이 계속 작동하도록 손대지 않은 채로
  남습니다.

이 모든 자리는 늘 그렇듯 대응 파일을 통해 다시 되돌릴 수 있습니다.

- **Outlook 메시지: 손상된 파일이 정리를 강제로 중단시켰습니다.** 특정
  손상된 `.msg` 파일은 알림 대신 중단으로 이어졌습니다. 이제는 읽을 수
  있는 만큼 읽힙니다.

- **대응 파일은 이제 오직 당신만 읽을 수 있습니다.** 이 파일은 원본 데이터를
  평문으로 담고 있으며, 지금까지는 일반적인 권한으로 결과물 옆에 놓여
  있었습니다 - 공유 저장소에서는 누구나 열 수 있었습니다. 정리된 결과물
  자체는 달라지지 않습니다. 이것은 애초에 전달되어야 하기 때문입니다.

- **나중에 불러오는 언어 모델은 압축을 풀기 전에 더 정밀하게 검사됩니다.**
  조작된 패키지 - 예를 들어 여러 작업 공간을 지원하는 회사 배포본 -
  는 압축을 풀 때 지정된 폴더 밖에 파일을 놓을 수 있었습니다. 일반적인
  나중 불러오기에는 아무런 변화가 없습니다.

- **스크린샷을 찍으면 - 바로 정리됩니다.** `Strg+Umschalt+B`, 「파일 →
  스크린샷 찍기 …」, 또는 작업 표시줄의 아이콘을 통해 화면 위로 사각형을
  그립니다. 그 안에 있는 것은 이후 다른 모든 파일과 같은 경로를
  거칩니다: 텍스트 인식이 화면 텍스트를 읽고, 이름, 주소, 전화번호,
  이메일 주소가 검게 칠해지며, 그 뒤 이미지가 편집기에서 열려 놓친
  부분을 사각형으로 추가로 검게 칠할 수 있습니다. 정리된 이미지는
  바탕화면(또는 설정한 출력 폴더)에 놓입니다. **원본** 촬영본은 어디에도
  저장되지 않으며 종료 시 삭제됩니다. 텍스트 인식은 평소 꺼져 있어도
  이 작업을 위해 켜집니다 - 이미지에서는 이것 없이는 아무것도 찾을 수
  없기 때문입니다. Mac에서는 시스템이 처음 한 번 「화면 기록」 권한을
  묻습니다.

- **이미지에 이제 그릴 수 있습니다: 사각형, 타원, 화살표, 텍스트, 번호가
  매겨진 단계 표시.** 여섯 가지 색과 세 가지 선 굵기로, 1부터 5까지의
  키로 선택할 수 있습니다. 스크린샷과 안내서를 위한 것입니다: 두 번째
  프로그램을 열지 않고도 중요한 부분을 가리킬 수 있습니다. 실행 취소와
  손잡이를 통한 조정은 다른 모든 막대와 마찬가지로 적용됩니다 - 즉
  주석은 설정한 뒤에도 옮기고 늘릴 수 있습니다.
  **그리기는 명확히 검게 칠하기가 아닙니다.** 그려진 사각형은 틀이지
  막대가 아닙니다: 그 아래에 있는 것은 읽을 수 있는 채로 남아 파일과
  함께 나갑니다. 정보를 제거하려면 여전히 「검게 칠하기」와 「픽셀화」가
  있습니다. 그림 도구는 그래서 도구 모음의 별도 줄에 있으며, 그중 하나가
  선택되어 있는 동안에는 안내 줄이 그 사실을 알려줍니다.

- **편집한 이미지는 클릭 한 번으로 클립보드에 들어갑니다.** 편집기의
  「이미지 복사」(또는 `Strg+C`)는 이를 있는 그대로 저장합니다 - 붙여넣기만
  하면 메시지나 메일에 넣을 수 있습니다. 이로써 키를 누르는 순간부터
  채팅에 이르는 경로는 네 단계이며 폴더가 필요 없습니다.

- **거기에 형광펜, 그림자, 그러데이션도 추가됩니다.** 「강조」는 면을
  가리지 않고 색을 입힙니다 - 그 아래의 내용은 읽을 수 있게 남으며,
  바로 이 점이 막대와 다른 점입니다. 「그림자」는 주석을 어수선한
  배경에서 돋보이게 하고, 「그러데이션」은 색을 방향에 따라 서서히
  사라지게 합니다. 둘 다 여섯 가지 그림 도구 모두에 적용됩니다.

- **누군가 겪기 전에 미리 고쳐진 것:** 새로운 도구 줄은 Maskuro를 이미
  사용해 본 모든 사람에게 거의 비어 보였을 것입니다 - 기억된 창 배치는
  그 이전 시기의 것이라 그것을 위한 공간을 남겨두지 않았을 것입니다.
  오래된 배치는 이제 폐기됩니다. 편집기 창은 그러면 한 번만 기본
  배치로 다시 나타납니다.

- **자체 스크린샷 기능을 끌 수 있습니다.** Greenshot, ShareX, 또는
  캡처 도구에 익숙한 사람은 「설정 → 프로그램」에서 「Maskuro로
  스크린샷 찍기」를 끕니다. Maskuro는 그러면 단축키를 아예 등록하지
  않습니다 - 그것은 당신의 도구에 남습니다 - 그리고 이 변경은 재시작
  없이 즉시 적용됩니다. 이렇게 찍은 이미지도 계속 정리할 수 있습니다:
  Strg+V로 클립보드에서 창으로 불러옵니다.

---

## 0.10.37-alpha.20260821 – 2026년 8월 21일

### 신규

- **익명화 시 이제 각 발견 위치가 자신만의 번호를 갖습니다.** 지금까지는
  모든 사람이 `[NAME]`, 모든 지명이 `[ORT]`였습니다 – 그래서 어느
  위치가 어느 값에 속하는지 더 이상 알 수 없었고, 되돌릴 것도
  없었습니다. 이제는 번호가 각 등장마다 계속 올라갑니다: 같은 이름이
  세 위치에서 `[NAME_1]`, `[NAME_3]`, `[NAME_7]`로 표시됩니다.
  문서 안에서는 여전히 어느 위치들이 서로 연결되는지 알 수 없지만
  – 대응 파일이 있으면 개별적으로 모두 되돌릴 수 있습니다. 그래서
  대응 파일은 익명화 시에도 다시 선택할 수 있습니다 – 결과물과는
  별도로 보관하시기 바랍니다.
- **모든 48개 문서 언어에서 월, 요일, 통화, 단위, 회사 법인 형태가
  더 이상 이름이나 지명으로 간주되지 않습니다.** 달력과 단위 이름은
  Unicode CLDR에서 나오며(생성된 것이지 직접 작성한 것이 아닙니다),
  법인 형태는 각국의 회사법에서 나옵니다 – 여러 단어로 된 경우
  ("sp. z o.o.", "Pty Ltd")와 앞에 붙는 경우("株式会社")도
  포함됩니다. 월 이름이 동시에 이름이기도 한 경우(Juli, August,
  May) 판단은 문장 구조를 따릅니다 – 날짜나 연도가 옆에 있으면
  날짜, 아니면 이름입니다. 이 밖에도 호칭과 직함, 완전한 인사말,
  문서 종류, 자체 언어 모델이 있는 28개 언어의 도로명 기본 단어,
  법률 약어(DSGVO, UStG, ABGB, § 6 Abs 1 Z 27 UStG), 그리고
  필드 값으로 쓰인 언어 이름("Sprache: Deutsch")도 포함됩니다.
  목록은 "도움말 → 단어 목록…"에 있습니다.
- **인도: 주소와 PIN 코드가 인식됩니다** – "15 गांधी मार्ग", "नई
  दिल्ली 110001"뿐 아니라 "15 Gandhi Marg, New Delhi 110001"도
  마찬가지입니다. 인도 국가 패키지는 지금까지 식별 번호만 알고
  있었습니다 – 힌디어 문서에서는 주소가 그대로 남아 있었습니다.
- **정리된 모든 오피스 파일은 전달 전에 다시 한번 패키지로
  열립니다.** 텍스트 발췌만으로는 Word, Excel, LibreOffice가 파일을
  거부할 상황(중복 항목, 끊어진 XML, 빠진 부분)을 알아챌 수
  없습니다. 그리고 정리 작업이 절대 바꿔서는 안 되는 것 – PDF의
  페이지 수, 표의 시트·행·셀, 프레젠테이션의 슬라이드 – 는 원본을
  기준으로 집계됩니다. 이 검사에 걸리면 결과물과 검증 보고서에
  주의 경고가 표시됩니다 – 원본은 변경되지 않습니다.
- **자동 모드도 이제 필드 전체를 마스킹합니다.** 마스킹 모드에서는
  짧은 줄(주소 블록, 표 셀, 머리글 정보)에서 막대가 발견된 값만이
  아니라 줄 전체를 덮습니다 – 단어 길이의 막대는 그 단어가 얼마나
  길었는지 알려줍니다. 옆의 레이블과 금액은 그대로 남고, 본문 줄
  (텍스트 너비의 절반보다 긴 경우)은 계속 단어 단위로 마스킹되어,
  문장 한가운데 이름 하나가 문장 전체를 검게 만들지 않습니다.
- **되돌린 것은 다시 원본처럼 보입니다.** PDF 편집기의 "원본
  되돌리기"와 "교체 되돌리기"는 이제 해당 영역을 원본 파일에서
  정확히 그대로 다시 기록합니다 – 같은 글꼴, 같은 크기, 같은 색상과
  위치, 스캔에서는 같은 픽셀까지. 지금까지는 텍스트가 대체 글꼴로
  새로 배치되어 눈에 띄게 다시 만든 것처럼 보였습니다. 이전
  마스킹의 막대는 이때 흰색으로 덮이는 대신 완전히 사라집니다 –
  표의 색이 있는 셀 배경은 그대로 유지됩니다. 이는 회전된 페이지,
  삽입된 양식 개체의 텍스트, 그리고 **작성된 양식 필드**에도
  적용됩니다 – 이를 위해 래스터화된 작업 사본에는 새로 렌더링된
  원본 페이지의 발췌 부분이 돌아옵니다 – 텍스트 레이어가 필드 값을
  전혀 모르는 곳에서도 마찬가지입니다. PDF 안의 **교체된 이미지**도
  이렇게 되돌아옵니다 – 픽셀화, 흐림 처리, 완전 제거 여부와 관계없이,
  전체든 드래그한 부분이든 마찬가지입니다. 원본 파일이 더 이상
  결과물 옆에 없는 경우에만 기존 방식이 유지됩니다.
- **마스킹되거나 대체 없이 제거된 값도 이제 Word, Excel, PowerPoint,
  OpenDocument에서 되돌릴 수 있습니다.** 지금까지는 되돌리기에
  텍스트 안의 자리표시자가 필요했습니다 – 막대나 빈 공간에는 되돌아갈
  방법이 없었습니다. 이제는 손대지 않은 원본 파일이 결과물 옆에
  있으면 검출 패널이 "마스킹됨"과 "제거됨" 항목을 제공합니다:
  Maskuro는 결과물을 원본과 비교해 막대나 빈 공간이 있던 자리에
  값을 다시 삽입합니다 – 서식까지 포함해서, 나뉜 텍스트 흐름도
  다시 완전해집니다. 텍스트, HTML, 이메일, 그리고 이메일의 오피스
  첨부파일에도 마찬가지로 적용됩니다 – 메일 본문에 자리표시자가
  있고 첨부파일에 막대가 있으면, 둘 다 한 번에 되돌려집니다.
- **이메일이나 Outlook 메시지의 PDF 첨부파일도 되돌릴 수 있습니다** –
  자리표시자(번호가 있거나 익명), 막대, 대체 없이 제거된 항목
  모두입니다. 표시 화면이 없어도 위치는 원본 첨부파일에서 가져옵니다
  – 값은 글자 단위로 정확하게, 원본의 읽기 순서대로 돌아옵니다.
- **마스킹된 값도 되돌릴 수 있습니다** – PDF와 텍스트 보기에서
  모두. 마스크("**** **** **** **** 3201")는 절대 유일하지 않으며,
  두 개의 번호가 같은 마스크를 가질 수 있습니다 – 그래서 되돌리기는
  절대 문자 그대로의 방식을 택하지 않고, 원본에 이 위치에 어떤
  값이 있었는지 묻습니다. 지금까지는 검출 패널에서 이런 줄이 아예
  조작조차 되지 않았습니다.
- **Word, Excel, PowerPoint, OpenDocument에 삽입된 이미지를 되돌릴
  수 있습니다.** 이미지 안에서 마스킹된 값은 패널의 해당 줄을 통해
  돌아옵니다 – Maskuro는 원본 이미지를 읽어 정확히 그 위치를
  가져옵니다. 흐리게 처리되거나 제거되거나, 얼굴과 코드가 처리된
  이미지는 편집 메뉴의 새 항목 "삽입된 이미지 되돌리기"를 통해
  원본 파일에서 전체를 가져옵니다 – 이메일이나 Outlook 메시지의
  오피스 첨부파일을 통해서도 마찬가지입니다. 첨부파일 자체로 걸려
  있고 텍스트 인식을 통해 마스킹된 이미지도 마찬가지로 자신의
  패널 줄을 통해 돌아옵니다.
- **꾸며낸 값도 텍스트 보기에서 되돌릴 수 있습니다.** 지금까지는
  패널이 그곳에서 "고유하지 않음"이라고 보고했습니다. 이제는
  되돌리기가 원본에서 값을 찾아, 결과물의 같은 위치에서 정확히
  꾸며진 대체 값을 요구합니다 – 꾸며낸 이름은 절대 모든 곳에서
  문자 그대로 대체되지 않습니다 – 어딘가에 실제로 그 이름이 존재할
  수도 있기 때문입니다.
- **Word, Excel, PowerPoint, OpenDocument에서의 되돌리기는 원본의
  서식을 유지합니다.** 값이 여러 실행에 걸쳐 있던 경우 – "Anna"는
  일반, "Musterfrau"는 굵은 빨간색 – 지금까지는 전체가 첫 번째
  실행으로 돌아와 굵기와 색을 잃었습니다. 이제는 글자가 원본처럼
  다시 나뉘어 배치됩니다 – Word 문단은 이후 원본과 바이트 단위로
  동일합니다. 이는 HTML 페이지, 이메일의 HTML 부분, Outlook
  메시지(.msg)의 HTML 본문에도 적용됩니다 – 이메일의 경우 정리
  작업이 지금까지 조용히 제거했던 Doctype도 함께 유지됩니다.
- **텍스트 파일은 자신의 인코딩을 유지합니다.** 정리와 되돌리기는
  이제 `.txt`, `.md`, `.csv`를 원래 제공된 인코딩 그대로 기록합니다
  – BOM이 있는/없는 UTF-8, UTF-16, Windows-1252. 지금까지는
  Windows-1252 파일이 항상 UTF-8이 되었고, UTF-16 파일은 교체할
  내용이 전혀 없어도 손상된 채로 돌아왔습니다.
- **되돌려진 이미지는 색상 모드를 유지합니다.** 그레이스케일
  스캔은 3배 큰 RGB 파일 대신 그레이스케일로 돌아오고, 팔레트는
  팔레트로, 흑백은 흑백으로 돌아옵니다 – 전체 이미지가 원본과
  같은 값으로 유지됩니다. 이미지 파일과 PDF 안의 이미지 모두에
  적용됩니다. CMYK와 16비트는 RGB로 유지됩니다 – PNG 결과물이
  둘 다 담을 수 없기 때문입니다.
- **이미지 안의 프레임은 그것이 닿는 편집 전체를 되돌립니다.**
  픽셀화된 얼굴은 인식된 상자 주위에 여백을 갖고 있습니다 – 프레임을
  얼굴 위로만 드래그한 사람은 픽셀화된 테두리를 그대로 남기게
  되었습니다. 이제 프레임은 원본과의 차이가 이어져 있는 전체
  범위까지 자동으로 확장됩니다 – 눈 부위 위로만 프레임을 드래그해도
  충분합니다. 옆의 분리된 막대는 그대로 남습니다 – 사진 전체가
  완전히 제거되거나 완전히 흐려진 경우에는 드래그한 프레임이
  그대로 적용됩니다. 이미지 파일과 PDF 안의 이미지 모두에
  적용됩니다.
- **줄 전체를 마스킹하는 막대.** 편집기의 줄 모드에서 막대는 이제
  발견된 단어 위만이 아니라 줄의 첫 단어부터 마지막 단어까지
  이어집니다 – 단어 길이의 막대는 그 단어가 얼마나 길었는지
  알려주며, 우편번호 앞 여섯 글자만으로도 지명을 추측할 수
  있습니다. 값 옆의 레이블, 금액, 표 열은 그대로 남습니다 – 막대는
  청구서의 줄이 아니라 필드를 덮습니다. "텍스트 줄" 옆의 새로운
  스위치 "줄 전체"는 인접 단어가 그대로 남아야 할 때 다시 단어
  단위로 전환합니다 – 선택은 기억됩니다.

### 수정됨

- **HTML 페이지와 이메일의 이미지가 검사되지 않은 채 남아 있었습니다
  – 로고 안의 이름이 정리 후에도 읽을 수 있게 남아 있었습니다.**
  페이지에 삽입된 이미지(`data:` 주소)는 전혀 처리되지 않았고,
  그 대체 텍스트만 처리되었습니다 – 메일의 HTML 부분에 있는 로고
  (파일 이름이 없는 인라인 이미지)는 첨부파일 필터를 통과하지
  못했고, 이름이 있는 이미지 첨부파일에서는 이미지 규칙(흐리게/
  제거)이 아무 효과가 없었습니다. 이제 세 가지 모두 일반 이미지
  파일과 같은 경로를 거칩니다: 유지되는 이미지에 대한 텍스트
  인식, 얼굴, 코드, 메타데이터, 이미지 규칙까지. 보고서는 이런
  이미지를 언급합니다 – 텍스트 인식 없이 검사되지 않은 채 남는
  경우의 경고도 포함해서 – 그리고 "삽입된 이미지 되돌리기"와
  검출 패널의 되돌리기도 이런 이미지를 함께 인식합니다.
- **텍스트 인식이 지원하지 않는 언어의 이미지가 있는 오피스 파일은
  전혀 정리할 수 없었습니다.** Mac에서는 시스템 자체의 텍스트
  인식이 사용됩니다 – 힌디어, 그리스어, 크로아티아어, 리투아니아어는
  지원하지 않으며 최근에는 그렇다고 알리기도 합니다 – 하지만
  Word, Excel, PowerPoint, OpenDocument에서는 그 때문에 **전체**
  정리 작업이 중단되어, 파일이 아예 생성되지 않았습니다. 텍스트
  자체는 문제없이 정리할 수 있었는데 – 이미지만 읽을 수 없었을
  뿐입니다. 이제는 PDF나 개별 이미지와 마찬가지로 파일이 작성되며,
  결과물에는 이미지가 검사되지 【않았다】는 사실이 이유와 함께
  "언어 관리"에 대한 안내와 함께 표시됩니다.

- **Excel 통합 문서에서는 선택 목록 안의 이름이 그대로
  남았습니다.** 드롭다운 필드(데이터 검증)의 목록은 이제 다른
  모든 셀 내용과 마찬가지로 정리됩니다 – 셀 범위 참조는 통합 문서가
  손상되지 않도록 손대지 않습니다.
- **자리표시자가 맞지 않는 곳에는 검은 막대가 있었습니다 – 이제는
  더 짧은 표기가 표시됩니다.** `[GEBURTSDATUM_1]` 대신
  `[GEBU_1]`이 표시되며, 가장 짧은 형태조차 맞지 않을 때에야
  마스킹이 이루어집니다. 막대는 아무에게도 그곳에 무언가 있었다는
  것을 알려주지 않지만, 짧은 자리표시자는 알려줍니다. 후속 수정
  편집기는 이미 이를 할 수 있었지만, 자동 정리는 지금까지
  그렇지 못했습니다. 대응 파일은 두 표기 방식을 모두 같은 값으로
  연결해, 축약된 형태도 되돌릴 수 있습니다.
- **"교체" 첫 클릭 시 후속 수정 창이 잠시 멈췄습니다.** 자리표시자의
  종류를 결정하는 인식 기능(`[BEGRIFF_3]` 대신 `[NAME_3]`)이
  바로 그 순간에 로드되었습니다 – 약 2~3초가 걸렸습니다. 이제는
  창을 열 때 백그라운드에서 미리 준비됩니다 – 측정 결과 2289
  밀리초가 193으로 줄었습니다.
- **동시에 실행되는 두 정리 작업이 같은 언어 모델을 두 번 로드할
  수 있었습니다** – 예를 들어 폴더 감시와 메인 창이 동시에 실행될
  때입니다. 각 모델이 수백 메가바이트를 차지하기 때문에 메모리
  요구량이 일시적으로 두 배가 되었습니다. 이제는 두 번째 실행이
  첫 번째 실행의 모델을 기다립니다.
- **날짜 줄의 지명은 이제 언어 모델이 단독으로 인식하지 못하는
  경우에도 제거됩니다:** 우편번호와 지명이 함께 있는 것으로
  확실히 발견된 값("3335 Amstetten")은 문서 전체에서 그 지명을
  함께 끌어당깁니다 – 전체 이름에서 성을 끌어당기는 것과
  마찬가지입니다. 그리고 이름 앞의 숫자가 포함된 약자
  ("T3 Hofbauer Christian")는 자리표시자와 함께 사라지지 않고
  읽을 수 있게 남습니다.
- **실제 발주서의 재판독에서 발견된 세 가지 누출을 막았습니다:**
  담당자 "T3 Hofbauer Christian"은 "T3"라는 약자 때문에 열 머리글로
  간주되어 읽을 수 있게 남아 있었습니다 – 언어 모델이 줄바꿈을
  넘어 열 머리글까지 읽어들인 지명은 "Pos."를 삼키고 고객의
  이름만 남겼습니다 – 그리고 호칭이 포함된 이름("Herr Robert
  Köttel")은 성만 끌어당기고 이름은 끌어당기지 않아, 모든 "Herr"가
  그 대상이 되었습니다. 이제 약자는 순수 문자만 인정되고, 두
  단어로 된 이름은 열 머리글로 간주되지 않으며, 검출은 열 머리글
  앞에서 잘리고, 호칭은 이름에 포함되지 않습니다.
- **주소 블록 바로 아래의 날짜 줄에 있는 지명("Melk, 05.08.2026")이
  읽을 수 있게 남아 있었습니다.** 언어 모델이 이를 우편번호 줄의
  지명과 하나로 붙여버려, 전체가 우편번호 패턴에 걸려 나갔습니다.
  이제는 남는 부분이 자체 검출로 남습니다. 새로운 결과 재판독
  기능(`werkzeuge/zweitlesung.py`)으로 발견되었습니다.
- **Mac: 시스템 텍스트 인식이 지원하지 않는 언어(예: 힌디어,
  그리스어, 크로아티아어, 리투아니아어)의 스캔이 검사된 것으로
  간주되었습니다.** 영어 대체 방식으로 읽혔고, 외국 문자는 이미지에
  그대로 남았으며, 보고서는 "아무것도 발견되지 않음"이라고
  했습니다. 이제는 이유와 함께 "이미지가 검사되지 【않았습니다】"라고
  표시되며, 언어 관리는 Tesseract 언어 파일이 있다는 이유만으로
  이런 언어에 대해 텍스트 인식을 더 이상 약속하지 않습니다.
- **PDF에서 교체된 단어 뒤의 문장 부호가 사라졌습니다.** "Aufnahme
  am 01.03.2026, Entlassung am 04.03.2026."에서 지금까지는
  "Aufnahme am [DATUM_1] Entlassung am [DATUM_2]"가 되었습니다 –
  쉼표와 마침표가 사라졌습니다 – 자리표시자와 이동된 날짜 모두에서
  발생했습니다. 이제는 인식된 값만 제거되고 다음 공백까지의 단어
  전체는 제거되지 않습니다 – 뒤에 오는 쉼표, 세미콜론, 마침표,
  괄호는 원래 위치에 그대로 남고, 자리표시자가 그 위를 지나가지
  않습니다.
- **러시아어와 우크라이나어는 단어 형태 분석용 보조 패키지
  (`pymorphy3`)가 없으면 눈에 띄지 않게 더 약한 다국어 모델로
  작동했습니다** – 자체 모델을 로드할 수 없어, "Львів"가 사람으로
  인식되었습니다. 이름 인식에는 단어 형태 분석이 필요하지 않습니다
  – 이제는 이것 없이도 모델이 로드되며, 지명은 다시 지명이
  됩니다.
- **16개 언어의 라이선스 안내가 예전 상태였습니다.** MPL 소스코드가
  "요청 시" 제공된다고 되어 있었고, QPDF가 MPL-2.0으로 표시되어
  있었으며, 표에는 일곱 개의 구성 요소(wordfreq, Qt, ONNX Runtime,
  tokenizers, zxing-cpp, llama.cpp, YuNet)가 빠져 있었고, spaCy
  단락은 영어였으며, 마지막에는 영어 대체 단락이 붙어 있었습니다.
  이제 18개 언어판 모두 독일어판과 동일한 상태입니다: 소스
  아카이브는 maskuro.com/quellcode/oss/에 영구히 게시되고, QPDF는
  Apache-2.0, Qt는 LGPL 경로, 모델 출처가 명시됩니다. 영어 표에도
  빠졌던 줄이 채워졌습니다.

- **속격의 계약 용어("des Angebotsinhaltes", "des Anbotes", "des
  Terminplanes")는 더 이상 지명으로 간주되지 않습니다.** 굴절
  어미가 있는 속격이나 여격 관사 뒤의 단독 단어는 사물 명사입니다
  – 지명은 굴절하지 않습니다("nach Graz"). 지명이 문서 다른 곳에
  관사 없이 등장하면("Burgenland"), "des Burgenlandes"도 계속
  인식됩니다.
- **이동, 마스킹, 꾸며낸 값이 있으면 PDF 페이지가
  래스터화되었습니다.** 제거 후 검증은 발견 사각형 안에 대괄호로
  된 자리표시자만 허용했습니다 – 이동된 날짜("01.07.2026")나
  마스킹된 값("****1234")은 놓친 잔여물로 간주되어, 안전을 위해
  페이지가 이미지로 변환되었습니다 – "교체"에서는 그렇지 않았습니다.
  이제 이런 페이지는 텍스트로 남으며, 패널이나 프레임에서의
  되돌리기는 다시 원본을 제공합니다.
- **여러 단어로 된 대체 값은 PDF에서 검출 패널을 통해 되돌릴 수
  없었습니다.** 꾸며낸 이름("Greta Mayrhofer")이나 마스킹된
  IBAN("**** **** **** **** 3201")은 여러 단어로 이루어져 있습니다
  – 발견 위치 검색은 단어 단위로 비교해 "자리표시자를 문서에서
  찾을 수 없습니다"라고 보고했습니다. 이제는 같은 줄의 연속된
  단어들이 함께 읽힙니다.
- **대체 없이 제거된 값을 되돌린 뒤에도 그 패널 줄이 그대로
  남았습니다.** 자리표시자 모드에서 대체 없이 값을 제거하는
  "마스킹" 전략으로 처리된 값은 패널이 사라짐을 측정할
  자리표시자가 없습니다. 이제는 값이 문서에 다시 나타나는 즉시
  해당 줄이 취소선으로 표시됩니다.

- **"E-Helfer"나 "U-Bahn" 같은 약어 복합어는 더 이상 이름으로
  간주되지 않습니다.**
- **음절 분리 잔여물("Leis-")과 지나치게 긴 합성어
  ("Bauarbeitenkoordinationsgesetzes", "Baustellenkoordinator")는
  더 이상 이름이나 지명으로 간주되지 않습니다.** 스캔된 입찰 문서
  텍스트에서 이로 인해 28개 단어가 덜 마스킹되었습니다.
- **스캔된 견적서의 항목 목록은 더 이상 이름 목록으로 간주되지
  않습니다.** 목록(짧은 줄)을 위한 추가 처리 과정은 "Kälterohr"와
  "Außengeräte"를 사람으로 만들었습니다 – 이제는 "1.1.5" 같은
  항목 번호가 줄 시작에 있으면 작동을 멈춥니다. 메일 대화 내역의
  날짜 줄은 이때 항목 번호로 집계되지 않습니다.
- **스캔된 견적서의 열 머리글과 항목 번호("Pos.", "Pos. 1.1.3",
  약자 "E/L/S")가 이름이나 지명으로 간주되었습니다.** 자신의 줄
  단독으로 있는 약어, 번호가 붙은 레이블, 줄 단위의 단독 문자는
  그렇지 않습니다.
- **후속 수정 창에서 비교 돋보기를 연 후 페이지가 "숨을 쉬었습니다"**
  – "페이지 너비"와 "맞춤"에서는 배율이 표시 영역에 종속되는데,
  이것이 스크롤바가 나타나거나 사라질 때마다 바뀌어, 이어지는
  모든 동작이 페이지를 조금씩 밀어냈습니다. 이제는 캔버스가 이를
  자동으로 안정될 때까지 따라갑니다. 그리고 확대 버튼, 슬라이더,
  단축키는 확대 도중 스크롤바가 나타나도 화면 중심을 유지합니다.
- **가로로 저장된 스캔이 이제 세로로 읽히며, 큰 스캔의 작은
  글자가 더 이상 유실되지 않습니다.** 24페이지짜리 스캔 견적서는
  모든 바닥글에 은행 IBAN 6개, 상업등기번호, UID를 읽을 수
  있게 남겨두고 있었습니다: 스캔이 PDF 안에서 90도 회전되어
  있었고, 텍스트 인식은 매우 큰 이미지에서 이미지 크기에 따라
  줄 전체를 놓치는 경우가 있었습니다. 이제는 눈에 보이는 회전이
  고려되고, 큰 이미지는 겹치는 띠 단위로 읽힙니다 – 바닥글은
  검게 처리됩니다.
- **기본 단어 앞에 하이픈이 있는 인명 도로("Josef
  Admanseder-Straße 7", "Abt-Karl-Straße 8",
  "Dr.-Karl-Renner-Straße 12")가 주소로 인식됩니다.** 스캔된
  견적서의 편지지 머리글에서는 이런 주소가 읽을 수 있게 남아
  있었습니다 – 패턴이 "Straße" 앞에 공백을 요구했기 때문입니다.
- **텍스트 인식에서 O를 0으로, l을 1로 잘못 읽은 IBAN도 이제
  인식됩니다.** 스캔의 작은 글자에서는 텍스트 인식이 숫자를
  글자로 읽는 경우가 많습니다 – 번호는 IBAN 형태였지만 체크섬이
  맞지 않아 그대로 남았습니다. 체크섬이 실패하면 이제 숫자로
  다시 읽는 방식을 시도합니다 – 그것이 맞으면 IBAN으로
  간주됩니다. 잘못된 체크 숫자는 계속 잘못된 것으로 남습니다.
- **"folgenden Codes auf der" 같은 문장 조각이 지명으로 간주되었습니다.**
  소문자로 시작하는 단어로 시작하는 이름이나 지명은 이름/지명이
  아닙니다 – 귀족 접두사("van Gogh", "de Vries")는 예외입니다.
- **편집기에서 마스킹 막대 옆에 마지막 글자가 남아
  있었습니다**("…6", "…t", "…g"), 그리고 막대의 높이가 줄이
  아니라 드래그한 프레임의 높이였습니다. 원인: 편집기가 페이지
  치수를 측정할 수 없으면, 모든 프레임을 "단어가 걸리지 않음"으로
  간주해 정확히 그대로 적용했습니다 – 절반 단어가 절대 남지
  않는다는 규칙 없이 말입니다. 편집기가 위치를 파악할 수 없었던
  개별 텍스트 명령에서도 같은 일이 발생했습니다. 이제는 항상
  옆의 단어 상자가 함께 고려됩니다 – 프레임이 실질적으로 겹치는
  것은 전체가 처리됩니다.
- **단어의 마지막 글자가 마스킹 막대 밖으로 튀어나왔습니다.**
  막대는 글꼴 메트릭에서 얻은 전진 너비를 기준으로 계산되었습니다
  – 글꼴이 더 넓은 형태로 글자를 그리면, 그 나머지가 막대 옆에
  남았습니다. 이제는 문자의 상자가 실제로 그려진 글자 형태까지
  함께 포함합니다.
- **페이지를 이미지로 변환할 때의 안내가 너무 많은 것을
  약속했습니다.** "표시는 그대로 유지됩니다"는 래스터화 이후에는
  맞지 않습니다 – 글자와 그래픽은 픽셀이 되고 파일은 더 커집니다.
  이제 안내는 이를 알리며, 래스터화되는 두 번째 이유(변환이 페이지를
  손상시켰을 것이라는 점)도 함께 알립니다.
- **제거된 값 뒤의 텍스트가 최대 1포인트만큼 왼쪽으로
  밀렸습니다.** 줄을 다시 배치할 때 시작점은 글자 형태의
  가장자리를 기준으로, 이어지는 부분은 펜 원점을 기준으로
  측정되어 – 첫 글자의 전진 너비가 오차로 남았습니다("C" 0.5pt,
  "I" 1.0pt). 이제 재배치는 항상 펜 원점을 기준으로 계산됩니다 –
  이어지는 텍스트는 10분의 1포인트 단위로 정확한 위치에
  놓입니다.
- **공백이 있는 오스트리아 UID("ATU 187 35901")와 라벨 아래 "FN"
  없이 표기된 상업등기번호("Firmenbuchnummer: 30799v")가
  인식됩니다.** 둘 다 스캔된 입찰 양식에 손으로 적혀 있었고, 텍스트
  인식이 올바르게 읽었음에도 그대로 남아 있었습니다.
- **가로로 놓인 PDF 페이지가 마스킹 후 이유 없이 이미지로
  변환되었습니다.** 무결성 검사는 원본과 결과물을 회전된 표시
  상태로 비교했지만, 마스킹 영역은 회전하지 않은 상태로
  계산했습니다 – 회전 표시가 있는 페이지에서는 자체 마스킹이 그
  영역과 어긋난 것으로 나타나 손상으로 간주되었습니다. 이런
  페이지는 이제 텍스트 레이어와 벡터 그래픽을 유지합니다.
- **정상적인 페이지도 자리표시자 뒤의 텍스트가 1포인트만큼 밀린
  경우 – 허용되는 정도임에도 – 가끔 불필요하게 이미지로 변환되었습니다.**
  이미지 비교가 자신의 허용 오차보다 더 정밀했기 때문입니다.
  이제는 0.5포인트 단위로 비교하여 허용 오차를 정확히 맞춥니다 –
  2포인트까지의 어긋남에는 반응하지 않고, 그 이상에는 모두
  반응합니다.
- **삽입된 양식 개체 안의 정보가 그대로 남아 있었습니다.** 일부
  서식은 편지지 머리글이나 맺음말을 자체 양식으로 저장해 페이지에
  삽입만 합니다. 그 안의 검출은 계획되고 제거된 것으로 집계되었지만
  실제로 기록되지는 않았습니다 – 텍스트는 그대로 남아 있었고,
  페이지 전체를 래스터화해야만 잡을 수 있었습니다. 이제는 양식
  자체가 다시 작성됩니다 – 여러 페이지에 걸친 양식은 한 번만
  작성됩니다.
- **아무것도 읽을 수 없는 상태가 아니었는데도 PDF 페이지가
  이미지로 래스터화되었습니다.** 7페이지짜리 견적서에서 이런 일이
  6페이지에서 발생했습니다 – 파일이 73KB에서 3.3MB로 커지고
  글자를 이미지에 내주었습니다. 원인은 문서 안에서 여러 번
  연속으로 나오지만 리더에서는 한 번만 보고되는 공백이었습니다 –
  제거된 정보 뒤의 텍스트는 그 너비만큼 오른쪽으로 밀렸고, 검증은
  발견 사각형 안에서 인접 단어를 발견해 래스터화를 선택했습니다.
  유지된 줄 조각들은 이제 다시 정확히 원래 위치에 있습니다 –
  같은 견적서가 래스터화된 페이지 하나 없이 정리됩니다(76KB).
- **키 이름과 청구서 머리글이 사람으로 간주되었습니다.** 접속
  파일에서는 환경 변수 이름("AWS_ACCESS_KEY_ID")이 그 값뿐 아니라
  자체적으로도 교체되었습니다 – 영어 청구서에서는 제목 "Bill
  to"가 이름으로 걸렸습니다. 밑줄이 있는 대문자 식별자는 절대
  이름이 아니며, 전체가 필드 레이블인 줄의 단어도 마찬가지입니다
  – 그 아래의 수신자는 계속 발견됩니다.
- **후속 수정 창의 검색이 대형 PDF 페이지에서 멈칫거렸습니다.**
  검색 필드의 글자 하나마다 강조 표시만 바뀌는데도 페이지 전체가
  다시 래스터화되었습니다. 이제 렌더링된 페이지 이미지는 페이지,
  확대, 보기가 동일한 한 그대로 유지됩니다 – 비교 돋보기 안의
  원본도 마찬가지입니다. 페이지 넘기기, 확대, 새로운 파일 상태는
  지금까지처럼 새로 그려집니다.
- **견적서의 항목 번호가 IP 주소나 전화번호로 간주되었습니다.**
  "1.3.3.4 … 5-Port Gigabit Switch" 같은 항목 줄에서는 "Port"가
  기술적 환경으로 계산되어 항목 번호가 네트워크 주소가 되었습니다
  – 이제는 "Port"가 독립된 정보("Port 80")로만 계산되고 단어의
  일부로는 계산되지 않습니다. 그리고 "1.3.3.6 216879"(항목
  번호와 품목 번호)는 더 이상 전화번호로 마스킹되지 않습니다.
  이런 목록의 실제 IP 주소와 전화번호는 계속 인식됩니다.
- **견적서의 항목 줄이 우편번호와 지명으로 간주되었습니다.**
  "35252 DIETZEL SALR"(제조사가 포함된 품목 번호)와 "1000
  AWG"(도체 단면과 함께 표시된 수량)는 번호가 매겨진 항목 줄에서
  주소로 마스킹되었습니다 – 숫자 뒤의 대문자 단어가 대문자 지명
  으로 간주되었기 때문입니다. 항목 목록에서는 더 이상 그렇지
  않습니다 – 주소 블록 안의 "1080 WIEN"과 소문자로 쓰인 지명은
  어디서나 계속 인식됩니다.
- **추가 이름 인식은 견적서의 직무 줄과 열 머리글을
  마스킹했습니다.** "Partiestundensatz Monteur + E-Helfer"는
  49번 사람으로 간주되었고, 열 머리글 "Pos. Bezeichnung Menge
  EH"는 19번 지명으로 간주되었습니다 – 19페이지짜리 주문서가 이
  때문에 읽을 수 없게 되었습니다. 이런 항목 줄의 검출은 이제
  이름에 없는 문자(더하기 기호, 슬래시, 숫자, 약어)를 자체적으로
  포함하면 제외됩니다 – 줄이 금액으로 끝나는 경우("Alternativ
  Markt … - PV/LS AC-Versorgung 1 290,00")도 마찬가지입니다.
  목록과 디렉터리 안의 이름 – 이 단계가 존재하는 목적 – 은
  영향을 받지 않습니다.
- **거래 약관에서 "der Kunde"가 모든 "Kunde"를 이름으로
  만들었습니다.** 추가 이름 인식이 관사를 검출에 포함하면, 이는
  두 단어로 된 이름으로 간주되어 같은 단어의 나머지 35개 위치를
  모두 보호했습니다. 이제는 관사가 제외되며, "der Kunde"도
  기존의 "des Kunden"과 마찬가지로 처리됩니다.
- **레이블이 값으로 간주되었습니다.** "E-Mail"이 일곱 번 이메일
  주소로 마스킹되었고, "Telefonnummer"와 "Faxnummer"가 전화번호로
  마스킹되었습니다. @가 없는 주소와 숫자가 없는 전화번호는 더
  이상 대상이 되지 않습니다.
- **한 글자로 된 열 약어("L: 154,50", "S: 0,00")가 이름으로
  간주되었습니다** – 태양광 견적서 하나에서 25번. 단일 문자는
  이름도 지명도 아닙니다.
- **PDF 페이지가 너무 자주 이미지로 변환되었습니다.** 실제
  견적서에서 발견된 두 가지 원인: PDF가 각 글자를 개별 명령으로
  배치하고 그 아래에 텍스트 문자 없는 공백 글리프가 있는 경우,
  그 지점부터 대응 관계가 하나씩 밀렸습니다 – 제거된 값의 마지막
  글자가 남았습니다("ŠkodaTopCar**d**") – 그리고 검증은 정당하게
  페이지를 래스터화했습니다. 그리고 줄 끝에서 나뉜 단어
  ("Datenschutz-")는 읽기 라이브러리의 하이픈 표시 때문에
  밀린 것으로 간주되었습니다. 두 가지 모두 해결되었습니다: 차량
  견적서는 래스터화된 페이지 4개에서 0개로, 19페이지짜리
  주문서는 7개에서 0개로 줄었습니다 – 글자는 계속 글자로 남고
  파일은 작게 유지됩니다.
- **두 가지 추가 래스터화 원인을 해결했습니다:** 문서 자체가
  "F1"이라는 이름의 글꼴을 포함하는 경우, 이미지 위의
  자리표시자가 그 글꼴로 그려져 읽을 수 없었습니다 – 이제는 자체
  라벨 글꼴이 겹치지 않는 이름을 갖습니다. 그리고 읽기 라이브러리에
  긴 텍스트 명령 중간의 공백이 없으면, 이제는 다중 바이트
  글꼴에서도 (같은 코드, 같은 문자로) 위치가 증명됩니다 – 끝으로
  추정되는 대신입니다 – 이전에는 제거된 값의 글자 하나가 남고
  나머지 텍스트가 눈에 띄게 옆으로 밀렸습니다. 마지막으로 두
  가지 경우: 수십 개의 공백 글리프로 이루어진 명령이 대응 관계를
  흐트러뜨렸고(그 뒤의 이름이 그대로 남았습니다), 전진 너비가 있는
  큰 제목이 첫 글자를 찾지 못했습니다(회사 이름이 그대로
  남았습니다). **실제 견적서 9건 중 이제 단 한 페이지도
  래스터화되지 않습니다** – 이전에는 90페이지 중 30페이지가
  그랬습니다.
- **래스터화 시 이미지가 검은 블록 아래 사라졌습니다.** 페이지를
  이미지로 변환해야 하는 경우 원본에서 렌더링됩니다 – 이는
  이미지 정리를 알지 못합니다. 지금까지는 이 때문에 손대지 않은
  부분을 포함해 페이지의 *모든* 이미지 영역이 막대 아래로
  들어갔습니다. 한 견적서에서는 주소와 두 개의 인증 로고가 같은
  편지지 머리글 이미지에 들어 있었습니다 – 막대가 로고까지
  함께 가져갔습니다. 이제는 이미 정리된 이미지가 삽입됩니다:
  그 안의 주소는 마스킹되고, 나머지는 그대로 보입니다. 제거된
  이미지는 검은 상자 대신 흰 종이를 남깁니다.

- **정리된 스캔은 원본보다 몇 배나 커졌습니다.** 무언가가
  마스킹된 모든 이미지는 압축되지 않은 원본 이미지로 파일에 다시
  들어갔습니다 – 24페이지짜리 스캔에서는 파일이 11.8MB에서
  52.9MB로 커졌습니다. 이제 이미지는 원래 있던 형태를 그대로
  유지합니다: 사진은 사진으로, 팩스 스캔은 흑백으로, 색이 없는
  이미지는 컬러 이미지로 저장되지 않습니다. 같은 파일이 이제는
  15.6MB이며, 눈에 보이는 차이는 없습니다.

- **사무기기에서 스캔된 PDF 파일이 줄무늬 패턴으로 돌아왔습니다.**
  이런 스캔은 선명한 흑백 텍스트 레이어를 거친 색상 이미지
  위에 겹쳐 놓습니다 – Canon, Xerox, Kofax가 이런 식으로 파일을
  만듭니다. 이미지 안에서 마스킹할 때 이 레이어가 잘못 다시
  기록되었습니다 – 결과는 읽을 수 없었습니다. 6페이지짜리
  견적서에서는 16개 이미지 중 9개가 영향을 받았습니다. 이제는
  올바르게, 자체 색상으로 처리되며, 마스킹된 부분은 실제로
  사라집니다.

- **"모든 이미지 제거"가 스캔된 페이지에서 텍스트를 함께
  없앴습니다.** 이런 스캔의 텍스트 레이어는 기술적으로는 이미지입니다
  – 함께 제거되거나 흐려져 빈 종이만 남았습니다. 이제는 그대로
  남습니다 – 로고, 도장, 서명은 계속 제거됩니다.

- **손상된 PDF 페이지 검사가 아주 미세한 어긋남 때문에 더 이상
  래스터화하지 않습니다.** 정리 시 새로 배치된 텍스트 조각은
  최대 2포인트까지 밀릴 수 있습니다 – 이미지 비교는 그럼에도
  이를 손상으로 간주해 페이지를 이미지로 새로 만들었습니다 –
  그 과정에서 표 선 같은 벡터 그래픽이 사라졌고, 검출 위치에는
  자리표시자 대신 막대가 놓였습니다. 이제 비교는 단어 검사와
  같은 작은 어긋남을 허용합니다 – 실제 손상은 계속 발견됩니다.

- **여러 값을 연속으로 되돌리는 작업이 Windows에서 더 이상 "액세스
  거부"로 실패하지 않습니다.** 오피스 파일에서 여러 패널 줄을
  짧은 간격으로 연속 되돌린 사람은 바이러스 검사기의 일시적인
  파일 잠금 때문에 실패할 수 있었습니다 – 이제는 이런 잠금을
  잠시 기다립니다.

- **명령 전달의 Windows 경로가 검증기를 종료시켰습니다.** 대기
  중인 인스턴스의 생존 검사가 Windows에서 실수로 자체 콘솔
  그룹에 실제 Ctrl+C를 보냈습니다 – 이제는 신호 없이 시스템에
  직접 조회합니다.

- **여러 단어로 된 필드 레이블이 작동하지 않고, 그 조각들이
  대신 작동했습니다.** "Date of birth", "Bank account", "Cuenta
  bancaria", "Numero de cliente"는 레이블 목록에 있었지만 개별
  단어로 나뉘어 있어 전혀 걸리지 않았고, 그 결과 "de", "of" 같은
  단어 조각이 레이블로 간주되었습니다 – 하지만 "de"는 이름의
  구성 요소이기도 합니다("Anna de Vries"). 둘 다 해결되었습니다:
  이제 표현은 전체로 작동하고, 조각들은 사라졌습니다.

- **"ß"가 포함된 독일어 인사말이 목록에 등록되어 있음에도 사람
  이름으로 처리되었습니다.** "Herzliche Grüße"나 "Mit freundlichen
  Grüßen" 아래에는 결과물에 자리표시자가 남아 있었습니다 – 두
  표현 모두 오래전부터 예외 목록에 있었음에도 말입니다. 원인은
  비교 시 절대 일치하지 않는 표기 방식에 있었습니다 – 5개 목록에
  걸쳐 8개 항목이 영향을 받았습니다. 이제 모두 정상 작동합니다.

- **"John Staff"가 대체되지 않았습니다.** 동시에 영어 열 제목이기도
  한 성이 레이블 필터에 의해 함께 걸러졌습니다. 제목은 계속
  손대지 않은 채로 남고, 그 아래의 이름은 다시 대체됩니다.

- **레이블이 붙은 양식 필드의 값은 AI 단계에서 계속 보호됩니다.**
  AI 단계의 로컬 심판은 지금까지 그 의미가 이미 필드 레이블로
  입증된 검출("Geburtsdatum:" 위의 값)도 판단하도록 제시받았고,
  이를 기각할 수 있었습니다. 이렇게 구조적으로 입증된 값은 더
  이상 제시되지 않습니다. 대응 파일은 이제 각 교체마다 인식
  경로("입증")도 추가로 표시합니다.

- **정리 시 유지된 텍스트가 손상된 PDF 페이지를 이제 인식하고
  원본의 이미지로 다시 구성합니다.** 일부 생성 프로그램의 글꼴에서는
  제거해야 할 정보가 모두 올바르게 제거되었음에도, 유지된 텍스트
  부분이 정리 후 검은 블록으로 나타나거나 단어들이 붙어버릴 수
  있었습니다. Maskuro는 이제 결과물을 원본과 단어 단위, 픽셀
  단위로 비교합니다 – 손상된 페이지는 깨끗한 이미지로 대체됩니다
  – 검출 위치 위의 마스킹 막대, 마스킹된 이미지 영역, 검색 가능한
  텍스트를 유지한 채로. 페이지는 읽을 수 있는 상태로, 제거는
  신뢰할 수 있는 상태로 유지됩니다.

### 변경됨

- **번역된 화면에서는 이제 모든 전문 용어가 어디서나 같은 이름을
  사용합니다.** 하나의 같은 독일어 단어에 대해 창마다 두세 개의
  다른 번역이 나란히 있었습니다: 검증 로그는 노르웨이어로 어떤
  곳에서는 "Revisjonslogg", 어떤 곳에서는 "Kontrollogg"였고,
  무료 등급은 어떤 곳에서는 "Gratisnivå", 어떤 곳에서는
  "Gratisversjon"이었습니다 – 그 외 십여 개 언어에서도 비슷했습니다.
  설정을 찾던 사람은 다음 창에서 다른 이름으로 그것을 발견했습니다.
  화면에서 어차피 가장 자주 쓰이는 단어로 통일되었습니다.

  이 과정에서 하나의 단어가 두 가지 **서로 다른** 것을 가리키던
  자리가 드러났습니다: 프랑스어, 그리스어, 한국어는 "마스킹"과
  "대체"에 같은 표현을 사용했습니다 – 하필이면 프로그램이 그
  차이를 설명하는 바로 그 자리에서였습니다("마스킹은 대체 없이
  제거하고, 대체는 형태를 유지합니다"). 이제 둘은 구분되어
  있습니다. 스웨덴어는 이 결정이 아직 남아 있습니다 – 그곳에서는
  마스킹을 "maskera"라고 부르는데, 이는 대체와 같은 단어입니다.

- **첫 실행 시 사용 유형에 대한 질문이 사라졌습니다.** 시작 직후
  "개인용인가요, 업무용인가요?"라는 창이 나타났고, 설정에도 이에
  관한 줄이 있었습니다. 둘 다 이제는 대체 없이 사라졌습니다.
  아무것도 좌우하지 않는 정보는 잘못된 라이선스를 원하는 사람을
  잘못 안내하고, 정직한 사람에게는 필요하지 않습니다 – 게다가
  아무도 라이선스 종류를 생각하지 않는 순간에 클릭 하나씩을
  요구했습니다. 어떤 라이선스가 맞는지는 그것이 결정되는 곳 –
  가격 페이지, 결제, 도움말 – 에 있습니다. Maskuro를 중앙에서
  배포하는 조직은 계속 설정 파일을 통해 사용 유형을 지정할 수
  있습니다.

- **라이선스 종류 안내가 해당되는 경우를 명확히 밝힙니다.**
  개인 라이선스는 오직 개인적 사용에만 적용됩니다 – 직업적이거나
  영업적인 모든 작업에는 기업 라이선스가 필요합니다 – 직원이 없는
  1인 사업자도 마찬가지입니다. 이는 라이선스 조건에는 그렇게
  적혀 있었지만, 프로그램에도 도움말에도 없었습니다 – 그곳에는
  항상 회사 도메인 이야기만 있었고, 이 경우는 정확히 포함되지
  않았습니다: 1인 사업자의 컴퓨터는 어떤 도메인에도 속하지
  않습니다. 개인 라이선스를 읽어들일 때의 안내는 이제 이를
  알리며, 설명서의 라이선스 장과 자주 묻는 질문에도 이를 위한
  별도 항목이 추가되었습니다. 여전히 아무것도 잠기지 않습니다.

- **아직 출시되지 않은 기능들이 이제 한곳에 모였습니다.** 설정에
  "개발자" 페이지가 생겼습니다 – 거기에는 최대 인식(AI)과 그
  검증 기능, 단어 목록 카탈로그, 폴더 감시가 있습니다. 셋 다
  구축은 되어 있지만 현장에서 검증되지 않았습니다 – 그래서
  개발자 라이선스가 있어야만 표시되며, 어디서나 동시에 그렇습니다:
  페이지, 메뉴 항목, 실행 시의 효과가 모두 같은 결정에 달려
  있습니다. 이 라이선스가 없으면, 이전에 켜져 있던 AI 단계는
  효과가 없어집니다 – 설정 자체는 삭제되지 않으며, 이 기능이
  출시되는 즉시 다시 적용됩니다.

### 개선됨

- **"무엇이 검색되는지"에 이름 인식에서 나온 세 가지 목록이 추가로
  표시됩니다.** 뒤에 오는 단어를 이름으로 읽게 만드는 호칭들 –
  그 뒤에도 아직 이름이 **아닌** 직함과 역할("Herr Bürgermeister
  Huber") – 그리고 문서번호, 사건번호, 사례번호를 인식하는 데
  쓰이는 80개의 다국어 레이블입니다. 셋 다 항상 작동해 왔지만
  개요에서는 보이지 않았습니다.

- **"무엇이 검색되는지"에 지금까지 빠져 있던 두 개의 단어 목록이
  표시됩니다.** 앞에 오는 단어를 이름으로 만드는 호칭과
  직함("Herr", "Frau", "Dr.")과, Maskuro가 "ÖNORM B 2110" 같은
  규격 참조를 사람과 구분하는 데 쓰는 표준화 기관의 약어입니다.
  둘 다 예전부터 인식에 영향을 미쳤지만 개요에는 없었습니다.

- **항목 목록, 목차, 사양 나열, 규격 참조는 이제 읽을 수 있게
  남습니다.** 인식은 이제 줄의 형태를 봅니다: 항목 줄("1.3.1
  Energieerdkabel 1kV"), 안내점이 있는 목차 줄, 나열("- kabelloses
  Laden mit Magnetring"), 수량/가격 줄 위, 열 머리글, 또는
  "mittels" 뒤에 있는 추정된 이름은 사물 명사이며 더 이상 대체되지
  않습니다. 실제 이름은 계속 보호됩니다 – 호칭, 필드 레이블,
  문서 다른 곳의 근거를 통해서입니다. 검증 코퍼스에서는 단 하나의
  정보도 보호를 잃지 않았습니다. 업무 코퍼스에서는 오탐이 25건에서
  6건으로 줄었습니다.

- **제목, 양식 레이블, 인사말은 독일어와 영어에서 더 이상 자주
  이름으로 오인되지 않습니다.** Maskuro가 사물 명사와 사람 이름을
  구분하는 데 쓰는 단어 목록이 크게 늘어났습니다: 청구서, 양식,
  관청 우편의 레이블("Aktenzeichen", "Verwendungszweck",
  "Kostenstelle", "Sort code", "Subtotal"), 지원서와 보고서의
  구획 제목("WERDEGANG", "QUALIFIKATIONEN", "SUMMARY",
  "REFERENCES"), 독일어와 영어의 문서 종류("Auftragsbestätigung",
  "Niederschrift", "Timesheet", "Agreement"), 안내서의 명령형
  ("Sende…", "Select…")입니다. 영어 쪽 목록은 지금까지 눈에
  띄게 빈약했습니다.

- **레이블이 붙은 필드는 이제 레이블이 복합어인 경우에도 그
  안에 무엇이 있는지 알려줍니다.** "Lieferanschrift",
  "Rechnungsadresse", "Sachbearbeiterin", "Kontoinhaber", "Contact
  person", "Billing address"는 옆이나 아래의 값을 이제 단순한
  "Anschrift"나 "Name"과 같은 종류로 분류합니다 – 체크박스가
  있는 작성된 양식에서는 이것이 발견과 놓침의 차이가 됩니다.

- **후속 수정 창에서 마우스 휠이 페이지 경계에서 계속
  스크롤됩니다.** 페이지 끝에서 계속 스크롤하면 다음 페이지의
  맨 위로 이동하고, 페이지 시작에서 반대로 스크롤하면 이전
  페이지의 맨 아래로 이동합니다 – 이렇게 하면 페이지 버튼을
  건드리지 않고도 문서를 처음부터 끝까지 스크롤할 수 있습니다.
  키보드(Page↑/Page↓)는 이미 가능했습니다 – 두 페이지 전환
  사이의 짧은 대기 시간이 트랙패드의 관성 스크롤이 문서 절반을
  넘어가지 않도록 막아 줍니다.

- **후속 수정 창의 페이지 축소판이 패널 중앙에 놓입니다.** 지금
  까지는 왼쪽 가장자리에 붙어 있었고, 창을 넓히면 오른쪽의 빈
  여백만 늘어났습니다.

- **후속 수정 창의 도구 모음이 그룹을 보여줍니다.** 구분선에
  이제 여백과 색이 생겼고, "검색"과 "모든 페이지에 적용"은
  도구들 옆에 자체 그룹으로 표시되며, "적용"은 효과가 있는
  문서 종류에서만 나타납니다. 도구 모음과 메뉴의 모든 항목에는
  이제 이미지가 있습니다: "텍스트 줄"과 비교 돋보기는 자체
  아이콘을 얻었고(돋보기는 지금까지 "전후 비교"와 아이콘을
  공유했습니다), 확대, 전체 페이지, 페이지 너비, 회전, 페이지
  넘기기와 단축키도 마찬가지입니다. "시스템 프로그램으로 열기"는
  이제 인쇄 옆의 도구 모음에도 있습니다 – 완성된 결과에서 익숙한
  프로그램으로 가는 길이 메뉴를 거치지 않고 클릭 한 번이 됩니다.

- **클립보드 정리에서 이제 다시 검토가 필요하다는 안내가
  표시됩니다.** 설정에서는 이 안내가 스위치 옆에 계속 표시됩니다:
  Maskuro는 개인정보를 놓치거나 정보를 잘못 처리할 수 있으며,
  붙여넣은 텍스트는 전달 전에 검토해야 합니다. 켤 때는 추가로
  메시지가 이를 알리고, 출력 영역에도 기록됩니다 – 알림 영역에
  아이콘이 표시되지 않는 경우에도 마찬가지입니다. 매번의 개별
  복사 작업에서는 의도적으로 표시되지 않습니다 – 하루에 50번씩
  나타나는 안내는 세 번째부터는 아무도 읽지 않을 것입니다.

## 0.10.36-beta.1 – 2026년 8월 20일

### 개선됨

- **기술적인 비즈니스 문서가 더 이상 과도하게 마스킹되지 않습니다.** 실제
  견적서와 발주서 11건에서 얻은 4가지 탐지 제어 장치: 항목 번호("1.3.1.1")는
  더 이상 IP 주소로 간주되지 않으며, 규격 참조("ÖNORM EN 62446")와 식별
  코드는 더 이상 우편번호나 전화번호로 간주되지 않고, 관사 뒤의 역할
  단어("der Kunde", "des Auftraggebers")도 더 이상 이름으로 간주되지
  않습니다 – 실제 견적서의 거래 약관에서 46개 역할 단어 전부가 마스킹되지
  않고 다시 읽을 수 있게 되었습니다. 국가 코드가 포함된 주소("A 3390
  Melk", "D-94032 Passau")는 이제 우편번호만 외따로 남기지 않고 완전히
  제거됩니다.

- **단어 목록을 이제 전체적으로 열람할 수 있습니다.** "도움말 → 단어
  목록…"에서 로컬에 사용되는 탐지 목록과 검증 목록을 언어, 용도, 출처,
  내용과 함께 검색할 수 있습니다. 여기에는 Wordfreq, 의료, 개인, 중앙
  관리 목록과 가상 대체값을 위한 저장소도 포함됩니다. 설명서에는 이
  목록에 대한 별도의 절이 있습니다.

- **완료된 파일 줄에 사용된 탐지 언어가 표시됩니다.** 이제 "완료" 뒤에
  "독일어" 또는 "영어" 같은 표시가 붙어, 부적절한 자동 언어 선택이
  즉시 눈에 띕니다. 다른 설치된 언어가 대신 사용되어야 했던 경우에는
  화살표가 두 언어를 모두 표시합니다.

- **새로운 비교 돋보기는 훑어보는 동안 원본의 해당 위치를 즉시 보여
  줍니다.** 확대된 원본 발췌 부분이 여전히 편집 가능한 결과물 위에서
  마우스 커서를 따라다니며, 텍스트의 경우에는 문단을 따라갑니다. 돋보기는
  창 가장자리에서 사용하거나 별도의 최대화 가능한 창으로 분리할 수
  있습니다. 확대 비율은 50~300퍼센트 사이에서 직접 조절할 수 있고,
  켜짐 상태와 마찬가지로 기억됩니다. "초기화"를 누르면 최대화되었거나
  불편한 위치에 도킹된 돋보기도 다시 왼쪽의 다룰 수 있는 크기로
  돌아옵니다. 되돌려진 원본 값은 돋보기 안에서 노란색으로 강조되어,
  읽는 중에 해당 단어가 바로 눈에 띕니다. 한 번 켜두면 이후 적합한
  문서에서도 다시 열립니다 – 프로그램을 다시 시작한 뒤에도 마찬가지입니다.
  기존의 전후 비교 전환 기능은 보기 메뉴에 그대로 남아 있습니다. 설명서에는
  이 기능에 대한 별도의 절이 있습니다.

- **오픈소스 및 모델 출처 증명이 이제 릴리스별로 정확합니다.** 패키지
  빌드는 동봉된 라이선스 텍스트의 해시를 포함한 기계로 읽을 수 있는
  구성 요소 목록을 생성합니다. MPL 소스, 모델 출처, 고정된 리비전, 변경
  사항, SHA-256은 별도로 증명되며, 나중에 내려받는 모델은 모델 폴더
  안에 출처 증명서를 직접 받습니다. 유동적이던 Tesseract와 spaCy 참조
  목록은 고정되었습니다. 모든 소스와 모델 부속물이 공개되고 검증될
  때까지 판매용 산출물은 잠긴 상태로 유지됩니다.

- **로컬 wordfreq 데이터는 라이선스가 완전히 입증되었습니다.** 패키지
  빌드는 버전 3.1.1, CJK를 포함한 변경되지 않은 소규모 목록 39개, 그리고
  중국어 문자표를 개수, 크기, 매니페스트 체크섬으로 검증합니다.
  Apache-2.0 코드 고지, 전체 CC-BY-SA-4.0 라이선스, 저작자 표시, 데이터
  출처, 그리고 생략된 대형·Jieba·미지원 목록이 패키지에 문서화되어
  있습니다.

- **흔히 쓰이는 문장 단어가 잘못 마스킹되는 일이 줄었습니다.** 이름
  탐지가 동사, 대명사, 관사, 전치사를 사람으로 오인할 경우, 로컬 빈도
  사전이 추가 검증 수단으로 작동합니다. 이 사전이 단독으로 결정하는
  경우는 없습니다 – 명사, 여러 단어로 된 이름, 그리고 필드·목록·호칭
  뒤의 이름은 계속 보호됩니다. 중국어, 일본어, 한국어는 이미 존재하는
  자체 언어 모델의 정확한 토큰 경계만 사용합니다. 존재하지 않는 언어에는
  비슷해 보이는 다른 언어의 사전을 대신 사용하지 않습니다. 이를 위해
  문서 텍스트가 인터넷으로 전송되는 일은 없습니다.

- **기술적인 제품·사양 용어가 더 이상 이름이나 지명으로 쉽게 오인되지
  않습니다.** 로컬 검증은 이제 빈도, 품사, 기술적 조어 방식, 주제
  분야를 함께 고려합니다. 그 결과 예를 들어 "Travel-Assistent",
  "Family-Bonus", "WLTP-Wert", "Easy-Start"와 합성된 번호·소유자·브레이크
  관련 용어가 문서에 그대로 남습니다. 영어 구성 요소는 독일어 사실
  텍스트에서도 로컬로 조회됩니다 – 실제 고유명사, 호칭, 사람·장소
  필드는 계속 우선됩니다. 또한 "2년 제조사 보증"은 더 이상 나이로
  간주되지 않습니다.

- **Qt/PySide 라이선스 권리가 이제 완전히 추적 가능합니다.** 프로그램
  패키지에는 GPL-3.0 전문, 정확한 Qt 버전, 소스코드 제공 안내, 그리고
  동적 라이브러리 교체 방법에 대한 독일어/영어 안내서(로컬 macOS
  재서명 포함)가 추가로 들어 있습니다. 배포된 버전의 정확한 소스
  아카이브가 자체 소스코드 페이지에서 이용 가능해질 때까지 판매용
  빌드는 차단됩니다.

- **라이선스와 업데이트 상태가 이제 각 등급마다 무엇이 적용되는지
  명확히 알려줍니다.** 라이선스 창과 업데이트 설정에는 업데이트가
  포함되어 있는지, 언제까지 적용되는지, 그리고 현재 버전을 계속
  사용할 수 있는지가 표시됩니다. 개인 라이선스는 기준일 이후에 나온
  버전은 더 이상 설치하지 않습니다 – 새로 내려받은 설치 프로그램도
  자체에 내장된 출시일을 기준으로 입력된 키가 해당 버전을 포함하는지
  판단합니다. 마지막으로 포함된 개인용 버전은 계속 사용할 수 있습니다.
  반대로 기업 구독이 만료되면 사용과 업데이트가 모두 종료됩니다 –
  체험 기간이나 무료 등급은 우회로로 열리지 않습니다.

- **개인 영구 라이선스는 이제 재설치 후에도 올바른 프로그램 버전을
  찾습니다.** 서명된 버전 카탈로그가 모든 안정 버전과 그 패키지를
  관리합니다. 구매에 포함된 마지막 설치 프로그램을 더 이상 구할 수
  없는 경우, 그다음으로 높은 이용 가능한 안정 버전이 자동으로 사용될
  수 있습니다 – 베타나 나이틀리는 절대 아닙니다. 너무 새 버전을
  설치하려는 경우, 고객은 허용된 버전을 설치하거나 새로운 업데이트
  기간을 위해 구매 페이지로 이동할 수 있습니다 – 버전이 조용히
  낮아지는 일은 없습니다. 이는 관리형 MSI 설치에도 동일하게 적용됩니다.

- **자동 얼굴 마스킹이 이제 명확하게 설명되어 있습니다.** 프로그램
  도움말과 개인정보 보호 문구는 이 기능을 "얼굴 영역 인식 및 알아볼 수
  없게 처리"라고 명명하며, 신원 확인, 재식별, 얼굴 비교, 생체 인식
  템플릿, 인물·얼굴 데이터베이스와는 구분합니다. 또한 완전히 로컬에서
  이루어지는 인식이 영역을 놓치거나 잘못 표시할 수 있으므로 결과를
  반드시 시각적으로 확인해야 한다는 점을 명확히 알립니다. 개별적으로
  정리된 이미지 파일에서도 이제 결과 보고서에 인식되어 픽셀화된 얼굴
  영역이 표시되며, 텍스트 인식이 누락된 경우를 더 이상 잘못 "완전히
  변경되지 않은 파일"로 설명하지 않습니다.

## 0.10.36-alpha.20260820 – 2026년 8월 20일

### 수정됨

- **익명화된 항목을 이제 순서와 관계없이 완전하게 되돌릴 수 있습니다.**
  기존의 되돌리기는 눈에 보이는 텍스트 기준점을 통해 값을 찾았습니다.
  촘촘한 표, 바로 인접한 자리표시자, 보이지 않는 오피스/메일 저장소에는
  이런 기준점이 없었습니다 – 그래서 어떤 용어는 다른 평문이 우연히 새
  기준점을 만들어 준 뒤에야 되돌릴 수 있게 되는 경우도 있었습니다. 이제는
  결과와 원본을 실제 형식 매체별로 완전한 대응 관계와 함께 비교하며,
  선택한 값에 해당하는 실제 위치에만 기록됩니다.

- **이름, 메일 주소, 번호, 사용자 지정 검사어는 탐지 결과가 겹치는
  경우에도 명확하게 조작할 수 있습니다.** 같은 평문 값이 두 종류에
  동시에 대응되는 경우, 실제로 해당 위치에 있는 자리표시자와 클릭한
  사이드바 항목이 함께 결정합니다. 대응되지 않는 값/자리표시자 쌍은
  여전히 안전하게 잠긴 상태로 유지됩니다.

- **메일 특수 사례에서 더 이상 숨겨진 자리표시자가 남지 않습니다.** 이는
  MIME으로 인코딩된 제목, 텍스트 첨부파일, EML과 MSG에서 HTML 마크업으로
  분리된 이름에 적용됩니다. 자체 문자셋 지정이 없는 UTF-8 HTML도 이제
  Outlook 파일에서 편집 단계마다 깨진 글자로 다시 인코딩되지 않으며,
  이미 그렇게 저장된 기존 결과도 계속 되돌릴 수 있습니다.

### 개선됨

- **새로운 검증 매트릭스가 모든 익명 사이드바 항목을 개별적으로, 그리고
  의도적으로 역방향으로 검사합니다.** 텍스트, 오피스, 웹, 메일 형식
  14종과 PDF를 모두 검사한 뒤, 수식, 속성, 관계, 주석, 메일 헤더,
  첨부파일, 내부 보조 저장소도 검사합니다. 전체 macOS 실행은 이제
  149/149개의 검사 스크립트가 정상입니다.

## 0.10.35-alpha.20260820 – 2026년 8월 20일

### 개선됨

- **언어별 측정이 이제 정말로 같은 것끼리 비교합니다.** 정기 측정 코퍼스는
  독일어와 영어로 동일한 14개 문서 사례에 동일한 텍스트 과제 7개와 이미지
  과제 4개를 담고 있습니다. 전체 실행은 존재하는 12개 코퍼스 언어 전체에
  대해 정확히 같은 매트릭스를 반복합니다. 아직 완전히 번역되지 않은
  서식, 표, 채팅 등의 구조 표본은 그대로 유지되지만, 별도로 표시되며
  더 이상 언어별 비율에 섞이지 않습니다.

- **전체 실행은 언어마다 각각의 측정 보고서를 작성합니다.** 언어
  스위치 없이 실행하면 의도적으로 독일어와 영어만 검사됩니다.
  `--alle-sprachen`은 완전한 12개 언어 코퍼스를 요구하며, 언어나 사례가
  하나라도 빠져 있으면 첫 문서를 처리하기 전에 중단됩니다. 이름이 같은
  결과는 언어별로 분리된 폴더에 저장됩니다. 전체 보고서에는 가중
  탐지율 외에 언어별 비율의 비가중 평균도 함께 표시됩니다.

- **공개된 언어 비교는 이제 실제 한계도 함께 보여 줍니다.** 텍스트
  인식을 포함한 정기 실행에서 독일어와 영어는 알려진 218/218건의
  항목을 오탐 없이 제거합니다. 텍스트 인식과 고급 단계를 포함한 전체
  테스트는 1,308건 중 1,255건을 오탐 17건과 함께 제거합니다. 11개
  언어가 100퍼센트에 도달하며, 힌디어는 51퍼센트입니다. 이전의 전체
  비율은 서로 다른 문서 수와 목표값 수에 기반한 것으로, 새 매트릭스와는
  비교할 수 없습니다.

## 0.10.34-alpha.20260819 – 2026년 8월 19일

### 수정됨

- **같은 이름이 여러 번 나오는 경우, 한 번만 되돌려도 사이드바에서 계속
  접근할 수 있습니다.** 지금까지는 첫 번째로 되돌린 `[NAME]` 위치만으로도
  이름 줄 전체가 사라졌습니다. 같은 이름의 다른 위치들은 그 결과 자리표시자로
  남았고, 때로는 다른 이름들이 되돌려질 때까지 일시적으로 잠겨 있기도
  했습니다. 이제 줄은 마지막 위치까지 처리된 뒤에야 사라집니다. 이미
  되돌려진 평문은 그래도 다시 자동으로 익명화되지 않습니다. 이는 부분적으로
  성공한 일괄 되돌리기와 PDF의 프레임 도구에도 동일하게 적용됩니다.

- **"교체 되돌리기"는 오피스 페이지 보기에서도 작동합니다.** 그곳에 보이는
  페이지는 일시적인 PDF 미리보기일 뿐입니다 – 이제 그 아래에 있는 실제
  Word, 표 계산, 또는 프레젠테이션 문서가 올바르게 변경되고, 그 다음에
  미리보기가 갱신됩니다.

- **되돌리기가 이제 값의 숨겨진 대응 위치까지 완전히 복원합니다.** Word,
  OpenDocument, Excel, PowerPoint 파일에서는 같은 정보가 수식, 주석, 차트,
  필드 값, 대체 텍스트, 참조 대상에도 추가로 들어 있을 수 있습니다. HTML,
  EML, MSG는 이를 속성, JSON, 메시지 헤더, 첨부파일에도 담고 있습니다.
  지금까지는 형식에 따라 그중 일부가 자리표시자로 남아 있었습니다. 이제는
  검출 범위에 표시된 어떤 항목이든 독립적으로, 원하는 순서대로 되돌릴 수
  있습니다. 보안상 의도적으로 제거된 메타데이터, 변경 이력, 전송 헤더는
  계속해서 제거된 상태로 유지됩니다.

- **이미지에서 되돌릴 때 더 이상 검은 테두리 선이 남지 않습니다.** 프레임의
  오른쪽과 아래쪽 가장자리가 원본에서 복사할 때 픽셀 하나씩 너무 좁게
  계산되었습니다. 이제 좌표가 마스킹 영역과 정확히 일치합니다.

### 개선됨

- **검증 테스트가 이제 지원되는 22개 파일 확장자 전부를 완전한 왕복
  경로로 검사합니다.** 내용이 풍부한 파일이 정리되고, 제공된 모든 값이
  복원된 뒤 깊이 있게 검사됩니다. 여기에 실제 사이드바 조작, 픽셀 단위
  이미지 비교, 오피스 형식 7종 전체의 눈에 보이는 LibreOffice 렌더링이
  추가됩니다. 개별 오류나 보안 사례를 다루는 소규모 회귀 테스트는 그대로
  유지되며, 중복으로 확인된 HTML 검사와 제거된 흑백 모드에 대한 테스트는
  삭제되었습니다.

- **이 버전의 전체 측정 코퍼스를 직접 재측정할 수 있도록 공개합니다.**
  패키지에는 12개 형식, 12개 언어로 작성된 294개의 합성 문서, 2,564건의
  알려진 항목, 기계로 읽을 수 있는 목표값 목록 4개, 그리고 설명서가
  들어 있습니다. 품질 페이지의 다운로드는 내용 기반 파일 이름을 사용하여,
  브라우저가 캐시에서 실수로 이전 버전을 제공하지 않도록 합니다.

## 0.10.33-alpha.20260819 – 2026년 8월 19일

### 신규

- **이미지 파일에서도 개별 위치를 원본에서 되돌릴 수 있습니다.** "원본
  되돌리기" 프레임 도구는 손대지 않은 원본 파일에서 같은 위치의 픽셀을
  그대로 복사해 옵니다. 원본이 없거나 이미지 크기가 다르면 이 기능은
  막힌 상태로 유지됩니다. 이를 통해 이동된 위치의 내용이 잘못 삽입되는
  일이 없도록 합니다.

### 개선됨

- **수동 마스킹 막대가 기본적으로 텍스트 줄에 맞춰집니다.** 여러 줄에
  걸친 드래그는 줄마다 균일한 높이의 막대를 만들고 줄 사이의 여백은
  그대로 둡니다. 서명, 그림, 기타 특수한 경우를 위해 "자유 프레임"이
  직접 지정한 높이로 되돌립니다.

- **편집기가 문서 바로 위에서 다음 동작을 설명합니다.** 안내 문구는
  문서 종류와 도구에 따라 바뀌며, 단어 클릭인지, 텍스트 선택인지, 프레임
  지정인지 알려 줍니다. 이와 함께 도구, 마우스 커서, 실시간 미리보기가
  손을 떼기 전에 이미 무슨 일이 일어날지 보여 줍니다.

### 제거됨

- **오류가 나기 쉬운 흑백 출력 기능이 제거되었습니다.** 일부 PDF에서
  보이지 않는 텍스트 필드가 래스터화된 페이지와 어긋난 위치에 남아
  있었습니다 – 겉보기 파일 용량 절감은 이 보안 및 표시 위험을 감수할
  만한 가치가 없었습니다. 일반 PDF 정리와 문제 있는 페이지를 선택적으로
  래스터화하는 기능은 그대로 유지됩니다.

## 0.10.32-alpha.20260819 – 2026년 8월 19일

### 신규

- **폴더 감시가 이제 정말로 백그라운드에서 실행됩니다.** 수신함, 발신함, 규칙은 「설정」
  아래 자체 페이지에 있습니다. 시작과 중지는 작업 표시줄 또는 메뉴 막대의 Maskuro 아이콘을
  통해 이루어지며, 이 항목은 해당 기능이 활성화된 라이선스에서만 표시됩니다. 설정 창은
  이후 닫아도, 메인 창을 아이콘으로 내려도 감시는 계속됩니다.

- **나중에 손보기 편집기에 이제 상시 학습 모드 스위치가 있습니다.** 이는 트레퍼 영역과
  「도구」 메뉴에 있습니다. 이를 끄면 되돌릴 때나 수동 수정 뒤에도 자체 규칙을 만들 것인지
  묻는 질문이 나타나지 않습니다. Maskuro는 이후 열리는 모든 문서에 대해 이 선택을
  기억합니다. 되돌리기 자체는 변함없이 작동합니다.

### 수정됨

- **큰 추가 모델을 다시 불러올 수 있습니다.** 공개 저장소가 Python의 일반 표준 식별자를
  403으로 거부했습니다. 이제 모델 다운로드는 다른 자체 서비스와 같은, 지정된 Maskuro
  네트워크 경로를 사용합니다. 약 596MB 크기의 파일과 그 체크섬은 변함없습니다.

- **최대화된 비교 돋보기가 도킹 시 더 이상 위쪽 가장자리에 좁은 막대로 남지 않습니다.**
  도킹 전에 자유 창 상태가 정규화됩니다. 저장된 최대화 상태도 다음에 열 때 변경 가능한
  크기로 되돌려집니다.

- **일괄 되돌리기가 이제 표와 기타 텍스트 형식에서 선택한 값을 정말로 모두 되돌립니다.**
  `[EMAIL]` 같은 익명화된 자리표시자의 경우 Maskuro는 지금까지 값을 순서대로 기록했습니다.
  첫 번째 값이 대체되자마자 남은 모든 발견 지점의 번호가 앞당겨졌지만, 이미 계산된 계획은
  여전히 예전 번호를 가리켰습니다. 그 결과 선택한 것 중 일부만 돌아왔습니다. 이제 같은
  자리표시자의 선택된 모든 값이 함께, 고정된 발견 지점 번호로 기록됩니다. 다른 되돌려진
  값 덕분에 어떤 자리가 비로소 명확해지면, Maskuro는 같은 과정에서 그 자리를 다시
  검사합니다 - 선택 순서는 더 이상 중요하지 않습니다.

- **「대체 되돌리기」는 이제 PDF에서 선택한 값을 빠뜨리지 않습니다.** 자리표시자가 다른
  단어 바로 뒤에 매우 가깝게 붙어 있거나, 원본에서 값 바로 옆에 쉼표가 붙어 있는 경우,
  위치 검사가 이웃 단어나 문장부호를 값에 잘못 포함시킬 수 있었습니다. 함께 되돌릴 때
  일부 자리표시자와 트레퍼 줄이 그대로 남아 있었습니다. 이 검사는 이제 실제 단어 시작
  위치를 기준으로 하며, 원본과 결과 사이의 다른 페이지 회전도 고려합니다.

- **되돌린 PDF 텍스트가 이제 원래 크기를 유지합니다.** 지금까지는 이미 작게 설정된
  자리표시자가 기준으로 쓰였습니다. 게다가 원본 텍스트에도 자리표시자용으로 정해진
  11포인트 상한이 적용되었습니다. 이제 원본 상자와 원본 글자 크기가 원본 파일에서
  그대로 넘어옵니다 - 사각형 도구에서도, 트레퍼 패널에서 되돌릴 때도 마찬가지입니다.

### 개선됨

- **검사 안내문이 이제 남은 위험을 더 명확하게 알립니다.** 안내문은 Maskuro가 데이터를
  놓치거나 정보를 잘못 처리할 수 있다는 점을 명시적으로 밝히며, 게시하거나 넘기기 전에
  완전한 검사와 필요하다면 수동 수정을 요구합니다. 이는 클립보드에서 온 텍스트에도
  적용되며, 17개 번역판 전부에 완전히 반영되었습니다.

- **검사 기록은 이제 그 안의 줄 안에서도 사용자 이름 없이 시작합니다.** 기록 자체는
  회사가 의도적으로 활성화하기 전까지는 꺼진 채로 있습니다. 그 뒤에도 추가 회사
  지정 없이는 어떤 줄에도, 중앙 월간 파일 이름에도 사용자 이름이 들어가지 않습니다.
  그 대신 무작위 로컬 프로필 비밀에서만 도출된, 추측할 수 없는 가명이 안전한 구분을
  위해 쓰입니다. 라이선스 대화상자는 더 이상 활성화를 권장하지 않고, 「기록 없음」을
  기본값으로 하며, 사전에 노동자평의회, 인사 대표, 데이터 보호를 미리 안내합니다.

- **대체하기는 이제 무엇을 대체하는지 이름을 붙입니다.** 표시한 이름은 `[NAME_3]`이
  되고, 지명은 `[ORT_1]`이 되고, 전화번호는 `[TELEFON_2]`이 됩니다 - 지금까지처럼
  모두 `[BEGRIFF_n]`이 되는 대신 말입니다. 종류는 클릭 시 인식됩니다. 명확하지
  않으면 - 일반 단어이거나, 선택 영역 안에 이름 *그리고* 지명이 함께 있으면 - 일반
  용어로 남습니다. 사실과 다른 종류를 주장하는 자리표시자는 아무것도 알리지 않는
  것보다 나쁠 것입니다.

- **나중에 손보기 창의 도구에 이제 단축키가 있습니다.** **S**는 검게 칠하고, **E**는
  대체하고, **Z**는 원본을 되돌리고, **V**는 픽셀화합니다. 텍스트 보기에서는 선택
  영역에 바로 적용되고, 페이지 보기에서는 도구를 선택합니다. **글자는 프로그램을
  조작하는 언어를 따릅니다** - 영어는 B/R/O/P, 이탈리아어는 O/S/R/P - 암기 도움은
  자신의 언어에서만 도움이 되기 때문입니다. 단축키는 버튼에 표시됩니다.
  검색창에 입력 중인 사람은 계속 글자를 입력합니다 - 그곳에서는 단축키가 작동하지
  않습니다.

- **프로그램은 하루에 한 번, 어떤 상태로 실행되고 있는지 알립니다 - 어떤 식별자도 없이.**
  이를 통해 우리는 얼마나 많은 설치본이 사용되고 있는지, 그리고 이것이 테스트 기간,
  무료 등급, 라이선스에 어떻게 분포되는지를 셉니다. 전송되는 것은 상태, 운영체제,
  버전, 채널, 국가, 언어, 환경, 인식 단계입니다 - **문서에 대한 것은 아무것도, 그리고
  당신의 컴퓨터를 다시 알아볼 수 있는 것도 아무것도** 없습니다. 당신이 보낸 두 개의
  알림은 우리에게 서로 다른 두 사람의 알림처럼 보입니다. 거기서 하나의 경로를 추적할
  수는 없습니다. 정확히 무엇이 전송되며 어떻게 끌 수 있는지는 개인정보 보호문 5번
  항목에 나와 있습니다.

- **가로로 놓인 페이지가 이제 저절로 올바른 방향으로 섭니다.** 표시 없이 비뚤게 스캔된
  종이는 나중에 손보기가 텍스트 흐름으로 인식해 화면을 바로잡습니다. 이것이 되지 않는
  곳 - 읽을 수 있는 텍스트가 없는 순수 스캔 - 에서는 「보기」 메뉴의 새 항목 두 개가
  수동으로 회전시킵니다(Strg+Umschalt+L과 Strg+Umschalt+R). 회전되는 것은 화면
  표시뿐입니다: 파일 자체는 그대로이며, 검게 칠하기는 여전히 클릭한 바로 그 자리를
  정확히 맞춥니다.

- **로컬 빌드가 이제 라이선스를 완전하고 눈에 보이게 함께 가지고 다닙니다.** 빌드는
  실제로 번들된 Python 패키지를 확인하고, 그 라이선스 텍스트를 버전 개요와 함께
  `lizenzen` 아래 저장하며, 빠진 것이 있으면 중단됩니다. Qt, Tesseract, 얼굴 인식
  모델도 필요한 텍스트를 갖추고 있으며, Maskuro 자체의 조건은 라이선스 계약서로
  동봉됩니다.

- **커서가 어느 자리표시자 안에 있는지 이제 보입니다.** 자리표시자를 클릭하면 괄호와
  번호를 포함해 전체가 환하게 표시됩니다. 「선택 영역 되돌리기」 버튼은 이전에도 단순
  클릭만으로 활성화되었지만, 어느 표시가 걸렸는지는 보이지 않았습니다. 마우스가
  버튼으로 이동해도 이 표시는 계속 남아 있습니다.

- **마우스 커서가 이제 어떤 도구가 선택되었는지 알려줍니다.** 네 가지 도구가 같은
  영역과 같은 동작을 공유합니다. 지금까지는 모두 똑같아 보였습니다. 십자선은 검게
  칠하기, 쥔 손은 대체하기, 편 손은 되돌리기를 뜻합니다.

- **조작된 Office 문서는 이제 프로그램 스스로 거부합니다.** Word, Excel, OpenDocument
  파일은 열 때 컴퓨터의 다른 파일을 텍스트로 끌어오거나 메모리를 가득 채우는 지시를
  담고 있을 수 있습니다. 둘 다 지금까지도 거부되었습니다 - 다만 Maskuro가 아니라
  내장된 XML 라이브러리가 거부한 것이었습니다. 이제는 패키지에 어떤 버전의 라이브러리가
  들어 있는지와 무관하게 프로그램 스스로 결정합니다. 보통의 문서에서는 아무것도
  달라지지 않습니다.

### 수정됨

- **트레퍼 패널은 이제 검게 칠한 자리표시자를 제거합니다.** 예를 들어 `[NAME_1]`을
  나중에 손보기 창에서 검게 칠했는데도, 문서에는 더 이상 그런 자리가 없는데도 그 값
  줄은 지금까지 오른쪽에 그대로 남아 있었습니다. 이제 그 줄은 마지막 발견 지점과 함께
  사라집니다. 같은 자리표시자가 다른 곳에도 있으면 남아 있습니다.

- **회전된 페이지에서 되돌릴 때 이웃 단어가 이제 그대로 남습니다.** 검게 칠한 막대는
  일부러 텍스트를 살짝 넘어서게 그려지는데, 이 좁은 여백만으로도 「im」 같은 인접
  단어가 함께 끌려갈 수 있었습니다. 이제는 뚜렷한 겹침만 인정되고, 가장자리의 접촉은
  인정되지 않습니다.

- **같은 줄에서의 두 번째 대체가 뒷부분 문장까지 가져갔습니다.** 「Sachbearbeitung
  Quaxi Blubbo übernimmt」를 연달아 두 번 대체한 사람은 「Sachbearbeitung
  [ORT_1] [ORT_2]」를 얻었습니다 - 뒤에 있던 단어는 아무 알림도 없이 그냥 사라졌습니다.
  원인은 옆의 자리표시자였습니다: 첫 번째 대체 뒤 줄의 나머지 부분은 공백으로
  시작하는데, 그 텍스트 위치를 찾는 검색이 이웃의 닫는 괄호를 함께 붙잡았습니다.
  그 뒤로 모든 것이 한 글자씩 밀려났습니다. 같은 줄에서 두 번 대체하거나 검게 칠한
  모든 경우가 영향을 받았습니다 - 옆에서 되돌릴 때도 마찬가지입니다.

- **대체하기는 이제 자리표시자가 너무 길면 검게 칠하지 않습니다.** 단어 옆에
  `[BEGRIFF_2]`가 들어갈 자리가 없으면 지금까지는 그 영역이 검게 덧칠되었습니다 -
  그러면 그곳에 한때 무언가 있었다는 사실조차 보이지 않았고, 되돌리는 것은 더더욱
  불가능했습니다. 이제는 더 짧은 표기가 쓰입니다: `[BEGR_2]`, `[BE_2]`, 그래도
  안 되면 `[B_2]`. 일련번호는 모든 단계에서 유지됩니다 - 되돌리기는 이 번호로
  자리를 다시 찾습니다. 가장 짧은 형태조차 들어가지 않는 경우에만 막대가 사용됩니다.

- **같은 줄에서 이미 검게 칠한 것이 있으면 대체하기가 텍스트를 그대로 남겼습니다.**
  나중에 손보기 창에서 원본에서 이름을 되돌리고, 그중 이름을 대체했으며(그 자리는
  공간이 없어 막대가 되었습니다), 그 뒤 성을 대체한 사람은 자리표시자는 삽입되었지만
  이름은 **제거되지 않은** 결과를 얻었습니다. 이는 오직 검토 경고를 통해서만
  드러났습니다. 원인은 줄 자체였습니다: 첫 번째 검게 칠하기 뒤 나머지 줄은 공백으로
  시작하며, 텍스트 위치를 찾는 검색은 여기서 아무 근거도 찾지 못했습니다. 이는 같은
  줄에서의 두 번째 검게 칠하기마다 해당되었습니다.

- **활성화되었지만 모델이 없는 고급 인식이 이제 눈에 띕니다.** 체크는 표시되어 있으면서
  모델이 없을 수 있었습니다 - 설정은 모든 설치본에 적용되지만, 모델은 프로그램 옆에
  있어야 하기 때문입니다. 그러면 이 단계 없이 아무 말도 없이 정리가 실행되었습니다.
  이제 체크는 모델이 없다는 것을 알리고, 결과에는 경고가 표시됩니다. 한 번 내린 선택은
  그대로 저장됩니다: 모델이 로드되는 즉시 다시 적용됩니다.

- **익명화 시 이제 올바른 용어가 되돌아옵니다.** 여러 용어를 수동으로 대체한 뒤 그중
  하나를 되돌린 사람은 항상 **첫 번째** 것을 얻었습니다 - 「Schmidt」가
  「Müller」가 되었습니다. 이 대응 관계는 자리표시자마다 하나의 대체만 기억했고,
  익명화 시에는 모두가 같은 자리표시자를 가지므로 두 번째와 그 이후 용어는
  빠졌습니다. 이제 각 값은 자기만의 줄을 가집니다 - 이전에는 너무 짧았던 대체
  목록에서도 마찬가지입니다.

- **표에서도 이제 되돌릴 수 있습니다.** CSV나 인사 명단에서는 자리표시자가 세미콜론
  하나로만 구분된 채 바로 옆에 나란히 있습니다. 지금까지 프로그램은 이 경우 어느 값이
  어느 자리에 있었는지 밝힐 수 없어 거부했습니다 - `[NAME]`은 되었지만
  `[GEBURTSDATUM]`과 `[TELEFON]`은 안 되었습니다. 이제는 줄을 모든 자리표시자
  지점에서 나눕니다. 어떤 자리가 정말로 여전히 모호하면 지금처럼 계속 거부합니다:
  잘못 되돌려진 값은 답을 하지 않는 것보다 더 나쁠 것입니다.

- **그리고 이제 거부 사실이 눈에 보입니다.** 지금까지는 창 하단에 은은한 회색으로
  표시되었고, 문장이 너무 길어 잘려나갔습니다 - 마치 아무 일도 일어나지 않는 것처럼
  보였습니다. 문장은 짧아졌고, 줄은 몇 초 동안 경고 색으로 빛납니다.

- **되돌리기는 이제 다음 조작 뒤에도 유지됩니다.** 익명화 중 여러 자리를 되돌린 뒤
  다른 무언가를 대체한 사람은 되돌린 자리들이 모두 다시 대체된 것을 발견하고 처음부터
  다시 시작해야 했습니다. 원인은 대응 관계였습니다: 그것이 값을 유지했고, 통일된
  자리표시자를 위한 자동 조정이 다음 저장 시 그 값을 다시 불러왔습니다. 이제는
  되돌린 것은 그대로 남습니다 - 같은 값의 다른 자리는 그로 인해 건드려지지 않습니다.

- **텍스트, Word, Excel, 이메일 파일에서 이제 정말로 자리표시자를 클릭 한 번만 하면
  됩니다.** 이에 대한 안내는 이미 이전 버전에 있었지만, 정확히 표시된 것이 없으면
  「선택 영역 되돌리기」 버튼이 잠긴 채로 있었습니다 - 그러니까 선택 영역 자체를
  설정했을 방법에 아예 이를 수 없었습니다.

### 수정됨

- **검사 기록은 이제 파일 이름을 드러내지 않습니다.** 이는 파일을 평문이 아니라 의도적으로
  해시값으로 기록합니다. 파일 이름이 의뢰인과 분쟁 대상을 드러낼 수 있기 때문입니다.
  하지만 이 해시값은 추측으로도 확인할 수 있었습니다 - 경로는 난수가 아닙니다. 이제는
  이 설치본의 무작위 값이 계산에 포함됩니다: 기록에서의 계수와 구분은 계속 작동하지만,
  외부에서의 역산은 더 이상 되지 않습니다.

## 0.10.31-alpha.20260819 – 2026년 8월 19일

### 개선됨

- **텍스트 파일과 표 파일에서도 마우스를 가져가면 자리표시자가 빨갛게 표시됩니다.** 지금까지는
  PDF 페이지에서만 빨간 미리보기가 있었습니다. 이제 두 보기 모두 같은 방식을 보여줍니다: 빨갛게
  표시된 것이 다음 조작의 대상이며, 클릭 한 번으로 되돌릴 수 있습니다.

- **단어를 클릭 한 번만 하면 됩니다 - 사각형은 편집기가 알아서 그립니다.** 나중에 손보기
  창에서는 지금까지 모든 자리마다 사각형을 직접 그어야 했습니다. 이제 클릭 한 번으로 충분합니다:
  틀은 단어를 둘러싸고 계속 잡을 수 있는 상태로 남아, 더 넓히거나 옮길 수 있습니다. 마우스를
  가져가면 단어가 이미 빨갛게 표시되므로, 클릭했을 때 무엇이 대상이 될지 미리 볼 수 있습니다.
  단어가 없는 곳에서는 지금까지처럼 사각형을 긋습니다.

- **더 이상 사각형으로 정확히 겨냥할 필요가 없습니다.** 자리표시자나 검게 칠한 부분 위로
  사각형을 그으면 언제나 그 자리 전체를 뜻하는 것이지, 절반을 뜻하는 것이 아닙니다. 그래서
  틀은 닿은 대상 전체로 저절로 커집니다: 자리표시자 전체, 막대 전체, 또는 스캔한 종이에서는
  검게 칠한 면 전체로 말입니다. 그은 사각형보다 작아지는 일은 없습니다.

- **이제 단어 단위로 검게 칠합니다.** 단어 절반 위의 사각형은 지금까지 절반만 검게
  칠했습니다 - 절반만 검게 칠한 이름도 여전히 이름입니다. 닿은 단어는 이제 전체가
  사라지고, 이웃 단어는 손대지 않은 채로 남습니다.

- **텍스트와 표에서는 클릭 한 번으로 자리표시자에 들어가면 충분합니다.** 「선택 영역
  되돌리기」는 지금까지 대괄호를 포함해 자리표시자를 정확히 표시해야 했습니다. 이제 커서를
  그 안에 놓기만 하면 되고, 선택 영역은 자리표시자 전체로 눈에 보이게 확장됩니다.

- **벨기에가 국가로 추가되었습니다.** 설정에서 선택 가능합니다. 이제 벨기에 전화번호,
  국가등록번호(Rijksregisternummer, 검사 숫자 포함), BTW/사업자번호(검사 숫자 포함),
  두 공용어로 된 주소, 우편번호와 지명이 인식됩니다. 지금까지 벨기에 전화번호는 그 나라
  자체가 목록에 아예 없었기 때문에 그대로 남아 있었습니다.

- **업데이트 채널은 이제 얼마나 일찍 새 소식을 받는지를 말해줍니다 - 얼마나 멀리가
  아니라.** 「테스트판」에 있는 경우 새로운 미리보기나 새로운 안정판이 전혀 제안되지 않았고,
  이를 알아채려면 채널을 직접 바꿔야 했습니다. 이제는 더 안정적인 것도 모두 제안됩니다:
  테스트판은 테스트판, 미리보기, 안정판을 받고, 미리보기는 미리보기와 안정판을 받습니다.
  반대는 없습니다 - 미리보기 채널에서는 더 최신이더라도 테스트판이 제안되지 않습니다.

- **설정 창에서 줄 간격이 더 넓어졌습니다.** 네 개의 페이지가 나머지 프로그램에서 적용되는
  격자 대신 자체 간격을 사용하고 있었습니다. 특히 「인식」 페이지에서는 이 때문에 체크박스가
  눈에 띄게 너무 촘촘하게 서 있었습니다.

### 수정됨

- **작성된 PDF 양식이 수동 편집 시 더 이상 비어 있게 표시되지 않습니다.** Maskuro는 이를
  위해 오직 일시적인 작업 사본만 고정된 페이지로 만듭니다: 입력된 값이 눈에 보이게 되어
  실제로 검게 칠할 수 있으며, 읽어낼 수 있는 양식 필드가 파일 안에 숨겨진 채 남지 않습니다.
  원본은 대화형 상태 그대로 변경되지 않습니다.
  이는 이제 동적 XFA 양식에도 적용됩니다: XFA를 지원하는 PDFium이 먼저 값과 페이지
  나눔을 구성하고, 그다음 오직 고정된 이미지 페이지로만 이루어진 새 PDF가 만들어집니다.
  XFA 구성이 실패하면 파일은 겉보기에 비어 있는 채로 열리는 대신 안전하게 거부됩니다.

- **「중단」이 이제 정밀 인식 중에도 작동합니다.** 지금까지 버튼은 클릭 시 잠겼지만
  작업은 마지막 블록까지 계속 계산되었습니다 - 긴 파일에서는 이것이 빠져나갈 길 없는
  몇 분을 의미했고, 그동안 버튼은 마치 효과가 있는 것처럼 보였습니다. 이제 작업은
  다음 블록에서 끝납니다.

- **CSV 파일에서 이제 이름 앞에 공백이 없어도 이름이 인식됩니다.** `P-1000;Brunnthaler,
  Elisabeth`에서 인사번호가 세미콜론을 넘어 이름에 바로 붙어 있어, 인식 입장에서는 이것이
  이름을 포함하지 않은 하나의 단어였습니다 - 인사 명단에서는 줄에 따라 이름 전체가
  그대로 남아 있었습니다. 전화번호, 수식, 파일의 열 수는 이로부터 영향을 받지 않습니다.

- **이름과 성 모두에 하이픈이 있는 이름도 이제 인식됩니다.** 「Marie-Luise
  Habsburg-Ott」는 문장 한가운데서 그대로 남아 있었던 반면, 같은 문장의
  「Dragan Mitrović」는 인식되었습니다 - 하필 두 개의 결합된 절반으로 이루어진
  조합을 언어 모델이 놓쳤습니다. 「Nord-Süd-Verbindung」이나 「Software-Entwickler」
  같은 결합된 일반 명사는 영향을 받지 않습니다.

## 0.10.30-beta.1 – 2026년 8월 18일

### 개선됨

- **텍스트 보기의 글꼴 크기를 이제 눈에 보이게 조절할 수 있습니다.**
  지금까지는 페이지 보기에서만 확대에 쓰이던 오른쪽 아래 슬라이더가,
  후속 수정 창의 텍스트·오피스 파일에서는 글꼴 크기(50~300%)를
  조절합니다 – 보기 메뉴의 "확대"/"축소"도 마찬가지입니다. Ctrl+마우스
  휠은 이전부터 가능했지만, 직접 시도해 본 사람만 알고 있었습니다 –
  이제는 슬라이더, 표시, 휠이 함께 작동합니다.

- **어두운 화면 모양에서는 이제 어두운 작업 영역 위에 흰 종이가
  놓입니다.** 지금까지는 반대였습니다 – 종이 주위에는 밝은 영역이
  남아 있었고, 텍스트 자체는 어두운 바탕에 밝게 표시되었습니다.
  이제는 두 화면 모양 모두에서 종이가 검은 글자가 있는 종이 흰색으로
  유지됩니다 – 어두운 모드에서도 어두워지지 않는 PDF 페이지처럼 –
  그리고 그 주위 영역이 어둡습니다.

### 수정됨

- **문장 중간의 마스킹 이후 문장의 나머지가 더 이상 사라지지
  않습니다.** 후속 수정 창에서 같은 위치를 세 번 다룬 사람 – 교체,
  마스킹, 그다음 "원본 되돌리기" – 은 문장 시작 부분이 삭제되는
  결과를 얻었습니다: "Rückfragen richten Sie bitte an das
  Rechnungswesen."가 경고 없이 "bitte an das Rechnungswesen."이
  되었습니다. 줄 중간에서 이미 한 번 무언가가 제거된 모든 위치에서
  발생했습니다.

- **시작 오류가 더 이상 종료까지 함께 무너뜨리지 않습니다.** 메인
  창의 구성이 중단되면, 그 후 작업 표시줄 아이콘을 통한 종료도
  함께 충돌했습니다 – 그리고 이 두 번째 오류는 오류 보고서에서 실제
  원인을 가렸습니다. 이제는 절반만 구성된 창에서도 프로그램이
  깔끔하게 종료되며, 저장된 설정은 그대로 유지됩니다.

- **"전후 비교"가 더 이상 문서 처음으로 이동하지 않습니다.** 후속
  수정 창에서 아래로 스크롤한 뒤 비교를 위해 원본으로 전환한
  사람은 다시 맨 위로 이동했습니다 – 그리고 위치를 손으로 다시
  찾아야 했습니다. 이제는 양방향 모두에서 같은 줄에 그대로
  머무릅니다.

- **양쪽 정렬된 줄에서 마스킹 시 마지막 글자가 남아 있었습니다.**
  텍스트 명령이 읽기 라이브러리가 보고하는 것보다 더 많은 글자
  형태를 그리는 경우 – 양쪽 정렬에서는 공백 하나를 흔히
  삼켜버립니다 – 대응 관계가 하나씩 밀려, "Dr. Michael Handler aus
  Willendorf"가 "[NAME] r aus f"가 되었습니다: 정리된 문장 한가운데
  두 글자가 남았습니다(실제 회의록에서 발견). 대응 관계는 이제
  읽을 수 있는 곳에서는 명령 자체의 문구를 통해 다시 검증됩니다 –
  더 이상 추측하지 않습니다.

- **"Lerchenfelder Gürtel 43/12"가 절반만 제거되었습니다.** 주소
  패턴은 Gürtel, Kai, Lände, Zeile, Markt, Graben을 도로명
  기본 단어로 알지 못했고, 번지수에는 슬래시가 포함된 부분(43/12,
  집/문)이 허용되지 않았습니다 – 번호가 자리표시자 옆에 그대로
  남았습니다. 둘 다 보완되었습니다 – 빈과 잘츠부르크의 주소는 이제
  완전히 처리됩니다.

- **저장된 웹페이지가 정리 후에도 계속 작동합니다.** 지연 로딩이
  data 속성에 저장하는 주소(`data-lazy-src`, `data-lazy-srcset`)가
  참조로 교체되었습니다 – 실제 지자체 페이지에서 16개 – 그 결과
  페이지의 이미지가 더 이상 로드되지 않았습니다. 웹 주소는 이제
  `src`와 `href`에서와 마찬가지로 그대로 남습니다 – data 속성 안의
  이름, 메일 주소, 전화번호는 계속 교체됩니다.

- **일본어와 한국어 문서가 중국어로 처리되었습니다.** 언어 인식이
  세 문자 체계를 모두 하나로 묶어, 일본어 텍스트(공백 없음)와
  한국어(조사가 붙어 있음)에서 기능어를 찾지 못했고 – 결국 목록의
  첫 번째 CJK 언어를 그대로 선택했습니다. 일본어 회의록과 한국어
  회의록이 이런 방식으로 중국어 모델로 읽혔습니다. 이제는 문자
  형태 자체가 판단합니다 – 가나는 일본어, 한글은 한국어입니다.

- **현장 테스트에서 나온 그 외 10개 언어의 추가 오류:** "Primar",
  "Gradonačelnik", "Ordfører", "Başkanı", "Δήμαρχος" 같은 관직명은
  더 이상 사람 이름으로 간주되지 않으며, 터키어 필드 표기("Adı",
  "Soyadı")와 그리스어 대화 표현("Ωραία", "Βεβαίως")은 더 이상
  제거되지 않고, 날짜가 포함된 결의·조항 번호("323/25-6-2008",
  "27 30.09.2024")는 더 이상 전화번호가 아니며, 마침표가 포함된
  문장 조각("10.An", "T.U.EE", "…pa")은 더 이상 웹 주소로 교체되지
  않습니다.

### 신규

- **원하는 경우 검증 보고서가 자동으로 생성됩니다.** 설정("프로그램"
  페이지)의 체크 하나로 정리할 때마다 자동으로 검증 보고서 PDF가
  저장됩니다 – 이름에 타임스탬프가 포함되고, 별도 폴더에, 결과물
  옆이 아닌 곳에 저장됩니다. 사후에는 이 문서를 만들 수 없습니다 –
  기록이 필요한 사람은 이를 통해 항상 갖게 됩니다. 기본값은 저장
  꺼짐입니다.

- **검증 로그를 이제 프로그램 안에서 켤 수 있습니다.** 기업
  라이선스를 읽어들일 때 Maskuro는 로그를 기록할지 한 번 묻습니다 –
  증빙은 처음부터 기록되어야 의미가 있습니다. 이를 위해 설정에
  스위치가 있습니다("프로그램" 페이지, 기업 라이선스나 체험 기간에
  표시됨) – 관리자의 설정 파일은 계속 적용되며 이전과 마찬가지로
  값을 강제할 수 있습니다. "켜짐"이라는 별도의 로그 줄이 언제부터
  기록이 시작되었는지 기록합니다 – 이로써 기록의 시작도 증명되고
  서명됩니다. 기본값은 로그 꺼짐입니다.

- **지표 패널이 AI 단계가 한 일을 보여줍니다.** 새로운 줄이 모델이
  얼마나 많은 불확실한 검출을 검토, 유지, 기각했는지, 그리고 추가로
  몇 개를 찾아냈는지 알려줍니다 – 지금까지는 후속 수정 편집기에서
  값 하나하나를 클릭하지 않으면 그 작업이 보이지 않았습니다. 오직
  숫자만 표시되며, 값이나 근거는 절대 표시되지 않습니다 – AI 작업이
  없으면 이 줄은 나타나지 않습니다.

- **이메일과 HTML 페이지에서도 이제 되돌리기가 가능합니다.**
  `.eml`, `.msg`, 저장된 웹페이지에서는 지금까지 자리표시자를
  되돌릴 수 없었습니다 – 애플리케이션은 정직하게 이를 알렸지만,
  이메일이야말로 개인정보가 가장 많은 형식입니다. 이제는 다른 곳과
  동일하게 되돌리기가 적용됩니다 – 검출 패널에서, 선택 영역으로,
  익명화된 자리표시자에도 적용됩니다. 이메일의 보이지 않는 HTML
  부분(Outlook이 실제로 표시하는 부분)도 함께 처리되어, 화면과
  메시지가 같은 내용을 말하게 됩니다.

- **검출 패널이 이제 익명화된 값도 – 값 단위로 – 되돌립니다.**
  "교체 되돌리기"는 지금까지 익명화된 파일에서는 잠겨 있었습니다 –
  "[NAME]"이 모든 이름을 동시에 나타내기 때문입니다. 이제 되돌리기는
  원본에서 어느 위치가 어느 값에 속하는지 조회합니다 – PDF에서는
  발견 위치의 좌표를 통해, 텍스트 보기에서는 원본과의 비교를
  통해서 – 선택한 값의 정확한 위치만 되돌립니다. 나머지 값의 줄은
  그대로 남습니다.

- **익명화된 자리표시자도 개별적으로 되돌릴 수 있습니다.** 익명화
  시에는 같은 종류의 모든 항목이 같은 이름을 갖습니다 – "[NAME]"은
  모든 사람을 나타내며, 지금까지는 그래서 개별 되돌리기가
  불가능했습니다. 이제는 어차피 결과물 옆에 있는 원본을 조회합니다:
  텍스트 보기에서 자리표시자를 선택하고 "선택 영역 되돌리기"를
  선택하면 – 정확히 그 위치가 정확히 그 값과 함께 돌아옵니다.
  원본에서 값을 확실하게 읽어낼 수 없으면 애플리케이션은 추측하지
  않고 그렇게 말합니다. 이때 대응 파일은 계속 생성되지 않습니다.

- **후속 수정 창이 정리 후 자동으로 열립니다.** 어떤 도구도
  모든 것을 찾지 못합니다 – 그래서 결과물을 검토하는 시선은 예외적인
  클릭이 아니라 정상적인 과정에 속합니다. 원하지 않는 사람은
  설정의 "인식"에서 이를 끌 수 있습니다("정리 후 결과를 후속 수정
  창에 표시").

### 개선됨

- **국가 선택이 새롭게 "자동"으로 바뀝니다.** 지금까지는 출고
  상태에서 화면 언어의 언어권이 기본값이었습니다 – 독일어 컴퓨터에서는
  네덜란드어나 프랑스어 문서도 DACH(독일·오스트리아·스위스) 인식기로만
  정리되어, "Universiteitslaan 1" 같은 주소가 그대로 남았습니다(실제
  공개 회의록에서 발견). 이제는 국가 선택이 문서의 언어를 따릅니다 –
  설정에서 고정 선택을 한 경우에는 그대로 유지됩니다.

- **잘못된 마스킹이 줄었습니다.** 검증 코퍼스와 6개 언어의 실제
  회의록으로 측정한 일련의 오류가 사라졌습니다: 법인 형태가 있는
  회사명("Musterfirma GmbH")은 더 이상 사람이나 지명이 아니라
  조직으로 간주됩니다 – 인사말과 단독 호칭("Saygılarımızla",
  "Buenas tardes", 단독 "Frau")은 더 이상 이름이 아닙니다 – 관직명
  ("Bürgermeister", "Sindaco", "Alcalde")은 그대로 남습니다 – 법률
  및 결의 번호("39/2015")와 천 단위 구분점이 있는 금액("330.000")은
  더 이상 전화번호가 아닙니다 – "Envíame"나 "Estarei" 같은 문장
  시작 단어는 더 이상 이름으로 처리되지 않습니다 – 빈 줄을 넘어선
  검출은 더 이상 이름으로 집계되지 않습니다. 청구서의 청구서
  번호는 증빙 정보로 그대로 유지됩니다 – 고객 번호와 문서 번호는
  계속 제거됩니다.

- **AI 모델을 내려받기 전에 이제 그것이 무엇에 쓰이는지
  표시됩니다.** 다운로드 대화상자는 모델의 작업을 알려줍니다 –
  경계 사례 검토, 추가 이름 검색, 규칙과 프로필 제안 – 그리고
  이것이 채팅 도우미가 아니라는 것을 솔직히 밝힙니다. FAQ는 같은
  질문("AI 단계는 무엇을 할 수 있고 무엇을 할 수 없나요?")에 모든
  언어판에서 자세히 답합니다.

### 수정됨

- **명령줄에서 생성한 검증 보고서 PDF를 이제 검색할 수 있습니다.**
  Windows에서는 헤드리스 PDF 경로가 글꼴이 전혀 없이 시작되었습니다
  – 모든 글자가 대체 상자로 그려졌고, 문서에는 읽을 수 있는 텍스트가
  없었습니다: 검색하거나 복사하려던 사람은 아무것도 찾지 못했습니다.
  이제 이 경우 보고서는 시스템 글꼴을 나중에 불러옵니다 – 텍스트는
  포함되어 검색할 수 있습니다. 창을 통해 만든 보고서는 이 문제의
  영향을 받은 적이 없습니다.

- **스캔의 여러 줄에 걸친 "원본 되돌리기"가 줄 사이에 검은 줄무늬를
  남겼습니다.** 이미지로 변환된 페이지에서는 프레임이 각 줄 밴드
  자체만 정리했습니다 – 이전 마스킹의 잔여물이 그 사이 빈틈에
  남았습니다. 이제는 드래그한 프레임이 줄 단위로 완전히 나뉘어
  처리됩니다.

- **자리표시자 위의 두 번째 프레임이 빨간 잔여물을 남겼습니다.**
  자리표시자는 그것이 대신하는 단어보다 거의 항상 넓습니다 – 그
  이후 같은 위치를 마스킹한 사람은 그 시작 부분만 처리하게 되어,
  문장 한가운데 "RIFF_1]"처럼 조각이 남았습니다. 그리고 되돌리기는
  이후 원본 텍스트를 단어의 위치가 아니라 이 조각의 위치에
  삽입했습니다. 이제 잘린 자리표시자는 항상 전체가 함께
  처리됩니다.

- **회전된 페이지에서 자리표시자 위의 마스킹이 관련 없는 문장을
  지웠습니다.** 나중에 그려진 자리표시자가 제거 시 그 앞의 텍스트와
  혼동되었습니다 – 자리표시자 자체는 그대로 남아 "여전히 문서에
  남아 있습니다"라는 경고가 표시되었고, 페이지의 다른 위치에서는
  프레임과 아무 관련 없는 문장이 대체 없이 사라졌습니다. 이제
  자리표시자는 그 문구를 통해 다시 찾아집니다 – "교체, 마스킹,
  되돌리기"의 연쇄 작업은 이제 가로로 넣은 페이지에서도 문제없이
  작동합니다.

- **설명서가 10개 언어에서 여전히 `python3-tk`를 권장하고
  있었습니다.** 문제 해결 항목에는 Linux에서 tkinter가 없을 수
  있다는 안내가 있었습니다 – Qt 화면으로 전환되기 이전 시대의
  조언으로, 더 이상 아무에게도 도움이 되지 않습니다. 이제는 모든
  언어판에 독일어판과 같은 문단이 들어 있습니다: Qt가 화면 표시에
  필요로 하는 시스템 라이브러리가 없다는 내용입니다.

- **설명서의 라이선스 장이 16개 번역 모두에서 예전 상태였습니다.**
  10개 언어에서는 여전히 Windows Server에는 서버 접속이 포함된
  기업 라이선스가 필요하며 그곳에는 체험 기간과 무료 등급이 없다고
  되어 있었습니다 – 자리가 기계가 아니라 사람을 세는 방식이 된
  이후로 둘 다 틀린 내용입니다. 또한 모든 곳에서 사용 중인 자리가
  언제 다시 비는지, 라이선스가 정기적으로 스스로를 확인하며 그때
  무엇이 전송되는지에 대한 정보가 빠져 있었고, 인터넷 없는 활성화는
  세 단계 없이, 그리고 그 후 컴퓨터가 1년간 연결 없이 작동한다는
  안내도 없이 짧은 요약으로만 남아 있었습니다.

- **후속 수정에 관한 일곱 개 문단이 10개 언어에서 빠져
  있었습니다.** 덴마크어, 핀란드어, 프랑스어, 이탈리아어,
  네덜란드어, 노르웨이어, 폴란드어, 포르투갈어, 스웨덴어, 스페인어로
  도움말을 읽은 사람은 오피스 파일용 페이지 보기도, "수동으로
  마스킹하기"도, 프로그램이 수정에서 학습하는 방식에 대한 전체
  절도 – 세 가지 폭을 담은 표까지 – 찾을 수 없었습니다. "무엇이
  인식되는지"에서는 같은 10개 언어판에 문서 안의 라벨을 통한
  경로가 빠져 있었습니다.

- **라이선스를 읽어들인 상태에서 프로그램이 더 이상 시작되지
  않았습니다.** 창 대신 "프로그램을 시작할 수 없습니다"가 표시되었고
  – 그것도 라이선스 종류와 관계없이 모든 라이선스에서 발생했습니다.
  원인은 검증 기간 종료 직전에 경고하는 라이선스 표시 줄에 있었습니다
  – 이 줄이 그곳에서 사용할 수 없는 것에 접근하고 있었습니다.
  라이선스가 없는 경우 – 체험 기간과 무료 등급에서는 – 오류가
  발생하지 않았고, 그래서 이제야 발견되었습니다.

- **양식에서는 필드 이름이 그대로 남습니다.** "Geburtsdatum"과
  "Anschrift"가 그 값과 함께 사라졌습니다 – 자리표시자가 작고 빨간
  글씨로 *필드 이름*의 위치에 표시되었고, 그 아래 필드는 비어
  있었습니다. 필드 이름은 데이터에 속하지 않습니다 – 이제는 그대로
  남고, 자리표시자는 값이 있던 위치에 표시됩니다.

- **외국어 문서 제목이 더 이상 이름으로 간주되지 않습니다.**
  이탈리아어 양식 위에는 "FATTURA"가, 스페인어 양식 위에는 "PERMISO
  PARENTAL"이 있었는데 – 둘 다 교체되었습니다. 문서 종류 단어
  목록에는 독일어 대응어만 있었습니다.

- **청구서에서 항목이 더 이상 사라지지 않습니다.** "Materialaufschlag
  1  84,00"이 주소로 오인되어 지명 자리표시자로 대체되었습니다 –
  그 결과 청구서에서 한 줄이 빠졌습니다. 금액으로 끝나는 줄은
  주소가 아니라 항목입니다 – 실제 주소("Hauptstraße 1  120,00")는
  영향을 받지 않습니다.

### 변경됨

- **"폴더 감시…"와 명령줄은 당분간 제공되지 않습니다.** 두 경로
  모두 구축되어 작동하지만, 둘 다 현장에서 검증되지 않았습니다 –
  폴더 감시는 한 번도 Windows 실사용 테스트를 거친 적이 없고,
  명령줄은 스크립트에 아무도 실제로 사용해 본 적 없는 스물네 개의
  스위치를 쥐여줍니다. 감독 없이 문서를 변경하는 기능은 검증 없이
  이루어져서는 안 됩니다 – 그래서 해당 테스트가 이루어질 때까지
  거두어들였습니다. 메뉴 항목은 없어졌고, `--wache`는 더 이상
  `maskuro --help`에 나타나지 않습니다.

- **읽기만 하거나 어차피 필요한 기능은 그대로 남습니다.** 검색
  실행(`--suchlauf`)과 검증(`--nachpruefen`)은 명령줄에서 계속
  작동합니다 – 이들은 어떤 파일도 변경하지 않습니다. 탐색기, 컨텍스트
  메뉴, 클립보드, 창을 통한 시작도 마찬가지입니다 – 여기에는 변화가
  없습니다.

- **"스캐너에서 가져오기"는 이제 설명서에 자체 장이 있습니다.**
  지금까지는 "폴더 감시" 항목 끝에 있었습니다. Mac에서는 그 안내가
  폴더를 감시하도록 하라는 것이었습니다 – 이제는 스캔한 페이지를
  창으로 드래그하라는 안내로 바뀌었습니다.

### 수정됨

- **여러 줄에 걸친 "원본 되돌리기"가 구조를 무너뜨렸습니다.**
  자리표시자, 변경되지 않은 직함, 두 번째 교체에 걸친 프레임은
  전체 영역을 **한 줄**로 다시 배치했습니다 – 세 줄이 하나가 되고,
  더 이상 들어가지 않는 부분은 막대가 되었습니다. 이제는 각 줄이
  개별적으로 되돌려집니다.

- **그리고 변경되지 않은 텍스트는 이때 그대로 유지됩니다.** 교체
  *그리고* 일반 텍스트에 걸쳐 드래그한 사람은 교체된 부분만 돌려받고
  – 나머지는 건드려지지 않습니다. 예전 자리표시자의 마지막 잔여물도
  이때 함께 사라집니다 – 이전에는 그 닫는 대괄호가 문장 한가운데
  남아 있었습니다.

- **교체 시 이제 예전 텍스트의 잔여물이 남지 않습니다.** 굵은
  제목에서 "1. R[BEGRIFF_2]ige [BEGRIFF_1] … che"가 나타났습니다 –
  자리표시자는 그 자리에 있었지만, 옆에 원본의 음절이 남아
  있었습니다. 이제는 단어들의 상자만이 아니라 여러분이 테두리로
  지정한 영역 전체가 정리됩니다.

- **익명 자리표시자는 더 이상 되돌려지지 않습니다.** 익명화 시
  모든 이름은 같은 `[NAME]`을 갖습니다. 되돌리기는 처음 나타난
  항목을 가져다 모든 발견 위치에 써넣었습니다 – "Georg Aigner"가
  "Anna Musterfrau"가 되어, 문서에 잘못된 이름이 들어갔습니다.
  이제는 어떤 정보를 의미했는지 더 이상 알 수 없다는 안내가
  표시됩니다 – 문서는 손대지 않은 채로 남습니다.

### 신규

- **"원본 되돌리기"는 이제 래스터화된 페이지에서도 작동합니다.**
  페이지가 이미지로 변환된 경우, 지금까지는 거부되었습니다 – 되돌려진
  텍스트가 페이지 이미지 아래에 놓이게 되기 때문입니다. 이제는
  이미지 안의 해당 위치가 정리되고 그 위에 텍스트가 기록됩니다 –
  스캔 위의 자리표시자와 마찬가지입니다. 이때 내용은 원본 파일에서
  가져오며, 원본은 래스터화되지 않습니다.

- **"선택 영역 되돌리기"가 이제 별도의 버튼으로 존재합니다.** 이전에도
  가능했지만, 우연히 자리표시자를 선택하고 "선택 영역 교체"를
  누른 경우에만 가능했습니다 – 우연히만 찾을 수 있는 기능은 사용자
  입장에서는 존재하지 않는 것과 같습니다.

### 변경됨

- **순수 텍스트, CSV, Outlook 메시지에는 더 이상 "선택 영역
  마스킹"이 없습니다.** 이 형식들은 막대를 가질 수 없습니다 – 이
  버튼은 그곳에서 자리표시자를 넣으면서 그렇다고 알리기도 했습니다
  – 하지만 이름과 다른 일을 하는 버튼은 있어서는 안 됩니다.

- **도구는 이제 해당 위치에서 할 수 있는 일이 없을 때 그렇게
  알립니다.** 자리표시자는 다시 교체할 수 없고, 마스킹 위에는
  자리표시자가 놓이지 않으며, 이미 원본이 있는 곳에는 되돌릴 것이
  없습니다. 지금까지 이런 동작들은 효과가 있는 것처럼 보였지만
  실제로는 아무 효과가 없었습니다.

## 0.10.29-alpha.20260817 – 2026년 8월 17일

### 수정됨

- **나중에 손보기 창에서 이제 그리는 모든 사각형이 제대로 작동합니다.** 같은 자리를 두 번 손본 경우 -
  먼저 대체하고, 그다음 검게 칠하고, 그다음 원본을 되돌리면 - 두 번째와 세 번째 조작은
  아무 반응 없이 사라졌습니다. 이전 조작에서 여전히 잡을 수 있는 상태였던 사각형이 이를 가로챘기
  때문입니다. 도구를 바꿀 때도 마찬가지였고, 심지어 예전 도구가 조용히 계속 작동했습니다.
- **너무 좁게 그은 사각형은 이제 너무 좁다고 알려줍니다.** 지금까지는 미리보기에서 단어가
  빨갛게 표시되었다가, 마우스를 놓으면 아무 반응 없이 아무 일도 일어나지 않았습니다.

- **Outlook 메시지를 드디어 나중에 손볼 수 있습니다.** `.msg` 파일은 나중에 손보기 창에서
  「이 형식은 여기서 표시할 수 없습니다」라고 표시되었습니다 - 손으로 손볼 방법이 전혀 없는
  유일한 지원 형식이었습니다. 이제 발신인, 수신인, 제목, 본문이 이름과 함께 화면에 표시되며,
  다른 텍스트 형식과 마찬가지로 표시하고 대체할 수 있습니다.

- **「선택 영역 대체」는 이제 이메일에서도 선택한 부분만 다룹니다.** 본문에서 이름을 표시한
  경우 머리글의 발신인과 수신인까지 함께 사라졌고, 알림 메시지는 본문에 실제로 들어간
  자리표시자와 다른 것을 알려주었습니다. 이제 표시한 값은 - 발신인란에 있다면 그곳에서도
  포함해서 - 모든 곳에서 대체되며, 그 밖에는 아무것도 건드리지 않습니다.

- **여러 줄에 걸친 사각형이 더 이상 텍스트를 망가뜨리지 않습니다.** 지금까지는 한 자리에만
  자리표시자 하나가 생겼습니다: 잘린 단어의 나머지 부분이 그대로 붙어 있고, 두 번째 줄의
  텍스트는 흔적도 없이 사라졌습니다 - 자리표시자도, 검은 막대도 없이 그냥 빈 공간만
  남았습니다. 이제 각 줄은 그 줄에 실제로 있던 값이 담긴 자기만의 자리표시자를 가집니다.

- **「원본 되돌리기」는 이제 검게 칠한 뒤에도 작동합니다.** 창은 성공했다고 알렸지만
  텍스트는 다시는 돌아오지 않았습니다: 검은 막대가 장애물로 간주되어 되돌린 텍스트가
  들어갈 자리가 없었습니다. 이제 막대가 물러나고, 되돌린 텍스트는 자리표시자처럼 빨갛게가
  아니라 보통 텍스트처럼 검게 표시됩니다.

- **손대지 않은 자리에서 「원본 되돌리기」는 이제 아무 일도 하지 않습니다.** 아무것도
  바뀌지 않은 텍스트 위로 사각형을 그으면 텍스트가 제거되고 더 작아지고 위치가 옮겨진
  채 다시 삽입되었으며 - 성공이라고 알려졌습니다. 이제 되돌릴 것이 없다고 표시됩니다.

### 신규

- **Word, Excel, PowerPoint, OpenDocument, 텍스트에서도 검게 칠할 수 있습니다.**
  지금까지는 그곳에 「선택 영역 대체」만 있었습니다. 막대는 별다른 이유 없이 PDF
  보기 전용이었습니다. 막대를 표시할 수 없는 곳 - 순수 텍스트와 Outlook 메시지 -
  에서는 지금까지와 마찬가지로 값이 자리표시자로 대체되며, 알림에도 그렇게 표시됩니다.

- **자리표시자를 표시하면 되돌려집니다.** 텍스트 보기(Word, Excel, PowerPoint,
  OpenDocument, 텍스트)에서 이제 자리표시자를 표시하고 「선택 영역 대체」를 누르면
  됩니다: 원래 값이 돌아옵니다. 지금까지는 이를 위해 창이 트레퍼 패널을 안내했습니다.

- **회의록의 발언자는 이름이 동시에 일반 단어인 경우에도 인식됩니다.** 「Gruber: 인수는
  다음 주에 이루어집니다.」는 대체되었지만, 「Bauer: 저는 동의합니다.」는 그대로
  남았습니다 - 인식 입장에서는 성씨가 명사처럼 보이기 때문입니다. 같은 형태의
  주의 문구는 손대지 않은 채로 남습니다: 「주의: 설비를 꺼야 합니다.」에서는 이름이
  만들어지지 않습니다.

- **확인할 수 없었던 경우에도 「최신 버전을 사용 중입니다」라고 알렸습니다.** 업데이트
  서버가 요청을 거부하면 - 같은 인터넷 주소에서 너무 많은 요청이 왔거나 서버 자체에
  장애가 있어서 - 프로그램은 예전 버전에 멈춰 있으면서도 그것이 최신 버전이라고
  말했습니다. 정확히 이 일이 8월 17일 Mac에서 일어났습니다: 0.10.28이 몇 시간째
  준비되어 있는 동안 0.10.25가 그대로 남아 있었습니다.

  이제 창은 상황을 알려주고, 다음 확인 시각을 알려주며 - 자신의 버전이 최신인지
  **확실하지 않다**는 점을 명시적으로 알려줍니다.

  대개 이는 자신의 컴퓨터 탓이 아닙니다: 많은 인터넷 회선에서는 수많은 고객이
  같은 인터넷 주소를 공유하며, 서버는 이들을 합산해 셉니다. 그래서 이 경우
  Maskuro는 버전 목록을 **두 번째 경로**로 찾아보며, 대체로 그래도 새 버전을
  찾아냅니다. 그래도 거부당하면 서버는 알려준 시각까지 조용히 두며 - 버튼을
  다시 눌러도 마찬가지입니다. 재촉해도 차단 시간만 늘어날 뿐입니다.

- **수량 정보가 더 이상 지명으로 간주되지 않습니다.** 어느 근로계약서에서
  「Vier-Tage-Woche(주 4일 근무)」가 지명 자리표시자 뒤로 사라졌습니다 - 계약
  대상 한가운데서 말입니다. 이제 숫자와 하이픈으로 이루어진 이런 단어 결합
  (「Drei-Punkte-Plan」, 「24-Stunden-Dienst」)은 그대로 남습니다. 주소는
  여기서 제외됩니다: 「Zwei-Brüder-Weg」는 계속 대체됩니다.

## 0.10.28-alpha.20260817 – 2026년 8월 17일

### 변경됨

- **라이선스 자리가 이제 실제로 집계됩니다.** 지금까지는 어떤
  작업 PC도 라이선스 서비스에 등록된 적이 없었습니다 – 10자리 라이선스는
  아무도 모르는 채로 얼마든지 많은 컴퓨터에서 작동했습니다. 새로운 방식:
  프로그램을 시작하는 컴퓨터가 자리 하나를 차지합니다 – 자리는 **7일간
  시작이 없으면** 자동으로 다시 비게 되어, 고장 난 기기나 퇴사한 직원이
  자리를 영구히 막는 일이 없습니다.

  약간의 초과분은 **표시만 되고 잠기지는 않습니다** – 구매한 수량의
  10퍼센트까지는 모두 계속 작동합니다. 여전히 등록된 옛 노트북 옆의
  새 노트북이 지원팀에 전화할 사안이 되어서는 안 되기 때문입니다. 그
  이상 추가되면 무료 등급으로 전환되며 이를 안내받습니다 – 먼저 있던
  컴퓨터들은 아무 영향을 받지 않습니다.

- **구매한 라이선스는 정기적으로 스스로를 확인합니다.** 이것이 **30일**
  동안 이루어지지 않으면, 다시 성공할 때까지 무료 등급이 적용됩니다.
  아무것도 차단되지 않으며, 일주일 전부터 창에 안내가 표시됩니다.
  컴퓨터가 다시 인터넷에 연결되면 저절로 해결됩니다. 체험 기간과
  무료 등급은 여전히 아무것도 알리지 않습니다 – 구매하지 않는 사람은
  절대 연락받지 않습니다.

- **"인터넷 없이 활성화하기"가 드디어 작동합니다.** 지금까지는 활성화가
  검사되고 저장은 되었지만, 그 이후로는 아무도 그것을 읽지 않았습니다 –
  권한에는 아무 영향이 없었습니다. 이제는 네트워크 접속이 없는
  컴퓨터를 위한 해결책이 됩니다 – **1년간** 유효하며, 그 후에는 새로운
  요청 코드로 새 활성화를 받으면 됩니다. 이를 위해서는 인터넷이 되는
  기기가 1년에 한 번만 필요합니다 – 해당 컴퓨터 자체는 계속 오프라인
  상태로 남을 수 있습니다.

- **활성화는 이제 고객 계정에서도 가능합니다** – 웹사이트의 "내 라이선스"
  아래에서입니다. 그곳에는 또한 어떤 컴퓨터가 라이선스에 연결되어 있고
  언제 그 자리가 다시 비는지도 표시됩니다 – 지금까지는 어디에도 보이지
  않았습니다. 로그인 없는 페이지는 상점 계정이 없는 모든 사람을 위해
  그대로 남아 있습니다 – 대신 라이선스 키만으로는 부족하도록 주문 시
  사용한 이메일 주소를 추가로 요구합니다.

- **그리고 이제 창에 요청 코드를 어디로 보내야 하는지 표시됩니다.**
  종이 안내에는 "인터넷에 연결된 기기에 입력하세요"라고만 되어 있고
  주소는 없었습니다 – 활성화 페이지는 이미 오래전부터 있었지만
  어디에서도 연결되어 있지 않았습니다. 이제는 대화상자, 설명서, FAQ에
  **maskuro.com/lizenz-freischalten**이 표시되며, 웹사이트의 라이선스
  키 아래에도 표시됩니다.

- **"인터넷 없이 활성화하기…" 버튼은 이제 계속 표시됩니다** – 라이선스가
  지금 유효하지 않은 경우에도 마찬가지입니다. 이전에는 라이선스와 함께
  사라졌습니다 – 즉 정확히 필요할 때 사라졌습니다.

- **"모든 자리가 사용 중"이 이제 사실을 말합니다.** 안내 문구는
  "프로그램은 변함없이 계속 작동합니다"로 끝났습니다 – 자리가 배정되지
  않은 경우 이는 더 이상 사실이 아닙니다. 이제는 당분간 무료 등급이
  적용된다고 표시됩니다.

### 신규

- **클립보드 정리를 켤 때 이제 확인이 필요하다는 안내가 함께 표시됩니다.**
  이 메시지는 이제 파일 결과에도 나타나는 것과 같은 문장을 담고 있습니다:
  Maskuro는 모든 경우에 모든 개인정보를 인식하지는 못합니다.

  이곳에서는 다른 곳보다 그 무게가 더 큽니다. 파일의 경우 전달하기 전에
  결과를 볼 수 있습니다. 클립보드는 그렇지 않습니다 – 복사하고
  붙여넣으면 정리된 텍스트가 이미 메일 창에 들어가 있습니다. 그래서
  메시지는 명시적으로 **붙여넣은** 텍스트를 검토하라고 말합니다.

  이 메시지는 켤 때 나타나며, 복사할 때마다 나타나지는 않습니다 –
  하루에 50번씩 나타나는 것은 세 번째부터는 아무도 읽지 않기
  때문입니다.

- **목록 아래의 "모두 복사" – 그리고 "모두 제거"는 자리를 옮깁니다.**
  새 버튼은 완료된 모든 결과를 한 번에 클립보드에 넣습니다 – 메일에
  첨부하거나 다른 프로그램에 붙여넣기 위해서입니다. 지금까지는 메뉴를
  통해서만 가능했고, 그마저도 **선택된** 줄에만 적용되었습니다 – 전체를
  의미했던 사람은 먼저 Ctrl+A를 눌러야 했습니다.

  이와 함께 버튼 줄이 새로 정리되었습니다: 왼쪽에는 무언가를 추가하는
  것이, 오른쪽에는 간격을 두고 무언가를 제거하는 것이 있습니다. "모두
  제거"는 지금까지 "추가…" 바로 옆에 있었고, 실수 한 번이면 목록
  전체를 잃었습니다. 이 같은 규칙은 8월 13일부터 이미 모든 완료된
  줄에 적용되고 있습니다.

- **인터넷이 없는 작업 PC도 이제 사내에서 언어 모델을 받습니다.**
  정리 작업은 그곳에서도 항상 연결 없이 가능했습니다 – 언어 모델을
  나중에 내려받는 것은 그렇지 않았고, 모델 하나의 용량은 수백
  메가바이트에 달합니다.

  관리자는 연결이 되는 컴퓨터 한 대에서 파일을 한 번 모아 공유
  폴더, 롤아웃, 또는 USB에 올려둡니다. 위치는 중앙에서 등록됩니다
  (`vorgaben.json`의 `modellquelle` 필드 또는 환경 변수
  `MASKURO_MODELLQUELLE`). 이후로는 모든 나중 다운로드 – 언어 모델,
  일본어 사전, 고급 단계 – 가 먼저 그곳을 이용하며, 파일이 없을 때만
  네트워크로 나갑니다.

  이때 체크섬은 변함없이 적용됩니다. 사내 파일 공유는 인터넷상의
  릴리스보다 설명하기 쉬운 경우가 많습니다 – 그것이 몰래 바꿔치기된
  모델로 가는 더 편한 길이 되어서는 안 됩니다.

  이런 저장소를 어떻게 만드는지, 그리고 인터넷 없이 라이선스와
  활성화가 어떻게 작동하는지는 `OFFLINE.md`에 나와 있습니다.

- **"원본 되돌리기" – 프레임이 지나치게 제거된 것을 되돌립니다.**
  후속 수정 창에 새로운 도구가 생겼습니다: 해당 위치 위로 프레임을
  드래그하면 텍스트가 원본에 있던 그대로 다시 나타납니다.

  이는 검출 패널이 남겨두었던 빈틈을 메웁니다. 그곳에서는 자리표시자가
  명확한 경우에만 교체를 되돌릴 수 있었습니다 – 즉 익명화 시 이런
  종류의 모든 항목에 「[NAME]」이 표시되는 경우나, 자리표시자가 전혀
  남지 않는 마스킹된 부분에서는 불가능했습니다. 바로 그런 곳에서
  오탐이 모입니다: "사용자", "재고번호", "서명"은 흔히 이름으로
  오인됩니다.

  프레임은 자리표시자를 필요로 하지 않습니다 – **위치**는 사각형에서
  나오고, **내용**은 원본 파일에서 나옵니다 – 전후 비교 전환 기능이
  보여주는 것과 같은 파일입니다. 익명화되었는지 가명화되었는지는
  더 이상 중요하지 않습니다.

  되돌려진 텍스트는 빨간색이 아니라 검은색으로 표시됩니다 – 다시
  평문이며 자리표시자가 아니기 때문입니다. 검출 목록에서 항목이
  사라지는 것은 그 자리표시자가 문서 **어디에도** 더 이상 없을
  때뿐입니다 – 같은 값이 여러 곳에서 교체된 경우, 나머지에 대해서는
  계속 남아 있습니다.

  이미지로 변환된 페이지에서는 이 도구가 작동을 거부하며 이유를
  설명합니다 – 되돌려진 텍스트는 페이지 이미지 아래에 놓이게 되어
  보이지 않을 것이기 때문입니다.

### 수정됨

- **"세부 정보"와 "지표"를 접을 때 이미지 잔여물이 화면에 남아
  있었습니다.** 접으면 내용의 일부가 창 아래 가장자리 밑으로 밀려
  들어갔지만, 다른 무언가가 그 위에 그려질 때까지 배경 위에 그대로
  남아 있었습니다.

  두 영역 모두 열려 있을 때 충분히 쓸모 있는 크기가 되도록 최소
  높이를 가지고 있습니다. 접을 때의 움직임은 최대 높이만 낮췄습니다 –
  최소 높이 아래로는 영역이 줄어들지 않습니다. 그래서 창이 이미 24로
  줄어드는 동안에도 내용은 200포인트 높이를 유지했습니다 – 그 차이가
  가장자리 아래에 남아 있었습니다. 이제는 움직이는 동안 최소 높이가
  일시적으로 물러났다가 이후 다시 돌아옵니다.

- **창은 접었다 펼치는 것을 반복할 때마다 점점 작아졌습니다.** 펼칠 때는
  화면 높이의 최대 92퍼센트까지만 커집니다 – 공간이 부족하면 필요한
  것보다 덜 커집니다. 접을 때는 그럼에도 전체 분량을 그대로 빼버렸습니다.
  이제는 펼칠 때 든 만큼만 정확히 되돌려줍니다.

- **마스킹된 정보의 일부가 눈에 보이게 남아 있을 수 있었습니다.**
  한 이력서에서 "*30.12.1991"의 "*30.1"이라는 글자가 결과물에 읽을 수
  있게 남아 있었습니다 – 즉 생년월일의 날짜와 월 앞부분이었습니다.
  프로그램은 이 잔여물을 이미 인식해서 그 때문에 페이지를 이미지로
  변환했습니다 – 그런데 바로 그것이 상황을 더 나쁘게 만들었습니다.
  이로써 잔여물은 더 이상 검색되지는 않았지만 여전히 읽을 수 있었고,
  더 이상 고칠 수도 없었기 때문입니다.

  원인은 두 검사 사이에 있었습니다. 더 엄격한 쪽은 제거된 정보의
  영역 안에 그곳에 있어서는 안 되는 무언가가 남아 있는지 확인합니다 –
  이는 결과를 문자 위치의 집합으로 보고합니다, 교체 시 읽기 순서가
  달라지기 때문입니다. 이런 위치를 변환 전에 덮어 칠하는 대체
  경로는 이 문자 위치 집합을 페이지의 텍스트로 찾으려 했습니다 –
  그리고 결코 찾지 못했습니다. 그래서 아무것도 덮이지 않았습니다.
  이 위치는 처음부터 알려져 있었으며, 이제는 새로 찾는 대신 그대로
  전달됩니다.

  이 문제는 이 검사만으로 잔여물이 발견된 모든 페이지에 영향을
  미쳤습니다 – 파일 종류와 언어에 관계없이 발생했습니다.

- **가로로 넣은 스캔에서는 텍스트 인식이 아무것도 찾지 못했습니다.**
  용지를 옆으로 눕혀 급지대에 넣으면 글자가 90도 회전된 파일이
  만들어집니다. 지금까지 Maskuro는 그 안에서 **단 하나의** 정보도
  읽지 못했습니다 – 그리고 그 이후 파일은 눈에 띄지 않는 것처럼
  보였습니다: 아무것도 발견되지 않았으니 아무것도 보고되지 않았고,
  주소는 이미지 안에 계속 읽을 수 있는 상태로 남아 있었습니다. 이제는
  텍스트 인식이 페이지를 스스로 바로 세웁니다 – 검증 이미지에서도
  모든 정보가 다시 발견됩니다.

  두 가지 한계는 명시적으로 밝혀 둡니다: **거꾸로**(180도) 놓인
  용지는 여전히 읽지 못하며, 매우 상태가 나쁜 스캔에서는 바로 세우는
  것이 도움이 되지 않습니다 – 그곳은 방향조차 파악할 만큼 읽을
  수 있는 내용이 부족합니다. 이 처리에는 이미지마다 약 5분의 1의
  시간이 추가로 필요합니다.

### 변경됨

- **"자동 설치"는 이제 실제로 하는 일을 그대로 나타냅니다.** 설정의
  체크는 실제로 이행하는 것보다 더 많은 것을 약속했습니다 – 새 버전을
  스스로 내려받고 설치를 시작하지만, 이 설치는 **눈에 보이게**
  진행되며 확인이 필요합니다 – Windows에서는 사용자 계정 컨트롤의
  확인 질문까지 포함됩니다. "자동"이라는 표현을 읽은 사람은 밤사이
  스스로 업데이트되는 컴퓨터를 기대했고, 아침에 설치 마법사 앞에
  서게 되었습니다. 이 체크는 이제 "업데이트를 자동으로 내려받고
  설치 시작하기"라고 표시되며, 그 아래에 그것이 무엇을 의미하는지
  설명하는 문장이 있습니다. 동작 자체는 바뀌지 않습니다 – Maskuro가
  눈에 띄지 않게 스스로를 교체하지 않는 것은 의도된 것이며 그대로
  유지됩니다.

## 0.10.27-alpha.20260817 – 2026년 8월 17일

### 신규

- **신규: 로펌 소프트웨어 연동을 위한 `--ersetzen`.** 결과물이 원본
  파일 옆이 아니라 원본 파일 자리를 대신 차지합니다. 이를 통해
  로펌 소프트웨어의 체크아웃·체크인("전자 파일에서 열기 및
  편집")이 별도의 인터페이스 없이 작동합니다: 소프트웨어가 파일을
  내보내고, 같은 위치에서 정리된 상태로 다시 받습니다.

  **이 스위치는 첫 번째 원칙을 우회합니다.** 그래서 명령줄에서만
  존재하며 – 창에는 없습니다 – 관리자가 이를 허용한 경우에만
  작동합니다(설정 파일의 `ersetzen` 항목). 허용되지 않으면 호출은
  중단되며 그 이유를 알립니다 – 조용히 두 번째 파일을 만드는 것은
  더 나쁜 오류입니다, 그러면 변경되지 않은 파일이 다시 체크인될
  것이기 때문입니다.

  먼저 인접 파일에 기록되며, 완성된 후에야 원본 자리를
  차지합니다. 중단이나 오류가 발생해도 원본은 **바이트 단위로
  변경되지 않은 채** 남고 어떤 조각도 남기지 않습니다. 검증 로그에는
  이 대체가 별도 필드로 기록됩니다 – 검사자는 여기에 정리되지 않은
  버전이 더 이상 없다는 것을 알아야 합니다.

- **설명서가 이제 첫 실행 시 나타나는 Windows 경고를 설명합니다.**
  새로운 첫 절 "Windows가 첫 실행 시 경고합니다 – 어떻게
  해야 하나요", 이미지 두 장과 세 단계로 구성됩니다: "추가 정보"는
  버튼이 아니라 작은 링크입니다 – 대부분이 바로 여기서 막힙니다 –
  그다음 "실행"입니다.

  거기에 "알 수 없는 게시자"라고 표시되는 것 자체가 경고의 전체
  내용입니다: 패키지는 현재 인증서 없이 배포됩니다. 저희는 이를
  숨기기보다 설명하는 것이 옳다고 생각합니다.

- **역방향 경로는 이제 텍스트와 대응 파일이 서로 맞지 않을 때를
  알아챕니다.** 다른 작업의 응답을 붙여넣은 사람은 지금까지 올바른
  텍스트 안에 낯선 이름이 들어오는 결과를 받았습니다 – 오류도,
  메시지도 없이 그냥 틀린 상태였습니다. Maskuro는 이제 마지막
  실행이 만든 자리표시자를 기억해, 여기에 속하지 않는 모든
  자리표시자를 알립니다. 이 중 어느 것도 마지막 실행에서 나온
  것이 아니면 아무것도 삽입되지 않고 창이 그 이유를 알립니다 –
  지금까지처럼 기한이 만료되었다고 추측하는 대신입니다.

  **한 가지 한계는 남아 있으며, 설명서에도 적혀 있습니다:**
  자리표시자는 실행마다 번호가 매겨지므로, 첫 번째 이름은 모든
  문서에서 `[NAME_1]`이 됩니다. 낯선 텍스트가 이런 자리표시자만
  담고 있으면 혼동을 알아챌 수 없습니다.

- **PDF를 이제 흑백으로 출력할 수 있습니다.** 작동 방식의 체크 하나로
  모든 페이지가 흑백 이미지로 변환됩니다 – 아래에는 보이지 않는
  텍스트 레이어가 있어 계속 읽고 검색할 수 있습니다. beA와 크기
  제한이 엄격한 유사한 전송 경로를 위한 기능입니다: 저희 측정
  코퍼스 기준 평균 **68% 더 작아집니다**(명령줄: `--monochrom`).

  **얼마나 효과가 있는지는 문서에 달려 있습니다** – 이는 체크
  옆에도 표시됩니다: 스캔되고 이미지가 많은 문서는 크게 줄어들지만,
  삽입된 글꼴이 없는 가벼운 텍스트 문서는 오히려 더 커질 수도
  있습니다. 일괄 처리에 적용하기 전에 파일 하나로 먼저
  시험해보십시오.

  대가: 모든 페이지가 다시 계산됩니다 – 천 페이지에서는 몇 분이
  걸립니다. 그리고 도판은 흑과 백 사이의 모든 것을 잃습니다 –
  텍스트에는 상관없지만 사진에는 그렇지 않습니다.

- **후속 수정 창의 검출 목록이 이제 개수를 함께 표시합니다.** 목록
  위에 "검출 5건"이 표시되고, 필터링하면 "5건 중 1건"이 표시됩니다.
  이는 "필터링했다"와 "5건이며, 그 전부를 확인했다"의 차이입니다 –
  이름이 정말 모든 곳에서 교체되었는지 확인하는 방법입니다.

- **검증 로그를 이제 검색하고 필터링할 수 있습니다.** "파일 → 검증
  로그" 아래의 보기에는 지금까지 표만 있고 그 외에는 아무것도
  없었습니다 – 한 달에 3천 건이 실행된 경우, 많은 일이 있었다는
  것은 알 수 있어도 무슨 일인지는 알 수 없었습니다.

  새로 추가된 것은 **검색 필드**, **세 가지 필터**(방식, 결과,
  종류), **페이지 넘기기**, 그리고 지금까지 없던 세 개의 열입니다:
  **방식**(마스킹 또는 교체), **신뢰도**, **소요 시간**입니다.
  목록 위에는 현재 무엇이 보이고 필터가 얼마나 가리고 있는지
  표시됩니다.

  "CSV로 저장…"은 이제 **화면에 있는 그대로**를 출력합니다 – 필터를
  적용한 사람은 필터링된 결과를 얻고, 메시지가 그 개수를
  알려줍니다.

  신뢰도나 소요 시간이 줄표(-)로 표시되면, 이 줄에 대해서는 아무것도
  측정되지 않았다는 뜻입니다 – 예를 들어 이 기능이 생기기 전의
  기록이기 때문입니다. 이런 값은 사후에 계산되지 **않습니다**.
  사용자별 필터는 여전히 없습니다 – 그래도 검색을 통해 개별 줄은
  찾을 수 있습니다.

### 제거됨

- **"이 프로그램 정보" 창의 투명성 안내가 다시 사라졌습니다.**
  0.10.22-beta.1부터 그곳에 있었고, 이 애플리케이션이 인공지능의
  지원을 받아 개발되었다고 알렸습니다. 이는 어디에서도 요구되지
  않았고, 하필 개인정보 보호 애플리케이션에서 어떤 사람들은 이를
  작동 방식에 대한 진술로 읽었습니다 – 마치 문서가 인터넷의
  서비스로 전송되는 것처럼 말입니다. 정리 작업은 계속해서 오직
  본인의 컴퓨터에서만 이루어집니다 – 이는 있어야 할 곳, "개인정보
  보호" 탭에 있습니다.

### 수정됨

- **프로그램이 자신의 아이콘을 더 나쁜 것으로 바꿔치기했습니다.**
  프로그램 안에서 컨텍스트 메뉴를 등록한 사람은 이후 설치 직후와는
  다른 작업 표시줄 아이콘을 갖게 되었습니다 – 비슷하지만 가운데가
  아니라 왼쪽 정렬된 막대에, 눈에 띄게 거칠었습니다. 그 원인은
  임시방편에 있었습니다: 프로그램이 아이콘 원본을 찾지 못하면
  스스로 하나를 그립니다. 이는 아이콘이 **전혀 없는** 경우를
  위한 것이었는데, 실제로는 동봉된 아이콘이 이미 있는 경우에도
  그렸습니다 – 그리고 그것을 덮어썼습니다. 설치 프로그램으로 설치된
  버전에는 원본이 없어서, 그곳에서는 모두가 영향을 받았습니다.
  기존 아이콘은 이제 손대지 않고 그대로 유지됩니다.

  **이미 영향을 받은 설치는 올바른 아이콘을 저절로 되찾지
  않습니다** – 한 번 다시 설치해야 합니다.

- **"Objektkennung: OB-4711-22"가 로그인 이름으로 간주되었습니다.**
  사용자 이름 인식기가 자신의 레이블을 앞의 단어 경계 없이
  검사했습니다 – 즉 그 레이블로 **끝나는** 모든 단어가 걸렸습니다:
  Objektkennung, Fahrzeugkennung, Gerätekennung. 그 뒤의 값이
  제거되었지만, 이는 로그인 이름과 아무 관련이 없었습니다.

  실제로 의미가 있는 복합어 – "Benutzerkennung", "Anmeldekennung" –
  는 목록에 개별적으로 있으며 계속 발견됩니다.


- **영어, 그리스어, 일본어, 한국어에서는 16개의 자리표시자가
  결과물에 독일어로 표시되고 있었습니다.** 이 네 언어 중 하나로
  화면을 설정한 사람은 새로운 정보 종류에 대해 독일어 라벨이
  문서에 기록되었습니다 – 비밀번호는 `[CREDENTIALS_1]` 대신
  `[ZUGANGSDATEN_1]`이 되고, 진단 코드는 `[DIAGNOSIS_CODE_1]`
  대신 `[DIAGNOSESCHLUESSEL_1]`이 되었습니다. 영향을 받은 것은
  건강, 진단, 투약, 진단 및 처방 코드, 종교, 노조, 정치적 견해,
  형사 정보, 접속 정보, 사용자 이름, 카드 정보, 좌표, 직업, 금액,
  특징이었습니다.

  나머지 44개 언어는 이 오류를 겪은 적이 없습니다 – 이들은 처음부터
  이런 종류가 담겨 있던 언어 파일에서 라벨을 가져옵니다. 바로 이
  네 언어는 다른 이유로 자체 표를 갖고 있습니다 – 이들의 문자는
  PDF 문자셋에서 살아남지 못해 라틴 문자 라벨을 사용합니다 – 이
  표에는 단순히 새로운 종류가 빠져 있었습니다.

  카탈로그 페이지를 번역하다가 발견되었습니다: 웹사이트는 영어
  독자에게 프로그램이 기록하지 않는 라벨을 약속하고 있었습니다.
  이제 검사 도구가 이 네 개의 표를 생성 가능한 모든 라벨의
  목록과 대조합니다.

- **규칙 창이 내용에 비해 너무 작게 열리지 않습니다.** "사용자
  지정 검색 패턴" 탭에서 마법사의 설명 줄("검색 대상: …")이
  "테스트 텍스트" 필드 뒤에 절반쯤 가려져 있었습니다 – 하필이면
  정규 표현식 지식 없이도 자신의 규칙이 올바른 것을 찾는지 확인할
  수 있는 바로 그 문장이었습니다. 창은 탭이 더 적었던 시절의
  고정된 최소 크기를 가지고 있어서 내용보다 작게 조절될 수
  있었습니다. 이제는 내용에 맞춰 크기가 결정되며, 모든 것이 읽을
  수 있게 유지되는 한도 안에서만 작아집니다.

- **표 수식 안의 이름이 더 이상 그대로 남지 않습니다.** 셀에는
  텍스트가 들어갈 자리가 하나 이상 있는데, 지금까지는 그중
  하나만 정리되었습니다. 수식 안에 이름이 있거나
  (`="Frau "&"Sieglinde Ortner"`) 수식의 마지막 계산 결과였던
  경우, 옆 셀의 같은 사람이 대체되었음에도 통합 문서에는 그대로
  변경되지 않은 채 남아 있었습니다. 그 셀을 클릭하면 편집
  줄에서 그것을 읽을 수 있었습니다.

  이제 둘 다 대체됩니다. 처리되는 것은 따옴표 안에 있는 것뿐입니다
  – 셀 참조, 함수 이름, 시트 이름은 그대로 유지됩니다,
  `=SUMME(K2:K6)`는 계속 계산됩니다. 같은 이름이 어디서나 같은
  자리표시자를 받기 때문에, `=SUMMEWENN(A:A;"Huber";B:B)`도 계속
  자신의 줄을 찾습니다.

- **다이어그램에 이름이 더 이상 표시되지 않습니다.** 다이어그램은
  축 레이블의 자체 사본을 저장합니다 – 원본 셀이 오래전에
  비어있어도 계속 그려집니다. 막대 아래에는 그래서 계속 다섯 개의
  사람 이름이 있었지만, 그 위의 표는 깨끗했습니다. 표와
  프레젠테이션 **모두**에 적용됩니다.

- **고정 텍스트가 있는 이름이 붙은 범위가 정리됩니다.** 이름이
  붙은 범위는 셀 참조 대신 고정 텍스트를 담을 수 있습니다 – 그
  안에 이름이 있으면 그대로 남아 있었습니다. 범위의 **이름**
  자체는 계속 그대로 남습니다 – 수식이 이를 참조하며, 이름을
  바꾸면 참조 오류가 발생합니다. 시트 이름과 마찬가지로 이는
  보고되지만 교체되지 않습니다.

- **한 번 인식된 생년월일은 문서 전체에서 사라집니다.** 날짜
  자체만으로는 아무 뜻도 없습니다 – 필드 단어가 있어야 비로소
  생년월일이 되며, 바로 그래서 청구서 날짜는 그대로 남습니다.
  하지만 같은 정보가 같은 문서 안에서 이 단어 없이 두 번째로
  나타나면 – 이미지 제목이나 작성된 양식 필드에서 – 몇 줄 위에서
  "geboren am …"으로 확실히 인식되었음에도 그대로 남아
  있었습니다. 전달되는 것은 **이** 문서에서 이미 생년월일로
  인식된 것뿐입니다 – 계속 추측은 하지 않습니다.

- **웹페이지의 구조화된 데이터가 생년월일을 노출합니다.** 검색
  엔진용 JSON-LD 블록에는 `birthDate`라는 키 아래에 날짜가 있습니다
  – 이 키는 그것이 무엇인지 알려줍니다, 다른 곳의 열 제목처럼
  말입니다. 이제 이것도 함께 읽힙니다 – "Birthday"와 "Birthdate"도
  이제 양식의 필드 명칭으로 간주됩니다.

- **생년월일과 사번은 표에서도 발견됩니다.** 셀에는 값만 단독으로
  있습니다 – `14.03.1988`. 그것이 무엇을 의미하는지는 오직 열
  제목이 말해주며, 그것은 많은 줄 위쪽에 있습니다. Excel에서는
  이미 함께 읽혔지만, LibreOffice 표와 CSV 파일에서는 그렇지
  않아 생년월일이 그대로 남아 있었습니다.

  둘 다 이제 제목도 함께 읽습니다 – **단, 그 자체가 필드 명칭인
  경우에만**. "Geburtsdatum" 아래에서는 날짜가 처리되지만,
  "Rechnungsdatum"이나 "Lieferdatum" 아래에서는 그렇지 않습니다.
  이는 의도적으로 신중한 해석입니다 – "Name" 같은 제목이 사람이
  전혀 등장하지 않는 임의의 문장 위에 있으면, 자리표시자가
  잘못 적용되었을 것입니다.

### 수정됨

- **정리된 CSV는 계속 표로 유지됩니다.** 인식은 CSV 줄을 하나의
  문장으로 읽어서, 발견 위치를 세미콜론 너머로 확장하기도
  했습니다. 자리표시자가 구분 기호를 삼켜버려 줄에 열이 하나 줄고,
  파일을 더 이상 표로 열 수 없었습니다. 이제 발견 위치는 셀
  경계에서 끝나며, 마스킹 인용부호는 그대로 남습니다. 영향을 받은
  셀은 이후 다시 한번 개별적으로 읽힙니다 – 그렇지 않으면 너무 긴
  검출이 가렸던 인접 셀이 정리되지 않은 채로 남을 것입니다.

- **프레젠테이션의 주석.** 슬라이드에 붙은 여백 메모 – 종종
  "Bitte Frau … vor der Sitzung anrufen"이 적혀 있는 바로 그
  자리 – 는 작성자의 이름까지 그대로 손대지 않은 채 남아 있었습니다.
  Excel에서는 둘 다 이미 오래전에 정리되었습니다 – PowerPoint는
  주석 텍스트와 작성자를 다르게 저장하고 있었고, 이것이 간과되었습니다.
  두 가지 형식 모두에 해당됩니다 – 이전 형식과 PowerPoint가 2019년
  이후 작성하는 형식 – 후자에서는 작성자에 연결된 업무용 메일
  주소도 마찬가지입니다. PowerPoint가 말풍선에 표시하는 이니셜도
  함께 제거됩니다.

- **LibreOffice 파일: 수식, 사용자 필드, 메모 작성자.** Excel에서는
  이미 정리된 것이 ODS 표에는 그대로 남아 있었습니다 – 그곳에서는
  수식이 별도의 요소가 아니라 셀의 속성으로 저장되어, 그 안의
  이름이 그대로 살아남았습니다. 다음에 열면 LibreOffice가 이를
  다시 계산해냈습니다.

  추가로 세 가지가 더 있습니다: **사용자 필드**의 값은
  OpenDocument에서 선언 부분 상단에 한 번, 본문에서는 참조만
  됩니다 – 지금까지는 참조만 대체되어, 다시 열면 예전 값이
  돌아왔습니다. **메모의 작성자**와 추적된 변경 사항의 작성자도
  그대로 남아 있었습니다. 그리고 **표**에서는 변경 사항 추적이
  전혀 정리되지 않았습니다 – 텍스트 문서와는 다르게 – 삭제된 셀
  내용과 편집자 이름이 그대로 보존되었습니다. 셀 참조와 합계
  수식은 이때 손대지 않습니다.

- **저장된 웹페이지가 자신의 속성을 노출합니다.** 페이지는 자신이
  담고 있는 것을 전부 보여주지 않습니다. 작성된 양식 필드는
  입력 내용을 `value`에 담고, JavaScript 인터페이스는 자신의
  데이터를 `data-…`에 저장하며, 검색 엔진용 블록(JSON-LD)은
  이름, 생년월일, 주소, 전화번호를 완전하고 잘 정리된 형태로
  반복합니다. 눈에 보이는 텍스트는 정리되었지만, 이 모든 것은
  그대로 남아 있었습니다.

  이제 이런 위치도 함께 정리됩니다 – `aria-…`(화면 낭독기가
  읽어주는 내용), `placeholder`, `summary`, 그리고 참조의 제안된
  파일 이름도 포함됩니다. JSON-LD 블록은 이때 데이터로 읽혀
  유효한 상태로 유지됩니다 – 키와 어휘는 그대로 남고, 값만
  사라집니다. 일반 JavaScript는 계속 손대지 않습니다.

- **이미지는 EXIF가 없어도 부가 데이터를 잃습니다.** 사진에는
  촬영자 이름, 촬영 시각, 촬영 위치의 GPS 좌표가 함께 기록됩니다
  – 부동산 광고의 경우 텍스트에 주소가 없어도 이것이 주소를
  드러냅니다. 지금까지는 이미지에 EXIF가 있을 때만 제거되었습니다.
  이런 정보가 **오직** XMP로만 저장되어 있거나(Lightroom과
  Photoshop이 이렇게 저장합니다) PNG의 텍스트 블록으로 저장된
  경우(`Author`, `Comment`)에는 이미지가 전혀 손대지지 않았습니다.
  이제 둘 다 인식되어 제거됩니다 – 문서 안에 들어 있어 그 안에
  보존되는 이미지에도 마찬가지입니다. 방향 정보는 계속 유지되며,
  부가 데이터가 없는 이미지는 불필요하게 다시 저장되지 않습니다.

- **표, 프레젠테이션, Word 문서의 참조 대상.** 참조가 어디로
  이어지는지는 텍스트가 아니라 파일의 별도 저장소에 있습니다.
  "Mail schreiben" 뒤의 메일 주소는 그래서 텍스트 안의 같은
  주소가 대체되었음에도 정리 작업에서 손상 없이 살아남았습니다.
  `mailto:`와 `tel:`은 이제 저장된 웹페이지에서와 마찬가지로
  거기서도 정리됩니다.

### 신규

- **진료 편지가 더 이상 손상된 채로 돌아오지 않습니다.** 지금까지
  이름 인식은 의약품 성분명을 사람 이름으로 여겼습니다:
  "Metoprololsuccinat"이 `[NAME]`이 되고, "Ramipril"이 `[ORT]`가
  되었습니다. 진단명은 손대지 않고 그대로 남는 반면, 투약 계획은
  이로써 정확히 반대로 쓸모없게 되었습니다. 측정 결과 이는
  **성분명의 63%**와 **임상 전문 용어의 53%**에 영향을
  미쳤습니다 – 독일어뿐 아니라 7개 언어에서 74%, 이탈리아어에서는
  검사한 전부였습니다.

  Maskuro는 이제 의료 용어를 인식하고 그대로 둡니다. 남는 비율은
  독일어 43%에서 6%로, 언어 전체에서는 74%에서 1%로 줄었습니다.
  앞에 호칭이 있는 경우("Sehr geehrte Frau …") 이름은 계속
  이름으로 유지됩니다 – 우연히 약물 이름처럼 들리더라도
  마찬가지입니다.

- **원하는 경우 질병과 약물명을 제거할 수 있습니다.** 설정에
  새로운 체크: "질병과 약물명도 함께 제거"(명령줄:
  `--mit-diagnosen`). 인사 파일, 해고 서류, 감정서처럼 진단명이
  누구와도 상관없는 곳을 위한 것입니다.

  **기본값은 꺼짐이며, 의도적입니다:** 진료 편지는 진단명과
  성분명으로 *이루어져* 있습니다. 이를 익명화하는 사람 – 연구용,
  교육용, AI 도구용 등 – 은 대개 바로 이 내용을 유지하고 오직
  누구에 관한 것인지만 없애고 싶어합니다. 그곳에서 진단명은
  본질적인 내용이지 신원 표시가 아닙니다.

  인식은 일반적인 명칭을 찾을 뿐 검토를 대신하지는 않습니다:
  의사가 분류상 "알코올로 인한 장애"에 해당하는 것을
  "C2-Abusus"라고 쓸 수 있기 때문에, 질병 목록은 절대 완전할 수
  없습니다.

- **진단 코드와 처방 코드가 발견됩니다.** ICD-10(`I48.2`),
  ATC(`A10BA02`), 그리고 약품 중앙 번호는 서술된 진단명과
  마찬가지로 건강 데이터입니다 – 퇴원 요약서와 청구 서류에서는
  오히려 더 흔한 형태입니다. GDPR 제9조에 따른 다른 특수 범주와
  마찬가지로 기본값은 켜짐입니다.

  진단 코드는 근거가 있을 때만 인식됩니다: 앞에 "ICD"가 있거나
  진단 줄 뒤 괄호 안에 있을 때입니다. 이 조건이 없다면 프로그램은
  기능키 **F10**을 중독 진단으로 여길 것입니다 – 분류상 F10은
  정확히 그것입니다.

- **완료된 파일을 이제 복사할 수 있습니다.** 완료된 각 줄에는
  "보기", "후속 수정", "폴더에서 보기" 옆에 네 번째 버튼이
  있습니다: **복사**. 이는 정리된 파일을 클립보드에 넣습니다 –
  거기서 Ctrl+V(Mac: ⌘V)로 메일, 채팅 창, AI 도구에 붙여넣을 수
  있습니다, 폴더를 거치지 않고 말입니다.

  복사되는 것은 **파일**이지 텍스트가 아닙니다 – 페이지 레이아웃,
  이미지, 마스킹 막대가 그대로 유지됩니다. 목록의 컨텍스트
  메뉴를 통해 여러 개의 선택된 결과물도 한 번에 클립보드로
  이동할 수 있으며, "파일" 메뉴에도 같은 경로가
  **"결과 복사"**로 있어 키보드를 선호하는 사람도 사용할 수
  있습니다.

- **국가 선택이 이제 문서를 따를 수 있습니다.** 신분증, 사회보험,
  세금 번호는 나라마다 다르며, 어떤 나라가 검사되는지는
  지금까지 세션 전체에 걸쳐 고정되어 있었습니다 – 화면 언어에서
  도출되었습니다. 독일어로 작업하며 프랑스어 문서를 정리하는
  사람은 그 안에서 독일 세금 ID를 찾았지, 프랑스 사회보장번호를
  찾지 않았습니다.

  규칙 창에는 이제 이를 위한 **"문서의 언어에 따라 자동"**이
  있습니다. 고정 선택도 그 옆에 그대로 남아 있으며, 이는
  의도적입니다: 언어 인식은 완벽하지 않습니다 – 잘못 인식하면
  잘못된 국가 선택이 적용됩니다. 한 나라의 서류만 다루는 사람은
  고정 목록으로 더 안전하게 작업합니다.

  **독일어** 패턴(세금 ID, 자동차 번호, 내선번호)은 이에 영향을
  받지 않습니다 – 이들은 국가 선택이 아니라 언어에 연결되어 있어,
  짧은 독일어 텍스트가 영어로 분류되더라도 계속 적용됩니다.

- **비밀번호, 키, 로그인 이름이 이제 발견됩니다.** 오류 메시지,
  로그, 설정 파일의 일부를 AI 창에 복사하는 사람은 거의 항상
  그 안에 접속 키를 가지고 있습니다 – 지금까지는 이것이 변경되지
  않은 채로 남았습니다.

  둘 다 인식됩니다: 그 자체로 식별 가능한 흔한 키 형태(`sk-…`,
  `ghp_…`, `AKIA…`, `AIza…`, `xoxb-…`, JSON 웹 토큰, 개인 키의
  헤더), 그리고 레이블이 붙은 형태 – "Passwort:", "API-Key =",
  "Token:", "Benutzername:". 이때 값만 대체되고 레이블은 절대
  대체되지 않습니다 – "Passwort: [ZUGANGSDATEN_1]"은 읽을 수
  있게 유지되며, 결과를 검토하는 사람은 그곳에 비밀번호가
  있었다는 것을 알 수 있습니다.

  로그인 이름과 비밀번호는 두 개의 별도 종류입니다. 비밀번호만
  제거하고 싶은 사람은 하나를 끄고 다른 하나는 유지할 수
  있습니다.

- **이미지 안의 바코드와 QR 코드가 알아볼 수 없게
  처리됩니다.** 스캔된 통지서에는 거의 항상 코드가 붙어 있고,
  그 안에는 사건번호가 담겨 있습니다 – 옆의 텍스트에서 제거되는
  것과 같은 번호입니다. 지금까지는 기계로 읽을 수 있는 형태가
  그대로 남아 있었습니다 – 번호 위의 막대는, 2cm 옆에서 기기가
  1초 만에 그것을 읽어낼 수 있다면 아무 소용이 없습니다.

  QR 코드, Data Matrix, Aztec, Code 128, EAN 및 그 밖의 일반적인
  형태가 인식됩니다. 알아볼 수 없게 만든다는 것은 픽셀화를
  의미하며, 얼굴보다 더 거칠게 처리합니다 – 코드의 오류 정정
  기능은 남은 몇몇 필드만으로도 놀랍도록 많은 것을 복원해내므로,
  어중간한 흐림 처리는 제거가 아닙니다.

  이 옵션은 "얼굴 알아볼 수 없게 처리" 옆에 있으며 마찬가지로
  **기본값이 켜짐**입니다. 옵션이 꺼져 있어도 보고서는 얼마나
  많은 이미지가 코드를 담고 있는지 알려줍니다 – 얼굴은 넘겨볼
  때 눈에 띄지만, 코드는 부속물로 여겨지기 때문입니다.

- **카드 검증 번호, PIN, 만료일이 발견됩니다.** 신용카드 번호는
  이미 프로그램이 찾고 있었습니다 – 옆의 세 가지 정보가 있어야
  비로소 사용 가능해지며, 모든 청구서에는 이들이 함께 표시됩니다.
  셋 다 레이블 뒤에서만 인식됩니다 – "123"만 있으면 그것은
  번지수이거나 페이지 번호이거나 수량입니다.

- **텍스트 안의 좌표가 발견됩니다.** 이미지에서는 Maskuro가
  지금까지도 촬영 위치를 제거해 왔습니다 – 같은 정보가 감정서나
  출동 보고서에 텍스트로 있으면 그대로 남아 있었습니다. 십진도와
  도-분-초 표기법이 인식됩니다. 십진도의 경우 "Standort",
  "Fundort", "Koordinaten" 같은 단어가 근처에 있어야 합니다 –
  그렇지 않으면 소수점 두 자리를 가진 모든 측정값이 위치
  정보가 되어버릴 것입니다.

- **이제 금액도 함께 제거할 수 있습니다.** 새로운 체크 "금액도
  함께 제거", 위의 날짜 정보와 마찬가지로 기본값은 **꺼짐**입니다:
  계약서에서 금액은 본질적인 내용이며, 모두 마스킹하는 것은
  아무도 보호하지 못합니다. 반면 급여명세서, 화해 제안서,
  계좌 명세서에서는 금액이 옆의 이름보다 그 사람에 대해 더 많은
  것을 말해주는 바로 그 정보입니다 – 이는 그 문서를 직접 보는
  사람만 알 수 있습니다.

  금액은 **통화 표시가 있을 때만** 인식됩니다: "4.250,00"만으로는
  수량이고, "4.250,00 EUR"이 되어야 비로소 금액입니다. 통화
  기호, 약자, 완전히 쓴 이름이 앞뒤 어디에 있든 인정되며,
  "990,– CHF" 표기도 포함됩니다.

- **GDPR 제9조에 따른 특수 범주가 인식됩니다.** 종교, 노조 소속,
  정치적 신념, 건강 정보 – 그리고 제10조에 따른 형사 정보도
  함께입니다. 이들은 규정이 원칙적으로 처리를 **금지하는**
  데이터입니다 – 그래서 새로운 그룹 중 유일하게 기본값이
  **켜짐**입니다. 남기고 싶은 사람은 그렇게 결정할 수 있습니다.

  실제로 나타나는 형태로 인식됩니다: 인사 카드의 양식 필드 –
  "Religionsbekenntnis: röm.-kath.", "Gewerkschaft: ÖGB", "Grad
  der Behinderung: 50", "Vorstrafen: keine" – 콜론이 옆에 있는
  형태와 위에 레이블이 있는 형태, 즉 작성된 서류가 제공하는
  형태 모두를 인식합니다.

  **본문 텍스트는 AI 단계의 몫입니다.** "그는 수년간 노조
  활동을 해왔습니다"도 같은 정보이며, 어떤 검색 패턴도 이를
  신뢰성 있게 찾지 못합니다. AI 단계는 이번 버전부터 이런
  범주도 명시적으로 검색합니다 – 본문 텍스트가 필요한 사람은
  이를 켤 수 있습니다.

- **인물 특징과 직업 – 이름 없이도 누구를 가리키는지 드러내는
  정보입니다.** 성별, 혼인 상태, 신장, 눈과 머리 색은 이번
  버전부터 제거됩니다 – 직업, 직책, 부서는 원하는 경우, 별도의
  체크("직업과 부서도 함께 제거")나 `--mit-berufen`을 통해
  제거됩니다.

  **왜 하나는 켜져 있고 다른 하나는 꺼져 있는지:** "구매부
  부장"이라는 표현은 한 회사 안에서 정확히 한 사람을 지칭합니다,
  옆의 이름이 마스킹되어 있더라도 마찬가지입니다 – 감정서나
  해고 통지서에서는 이것도 제거되어야 합니다. 반대로 직원
  명부는 직책명으로 *이루어져* 있습니다 – 기본값으로 이를
  제거했다면 빈 종이만 돌려주었을 것입니다. 어떤 경우에
  해당하는지는 서류를 직접 보는 사람만 압니다. 위에 언급한
  특징들은 거의 항상 양식 필드에만 나타나고, 드물며, 절대
  본질적인 내용이 아닙니다 – 그래서 아무 대가도 들지 않습니다.

- **낯선 파일 검증하기.** "파일 → 파일 검증…"은 이미 마스킹된
  문서를 다시 읽어 아직 남아 있는 것을 알려줍니다 – **어느
  위치**인지도 함께: 페이지와 줄, 종류와 길이. 누군가 다른
  사람의 작업을 검토하는 경우를 위한 것입니다: 로펌에서 온 파일,
  관청의 회신, 발송 전 자체 발신 우편물 등입니다.

  **값 자체는 보고서에 표시되지 않습니다.** 해당 위치를 펼쳐보는
  사람은 어차피 그것을 볼 수 있습니다 – 그래서 이 보고서는
  그 자체로 개인정보 모음이 되지 않고도 저장하고 전달될 수
  있습니다.

  **그리고 보고서는 어떤 경우든 자신이 무엇을 볼 수 없었는지
  알려줍니다.** 이미지는 읽히지 않습니다 – 텍스트 레이어가 없는
  스캔의 경우 "발견 위치 없음"은 *검사되지 않음*을 뜻하지
  *깨끗함*을 뜻하지 않습니다. 명령줄에서는 반환값이 이를
  구분합니다: `--nachpruefen`은 검사되고 깨끗함이면 0, 발견
  위치가 있으면 4, 검사 불가능하면 5를 반환합니다. 이를 통해
  발신 우편물을 그냥 통과시키는 대신 자동으로 보류할 수
  있습니다.

- **검증 보고서: 정리마다 한 장씩.** "파일 → 검증 보고서
  저장…" – 또는 명령줄의 `--pruefbericht <ordner>` – 는 실행에
  대한 정보, 발견된 종류와 개수, 두 개의 지표, 검증 표시가
  담긴 한 쪽짜리 PDF(선택적으로 CSV나 텍스트)를 작성합니다.
  파일철과 감독 기관을 위한 것입니다: 검증 로그가 신뢰할 수
  있는 증거이지만, 아무도 JSON-Lines 파일을 제출하지는
  않습니다.

  **이때 새로 추가된 두 숫자**는 지금까지 어디에서도 볼 수
  없었던 것입니다: *평균 신뢰도* – 인식이 찾아낸 것에 대해
  얼마나 확신했는지 – 와 *마스킹 비율*, 텍스트 중 대체된 문자의
  비율입니다. 둘 다 각자의 한계와 함께 표시됩니다: 신뢰도는
  놓친 것에 대해서는 **아무것도** 말해주지 않으며, 그 옆에는
  항상 몇 건의 검출을 기준으로 한 것인지도 표시됩니다 – 비율은
  이미지를 계산에 넣지 않아 이미지가 많은 문서에서는 너무 높게
  나옵니다.

  **검출 값은 이 문서에 표시되지 않습니다** – 로그와 검색
  실행에서와 같은 한계입니다. 아래에는 같은 것을 말하지 않는
  두 줄이 있습니다: 체크섬은 이 문서가 변경되지 않았음을
  보여주고, 로그 줄(로그가 실행 중일 때만)은 이 실행을
  증명하는 **서명된** 줄을 가리킵니다. 오직 그것만이 출처를
  증명합니다.

- **"얼마나 확실했나요?" – 결과물의 지표.** 결과물 아래의
  "지표" 버튼은 지금까지 어디에도 보이지 않았던 것을
  펼칩니다: 발견 위치, 단어, 문자, 종류별 분포를 막대 줄로,
  그리고 평균 신뢰도와 마스킹 비율까지. 검증 보고서와 같은
  숫자지만, 즉시 인쇄 없이 볼 수 있습니다.

  **같은 화면에 그 한계도 함께 표시됩니다:** 신뢰도 옆에는
  몇 건의 검출을 기준으로 한 것인지가 표시되고, 그 아래에는
  놓친 것에 대해서는 **아무것도** 말해주지 않는다는 문장이
  있습니다. 이 문장 없이 백분율만 있으면 검출률처럼 읽히며 –
  그렇게 이해하는 사람은 숫자가 없는 것보다 더 나쁜
  상황입니다.

  계산은 펼칠 때만 이루어집니다: 마스킹 비율의 분모는 파일마다
  읽기 작업 하나를 필요로 하며, 이 숫자를 전혀 보지 않는
  사람이 그 비용을 지불해서는 안 됩니다.

- **작성하지 않고도 사용자 지정 검색 패턴을 만들 수
  있습니다.** "사용자 지정 검색 패턴" 탭은 이제 세 단계로
  안내합니다: *무엇을 찾으시나요? → 그런 정보가 실제로 어떻게
  생겼나요? → 이름을 붙이고 저장.* 예시를 하나 입력하면 –
  예를 들어 `KD-004711` – 프로그램이 그로부터 규칙을 도출하고
  무엇을 찾는지 말로 풀어씁니다. 검출 개수가 표시되는 미리보기가
  타이핑할 때마다 함께 확인합니다.

  **정규 표현식은 전혀 등장하지 않습니다.** 능력이 문제였던
  적은 없습니다 – 사용자 지정 검색 패턴은 오래전부터 있었지만,
  `\bKD-\d{6}\b` 같은 표현이 필요했고, 로펌이나 인사부서에서
  이를 직접 쓰는 사람은 없습니다. 직접 *쓰고 싶은* 사람은
  전문가 모드를 펼치면 됩니다.

  **템플릿 카탈로그가 새롭게 정리되었습니다:** 이름, 설명,
  예시 값이 있는 13개의 카드가 카테고리 태그 – 재무, 관청,
  연락처, 인사, 의료 – 로 필터링됩니다.

  그리고 도출된 패턴이 너무 넓게 걸리면 프로그램이 스스로
  알립니다: 숫자로만 이루어진 예시는 모든 연도와 모든 금액에
  걸리며, 표현식을 읽을 수 없는 사람은 이를 알아채지 못할 수도
  있습니다.

- **56개의 체크 표시 대신 7개의 태그.** 새로운 탭 "무엇이
  검색되는지"는 인식 가능한 모든 종류를 일곱 그룹으로
  묶습니다 – 인물, 연락처와 지명, 식별 번호, 재무, 기술, 특수
  범주, 회사와 사용자 지정. 태그 하나가 해당 그룹 전체를
  전환하고, "모두 켜기"와 "모두 끄기"는 목록 전체를 전환합니다
  – 그 아래에서는 각 종류를 개별적으로 체크할 수 있습니다.

  **기본값은 모두 켜짐이며, 계속 그렇게 유지됩니다.** 여기서
  꺼지는 것은 아예 검색되지 않습니다 – 규칙 창이 허용하는 가장
  큰 개입이며, 모든 문서에 영향을 미칩니다. 그래서 목록 아래에는
  언제나 몇 개의 종류가 꺼져 있는지 표시되고, 저장되는 것은
  꺼진 것뿐입니다: 새로운 종류는 그저께 만든 규칙 파일에서도
  자동으로 켜져 있게 되며, 조용히 빠지지 않습니다.

- **프레임을 모든 페이지에 적용.** 후속 수정 창에서 **모든
  페이지에 적용** 버튼은 마지막으로 드래그한 프레임을 가져와
  이후 모든 페이지의 같은 위치를 마스킹합니다 – 편지지 머리글,
  바닥글, 사건번호 필드를 위한 것입니다. 80페이지짜리 스캔
  파일에서는 이것이 20분을 2분으로 줄여줍니다.

  **"같은 위치"란 종이 위의 같은 *상대* 위치를 의미합니다.**
  급지대에서 나온 묶음에는 정기적으로 가로 페이지가, 다른
  페이지는 A3가, 또 다른 페이지는 회전되어 있습니다 – 절대
  좌표로 적용된 사각형은 그곳에서 편지지 머리글 옆에 놓이게
  됩니다 – 그러면 막대는 보이지만 일이 끝났다고 착각하게
  됩니다.

  **마스킹되지, 대체되지 않습니다** – 원래 프레임이
  자리표시자였더라도 마찬가지입니다: 같은 사각형 아래 40페이지에는
  1페이지와는 다른 것이 있으며, 같은 번호를 가진 자리표시자는
  존재하지 않는 동일성을 주장하게 됩니다.

- **마스킹 막대 위의 주석.** 문서 열람권에서는 각 마스킹 옆에
  왜 마스킹되었는지가 표시됩니다. 설정의 새로운 필드 **막대 위
  주석** – 또는 `--balkenvermerk` – 는 각 막대에 짧은 텍스트를
  기록합니다: "§ 203 StGB", "DSGVO", "vertraulich". 관청이
  발급하는 문서라면 이것이 차이를 만듭니다: 수신자는 어차피
  받지 못할 로그 없이도 그 이유를 볼 수 있습니다.

  **기본값은 비어 있습니다**, 주석은 발급된 문서에서 눈에 보이며
  그 자체가 정보이기 때문입니다 – 어떤 명목으로 무언가가 보류되는지
  수신자에게 알려줍니다. 이는 **마스킹**에만 작동합니다 – 자리
  표시자가 있는 곳에는 막대가 없습니다. 읽을 수 있는 텍스트가
  들어갈 만큼 크지 않은 막대에서는 생략됩니다 – 읽을 수 없는
  주석은 오류처럼 보입니다.

- **인터넷 연결 없이 활성화하기 – 이제 완전합니다.** 라이선스
  창에는 "인터넷 없이 활성화"가 이미 오래전부터 있었습니다 –
  위에는 가지고 나갈 요청 코드, 아래에는 돌아오는 활성화를
  입력하는 필드가 있었습니다. 다만 지금까지는 **아무도 이를
  발급할 수 없었습니다** – 이를 위한 도구가 없어서 코드는
  허공으로 사라졌습니다. 이제 해결되었습니다.

  차단된 컴퓨터를 사용하는 관청과 로펌에는 이것이 예외가 아니라
  일상입니다 – 그리고 "문서는 이 컴퓨터를 벗어나지 않습니다"라는
  약속이 가장 무겁게 작용하는 바로 그 대상입니다. 이 코드는
  문서에 대해 아무것도 알려주지 않습니다 – 라이선스 식별자와
  컴퓨터의 해시값만 담고 있습니다.

- **스캐너에서 가져오기.** "파일 → 스캐너에서 가져오기…"는
  묶음을 직접 읽어들여 목록에 페이지를 추가합니다 – 우편실
  입장에서는 두 단계와 한 단계의 차이입니다. 급지대는 마지막
  페이지까지 비워지며, 기기, 해상도, 색상은 이미 익숙한 스캐너
  시스템 대화상자에서 선택합니다.

  **자동으로 정리되지는 않습니다.** 먼저 무엇이 들어왔는지
  확인한 뒤 다른 파일과 마찬가지로 "정리"를 눌러야 합니다 –
  자동으로 진행되는 스캔은 비뚤어지게 들어간 묶음을 미리 볼
  기회를 앗아갈 것입니다.

  **이 기능은 Windows에서만 제공되며**, Mac에서는 메뉴 항목이
  이를 알립니다: 그곳에서는 스캐너 소프트웨어가 폴더에 저장하고,
  "폴더 감시…"가 그곳에 도착하는 모든 것을 정리합니다.

### 그 밖에

- **발견되는 모든 정보의 목록이 이제 함께 제공되며**
  (`hilfe/GEFUNDENE-ANGABEN.md`) 소스코드에서 생성됩니다: 35개국
  177개 종류, 그중 23개는 체크섬 검증을 포함합니다. 어떻게
  집계했는지도 알려줍니다 – 저희는 `[NAME]`을 한 번으로
  집계하지만, 다른 곳에서는 이름, 미들네임, 성을 세 개의 항목으로
  집계합니다.

- **마스킹은 이제 Word, PowerPoint, OpenDocument, HTML에서도
  가능합니다.** 자리표시자와 마스킹 사이의 선택은 지금까지
  PDF 파일에만 있었습니다. 이제는 다른 형식도 가능합니다:
  검출이 제거되고 그 자리에 검은 막대가 놓입니다 – 문서 자체
  안에, 그 위의 이미지가 아니라 말입니다. 파일을 전달하는
  사람은 마스킹된 파일을 전달하는 것이지, 마스킹된 부분이
  여전히 텍스트로 그 아래에 깔려 있는 파일을 전달하는 것이
  아닙니다.

  **선택은 별도로 이루어집니다**, 두 개의 선택 필드로: "PDF의
  경우"와 "Word, PowerPoint, OpenDocument, HTML의 경우". 이는
  서로 다르게 원하는 경우가 많기 때문입니다 – 마스킹된 PDF는
  관청으로 가고, 같은 내용의 Word 파일은 사내에서 계속 돌아다니며
  읽을 수 있는 상태를 유지해야 합니다. 명령줄에서는 각각
  `--pdf-modus`와 `--office-modus`입니다 – 이전 버전에서 저장된
  "마스킹"은 계속 PDF에 적용됩니다.

  표, 순수 텍스트, CSV, 이메일에서는 막대를 사용할 수 없습니다
  – 그것을 놓을 면적이 없기 때문입니다. 그곳에는 계속
  자리표시자가 사용되며, 결과물은 이제 그것을 조용히 하는
  대신 **알려줍니다**.

- **신규: "제거" – 발견 위치가 그냥 비워집니다.** 자리표시자와
  마스킹 옆의 세 번째 작동 방식이며, **모든** 형식에서 가능한
  유일한 방식입니다: 무언가를 생략하는 데는 면적이 필요하지
  않습니다. PDF에서는 아무것도 그려지지 않고, Word와 HTML에서는
  그 자리가 비어 있으며, 표에서도 마찬가지입니다.

  이는 세 가지 중 가장 조용한 방식입니다: 결과물을 읽는 사람은
  그곳에 무언가가 있었다는 것을 알 수 없습니다 – 값의 길이조차
  더 이상 드러나지 않습니다. 누군가 검토해야 하는 서류에서는
  자리표시자가 대개 더 나은 선택으로 남습니다.

  이미지에서는 이 세 가지 선택 중 어느 것도 적용되지 않습니다:
  픽셀은 자리표시자로 대체할 수도, 생략할 수도 없습니다. 그곳에서
  텍스트 인식이 찾아낸 것은 지금까지처럼 항상 덧칠됩니다.

- **후속 수정 창은 더 이상 존재하지 않는 교체를 주장하지
  않습니다.** 오른쪽에는 각 값마다 자리표시자가 표시되었습니다
  – 그 안에 하나도 없는 마스킹된 파일에서도 마찬가지였습니다.
  그런 줄을 클릭해도 아무것도 표시되지 않았고, "되돌리기"는
  헛수고였습니다. 이제는 "마스킹됨" 또는 "제거됨"이 표시되며,
  이런 줄은 애초에 되돌릴 수 없습니다: 텍스트가 사라졌으니
  되돌릴 것이 없습니다. 이는 마스킹된 PDF 파일, Word와
  OpenDocument, 그리고 이미지에서 발견된 모든 것에 적용됩니다.

- **텍스트 보기가 이제 막대를 막대로 보여줍니다.** 마스킹된
  Word 파일은 후속 수정 시 **비어** 보였습니다: 마스킹된
  위치에는 프로그램이 텍스트를 삼켜버린 것처럼 빈틈만
  있었습니다. 원인은 표시 방식이지 결과물이 아니었습니다 –
  문서 자체에는 막대가 처음부터 올바르게 있었습니다. 이제는
  보기에도 결과물처럼 검게 표시됩니다 – Word, PowerPoint,
  OpenDocument, HTML에서 모두 마찬가지입니다.

- **Outlook 메시지(`.msg`)가 이제 정리됩니다.** `.eml`은
  이미 오래전부터 있었습니다 – 하지만 독일 기업에서는 Outlook이
  곧 이메일이며, 그곳에서는 저장된 메시지가 `.msg`라고
  불립니다. 이로써 가장 밀도 높은 개인정보 형식이 가장 널리
  쓰이는 저장 형태에서도 다뤄집니다: 제목, 발신자, 수신자 줄,
  메시지 본문, HTML 버전, 수신자 목록, 첨부파일 – 첨부파일은
  기존 경로를 통해, 메일 본문과 같은 자리표시자로 처리됩니다.

  **`.msg`는 같은 텍스트를 여러 번 담고 있으며**, 이것이
  함정입니다: 순수 텍스트, HTML, **그리고** RTF로 말입니다.
  순수 텍스트만 정리한 사람은 아무것도 하지 않은 셈입니다 –
  Outlook은 RTF를 우선 표시합니다. 그래서 RTF 버전은 완전히
  제거되며, Received 체인이 담긴 인터넷 헤더와, 어떤 텍스트
  정리도 견뎌내는 이름과 주소가 담긴 바이너리 검색 키도
  마찬가지로 제거됩니다. 결과물은 계속 Outlook에서 열리며,
  서식 없이 텍스트를 표시합니다 – 보고서가 이를 명시적으로
  알립니다.

- **정규 표현식을 작성하는 대신 규칙을 자신의 말로 설명하기.**
  규칙 창은 많은 것을 할 수 있지만 이를 위해 정규 표현식
  패턴이 필요했습니다 – 대부분의 사람이 여기서 멈추는 지점입니다.
  이제는 한 문장이면 충분합니다: "12 C 345/26 형식의 저희 사건
  번호는 그대로 남아야 합니다." AI 단계가 이로부터 용어와
  검색 패턴을 제안합니다.

  **여러분이 체크한 것만 적용되며 – 기본값은 아무것도 체크되지
  않은 상태입니다.** 각 제안에는 그것이 무엇을 의미하는지에
  대한 문장과, 여러분이 함께 제공할 수 있는 예시 텍스트에서의
  검출 횟수가 표시됩니다. 보호를 **제거**하는 것은 그렇게
  표시됩니다: "이 용어는 항상 제거"와 "이 용어는 절대 제거
  안 함"은 목록에서 그렇지 않으면 똑같아 보일 것입니다. 무엇에나
  걸릴 만한 제안은 애초에 표시되지 않습니다.

- **검증 로그가 이제 모든 작업 PC에 걸쳐 합산됩니다.** 어떤
  조직이 `protokoll_pfad`를 통해 로그를 공유 폴더에 두면, 각
  작업 PC가 자신의 월간 파일을 그곳에 기록합니다 – 지금까지는
  30개 자리를 가진 개인정보 보호 담당자가 30개 파일을 개별적으로
  확인해야 했습니다. 목록 위에는 이제 그 달의 합계를 보여주는
  줄이 있고, **이름과 함께 끊어진 체인을 알려줍니다**: 사후
  변경은 누군가가 확인해야만 드러나는데, 30개 파일을 수동으로
  확인하는 사람은 아무도 없습니다.

  **인물별 집계는 없습니다** – 이 보기에서도 마찬가지입니다.
  "누가 얼마나 정리했는지"에 대한 순위는 행동 및 성과 통제에
  적합할 것이고, 공동결정법상 중요한 것은 바로 이 사실이지
  의도가 아닙니다. 집계되는 것은 조직 전체에 걸친 실행, 파일,
  검출 건수입니다.

- **"서류에서 프로필 제안하기": 44개 종류를 일일이 확인하는
  대신 규칙에 한 번만 물어보기.** 규칙 창에 새로운 버튼이
  있습니다: 이는 AI 단계에 서류를 보여주고, 그것이 무엇인지 –
  진료 편지, 지원서, 계약서, 청구서, 통지서 – 를 판단한 뒤, 그에
  맞는 전략을 제안합니다. 예를 들어 진료 편지에서는 날짜 정보가
  대체되는 대신 이동됩니다 – 진료 기록에서는 시간 순서가
  본질적인 내용이기 때문입니다.

  **프로필은 프로그램 안에 있으며, 모델은 그중에서 선택할
  뿐입니다** – 마스킹 규칙은 언어 모델이 좋은 아이디어라고
  여기는 것에 좌우되지 않습니다. 각 항목은 개별적으로, 근거와
  함께 제안됩니다 – 확인 없이는 아무것도 적용되지 않으며,
  여러분이 직접 정한 것은 손대지 않은 채로 남습니다. AI 단계
  없이는 안전한 기본값 – 모든 것에 자리표시자 – 이 계속
  적용됩니다.

- **새로운 전략 "꾸며내기": 자리표시자 대신 그럴듯한 가짜
  값.** "[NAME_1]이 [NAME_2]에게 [ORT_1]에서 편지를 보냈습니다"
  대신 "Frau Berger schrieb an Herrn Doppler in Fulda" –
  교육 자료, 시연용 파일, 테스트 데이터셋, 그리고 이후 AI에
  제시될 모든 것을 위한 것입니다. 호칭, 문장 구조, 가독성이
  유지됩니다.

  같은 값은 같은 가짜 값을 받습니다 – 한 작업의 모든 파일에
  걸쳐, 그리고 같은 규칙 파일을 가진 모든 컴퓨터에서 – **어디에도
  대응 관계가 저장되지 않은 채로**(해시 처리와 같은 방식입니다).
  이메일 주소는 예약된 예시 도메인에 있고, 전화번호는 이를
  위해 남겨진 영역에 있으며, 꾸며낸 IBAN은 올바르게 계산된
  체크섬을 갖습니다. 이름, 지명, 주소, 회사, 이메일, 전화,
  IBAN에 가능합니다 – 다른 종류에는 이 규칙이 효과 없이 남는
  대신 거부됩니다.

  **보고서는 꾸며냈다는 사실을 명시적으로 알립니다.** 이렇게
  정리된 문서는 진짜처럼 읽히지만 진짜가 아닙니다 – 증빙으로는
  쓸 수 없으며 원본으로 전달되어서는 안 됩니다.

- **대조 검사: "누가 계속 식별 가능한가요?"** AI 단계 아래의
  새로운 체크는 **완성된 결과물**을 다시 한번 언어 모델에
  제시하며, 정리에도 불구하고 누가 식별 가능한지 묻습니다.
  이는 세상의 어떤 인식도 찾을 수 없는 경우를 위한 것입니다,
  그곳에 이름 자체가 없기 때문입니다: "관할구역의 유일한
  산파", "3월 화재 이후 사직한 동료." 어떤 패턴도 걸리지
  않지만, 현장에서는 누구나 누구인지 압니다.

  **이 과정에서 아무것도 제거되지 않습니다.** 해당 위치는
  근거 문장과 함께 보고서에 표시되며, 결정은 사람이 직접
  내립니다 – 스스로 서류 안의 문장을 의심스럽다는 이유로
  가져가는 프로그램은 정리를 다시 쓰기로 만들어버리고, 무엇이
  빠졌는지 아무도 알 수 없게 됩니다. 파일당 최대 5개 위치까지 –
  모델이 문자 그대로 증명할 수 없는 것은 제외됩니다. 명령줄에서는:
  `--restrisiko`를 `--ki`와 함께 사용합니다.

- **AI에서 돌아오는 길: "응답 되돌려 번역".** 지금까지는
  절반의 순환만 구축되어 있었습니다 – 텍스트 복사, 정리된
  내용 붙여넣기, AI에 제시. 응답은 `[NAME_1]`이 담긴 채로
  돌아왔고, 이를 사용해야 하는 사람은 손으로 뺐던 것을 손으로
  다시 넣어야 했습니다. 이제 "프로그램" 메뉴에 돌아오는 경로가
  있습니다: 응답 복사, 항목 클릭, 실제 이름이 다시 그
  자리에 나타납니다.

  이를 위한 대응 관계는 **오직 메모리에만** 있으며, 항상
  마지막으로 정리된 곳에만 적용되고, 한 시간 후 만료됩니다 –
  클립보드 감시를 끄는 사람은 즉시 이를 잃게 됩니다. 되돌릴
  수 있는 것은 대체된 것뿐입니다 – 마스킹, 마스크 처리, 해시
  처리된 것은 되돌릴 수 없으며, 프로그램은 그 때문에 얼마나
  많은 위치를 그대로 두어야 했는지 알려줍니다. 관리형 설치에서는
  `rueckweg` 설정을 통해 이 되돌리기 경로를 완전히 끌 수
  있습니다.

- **폴더 감시: 넣은 것은 곧 정리된 상태로 출구 폴더에
  놓입니다.** 우편실, 사서함 팀, 스캔 폴더를 위한 것입니다 –
  한 번 설정하면 그 후로는 아무도 클릭할 필요가 없습니다.
  "파일 → 폴더 감시…"에서 찾을 수 있으며, 명령줄에서는
  `--wache <ordner>`입니다.

  원본은 있던 자리에 그대로 남으며, 원하는 경우 변경 없이
  "완료됨" 하위 폴더로 이동합니다 – 아무것도 덮어써지지
  않습니다. 파일은 완전히 기록된 뒤에야 처리됩니다 – 네트워크로
  아직 복사 중인 파일이라면 절반만 읽힌 상태로 정리된 것으로
  보고될 수 있기 때문입니다. 잘못된 것은 그대로 남고 알려지며,
  무한히 반복되지 않습니다. 그리고 감시 기능은 파일 이름 없이
  완료 항목을 기억합니다 – 수신함에 있는 것은 종종 파일 이름
  만으로도 무슨 내용인지 드러내기 때문입니다.

  **본인 사용자 프로필 밖의 폴더 – 예를 들어 네트워크 드라이브 –
  를 감시하려면 자동화 라이선스가 필요합니다.** 여러 사람이
  접근할 수 있는 폴더는 서비스이지 개인 작업 공간이 아닙니다 –
  자신의 프로필 안이나 체험 기간 동안에는 이 제한이 적용되지
  않습니다.

### 수정됨

- **설정이 오른쪽에서 잘려 있었습니다.** 창은 고정된 크기로
  열렸고, 개발에 사용된 글꼴 크기에만 맞았습니다 – Mac에서는
  "지금 확인", "변경…"과 그 옆의 안내가 절반쯤 화면 밖에
  있었습니다. 이제는 각 페이지가 필요로 하는 만큼 넓게 열립니다
  – 모든 언어와 모든 글꼴 크기에서, 화면 크기로만 제한됩니다.

- **"지금 확인"이 이제 눈에 보이게 응답합니다.** 결과는 메인
  창의 상태 표시줄에 표시되었습니다 – 즉 질문이 이루어진
  설정 창 뒤쪽이었습니다. 확인한 사람은 아무것도 보지
  못했습니다. 이제는 설정 창 위에 메시지로 응답이 표시되며,
  새 버전이 있으면 바로 설치로 이어집니다. 프로그램 시작 시에는
  지금까지처럼 상태 표시줄에 남으며, 묻지 않고 창이 열리는
  일은 없습니다.

- **Mac에서 복사된 파일이 클립보드에 도착하지 않았습니다.**
  정리된 파일을 클립보드에 다시 놓는 작업은 성공을 보고했지만
  실제로는 쓸모 있는 것을 저장하지 않았습니다 – 붙여넣어도
  아무것도 나오지 않았습니다. 파일을 클립보드에 기록하는 모든
  기능이 영향을 받았습니다.

- **그리고 Mac에서는 클립보드에서 첫 번째 파일만 읽혔습니다.**
  Finder에서 파일 세 개를 복사하고 "클립보드 지금 정리"를 선택한
  사람은 그중 두 개를 정리되지 않은 채로 돌려받았습니다 – 아무런
  안내도 없이 말입니다. 이제는 전부 처리됩니다.

- **"파일 검증"이 이제 드래그한 파일도 받아들입니다** – 메인
  창과 마찬가지로. 놓은 파일은 기존 선택을 버리는 대신
  추가됩니다 – 같은 파일을 두 번 놓아도 변화가 없으며,
  프로그램이 읽을 수 없는 파일은 삼켜지는 대신 알려집니다.

- **그리고 창이 여러분을 기다리고 있다고 알립니다.** 빈 상자와
  회색 "검증" 버튼으로 열렸습니다 – 이는 선택이 없는 것이
  아니라 아무것도 없는 것처럼 보였습니다. 이제는 "아직 선택된
  파일이 없습니다 – 여기로 드래그하거나 아래의 '파일 선택…'을
  통해 고르십시오"라고 표시됩니다.

- **긴 실행이 이제 실행 중이라고 알립니다.** "정밀 인식용 추가
  모델을 불러오는 중 – 잠시만 기다려 주세요…"는 인식이 계산하는
  동안 계속 표시되었습니다 – 47,500 단어짜리 파일에서는 18분
  동안, 로딩 자체는 9초 만에 끝났음에도 그랬습니다. 이를 본
  사람은 프로그램이 멈췄다고 여기게 됩니다. 이제는 그 뒤에
  "정밀 인식 작동 중 – 긴 텍스트에서는 몇 분이 걸립니다"가
  이어지고, 상태 표시줄이 함께 집계합니다: "정밀 인식
  (7/312)". 이는 모델의 반복 루프에서 보고됩니다 – 250단어마다,
  즉 약 6초마다 – 텍스트 블록 단위가 아닙니다: 텍스트 블록 하나가
  1만 2천 단어를 담고 몇 분이 걸릴 수 있기 때문입니다.

- **중단된 실행이 이제 중단되었다고 알립니다.** "중단"을 누른
  사람은 이후 "0/1개 파일 정리됨"을 읽었습니다 – 정확히
  집계되었지만 잘못된 정보였습니다. 어떤 파일에서 발생했는지
  알리는 메시지가 같은 순간 집계 메시지로 덮어써졌습니다.
  그리고 파일 목록에는 더 이상 아무것도 실행되지 않는데도
  "실행 중…"이 계속 표시되었습니다 – 이제는 "중단됨"이
  표시됩니다.

- **개인정보 보호에 대한 문장이 잘려 있었습니다.** "… 클라우드
  없음, 업로드 없음. 자세한 내용은 개인정" – 프로그램이
  시작될 때의 창 너비에서는 단어 중간에서 끝났습니다. 이제는
  전체 너비를 사용합니다.

- **라이선스 서비스가 무언가를 알릴 수 있었지만 아무도
  듣지 않았습니다.** 모든 라이선스 자리가 사용 중이거나,
  라이선스가 만료되었거나, 키를 알 수 없거나, 공급업체의
  라이선스 관리가 꺼져 있으면, 서비스는 정확히 그 이유를
  전송합니다 – 처음부터 이를 **한 번** 설명받도록 예정되어
  있었습니다. 하지만 표시된 적이 없었습니다. 이제는 프로그램이
  변함없이 계속 작동한다는 것을 먼저 알리고, 그다음 무엇이
  문제인지 알리는 안내가 표시됩니다. 이유마다 한 번씩입니다 –
  닫아버린 사람은 매일의 검사에서 다시 보지 않지만, 이유가
  바뀌면 다시 보게 됩니다.

- **상점에서 구매한 다중 자리 라이선스가 "1자리"로
  표시되었습니다.** 상점은 준비된 키를 배포하고 구매한 자리
  수를 자체적으로 관리합니다 – 하지만 표시된 것은 키 자체에서
  나온 숫자였고, 이는 모든 재고 키에서 1자리로 되어
  있습니다. 8자리를 구매한 사람은 "1자리"라고 읽었습니다 –
  그리고 두 번째로 등록된 컴퓨터부터는 표시가 빨간색으로
  바뀌며 "관리자에게 문의하세요"라고 나왔습니다. 이제는 서비스가
  마지막으로 보고한 숫자가 적용됩니다 – 응답이 없으면 키의
  값이 유지되며, 구매한 범위보다 작아지는 일은 절대 없습니다.
  추가 구매와 연장에도 마찬가지입니다: 이들은 공급업체 쪽에서
  자리 수를 변경할 뿐 키 자체는 바뀌지 않습니다.

- **구매 후 "Maskuro 개인 라이선스에 라이선스됨"이라고
  표시되었습니다.** 이는 이름이 아니라 키가 준비될 때 사용되는
  자리표시자입니다 – 여러분의 이름은 그곳에 있을 수 없습니다,
  키가 구매 전에 이미 서명되기 때문입니다. 낯선 이름을
  여러분의 이름인 것처럼 보여주는 대신, 이제는 단순히
  "개인 라이선스"와 자리 수가 표시됩니다. 본인 이름으로 발급된
  라이선스에서는 이름이 그대로 표시됩니다.

- **도움말 메뉴에 "Hilfe _FAQ"라고 표시되어 있었습니다.**
  Qt가 & 기호를 키보드 단축키 표시로 읽어서 밑줄로
  바뀌었습니다. 이제는 "Hilfe & FAQ"로 표시됩니다.

- **프로그램이 트레이 아이콘으로 사라지면 설정 창이 그대로
  남아 있었습니다** – 메인 창이 닫힌 후에도 마찬가지였습니다.
  이제는 함께 사라집니다. (이 버전에만 해당됩니다 – 이 별도
  창은 새로 생긴 것입니다.)

- **거부된 라이선스 요청이 이제 그 이유를 알려줍니다.**
  라이선스 서비스가 이유 없이 요청을 거부하면, 라이선스 창에는
  빨간색으로 "알 수 없는 응답."이라고 표시되었습니다 – 여러분도
  지원팀도 이 문장으로는 아무것도 할 수 없고, 이는 여러분이
  자신의 키에서 오류를 찾도록 만듭니다. 이제는 실제로 무슨
  일이 일어났는지 표시됩니다: 서비스가 이유 없이 거부했다는
  것과 누구에게 연락해야 하는지가 표시됩니다. 공급업체의
  라이선스 관리가 일시적으로 꺼져 있는 경우에도 이것이
  명시되며, 여러분의 키는 그 영향을 받지 않는다는 안내도
  함께 표시됩니다.

- **Mac에서는 설정된 언어가 갑자기 빠진 것으로 간주되었습니다.**
  시작할 때 프로그램은 "설치된 언어 모델이 없습니다"라고
  알리며 초기 설정을 제안했습니다 – 언어가 이미 오래전에
  로드되어 있었음에도 말입니다. "문서 언어"에서 확인해 보면
  모두 다 있었습니다. 프로그램은 시작 경로에 따라 두 개의 다른
  위치에서 이를 찾았습니다: 프로그램 폴더에서 시작하면 찾았지만,
  같은 빌드를 단순 폴더로 실행하면 아무것도 없는 자신 옆에서
  찾았습니다. 이제는 Mac에서 프로그램이 어떻게 패키징되었든
  예외 없이 사용자 프로필의 같은 위치를 사용합니다. 아무것도
  다시 다운로드할 필요가 없습니다.

- **"새로운 기능"이 목록 절반만 표시했습니다.** 업데이트 후
  창이 문장 중간에서 끊겼고, 남은 항목들은 빈 글머리 기호로
  표시되었습니다. 원인은 꺾쇠괄호 안의 자리표시자 – 예를 들어
  `<datei>.docx` – 였습니다. 표시 기능은 이를 마크업으로
  간주해 그 이후 모든 것을 버렸습니다. 하필이면 보안에 관한
  새 기능이 이 영향을 받았습니다. 도움말은 이런 자리표시자를
  예전부터 올바르게 표시해 왔습니다 – 이제는 이 창도 그렇게
  합니다.

- **두 손가락으로 꼬집는 동작이 이제 후속 수정 창에서 확대를
  실행합니다.** 트랙패드에서는 이것이 *바로* 확대 제스처입니다 –
  편집기에서는 지금까지 아무 일도 일어나지 않았고, 특정 위치를
  자세히 보고 싶은 사람은 슬라이더나 Ctrl+마우스 휠을 써야
  했습니다. 페이지는 이제 제스처를 즉시 따라가며, 손을 떼면
  다시 선명하게 그려집니다.

- **보고 있는 위치를 기준으로 확대됩니다.** 꼬집기는 손가락
  사이의 점을 기준으로 확대되고, Ctrl+마우스 휠은 커서 아래
  지점을 기준으로 확대됩니다. 버튼, 단축키, 확대 슬라이더는
  중심을 고정합니다 – 이들에는 가리키는 위치라는 것이 없기
  때문입니다. 이전에는 모든 방식에서 스크롤 값만 그대로
  유지되었습니다: 맞춰진 페이지에서는 이것이 상단 가장자리를
  유지했고, 그 아래의 모든 것은 확대할 때 화면 밖으로
  밀려났습니다.

- **"전후 비교"는 페이지 보기에서 죽은 버튼이었습니다.** 페이지
  보기가 켜져 있는 동안에는 눌러졌지만, 매번 원본을 열 수
  없다고 알렸습니다. 그곳에는 비교할 것도 없습니다: 페이지
  보기는 정리된 버전의 이미지이며, 원본에 대응되는 것이
  없습니다. 이제 이 버튼은 비활성화되며, 마우스를 올리면
  그 이유와 대안(텍스트 보기)을 알려줍니다. 이 버튼의 설명은
  게다가 "텍스트 보기든 페이지 보기든 관계없이" 비교가
  이루어진다고 명시적으로 약속하고 있었습니다 – 이는 한 번도
  사실이 아니었습니다.

- **페이지 보기가 LibreOffice를 충돌시켰습니다.** 두 개의
  페이지 보기가 동시에 생성되면 – 예를 들어 미리보기가 아직
  계산 중인데 "PDF로 마스킹"을 실행하면 – 시스템은
  LibreOffice 충돌을 알렸지만 결국 페이지는 나타났습니다:
  두 실행 모두 LibreOffice의 같은 작업 저장소에 접근했고,
  이는 허용되지 않습니다. 이제는 항상 한 실행만 그것을 사용하며,
  나머지는 자체 저장소로 우회합니다. 그 대가로 몇 초가 더
  걸리지만 더 이상 오류 메시지가 나타나지 않고, 어떤 실행도
  결과 없이 끝나지 않습니다. 이미 실행 중인 작업 옆의 두 번째
  렌더링 요청은 아예 받아들여지지 않습니다.

- **"원본 보기"가 프로그램을 종료시킬 수 있었습니다.** 원본을
  열 수 없는 경우 – 이동되었거나, 이름이 바뀌었거나, 암호가
  걸려 있거나, 분리된 드라이브에 있는 경우 – 후속 수정 창이
  예고 없이 중단되고, 열려 있던 작업 사본을 잃었습니다. 이제는
  안내가 표시되고, 스위치가 되돌아가며, 정리된 버전은 그대로
  유지됩니다. 원본이 근본적으로 옆에 맞지 않는 경우 – 예를 들어
  Word 파일에서 생성된 PDF 페이지 보기 – 스위치는 처음부터
  비활성화되어 있으며, 마우스를 올리면 그때마다 같은 안내를
  보여주는 대신 이유를 알려줍니다.

- **오류 보고서가 전혀 도착하지 않았습니다.** 오류를 신고하려던
  사람은 "상대측이 보고서를 거부했습니다"라는 메시지를 받았고
  – 아무도 이를 본 적이 없었습니다. 두 가지 원인이 있었고, 둘
  다 전송 경로에 있었습니다: 프로그램이 서버에 자신을 증명하지
  않아 대량 접근 방지 기능에 의해 거부되었고, 주소는 프로그램이
  따라가지 않는 두 번째 이름을 가리키고 있었습니다. 둘 다
  해결되었고, 보고서가 다시 나갑니다. **라이선스 활성화도
  마찬가지였습니다**: 등록, 해지, 조회가 서비스에 도달하지
  못했습니다 – 다만 그곳에서는 눈에 띄지 않았습니다, 응답 없는
  요청이 의도적으로 라이선스를 바꾸지 않기 때문입니다. 그리고
  거절이 여전히 설명되지 않는 경우, 이제는 모든 원인이 같아
  보이는 대신 기술적인 번호가 함께 표시됩니다.

- **"원본 보기" 클릭이 프로그램을 종료시킬 수 있었습니다.**
  원본을 열 수 없는 경우 – 이동, 이름 변경, 분리된 네트워크
  드라이브, 암호, 손상 등 – 후속 수정 창이 열려 있던 모든
  작업 사본과 함께 사라졌습니다. 이제는 전환 스위치가 정리된
  버전에 머무르고, 상자가 무슨 일인지 알려줍니다 – 신고하고
  싶은 경우를 위해 기술적인 이유가 세부 정보에 표시됩니다.
  표시할 수 없는 결과물에도 마찬가지로 적용됩니다: 창이
  열리고 그것을 알리는 대신 사라지지 않습니다.

- **충돌 여부를 묻는 질문이 너무 자주 나타났고 – 그것이
  묻는 흔적을 지워버렸습니다.** 아무것도 충돌하지 않았을
  때도 나타났습니다: 이 기록은 어딘가에서 예상치 못한 오류가
  발생하는 즉시 생성되며, 프로그램이 이를 견뎌내고 그 후
  정상적으로 종료되더라도 마찬가지입니다 – 한 번도 정리된
  적이 없었습니다. 그리고 "아니오"라고 답한 사람은 그 사건의
  유일한 세부 정보를 파괴했습니다 – 기록은 질문이 *표시되는*
  순간에 이미 사라졌습니다. 둘 다 해결되었습니다: 정상적인
  종료는 기록을 정리하고, 실제 중단에 대해서만 질문하며, 표시는
  여러분의 응답 후에야 이루어집니다. 세부 정보는 어차피 본인
  컴퓨터의 오류 로그에 남아 있습니다 – 아무것도 보내고 싶지
  않은 사람도 이로 인해 잃는 것은 없습니다. 전송되는 것은
  여전히 여러분이 미리 완전히 보고 직접 승인한 것뿐입니다.

- **"정리"가 조용히 잠긴 채로 남아 있을 수 있었습니다.**
  언어 모델이 로딩 중 멈추면 버튼이 설명 없이 비활성화된
  상태로 남았습니다. 클릭해도 아무 반응이 없었고, 상태
  표시줄은 10분이 지나도 변함없이 "언어 모델을 불러오는 중…"을
  표시했습니다. 원인: 백그라운드 작업의 오류가 파일 관리자에서
  시작할 때는 아무도 보지 않는 곳으로 전달되었습니다 – 남은
  것은 작업할 준비가 된 것처럼 보이지만 어떤 클릭에도 반응하지
  않는 창이었습니다. 이제는 이런 오류가 오류 로그에 기록되고,
  언어 모델 로딩은 조용히 포기하는 대신 어떤 경우든 실패를
  알리며, 그래도 조용히 남아 있으면 45초 후 애플리케이션이
  무언가 잘못되었다고 알리며 세부 정보에 조언을 표시합니다.
  잠긴 버튼은 마우스를 올리면 그 이유를 알려줍니다. 첫 로딩이
  길게 걸리는 것은 이때 침묵으로 간주되지 않습니다 – 진행
  상황이 보고되는 한 조용히 유지됩니다. 이 모든 것은 충돌로
  집계되지 않습니다 – 애플리케이션은 계속 작동하며, 그래서
  다음 시작 시에도 이에 대해 묻지 않습니다.

- **Mac에서는 프로그램이 더 이상 업데이트를 찾지 못했습니다
  – 그리고 최신 상태라고 알렸습니다.** Mac 버전에는 루트
  인증서 목록이 포함되어 있지 않았습니다 – 이를 빌드된
  컴퓨터에만 존재하는 위치에서 찾고 있었습니다. 그래서 어떤
  서버와도 상대가 누구인지 확인할 수 없었고, 모든 연결을
  중단했습니다: 업데이트 없음, 라이선스 활성화 없음, 언어
  모델 다운로드 없음, 오류 보고 없음. 이전 버전들은 이를 조용히
  "최신 버전을 사용 중입니다"로 바꿔버렸습니다. 인증서는 이제
  프로그램 자체 안에 있으며, 그곳에서 찾지 못하면 시스템의
  인증서를, Mac에서는 최후의 수단으로 키체인의 인증서를
  사용합니다 – 그것마저 전혀 없으면, 최신 버전이라고 주장하는
  대신 그 사실을 알립니다. 이 검사 자체는 절대 꺼지지
  않습니다.

  이 업데이트 한 번만큼은 Mac 사용자가 직접 설치해야 합니다:
  서버에 도달하지 못하는 버전은 스스로를 갱신할 수도
  없습니다.

### 변경됨

- **메인 창이 정리되었습니다.** 아래쪽에는 같은 크기의
  버튼 여섯 개가 나란히 있었습니다 – "정보…", "안내서",
  "도움말 & FAQ"도 그 아래에 있었는데, 같은 세 가지 경로가
  이미 위쪽 도움말 메뉴에 있었습니다. 이제는 이들이 펼쳐지는
  "도움말" 버튼 하나로 통합되었습니다 – 사라지는 것은 없습니다.
  아래쪽에는 실제로 시작할 때 쓰는 두 가지 경로만 남습니다:
  "정리"와 "수동으로 마스킹…".

- **프로그램이 지금 무엇을 하고 있는지가 이제 고정된 위치에
  표시됩니다.** 메시지("언어 모델을 불러오는 중…", "(3/7)
  brief.pdf", "7개 중 5개 파일 정리됨.")는 지금까지 두 줄의
  버튼 사이에 회색 텍스트로 걸려 있었습니다. 이제는 자체
  영역을 얻었고, 앞에 색이 있는 점이 붙습니다: 아무것도
  실행되지 않을 때는 회색, 작업 중에는 파랑, 정상적으로
  완료되면 초록, 안내가 있으면 노랑입니다. 이 점은 옆에
  적힌 것 외의 어떤 정보도 알려주지 않습니다 – 그저 더 빨리
  알려줄 뿐입니다.

- **설정이 독립된 창이 되었습니다.** 지금까지는 메인 창 안에
  있었습니다 – "추가 설정" 아래에서 펼치는 4개 탭이 있는
  상자였고, 그 내용에 비해 너무 작았습니다: 그 안에는 항상
  스크롤바가 있었고, 익명화와 가명화 사이의 선택은 화면
  절반 밖에 있었습니다. 이제 버튼은 "설정…"이라고 표시되며
  사이드바가 있는 창을 엽니다 – 네 개의 페이지 각각이 완전히
  들어맞습니다. 메인 창은 열릴 때 더 이상 팝업하지 않으며,
  파일 목록을 옆에서 함께 볼 수 있습니다. 바뀐 것은 설정이
  어디에 있는지뿐입니다 – 무엇이 있고 무엇을 하는지는
  그대로입니다.

- **"세부 정보"는 튀어 오르는 대신 펼쳐집니다.** 지금까지는
  창이 한순간에 커졌고, 무엇이 바뀌었는지 찾아야 했습니다.
  이제는 그곳으로 움직입니다.

- **글꼴 크기와 여백이 창 전체에서 같은 기준을 따릅니다.**
  제목은 두 곳에서 크기가 달랐고, 같은 등급의 줄이 서로
  다른 간격으로 떨어져 있었습니다. 이는 눈에 띄는 변화가
  아니라 안정감으로 나타납니다.

- **익명화가 이제 기본값입니다.** 지금까지는 가명화가
  기본값이었습니다: 같은 사람은 같은 번호(`[NAME_1]`,
  `[NAME_2]`)를 받아 관계가 읽을 수 있게 남았지만, 법적으로는
  여전히 **개인정보**였습니다. 아무것도 설정하지 않는 사람은
  이제 GDPR의 범위에서 데이터를 벗어나게 하는 방식을 얻습니다:
  같은 종류의 모든 검출이 같은 이름(`[NAME]`)을 갖습니다.
  번호 매기기는 여전히 선택할 수 있으며 같은 창에 변함없이
  있습니다 – 기존 설정은 그대로 유지됩니다. 명령줄에서는
  `--pseudonymisieren`(또는 `--mit-nummerierung`)이 되돌립니다.

- **익명화된 자리표시자는 더 이상 개별적으로 되돌릴 수
  없습니다.** 익명화하면 모든 사람에 대해 같은 자리표시자를
  받습니다 – 그래서 특정 이름에 속하는 개별 위치가 더 이상
  존재하지 않습니다. 그럼에도 후속 수정 창은 "교체
  되돌리기"를 제공했습니다: 클릭하면 *하나의* 값이 *모든*
  위치에 삽입되었을 것입니다. 이런 줄은 이제 마스킹된 정보와
  마찬가지로 흐리게 표시되고, 클릭하면 이유가 표시되며, 수동으로
  추가된 검출은 문서 다른 곳 어디에도 없는 번호를 더 이상
  받지 않습니다.

  같은 이유로 익명화된 실행 후에는 "응답 되돌려 번역"도 더
  이상 없습니다 – 이전에는 각 사람의 자리에 낯선 이름 하나가
  들어갔을 것입니다. 이 순환이 필요한 사람은 "가명화"를
  선택합니다 – 애플리케이션은 이제 만료된 대응 관계를
  가리키는 대신 이를 그렇게 알립니다.

  명령줄에서는 `--zuordnung`이 익명화 시 이제 대응 번역이
  아닌 파일을 쓰는 대신 중단됩니다 – 창에서는 이 체크가
  이미 오래전부터 비활성화되어 있었습니다. `--pseudonymisieren`을
  함께 사용하거나 `--zuordnung`을 생략해야 합니다 – 메시지가
  이를 알려줍니다. 이때 결과물 자체가 생성되지 않아, 스크립트가
  절반만 작업된 상태로 남지 않습니다.

- **업데이트 채널이 새롭게 "안정"으로 설정됩니다.** 자체적으로
  선택하지 않으면 지금까지는 현재 실행 중인 버전이 어떤
  빌드에서 왔는지에 따라 채널이 정해졌습니다 – 한 번이라도
  테스트 버전을 시도한 사람은 그 후 계속 테스트 버전을
  제공받았습니다. 채널 전환은 하나의 결정이며 계속 결정으로
  남아야 합니다 – 그래서 기본값은 "안정"입니다. 설정된 채널은
  그대로 유지됩니다.

### 개선됨

- **"Beschwerdevorgang"이 더 이상 지명으로 간주되지 않습니다.**
  "Aktennotiz – Beschwerdevorgang 12 C 345/26"이라는 제목에서
  프로그램은 이 사건까지 함께 마스킹했습니다: 언어 모델이
  이를 지명으로 여겼고, 문맥과 무관하게 그랬습니다. 추가된
  것은 개별 단어가 아니라 합성어의 **기본 단어**입니다 –
  "vorgang"과 "notiz"는 이로써 사업, 회계, 결제 사건이나
  전화 메모도 함께 포함합니다. 검사한 30개 행정 용어 중 이전에는
  3개가 오탐을 일으켰지만, 이제는 하나도 없습니다 – 옆에 있는
  것은 여전히 모두 발견됩니다("Beschwerdevorgang: Bernd
  Meisinger"는 이름은 잃지만 제목은 잃지 않습니다).

- **익명화가 다시 기록을 남깁니다 – 후속 검증과 로그를
  위해서입니다.** 익명화 작동 방식에서는 프로그램이 발견된
  값을 기억하지 않았습니다. 이로 인해 두 가지가 침묵했습니다:
  문서 전체의 일관성 후속 검증(나중에 단독으로 나타나는 성이
  그대로 남는 문제)과 검증 로그의 교체 목록입니다. 익명화가
  더 드문 선택이었을 때는 이것이 거의 눈에 띄지 않았습니다 –
  기본값이 되면서 이것이 일상적인 경우가 되었을 것입니다.
  문서에서는 아무것도 바뀌지 않습니다: 자리표시자는 계속
  번호 없이 남습니다.

- **"Kein personenbezogenes Datum"이 이제 "keine
  personenbezogene Angabe"로 바뀌었습니다.** 되돌리기
  대화상자와 얼굴 경고에서는 법률 용어 *Datum*(복수형
  "Daten"의 단수형)이 사용되었습니다. 이는 날짜로 읽혔습니다,
  특히 애플리케이션이 다른 곳에서 "날짜 정보도 함께 제거"를
  제공하기 때문입니다. 이제는 어디서나 같은 창의 위쪽 네
  가지 이유와 마찬가지로 "Angabe"라고 표시됩니다.

- **출처 문구는 이제 "정보" 창에만 있습니다.** "Made with ♥
  in Austria"는 메인 창 아래 버튼 줄 한가운데에 있어서 마치
  또 다른 버튼처럼 보였습니다. 이제는 계속 "정보" 창에
  있습니다 – 사람들이 찾아볼 만한 곳입니다.

- **드롭 영역에 이제 눈에 보이는 경계가 있습니다.** 점선
  테두리는 너무 희미해서 창과 거의 구분되지 않았습니다 –
  이 영역이 단순한 면적일 때는 상관없었습니다. 탭 키로 이동할
  수 있는 버튼이 된 이후로는 이 선이 이를 조작 요소로 보여주는
  유일한 것입니다 – 그래서 규정이 요구하는 값으로 밝기가
  높아졌습니다.

## 0.10.22-beta.1 – 2026년 8월 15일

### 신규

- **클립보드 감시를 끄면 정말로 꺼집니다.** 감시 기능은 원본을
  되돌릴 수 있도록 최근 내용을 메모리에 보관합니다 – 지금까지는
  감시를 꺼둔 경우에도 마찬가지였습니다. 이제는 끌 때 이력이
  삭제됩니다. 끈 이후에는 복원할 수 없게 되며, 바로 그것이
  의도입니다 – 꺼짐은 꺼짐을 의미합니다.
- **오류 로그에 더 이상 파일 경로가 담기지 않습니다.** 이 로그는
  본인 컴퓨터에만 남아 있었고 자동으로 전송된 적도 없었지만, 경로를
  평문으로 기록했으며 파일 이름이 내용보다 더 많은 것을 드러내는
  경우가 많습니다. "…/Scheidung_Mueller_Vergleich.docx"는 이제
  기록 시 `<datei>.docx`가 됩니다 – 확장자는 문제 해결에 도움이
  되므로 남습니다. 충돌 후의 기록도 마찬가지입니다.
- **교체 목록이 이제 스스로 경고합니다.** 이는 원본 데이터가
  평문으로 담긴 유일한 파일이며, 결과물 옆에 있습니다 – 폴더를
  전달하는 사람은 이것도 함께 전달합니다. 이제는 파일 **안**의
  첫 줄에 경고가 표시되고, 출력 영역에는 파일 이름 대신 전체 경로가
  표시되며, 명령줄에서도 처음으로 이 파일이 언급됩니다 – 지금까지는
  그것이 생성되었다는 사실조차 알 수 없었습니다.
- **익명화 또는 가명화가 이제 이름이 있는 선택 사항이 되었습니다.**
  지금까지 이 자리에는 "같은 이름을 같은 이름으로 표기 – 그러면
  AI가 누가 누구인지 계속 알 수 있음"이라는 체크가 있었습니다. 이는
  이점을 설명했지만 결과는 숨겼습니다 – 번호가 매겨진 자리표시자
  (`[NAME_1]`, `[NAME_2]`)는 **가명화**이며, 가명화된 데이터는
  여전히 개인정보입니다 – 이것으로 익명화했다고 믿었던 사람은
  틀렸습니다. 이제는 두 방식이 각각의 대가와 함께 나란히
  제시됩니다. 기본값은 가명화로 유지됩니다 – 이후에도 읽히거나
  AI가 처리할 문서는 그 관계 정보가 필요하기 때문입니다. 익명화 시
  교체 목록은 잠깁니다 – 그렇지 않으면 결과를 다시 원래대로 되돌릴
  수 있게 되기 때문입니다. 설명서와 FAQ는 18개 언어 모두에서 이
  차이를 설명하며, 명령줄에서는 이제 이 스위치를
  `--anonymisieren`이라고도 부릅니다.
- **드롭 영역 위의 문구가 이제 실제로 맞는 말을 합니다.** 지금까지
  "100% 로컬 처리 – 클라우드와 계정 없이, GDPR 친화적"이라고
  약속했습니다. 문서에 대해서는 맞지만, 프로그램 자체에 대해서는
  이렇게 일반화하기 어렵습니다 – 업데이트를 확인하고, 원하는 경우
  오류를 보고하고, 모델을 추가로 내려받고, 구매한 작업 자리를
  등록합니다. 이제는 더 좁고 실제로 유효한 문구가 표시됩니다:
  문서는 이 컴퓨터를 벗어나지 않습니다.
- **결과물에는 이제 항상 검토가 필요하다는 안내가 표시됩니다.**
  지금까지 Maskuro는 정상적인 실행 후 초록색으로 "정보 12건
  제거됨"만 표시하고 그 외에는 아무것도 표시하지 않았습니다 – 이는
  모든 것을 찾았다는 보증처럼 읽힙니다. 안내는 구체적으로 무언가를
  검사할 수 없었을 때만(이미지, 알 수 없는 첨부파일) 나타났습니다.
  이제는 모든 결과물 아래에 눈에 띄게, 모든 개인정보가 항상
  인식되는 것은 아니며, 검토는 사용자의 몫이고, 빠진 부분은 직접
  보완해야 한다는 문구가 표시됩니다 – 창, 출력 영역, 명령줄
  모두에서입니다. 닫아서 없앨 수 있는 알림 창이 아닙니다 – 이 문장은
  항상 그 자리에 있습니다. 간단 안내서도 이제 같은 문구를
  사용합니다.
- **업데이트 후 시작할 때 무엇이 바뀌었는지 표시됩니다.** 지금까지는
  업데이트가 조용히 이루어져 재시작과 구분할 수 없었습니다. 이제는
  "새로운 기능"이 한 번 나타납니다 – 여러 버전을 건너뛴 경우 그
  사이의 내용도 함께 보여줍니다. 맨 처음 실행할 때는 나타나지
  않습니다 – 거기서는 계속 간단 안내서가 안내를 담당합니다.
- **중국어와 일본어에서 이제 이름을 찾습니다.** 지금까지는
  **전혀** 찾지 못했습니다 – 적게가 아니라 전혀입니다. 두 언어
  모델 모두 단어 분리 기능이 없어서, 공백이 없는 문장은 하나의
  단어로 취급되었습니다 – 프로그램은 조용히 다국어 대체 모델로
  전환했습니다. 이제 두 언어 모두 다른 언어와 마찬가지로 사람과
  지명을 인식합니다. 일본어 사전은 언어와 함께 내려받으며 프로그램
  안에 포함되어 있지 않습니다 – 그것만으로도 200MB에 달해, 그렇지
  않으면 모두가 이를 짊어져야 했을 것입니다.
- **루마니아가 국가로 선택 가능합니다.** 지금까지는 전혀 없었습니다.
  이로써 루마니아 주소("Strada Victoriei 30"), 지명이 포함된
  우편번호("010061 București"), 그리고 개인 식별번호(Cod Numeric
  Personal)가 인식됩니다 – 후자는 체크섬이 맞는 경우에만 인식되어,
  청구서의 아무 13자리 숫자나 표시되지 않도록 합니다. 그 전까지는
  루마니아 문서에서 우편번호가 마스킹된 지명 옆에 읽을 수 있는
  상태로 남아 있었습니다.
- **편집기의 "페이지 래스터화".** PDF에서 텍스트를 제거할 수 없는
  경우 – 외부 생성 프로그램의 파일에서 발생합니다 – 이제 원하는
  경우 페이지를 그 이미지로 대체할 수 있습니다: 텍스트는 되돌릴 수
  없이 사라지고, 페이지는 읽을 수 있고 검색 가능한 상태로
  남습니다. 이 상황을 알리는 경고는 이 작업을 버튼으로 바로
  제공합니다 – "도구 → 페이지 래스터화"를 통해서도 직접 실행할 수
  있습니다. 실행 취소로 페이지를 되돌릴 수 있습니다.
- **화면이 이제 크로아티아어, 그리스어, 리투아니아어, 슬로베니아어,
  일본어, 한국어로도 제공됩니다.** 이로써 18개 언어가 되었습니다.
  설명서, FAQ, 법률 텍스트도 이 여섯 언어 모두에서 완전히
  제공됩니다. 정리된 문서 안의 라벨은 화면 언어를 따릅니다 –
  `[NAME_1]`이 `[IME_1]`, `[ΟΝΟΜΑ_1]`, `[VARDAS_1]`, 또는
  `[氏名_1]`이 됩니다. **그리스어, 일본어, 한국어에서는 라벨이
  라틴 문자로 표시됩니다** – `[ONOMA_1]`, `[SHIMEI_1]`,
  `[IREUM_1]`. 화면은 각자의 고유 문자로 남고, 문서에 기록되는
  것만 라틴 문자입니다. 이유는 PDF 문자셋에 있습니다 – 그리스어와
  일본어 라벨은 이전에는 `[??_1]`로 표시되어, 이름과 지명을 구분할
  수 없게 되었습니다.
- **9개 국가가 추가되었고, 기존 7개 국가는 완전해졌습니다.** 새로
  인식되는 것은 **크로아티아, 슬로베니아, 그리스, 리투아니아,
  북마케도니아, 러시아, 우크라이나, 중국, 일본**의 신분증, 세금,
  사회보험 번호와 주소입니다. 기존 국가에서는 더 심각했던
  공백이 메워졌습니다 – **네덜란드**와 **포르투갈**에는 지금까지
  개인 번호가 전혀 없었습니다 – 네덜란드의 BSN과 포르투갈의
  NIF가 두 나라의 거의 모든 서류에 등장함에도 인식되지 않았습니다.
  폴란드는 세금번호 NIP를, 덴마크·노르웨이·핀란드는 각자의 주소를,
  캐나다는 우편번호를 얻었습니다. 이로써 **35개 국가**가 되었습니다.

### 제거됨

- **Linux용 패키지는 당분간 다시 제공되지 않습니다.** 소스코드는
  그곳에서도 작동하지만, 이 안내서가 약속하는 세 가지가 Linux에는
  없습니다 – 자동 시작, 전역 단축키, 그리고 작업 환경에 따라
  트레이 아이콘까지. 설명된 것보다 할 수 있는 게 적은 패키지를
  배포하는 것은 옳은 방법이 아닙니다. Windows와 macOS는 영향을
  받지 않습니다.

### 개선됨

- **문서번호가 이제 모든 언어에서 발견됩니다.** "Aktenzeichen
  12/2026-AB"는 제거되었지만, "File reference 12/2026-AB"나
  "Sygnatura 12/2026-AB"는 그대로 남았습니다 – Maskuro가 이런
  번호를 인식하는 필드 단어가 독일어에만 있었기 때문입니다. 이제는
  12개 언어의 대응어를 인식합니다 – 지금까지와 마찬가지로 번호만
  교체되고 그 앞의 라벨은 그대로 남아, 결과물에서 무엇이 제거되었는지
  알아볼 수 있습니다.
- **대기 상태의 Maskuro는 이제 약 0.5기가바이트 더 적게 사용합니다.**
  지금까지는 시작할 때 정밀 인식의 추가 모델도 함께 로드되어, 첫
  정리 작업이 지연되지 않도록 했습니다. 측정 결과 이는 648MB의
  메모리를 소비하고 1.9초를 절약했습니다 – 그리고 창을 열었다가
  다시 닫기만 해도 그 비용이 들었습니다. 이제 이 모델은 실제로
  필요할 때 처음으로 로드됩니다 – 상태 표시줄이 이를 알립니다.
  언어 모델은 계속 시작 시 로드됩니다 – 클립보드 감시가 즉시
  필요로 하기 때문입니다.
- **드롭 영역은 이제 마우스 없이도 조작할 수 있습니다.** "파일을
  여기로 드래그하세요"는 클릭에만 반응하는 영역이었습니다 –
  키보드로는 접근할 수 없었고, 화면 낭독기는 이를 텍스트가 담긴
  틀로만 읽었을 뿐, 실제 그것이 무엇인지는 읽지 않았습니다. 이제는
  버튼입니다 – 탭 키로 이동할 수 있고, 스페이스바와 엔터 키로 파일
  선택 창을 열 수 있으며, 포커스가 있으면 테두리로 확인할 수
  있습니다. "파일 → 파일 선택"이라는 메뉴로는 이전에도 가능했지만,
  그것을 알아야만 했습니다.
- **정리된 파일의 이름도 이제 읽어줍니다.** 파일 목록에서는 원본
  아래의 두 번째 작은 줄로 표시되지만, 지금까지는 시각적으로만
  그려져 있었고 화면 낭독기는 원본만 읽었습니다. 바로 이 줄이야말로
  "폴더에 손대지 않은 원본이 있으니 실행이 아무 효과가 없었을
  것이다"라는 오해를 막기 위한 것입니다. 이제는 이 줄이
  "rechnung.pdf, 결과: rechnung_bereinigt.pdf"로 읽힙니다.
- **라벨 없는 조작 요소가 이제 자신의 용도를 알려줍니다.** 파일
  목록의 아이콘 버튼, 후속 수정 창의 그리기 버튼, 그리고 모든 선택
  및 입력 필드는 화면 낭독기에 이름이 없었습니다 – "버튼", "콤보
  상자"라고만 안내되고 무엇에 대한 것인지는 없었습니다. 한 줄에
  있는 버튼들은 이제 파일 이름도 함께 언급합니다 – 20개 항목이
  있는 목록에서는 이전에는 같은 문장을 스무 번 들어야 했습니다.
- **키보드로 조작하는 사람은 이제 다시 자신의 위치를 볼 수
  있습니다.** "정리" 버튼과 파일 목록의 아이콘 버튼은 색이 있게
  디자인되어 있어, 시스템이 보통 포커스 있는 요소 주위에 그리는
  테두리가 사라졌습니다 – 탭으로 이동할 때 시선이 갈 곳을
  잃었습니다. 이제 둘 다 차례가 되면 자체 테두리를 갖습니다. 버튼의
  크기는 이때 바뀌지 않습니다.
- **일곱 개의 글자 색이 두 화면 모양 모두에서 너무 옅었습니다.**
  일반적인 기준(WCAG 2.1)으로 다시 측정한 결과, 옅은 안내 줄,
  드롭 영역의 보조 텍스트, 안내서의 항목들, 그리고 어두운
  화면에서는 파랑과 빨강까지 4.5:1 기준 아래에 있었습니다 – 밝은
  조명과 좋은 시력에서는 읽을 수 있지만 그렇지 않으면 어렵습니다.
  모두 밝기가 조정되었습니다 – 단계는 그대로 유지되어, 텍스트는
  여전히 보조 텍스트로 읽힙니다. 나머지 세 개 – 경고와 성공을
  알리는 색 – 는 기준을 아슬아슬하게 지키고 있었지만 함께
  조정되었습니다 – 이를 읽지 못하는 사람은 무언가 잘못되었다는
  정보 자체를 읽지 못하게 되기 때문입니다. 눈에 띄게 바뀐 것은
  어두운 화면의 "정리" 버튼뿐입니다 – Windows 11의 강조 버튼과
  마찬가지로 이제 흰색 대신 어두운 색 글자를 사용합니다.
- **파일 목록의 각 줄에 이제 자체 X 표시가 있습니다.** 지금까지는
  줄을 먼저 선택한 뒤 "제거"를 클릭해야 했습니다 – 사소한 일에
  두 단계가 필요했습니다. X 표시는 줄 오른쪽에 있고 한 번의 클릭만
  필요합니다. 아래에 있던 "제거" 버튼은 이로써 사라졌습니다 –
  여러 줄을 한 번에 없애고 싶은 사람은 선택한 뒤 컨텍스트 메뉴의
  항목을 사용합니다. 이 항목은 몇 개인지도 알려줍니다. "모두
  제거"는 그대로 남습니다. 목록에서 제거되는 것은 항상 줄뿐입니다 –
  디스크의 파일이 삭제되는 일은 없습니다.
- **AI 검사 전에 이제 이 컴퓨터가 적합한지 표시됩니다.** 지금까지는
  창에 모델의 크기만 표시되었습니다. 성능이 약한 컴퓨터에서 이를
  켠 사람은 첫 문서에서야 – 5.4GB를 내려받은 뒤에야 – 시간이 매우
  오래 걸린다는 것을 알게 되었습니다. 이제 창은 **사전에** 메모리와
  여유 공간을 알려주고 그것이 무엇을 의미하는지 설명합니다 –
  **사후에는** 속도가 측정되어 관련된 크기로 표시됩니다: "이
  컴퓨터에서 10페이지 문서는 약 12분이 걸립니다." 너무 느리면
  프로그램은 이를 만류하고 이 단계를 다시 끄도록 제안합니다 –
  금지하지는 않습니다.
- **속도 측정이 이제 모든 컴퓨터에서 실행됩니다.** 지금까지는
  그래픽 가속을 추가로 설정한 경우에만 실행되었는데 – 이는
  Windows에서만 가능합니다. 다른 모든 컴퓨터에서는 프로그램이
  다른 컴퓨터를 기준으로 소요 시간을 추정했고, 하필 속도가 느린
  곳에서 추정이 빗나갔습니다.
- **터키어 주소가 스캔에서도 발견됩니다.** 스캔한 편지지 머리글에는
  "34710 İstanbul"이 읽을 수 있게 남아 있었지만, 그 옆의 텍스트에서
  같은 정보는 사라졌습니다 – 텍스트 인식은 터키어 İ를 점 없이
  읽는데, 패턴은 대문자를 기대했습니다. "Bağdat Caddesi"도
  마찬가지였습니다.
- **자체 도로명이 없는 스페인어 주소가 발견됩니다.** "Gran Vía
  5"는 그대로 남아 있었는데, 패턴이 도로 유형 뒤에 이름 단어를
  더 기대했기 때문입니다 – "Calle Mayor"에는 이름이 있지만, "Gran
  Vía"에서는 유형 자체가 이미 이름입니다. 이제는 "La Rambla"와
  "Castellana"도 마찬가지로 적용됩니다.
- **"이 프로그램 정보" 창에 이제 투명성 안내가 표시됩니다** – 이
  애플리케이션이 인공지능의 지원을 받아 개발되었다는 내용입니다.
  이는 프로그램의 개발 과정에 관한 것이지 작동 방식에 관한 것이
  아닙니다 – 정리 작업은 계속해서 오직 본인의 컴퓨터에서만
  이루어집니다.
- **"언어 관리"가 이제 사용 가능한 언어를 먼저 보여줍니다.** 48개
  문서 언어 중 절반에는 자체 언어 모델이 없습니다 – 그곳에서는
  다국어 대체 모델이 이름을 약하게만, 일부 문자에서는 전혀
  인식하지 못합니다. 목록에서 나란히 있으면 모두 동등해 보였습니다.
  기본값은 이제 자체 모델이 있는 언어만 보여줍니다 – "표시됨"을
  통해 나머지 언어를 언제든지 표시할 수 있으며, 그것이 무엇을
  할 수 있고 할 수 없는지 설명하는 문장도 함께 나타납니다.
  아무것도 사라지지 않으며, 제한된 언어를 설정해 둔 사람은
  그대로 유지합니다.
- **빠진 언어에 대한 질문이 이제 해결책을 알려줍니다.** 아직
  설정되지 않은 언어가 인식되면, 지금까지는 "로드" 또는 "그냥
  진행"만 제공했습니다. 하지만 인식이 틀릴 수도 있습니다 – 짧은
  양식과 본문이 적은 목록에서는 몇 단어만으로 결정됩니다. 그래서
  이제는 창에 "자동 인식"을 쓰는 대신 취소하고 올바른 언어를
  직접 선택할 수 있다는 안내가 표시됩니다. 이렇게 하면 전혀
  필요 없는 언어를 위해 수백 메가바이트를 내려받는 일을 막을 수
  있습니다.
- **자리표시자 라벨이 이제 화면 언어를 사용합니다.** "[NAME_1]",
  "[ADRESSE_2]" 등은 지금까지 설정된 언어나 문서의 작성 언어와
  관계없이 항상 독일어였습니다. 이제는 화면 언어를 따릅니다 –
  영어라면 "[NAME_1]", "[ADDRESS_2]"가 됩니다. 문서 언어는
  아닙니다 – 이는 "자동 인식" 시 추측이며 때로는 틀립니다. 화면
  언어는 절대 틀리지 않습니다.
- **후속 수정 시 확인 질문이 줄었습니다.** 결과물이 어디에
  저장되는지는 이제 아래쪽 표시줄에 계속 표시됩니다("→
  vertrag_bereinigt.pdf", 툴팁에 폴더) – 이를 클릭하면 바로 저장하지
  않고 다른 위치를 선택할 수 있습니다. 첫 저장 시의 확인 질문은
  이로써 사라졌습니다. "이미 편집됨 – 새로 시작할까요?"라는 질문은
  세션 동안 기억할 수 있으며, 정보만 전달하던 두 개의 안내 창은
  이제 상태 표시줄에 표시됩니다. 남아 있는 질문은 되돌릴 수 없는
  손해를 막는 것들입니다 – 닫을 때의 저장되지 않은 작업과 제거되지
  않은 텍스트에 대한 경고입니다.
- **결과물은 이제 스캔 자체를 읽을 수 없었던 부분도 알려줍니다.**
  스캔한 문서에서는 기기의 텍스트 인식이 모든 것을 정확히 읽지는
  못합니다 – "Solarstraße 9"가 "Solaret^aß« B"가 되는 식입니다.
  이렇게 잘못 읽힌 것은 어떤 검사도 더 이상 찾을 수 없습니다 –
  모든 검색 패턴에 그저 알파벳 뒤죽박죽으로 보입니다. 프로그램은
  이를 바꿀 수는 없지만, 이제는 이런 위치를 페이지 번호와 함께
  알려줍니다 – 대개 도장, 편지지 머리글, 손으로 적은 추가 사항이
  있는 곳입니다. 이는 경고가 아니라 안내입니다 – 조판된 문서에서는
  나타나지 않습니다.
- **파일 목록이 이제 결과물의 이름을 보여줍니다.** 실행 후 파일
  이름 아래에는 정리된 파일의 이름이 표시됩니다("→
  vertrag_bereinigt.pdf"). 지금까지는 "세부 정보" 뒤의 로그에만
  있었고, 폴더를 확인한 사람은 손대지 않은 원본만 보았습니다.
  원본 이름은 그대로 남습니다 – 그렇지 않으면 결과물이 어느
  파일에서 왔는지 알 수 없게 됩니다.
- **완료된 줄의 버튼이 더 크고 명확해졌습니다.** 보기, 후속 수정,
  "폴더에서 보기"는 면적이 없는 평평한 아이콘이어서 목록 안에서
  묻혔습니다 – 하지만 실행 후에는 이것들이야말로 유일하게 클릭할
  것입니다.

### 수정됨

- **외국어 화면에서는 마스킹, 대체, 해시 처리에 대한 사용자 지정
  규칙이 조용히 무시되었습니다.** 이름을 대체 대신 마스킹하도록
  지정한 사람도, 프로그램이 독일어나 영어로 작동하지 않는 순간
  결국 대체된 결과를 받았습니다. 설정은 표시되어 있었지만 작동하지
  않았고, 결과에서는 그 차이를 볼 수 없었습니다. 12개 화면 언어
  중 9개가 영향을 받았습니다.
- **"라벨 언어" 설정은 독일어와 영어 외에는 아무 효과가
  없었습니다.** "독일어"와 "영어"는 선택할 수 있었지만, 문서에는
  계속 화면 언어가 표시되었습니다. 이제는 세 가지 선택지 모두
  작동합니다 – "화면과 같음"이라는 기본값은 변함없이 지금까지와
  같은 결과를 냅니다.
- **짧은 텍스트 발췌 – 예를 들어 복사된 메일 인용문 – 에서는
  이름이 그대로 남았습니다.** 클립보드를 통해 발췌문을 정리한
  사람은 대개 이메일 주소만 마스킹되고 그 아래 이름은 그렇지
  않은 결과를 받았습니다. 결정적인 것은 단순한 줄 수였습니다 –
  6줄부터는 프로그램이 발췌문을 목록으로 인식해 그 안의 이름을
  찾았고, 그 이하에서는 찾지 못했습니다 – 복사된 메일 인용문은
  5줄입니다. 제목 등 임의의 추가 줄 하나만으로 결과가 뒤집혔습니다.
  이제는 4줄이면 충분하며, 측정에서 검사된 이름의 3분의 1만이
  아니라 전부가 사라집니다. 더 긴 문서와 본문에는 영향이 없습니다.
- **AI 검사의 그래픽 가속은 설정한 뒤에도 다시 꺼졌습니다.**
  설정 후 프로그램은 이 컴퓨터에서 그래픽이 프로세서보다 실제로
  더 빠른지 측정합니다 – 이 측정은 그러나 알림 없이 항상
  실패했고, "둘 다 같은 속도"라는 결과가 프로세서를 선택하도록
  결정했습니다. 65MB를 내려받은 사람은 그 이후 이전보다 더
  낮은 성능을 얻었습니다. 이제 측정이 정상적으로 실행됩니다 –
  실패하더라도 더 이상 아무것도 바꾸지 않습니다.
- **시간 예측은 모든 컴퓨터에서 다른 컴퓨터의 속도로
  계산되었습니다.** 이는 같은 측정에 의존하는데, 그 측정이
  실행되지 않는 한 개발용 컴퓨터의 값이 적용되었습니다. "약 2분"이
  느린 컴퓨터에서는 30분을 의미할 수도 있었습니다.
- **AI 단계는 새롭고 훨씬 더 나은 언어 모델을 사용합니다**
  (Qwen3-4B 대신 Qwen3.5-9B), 그리고 더 이상 독일어와 영어에
  국한되지 않고 12개 언어로 작동합니다. 검증 코퍼스로 측정한
  결과: 이 단계 없이 찾은 정보와 같은 수를 찾지만, 불필요한
  마스킹은 절반 미만으로 줄었습니다(75 → 31). 모델은 더
  커졌고(2.4GB 대신 5.4GB) 계산 시간은 약 두 배가 필요합니다 –
  켤 때 한 번 로드되며 이때 이전 모델은 삭제됩니다.
- **프랑스어, 이탈리아어, 스페인어, 포르투갈어, 폴란드어,
  터키어, 스웨덴어 주소가 이제 완전히 제거됩니다.** 지금까지는
  도로명과 지명만 사라졌습니다 – 번지수와 우편번호는 읽을 수
  있는 상태로 남았습니다("[ORT_1] 28, 28013 [ORT_2]"). 이 언어들에는
  자체 주소 패턴이 없었는데, 이제 추가되었습니다.
- **그리스어와 한국어는 이름을 전혀 찾지 못했습니다.** 그리스어는
  대체 모델 때문이었습니다 – 이제 로드할 수 있는 자체 모델로
  이름과 지명이 깔끔하게 인식됩니다. 한국어는 프로그램 때문이었습니다:
  이름이 대문자로 시작한다고 가정했는데, 한글에는 대문자가
  없습니다. 특히 짧은 단위 – 표 셀, 양식 필드, 목록 항목 – 가
  영향을 받았습니다.
- **로드할 수 없는 언어 모델이 정리 작업을 중단시켰습니다.** 오류
  메시지 대신 이제는 다국어 모델이 대신 사용되며, 결과물에는
  더 약한 인식으로 작업했다는 안내가 표시됩니다. 현재는 단어
  분리가 필요한데 프로그램에 아직 포함되지 않은 중국어와 일본어에
  해당됩니다.
- **자체 모델이 있는 언어도, 다른 아무 언어가 로드되어 있으면
  설치된 것으로 간주되었습니다.** 예를 들어 터키어를 설정한
  사람은 다국어 대체 모델을 함께 받았습니다 – 그러면 중국어, 일본어,
  한국어, 그리스어는 체크가 되고 "0MB"로 목록에 표시되었지만,
  실제 자체 모델은 없었습니다. 이 때문에 다시 내려받을 수 없었고,
  계속 더 약한 대체 모델로 작동했습니다. 이제 목록은 실제 상태와
  다운로드 용량을 함께 보여줍니다.
- **오류가 발생한 인식 단계는 침묵했습니다.** "고급 인식"이나
  "최대 인식 (AI)"이 켜져 있었지만 모델을 실행할 수 없었던 경우,
  프로그램은 아무 말 없이 그 단계 없이 계속 작동했습니다. 결과물은
  다른 모든 결과물처럼 보였고, 스위치는 계속 "켜짐"으로 표시되어
  있었습니다 – 그래서 기본 단계의 결과를 얻을 수 있는 최선의
  것으로 여기게 되었습니다. 이제 결과물은 이를 알리고 둘 다
  언급합니다 – 무엇이 검사되지 않았는지, 그리고 모델을 어떻게
  다시 로드할 수 있는지. 이 경우는 드물지 않습니다 – 일부
  컴퓨터에서는 그래픽 가속이 없으면 AI 단계가 로드에 실패합니다.
- **추가 모델 로드 오류가 전체 정리 작업을 중단시켰습니다.** "고급
  인식"에서는 모델의 평가만 보호되었고 읽어들이는 과정은 아니었습니다
  – 파일이 손상되었거나 컴퓨터와 맞지 않을 때 문제가 발생하는
  곳이 바로 그곳입니다. 오류 메시지 대신 이제는 기본 단계의
  결과물과 안내가 함께 제공됩니다.
- **언어를 더 이상 제거할 수 없었고 – 그래서 다시 로드할 수도
  없었습니다.** "언어 관리"에서 체크를 없애고 변경 사항을 적용한
  사람은 "독일어 제거됨"이라는 문구를 보았지만 체크는 곧바로 다시
  표시되었습니다. 원인은 프로그램 폴더에서의 복사였습니다 – 모든
  사용자용 설치에서는 언어 모델이 쓰기 금지된 프로그램 폴더에
  있고, 프로그램은 빠진 것을 그곳에서 가져오지, 수백 메가바이트를
  새로 내려받지 않습니다. 이 복사 작업은 접근할 때마다 함께
  실행되었고 – 방금 삭제한 언어를 그 즉시 다시 복사해 넣었습니다.
  이제는 한 번만 실행됩니다 – 나중에 내려받은 언어 모델은 이때
  그대로 유지됩니다. 또한 프로그램은 삭제 후 확인합니다 – 제거되지
  않은 것은 이제 "제거됨" 대신 실패로 보고됩니다.
- **모든 사용자용 설치에서는 추가로 내려받은 것을 저장할 수
  없었습니다.** 모든 사용자를 위해 프로그램을 설치한 사람은
  "Program Files"에 프로그램이 있으며, 관리자 권한 없이는 그곳에
  아무것도 쓸 수 없습니다. 언어 모델에는 이미 오래전부터 대체
  위치가 마련되어 있었지만, 다른 것들에는 없었습니다:
  - **페이지 보기 구성 요소**는 290MB를 내려받은 뒤 프로그램
    폴더에 압축을 풀려 했고, 이유를 알리지 않은 채 실패했습니다.
    이제는 원래 의도대로 언어 모델이 있는 곳에 함께 저장됩니다.
  - **그래픽 가속**은 대체 위치로 옮길 수 없습니다 – 프로그램
    자체 안의 라이브러리를 교체하기 때문입니다. 먼저 로드를
    시도한 뒤 말없이 실패하는 대신, 이제 프로그램은 사전에 여기서는
    불가능하다는 것과 그것이 무엇을 의미하는지 알립니다 – 최대
    인식은 계속 작동하되 프로세서로만 처리됩니다.
  - 동봉된 **텍스트 인식 언어**는 제거할 수 없었습니다 – 프로그램
    폴더에서 즉시 다시 복원되었기 때문입니다. 언어 모델과 같은
    원인, 같은 해결책입니다.
  - 언어를 제거할 때 **다른 Tesseract 설치의 언어 데이터**가
    삭제될 수 있었습니다. 이제는 자체 폴더만 건드립니다.
  - 대체 위치는 지금까지 Windows에서만 적용되었습니다. `/opt`로
    가는 Linux 아카이브는 같은 문제를 겪었지만 같은 해결책이
    없었습니다.
- **후속 수정 시 단어 하나만 프레임으로 지정했는데도 줄 전체가
  사라졌습니다.** 이미 정리된 파일에서 자리표시자를 마스킹한
  사람은 그 자리표시자가 있던 줄 전체를 잃었습니다 – "Sehr geehrte
  Frau Doktor [NAME_1]"에서 아무것도 남지 않았습니다 – 메시지에는
  "문서에서 단어 하나 제거됨"이라고만 표시되었습니다. 이미 한 번
  프로그램을 거친 모든 파일이 영향을 받았습니다 – 즉 후속 수정
  기능이 존재하는 바로 그런 경우입니다. 나머지 텍스트는 이제
  변경되지 않은 위치에 그대로 남습니다.
- **명단 위의 "EMPLOYEES"가 그 자체로 마스킹되었습니다.** 0.10.19의
  "MITARBEITER"와 같은 경우이며, 다만 영어였고 – 그때는 남아
  있었습니다. 대문자로만 쓰인 경우 언어 모델은 판별 근거를 잃고,
  이 제목은 실제 이름들이 줄지어 있는 위에 놓여 있습니다. 그 아래의
  이름들은 계속 발견됩니다. "staff"는 목록에 추가되지 않았습니다 –
  이는 실제로 쓰이는 성이며, 이를 추가하면 모든 "John Staff"가
  함께 처리될 것입니다 – 예전 "Arbeiter"에서와 같은 판단입니다.
- **법인 형태가 두 번 교체되었습니다.** 스캔한 편지지 머리글에서
  언어 모델은 "GmbH", 주소, 우편번호를 **하나의** 지명으로
  읽었습니다. 주소와 우편번호는 그 뒤 각자 자기 부분을 잘라냈고,
  남은 법인 형태가 별도의 검출로 남았습니다 – 결과물에는 "[ORT_1]
  GmbH"를 의미했지만 "[ORT_1] [ORT_2]"로 표시되었습니다. 회사
  이름은 계속 대체되지만 – 이제는 단독의 부가 표기만 그대로 남아,
  결과물이 빈칸 채우기 연습이 아니라 편지지 머리글처럼 읽힙니다.
- **잘려나간 검출은 다시 검사되지 않았습니다.** 위의 사례와 같은
  원인이며, 더 넓게 퍼져 있습니다 – 추측성 검출을 걸러내는 필터는
  인식기가 **보고하는** 것을 기준으로 작동했지, 겹침 해소 이후
  남은 결과를 기준으로 하지 않았습니다. 긴 검출이 더 강한
  인식기에 의해 잘려나가면, 그 조각은 평가된 것과는 다른 텍스트가
  되지만 아무도 다시 살펴보지 않았습니다. 이제는 다시 살펴봅니다.
- **"최신 버전을 사용 중입니다" – 확인 자체가 불가능했음에도
  표시되었습니다.** 업데이트 채널을 "미리보기(베타)" 또는 "안정
  – 권장"으로 설정한 사람은 이 안내를 받았지만, 사실 이 채널에는
  지금까지 아무것도 출시된 적이 없었습니다. 이제는 프로그램이
  정확히 그 사실을 알리고, 설정에서 다른 채널을 선택하도록
  제안합니다.
- **로딩 중 창을 닫으면 스레드가 충돌했습니다.** Maskuro를 시작한
  후 언어 모델이 아직 로딩 중인 동안 바로 창을 닫은 사람은 로그에
  오류 보고서를 받았습니다 – 로딩 과정이 더 이상 존재하지 않는
  창에 신호를 보냈습니다. 눈에 보이는 영향은 없었지만, 로그에는
  단지 누군가가 프로그램보다 빨랐을 뿐인데도 충돌로 기록되었습니다.
- **결과물은 이제 읽히는 것이 아니라 실제로 보여지는지 확인됩니다.**
  지금까지는 값이 텍스트에 더 이상 없으면 페이지가 깨끗한 것으로
  간주되었습니다. 스캔에서는 이것이 증명이 되지 않습니다 – 그곳의
  보이는 텍스트는 이미지이기 때문입니다. 그래서 마지막에는 결과물의
  영역이 실제로 마스킹되었는지 확인됩니다 – 그곳에 여전히 밝은
  종이가 있으면 보고서는 "교체됨" 대신 명확히 그 사실을 알립니다.
- **교체된 정보가 이미지에 그대로 남아 있었습니다.** 값이 이미지
  위에 있었던 경우 – 스캔한 편지지 머리글, 도장, 스캔된 페이지
  전체 – 문서 텍스트에서는 제거되었지만 여전히 **보였습니다**:
  사람이 읽는 것은 그곳의 픽셀입니다. 보고서는 그럼에도 "교체됨"이라고
  보고했습니다. 이제는 어떤 전략이 설정되어 있든 이미지의 영역이
  마스킹되며, 자리표시자는 그 배경 위에 밝게 표시됩니다 – 보기에는
  좋지 않지만 정직하며, 대응 관계는 유지됩니다. 이미지 형식을
  편집할 수 없는 경우 결과물은 이제 이를 명확히 알립니다 – 깨끗해
  보이는 대신입니다.
- **스캔에서는 자리표시자가 완전히 빠져 있었습니다.** 스캔된
  페이지의 텍스트 레이어는 보이지 않게 그려지며, 그 안에 삽입된
  자리표시자도 이를 물려받았습니다 – 설정은 되었지만 보이지
  않았습니다. 발견 위치에는 그 이후 아무것도 표시되지 않았습니다.
- **전혀 실행될 수 없었던 텍스트 인식이 성공한 것으로 간주되었습니다.**
  언어 파일이 없거나 인식 엔진이 중단된 경우, 보고서에는 "이미지가
  텍스트 인식으로 검사됨(발견 위치 0건)"이라고 표시되었습니다 –
  즉 실제로 이루어지지 않은 검사였습니다. 스캔에서는 이것이
  유일한 검사입니다 – 페이지 이미지에 읽을 수 있는 주소가 있는
  계약서도 완료된 것으로 간주되었습니다. 이제는 보고서가 아무것도
  검사되지 않았다는 사실과 그 이유를 알립니다.
- **언어 파일을 잘못된 폴더에서 찾았습니다.** 자체 언어 폴더에
  문서의 언어가 아닌 다른 언어가 있으면, 인식 엔진에 바로 이
  폴더가 지정되어 실패했습니다 – 알맞은 언어가 바로 옆에 있었음에도
  그랬습니다. 이제는 **언어**를 찾지 폴더를 찾지 않습니다.
- **제거되지 않은 텍스트에 대한 경고가 존재하지 않는 기능을
  권했습니다.** 경고는 "PDF로 마스킹"을 가리켰지만 – 이는
  *오피스* 파일의 PDF 보기를 생성하는 기능이며 PDF 자체에는 전혀
  제공되지 않습니다. 경고를 따르려던 사람은 헛수고를 했습니다.
  이제는 실제로 문제를 해결하는 버튼이 표시됩니다.
- **편집기에서 막대와 자리표시자가 표시된 위치와 어긋났습니다.**
  줄이 하이픈으로 끝나고 단어가 다음 줄에서 이어지는 모든 PDF에서
  발생했습니다 – 계약서 텍스트가 계속 나뉘어 조판되는 스캔에서
  특히 눈에 띕니다. 두 줄 반쪽이 하나의 단어로 간주되어 본문
  전체에 걸쳐 있었고, 그 근처의 모든 프레임이 이 확장 범위를
  물려받았습니다. 인식 자체는 이로 인해 바뀌지 않습니다 – 측정
  코퍼스는 이전과 같은 결과를 냅니다.
- **편집기는 텍스트가 제거되었는데도 "여전히 문서에 남아
  있습니다"라고 경고했습니다.** 같은 단어가 페이지에 여러 번
  나오면 – 계약서에서는 일반적입니다 – 자체 검사는 조작할 때마다
  실패로 보고했습니다. 이제는 단어가 어디든 남아 있는지만 확인하지
  않고 등장 횟수를 셉니다. 실제로 실패한 경우에는 변함없이
  경고합니다.
- **결과 파일 이름은 어느 언어에서든 "_bereinigt"였습니다.** 원래
  의도는 이 접미사가 화면 언어를 따르는 것이었습니다 – 영어에서는
  실제로 그랬지만("_cleaned"), 나머지 16개 언어에서는 그렇지
  않았습니다. 핀란드어로 프로그램을 사용한 사람은
  "asiakirja_bereinigt.pdf"를 받았습니다. 이제 파일은
  "asiakirja_puhdistettu.pdf"가 되고, 일본어에서는
  "書類_除去済み.pdf"가 되는 식입니다 – 각 화면이 완료 안내에서
  사용하는 단어를 그대로 씁니다. 사용자 지정 접미사를 설정한
  사람은 그것을 그대로 유지합니다.
- **"언어 관리"는 항상 독일어로 표시되었습니다.** 48개 문서
  언어 목록에는 어떤 화면이 설정되어 있든 독일어 이름이 표시되었습니다
  – 핀란드어 사용자는 "Chinesisch"라고 읽었습니다. 이제는 그
  언어로 이름이 표시되고 그 뒤에 고유 이름이 함께 표시됩니다 –
  "Kiina (中文)". 고유 이름을 함께 표시하는 것은 의도적입니다 –
  자신의 언어 이름으로 알아보는 사람은 핀란드어 단어가 무슨 뜻인지
  몰라도 그 언어를 찾을 수 있습니다.

## 0.10.19 – 2026년 8월 12일

### 개선됨

- **컨텍스트 메뉴 항목이 이제 사용 중인 언어로 표시됩니다.** 지금까지는
  시스템과 상관없이 항상 독일어 문구가 표시되었습니다 – 영어 Windows에서도
  마찬가지였습니다. 이제는 설정된 화면 언어를 따르며, 언어를 바꾸면 새로
  설치하지 않아도 항목 이름이 즉시 바뀝니다. (Windows에 해당; macOS와
  Linux에서는 메뉴 이름이 곧 파일 이름이기도 해서 – 이 부분은 나중에
  제공됩니다.)
- **편집기가 마지막으로 작업한 보기 방식을 기억합니다.** 페이지 보기를 사용하는
  사람은 다음 문서를 열 때 매번 다시 켜지 않아도 자동으로 그 보기로
  열립니다. 한 번도 사용한 적이 없는 사람은 아무런 차이를 느끼지 못합니다 –
  이 기능에 필요한 구성 요소가 이미 로드되어 있을 때만 복원되며, 이를 위해
  별도로 무언가를 새로 불러오지는 않습니다.

### 수정됨

- **명단 위의 "직원"이라는 글자 자체가 마스킹되었습니다.** 직원 명부와
  조직도에서 이 제목이 이름으로 오인되어 사라지곤 했습니다 – 실제 이름들이
  줄지어 있는 바로 위에 놓여 있고, 대문자로만 쓰인 언어 모델에는 구분할
  근거가 없기 때문입니다. 그 아래의 이름들은 계속해서 정상적으로
  탐지됩니다.
- **수량 표기가 주소로 오인되었습니다.** 청구서, 납품서, 재고 목록에서
  "3390 Protokoll", "1030 Betrag", "3390 Lager" 같은 표기가 우편번호와
  지명으로 오인되어 사라지곤 했습니다 – 네 자리 숫자는 무엇이든
  오스트리아 우편번호처럼 보입니다. 숫자 뒤에 애플리케이션이 사물,
  부서, 업무, 필드 항목명으로 인식하는 단어가 오면 이제 그대로
  남습니다. 실제 지명은 영향을 받지 않으며, 그런 단어와 동시에
  지명이기도 한 경우("4692 Ort")도 마찬가지입니다. 다만 숫자 뒤에
  아주 평범한 단어가 오는 경우("3390 Regal")는 아직 해결되지 않았습니다 –
  이를 위해서는 우편번호 목록이 필요합니다.
- **도움말에 존재하지 않는 메뉴 항목이 언급되어 있었습니다.** 설명서와
  이미지, 설치 마지막에 뜨는 안내 메시지에는 "AI용 문서 정리"라고
  되어 있었지만, 컨텍스트 메뉴의 실제 항목 이름은 "개인정보 제거"입니다.
  도움말을 따라간 사람은 헛수고를 했을 것입니다. 이제 세 곳 모두 실제
  이름 그대로 메뉴 항목을 지칭합니다.
- **"시스템과 함께 시작"을 끌 수 없었습니다.** 설치할 때 "Windows와 함께
  시작"에 체크했던 사람은 설정에서 여전히 체크가 비어 있는 것을
  보게 되었고 – 더 심각하게는, 애플리케이션 안에서 켜고 끄는 것이
  아무 효과가 없어 프로그램은 계속 Windows와 함께 시작되었습니다. 원인은
  Windows가 시작 프로그램을 확인하는 위치가 두 곳이라는 데 있었습니다 –
  애플리케이션은 그중 한 곳만 알고 있었습니다. 이제 두 곳 모두 반영되며,
  스위치는 실제 상태를 보여 주고 양쪽 방향 모두에 작동합니다. 또한
  작업 관리자에서 항목을 끈 경우도 이제 애플리케이션에 반영되며, 거기서
  다시 켜면 그 비활성화가 해제됩니다.
- **명단 위의 제목이 마스킹되었습니다.** "TEILNEHMERLISTE
  WERKSTATTGESPRÄCH" 또는 "MITARBEITERÜBERSICHT INNENDIENST"처럼 사람
  목록 위에 놓인 제목이 이름으로 오인되어 사라지곤 했습니다. 대문자로만
  쓰인 경우 언어 모델은 가장 좋은 판별 근거를 잃게 되고, 독일어에서는
  모든 명사가 대문자로 시작하므로 "Teilnehmerliste Werkstattgespräch"도
  "Anna Huber"처럼 보이게 됩니다. `-liste`, `-dienst`, `-gespräch`,
  `-sitzung`, `-besprechung`으로 끝나는 합성어는 이제 그대로 남습니다.
  기본 단어만 있는 경우는 여전히 이름으로 간주됩니다: *Liste*와 *Dienst*는
  실제로 쓰이는 성이지만, *Teilnehmerliste*는 그렇지 않습니다.
- **세로로 놓인 표기에 읽을 수 없는 자리표시자가 붙었습니다.** 페이지
  여백의 문서번호, 책등 옆의 담당자 약자, 세로로 놓인 표 머리글 등 –
  이런 표기는 탐지되어 제거되기는 했지만, 자리표시자가 텍스트를 가로질러
  나타나고 1~2포인트 크기로 눌린 채, 때로는 종이 경계를 벗어나기도
  했습니다. 이제는 텍스트를 따라 – 세로로, 읽을 수 있는 크기로, 원래
  표기가 있던 방향 그대로 – 나타납니다. 사후에 회전된 페이지(일부 출력
  프로그램이 만들어내는, 가로로 쓰였지만 페이지 회전 값이 기록된 경우)도
  마찬가지입니다 – 여기서도 자리표시자는 이제 화면에 보이는 그대로
  놓입니다. "Sehr geehrte Frau Doktor Anneliese Berger"에서는 "Anneliese"만
  이름으로 인식되었습니다 – "Berger"는 문서에 그대로 남았습니다. 미들
  네임이 있는 모든 이름도 마찬가지였습니다("Frau Anna Maria Berger").
  원인은 호칭 뒤 이름을 처리하는 규칙에 있었습니다 – 이 규칙은 두 개의
  단어 자리만 가지고 있었고, 직함이나 미들 네임이 첫 번째 자리를
  차지해 버렸습니다. "Dr."에서는 전혀 드러나지 않았는데, 마침표가 규칙을
  끊어 언어 모델이 이름 전체를 붙잡았기 때문입니다. 이제 직함은 자리를
  차지하지 않고 건너뛰며, 이름은 세 부분으로 이루어질 수 있습니다. 이름
  **뒤에** 오는 역할은 여전히 함께 처리되지 않습니다: "Frau Anna Huber
  Geschäftsführerin"에서는 이름만 대체되고, 역할은 그대로 남습니다.
